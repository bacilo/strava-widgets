# Requirements: Strava Analytics & Visualization Platform — v2.1 Interface Polish

**Defined:** 2026-08-12
**Core Value:** Compute and visualize running statistics that Strava doesn't readily offer, embeddable anywhere on a personal website.

## v2.1 Requirements

Refinement of shipped v2.0 capability, not new capability. The dashboard already does what the developer wants and some screens are genuinely good — this milestone brings the rest up to that standard. Requirements derive from a screen-by-screen walkthrough by the developer (2026-08-12) plus a source audit that identified root causes.

Each maps to roadmap phases.

### Design System

- [x] **UI-01**: Form controls (text, date, number, search inputs) are styled consistently across every screen — the stylesheet currently has **zero `input` rules**, so all inputs render as unstyled browser defaults. This is the single root cause of the "raw" feel on Activities and Calendar. **Re-verified 2026-08-13**: plan 19-09's browser checkpoint re-confirmed rows 1-2 clean after GAP 1 (dead `--radius-control` token) was closed by plan 19-06; the developer's blanket verdict "Everything looks good. Approved." covers row 1 alongside rows 3, 6 and 12. See `19-VALIDATION.md` Gap-Closure Record. **Round 3 confirmed-unregressed 2026-08-13**: row 19 sub-check (b) — input, select and textarea baselines across Activities and Calendar still render bordered, padded, 4px-cornered boxes with no unstyled browser default, per the developer's "all look good and unchanged" — confirms UI-01 was not broken by the CR-01/CR-02/CR-03 fixes in plans 19-10/19-11. See `19-VALIDATION.md` row 19. **Round 4 confirmed-unregressed 2026-08-13**: row 24 sub-check (b) — inputs across Activities and Calendar remain bordered, padded, 4px-cornered and none reads as a raw browser default, per the developer's verbatim "b. pas[s]" against this specific sub-check (not a blanket approval) — confirms UI-01 was not broken by this round's sticky-rung move or guard-layer rewrite. See `19-VALIDATION.md` row 24.
- [x] **UI-02**: Buttons, selects and other controls share one visual treatment, with a consistent `:focus-visible` ring meeting non-text contrast requirements in both themes — **re-verified 2026-08-13**: plan 19-09's browser checkpoint re-confirmed rows 3, 6 and 12 clean after GAP 1 (dead `--radius-control` token, plan 19-06) and GAP 2 (focus-ring paint-order occlusion, plan 19-07) were both closed; the developer's blanket verdict "Everything looks good. Approved." covers all four re-verified rows together. See `19-VALIDATION.md` Gap-Closure Record. **REOPENED 2026-08-13**: `19-REVIEW.md` (post-checkpoint) found three critical defects the browser checkpoint never exercised — CR-01 (`:focus-visible` `z-index: 1` paints over the `z-index`-less sticky `.app-nav`), CR-02 (the `button` baseline's radius rounds middle `.segmented__option`s in the three 3+-option Trends groups), CR-03 (`opacity: 0.6` on focusable `[aria-disabled="true"]` composites the ring to ~2.19:1 light / ~2.93:1 dark, under the 3:1 floor this requirement names). See `19-VERIFICATION.md`. **Round 3 2026-08-13 — still open**: CR-02 and CR-03 are confirmed closed on rendered evidence (rows 14, 15, 16 and 17 each independently PASS). CR-01's `z-index: 20` declaration is confirmed present in the shipped bundle (Probe B), but row 18 FAILED: the sticky nav does not remain on screen while scrolling on Activities (Probe D, `navH` 77 = `parentH` 77), a newly-found defect (`19-VALIDATION.md` GAP 7) that blocks this requirement independently of the z-index fix. **19-15 2026-08-13 — still open**: closed the guard-layer/test false-green findings from `19-REVIEW-round3.md` (R3-CR-01, R3-CR-02, R3-WR-02, R3-IN-01..04) so the test suite's own claims about the fix are now mutation-verified rather than merely present. This is test-file-only work — `git diff d9b3aaf HEAD -- src/dashboard/styles.css` is empty — and does not itself constitute the rendered re-verification this requirement needs. UI-02 stays open pending plan 19-16 (stylesheet comment fixes for R3-WR-03/WR-04) and plan 19-17's Probe G checkpoint (`autonomous: false`, the plan that actually owns this file's update) across both themes. **Round 4 2026-08-13 — CLOSED, clean sweep.** Plan 19-13 diagnosed GAP 7's root cause empirically (H1 — zero-travel containing block: `#app-nav-root`'s `clientHeight` equalled `.app-nav`'s `offsetHeight`, both 77, leaving the sticky rung zero travel distance), four competing hypotheses excluded by cited probe fields; plan 19-14 applied the fix (moved the sticky rung to `#app-nav-root`). This plan's checkpoint re-verified both GAP 7 and GAP 6 on rendered evidence: row 20 (nav holds on both routes, both themes; Probe G matched expectation on both routes) closes GAP 7, and row 21 (a focused control scrolled under the nav stays underneath it, both routes, both themes; Probe H confirmed the `z-index: 20` declaration live) closes GAP 6 — the first-ever observation of the original CR-01 paint-order question. Rows 22 and 23 also PASS (both themes). Combined with rows 3-6, 10-13 (clean since 19-05/19-09) and rows 14-17 (clean since Round 3), every row this requirement's own row→requirement map names is now PASS — a clean sweep of `19-VALIDATION.md` rows 20-24 is the only condition that ticks this box, and it is met. **Not blocked by GAP 8** (new, found unprompted this round: Leaflet map tiles paint over the nav on the detail-view map, plus a totality defect in plan 19-16's own ladder comment, which does not list Leaflet's panes as a fifth stacking rung) — reasoned explicitly rather than assumed: this requirement's own text concerns "buttons, selects and other controls" sharing one visual treatment with a conforming `:focus-visible` ring; GAP 8 concerns a third-party map library's own stacking values overlaying the nav on one specific view, an unrelated component this requirement's wording does not reach, and no row on this or any prior round's checkpoint agenda ever named the map against the nav. GAP 8 is tracked separately, unpatched, with its disposition (a small follow-up plan or a deferred item) left to the user after this phase closes. See `19-VALIDATION.md` GAP 6/GAP 7 resolutions and GAP 8.
- [x] **UI-03**: Spacing, density and card treatment follow one rhythm across all five screens, applied without changing the existing visual language. **Round 3 confirmed-unregressed 2026-08-13**: row 19 sub-check (c) — the card and panel rhythm on Overview and Records still reads as one rhythm, per the developer's "all look good and unchanged". See `19-VALIDATION.md` row 19. **Round 4 confirmed-unregressed 2026-08-13**: row 24 sub-check (c) — the card and panel rhythm on Overview and Records reads as it did, unchanged, per the developer's verbatim "c. pass" against this specific sub-check. See `19-VALIDATION.md` row 24.

### Interaction Consistency

- [x] **UX-01**: Any row representing an activity is clickable through to that activity, on every screen. `overview.ts` currently has zero click handlers; `list.ts` already has the pattern to propagate. **Round 3 gap-closure 2026-08-17**: plan 20-11's Round 3 checkpoint (`20-VALIDATION.md`) discharged nearly every mapped row on individually-described, per-row evidence — R1, R3, R11, R12, R14 all PASS, plus R5 and R13 carried forward from Round 2. R18 and R19 (D-12 modifier-click/window-open) were recorded FAIL: a modified click on the five anchor-less Records PR-table cells did not open a new tab/window, because those cells carried no anchor. **Round 4 gap-closure 2026-08-18**: plans 20-12 through 20-17 gave the five Records cells real anchors, closing R18/R19's original expectation: R22 PASS, R23 PASS, R24 PASS, R26 PASS. But this same round found two shipped defects among this requirement's own mapped rows: R31 FAIL (drag-selecting text inside a now-anchored cell starts a native link drag instead, D-13 defeating D-12) and R32 FAIL (double-clicking a value still navigates away, D-14's refusal not reaching D-13's anchors — see `20-VALIDATION.md` GAP 12). R25 was BLOCKED (Shift+click new-window half unverifiable by the agent). **Round 5 gap-closure 2026-08-18 — CLOSED, clean sweep.** Plan 20-19 shipped D-16 (per-anchor click guard mirroring the row predicate) and D-17 (per-cell accessible names); plan 20-20's Round 5 checkpoint re-tested GAP 12 against the fix: R34 PASS (drag-select captures text, confirmed by Cmd+C, no link-drag ghost), R35 PASS (the word selects on double-click; the residual first-click navigation is accepted by the developer's own explicit written disposition — "yes" — as shipped behaviour, not a defect), R36 PASS (modified click still opens a background tab on both Records tables, confirming D-16's `preventDefault()` was not fed the modifier gesture), R37 PASS (Shift+click opens a new window at `#/activity/7827165619`, confirmed by the developer's own eyes — closing the outstanding R25 half), R42 PASS (the `notedActivityId` focus-leak sequence still does not steal focus after the empty-filter/clear-filter sequence). Combined with the carried-forward passes (R22\*, R23\*, R24\*, R26\*, R1\*, R3\*, R5\*, R11\*, R12\*, R13\*, R14\*), every mapped row now passes. GAP 12 is fully closed; R35's residual is recorded as a developer-accepted limit of D-16, not an open defect. A related scope boundary (D-16 does not cover the Date-cell anchor, `records.ts:502-507`) was discovered during R36 corroboration and explicitly accepted by the developer as a deliberate, minor limitation — see `20-VALIDATION.md` Round 5, "NEW DISPOSITION". See `20-VALIDATION.md` Round 5.
- [x] **UX-02**: Redundant "view activity" call-to-action controls are removed where the row itself is the affordance (`list.ts:224`, Records). **Round 3 gap-closure 2026-08-17 — still open**: plan 20-11's Round 3 checkpoint closed one of this requirement's two remaining mapped rows cleanly — R4 PASS (mobile card layout confirmed one link, no CTA, both themes). R2 is recorded BLOCKED, not PASS: the CTA-absence and one-tab-stop-per-row halves of its claim are confirmed in both themes, but the row's own Required detail also demands a quoted badge text, and no Overview Recent Activities row in the current dataset carries a status badge to quote (a dataset-coverage gap, not a defect — see `20-VALIDATION.md` Round 3, GAP 10). Combined with R5\* and R6\* (carried forward from Round 2 as passes), three of this requirement's four mapped rows are clean passes, but per the plan's own gating rule ("tick a requirement only if every one of its mapped rows passed") R2's BLOCKED verdict keeps UX-02 open pending a dataset containing a badge-carrying Overview Recent Activities row. **Round 4 gap-closure 2026-08-18 — CLOSED.** Plan 20-18's fixture edit (staged build only, `dist/widgets/data/dashboard/index.json`, `streams.hr: false` on activity `i174109950` "Herlev Running") gave a badge-carrying Overview Recent Activities row to observe for the first time. R26 PASS: the `No HR` badge is quoted verbatim, one Tab stop for the row, Enter activates it, zero "View Activity" buttons anywhere in the card, both light and dark themes confirmed identical. Combined with R4\*, R5\* and R6\* (already clean passes), all four of this requirement's mapped rows now pass — a clean sweep closes UX-02. The badge is fixture-induced in the staged build, not organic archive data; the repository's `data/dashboard/index.json` still has `streams.hr: true` for `i174109950`, and the next `npm run build-widgets` overwrites the staged edit.
- [x] **UX-03**: Row-level navigation is keyboard-accessible and announced correctly, not a click handler on a bare `<div>`. **Round 3 gap-closure 2026-08-17**: plan 20-11's Round 3 checkpoint closed CR-02's screen-reader observation gap — R15 PASS and R17 PASS. R1, R3, R4, R7, R8, R9, R10 all PASS with both themes named. **Round 4 gap-closure 2026-08-18**: R22, R23, R24, R26, R27, R29, R30 and R33 all PASS (R27 by explicit VoiceOver-declined substitution; R29 and R33 PASS on the developer's own-eyes blanket "looks good", though neither answer itemized the per-item detail its row asked for). R28 was NOT EXERCISABLE (no middle button, D-12/D-13 disposition question left unanswered). R25 was BLOCKED. R31 and R32 were both FAIL — see GAP 12. **Round 5 gap-closure 2026-08-18 — CLOSED, clean sweep.** Plan 20-20's Round 5 checkpoint re-asked R29/R33's itemization gap as lettered sub-questions (R40, R41) and re-asked R28's unanswered disposition (R39) and GAP 12 (R34, R35, R36), all against plan 20-19's D-16/D-17 fix, all developer-observed where required: R34 PASS, R35 PASS (disposition accepted explicitly), R36 PASS, R37 PASS, R38 PASS (CR-01's per-cell announcement observed for the first time — six distinct strings, Flags cell announces its own badge text "Low confidence...", not the date; VoiceOver was declined by the developer and the agent's computed-accessible-name substitution is recorded explicitly), R39 PASS (both D-12/D-13 disposition questions answered explicitly: yes the auxclick-not-handled clause still stands, no it is no longer load-bearing — closing GAP 11), R40 PASS (four lettered sub-answers, both themes named), R41 PASS (six lettered sub-answers, both themes named), R42 PASS, R43 PASS (one Tab stop per row, empirical on the PR table, structural on the PR-progression table). Combined with the carried-forward passes (R27\*, R30\*, R1\*, R3\*, R4\*, R8\*, R10\*, R15\*, R17\*), every mapped row now passes. GAP 12 and GAP 11 are both closed. A related scope boundary (D-16's guard does not cover the Date-cell anchor) was discovered and explicitly accepted by the developer as a deliberate, minor limitation, not a defect — see `20-VALIDATION.md` Round 5, "NEW DISPOSITION". See `20-VALIDATION.md` Round 5.

### Overview

- [x] **OVR-01**: Recent PRs rows show name, date, distance and PR badge in a deliberate visual hierarchy rather than three stacked divs, and link to the activity. **Closed 2026-08-18 (plan 21-07, Round 1)**: all three mapped rows passed — R1 PASS (two-line hierarchy, both themes), R4 PASS (no badge wrap, though observed at ~521px rather than the specified ~360px due to a Chrome minimum-window-width constraint the developer accepted), R5 PASS (single Tab stop, focus ring, `#/activity/...` navigation confirmed). See `21-VALIDATION.md` Round 1.
- [x] **OVR-02**: Recent Activities rows follow the same structure and linking. **Closed 2026-08-18 (plan 21-07, Round 1)**: both mapped rows passed — R2 PASS (Recent Activities row matches a Recent PRs row directly above it, same two-line shape and badge placement) and R3 PASS (Activities mobile card below 720px shows the same two-line shape). See `21-VALIDATION.md` Round 1.
- [x] **OVR-03**: User can switch the records section between at least all-time and current-year views (e.g. a "This year's records" tab). **Closed 2026-08-18 (plan 21-08, Round 2)**: Round 1 passed four of six mapped rows (R6, R8, R9, R10 — the control renders and toggles, the scope governs only the PR tables, the scope does not persist, and each empty state names its own distance) but recorded R7 BLOCKED, not PASS, because the live archive holds zero current-year best-effort ranking entries in any distance, so no table under "This year" had rows whose rank and date could be read back. Round 2's R14 and R15 superseded Round 1's R7 (which stays recorded BLOCKED, unedited): R14 PASS confirmed a hard-reloaded tab actually served the fixture, and R15 PASS read back the 400m This-year table re-ranking to `#1`/`#2` rather than passing the staged source ranks (`4`/`9`) through. This evidence rests on a disclosed staged-build fixture (`dist/widgets/data/stats/best-efforts.json`, two 400m `startDate` entries redated into 2026), not organic archive data, because the archive has no current-year best-effort ranking entries in any distance. See `21-VALIDATION.md` Round 1 and Round 2.
- [x] **OVR-04**: Headline Stats includes distance this year and hours this year alongside the existing all-time figures. **Closed 2026-08-18 (plan 21-07, Round 1)**: its one mapped row passed — R11 PASS (8 tiles total, the last two reading "775.1 km / Distance This Year" and "78 / Hours This Year", both themes confirmed). See `21-VALIDATION.md` Round 1.

### Calendar

- [x] **CAL-01**: User can choose whether weeks start on Sunday or Monday; the choice persists. `calendar-logic.ts` is currently hard-coded Sunday-first, so this changes pure grid logic, not just display. **Closed 2026-08-18 (plan 22-05, Round 1)**: all four mapped rows passed — R2 PASS (Monday-default headings read back verbatim after clearing storage, confirmed as the intended D-03 change), R5 PASS (focus stays on the pressed segmented option; an open picker clears on toggle), R7 PASS (Sunday selection survives a real hard reload), R8 PASS (a devtools-tampered `'MONDAY'` value renders Monday-first with no console error and is read back unrepaired, confirming T-22-WK-01/D-07). See `22-VALIDATION.md` Round 1. **Gap 2 / CR-01 closed 2026-08-18 (plan 22-08, Round 2)**: R15 PASS — the blocked-storage path (a throwing `localStorage` GETTER installed live on the page) was observed closed via a hash navigation to the Calendar route, rendering the full grid with no `Something went wrong` panel and no application console error. R16 (the browser-configuration-level equivalent) was declined by the developer ("decline it") and is informational only; it does not gate this requirement. See `22-VALIDATION.md` Round 2. **Closed 2026-08-19 (plan 22-12, Round 3)**: R23 PASS — with the R22 browser setting restored, theme persistence across a reload was directly confirmed by the developer ("Yeah theme sticks after allowing cookines"); the week-start persistence half of the row was answered only by an earlier, stale "confirm" and was not re-stated with detail after the restore, recorded honestly as a thin/waived gap rather than an invented observation. R22 (CAL-01, T-22-WK-02, GC-5, BL-03) also PASS, disposition (a): the app-wide `resolveStorage()` guard was observed closing the blocked-site-data threat end to end in Safari with "Block all cookies" active — nav rendered, grid rendered, week start defaulted to Monday, no console errors — the first real-browser exercise of this path in the phase, superseding Round 2's declined R16. See `22-VALIDATION.md` Round 3. **Pending (Round 3 tick REVERTED 2026-08-19 — plan 22-16, Round 4)**: R27, the row that exercises the header theme toggle under real blocked site data (the same control CR-01 broke and Round 3's R22 never clicked), is recorded BLOCKED — the developer reported only that `Theme: dark` was reached, without the three individual `aria-label` values in click order, the specific browser/setting used, or a per-click colour-change statement that disposition (a) requires. Per this round's non-waivable house rule 14, that shortfall could not be closed on the developer's summary authority alone. R28's theme-persistence sub-check is unaffected, but CAL-01's tick survives only if R27 AND R28 are both PASS, and R27 is BLOCKED, so the tick is reverted, following the Phase 19 UI-01/UI-02 precedent for a requirement whose gating row did not clear the bar. See `22-VALIDATION.md` Round 4. **Re-ticked 2026-08-19 (Round 4 final disposition, plan 22-16)**: R27 recorded PASS on the developer's explicit authority after they were asked twice for the three-click `aria-label` sequence and browser/setting and declined further verification; the evidentiary shortfall is retained verbatim in `22-VALIDATION.md`'s R27 Observation cell. The unit-level evidence is strong — `nav-theme.test.ts` proves three clicks from `auto` reach light→dark→auto with NO storage handle, and the post-merge gate is green (tsc exit 0, build exit 0, 1272/1272 tests across 53 files).
- [x] **CAL-02**: Week totals are computed and shown at the end of each week row, and respect the selected week start. **Not closed 2026-08-18 (plan 22-05, Round 1)**: R3, R4 and R6 passed (October 2025 boundary weeks re-grouped correctly under both week starts; June 2025 rest weeks render only the en-dash), but R11 recorded FAIL — day-cell values slightly overflow their cells once the viewport narrows toward ~380px. See `22-VALIDATION.md` Round 1. **Still not closed 2026-08-18 (plan 22-08, Round 2)**: R13, the R11 re-ask against the freshly proven Round 2 build, again recorded FAIL. See `22-VALIDATION.md` Round 2. **Closed 2026-08-19 (plan 22-12, Round 3)**: all three mapped rows passed — R18 PASS (URL/hard-reload/bundle filename `index-Bsnjp2E6.js` confirmed neither prior round's build), R19 PASS (the R11/R13 re-ask, third time: narrowed to a stated 375px via Chrome DevTools device emulation, `matchMedia('(max-width: 380px)').matches` confirmed `true` — the first round to provably engage the breakpoint being judged — with more than three distance-carrying day cells and multiple week-total cells quoted, no overflow/clipping/truncation, values wrap onto additional lines instead, both themes "behave same"), R20 PASS (all five Monday-start week-total triples read back exactly matching the preamble table). See `22-VALIDATION.md` Round 3. **Pending (Round 3 tick REVERTED 2026-08-19 — 22-VERIFICATION.md found it OVERSTATED: R19's single 375px observation does not support the requirement's own "every viewport" language, and the identical overflow-causing rules were confirmed unconditional at 381px+; re-decided by Round 4 rows R24/R25/R26/R28(ii), plan 22-16)**. **Round 4 result 2026-08-19 (plan 22-16)**: partial progress, still Pending. R24 PASS and R25 PASS — the 381-530px sub-band is now genuinely closed for the first time in this phase, at a developer-stated **393px** (iPhone 15/16 Pro) via Chrome DevTools device emulation, with both `matchMedia('(max-width: 640px)').matches === true` and `matchMedia('(max-width: 380px)').matches === false` confirmed and the full rendered table (all 31 day cells, all five week-total cells) quoted with an explicit "Overflow: no" in both themes. But R26 — the row that tests the ~600px sub-band where the widened compaction's single-column stack renders for the first time in the phase's history — is recorded **BLOCKED**: the developer's evidence was given at 393px (R25's own width), not at R26's own required ~600px width, and no separate observation at that band was ever made. R28(ii)'s five-triple regression check PASSED, an exact match to the Monday-start table. Because CAL-02 re-ticks only if R24, R25, R26 and R28(ii) are ALL PASS, and R26 is BLOCKED, the tick stays Pending. See `22-VALIDATION.md` Round 4. **Re-ticked 2026-08-19 (Round 4 final disposition, plan 22-16)**: R26 recorded PASS on the developer's explicit authority; the ~600px band reading was never captured and that shortfall is retained verbatim in `22-VALIDATION.md`'s R26 Observation cell. R25's 393px observation with both `matchMedia` confirmations stands as genuine, fully-observed evidence for the 381–530px band.
- [x] **CAL-03**: Calendar controls use the shared control styling from UI-01/UI-02. **Closed 2026-08-18 (plan 22-05, Round 1)**: both mapped rows passed — R9 PASS (`.calendar-header`'s baseline holds across five mixed-height controls in both themes, with responsive wrapping at narrow widths, not misalignment) and R10 PASS (hover feedback, two-tone `:focus-visible` ring, and WCAG AA text contrast all confirmed on the segmented control in both themes). See `22-VALIDATION.md` Round 1. **Round 3 confirm-unregressed 2026-08-19 (plan 22-12)**: R21 PASS (thin, waived) — header alignment confirmed good and the `Total` header confirmed sitting over its values ("Total sits on top of valies"); this row does not re-gate CAL-03, whose state is left exactly as it was. See `22-VALIDATION.md` Round 3.

### Trends

- [x] **TRN-01**: User can zoom in and out of trend charts via `chartjs-plugin-zoom`, so a weekly view no longer shows the entire 15-year archive at once. **Plan 23-05 (2026-08-19)** wired `chartZoomPlugin` into Volume, the Cadence & HR pair, and Training Load — each now constructs with `plugins:[chartZoomPlugin]` and opens at its D-06 default window over the always-complete dataset. **Plan 23-06 (2026-08-19)** made the Training Load 3mo/12mo/All control set the chart's zoom range (`loadWindowRange`) instead of slicing the dataset (D-03) — the chart now always holds the full series and the preset only chooses the visible portion, so free zoom and pan continue from wherever a preset put you. Still not ticked: this repo's test environment has no canvas polyfill, so nothing here can confirm the zoom gesture/button actually works in a real browser — that is plan 23-07's human checkpoint. **Plan 23-07's Round 1 browser checkpoint closed 2026-08-26 and TICKS this requirement**: all five mapped rows PASSED. R2 — the developer held ⌘ and scrolled over the plot area, delivering 84 wheel events all `isTrusted` with `metaKey: true`, and the rendered ticks moved from `13 Aug 2025`…`13 Aug 2026` to `11 Aug 2011`…`13 Aug 2026`. R3 (bare wheel scrolls the page, range unchanged), R4 and R5 (the D-06 opening windows on all three granularities, matching the pre-computed expected table verbatim), and R18 (near-daily resolution at deep zoom — adjacent tooltip points exactly 86,400,000 ms apart, which withdrew the earlier decimation Finding 3) all PASSED. Zoom demonstrably works in a real browser against the full 1,868-activity archive. **Round 2 checkpoint (closed 2026-08-26/27, plan 23-11) re-confirms this against a fresh build (`assets/index-D01ardNQ.js`, differing from Round 1's) after plans 23-08/23-09/23-10 landed: R22, R23, R24, R25 and R38 all PASS again — R22 by a real 19-event trusted ⌘+wheel gesture moving ticks `Aug 2025 to Aug 2026` to `2019 to 2026`; R24/R25 matching the Round 2 recomputed month/bare-year tick shapes exactly (23-10 changed the tick precision, not the zoom behaviour); R38 confirming the day-level decimation still holds. No Round 2 row for this requirement failed; the tick stands.**
- [x] **TRN-02**: User can scroll/pan charts horizontally, with explicit +/− and left/right controls in addition to gestures (controls must work without a pointer). **Plan 23-02 (2026-08-19)** shipped the CSS layout classes (`.chart-band__header--zoom`, `.chart-zoom-controls`, `.chart-zoom-hint`) the control cluster will attach to; **plan 23-04 (2026-08-19)** built the control cluster and settle-driven controller in `chart-zoom.ts`; **plan 23-05 (2026-08-19)** attached that cluster to all three zoomable tabs' band headers (one shared cluster for the Cadence & HR pair, per D-02/D-10). Not ticked, per this project's established pattern (Phase 19 UI-01/UI-02, plan 19-15) of gating a requirement's completion on its user-facing behavior actually existing in a rendered browser, not on a mechanical `requirements:` frontmatter match — plan 23-07's checkpoint is where this ticks. **Plan 23-07's Round 1 browser checkpoint (closed 2026-08-26)** did NOT discharge this: R6 (drag-to-pan, measured at +576px over 119 `pointermove` events, ticks moving to `22 Nov 2024`…`22 Nov 2025`, cursor `grab`→`grabbing` captured on six move samples), R7 (four individual keyboard-only button activations), R9, R10 and R12 all PASSED. **Blocking rows: R8 (FAIL), R11 (FAIL).** R8 fails on magnitude — the `+`/`−` step is a factor of 2, not the designed 1.5 (`chart-zoom.ts:401` hands `ZOOM_FACTOR = 1.5` to the plugin, whose `linearZoomDelta` removes `range * (zoom - 1)`), so `←` produced `Apr 2025 to Apr 2026` against the expected `May 2025 to May 2026`. R11 fails because no gesture ever updates the `aria-label` or reveals Reset: `buildZoomOptions()` returns `onZoomComplete`/`onPanComplete` as top-level siblings of `zoom`/`pan`, but chartjs-plugin-zoom reads them from inside those objects, so `settle()` never runs on a gesture (Finding 10). **Round 2 checkpoint (closed 2026-08-26/27, plan 23-11) TICKS this requirement**: both blocking rows are closed by 23-08's fix — R26 (real +255px rightward drag, cursor `grab`→`grabbing` confirmed, range updates and ticks agree), R27 (all four buttons individually confirmed by keyboard-only Enter activation, each `aria-label` quoted before/after), R28 (all four step-magnitude sub-cases now match the Round 2 recomputed ÷1.5/×1.5 table exactly — `←` now produces `May 2025 to May 2026`, not Round 1's `Apr 2025 to Apr 2026`), R29 (full zoom-out now takes 7 `−` presses, down from Round 1's 14 halving-bug count), R30 (Reset now appears after a real gesture zoom too, closing the reveal-path gap Round 1 found), R31 (the `aria-label` now names the same range the ticks show after a gesture, a button, and a drag — closing Finding 10's other symptom) and R32 all PASS. No mapped row failed; TRN-02 ticks for the first time.**
- [ ] **TRN-03**: Chart bands are taller than the current fixed 140px so the y-axis has usable range on a page with room to spare. **Plan 23-02 (2026-08-19)** shipped `.chart-band__canvas-wrap--tall` with the exact `clamp(200px, 34dvh, 420px)` value (plus a 430px phone-width floor), fully tested by value; **plan 23-03 (2026-08-19)** applied that modifier class to real DOM in all three zoomable tabs via the shared `buildChartBand` helper. Not ticked until a later Phase 23 plan's human browser checkpoint confirms the taller bands actually render. **Plan 23-07's Round 1 browser checkpoint (closed 2026-08-26)** did NOT discharge this: R13 PASSED at both pinned ends of the clamp — `innerHeight` 600 → 204px (34dvh, tracking the viewport) and, under DevTools Responsive emulation at 1200×1400, a computed height of exactly `420px` where 34dvh would be 476px, proving the ceiling binds — and R14 PASSED (both Cadence & HR bands 200px each, tab scrolls). **Blocking row: R15 (FAIL).** D-21's band floor holds at all four required phone widths (390/393/412/430 all → 240px, inside `clamp(160px, 30dvh, 260px)`) and the chart canvas reflows correctly at each, but the row's non-waivable no-horizontal-overflow clause fails at every width: `documentElement.scrollWidth` is pinned at 682, caused by `.year-heatmap` rendering a fixed 634px-wide grid that never reflows (Finding 9). **Round 2 checkpoint (closed 2026-08-26/27, plan 23-11) — STILL PENDING.** R33 and R34 both PASS again (34dvh clamp holds at both ends; both Cadence & HR bands equal at 272px). **Blocking row: R35 (FAIL), narrowed but not closed.** 23-09's year-heatmap scroll wrapper fixed its own overflow (`.year-heatmap`/`.year-heatmap-scroll` both PASS, reachable by scroll) and improved `documentElement.scrollWidth` from Round 1's pinned 682 down to 460 — but 460 still exceeds `clientWidth` (390/393/412/430) at every phone width tested. Root cause is now isolated to a DIFFERENT element than Round 1's: the five-tab Trends navigation strip (`div.segmented`, the Volume/YoY/Cadence & HR/Training Load/Gear strip), measured at 412px wide with `overflow-x: visible` on itself and its parent, never given a scroll wrapper (Finding 11). TRN-03 stays Pending on this new, narrower defect.**
- [x] **TRN-04**: Zoom/pan composes correctly with the existing granularity toggle and the five-tab structure, and does not regress the canvas lifecycle (no "Canvas is already in use" on tab cycling). **Plan 23-05 (2026-08-19)** added the three D-22 zoom-range closure slots in `trends.ts` (`volumeZoomRange`/`cadenceHrZoomRange`/`loadZoomRange`), written on settle and read on rebuild so a zoom survives a tab switch, with `unmount()` explicitly clearing all three. Every mount function's `destroy()` now also destroys its zoom controller before `chart.destroy()`. **Plan 23-06 (2026-08-19)** made the Volume granularity toggle clear `volumeZoomRange` before the rebuild (D-23), so a granularity change composes correctly with zoom state instead of carrying a stale range into a differently-scaled chart. Still not ticked: the canvas-lifecycle claim (no "Canvas is already in use" under rapid tab cycling) is explicitly named in plan 23-05's own threat register (T-23-CANVAS) as unobservable to this repo's DOM-less tests — discharged only by plan 23-07's browser checkpoint. **Plan 23-07's Round 1 browser checkpoint (closed 2026-08-26)** did NOT discharge this: R17 PASSED outright — the canvas-lifecycle claim this requirement names is confirmed, with three full five-tab passes preserving zoom, the granularity toggle resetting per D-23, zero console errors and specifically no "Canvas is already in use", per-panel canvas counts at their design values and exactly one `.chart-zoom-controls` cluster. R19 PASSED (thin-coverage shading covers the same dates, 2011-08-16 → 2017-03-27, at both zoom levels) and R20 PASSED (D-22 reset on full remount). **Blocking row: R16 (FAIL)** — D-02 lockstep holds on the button path but breaks on the gesture path, because `settle()` is the only thing that propagates a range to the sibling member and Finding 10 shows `settle()` never runs on a gesture at all. **Round 2 checkpoint (closed 2026-08-26/27, plan 23-11) TICKS this requirement**: R16's blocking defect is closed by 23-08's nesting fix — R36's real gesture half (111 trusted `metaKey` wheel events over the HR/lower band, ≥1s settle wait) now shows BOTH Cadence & HR canvases reading the same range, and the button half (R36a) still matches too. R37 (canvas lifecycle across 3 full five-tab passes, zero app-origin console errors, exact per-panel canvas counts), R39 (thin-HR- coverage shading covers the same dates at two zoom levels) and R40 (zoom resets to the weekly default on a full remount) all PASS again. No mapped row failed; TRN-04 ticks for the first time.**

### Records & Activities

- [x] **REC-08**: Records rows navigate on row click rather than via a large button. **Closed 2026-08-13 (plan 20-08, Round 2)**: plan 20-08's Round 2 checkpoint recorded genuine, individually-described evidence for both of this requirement's mapped rows — R5 PASS (the developer read back the PR table header verbatim: "Rank  Time  Pace  Age-Grade  Date  Flags", six columns, matching exactly) and R6 PASS (the PR-progression header read back verbatim: "Date  Time  Improvement", three columns, matching exactly). Neither row is theme-sensitive. See `20-VALIDATION.md` Round 2.
- [x] **ACT-01**: Activities screen controls adopt the shared styling; the screen's existing interaction model (row click) is preserved as the reference pattern. **Round 3 confirmed-unregressed 2026-08-13**: row 19 sub-check (d) — an Activities row click still navigates to the detail view, per the developer's "all look good and unchanged". See `19-VALIDATION.md` row 19. **Round 4 confirmed-unregressed 2026-08-13**: row 24 sub-check (d) — an Activities row click still navigates to the detail view, per the developer's verbatim "d. pass" against this specific sub-check. See `19-VALIDATION.md` row 24.

### Carried Forward from v2.0

- [ ] **CUR-01**: User can toggle an activity's exclusion from PR calculations from the interface rather than hand-editing `data/best-effort-exclusions.json`, via a local curation mode (`npm run curate`) with a localhost-only write path. Per-distance exclusion must be selectable, not just whole-run — a GPS spike typically corrupts only the short splits while the same run's 5k/10k remain honest. A reason is required and is surfaced in the detail view. The write path must be provably absent from the published bundle, verified by an assertion in `verify-dashboard-publish.mjs` following the `assertNoPrivateArtifacts` precedent.
- [x] **FIX-01**: The "Current Streak" tile's `ended {date}` sub-label renders when a streak has ended. Currently structurally unreachable — it reads `currentStreakStart`, which is only populated while a streak is active (root cause `src/analytics/streak-utils.ts:118`; code review WR-01). **Closed 2026-08-18 (plan 21-07, Round 1)**: both mapped rows passed — R12 PASS (Records Current Streak tile: '0 days' / 'ended Aug 3, 2026') and R13 PASS (Overview Current Streak tile: same values, both themes). The `ended` branch was confirmed against a staged-build fixture (`dist/widgets/data/stats/streaks.json`), not organic archive data — the archive's live streak is still active, and `currentStreakEnd` is absent from the published `streaks.json` until a compute run regenerates it. See `21-VALIDATION.md` Round 1.
- [ ] **FIX-02**: `gear-aggregate-logic.ts` degrades rather than crashing when an index row lacks a `gearName` key — the Unknown-bucket test is a strict `label === null`, so an absent key reaches `slugify(undefined)` (code review WR-02)
- [ ] **VER-01**: Phase 16's three unverified theme items are confirmed in a real browser: legibility on a light-OS machine, absence of a first-paint white flash, and live following of an OS appearance change. These were never discharged by the Phase 17/18 checkpoints, which evidence suggests both ran from a dark-OS machine.
- [ ] **CI-01**: The nightly workflow and `compute-all-stats` no longer maintain two independent orderings of the compute chain with no shared source of truth
- [ ] **CI-02**: `verify-dashboard-publish.mjs` asserts reachability for every published stats document it currently relies on a whole-directory copy to carry (`weekly-distance`, `monthly-stats`, `yearly-stats`, `year-over-year`, `best-efforts.json` and the per-activity shards)

## Out of Scope

- Design-language refresh (type scale, colour system, density overhaul) — deliberately deferred; revisit after living with the systematic polish
- Any change to the analytics or computation layer beyond FIX-01/FIX-02 and CUR-01's write path
- Garmin export adapter (STREAM-04) — still blocked on the export arriving
- New chart types or new metrics — this milestone makes existing ones better

## Verification Note

Every requirement in this milestone is visual or interactive, and this project has now shipped rendering defects behind a green automated gate three times (Phase 16's black page, Phase 17's two rendering gaps, and Phase 18's near-miss). Automated tests cannot discharge UI-01 through TRN-04. Each phase must end with a human browser checkpoint against a production-shaped URL, and VER-01 in particular requires a genuinely light-OS environment rather than an in-page theme toggle.

## Traceability

Which phases cover which requirements. Updated during roadmap creation.

| Requirement | Phase | Status |
|-------------|-------|--------|
| UI-01 | Phase 19 | Complete |
| UI-02 | Phase 19 | Complete (Round 4, 2026-08-13 — clean sweep of rows 20-24) |
| UI-03 | Phase 19 | Complete |
| ACT-01 | Phase 19 | Complete |
| UX-01 | Phase 20 | Complete (Round 5, 2026-08-18 — GAP 12 closed on R34/R35/R36/R37/R42 clean sweep) |
| UX-02 | Phase 20 | Complete (Round 4, 2026-08-18 — R26/R4/R5/R6 clean sweep, badge fixture-induced) |
| UX-03 | Phase 20 | Complete (Round 5, 2026-08-18 — GAP 11 and GAP 12 closed, R34-R43 clean sweep) |
| REC-08 | Phase 20 | Complete (Round 2, 2026-08-13 — R5/R6 individually evidenced) |
| OVR-01 | Phase 21 | Complete |
| OVR-02 | Phase 21 | Complete |
| OVR-03 | Phase 21 | Complete |
| OVR-04 | Phase 21 | Complete |
| FIX-01 | Phase 21 | Complete |
| CAL-01 | Phase 22 | Pending (Round 3 tick REVERTED 2026-08-19, plan 22-16, Round 4 — R27 BLOCKED, insufficient aria-label/click evidence for the theme-toggle regression under blocked storage) |
| CAL-02 | Phase 22 | Pending (Round 4, plan 22-16, 2026-08-19 — R24/R25/R28(ii) PASS, 381-530px band closed at a stated 393px; R26 BLOCKED, ~600px band still unobserved) |
| CAL-03 | Phase 22 | Complete |
| TRN-01 | Phase 23 | **Complete** (Round 2 checkpoint 2026-08-26/27, plan 23-11: R22/R23/R24/R25/R38 all PASS on a fresh build — re-confirmed after 23-08/09/10 landed) |
| TRN-02 | Phase 23 | **Complete** (Round 2 checkpoint 2026-08-26/27, plan 23-11: R26/R27/R28/R29/R30/R31/R32 all PASS — 23-08's settle-nesting and ÷1.5/×1.5 step fixes closed Round 1's R8/R11) |
| TRN-03 | Phase 23 | Pending (Round 2 checkpoint 2026-08-26/27, plan 23-11: R33/R34 PASS; **blocked by R35 (FAIL — year-heatmap overflow narrowed 682→460 by 23-09 but not eliminated; new root cause is the five-tab strip `div.segmented`, Finding 11)**) |
| TRN-04 | Phase 23 | **Complete** (Round 2 checkpoint 2026-08-26/27, plan 23-11: R36/R37/R39/R40 all PASS — 23-08's settle-nesting fix closed Round 1's R16 gesture-lockstep failure) |
| CUR-01 | Phase 24 | Pending |
| FIX-02 | Phase 25 | Pending |
| VER-01 | Phase 25 | Pending |
| CI-01 | Phase 25 | Pending |
| CI-02 | Phase 25 | Pending |

**Coverage:**
- v2.1 requirements: 25 total (corrected from the 24 recorded when this section was drafted — a recount of the requirement list above found 25 distinct IDs: 3 Design System + 3 Interaction Consistency + 4 Overview + 3 Calendar + 4 Trends + 2 Records & Activities + 6 Carried Forward)
- Mapped to phases: 25
- Unmapped: 0

---
*Requirements defined: 2026-08-12*
