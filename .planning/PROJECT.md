# Strava Analytics & Visualization Platform

## What This Is

A personal running-data pipeline and visualization platform. It ingests run data (via intervals.icu since Aug 2026, originally the Strava API), commits per-activity streams for the whole 1,899-activity archive, computes custom statistics (aggregations, streaks, trends, geographic data, self-computed best efforts, training load) and publishes two things to GitHub Pages: ten embeddable Custom Element widgets (stats, comparisons, streaks, geographic tables and statistics, route maps, heatmap, pin map — all HTML-attribute-configurable with dark/light theming) with standalone full-page map views, and a static-SPA analytics dashboard over pre-computed JSON with an activity browser, per-run detail views, PR records, trends and a localhost-only curation mode. Since v2.2 every pace figure the dashboard derives is gap-aware and coverage-honest, every activity carries visible quality signals (pace, device era, elevation), and PR ranking is guarded by a personal plausibility ceiling that demotes-and-flags rather than deletes. Lives in its own repo (strava-widgets), with a daily GitHub Actions pipeline for automated refresh.

## Core Value

Compute and visualize running statistics that Strava doesn't readily offer, embeddable anywhere on a personal website.

## Current State

**Shipped: v2.2 Pace Data Quality** (2026-09-19) — phases 26-31, 69 plans, 168 tasks, 33/33 requirements, 11 days; milestone audit `passed` (33/33, 6/6 phases, 11/11 seams, 4/4 flows, 6/6 Nyquist). Live at https://bacilo.github.io/strava-widgets/ on the merged 1,899-activity archive. Every stream-derived pace figure now comes from one gap-aware, adaptive-window derivation with exact, always-visible coverage; every activity carries six individually-disclosed quality signals (decimation, gap profile, impossible samples, device era, elapsed-vs-moving, elevation) badged with their measured values; a personal plausibility ceiling derived from the athlete's own filtered history demotes-and-flags implausible efforts from PR ranking without deleting them (32 on the merged archive, reconciled three ways, the diff signed by the developer against its sha256); a curation review queue lists every demoted effort for one-click whole-activity exclusion through the existing write path; and `data/streams/` is byte-identical to where v2.1 left it. Phase 31, added from the close-out audit, closed the milestone's five silent-and-passing findings at the source before completion. Archived to `milestones/v2.2-ROADMAP.md`, `v2.2-REQUIREMENTS.md` and three audits. Full narrative in `.planning/MILESTONES.md`; lessons in `.planning/RETROSPECTIVE.md`.

The milestone's defining pattern: every defect that mattered was caught by re-deriving a number independently of the code that produced it — never by a test going red. Each phase now ships a classifier-independent recount of its own headline figure, every generated artifact is proven idempotent by a second run, and checkpoint rows are pinned to values derived outside the changed code. That sits alongside v2.1's "watch it fail first" as the project's second load-bearing convention.

**Shipped: v2.1 Interface Polish** (2026-09-05) — phases 19-25, 103 plans, 25/25 requirements. The whole dashboard now meets the standard its best screens already set: one shared visual treatment for every control with a focus ring visible in both themes, every activity row a real keyboard-operable link honouring the browser's own click contract, a rebuilt Overview on the shared row renderer, a selectable Sunday/Monday calendar week start driving per-week totals, gesture- and keyboard-driven zoom/pan on taller trend bands, a localhost-only curation mode whose write path is provably absent from the published bundle, and a CI chain with one source of truth for compute-step ordering. Every phase closed on a human browser checkpoint, by design.

Per-phase detail follows, retained unedited — Phase 21 (Overview rebuild) complete 2026-08-18, 8 plans across five waves plus one gap-closure round; re-verification scored 6/6. Overview's Recent PRs and Recent Activities now render through the same shared row renderer as the Activities list (`renderActivityRow`, made multi-surface-safe via an `idPrefix` so three surfaces can coexist), the Records section gained an all-time / this-year scope control on the Phase 19 `.segmented` treatment, Headline Stats gained distance and hours this year, and the Current Streak tile renders its `ended {date}` sub-label — a two-layer fix, since `streak-utils.ts` only populated `currentStreakStart` while a streak was active. OVR-01..OVR-04 and FIX-01 all closed. Two of the five requirements deliberately resolve onto the Records screen rather than Overview (D-01, D-15), decided in `21-CONTEXT.md`. Both human checkpoint rounds needed disclosed staged-build fixtures, because the live archive can produce neither an ended streak nor a current-year PR ranking row; Round 2's fixture was discriminator-designed — source ranks left at `4`/`9` so the year scope had to be observed re-ranking to `#1`/`#2`, a value read-back rather than a "rows appeared" check. Phase 20 (row-click interaction pattern) complete 2026-08-18, 20 plans across five gap-closure rounds. Every activity row on every screen is now a real anchor: one keyboard stop per row landing on the Date link, modifier-clicks handled natively by the browser, and a shared `shouldNavigateOnRowClick` predicate consulted from both the row listener and each cell anchor so drag-select no longer navigates. UX-01/02/03 and REC-08 all closed. Two behaviours were put to the developer during the Round 5 browser checkpoint and accepted as shipped: the Date-cell anchor sits outside D-16's guard, and a double-click's first click still navigates. Phase 19 (design system & control styling) complete 2026-08-13, 17 plans across four gap-closure rounds. **Phase 23 (Trends zoom, pan & taller bands) complete 2026-08-27** — 13 plans across nine waves and three human browser checkpoint rounds; `23-VERIFICATION.md` `status: passed`, 5/5, TRN-01..TRN-04 all satisfied. Trend charts now zoom and pan by gesture and by keyboard-reachable on-screen controls, on taller bands, without disturbing the five-tab structure, the granularity toggle or the canvas lifecycle. Two structural lessons worth carrying: (1) Round 3's clean sweep was followed by a code review that found a Critical (CR-01) none of the three browser rounds could have caught — the Cadence & HR call site passed the opening window where archive bounds belong, caging gesture zoom-out at ~5 of ~15 years. Every round either zoomed inward or used buttons, so no row ever gestured OUTWARD on that tab; the rows that did touch it only asserted the two bands agreed with each other. Fixed in `49add71` and closed by a real gesture that walked THROUGH the bug's old clamp (`Jul 2021 to Aug 2026`) to the archive floor (`Jul 2011 to Aug 2026`). Checkpoint rows should assert reachable EXTENT, not just internal agreement. (2) Both `.year-heatmap-scroll` and `.trends-tablist-scroll` were needed before `documentElement.scrollWidth` finally equalled `clientWidth` at phone widths — an overflow fix that narrows the number is not a fix, and the row was rightly scored on equality across all three rounds. **Phase 24 (Local Curation Mode) complete 2026-09-02** — 17 plans across ten waves, four human browser checkpoint rounds and four gap-closure rounds; `24-VERIFICATION.md` `status: passed`, 4/4, CUR-01 closed. `npm run curate` serves the dashboard from a localhost-only Node server with an inline exclusion tickbox and required reason on the activity detail view; the write path is proven absent from the published bundle by two independent layers — a build-time content scan (`curation-guard.mjs`) and an HTTP-layer assertion (`verify-dashboard-publish.mjs`). Exclusion is whole-activity, not per-distance (D-04 amended CUR-01 mid-phase). Three structural lessons worth carrying: (1) a checkpoint row can be *unsatisfiable* — R19 and R26 both failed because their own mandated setup emptied the discriminator they existed to test, and only Round 4's explicit reachability proof (assert `wasPRAtTheTime === true` from the served shard, HALT before presenting otherwise) made the row able to discriminate at all. (2) The live exclusions document must beat the precomputed `excludedFromRecords` flag in BOTH directions; the stale-TRUE mirror direction went uncovered for three rounds because no row constructed it, and a divergence mutation was shown to pass `tsc --noEmit` while failing the seam tests. (3) Duplicated logic defeats checkpoints — the header/panel divergence only closed once both derivations were forced through one exported `resolveExcluded`. Still open and NOT resolved: WR-19 (an unreadable directory throws an uncaught `EACCES` out of the curation guard — the directory-shaped sibling of the file case 24-15 fixed; fails closed, so publish-safety is intact) plus IN-17/IN-18, all captured in `.planning/todos/pending/`. **Phase 25 (CI hardening & light-theme verification) complete 2026-09-04** — 12 plans across nine waves, two human browser checkpoint rounds and one gap-closure round; `25-VERIFICATION.md` `status: passed`, 4/4, FIX-02/CI-01/CI-02/VER-01 all closed. The gear-aggregate crash now degrades into the Unknown bucket for an absent `gearName` key, the nightly workflow's eight hand-maintained compute steps collapsed onto a single `COMPUTE_ALL_STATS_STEPS` source of truth, the publish verifier asserts six documents by name, and Phase 16's three theme items are finally discharged against production. This closes v2.1 Interface Polish: all seven phases, 103 plans. The phase's defining difficulty was again evidence rather than feature. Round 1 returned R2 and R6 BLOCKED and correctly withheld ALL FOUR requirement ticks — R2 because no captured frame could be obtained before production's 612 ms first paint (the extension's screenshot round trip floored at ~855 ms), and R6 because CI-01's live-run evidence had never existed and the row was unsplittable while it didn't. Three structural lessons worth carrying: (1) GAP-25-01 was closed by MEASURING a mechanism before drafting a row, not by retrying a faster capture — three candidates were swept against the real first paint and a stripped-bootstrap negative control proved the winner could report white on a broken page, because a mechanism unproven in the failure direction is vacuous. (2) The sweep nearly produced a false pass: every run's first screencast frame is `rgb(18,18,18)`, Chrome's UA-default canvas fill, arriving before `navResponseEnd` — excluded by a stated rule rather than silently, since uncorrected it would have handed the losing candidate a wide margin it does not have. (3) An unsplittable row is a planning defect, not a verdict — splitting R6 into R6a/R6b/R6c gave each requirement its own discriminator and let FIX-02 and CI-02 close on their own evidence instead of being held hostage by CI-01's missing dispatch. Row-baseline integrity held across the whole round: exactly four lines changed in `25-VALIDATION.md` after drafting, all four of them Verdict cells. Still open and NOT resolved: `25-REVIEW.md`'s CR-01 (an unawaited CDP listener rejection crashes `first-paint-capture.mjs` outside `main()`'s promise chain, leaking the Chrome child process and temp profile), WR-01/WR-02 (no socket timeout, no child-process `error` listener), WR-03 (~35 unguarded `JSON.parse` calls in `verify-dashboard-publish.mjs` abort the accumulate-and-report gate on first malformed body) and WR-04 (`src/index.ts:314` prints the success line unconditionally, before the DEGRADED STEPS block, so that string carries no discriminating information). Inherited, not introduced, and now CLOSED: a raw NUL byte in `src/analytics/gear-aggregate-logic.ts`'s Unknown-bucket sentinel, present since `feat(18-05)` on 2026-08-11, made that one file binary to git and therefore undiffable and unblameable. Removed 2026-09-04 by `ea51679b` (Phase 25, WR-05); verified absent at the milestone close.

**Shipped: v2.0 Training Dashboard** (2026-08-12) — phases 14-18, 56 plans.

A full analytics dashboard lives at https://bacilo.github.io/strava-widgets/ as a static SPA over pre-computed JSON: hash routing across six views, committed per-activity streams for the whole archive, a self-computed best-effort engine, an activity browser with per-run pace/HR/cadence detail and route maps, and a Records + Trends layer covering PR tables and evolution, Riegel race predictions, WMA age-grading, volume/consistency/year-over-year, cadence & HR, CTL/ATL/TSB training load, and per-shoe gear analysis.

Milestone audit: `tech_debt` — 29/29 requirements satisfied, 0 blockers, 41/43 integration checks wired, 6/6 E2E flows. Deferred items are listed in STATE.md and `.planning/v2.0-MILESTONE-AUDIT.md`; the notable one is Phase 16's three unverified theme/first-paint UAT items.

<details>
<summary>v2.0 milestone goal and target features (archived)</summary>

**Goal:** A full analytics dashboard (static SPA on GitHub Pages) for browsing the complete running archive — activities with pace/cadence/HR detail, self-computed best-effort records, and weekly/monthly/yearly/all-time stats — built as a flexible foundation that many more functions can plug into over time.

**Target features:**
- Stream ingestion — parse time-series (pace, HR, cadence, elevation) from local Strava export FIT/GPX via provenance.json; intervals.icu streams for new activities going forward
- Activity browser — list/filter/sort the full archive; per-activity detail view with pace/HR/cadence charts and route map
- Best efforts — fastest 400m/1k/1mi/5k/10k/HM/marathon computed within each run from streams; PR lists per distance
- Records & trends — weekly/monthly/yearly/all-time aggregates and records
- Dashboard SPA shell — client-side routing over pre-computed JSON, designed for long-term extensibility

**Key context:** Flexibility was the explicit priority — not everything shipped. Static-only hosting constrains everything to pre-computed JSON.

</details>

<details>
<summary>v2.2 milestone goal and target features (archived)</summary>

**Goal:** Make every pace figure the dashboard derives honest — one gap-aware derivation, plausibility guards that actually bind, and per-activity quality signals you can see — without rewriting a single committed stream.

**Target features:**
- One gap-aware pace derivation replacing the two divergent ones today (`derivePaceSeries`'s 20s window in `detail-charts-logic.ts:97` vs the raw per-sample `dt / (dd / 1000)` in `detail-zones.ts:76`)
- Honest coverage — recording gaps (1,233 activities have one >10s) and pause gaps (321 have >5 min of ≥30s gaps) stop manufacturing pace values and stop being silently dropped
- PR plausibility that binds — a ceiling derived from demonstrated personal ability rather than the world record; rejected efforts stay visible, flagged and overridable, never deleted
- Per-activity quality signals and badges — device era, stair-step ratio, impossible-sample count, gap profile, elapsed-vs-moving divergence
- A review queue in local curation mode feeding the existing manual PR-exclusion list
- Elevation as a quality signal — implausible altitude flagged by mechanism across 71 activities (3.8%, all device families): sub-ground-level readings (11), barometric closure drift >60 m (34, worst 198 m), implausible vertical rate (39). Flag only; no DEM correction, no grade-adjusted pace
- Cross-era consistency across 4 watches, Runkeeper, the Strava app and intervals.icu (1,802 FIT / 38 GPX / 24 intervals streams; 716 activities record no device at all)

**Key context:** Scoped from a live archive investigation, not a feature wish-list. The defining finding is that a large share of the visible pace error is self-inflicted rather than device error: `derive-stream.ts:26` caps streams at `MAX_SAMPLES = 3000` and decimates by index selection to a 2-3s floor above it, which hits 995 of 1,864 activities (53%) and 151 of the 154 worst stair-step activities. Because it decimates by *selection* on a signal whose distance updates every ~2s, it aliases — manufacturing the alternating full/zero distance pattern that produces the phantom fast mode in the pace histogram. Activity 4556693525 is the worked example: the shipped histogram covers 72% of elapsed time and shows a fake 2:45 cluster plus 8:15 and 72:00 buckets, all of which vanish under the 20s window the chart beside it already uses, leaving one clean distribution centred where the per-km splits (4:35, 4:18, 5:17, 5:22, 5:44, 5:24, 5:22, 6:12, 6:08, 6:53; overall 5:35) say the run actually was.

**Explicit non-goals, each with a reason:**
- **`data/streams/` stays byte-identical.** No re-derivation this milestone despite 1 Hz originals being available locally for 94.5% of the archive (named by `upload_id` under `export_data/`, gitignored) and carrying an unused device `speed` channel present in 99.8% of records. Deferred on three grounds: ~200 MB of permanent git cost (streams are 143 MB, `.git` is 172 MB, and 1 Hz roughly doubles 2.7M samples to 5.3M); measured movement of only −1.6s to +5.0s on the tested activity; and the direction of that movement — higher resolution makes efforts *faster*, so re-derivation must follow rejection, never precede it. Revisit next milestone once quality signals exist to measure whether it helped.
- **Genuine device over-measurement is flagged, never corrected** — the Suunto reports 17 m/s over a single second on a descent where GPS geometry says 0 m, and position exists in only 51.3% of that file's records, so geometry cannot arbitrate. Removing that distance would change the activity total and every split.
- **Device `moving_time` stays the shipped aggregate.** Elapsed-vs-moving becomes a visible signal, not an authoritative recomputation.
- **PRs move by demotion, never recalculation.** `findBestEffort` integrates distance at interpolated crossings, so pace smoothing is invisible to it; the only defensible movement is rejecting efforts that should never have ranked.
- **Stopped-watch correction is out.** Nothing in the stored data distinguishes a deliberate rest from a forgotten stop — both are a pause with +0 m.
- **Transport gaps need no work** — stopping the watch records no distance; only 9 activities advance >100 m across a ≥30s gap, all at ≤14 km/h.

**Outcome (2026-09-19):** every target feature shipped; figures that moved during execution are recorded in `milestones/v2.2-REQUIREMENTS.md` § Requirement outcomes (no-device-name cohort 716 → 663 by better classification; elevation drift 34 → 21 once loop-gated; the composite severe rate measured 15.8%, not the ~5% the criterion guessed, and reported rather than tuned to). The non-goals above all held.

</details>

<details>
<summary>v2.1 milestone goal and target features (archived)</summary>

**Goal:** Bring the whole dashboard up to the standard its best screens already set — consistent interaction, properly styled controls, and charts you can actually navigate.

**Target features:**
- Design system pass — style form controls (the stylesheet has zero `input` rules today), unify buttons and focus states, one spacing rhythm across all five screens
- Interaction consistency — every activity row clickable, redundant "view activity" CTAs removed, Overview and Records brought up to the Activities standard
- Overview rebuild — structured PR/activity rows, a current-year records tab, distance and hours this year in Headline Stats
- Calendar — selectable Sunday/Monday week start affecting week totals, totals at the end of each week row
- Trends — zoom and horizontal pan via `chartjs-plugin-zoom` with explicit +/− and arrow controls, taller chart bands
- Carried forward — exclusion tickbox via local curation mode, two code-review fixes, Phase 16's unverified theme items, two CI hardening items

**Key context:** Refinement, not new capability — the dashboard already does what it should and some screens are genuinely good. Scope came from a screen-by-screen walkthrough plus a source audit that traced complaints to root causes. Deliberately excludes a design-language refresh. Every requirement here is visual or interactive, and automated gates have missed rendering defects in this project three times, so each phase ends with a human browser checkpoint.

</details>

## Requirements

### Validated

- ✓ All stream-derived pace comes from one shared module, enforced by a structural audit that is demonstrated failing on a planted second implementation (PACE-01) — Validated in Phase 26: Shared Gap-Aware Pace Derivation & Honest Coverage
- ✓ Recording and pause gaps are classified and clipped rather than smoothed into manufactured pace, with exactly itemisable covered-time accounting (PACE-02, COV-01) — Validated in Phase 26
- ✓ The smoothing window adapts to each activity's own distance-advance interval instead of a fixed 20s, recovering exemplar 5059204779 from 94.81% fast-mass / 30% coverage to 1.22% / 97% (PACE-03, PACE-04) — Validated in Phase 26
- ✓ Coverage is always on screen and gaps are disclosed on the splits, never silently dropped (PACE-05, COV-02) — Validated in Phase 26
- ✓ The residue adaptive windowing does not fix is quantified and committed as a regenerable deliverable rather than smoothed into plausibility (PACE-06) — Validated in Phase 26
- ✓ A metadata-vs-stream pace cross-check flags material disagreement rather than displaying metadata as fact, firing on exactly 1 of 1,890 activities (PACE-07) — Validated in Phase 26
- ✓ A stratified, reusable fixture library that Phases 27-30 compose rather than rebuild (ERA-03) — Validated in Phase 26
- ✓ Five per-activity quality signals (decimation ratio, impossible-sample count, gap profile, elapsed-vs-moving divergence, device era) computed in CI onto every index row with `DASHBOARD_INDEX_SCHEMA_VERSION` unchanged, plus a lazy per-activity evidence shard fetched exactly once per opened detail view (QUAL-01, QUAL-02) — Validated in Phase 27: Per-Activity Quality Signals
- ✓ Badges name the condition and its measured value on every row surface and an always-on Quality Signals section discloses all signals on the detail view, healthy or not; one "has any severe signal" filter (QUAL-03, QUAL-04) — Validated in Phase 27
- ✓ Severity is calibrated against a live denominator and reproduced by a classifier-independent recount; the measured composite severe rate (299 of 1,890, 15.8%) is reported as a finding rather than retuned toward the ~5% the criterion guessed (QUAL-05) — Validated in Phase 27
- ✓ Device family, not file format, drives branching (fēnix 6 Pro vs Suunto 9 FIT differentiated), and no-device-name is its own explicit category, never a fabricated default — 663 of 1,899 (ERA-01, ERA-02) — Validated in Phase 27
- ✓ `npm test` (the nightly deploy gate) no longer reads the live owner-editable exclusions file for its CR-01 regression tests; a curation edit cannot turn the deploy red (TD-01) — Validated in Phase 31: Tech-Debt Closure
- ✓ `copyJsonTree` compares size-then-digest instead of mtime and logs stale replacements — its first real run replaced a stale same-size `geo-metadata.json` the old rule had kept (TD-02) — Validated in Phase 31
- ✓ The recount fails closed on four malformed-exclusion classes, the queue counts and renders the entries it ignored, a parity test pins the two together, and the Records note names an unrecognised guard instead of folding it into the total (TD-03) — Validated in Phase 31
- ✓ The ceiling demotion reason states its margin at three decimals, closing a shipped "4.63 m/s exceeds 4.63 m/s" (TD-04) — Validated in Phase 31
- ✓ All five calibration artifacts of record regenerated twice against the merged archive and proven idempotent; five stale hand-written figures corrected in place with dated provenance; PR-04 re-signed against `28-DIFF.md` sha256 `97e1782c…` (TD-05) — Validated in Phase 31
- ✓ No v2.2 phase carries a pre-execution validation record into the archive — 26/27/29-VALIDATION.md reflect execution, 27 flipped partial → passed after G-02 closed (TD-06) — Validated in Phase 31
- ✓ `compute-best-efforts.ts` runs a strict, deterministic three-pass shape — accumulate, derive ceiling, filter-and-flag — with no iteration to convergence (PR-01) — Validated in Phase 28: PR Plausibility Ceiling
- ✓ A per-distance personal plausibility ceiling (1.28 × p90, minimum population 100) is derived only from the already-filtered, owner-exclusion-free population, never the raw archive (PR-02) — Validated in Phase 28
- ✓ Efforts over the ceiling are demoted and flagged, never deleted, and stay visible with their reason in the activity detail view — including owner-excluded efforts, which carry both claims separately (PR-03) — Validated in Phase 28
- ✓ An archive-wide before/after PR diff (`28-DIFF.md`) was regenerated from the fixed code and re-signed by the developer against its sha256 (PR-04) — Validated in Phase 28
- ✓ The guard is validated archive-wide: 31 ceiling demotions reconciled three ways (diff, `byGuard.ceiling`, a classifier-independent sweep), with the pinned 4556693525 400m effort as a permanent regression fixture (PR-05) — Validated in Phase 28
- ✓ `npm run curate` serves a review queue at `/__curate/queue`, one nav click from any dashboard view, listing every activity with a demoted best effort — 47 rows on the 2026-09-18 archive, equal to the independent recount's activity-level count, already-excluded rows kept and marked (CUR-01) — Validated in Phase 29: Curation Review Queue
- ✓ The queue's exclude/edit/remove control imports the overlay's `saveExclusion`/`removeExclusion`/`runRecompute` transport — no second fetch site, the same origin gate, atomic write and id validator; cross-origin PUT and GET both 403 (CUR-02) — Validated in Phase 29
- ✓ Both publish guards cover the two new routes and were each shown red under a planted leak and green on the served build (`findCurationArtifacts` → `[]`, `verify-dashboard` 66/66, digest-matched) (CUR-03) — Validated in Phase 29
- ✓ Implausible altitude is detected and flagged archive-wide by three independent, total detectors — sub-ground minimum (11), loop-gated barometric closure drift (21, `LOOP_RADIUS_M = 100` derived from the archive; the original 34 was a raw-difference measurement), and per-sample vertical rate (39) — as a sixth `elevation` signal on the index row and shard that never joins the `anySevere` composite (D-06, composite still 299), badged severe-only on every list surface and disclosed always-on in the detail view; flag only, no DEM lookup, no correction (ELEV-01) — Validated in Phase 30
- ✓ The elevation cohort is validated against the whole archive: `30-CALIBRATION.md` (loop-gated union 60 of 1,865 = 3.2%, raw-definition union 71, inclusion-exclusion checked, 13 loop-gate exclusions listed by id, Suunto 9 / fēnix 6 Pro / no-device-name / vívoactive 4 all represented) is regenerable and idempotent, and `compute-elevation-recount.mjs` — which never imports the classifier and now fails closed if any row lacks the signal — reproduces it at zero delta (ELEV-02) — Validated in Phase 30
- ✓ `gear-aggregate-logic.ts` degrades into the Unknown bucket instead of crashing `slugify(undefined)` when a row's `gearName` key is absent (FIX-02) — Validated in Phase 25: CI Hardening & Light-Theme Verification
- ✓ The nightly workflow and `compute-all-stats` share one source of truth for compute-step ordering, proven by a live dispatched run (CI-01) — Validated in Phase 25: CI Hardening & Light-Theme Verification
- ✓ `verify-dashboard-publish.mjs` asserts reachability by name for six stats documents rather than trusting a directory copy (CI-02) — Validated in Phase 25: CI Hardening & Light-Theme Verification
- ✓ Phase 16's three theme/first-paint items confirmed against production: light-OS legibility, no first-paint white flash, live-follow of an OS appearance change in both directions (VER-01) — Validated in Phase 25: CI Hardening & Light-Theme Verification
- ✓ Trend charts zoom and pan via gesture and pointer-free on-screen controls, on taller bands, without regressing the five-tab structure, granularity toggle or canvas lifecycle (TRN-01..TRN-04) — Validated in Phase 23: Trends Zoom, Pan & Taller Bands
- ✓ Strava OAuth authentication and token management — v1.0
- ✓ Fetch and store run activity data from Strava API — v1.0
- ✓ Weekly km aggregation and chart widget — v1.0
- ✓ Year-over-year totals (km, runs, hours) — v1.0
- ✓ Custom time-period aggregations (monthly, yearly) — v1.0
- ✓ Streak and pattern detection (consecutive run days, time-of-day, seasonal trends) — v1.0
- ✓ Embeddable widget system for static site consumption — v1.0
- ✓ Data refresh pipeline (daily automated rebuild via GitHub Actions) — v1.0
- ✓ Geographic data extraction from activities (countries, cities from GPS coordinates) — v1.1
- ✓ Geographic statistics (runs/distance per city and country, ranked lists, CSV export) — v1.1
- ✓ Geographic table widget with sortable columns and pagination — v1.1
- ✓ Widget customization via HTML data-attributes (title, labels, colors, size, theme) — v1.1
- ✓ Dark/light mode support with auto-detection — v1.1
- ✓ Responsive container-based widget sizing — v1.1
- ✓ All widgets migrated to Custom Elements — v1.1

- ✓ Accurate city-level geocoding via GeoNames (166K cities, replacing UN/LOCODE) — v1.2
- ✓ Multi-city tracking per run using decoded route polylines — v1.2
- ✓ Single-run route map widget with zoom/pan and auto-fit — v1.2
- ✓ Multi-run overlay widget showing latest N runs — v1.2
- ✓ Route browser widget with list selection and embedded map — v1.2
- ✓ Heatmap widget with date filtering and color scheme options — v1.2
- ✓ Pin map widget with city/country toggle and visual encoding — v1.2
- ✓ Standalone full-page views for heatmap, pin map, and route browser — v1.2
- ✓ Leaflet map infrastructure with Shadow DOM CSS injection and CDN externalization — v1.2

- ✓ Committed per-activity stream data (time, distance, HR, cadence, elevation) via local backfill + daily sync, with per-channel availability manifest (STREAM-01/02/03) — v2.0
- ✓ Best-effort engine — fastest 400m..marathon computed within every run from streams (REC-01) — v2.0
- ✓ Dashboard SPA shell — hash routing, document-level theming, lazy data contract (DASH-01/02/03) — v2.0
- ✓ Activity browser and detail views — filter/sort the archive, per-run pace/HR/cadence charts, route maps, splits and zones (BROWSE-01..06, DETAIL-01..05) — v2.0
- ✓ Records, trends & differentiators — PR tables with honesty badges, PR-evolution charts, Riegel race predictions, WMA age-grading, volume/consistency/year-over-year, cadence & HR, CTL/ATL/TSB training load, per-shoe gear analysis (REC-02..07, TREND-01..05) — v2.0

- ✓ Design system & control styling — shared box treatment for every input/select/textarea, one button baseline with shared hover and disabled states, a two-tone focus ring visible on any fill, and one card/panel/grid rhythm across all five screens (UI-01, UI-02, UI-03, ACT-01) — v2.1, Phase 19
- ✓ Row-click interaction pattern — every activity row on every screen a real anchor with one keyboard stop, redundant "View Activity" CTAs removed, and modifier-click/middle-click/drag-select/double-click all deferred to the browser's own link contract via a shared `shouldNavigateOnRowClick` predicate (UX-01, UX-02, UX-03, REC-08) — v2.1, Phase 20
- ✓ Overview rebuild — Recent PRs and Recent Activities on the same shared `renderActivityRow` as the Activities list, an all-time/this-year records scope control, distance and hours this year in Headline Stats, and the Current Streak `ended {date}` sub-label (OVR-01, OVR-02, OVR-03, OVR-04, FIX-01) — v2.1, Phase 21
- ✓ Calendar week start & totals — selectable Sunday/Monday week start that persists and drives which days each week total sums, with per-week totals at the end of every row, legible down to phone widths (CAL-01, CAL-02, CAL-03) — v2.1, Phase 22
- ✓ Local curation mode — localhost-only `npm run curate` server with an inline whole-activity PR-exclusion tickbox, the write path proven absent from the published bundle by a build-time content scan and an HTTP-layer assertion (CUR-01) — v2.1, Phase 24

### Active

No active milestone — v2.2 shipped 2026-09-19. Next milestone starts with `/gsd-new-milestone`, which recreates `.planning/REQUIREMENTS.md`.

Candidates the v2.2 close explicitly handed forward (each deliberately out of scope for v2.2, recorded so it is not re-litigated):

- **Stream re-derivation from the 1 Hz originals** (STREAM-05/06 in `milestones/v2.2-REQUIREMENTS.md` § Future Requirements) — 94.5% of the archive has 1 Hz FIT/GPX locally with an unused device `speed` channel; deferred on ~200 MB of permanent git cost and because higher resolution makes efforts *faster*, so it had to follow rejection. The quality signals now exist to measure whether it would help.
- **`index-client.ts` `ParsedDashboardIndexRow` retype** — phase-sized; the parsed-row shape is typed against the producer, which is how Phase 27's G-04 (a bare `row.quality` destructure blanking the Activities list) got past the compiler.
- **CUR-04** queue dismiss action (flag as reviewed-and-kept, distinct from exclude).
- **28 WR-06** opt-in ceiling-file write gate — a developer decision on the D-07 mechanism, not a defect.
- **30 WR-02 / WR-03** elevation badge wording ("altitude -282 m below ground" reads as a double negative; stream-less activities get the "position unknown" drift line) — checkpoint-blessed, so rewording is a product call.

Carried forward, unscheduled:

- STREAM-04 (Garmin export adapter) — deferred out of v2.0, still blocked on the export arriving. Tracked in `.planning/todos/pending/`. Acknowledged at three consecutive milestone closes.
- Open code-review findings from v2.2, none shipped-behaviour blockers: `26-REVIEW.md` WR-06/WR-07, `27-REVIEW.md` WR-02/WR-03, `28-REVIEW.md` IN-05..IN-08, `29-REVIEW.md` WR-01 (unescaped stylesheet href on the local-only queue page), WR-04 (ticking a pending row exposes a no-op "Remove exclusion" that loses the typed reason), WR-05, WR-06 (`formatPace` renders `NaN:NaN/km` on undefined), `30-REVIEW.md` IN-01..IN-07, `31-REVIEW.md` IN-01..IN-08.
- Open code-review findings from v2.1, likewise non-blocking: Phase 23's 7 Warning + 5 Info (`23-REVIEW.md`), Phase 24's WR-19 (an unreadable directory throws an uncaught `EACCES` out of `findCurationArtifacts` — fails closed, so publish-safety is intact), and Phase 25's CR-01 (an unawaited CDP listener rejection leaks a Chrome child process out of `first-paint-capture.mjs`) plus WR-01..WR-04.
- Phase 16's `16-HUMAN-UAT.md` (4 pending scenarios) and `16-VERIFICATION.md` (`human_needed`) remain open as artifacts, though the three theme/first-paint items behind them were discharged against production by Phase 25's VER-01.
- The `1-fix-daily-widget-refresh-github-actions-` quick task keeps tripping `audit-open` (SUMMARY exists, no `status:` field) — three closes running. Add the field or teach the query to fall back to `requirements-completed`.

### Out of Scope

- Website redesign — this project outputs widgets, doesn't modify bacilo.github.io itself
- Social features — this is a personal dashboard, not multi-user
- Non-running activities — focus on runs only for now
- Mobile app — web widgets only
- Real-time Strava sync via webhooks — daily rebuild is sufficient
- AI training recommendations — massive scope, liability, sports science needed
- Map styling themes (topographic, neon, minimal) — deferred to future milestone
- Animated run playback on maps — deferred to future milestone
- Street View playback along runs — deferred to future milestone
- Maps as post/page backgrounds — deferred to future milestone
- Street-level geocoding — city-level is sufficient, poor accuracy at finer granularity
- Unlimited theme customization — controlled CSS variable presets, prevents layout breakage
- Every-street completion (Wandrer clone) — massive complexity, OpenStreetMap integration

## Context

Shipped v1.0 + v1.1 + v1.2 + v2.0 + v2.1 + v2.2. v2.2 added ~26,700 lines across src/scripts/CI in 69 plans (phases 26-31) over 11 days; 36,210 LOC of non-test TypeScript in src/ (up from 29,955) and 2,192 tests across 85 files (up from 1,617 across 63). Roughly a third of the new source is calibration, recount and regeneration scripts under `scripts/` — the milestone's evidence layer — and seven committed artifacts of record (`26-RESIDUAL.md`, `27-CALIBRATION.md`, `28-CEILING-CALIBRATION.md`, `28-DIFF.md`, `30-CALIBRATION.md`, `data/best-effort-ceiling.json`, and the per-activity `data/stats/pace-quality/{id}.json` shards, which carry the elevation signal too) are regenerable and proven idempotent.
v2.1 added ~15,900 lines in 103 plans (phases 19-25) over 24 days; its test count nearly doubled (884 → 1,617) with almost no new product surface — the growth is guard layers, because that milestone's requirements were all visual or interactive and the phases repeatedly found that an assertion which cannot be watched failing is not evidence.
Tech stack: TypeScript, Node.js 22, Chart.js, Leaflet 1.9.4, Vite (IIFE bundles + the dashboard SPA), Custom Elements, Shadow DOM, GitHub Actions, vitest.
1,899 run activities synced (1,874 with a computable stream); per-activity streams committed for the full archive and byte-unchanged since v2.0; data pipeline migrated off the Strava API to intervals.icu (Aug 2026). 10 Custom Element widgets + 3 standalone pages + the dashboard SPA deployed to GitHub Pages. Measured archive-quality facts as of 2026-09-19: 299 activities (15.8%) carry at least one severe pace-quality signal, 60 (3.2%) an elevation flag, 32 best efforts sit over the personal ceiling, 1 activity's metadata pace disagrees materially with its own stream.
Geographic coverage: 23 countries, 57 cities from 92% of activities (GeoNames offline geocoding, zero API calls).
Multi-city tracking: 86% of activities pass through multiple cities via polyline route sampling.
Repository: github.com/bacilo/strava-widgets (public).

**Future vision:** Map styling themes (topographic, neon, minimal), maps as post backgrounds, animated run playback, Street View playback along runs, choropleth country maps, region completion badges, pace/speed color coding on routes.

## Constraints

- **Hosting**: GitHub Pages — no server-side rendering, static assets only
- **API**: data ingestion migrated off the Strava API to intervals.icu (Aug 2026); the Strava rate limits (100 requests per 15 minutes, 1000 per day) no longer bind the daily pipeline, and data is cached locally regardless
- **Auth**: Strava OAuth refresh token flow — tokens stored in GitHub Secrets + committed tokens.json
- **Embedding**: Widgets must work within Jekyll+Astro pages as self-contained IIFE bundles
- **Geocoding**: Offline only (offline-geocoder with GeoNames cities1000) — no external API calls
- **Maps**: Leaflet externalized to CDN — widgets use global `L`, Shadow DOM CSS injection required

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Separate repo from website | Keeps data pipeline independent, cleaner separation of concerns | ✓ Good |
| Runs only for v1 | Simplifies data model, user primarily interested in running | ✓ Good |
| Daily rebuild via GitHub Actions | Simpler architecture than live API, sufficient for personal use | ✓ Good |
| Native fetch (no HTTP libraries) | Node.js 18+ built-in, fewer dependencies | ✓ Good |
| Shadow DOM for widget isolation | Host page styles cannot affect widgets | ✓ Good |
| Vite IIFE bundles | Single-file embeddability, no module loader required | ✓ Good |
| Git-tracked activity data | CI needs committed data for incremental sync | ✓ Good |
| CI token bootstrap pattern | First run uses secret, subsequent runs use committed tokens | ✓ Good |
| TDD for streak logic | Complex edge cases benefit from test-first | ✓ Good |
| UTC everywhere for dates | Timezone safety, consistent across environments | ✓ Good |
| Offline geocoding (offline-geocode-city) | Zero API calls, no rate limits, no cost, 217KB library | ✓ Good |
| Git-tracked location cache | >90% cache hit rate across CI builds, 114 unique locations | ✓ Good |
| Coordinate rounding (4 decimal places) | ≈11m precision balances accuracy with cache efficiency | ✓ Good |
| Native Custom Elements API | Zero dependencies, full attribute lifecycle control | ✓ Good |
| ResizeObserver + requestAnimationFrame | Prevents "ResizeObserver loop" browser errors | ✓ Good |
| Constructible Stylesheets for tables | Shared CSS across widget instances, reduced memory | ✓ Good |
| Non-blocking geocoding in CI | Geo failures don't halt stats pipeline | ✓ Good |
| Distance ranking over activity count | More meaningful geographic statistics | ✓ Good |
| UTF-8 BOM for CSV export | Special characters display correctly in Excel | ✓ Good |
| GeoNames over UN/LOCODE | 166K cities vs 5K, fixes suburb-instead-of-city problem | ✓ Good |
| Versioned geocoding cache (v2) | Safe migration with metadata tracking, old data archived | ✓ Good |
| Pre-computed route data (JSON) | 72% payload reduction vs loading full activity data in widgets | ✓ Good |
| Leaflet externalized to CDN | Keeps widget bundles < 50KB, shared across all map widgets | ✓ Good |
| Vite ?inline CSS for Shadow DOM | Bypasses document.head injection, CSS penetrates encapsulation | ✓ Good |
| Pre-decoded heatmap points | Zero UI blocking for 1,808 routes, trades file size for performance | ✓ Good |
| Quintile color scale for pin map | Clear visual hierarchy with 5 teal-to-orange levels | ✓ Good |
| Vite multi-page build for standalone | Clean output paths, pages load existing IIFE bundles (no duplication) | ✓ Good |
| Human browser checkpoint ends every v2.1 phase | Automated gates had missed shipped rendering defects three times; no jsdom or headless browser in the repo | ✓ Good — caught defects in 19, 20, 21, 22, 23 |
| Element selectors (`input`, `button`) over per-site classes | Reaches all 13 control and 31 button creation sites with zero TypeScript change | ✓ Good |
| Two-tone `box-shadow` focus ring over `outline` | An accent-only outline is invisible against the `--accent-strong` active fills | ✓ Good |
| Rows are real `<a>` elements, not click-decorated `<div>`s | Hands modifier-click, middle-click and drag-select back to the browser instead of reimplementing them | ✓ Good |
| One shared `renderActivityRow` across four surfaces | Duplicated row logic had already drifted between Overview and Activities | ✓ Good |
| Whole-activity PR exclusion, not per-distance (D-04) | Narrowed CUR-01 mid-phase; per-distance had no real use case | ✓ Good |
| Two independent publish guards for the curation write path | A build-time content scan and an HTTP-layer assertion fail independently; a divergence mutation passed `tsc` while failing the seam tests | ✓ Good |
| `COMPUTE_ALL_STATS_STEPS` as one compute-ordering source | The workflow's 8 hand-maintained steps had already drifted from `compute-all-stats` | ✓ Good |
| Guard assertions must be watched failing before they may pass | Mutation-proving caught several assertions that could not fail (case-blind scans, first-rule-wins CSS checks) | ✓ Good — the milestone's most load-bearing habit |
| Checkpoint rows must assert reachable extent, not internal agreement | Phase 23's CR-01 survived three clean browser rounds because every row only checked that two bands agreed with each other | ⚠️ Revisit — the lesson is recorded but not yet enforced by tooling |
| Accepting thin evidence on developer authority | Phase 22's R26/R27 closed without their own mandated observations; the shortfall is retained verbatim rather than laundered | ⚠️ Revisit — honest, but it means two closures rest on assertion rather than observation |
| Adaptive per-activity pace window (`max(20, 2.5 × p90(advance interval))`) over a fixed 20s | The fixed window itself manufactured the phantom fast mode on 60s-emission devices; adaptivity recovered all three "beyond repair" activities | ✓ Good — 153 of 154 strictly improve, residual 14 all marginal |
| Strict-priority gap classification with an exact-sum identity | Independent overlapping checks cannot sum to the stream's own span; the identity is verified across all 1,865 streams | ✓ Good |
| `data/streams/` stays byte-identical through v2.2 | Re-derivation from 1 Hz originals costs ~200 MB of git and makes efforts faster, so it must follow rejection | ✓ Good — revisit now that quality signals exist to measure it |
| Ceiling parameters measured, not chosen (`1.28 × p90`, min population 100) | Recorded with evidence in a regenerable calibration artifact; the archive drifted against the planning discussion's numbers within a week | ✓ Good |
| Demote-and-flag, never delete, through one shared demotion path | The old delete silently removed 34 efforts across 28 activities; one path means one place for the reason string | ✓ Good |
| Split an unsatisfiable criterion instead of retuning a locked threshold | Phase 27's "under ~5%" was jointly unsatisfiable with D-04; the measured 15.8% is reported, not tuned away | ✓ Good — honest, and the tension is left visible in QUAL-05 |
| Elevation excluded from the severe composite (D-06) | Keeps the Phase 27 recount byte-stable at 299 and the "any severe" filter meaning pace-trust | ✓ Good |
| A classifier-independent recount per phase, and two-run idempotence for every generated artifact | Phase 28's Round 1 passed on the defect's own output because recount and classifier shared a blind spot; Phase 27's prose correction silently reverted on regeneration | ✓ Good — the milestone's most load-bearing habit |
| Insert a tech-debt phase from the close-out audit rather than complete with the debt | Five findings shared the silent-and-passing shape the project's own lessons single out | ✓ Good — closed at source in one day, each with a demonstrated-failing test |
| Executing a milestone 402 commits ahead of an origin the nightly CI kept moving | Avoided merge churn during execution; cost one pre-close obligation (MERGE-01) and a re-sign | ⚠️ Revisit — merge origin at least per phase; the archive moved under two calibration artifacts |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-09-19 after the **v2.2 Pace Data Quality** milestone close — 6 phases (26-31), 69 plans, 168 tasks, 33/33 requirements, 11 days; audit `passed` on every axis. ~26,700 lines across src/scripts/CI; 2,192 tests across 85 files, up from 1,617 across 63. Archived to `milestones/v2.2-ROADMAP.md`, `v2.2-REQUIREMENTS.md`, and the three audits (`v2.2-INTERIM-AUDIT.md`, `v2.2-CLOSEOUT-AUDIT-2026-09-18.md`, `v2.2-MILESTONE-AUDIT.md`). Two inherited items acknowledged as deferred for the third consecutive close (STATE.md § Deferred Items → v2.2 close). This close made no corrections to phase artifacts — Phase 31 had already reconciled every stale figure the close-out audit named, and the final audit re-derived all of them at HEAD `3cc8d1db`. The per-phase history below is retained unedited.*

*Previously: 2026-09-19 — **Phase 31 (Tech-Debt Closure — Silent-Failure Guards & Docs Reconciliation) complete, added from the v2.2 close-out audit and closing the milestone's last open phase**: 10 plans across four waves (seven parallel worktree executors, one regeneration plan, one docs plan, one blocking PR-04 re-sign), `31-VERIFICATION.md` `status: passed`, 6/6, TD-01..TD-06 closed. The audit itself came back `tech_debt` (27/27 requirements, 5/5 phases, 7/7 seams, 3/3 flows, no blockers) with one pre-close obligation — MERGE-01: the whole milestone sat 402 commits ahead of origin against an archive origin had moved past. Discharged the same morning: origin merged (+9 activities → 1,899), regenerated, and exactly one record changed hands — `3475730418@1mi` (the post-Phase-28 #1 mile) tipped over the slightly lowered ceiling by 0.002 m/s, its own 400m/1k already world-record-demoted, so a correct demotion; the developer re-signed PR-04 (Round 3) and the milestone deployed. That re-sign surfaced the thin-margin reason reading "4.63 exceeds 4.63", which became TD-04. What Phase 31 changed: the four CR-01 tests no longer couple `npm test` (the nightly deploy gate) to the live exclusions file; `copyJsonTree` compares content instead of mtime and logs replacements — on its first real run it replaced a stale same-size `geo-metadata.json` the old rule had been keeping; the Records note names an unrecognized guard instead of dropping it; the recount fails closed on malformed exclusions while the queue counts and shows them, with a parity test so the two cannot silently diverge; the reason states its margin at 3 dp (`<0.001` below resolution); all five calibration artifacts regenerated twice against the merged archive (`26-RESIDUAL` scanned 1,874 not 1,875, `28-CEILING-CALIBRATION` now measures rather than asserts its 32-demotion agreement, `28-DIFF` carries the reason text and a fresh Round 4 sign-off bound to `97e1782c…`); five stale hand-written figures corrected in place with dated notes; 26/27/29-VALIDATION.md backfilled by the tool (one vacuous `-t` filter matching 0 tests found and repointed). Lessons: (1) a code review after execution again caught what the suite could not — the recount silently `continue`d past `null` entries (CR-01, fixed in-session with WR-01..03); (2) a pure `export { x } from` re-export leaves the name unbound in the exporting module's own scope, and the unit tests that import the module cannot see it — only the archive sweep did; (3) an audit's tech_debt list can carry a closed item (27 G-01) for a day — verify before scoping. Carried forward, non-blocking: `31-REVIEW.md` IN-01..IN-08 (cosmetic), and the deliberately out-of-scope set recorded in ROADMAP § Phase 31. Security enforcement is on and Phase 31 has no SECURITY.md yet — `/gsd-secure-phase 31` before the milestone closes.*

*Last updated: 2026-09-18 — **Phase 30 (Elevation Quality Signal) complete, closing the v2.2 Pace Data Quality milestone**: 8 plans across four waves, one human-verify checkpoint round (30-08, R1–R8 all PASS, R2's Overview surfaces disclosed NOT EXERCISABLE because no top-10/top-5 row is elevation-severe); `30-VERIFICATION.md` `status: passed`, 12/12, ELEV-01/ELEV-02 closed. Three total altitude detectors land as the sixth quality signal, structurally excluded from `anySevere` (the exclusion test was shown red-then-green; the Phase 27 recount still reports 299). Two measurement corrections worth remembering: the requirement's "34 closure-drift" figure was a raw start/end difference — loop-gating it against a 100 m radius derived from the archive's own start/end-distance distribution yields 21, with the 13 exclusions listed by id in `30-CALIBRATION.md`; and the D-08 carry-forward correlation measured 39/72 (54%) rather than research's preliminary 26%, spot-checked as genuine. Process notes: (1) the checkpoint round was driven by the orchestrating agent in the developer's Chrome at the developer's direction and signed off "approved" — recorded as such in `30-VALIDATION.md`, not as the developer's own observation; (2) the wave-2 post-merge suite went red only because the primary checkout's gitignored `index.json` predated elevation while the publish gate now demands six sub-keys — artifact staleness, not a code conflict, fixed by regenerating with the served digest verified; (3) code review (`30-REVIEW.md`, 2 Critical / 4 Warning / 7 Info) ran before verification and its Criticals were fixed in-session — CR-01 (unguarded `quality.elevation` reads would blank the list on a stale index row, the Phase 27 G-04 shape again, now guarded with regression tests) and CR-02 (the D-15 recount printed PASS on an index with no elevation at all; now fails closed) — plus WR-01 and WR-04. Carried forward, non-blocking: WR-02 ("altitude -282 m below ground" reads as a double negative) and WR-03 (stream-less activities get the "position unknown" drift wording) — both pinned by tests and blessed by the approved round, so rewording is a product call — and IN-01..IN-07.*

*Last updated: 2026-09-18 — **Phase 29 (Curation Review Queue) complete**: 8 plans across five waves, one human browser checkpoint round (29-08, R1–R10 all PASS, PD-01 ruled approve — keep the `index.json` name join); `29-VERIFICATION.md` `status: passed`, 3/3, CUR-01..CUR-03 closed. A review queue now lists every activity carrying a demoted best effort (all three guards), keeps the already-excluded share listed and marked, and puts the existing whole-activity exclude control on every row — every write imported from the overlay transport, never re-implemented, which `scripts/curate-queue.test.mjs` pins structurally. The checkpoint compared the rendered extent against a number derived OUTSIDE the changed code: `compute-pr-ceiling-recount.mjs` gained an activity-level count (47) with three named decoys the header had to reject (65 efforts, 28 ceiling-only, 35 flagged-minus-excluded), all four measured on the day and mutually distinct. Two lessons: (1) two plans' verify steps grepped the esbuild bundle for literal `/__curate/…` substrings that cannot exist because the overlay builds them from a `CURATE_PREFIX` template literal — an unsatisfiable check, replaced by an equivalent regex; (2) the bare root `http://127.0.0.1:4173/` is 403 by design, which reads as "broken" to a developer who has not been told the mount is `/strava-widgets/` — worth a line in the startup log. Also landed: IN-17 (one leaked path → exactly one violation) and IN-18 (regex-shape WR-17 pins). Carried forward, non-blocking (`29-REVIEW.md`, 0 Critical / 6 Warning / 5 Info): WR-01 (`renderQueuePage` writes the extracted stylesheet href unescaped — a local build artifact is the trust boundary), WR-02 (the two "independent" flagged counts disagree on duplicate / `reason: null` / `__proto__` exclusion entries; latent on the real 12-entry file), WR-03 (unreadable exclusions file is fatal to the recount verdict, the tolerant branch unreachable), WR-04 (ticking a pending row exposes "Remove exclusion", which sends a no-op DELETE and reloads, losing the typed reason), WR-05 (dead, unencoded `QueueRow.detailUrl` beside the encoding `activityDetailUrl`), WR-06 (`formatPace` renders `NaN:NaN/km` on undefined input).*

*Last updated: 2026-09-17 — **Phase 28 (PR Plausibility Ceiling) complete**: 15 plans (9 planned across six waves, plus 6 gap-closure plans across three waves), two human browser checkpoint rounds and two verification rounds; `28-VERIFICATION.md` `status: passed`, 5/5, PR-01..PR-05 closed. A personal ceiling now demotes-and-flags implausible efforts from PR ranking without deleting them. The defining event: Round 1's checkpoint and verification passed on the defect's own output — CR-01 left 13 owner-excluded, over-ceiling efforts (including the pinned fixture 4556693525) with `demotion: null`, and the recount reported "18 = 18" because both artifacts shared the blind spot. Gap closure gave the recount a classifier-independent per-effort sweep (shown FAILING on the pre-fix archive before the fix landed), applied the ceiling to excluded efforts, predicted 31 before regeneration and landed on 31/31/31, and re-ran the checkpoint with every row naming the Round 1 state as a FAIL. Carried forward, non-blocking (`28-REVIEW.md` 2026-09-17): WR-07 (calibration generator emits data-independent prose), WR-08 (`28-CEILING-CALIBRATION.md`'s Demoted column still excludes the 13 owner-excluded demotions, 18 vs 31), WR-09 (four tests read the live exclusions file; a curation edit could fail `npm test` and block the nightly deploy), IN-05..IN-08, and WR-06's one remaining stale comment.*

*Last updated: 2026-09-10 — **Phase 27 (Per-Activity Quality Signals) complete**: 12 plans across eight waves (10 planned plus two gap-closure), one human browser checkpoint round; `27-VERIFICATION.md` `status: passed`, 7/7, all seven requirements (QUAL-01..05, ERA-01, ERA-02) closed. Every activity now carries five individually-disclosed quality signals on the dashboard index row and a lazy per-activity shard, badged per-row with text that names the condition AND its measured value, filterable by a single "has any severe signal" toggle, and disclosed in full on the detail view whether or not the activity is flagged. `DASHBOARD_INDEX_SCHEMA_VERSION` is unchanged at 1 — the addition is genuinely additive. The measured composite severe rate is **299 of 1,890 activities (15.8%)**, reproduced by five independent paths. The defining event was a criterion that could not be satisfied. ROADMAP Criterion 4 required the top tier be "under ~5% (≈90 activities)", but D-04 locks severe-decimation verbatim to Phase 26's 154-activity cohort — 8.15% on its own, a floor 59 activities above the target before the other two signals contribute anything. D-02 forbids retuning toward the number. The executor surfaced the contradiction instead of tuning to fit, and the developer chose `split-the-criterion`: Criterion 4 is now 4a, a genuinely failable GATE (live denominator, classifier-independent recount per D-03, threshold demonstrated responsive in both directions), and 4b, the rate as a REPORTED FINDING. QUAL-05's "materially above ~5% is a calibration failure" language was deliberately left in visible tension with the measured rate rather than dissolved. Three structural lessons worth carrying, all variations on one theme — **every defect this phase found was caught by re-deriving a number independently, never by a test going red.** The suite was green at 73/73 throughout. (1) A correction recorded only in prose is not a correction: 27-03 fixed the stream denominator from 1,866 to 1,865 in `27-CALIBRATION.md`'s text but not in its generator, which counted `data/streams/manifest.json` as a stream; re-running the sweep to verify a checkpoint row silently reverted 42 lines including the correction itself (G-01, closed by 27-11 with a regression test and idempotence verified). Its sibling still lives in Phase 26's `compute-pace-residual.mjs:277` (G-03, out of scope, logged). (2) A guard that three neighbours already apply is the one most likely to be missed: `qualityBadgeSpecs` destructured `row.quality` bare while `rowIsAnySevere`, `rowPaceDisagreement` and `detail.ts` all guarded, and because both row render loops lack try/catch, one row lacking the key blanked the ENTIRE Activities list — the compiler missed it only because the function was typed against the producer shape rather than the parsed one whose own docblock says no key can be assumed present (G-04, Critical, closed by 27-12). (3) A test that passes can still be defending nothing: Criterion 3's explanation strings were protected only by an accidental `''`-vs-`undefined` asymmetry, proven when the new direct assertion failed on a broken state the old drift test called green (G-05, closed by 27-12). Still open, non-blocking: G-02 (ROADMAP Criterion 5 and ERA-02 cite a 716-activity no-device-name cohort; the live index has 663, the shortfall explained by better classification rather than a defect — ERA-02's actual requirement holds), G-03 above, and `27-REVIEW.md`'s WR-02/WR-03.*

*Last updated: 2026-09-10 — **Phase 26 (Shared Gap-Aware Pace Derivation & Honest Coverage) complete**: 16 plans across twelve waves, three human browser checkpoint rounds and three verification rounds; `26-VERIFICATION.md` `status: passed`, 7/7, all ten requirements (PACE-01..07, COV-01, COV-02, ERA-03) closed. The two divergent pace formulas are now one shared module, coverage is exact and always visible, and the metadata-vs-stream cross-check fires on exactly 1 of 1,890 activities. The defining lesson was again evidence rather than feature: rounds 1 and 2 both closed clean, and re-verification then found CR-03 — the pace CHART band still derived under the fixed 20s window while the histogram, caption and splits used the adaptive one, reproducing the exact 94.81%/30% phantom-fast-mode reading the phase existed to eliminate, on the phase's own exemplar. No earlier row could have caught it because none asked what the chart plots; they asked only whether two surfaces agreed with each other. Round 3 fixed that by pinning both to a value derived OUTSIDE the changed code — the 150s-adaptive series' own extent — with a tick-label discriminator (≥15:00/km) unreachable under the old window. Also closed: CR-02, where a stale `index.json` missing the `paceDisagreement` key painted a false "Pace disputed" badge and then threw in the render path. Carried forward, non-blocking: WR-06 (the single-source audit's docblock overclaims against a deliberately-obfuscated evasion) and WR-07 (`rowPaceDisagreement`'s parameter type is stricter than its own docblock), plus a logged deferred item — `copyJsonTree`'s mtime guard makes `build-widgets` silently refuse to overwrite any locally-edited `dist/` artifact, which ate a fixture restore during Round 3 and was caught only by a served-path digest check.*


*Previously: 2026-09-08 — **v2.2 Pace Data Quality** milestone opened. Scoped from a live investigation of all 1,864 committed streams rather than a feature wish-list: the pace histogram differentiates at the native sample interval while the chart beside it uses a 20s window; `derive-stream.ts`'s `MAX_SAMPLES = 3000` decimation aliases 53% of the archive; 662 activities carry at least one physically impossible sample, and the existing `WORLD_RECORD_SPEED_MPS` guard admits a 44.0s 400m. Committed streams are explicitly out of scope for modification.*

*Previously: 2026-09-05 after the **v2.1 Interface Polish** milestone close — 7 phases (19-25), 103 plans, 250 tasks, 25/25 requirements, 24 days. ~15,900 lines across src/scripts/CI; 1,617 tests across 63 files, up from 884 at the v2.0 close with almost no new product surface, the growth being guard layers.*

*Two corrections were made during the close rather than carried into the archive. `22-VERIFICATION.md` was stale — dated 2026-08-19T09:30:00Z at `gaps_found` 5/8, it was the report that triggered Phase 22's own Round 4 gap-closure work and was never re-run afterward; re-verified 2026-09-05 to `passed` 8/8, each closure re-derived from source and mutation-tested rather than accepted from the Round 4 summaries, and the prior report's central premise (a 380px-scoped overflow fix) proved factually false, the breakpoint being 640px with all three rules it named as unconditional overridden inside it. And `REQUIREMENTS.md` contradicted itself, recording CAL-01/CAL-02 as re-ticked `[x]` at lines 33-34 while the phase-map rows at 88-89 still read "Pending" — left alone, the archive would have frozen both as Pending permanently.*

*No milestone audit was run for v2.1, unlike v1.1 and v2.0. The close rests on phase-level evidence instead: all seven phases at `status: passed`, 25/25 requirements ticked, and the Phase 22 re-verification above. Five items were acknowledged as deferred (STATE.md § Deferred Items → v2.1 close), of which two are inherited v2.0 Phase 16 artifacts and one is a confirmed `audit-open` false positive.*

*The milestone's defining difficulty was never the features — it was the evidence. Across seven phases the recurring failure mode was a check that could not fail: a checkpoint row whose own mandated setup destroyed the discriminator it existed to test (Phase 24's R19 and R26), rows that asserted two bands agreed with each other while never testing whether either was reachable (Phase 23's CR-01, found by code review after three clean browser rounds), CSS assertions that any rule could satisfy, a case-blind tabindex scan, and null-override tests that passed only because the vitest environment happened to have no `globalThis.localStorage`. The habit that answered it — watch an assertion fail against the real defect before letting it pass — is now the project's most load-bearing convention, and is what the near-doubling of the test count actually bought.*

*The per-phase history below is retained unedited.*

*Previously: 2026-09-04 — Phase 25 (CI hardening & light-theme verification) complete, closing the **v2.1 Interface Polish milestone**: seven phases, 103 plans. 12 plans across nine waves, two human browser checkpoint rounds and one gap-closure round; `25-VERIFICATION.md` `status: passed` 4/4, FIX-02/CI-01/CI-02/VER-01 all satisfied. Round 1 correctly withheld all four ticks on two BLOCKED rows, and the gap-closure round closed both gaps with measured evidence rather than inference. GAP-25-01's rule was mechanism-first: measure three capture candidates against production's real 612 ms first paint, prove the winner can report WHITE on a stripped-bootstrap control, and only then draft the row — the ordering argument (inline bootstrap complete at `domInteractive` 287.9 ms plus the `node:vm` parity pin) was rejected by name as a closure route, since a round that restates a sound inference has not closed the gap. The sweep nearly produced a false pass: all nine runs' first frame is `rgb(18,18,18)`, Chrome's UA-default fill arriving before `navResponseEnd`, excluded by a stated rule and disclosed rather than applied silently. GAP-25-02's lesson was that an unsplittable row is a planning defect — splitting R6 into R6a/R6b/R6c let FIX-02 and CI-02 close on their own evidence instead of being held hostage by CI-01's never-performed dispatch, which then ran as `33903407761` (`success`, all eight step names from one invocation, enumerated from source rather than read off the log). Row-baseline integrity held: exactly four lines changed in `25-VALIDATION.md` after drafting, all four Verdict cells. Still open and NOT resolved: `25-REVIEW.md`'s CR-01 (unawaited CDP listener rejection crashes `first-paint-capture.mjs` outside `main()`'s promise chain, leaking the Chrome process and temp profile — the same session-detach failure that eliminated Candidate B), WR-01/WR-02 (no socket timeout, no child `error` listener), WR-03 (~35 unguarded `JSON.parse` calls abort the publish gate on first malformed body) and WR-04 (`src/index.ts:314` prints "All statistics generated successfully!" unconditionally, before the DEGRADED STEPS block, so it carries no discriminating information). Inherited, not introduced: a raw NUL byte in `gear-aggregate-logic.ts:143`'s Unknown-bucket sentinel, present since `feat(18-05)` 2026-08-11, making that one file binary to git and so undiffable and unblameable. Previously — 2026-09-02 — Phase 24 (Local curation mode) complete: 17 plans across ten waves, four human browser checkpoint rounds, four gap-closure rounds, `24-VERIFICATION.md` `status: passed` 4/4, CUR-01 satisfied. The phase's defining difficulty was not the feature but the *evidence*: Rounds 2 and 3 each produced a row (R19, then R26) whose own mandated setup destroyed the discriminator it was meant to test — pressing Recompute flipped `wasPRAtTheTime` back to false, so the row could no longer tell a correct implementation from one reading the stale precomputed flag. Round 4 fixed this structurally: Task 1 asserts reachability from disk (`wasPRAtTheTime === true` on the served shard, automated check, HALT before any human is asked) and pins the expected extent from `data/stats/best-efforts.json` rankings — a document the round never edits — so verdicts are judged against independently-derived data instead of the UI agreeing with itself. R32 then observed the previously-uncovered mirror direction (precomputed flag stale-TRUE + no live entry ⇒ still renders `PR — 400m`, exactly one badge) and R34 the restore, with the discriminator re-quoted from disk at the instant of observation. Two evidentiary caveats were disclosed rather than buried: R33's gestures were tool-driven (no `confirm()` gates that row) and R34's Cancel-noop sub-check rests on developer report alone. Carrying forward the Phase 23 lesson in a new shape: duplicated logic defeats checkpoints — the header-badge/panel-row divergence (WR-05/WR-17) only closed once both derivations were forced through a single exported `resolveExcluded`, and a divergence mutation was demonstrated to pass `tsc --noEmit` while failing the seam tests, which is why both guard layers exist. Still open and NOT resolved: WR-19 (unreadable *directory* throws an uncaught `EACCES` out of `findCurationArtifacts` — the directory sibling of the file case plan 24-15 fixed; fails closed, build still aborts, so publish-safety is intact) and IN-17/IN-18, captured in `.planning/todos/pending/`. Previously — 2026-08-27 — Phase 23 (Trends zoom, pan & taller bands) complete: 13 plans across nine waves, three human browser checkpoint rounds, `23-VERIFICATION.md` `status: passed` 5/5, TRN-01..TRN-04 all satisfied. Round 3 closed a clean sweep (12 PASS / 0 FAIL across R43-R54); R46(b) measured `documentElement.scrollWidth` EQUAL to `clientWidth` at all four phone widths (390/393/412/430) for the first time, closing R35 after three rounds of 682 → 460 → equal. The post-checkpoint code review then found a Critical (CR-01) that no round could have caught — `buildChannelBand` handed the zoom plugin the opening window instead of the archive bounds, capping Cadence & HR gesture zoom-out at ~5 of ~15 years; every round either zoomed inward or used buttons, and the rows that did touch the path only asserted the two bands agreed with each other. Fixed in `49add71` with a guard verified to fail when the bug is reintroduced, then closed by a real gesture walking through the old clamp (`Jul 2021 to Aug 2026`) to the archive floor (`Jul 2011 to Aug 2026`). Still open and NOT resolved: 7 Warning + 5 Info findings in `23-REVIEW.md` (notably WR-01/WR-02 stale Training Load `aria-pressed`, WR-05 a focusable `<summary>` inside the `overflow-x:auto` wrapper, WR-06 two negative CSS guards that stay green if the selector is deleted, WR-07 chart `options` objects typed `as any`), and Finding 12 (raw epoch ms in the Training Load tooltip title) deferred per `deferred-items.md`. Phone-width evidence across all three rounds rests on same-origin iframe emulation — Chrome cannot size a window below 500px — a disclosed, pre-accepted project convention. Previously — Phase 21 (Overview rebuild) complete: 8 plans in five waves, one gap-closure round, OVR-01/OVR-02/OVR-03/OVR-04/FIX-01 all verified, `21-VERIFICATION.md` re-verified 6/6 `status: passed` (superseding an earlier 5/6 `gaps_found`). Success criterion 3 could not be discharged by the archive — it holds zero current-year best-effort ranking entries in any of the seven distances, so Round 1's R7 was recorded BLOCKED against seven empty states. Round 2 closed it with a disclosed staged-only fixture in `dist/widgets/data/stats/best-efforts.json`, two 400m entries redated into 2026 with their source ranks deliberately left at `4` and `9`; the developer read back `#1` / `Mar 14, 2026` and `#2` / `Jun 2, 2026`, proving a real 1..N re-rank over the filtered subset rather than a pass-through. Fixture torn down and the staged build restored to archive values before the plan closed. `21-REVIEW.md` clean (0 critical, 0 warning, 2 info). The house rule since checkpoint 16-09 held again: the 7 unit tests over `filterRankingsToYear` were exactly what the manual row could not be discharged by. Previously — Phase 20 (row-click interaction pattern) complete: 20 plans, five gap-closure rounds, UX-01/UX-02/UX-03/REC-08 all verified, `20-VERIFICATION.md` 4/4 on a ten-row human browser checkpoint (`20-VALIDATION.md` Round 5, `status: passed`, `nyquist_compliant: true`). Two developer-accepted scope boundaries recorded rather than fixed: the Date-cell anchor is built outside `buildCellLink` so D-16's drag/click guard does not cover it, and a double-click's first click still navigates (`MouseEvent.detail` is 1 at fire time; a delay is forbidden by `row-navigation.test.ts`). One out-of-scope defect surfaced by this round's code review and recommended for separate tracking: the PR-progression Improvement column hard-codes a minus and applies `Math.abs()` at `records.ts:650-651`, inverting the sign for a non-improving step — latent, and byte-identical back to Phase 18's `d85e88a`, so it predates Phase 20.*


*Previously: 2026-08-13 — Phase 19 (design system & control styling) complete: 17 plans, four gap-closure rounds, UI-01/UI-02/UI-03/ACT-01 all verified on rendered browser evidence. One open decision carried forward: GAP 8 (Leaflet map panes paint over the nav; the sticky-layer ladder comment's totality claim is incomplete) — recorded, unpatched, disposition deferred to the user. Three latent test-helper warnings from `19-REVIEW-round4.md` are advisory and unreachable by existing tests.*

*Previously: 2026-08-12 — v2.0 Training Dashboard shipped and archived (phases 14-18, 56 plans, 29/29 requirements). Milestone audit returned `tech_debt`: no blockers, but Phase 16's three theme/first-paint UAT items remain genuinely unverified and two integration warnings (CI-vs-compute-all-stats ordering divergence; incomplete publish-verifier reachability coverage) are carried forward. See .planning/v2.0-MILESTONE-AUDIT.md and STATE.md Deferred Items.*