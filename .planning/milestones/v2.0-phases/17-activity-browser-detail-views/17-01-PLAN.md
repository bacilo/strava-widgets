---
phase: 17-activity-browser-detail-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/styles.css
  - src/dashboard/styles.test.ts
autonomous: true
requirements: [BROWSE-01, BROWSE-02, BROWSE-05, BROWSE-06, DETAIL-02, DETAIL-03, DETAIL-04, DETAIL-05]

must_haves:
  truths:
    - "Every new surface this phase renders has a design token and a class rule to hang off, defined in exactly one file"
    - "Pagination current-page and the x-axis segmented control use --accent-strong (WCAG AA safe), never raw --accent"
    - "The list switches from table to card layout at 720px, a breakpoint distinct from the existing 640px nav collapse"
    - "The splits table scrolls horizontally inside its own wrapper; the page body never scrolls horizontally"
    - "Implements CONTEXT.md locked decisions: D-01, D-08, D-26"
  artifacts:
    - path: "src/dashboard/styles.css"
      provides: "--accent-strong + --chart-* tokens, 720px breakpoint, all Phase 17 component classes"
      contains: "--accent-strong"
    - path: "src/dashboard/styles.test.ts"
      provides: "Node-environment regression guards for the new tokens and breakpoint"
      contains: "accent-strong"
  key_links:
    - from: "src/dashboard/styles.css"
      to: ":root[data-theme=\"dark\"]"
      via: "per-theme token declaration (never a bare :root-only token)"
      pattern: "data-theme=\"dark\""
    - from: "src/dashboard/styles.test.ts"
      to: "src/dashboard/styles.css"
      via: "readFileSync + declarationsFor() text assertions"
      pattern: "declarationsFor"
---

<objective>
Establish the complete CSS foundation for Phase 17 in one file, so that the six view/component
plans that follow never touch `styles.css` and can therefore run in parallel.

Purpose: `styles.css` is the single design-token source of truth (16-D04). If the list, calendar,
and detail plans each edited it, they would serialize into one long chain and collide. Owning it
in a single Wave 1 plan removes that contention and pins every visual decision from 17-UI-SPEC.md
before any markup is written.

Output: extended `src/dashboard/styles.css` (new tokens, new 720px breakpoint, all Phase 17
component classes) and extended `src/dashboard/styles.test.ts` (Node-env regression guards).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-activity-browser-detail-views/17-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --accent-strong and the chart-channel tokens, per theme</name>
  <files>src/dashboard/styles.css, src/dashboard/styles.test.ts</files>
  <read_first>
    - src/dashboard/styles.css (lines 1–60: the `:root`, `:root[data-theme="light"]`, `:root[data-theme="dark"]` token blocks — note every color token is declared THREE times: once in bare `:root` as a pre-theme fallback, once per theme block)
    - src/dashboard/styles.test.ts (the `declarationsFor()` / `cssNoComments` harness and the existing token-parity describe block)
    - .planning/phases/17-activity-browser-detail-views/17-UI-SPEC.md (§ Color — the "Accent-strong" and "Chart channel colors" tables, plus the Calendar Distance Tint Scale)
  </read_first>
  <action>
Add five new color custom properties to `src/dashboard/styles.css`, declared in all three existing
token locations exactly as `--accent` already is: the bare `:root` fallback block, the
`:root[data-theme="light"]` block, and the `:root[data-theme="dark"]` block. Bare `:root` carries
the light values (matching how `--accent: #fc4c02` is already duplicated there).

Exact values, per 17-UI-SPEC.md § Color:

| Token | Light (and bare `:root`) | Dark |
|---|---|---|
| `--accent-strong` | `#b3390a` | `#c2410c` |
| `--chart-pace` | `#fc4c02` | `#ff6b35` |
| `--chart-hr` | `#e11d48` | `#fb7185` |
| `--chart-cadence` | `#0891b2` | `#22d3ee` |
| `--chart-elevation` | `#16a34a` | `#4ade80` |

`--chart-pace` is deliberately a literal alias of `--accent` per theme (not a `var(--accent)`
reference) so a chart palette read via `getComputedStyle` returns a concrete hex.

Also add the five fixed HR-zone colors as theme-independent tokens in the bare `:root` block only
(they are identical in both themes per 17-UI-SPEC.md § HR zone colors): `--zone-1: #3b82f6`,
`--zone-2: #22c55e`, `--zone-3: #eab308`, `--zone-4: #f97316`, `--zone-5: #ef4444`.

Do NOT change any existing token value. Do NOT add a `prefers-color-scheme` media query —
`data-theme` remains the only source of truth (existing styles.test.ts asserts this).

Then extend `src/dashboard/styles.test.ts` with a new `describe` block, "styles.css — Phase 17
tokens", reusing the existing `declarationsFor()` helper, asserting: `--accent-strong: #b3390a`
present in the light theme block; `--accent-strong: #c2410c` present in the dark theme block;
all four `--chart-*` tokens present with the exact light values in the light block and the exact
dark values in the dark block; and all five `--zone-*` tokens present in the bare `:root` block.
  </action>
  <verify>
    <automated>npm test -- --run src/dashboard/styles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/dashboard/styles.test.ts` exits 0 with the new "Phase 17 tokens" describe block reporting passing tests.
    - Source assertion: `grep -c 'accent-strong' src/dashboard/styles.css` returns at least 3 (bare root, light, dark).
    - Source assertion: `sed 's|/\*.*\*/||g' src/dashboard/styles.css | grep -c 'chart-elevation'` returns at least 3.
    - Existing test `no prefers-color-scheme media query was introduced` still passes.
  </acceptance_criteria>
  <done>All five theme-scoped tokens plus five zone tokens exist in styles.css and are guarded by passing Node-environment text assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Add list, filter, pagination, and calendar component CSS with the 720px breakpoint</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - src/dashboard/styles.css (full file: existing `.view`, `.card`, `.activity-row`, `.badge`, `.cta`, `.error-state`, `.stat-grid`, `.app-nav__link[aria-current="page"]` accent-border precedent at ~lines 130-135, and the single existing `@media (max-width: 640px)` block at line 287)
    - .planning/phases/17-activity-browser-detail-views/17-UI-SPEC.md (§ Spacing Scale exceptions, § 1 Activity List, § 2 Filter Bar, § 3 Calendar, § Calendar Distance Tint Scale)
  </read_first>
  <action>
Append a clearly commented "Phase 17 — Activity browser" section to `src/dashboard/styles.css`
defining the following class contract. These exact class names are the contract that plans 17-08,
17-09, and 17-10 will build markup against — do not rename them.

List table + card switch:
`.activity-table-wrapper`, `.activity-table` (full width, `border-collapse: collapse`, header row
background `var(--surface)`), `.activity-table th` / `td` (`padding: var(--space-sm) var(--space-md)`,
`border-bottom: 1px solid var(--border)`), `.activity-table__sort-button` (full-cell transparent
`<button>`, `.text-label` sizing, `cursor: pointer`, inherits color), `.activity-table__sort-arrow`
(`color: var(--accent)`, inline-flex, only rendered on the active column),
`.activity-table tbody tr` (`cursor: pointer`; hover background a subtle darkening of `var(--surface)`),
`.activity-table__row--highlight` (background `color-mix(in srgb, var(--accent) 15%, transparent)`
with a 1.5s `transition: background-color` to transparent, for the D-08 return-from-detail flash),
and `.sort-select` (the ≤720px card-mode sort dropdown).

Layout switch (D-01, whose "roughly 700px" 17-UI-SPEC pins at 720px): add a NEW `@media (max-width: 720px)` block — distinct from and additional to the
existing 640px block, which must not be modified — that sets `.activity-table-wrapper { display: none; }`
and `.activity-list--cards { display: block; }`; and a complementary `@media (min-width: 721px)`
rule (or default + override) hiding `.activity-list--cards` and `.sort-select` above 720px. Exactly
one of the two layouts is visible at any width.

Filter bar: `.list-toolbar` (flex, wraps, `gap: var(--space-md)`), `.list-search` (text input,
full-width on narrow), `.filter-toggle` (button, `.text-label`), `.filter-panel` (`display: none`)
and `.filter-panel--open` (`display: grid`, `gap: var(--space-md)`, `background: var(--surface)`,
`padding: var(--space-md)`, `border: 1px solid var(--border)`, `border-radius: 8px`),
`.filter-field` (label + input pair), `.preset-chip` (button, pill shape).

Chips (17-UI-SPEC § 2, pinned): `.chip-row` (flex, wraps, `gap: var(--space-sm)`), `.chip`
(`background: var(--surface)`, `color: var(--text)`, `border: 1px solid var(--accent)`,
`border-radius: 999px`, `padding: var(--space-xs) var(--space-sm)`, `.text-label` sizing),
`.chip__remove` (button, `min-width: 24px`, `min-height: 24px`, `color: var(--text-secondary)`,
transparent background), `.chip-clear-all` (button, `.text-label`).

Pagination: `.pagination` (flex, `gap: var(--space-sm)`, `align-items: center`, wraps),
`.pagination__button` (`min-width: 32px`, `min-height: 32px`, `background: var(--surface)`,
`color: var(--text)`, `border: 1px solid var(--border)`), `.pagination__button--current`
(`background: var(--accent-strong)`, `color: #ffffff`, `border-color: var(--accent-strong)` —
this is the ONLY list surface allowed to use `--accent-strong`), `.pagination__ellipsis`,
`.pagination__label` (`.text-label`).

Empty state: `.empty-state` — mirror the existing `.error-state` rule's structure (centered,
`padding: var(--space-2xl) var(--space-lg)`) but as its own class so a zero-match filter result is
visually distinct from a fetch failure.

Calendar: `.calendar-header` (flex, wraps, `gap: var(--space-md)`, `align-items: baseline`),
`.calendar-nav` (button group), `.calendar-jump` (month input / select), `.calendar-grid`
(`display: grid`, `grid-template-columns: repeat(7, 1fr)`, `gap: var(--space-xs)`),
`.calendar-weekday` (`.text-label`, `color: var(--text-secondary)`, centered),
`.calendar-day` (button, `min-width: 32px`, `min-height: 32px`, `padding: var(--space-sm)`,
`border: 1px solid var(--border)`, `border-radius: 4px`, `background: transparent`,
`color: var(--text)`, `display: grid` with the day number top-left, distance centered, count
bottom-right), `.calendar-day--outside` (no border, no background, `visibility: hidden`),
`.calendar-day--rest` (`color: var(--text-secondary)`, `cursor: default`),
`.calendar-day__number` / `.calendar-day__distance` / `.calendar-day__count`,
and the four tint steps `.calendar-day--tint-1` through `.calendar-day--tint-4` with backgrounds
`color-mix(in srgb, var(--accent) 12%, transparent)`, `… 25% …`, `… 40% …`, `… 55% …`
respectively. Text color inside a tinted cell stays `var(--text)` in every step — do not add a
per-tint text-color override (17-UI-SPEC contrast directive). Add `.calendar-picker` for the
multi-run day picker panel (`background: var(--surface)`, `border: 1px solid var(--border)`,
`padding: var(--space-md)`).

Typography rule: every new class uses one of the four existing roles (`.text-body`, `.text-label`,
`.text-heading`, `.text-display`) applied in markup or their exact font-size/weight/line-height
values inline. Do not introduce a fifth size (no 12px anywhere).
  </action>
  <verify>
    <automated>npm test -- --run src/dashboard/styles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `sed 's|/\*.*\*/||g' src/dashboard/styles.css | grep -c '@media (max-width: 720px)'` returns 1.
    - Source assertion: `sed 's|/\*.*\*/||g' src/dashboard/styles.css | grep -c '@media (max-width: 640px)'` still returns 1 (the existing nav breakpoint is untouched).
    - Source assertion: every one of `.activity-table`, `.chip__remove`, `.pagination__button--current`, `.calendar-day--tint-4`, `.empty-state`, `.sort-select` appears as a rule head in styles.css.
    - Source assertion: `sed 's|/\*.*\*/||g' src/dashboard/styles.css | grep -c 'font-size: 12px'` returns 0.
    - `npm test -- --run src/dashboard/styles.test.ts` still exits 0.
  </acceptance_criteria>
  <done>All list/filter/pagination/calendar classes exist with the pinned tokens, and the 720px breakpoint coexists with the untouched 640px breakpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Add detail-page CSS — chart bands, route map, splits table, distribution bars</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - src/dashboard/styles.css (the file as extended by Task 2, plus the existing `.card` and `.stat-grid` rules being reused unchanged)
    - .planning/phases/17-activity-browser-detail-views/17-UI-SPEC.md (§ 4b Route map, § 4c Charts, § 4d Splits table, § 4e Pace distribution / HR zones, § 5 Responsive summary)
  </read_first>
  <action>
Append a "Phase 17 — Activity detail" section to `src/dashboard/styles.css` defining the class
contract that plans 17-11, 17-12, 17-13, and 17-14 will build against.

Section rhythm: `.detail-section + .detail-section { margin-top: var(--space-2xl); }` (48px between
stats header → map → charts → splits → breakdowns, per 17-UI-SPEC § Spacing).

Route map: `.route-map` (a `.card`-styled wrapper), `.route-map__canvas`
(`min-height: 220px`, `width: 100%`; inside `@media (min-width: 721px)` raise to `min-height: 320px`),
`.route-map__caveat` (`.text-label` sizing, `color: var(--text-secondary)`, `margin-top: var(--space-sm)`)
for the D-26 "Position is estimated from distance along the route, not GPS-matched" honesty line.

Chart bands: `.chart-stack` (`display: grid`, `gap: var(--space-lg)`), `.chart-band` (`.card`
styling), `.chart-band__canvas-wrap` (`position: relative`, `height: 140px`; inside a NEW
`@media (max-width: 380px)` block set `height: 112px` — the only band-height concession, and a
third distinct breakpoint alongside 640px and 720px), `.chart-band__header` (flex row: title +
overlay picker), `.overlay-picker` (flex, wraps, `gap: var(--space-sm)`, `.text-label` sizing),
`.overlay-picker__hint` (`color: var(--text-secondary)`, carries the "Up to 2" copy).

Segmented control (x-axis toggle): `.segmented` (inline-flex, `border: 1px solid var(--border)`,
`border-radius: 4px`, overflow hidden), `.segmented__option` (button,
`background: var(--surface)`, `color: var(--text-secondary)`, `padding: var(--space-xs) var(--space-md)`),
`.segmented__option--active` (`background: var(--accent-strong)`, `color: #ffffff`). This is the
second and final surface permitted to use `--accent-strong`.

Splits table: `.splits-scroll` (`overflow-x: auto`, `-webkit-overflow-scrolling: touch`, contained
inside the splits `.card`; the page body must never scroll horizontally, so do not add any rule
that widens `body` or `.view`), `.splits-table` (`border-collapse: collapse`, `min-width: 640px`
so the wrapper actually scrolls on a phone rather than crushing seven columns),
`.splits-table th`/`td` (`padding: var(--space-sm) var(--space-md)`, `white-space: nowrap`),
`.splits-table__km` (`position: sticky`, `left: 0`, `width: 64px`, `background: var(--surface)`,
`z-index: 1` — an opaque background is mandatory or rows scroll through it),
`.splits-table__partial` (`font-style: italic`; still `.text-label` size and weight 400 — italic is
a style, not a fifth weight).

Pace comparison bar (`vs. Avg` column): `.pace-bar` (`position: relative`, `height: 8px`,
`min-width: 96px`), `.pace-bar__track` (`background: var(--border)`, full width),
`.pace-bar__tick` (1px `var(--text-secondary)` vertical rule at 50%, the activity-average anchor),
`.pace-bar__fill` (`background: var(--chart-pace)`, absolutely positioned, extends left or right
from the 50% tick).

Distribution and zone bars (both hand-rolled DOM bars, no canvas): `.distribution`
(`display: grid`, `gap: var(--space-xs)`), `.distribution__row` (grid: label / bar / value),
`.distribution__label` (`.text-label`, `white-space: nowrap`), `.distribution__bar`
(`height: 16px`, `background: var(--chart-pace)`, width set inline as a percentage by the renderer),
`.distribution__value` (`.text-label`, `color: var(--text-secondary)`), and five zone modifiers
`.distribution__bar--zone-1` … `--zone-5` whose backgrounds are `var(--zone-1)` … `var(--zone-5)`.

Prev/next detail nav: `.detail-nav` (flex, `gap: var(--space-md)`, `.text-label` links).
  </action>
  <verify>
    <automated>npm test -- --run src/dashboard/styles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: every one of `.chart-band__canvas-wrap`, `.segmented__option--active`, `.splits-scroll`, `.splits-table__km`, `.pace-bar__fill`, `.distribution__bar--zone-5`, `.route-map__caveat`, `.detail-section` appears as a rule head in styles.css.
    - Source assertion: `sed 's|/\*.*\*/||g' src/dashboard/styles.css | grep -c '@media (max-width: 380px)'` returns 1.
    - Source assertion: `sed 's|/\*.*\*/||g' src/dashboard/styles.css | grep -c 'var(--accent-strong)'` returns exactly 2 — the only two consumer rules are `.pagination__button--current` and `.segmented__option--active`.
    - Source assertion: `sed 's|/\*.*\*/||g' src/dashboard/styles.css | grep -cE '^[[:space:]]*--accent-strong[[:space:]]*:'` returns exactly 3 (bare `:root`, light theme, dark theme declarations).
    - Source assertion: `sed 's|/\*.*\*/||g' src/dashboard/styles.css | grep -c 'overflow-x: auto'` returns 1 and that rule belongs to `.splits-scroll`.
    - `npm test` (full suite) exits 0.
  </acceptance_criteria>
  <done>All detail-page classes exist, `--accent-strong` is consumed by exactly the two permitted controls, and the horizontal-scroll containment rule is scoped to `.splits-scroll` alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static CSS → browser rendering | No untrusted input crosses this boundary; `styles.css` is authored, committed, and served as a static asset over HTTPS from GitHub Pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-CSS-01 | Information Disclosure | `.calendar-day--tint-*` opacity wash | accept | Tint level is derived from already-public distance data; no new disclosure surface beyond what `data/dashboard/index.json` already publishes |
| T-17-CSS-02 | Tampering | `color-mix()` / `var()` token indirection | accept | No user-controlled value is ever interpolated into a CSS declaration in this plan; all values are literal hex authored at build time |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (17-RESEARCH.md § Package Legitimacy Audit: "installs no new external packages"); nothing to gate |
</threat_model>

<verification>
- `npm test` full suite green (373 existing tests plus the new token guards).
- `npm run build-widgets` completes and `dist/widgets/dashboard/assets/*.css` contains `--accent-strong`.
- No rule outside `.splits-scroll` introduces horizontal overflow.
</verification>

<success_criteria>
- `--accent-strong` and the four `--chart-*` tokens are declared in the bare `:root`, light, and dark blocks.
- Exactly two rules consume `var(--accent-strong)`.
- Three distinct breakpoints coexist: 640px (pre-existing nav), 720px (new list switch), 380px (new chart band height).
- Every class name listed in Tasks 2 and 3 exists, so downstream plans never need to edit `styles.css`.
</success_criteria>

<output>
Create `.planning/phases/17-activity-browser-detail-views/17-01-SUMMARY.md` when done
</output>
