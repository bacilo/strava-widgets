---
phase: 18-records-trends-differentiators
plan: 15
type: execute
wave: 5
depends_on: ["18-10", "18-14"]
files_modified:
  - src/dashboard/views/trends.ts
  - src/dashboard/views/trends-charts.ts
autonomous: true
requirements: [TREND-03, TREND-04, TREND-05]
must_haves:
  decisions: [D-03, D-13, D-14, D-15, D-16, D-18, D-19]
  truths:
    - "Monthly average cadence and heart rate render as two stacked charts whose x-axes start at the same pixel, with a month that has no data appearing as a gap"
    - "A fitness and freshness chart shows CTL, ATL and TSB over a selectable window, switchable between two transparent TRIMP models without refetching"
    - "Spans where runs happened but carried no heart rate are shaded and captioned as absence, not as zero training"
    - "The Gear tab lists every shoe including an Unknown row, states real coverage in plain numbers, and charts at most eight shoes plus an Other bar"
  artifacts:
    - path: "src/dashboard/views/trends-charts.ts"
      provides: "Cadence/HR stacked bands, training-load chart with coverage shading, gear bar chart"
      exports: ["mountChannelBands", "mountTrainingLoadChart", "mountGearChart"]
  key_links:
    - from: "src/dashboard/views/trends-charts.ts"
      to: "chart-theme.ts Y_AXIS_WIDTH_PX"
      via: "pinned y-gutter on both stacked bands"
      pattern: "Y_AXIS_WIDTH_PX"
---

<objective>
Ship the remaining three Trends tabs: Cadence & HR, Training Load, and Gear (D-03, D-13, D-14, D-15, D-16, D-18, D-19; TREND-03, TREND-04, TREND-05).

Purpose: two of Phase 17's shipped defects live in this territory. GAP 2 was stacked chart bands whose x-axes did not line up because each auto-sized its own y-gutter — the Cadence & HR tab reuses the pinned-gutter fix **preemptively** rather than rediscovering it. And D-15's shaded no-HR spans are this phase's single most load-bearing honesty requirement: the user's verbatim constraint is that absences be visible and never made up.
Output: all five Trends tabs live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-records-trends-differentiators/18-UI-SPEC.md
@.planning/phases/18-records-trends-differentiators/18-PATTERNS.md
@.planning/phases/18-records-trends-differentiators/18-CONTEXT.md
@.planning/phases/18-records-trends-differentiators/18-10-SUMMARY.md
@.planning/phases/18-records-trends-differentiators/18-14-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Cadence & HR tab — two stacked bands with one pinned gutter</name>
  <files>src/dashboard/views/trends-charts.ts, src/dashboard/views/trends.ts</files>
  <read_first>
    - `src/dashboard/views/detail-charts.ts` — the `Y_AXIS_WIDTH_PX` constant's **full rationale comment** (moved to `chart-theme.ts` by plan 18-04) and its application at `scale.width = Y_AXIS_WIDTH_PX` (~line 514); also the `.chart-band` / `.chart-stack` / `.chart-band__canvas-wrap` markup this tab mirrors
    - `.planning/STATE.md` § Blockers/Concerns — Phase 17 GAP 2, still OPEN: chart band x-axis origins were not vertically aligned because each band auto-sized its own y-gutter to its widest tick label
    - `src/dashboard/views/trends-cadence-hr-logic.ts` (plan 18-10) — `buildMonthlyChannelSeries`, `MONTHLY_CHANNELS`, `channelLabel`, and `MonthlyPoint.value: number | null`
    - `src/dashboard/views/chart-theme.ts` — `resolveToken`, `resolveThemeColors`, `Y_AXIS_WIDTH_PX`
    - `18-UI-SPEC.md` § 10 — two stacked single-axis charts, never one dual-axis chart; the pinned-gutter reuse described as "a mandatory reuse, not a suggestion"; `spanGaps: false`; the two canvas `aria-label`s
  </read_first>
  <action>
Add `mountChannelBands(root: HTMLElement, cadence: MonthlyPoint[], hr: MonthlyPoint[]): { destroy(): void }` to `src/dashboard/views/trends-charts.ts`.

Build **two** stacked single-axis line charts sharing one x-axis meaning (month), reusing the existing `.chart-stack` / `.chart-band` / `.chart-band__canvas-wrap` markup — **not** one dual-axis chart. Dual axes are a readability anti-pattern this codebase already rejected once (17-D19's "no competing right-hand tick labels" rule for detail overlays); reapply the same discipline.

**The gutter is the load-bearing detail.** Both bands set `scale.width = Y_AXIS_WIDTH_PX` from `chart-theme.ts` in an `afterFit`-style hook, exactly as `detail-charts.ts` does. Cadence labels (`170`) and HR labels (`142`) happen to be similar widths today, which is precisely why an executor might skip the pin — and why a future channel with wider labels would silently reintroduce Phase 17's GAP 2. Pin it unconditionally and add a comment saying so.

Per band:
- `type: 'line'`, x-axis `type: 'linear'` over epoch-ms month starts with a `ticks.callback` formatting `MMM YYYY`. **Never** a `TimeScale`.
- **`spanGaps: false`** and `null` values passed through from `MonthlyPoint.value` — a month with no qualifying data must be a genuine **gap in the line**, never a zero and never an interpolated bridge (§ 10, and D-15's principle applied to a new context). Do not filter the `null` points out of the dataset; filtering them would close the gap.
- Cadence band colour from `resolveToken('--chart-cadence', ...)`, HR band from `resolveToken('--chart-hr', ...)`.
- Canvas `aria-label`s: `Average cadence by month chart` and `Average heart rate by month chart`.
- Band headings from `channelLabel(...)` — the cadence heading must state the single-leg rpm unit, matching the detail view's existing `Cadence (rpm, single-leg)` stat-card label, because the index value is deliberately not doubled.
- Tooltip shows the value plus the contributing-run count, so a reader can see why a month is thin.
- One idempotent `destroy()` destroying both.

In `trends.ts`, build the Cadence & HR tabpanel: compute both series from the already-fetched index rows via `buildMonthlyChannelSeries`, mount the bands, and destroy them on tab switch (§ 7's locked destroy-and-rebuild lifecycle). Below the bands, a `.text-label` caption stating how many months have no data for each channel, derived from the series — plain numbers, not a hardcoded claim.
  </action>
  <verify>
    <automated>npm run build && npm run build-widgets</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` and `npm run build-widgets` exit 0.
    - `grep -c "Y_AXIS_WIDTH_PX" src/dashboard/views/trends-charts.ts` returns at least `2` (used by both bands, plus any earlier chart).
    - `grep -c "spanGaps: false" src/dashboard/views/trends-charts.ts` returns at least `1` and `grep -c "spanGaps: true" src/dashboard/views/trends-charts.ts` returns `0`.
    - `grep -cE "\.filter\(.*value !== null|\.filter\(.*value != null" src/dashboard/views/trends.ts src/dashboard/views/trends-charts.ts` returns `0` (nulls are passed through, not filtered out — filtering would close the gap).
    - `grep -c "Average cadence by month chart" src/dashboard/views/trends-charts.ts` returns `1` and `grep -c "Average heart rate by month chart" src/dashboard/views/trends-charts.ts` returns `1`.
    - `grep -c "single-leg" src/dashboard/views/trends-charts.ts src/dashboard/views/trends.ts` returns at least `1`.
    - `grep -cE "yAxisID|position: 'right'" src/dashboard/views/trends-charts.ts` returns `0` (no dual-axis chart).
    - Manual smoke recorded in the summary: both bands render, their plot areas begin at the same x offset, and at least one visible line gap is present. Full visual verification is plan 18-16's checkpoint.
  </acceptance_criteria>
  <done>Cadence and HR render as two aligned stacked bands with genuine gaps for months that have no data, with Phase 17's gutter fix applied preemptively rather than rediscovered.</done>
</task>

<task type="auto">
  <name>Task 2: The Training Load tab — CTL/ATL/TSB, model toggle, window control, and coverage shading</name>
  <files>src/dashboard/views/trends-charts.ts, src/dashboard/views/trends.ts</files>
  <read_first>
    - `src/dashboard/views/detail-charts.ts` lines 231-254 — `createCrosshairPlugin`: the exact local, **per-instance** plugin shape (`id`, `afterDraw`, registered via the chart's own `plugins: [...]` array and **not** via `Chart.register`) that the coverage-shading plugin must mirror
    - `src/dashboard/views/detail-charts.ts` ~line 540 — the `Filler`-based alpha-blended area fill (`backgroundColor: hexToRgba(palette[channel], 0.18)`) the CTL series reuses
    - `src/dashboard/views/trends-training-load-logic.ts` (plan 18-10) — `parseTrainingLoad`, `parseTrimpModel`, `parseLoadWindow`, `TRAINING_LOAD_WINDOWS`, `sliceLoadWindow`, `selectModelSeries`, `findThinCoverageSpans`, `coverageCaption`
    - `src/dashboard/views/chart-theme.ts` — `resolveToken`, `hexToRgba`, `Y_AXIS_WIDTH_PX`
    - `.planning/phases/18-records-trends-differentiators/18-07-SUMMARY.md` and `18-10-SUMMARY.md` — the live spine length, model availability, and the observed coverage-span count and shaded-day total
    - `18-UI-SPEC.md` § 11 — the three series and their tokens, the Edwards/Banister `.segmented` toggle with `role="group"` + `aria-pressed` (correct here, unlike the outer tablist), the 3mo/12mo/All window control, the **flat low-opacity rectangle** shading (not a hatch), the caption copy, the disabled-Banister treatment, and the canvas `aria-label`
    - `.planning/phases/18-records-trends-differentiators/18-01-SUMMARY.md` — the locked config-path deviation (`data/private/athlete-private.json`) that the training-load config notice must name
  </read_first>
  <action>
Add to `src/dashboard/views/trends-charts.ts`:

`createThinCoverageShadingPlugin(getSpans: () => CoverageSpan[], color: string): Plugin<'line'>` — a sibling of `createCrosshairPlugin` in exactly the same shape: a local plugin with an `id` and an `afterDraw` hook, registered through each chart instance's own `plugins: [...]` array, **never** via `Chart.register`. For each span it computes `xScale.getPixelForValue(span.startX)` and `...(span.endX)`, guards both for finiteness, and fills a **flat rectangle** from `chartArea.top` to `chartArea.bottom` at low opacity. A flat fill, not a diagonal hatch — a flat rectangle is trivially verifiable in a screenshot and a hand-rolled hatch is one more canvas primitive this phase does not need to risk getting subtly wrong.

`mountTrainingLoadChart(canvas: HTMLCanvasElement, points: LoadPoint[], spans: CoverageSpan[]): { destroy(): void }`:
- Three datasets over `{ x: epochMs, y }`: **CTL** as a filled area using `Filler` with `hexToRgba(resolveToken('--load-ctl', ...), 0.18)`, **ATL** as a plain line in `--load-atl`, **TSB** as a plain line in `--load-tsb`.
- x-axis `type: 'linear'` with a `ticks.callback` formatting `MMM YYYY`. **Never** a `TimeScale`.
- y-axis pinned to `Y_AXIS_WIDTH_PX`.
- `pointRadius: 0` (the series can carry 5,000+ days) and Chart.js's built-in `Decimation` with the LTTB algorithm, mirroring `detail-charts.ts`'s `DECIMATION_CONFIG`. Register `Decimation` if it is not already registered in this module.
- Canvas `aria-label`: `Training load chart: CTL, ATL, and TSB over time`.
- The shading plugin in the instance's own `plugins` array.
- Idempotent `destroy()`.

In `trends.ts`, build the Training Load tabpanel:
- Fetch `data/stats/training-load.json` (this tab's own fetch, guarded by the `requestToken` check) and parse with `parseTrainingLoad`. A `null` result renders a named empty state, not an empty canvas.
- A `.segmented` **TRIMP model** toggle with `role="group"` + `aria-pressed`, options `Edwards` and `Banister`, defaulting to Edwards (D-14). Switching is a **pure client-side re-render**: both series live in the one fetched document, so the toggle never refetches. Destroy and rebuild the chart on switch.
- When the document reports `models.banister === false`, the Banister option is **`disabled`, not hidden** — the model still exists, it just is not configured — and a `.config-notice` renders the document's own `banisterDisabledReason`, falling back to `Training load is off — add birthDate, sex, and restingHr to data/private/athlete-private.json to enable it.` (the path per plan 18-01's locked deviation, superseding 18-UI-SPEC's literal `data/config/athlete.json` string). Edwards stays fully functional — its only input is the five bpm boundaries already in the public config.
- A `.segmented` **window** control with `3mo` / `12mo` / `All`, defaulting to `12mo` via `parseLoadWindow`. It scopes only what is displayed; the underlying series always covers the full archive (D-16). Rebuild the chart on change.
- Compute spans with `findThinCoverageSpans` over the **windowed** slice so the shading matches what is on screen, and pass them to the plugin.
- Directly below the chart, a `.text-label` caption from `coverageCaption(spans)` — the exact string `Shaded regions indicate no HR data (not zero training).` when any span exists, and nothing when none do. The shading and the words say the same thing twice, deliberately: this is the phase's single most load-bearing honesty requirement.
- If `selectModelSeries` returns an empty array (a model with no data), render the named empty state rather than an empty chart — never a flat line at zero.
  </action>
  <verify>
    <automated>npm run build && npm run build-widgets && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - All three commands exit 0.
    - `grep -c "Chart.register" src/dashboard/views/trends-charts.ts` returns `1` (one registration call for the module), and the coverage-shading plugin is **not** among its arguments — confirm by inspection that it is passed via an instance `plugins:` array instead.
    - `grep -c "afterDraw" src/dashboard/views/trends-charts.ts` returns at least `1`.
    - `grep -cE "setLineDash|createPattern|hatch" src/dashboard/views/trends-charts.ts` returns `0` (flat rectangle, not a hatch).
    - `grep -c "\-\-load-ctl" src/dashboard/views/trends-charts.ts`, `--load-atl`, and `--load-tsb` each return at least `1`.
    - `grep -c "Training load chart: CTL, ATL, and TSB over time" src/dashboard/views/trends-charts.ts` returns `1`.
    - `grep -c "Shaded regions indicate no HR data (not zero training)." src/dashboard/views/trends.ts` returns `0` — the string lives only in `trends-training-load-logic.ts` and reaches the view through `coverageCaption`, so it has exactly one home.
    - `grep -c "data/private/athlete-private.json" src/dashboard/views/trends.ts` returns at least `1` (the fallback notice names the correct path).
    - `grep -c "disabled = true\|\.disabled" src/dashboard/views/trends.ts` returns at least `1` (the Banister option is disabled, not removed) and no code path removes or hides that button.
    - `grep -cE "TimeScale|chartjs-adapter" src/dashboard/views/trends-charts.ts` returns `0`.
    - Manual smoke recorded in the summary: the chart renders three series over the default 12-month window; the window control changes the visible range; with no private config the Banister option is visibly present and disabled with the notice showing; at least one shaded region is visible somewhere in the `All` window along with the caption.
  </acceptance_criteria>
  <done>Fitness and freshness render over a selectable window with a transparent, switchable model, and the ~180 no-HR runs appear as shaded, captioned absence rather than as a training valley.</done>
</task>

<task type="auto">
  <name>Task 3: The Gear tab — sortable table, coverage sentence, and a bounded bar chart</name>
  <files>src/dashboard/views/trends-charts.ts, src/dashboard/views/trends.ts</files>
  <read_first>
    - `src/dashboard/views/list.ts` — `buildHeaderRow` and the click-to-sort column-header pattern including `buildSortArrowSvg` and the `aria-sort` handling; this is genuine full reuse against a ≤17-row dataset, not a re-derivation
    - `src/dashboard/views/trends-gear-logic.ts` (plan 18-10) — `parseGearAggregate`, `parseGearSort`, `GEAR_SORT_KEYS`, `sortShoes`, `buildGearChartBuckets`, `GEAR_CHART_MAX_CATEGORIES`, `coverageSentence`
    - `src/dashboard/views/chart-theme.ts` — `resolveToken`, `Y_AXIS_WIDTH_PX`
    - `.planning/phases/18-records-trends-differentiators/18-05-SUMMARY.md` and `18-10-SUMMARY.md` — the live per-year coverage table and the generated coverage sentence
    - `18-UI-SPEC.md` § 12 — the seven table columns, the always-present Unknown row, the `Shoe N` fallback, the `--cat-1..8` top-8 bar chart plus a neutral merged Other bar, the caption, and the canvas `aria-label`
  </read_first>
  <action>
Add `mountGearChart(canvas: HTMLCanvasElement, buckets: GearChartBucket[]): { destroy(): void }` to `src/dashboard/views/trends-charts.ts`:
- Bar chart, x-axis `type: 'category'` over bucket labels, one bar per bucket, height = `distanceM / 1000`.
- Named buckets take `--cat-1` through `--cat-8` in order via `resolveToken`. The **Other** bucket takes `hexToRgba(resolveToken('--text-secondary', ...), 0.5)` — deliberately outside the eight-colour budget, because "Other" is not itself a category.
- y-axis pinned to `Y_AXIS_WIDTH_PX`, `ticks.callback` appending ` km`.
- Canvas `aria-label`: `Total distance by shoe chart, top 8 shown`.
- Idempotent `destroy()`.

In `trends.ts`, build the Gear tabpanel:
- Fetch `data/stats/gear-aggregate.json` (this tab's own fetch, `requestToken`-guarded) and parse with `parseGearAggregate`. A `null` result renders a named empty state.
- **Coverage sentence first**, as a `.text-body` line above everything else: `coverageSentence(totals, byYear)`. D-18 requires the section state its real coverage in plain numbers; putting it above the chart means a reader sees the caveat before the picture.
- **Bar chart** from `buildGearChartBuckets(shoes)`, with its caption (`Top 8 shoes shown by distance; see the table below for all {n}.`) rendered as a `.text-label` directly below.
- **Sortable table** (`.activity-table.pr-table`) with columns Shoe | Distance | Runs | Avg Pace | Avg HR | Date Range | Coverage %, reusing `list.ts`'s click-to-sort header pattern with `aria-sort` on the active column. Sorting calls `sortShoes`, which pins the Unknown row last in every direction. Numeric columns get `.pr-table__numeric`.
  - Distance in km to one decimal; Avg Pace via `formatPace`; Avg HR rounded or an em-dash when `null` (never `0`); Date Range via `formatActivityDate` on `firstDate`/`lastDate`, or an em-dash when absent; Coverage % is that shoe's share of total geared distance.
  - The **Unknown row always renders** when its run count is greater than zero, with no special styling beyond the literal label — its mere presence, not a colour, is what makes the absence honest.
- Re-sorting rebuilds nothing on the canvas (the chart is unaffected by table sort order); changing tabs destroys the chart per the standard lifecycle.
- Every label written with `textContent`. Shoe names are blank in the config today but will be athlete-typed free text once filled in, so this must not regress to `innerHTML`.
  </action>
  <verify>
    <automated>npm run build && npm test && npm run build-widgets && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - All four commands exit 0.
    - `grep -c "Total distance by shoe chart, top 8 shown" src/dashboard/views/trends-charts.ts` returns `1`.
    - `grep -cE "\-\-cat-[1-8]" src/dashboard/views/trends-charts.ts` returns at least `1` and the token index is derived from the bucket position rather than eight hardcoded branches.
    - `grep -c "innerHTML" src/dashboard/views/trends.ts` returns `0`.
    - `grep -cE "'Unknown'|\"Unknown\"" src/dashboard/views/trends.ts` returns `0` — the label comes from the data (`UNKNOWN_GEAR_LABEL` via the aggregate), never from a literal in the view.
    - `grep -cE "\b62\b|\b19\b|1,160" src/dashboard/views/trends.ts` returns `0` (coverage numbers are derived, never hardcoded).
    - `grep -c "aria-sort" src/dashboard/views/trends.ts` returns at least `1`.
    - Manual smoke recorded in the summary: the Gear tab shows a coverage sentence with real numbers, a bar chart with at most 8 named bars plus one Other bar, and a table listing all 16 shoes plus a non-zero Unknown row that stays last after clicking every sortable column header.
    - Cycle all five tabs twice and confirm **no** `Canvas is already in use` error appears in the console. Record the observation.
  </acceptance_criteria>
  <done>The Gear tab states its real coverage before showing anything, charts at most eight shoes plus an honest Other bar, and keeps every shoe — and every ungeared run — visible in the table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched public JSON → canvas and DOM | Two more published documents are parsed and rendered |
| athlete-typed shoe names → DOM | Blank today, free text once the developer fills in `data/config/gear.json` |
| control state → chart configuration | Model and window selections drive series selection and slice bounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-XSS-01 | Tampering / XSS | shoe labels, all rendered text | mitigate | `textContent` only; `grep -c innerHTML src/dashboard/views/trends.ts` returns `0`. Gear names are blank today but will carry athlete-typed text, so this must not regress (T-16-VW-01) |
| T-18-TAB-02 | Tampering | model and window controls | mitigate | `parseTrimpModel` and `parseLoadWindow` allow-list before use (plan 18-10), and `parseTrimpModel` additionally clamps to `edwards` when the document reports Banister unavailable, so no supplied value can yield an empty chart |
| T-18-CANVAS-01 | Denial of Service (runtime crash) | Chart.js canvas reuse | mitigate | Destroy-and-rebuild on every tab switch and every control change; the acceptance criteria require cycling all five tabs twice with a clean console |
| T-18-HONEST-07 | Spoofing (fabricated data) | coverage shading, empty months, Unknown row, null avg HR | mitigate | Shading marks only genuine no-HR-**run** spans (plan 18-10 test case 7 proves a rest gap is not shaded) and is captioned in words; empty months are line gaps not zeros; the Unknown row always renders; a null average HR is an em-dash, never `0` |
| T-18-HONEST-08 | Repudiation (hidden model) | disabled Banister option | mitigate | The option is `disabled`, never hidden, with an actionable notice naming the exact file and fields — the 17-D31 precedent extended by D-13 |
| T-18-A11Y-04 | Information Disclosure (of meaning) | coverage shading, gear table | mitigate | The shading's meaning is duplicated as visible caption text, and the gear table carries `aria-sort`; no fact exists only inside a Chart.js tooltip |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs authorized. The shading plugin is hand-written in the codebase's own local-plugin idiom; no annotation or plugin package is approved. An executor that concludes an install is necessary must stop and run the Package Legitimacy Gate |
</threat_model>

<verification>
- `npm run build`, `npm test`, `npm run build-widgets`, `npm run verify-dashboard` all exit 0.
- All five Trends tabs mount, and cycling them twice produces a clean console.
- Gutter alignment, gap visibility, shading distinguishability and both themes are **manual-only** and belong to plan 18-16's checkpoint.
</verification>

<success_criteria>
All five Trends tabs are live, and every place this phase could have quietly invented data — a month with no runs, a stretch with no heart rate, a run with no shoe, a model with no config — shows an honest, named absence instead.
</success_criteria>

<output>
Create `.planning/phases/18-records-trends-differentiators/18-15-SUMMARY.md` when done. Record the smoke-check observations per tab, the observed number of visible shaded regions in the `All` window, and confirm explicitly that cycling all five tabs twice produced no `Canvas is already in use` error.
</output>
