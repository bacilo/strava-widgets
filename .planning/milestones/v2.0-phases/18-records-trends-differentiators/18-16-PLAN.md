---
phase: 18-records-trends-differentiators
plan: 16
type: execute
wave: 6
depends_on: ["18-11", "18-12", "18-13", "18-14", "18-15"]
files_modified:
  - .planning/phases/18-records-trends-differentiators/18-VALIDATION.md
autonomous: false
requirements: [REC-02, REC-03, REC-04, REC-05, REC-06, REC-07, TREND-01, TREND-02, TREND-03, TREND-04, TREND-05]
must_haves:
  decisions: [D-12]
  truths:
    - "Every rendering behaviour this phase adds has been confirmed by a human in a real browser, under a production-shaped URL"
    - "The age-grade percentages have been cross-checked against an independent external calculator"
    - "The validation record states which requirements were confirmed and which surfaced defects, verbatim"
  artifacts:
    - path: ".planning/phases/18-records-trends-differentiators/18-VALIDATION.md"
      provides: "Completed per-task verification map and the human checkpoint record"
      contains: "nyquist_compliant"
  key_links:
    - from: "18-VALIDATION.md"
      to: "18-UI-SPEC.md § 19"
      via: "every checklist item resolved with a verbatim observation"
      pattern: "§ 19"
---

<objective>
Close the phase with the verification the automated suite structurally cannot do: a human, in a real browser, against a production-shaped URL — and reconcile the validation record with what was actually built.

Purpose: this is the phase's actual gate. **Phase 16 shipped a black page behind a 15/15 green gate. Phase 17 shipped two rendering defects behind 592/592 tests, a clean `tsc`, and 20/20 `verify-dashboard`.** Both escapes were rendering defects invisible to automated checks, and Phase 17's two are still OPEN. A five-tab, ~15-chart phase with no jsdom and no headless browser is maximally exposed to exactly this class.
Output: a completed validation record and a signed-off (or explicitly PARTIAL) checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-records-trends-differentiators/18-VALIDATION.md
@.planning/phases/18-records-trends-differentiators/18-UI-SPEC.md
@.planning/phases/18-records-trends-differentiators/18-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full gate and reconcile the validation record</name>
  <files>.planning/phases/18-records-trends-differentiators/18-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/18-records-trends-differentiators/18-VALIDATION.md` — the whole file: the Per-Task Verification Map with its `TBD` rows, the Wave 0 Requirements checklist, the Manual-Only Verifications table, and the Validation Sign-Off block
    - Every `18-*-SUMMARY.md` produced by plans 01-15 — the actual test files created, the mutation-check observations, and the live-archive measurements recorded
    - `.planning/phases/18-records-trends-differentiators/18-01-SUMMARY.md` — the locked config-path deviation, which supersedes one Wave 0 item (see below)
  </read_first>
  <action>
Run the complete gate from a clean state and record every exit code:
`npm run build && npm test && node dist/index.js compute-all-stats && npm run build-widgets && npm run verify-dashboard`

Then update `.planning/phases/18-records-trends-differentiators/18-VALIDATION.md`:

1. **Per-Task Verification Map** — replace every `TBD` with the real plan number, wave, threat ref, and the automated command that actually exists. Set each row's `File Exists` to ✅ and `Status` to ✅ green (or ❌/⚠️ with a note). Add rows for the tests that were created beyond the original map: `gear-naming.test.ts`, `gear-aggregate-logic.test.ts`, `athlete-private.test.ts`, `compute-training-load.test.ts`, `compute-age-grading.test.ts`, `detail-best-efforts-logic.test.ts`, `age-grading-client.test.ts`, `trends-logic.test.ts`.

2. **Wave 0 Requirements** — tick every delivered item. **One item is superseded and must be marked so, not silently ticked:** the entry reading "`src/dashboard/data/athlete-config-client.test.ts` — extend with cases for the new `birthDate` / `sex` / `restingHr` fields (D-12)". Plan 18-01's locked deviation moved those fields out of `data/config/athlete.json` entirely, into the gitignored, never-published `data/private/athlete-private.json`, because the repo is public and `copyDataFiles` wholesale-copies `data/config/`. The public `athlete.json` and its client therefore gained **no** new fields and needed no new cases. Mark the item `SUPERSEDED by 18-01's locked deviation` and record that its coverage now lives in `src/analytics/athlete-private.test.ts`. Do not quietly tick it — a superseded requirement that looks satisfied is how a real gap hides.

3. **Manual-Only Verifications** — leave the table's rows intact; Task 2 fills in results.

4. Do **not** set `nyquist_compliant: true` or `status: approved` yet. Task 2's outcome decides both.

Also record, in a new "Live archive measurements at phase close" section, the values the summaries captured: the Riegel fit (points, distinct activities, exponent), the per-distance PR-evolution step counts, the training-load spine length and its count of days with runs but zero HR runs, the gear coverage percentages for 2020/2023/2026, and the WMA editions bundled. These are the numbers a future phase will want and that nothing else in the repo records.
  </action>
  <verify>
    <automated>npm run build && npm test && node dist/index.js compute-all-stats && npm run build-widgets && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the gate exits 0; record each exit code and the test/assertion counts in the summary.
    - `grep -c "TBD" .planning/phases/18-records-trends-differentiators/18-VALIDATION.md` returns `0`.
    - `grep -c "SUPERSEDED" .planning/phases/18-records-trends-differentiators/18-VALIDATION.md` returns at least `1`.
    - Every one of the eleven requirement IDs (REC-02..07, TREND-01..05) appears at least once in the Per-Task Verification Map with a non-`TBD` plan number.
    - The "Live archive measurements at phase close" section contains all six recorded measurements.
    - `nyquist_compliant` is still `false` and `Approval` is still `pending` at the end of this task.
  </acceptance_criteria>
  <done>The full automated gate is green and the validation record reflects what was actually built, including the one superseded Wave 0 item marked as such rather than ticked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-browser verification of every rendering behaviour</name>
  <files>.planning/phases/18-records-trends-differentiators/18-VALIDATION.md</files>
  <action>
Do not write or change any source code in this task. Serve the already-built publish directory under a production-shaped `/strava-widgets` path, walk 18-UI-SPEC.md § 19's checklist item by item as set out in `<how-to-verify>` below, and record a verbatim observation for each of the 20 items in `18-VALIDATION.md`'s Manual-Only Verifications table. Then set `nyquist_compliant` and the `Approval` line from the outcome: `true`/`approved` only if every item passed, otherwise leave `nyquist_compliant: false` and record `PARTIAL` with the confirmed requirement IDs listed and every failure logged verbatim as gap-closure work, following the 16-09 and 17-15 precedent.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/18-records-trends-differentiators/18-VALIDATION.md','utf8');if(/pending/.test(s.split('Approval:')[1]||'pending'))throw new Error('Approval line still pending');if(!/approved|PARTIAL/.test(s))throw new Error('no verdict recorded');console.log('checkpoint verdict recorded')"</automated>
  </verify>
  <done>All 20 checklist items have a recorded verbatim observation, and `18-VALIDATION.md` carries an explicit verdict of either approved or PARTIAL with the failures logged unpatched.</done>
  <acceptance_criteria>
    - `18-VALIDATION.md`'s Manual-Only Verifications table has a non-empty observation for every one of the 20 items — no blank cells and no bare ticks.
    - The `Approval:` line reads either `approved {date}` or `PARTIAL {date}`; it no longer reads `pending`.
    - If PARTIAL: each failure is quoted verbatim in the developer's own words, and the confirmed requirement IDs are listed explicitly and separately from the failing ones.
    - `nyquist_compliant` is `true` only when the verdict is `approved`.
    - `git status --porcelain data/private/` produces no tracked-file entry (the temporary real config from item 20 stayed untracked).
    - No file under `src/` or `scripts/` was modified by this task: `git status --porcelain src scripts` is empty.
  </acceptance_criteria>
  <what-built>
Phase 18 shipped two new views and one extended view, all verified so far only by `tsc`, unit tests over pure functions, and HTTP-level publish assertions. Nothing has confirmed a single pixel.

- `#/records` — sticky four-item jump list, four superlative tiles, seven PR tables (including the marathon empty state and a combined age-grading config notice), a seven-card PR-evolution grid with stepped charts and native `<details>` progression tables, and a Race Predictions section with the standard Riegel matrix and a self-suppressing fitted exponent.
- `#/trends` — a rolling-totals strip, a five-tab ARIA tablist with a bookmarkable `?tab=` parameter, and five tabs: Volume (one chart with a granularity toggle plus a 53×7 year consistency heatmap), Year-over-Year, Cadence & HR (two stacked bands), Training Load (CTL/ATL/TSB with a model toggle, window control and no-HR shading), and Gear (coverage sentence, bounded bar chart, sortable table).
- `#/activity/:id` — named per-distance PR badges and a "Best Efforts This Run" panel.
- Both `STUB_PHASE` entries are gone; `records.stub.ts` and `trends.stub.ts` are deleted.
  </what-built>
  <how-to-verify>
**Serve the build under a production-shaped path.** Serving at the server root would hide the exact class of defect that produced Phase 16's black page, so mount it under `/strava-widgets` the way `verify-dashboard-publish.mjs` does:

1. `npm run build && node dist/index.js compute-all-stats && npm run build-widgets`
2. `mkdir -p /tmp/gh-pages && ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets`
3. `cd /tmp/gh-pages && python3 -m http.server 8099`
4. Open `http://localhost:8099/strava-widgets/#/records` with the browser console visible for the entire session.

Work through 18-UI-SPEC.md § 19's checklist. For each item, record a verbatim observation — **not** a tick. Where an item fails, write down exactly what you saw, in the developer's own words, rather than paraphrasing it into something fixable.

**Records**
1. **Hash-router safety (§ 1 landmine).** Click each of the four jump-list items. The URL hash must stay `#/records` — it must never become `#records-pr-tables` or similar — and the page must scroll to the right section.
2. **Sticky offset (§ 1 landmine).** With the jump list visible, resize the window across the 640 px breakpoint in both directions. No gap and no overlap may form between `.app-nav` and the jump list in either state.
3. **Seven evolution charts (§ 3).** Each shows a visibly **descending** step line, its own independent y-range, and a big `.text-display` current-PR number that matches the chart's lowest point.
4. **Evolution grid breakpoints (§ 3).** Count columns at <640 px, 640-999 px and ≥1000 px: must be 1, 2, 3.
5. **Marathon empty state (§ 2).** Named empty state, not a blank table and not a crash; still reachable from the jump list.
6. **Age-grade column (§ 2).** With no `data/private/athlete-private.json`: every Age-Grade cell shows an em-dash and **exactly one** config notice appears (not one per table). The notice names `data/private/athlete-private.json`.
7. **Riegel suppression (§ 4b).** Confirm which branch renders and that it matches the live data: either a fitted table naming its distances, or the suppression copy naming the **actual** current distinct-activity count.
8. **Progression disclosure (§ 3).** Open a `<details>`; the table shows signed improvements with a leading `−` for faster times.

**Trends**
9. **Tablist keyboard model (§ 7).** Tab to the tablist. Left/Right must move **and activate** across all five tabs (content changes on arrow, not on Enter). Home/End jump to Volume/Gear.
10. **Bookmarkability.** Load `#/trends?tab=training-load` directly; it must open on that tab. Then use browser Back and Forward across several tab changes.
11. **Canvas lifecycle.** Cycle all five tabs twice, quickly. **No `Canvas is already in use` error may appear in the console** — and no other console error either.
12. **Year heatmap (§ 8).** 53×7 grid, no overlapping or missing cells. Rest days render with **zero** accent tint, visibly distinct from the faintest active day. "View as table" discloses real per-day data.
13. **Cadence & HR (§ 10).** Both bands' plot areas begin at the **same x offset** — this is the Phase 17 GAP 2 signature; check it deliberately. At least one genuine line gap is visible for a month with no data, and it is a gap, not a dip to zero.
14. **Training load (§ 11).** Shaded no-HR regions are visibly distinguishable from a genuine zero-load valley, and the caption text is present and legible. Switching Edwards↔Banister — with Banister disabled, confirm the option is **visible but disabled** with its notice; the notice names `data/private/athlete-private.json`. Change the window control and confirm the visible range changes.
15. **Gear (§ 12).** At most 8 named/numbered bars plus one Other bar. The table lists all 16 shoes plus an Unknown row with a non-zero count, and Unknown stays **last** after clicking every sortable column header in both directions. The coverage sentence shows real numbers.

**Detail**
16. **PR badge + panel (§ 5).** Open an activity with multiple PRs (`node -e "console.log(require('./data/dashboard/index.json').activities.filter(r=>r.prCount>1).slice(0,3).map(r=>r.id))"`). Confirm named per-distance badges and a best-efforts panel whose PR rows are **permanently** highlighted (the highlight must not fade after a second or two — that would mean the transition was copied along with the colour).

**Cross-cutting**
17. **Both themes.** Repeat every colour-sensitive check (heatmap tint-0, training-load shading, `--cat-1..8` bars, `--load-*` lines) in dark mode. The new tokens are theme-invariant by design and must still read clearly against both surface pairs.
18. **New-token contrast (§ 16).** `--cat-1..8` and `--load-tsb` are theme-invariant. Eyeball each against both `--bg`/`--surface` pairs (light `#fff`/`#f5f5f7`, dark `#1a1a2e`/`#242444`) for ≥3:1 non-text contrast. There is no automated canvas-contrast check available in this repo.
19. **`STUB_PHASE` removal.** Confirm neither `#/records` nor `#/trends` shows any stub panel.

**Independent age-grade cross-check (RESEARCH Assumption A1).**
20. Temporarily create `data/private/athlete-private.json` with your real `birthDate` and `sex`, run `node dist/index.js compute-age-grading && npm run build-widgets`, reload Records, and pick **two** PR rows at different distances. Enter the same time, distance, age and sex into an independent public age-grade calculator (for example `runbundle.com` or `marathonhandbook.com`). The percentages must agree within about 1 point.
    **This is the phase's only check on REC-06's correctness — the bundled WMA factor tables have no other self-check.** If they disagree by more than ~1 point, report it as a defect and do **not** approve; the tables need re-sourcing, not the display adjusting.
    Delete `data/private/athlete-private.json` afterwards, or keep it — it is gitignored either way. Confirm with `git status` that it does not appear as an untracked file.

**Then record the outcome in `18-VALIDATION.md`.** Follow the 16-09 and 17-15 precedent exactly: if any item fails, record the checkpoint as **PARTIAL**, list the passing requirement IDs only, and log each failure verbatim as gap-closure work rather than patching it under checkpoint pressure. Set `nyquist_compliant` and `Approval` accordingly.
  </how-to-verify>
  <resume-signal>Type "approved" if every item passed, or describe each failure verbatim — including which of the 20 items it belongs to. Anything less than every item passing is recorded as PARTIAL, not approved.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → production URL shape | Serving at the server root instead of under `/strava-widgets` would hide the Phase 16 defect class |
| temporary private config → git | The developer creates a real `birthDate` file on disk during item 20 |
| human judgement → phase gate | This checkpoint is the only verification of ~15 canvases and all DOM behaviour |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-PII-09 | Information Disclosure | temporary real private config | mitigate | The file lives at the gitignored `data/private/athlete-private.json` path; item 20 requires an explicit `git status` confirmation that it is untracked, and plan 18-01's `assertNoPrivateArtifacts` fails the build if it ever reaches `dist/widgets/` |
| T-18-VERIFY-02 | Repudiation (false-green gate) | serving the build at the root | mitigate | The checkpoint mounts under `/strava-widgets`, matching production and matching what `verify-dashboard-publish.mjs` was fixed to do after the Phase 16 postmortem |
| T-18-VERIFY-03 | Repudiation (paraphrased failure) | checkpoint recording | mitigate | Failures are recorded verbatim as gap-closure work under the 16-09/17-15 precedent, and a partial result is recorded as PARTIAL with only the confirmed requirement IDs listed |
| T-18-WMA-03 | Integrity (undetectable wrongness) | bundled WMA factor tables | mitigate | Item 20 is an independent external cross-check — the only one REC-06 has. A disagreement blocks approval rather than being absorbed |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs authorized. The verification server uses `python3 -m http.server`, already present; do not `npm install` a static server |
</threat_model>

<verification>
- The full automated gate is green before the checkpoint opens.
- The checkpoint was performed against `http://localhost:8099/strava-widgets/`, not against a root-served build.
- Every one of the 20 items has a recorded verbatim observation, not a tick.
- `git status` confirms no private config file is tracked.
</verification>

<success_criteria>
Every behaviour this phase added has been seen working by a human in a real browser under a production-shaped URL — or has been recorded, verbatim and unpatched, as gap-closure work.
</success_criteria>

<output>
Create `.planning/phases/18-records-trends-differentiators/18-16-SUMMARY.md` when done. Record the checkpoint verdict (approved or PARTIAL), the confirmed requirement IDs, every failure verbatim, and the two age-grade values compared against the external calculator with their source.
</output>
