---
phase: 18-records-trends-differentiators
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/views/chart-theme.ts
  - src/dashboard/views/detail-charts.ts
  - src/dashboard/views/list.ts
  - src/dashboard/views/list.test.ts
  - src/dashboard/styles.css
autonomous: true
requirements: [REC-02, REC-03, REC-04, REC-07, TREND-01, TREND-04, TREND-05]
must_haves:
  decisions: [D-15, D-19]
  truths:
    - "Every new chart in this phase resolves its colours from the live theme tokens, from one shared module, with no second copy of the resolution code"
    - "Every new table and badge in this phase reuses the single existing formatter and badge helpers rather than forking them"
    - "A low-confidence badge's explanation is reachable without hovering"
  artifacts:
    - path: "src/dashboard/views/chart-theme.ts"
      provides: "Shared chart theming: token resolution, channel palette, theme colours, hex→rgba, pinned y-axis gutter"
      exports: ["resolveToken", "resolveChannelPalette", "resolveThemeColors", "hexToRgba", "Y_AXIS_WIDTH_PX"]
    - path: "src/dashboard/views/list.ts"
      provides: "formatEffortDuration + exported appendBadge + appendLowConfidenceBadge"
      exports: ["formatEffortDuration", "appendBadge", "appendLowConfidenceBadge"]
    - path: "src/dashboard/styles.css"
      provides: "All Phase 18 CSS classes and tokens from 18-UI-SPEC § 17"
      contains: ".pr-table"
  key_links:
    - from: "src/dashboard/views/detail-charts.ts"
      to: "src/dashboard/views/chart-theme.ts"
      via: "import of the extracted helpers"
      pattern: "from './chart-theme.js'"
---

<objective>
Lay down the three shared presentation foundations every remaining Phase 18 view plan depends on: the extracted chart-theming module, the new shared formatter/badge helpers in `list.ts`, and the complete stylesheet additions from 18-UI-SPEC § 17.

Purpose: this phase adds ~15 charts and four new table types across three views. Without a single owner for theming, formatting and CSS, each of those plans would independently re-derive them — which is exactly how `formatPace` acquired a second home and shipped an 11-row rounding defect (documented in `list.ts`'s own JSDoc). Mirrors 17-01's "single owner of `styles.css`" precedent.
Output: one shared chart-theme module, three new `list.ts` exports, and every new CSS class/token declared once.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-records-trends-differentiators/18-UI-SPEC.md
@.planning/phases/18-records-trends-differentiators/18-PATTERNS.md
@.planning/phases/18-records-trends-differentiators/18-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract chart theming into a shared module and rewire detail-charts.ts</name>
  <files>src/dashboard/views/chart-theme.ts, src/dashboard/views/detail-charts.ts</files>
  <read_first>
    - `src/dashboard/views/detail-charts.ts` lines 1-160 — the lazy-chunk-boundary header comment, the `Y_AXIS_WIDTH_PX = 72` constant and its full rationale comment (lines 63-78), and `resolveToken` / `resolveChannelPalette` / `resolveThemeColors` / `hexToRgba` (lines 92-151), which are the exact four functions being moved
    - `src/dashboard/styles.css` lines 15-90 — the `:root` / `[data-theme="light"]` / `[data-theme="dark"]` token blocks these functions read
    - `src/widgets/comparison-chart/chart-config.ts` — the hardcoded light/dark literal table that is correct for Shadow-DOM widgets and **must not** be copied into dashboard views (18-UI-SPEC § 14)
  </read_first>
  <action>
Create `src/dashboard/views/chart-theme.ts` and **move** (not copy) these from `detail-charts.ts`, preserving their existing JSDoc verbatim:
- `resolveToken(name: string, fallback: string): string`
- `ChannelPalette` interface and `resolveChannelPalette(): ChannelPalette`
- `ThemeColors` interface and `resolveThemeColors(): ThemeColors`
- `hexToRgba(hex: string, alpha: number): string`
- `Y_AXIS_WIDTH_PX` (currently `72`) together with its **entire** existing rationale comment, which documents that it is the fix for Phase 17's GAP 2 (per-chart y-gutter auto-sizing misaligning stacked bands' x-axes). 18-UI-SPEC § 10 calls reusing this "a mandatory reuse, not a suggestion" — moving it here is what makes that reuse possible from the Trends charts.

All five must be `export`ed. This module **must not** import `chart.js` — it reads CSS custom properties only. That keeps it importable from non-lazy code without dragging Chart.js into the main bundle, and is the reason it is a separate file rather than an export off `detail-charts.ts` (which is a deliberate lazy-chunk boundary).

Add a header comment stating: this is the single source of chart colour resolution for every **dashboard view**; colours come from live CSS custom properties on `document.documentElement` so a theme switch is reflected without a literal table; the hardcoded light/dark table in `src/widgets/*/chart-config.ts` is correct there (Shadow DOM, no access to `data-theme`) and is a regression here.

Then edit `detail-charts.ts`: delete the five moved declarations and import them from `./chart-theme.js`. Every existing call site (`resolveToken(...)`, `hexToRgba(...)`, `scale.width = Y_AXIS_WIDTH_PX`, etc.) keeps working unchanged. Do not alter any chart behaviour, any registration list, or the crosshair plugin.
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/dashboard && npx vite build --config vite.dashboard.config.ts 2>/dev/null || npm run build-widgets</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc) exits 0.
    - `grep -c "function resolveToken" src/dashboard/views/detail-charts.ts` returns `0`; the same grep against `src/dashboard/views/chart-theme.ts` returns `1`. Repeat for `resolveChannelPalette`, `resolveThemeColors`, `hexToRgba`.
    - `grep -c "Y_AXIS_WIDTH_PX = 72" src/dashboard/views/chart-theme.ts` returns `1` and the same grep against `detail-charts.ts` returns `0`.
    - `grep -c "from './chart-theme.js'" src/dashboard/views/detail-charts.ts` returns `1`.
    - `grep -c "chart.js" src/dashboard/views/chart-theme.ts` returns `0`.
    - The GAP-2 rationale comment survived the move: `grep -c "gutter\|x-axes would not line up\|shared hover crosshair" src/dashboard/views/chart-theme.ts` returns at least `1`.
    - `npm run build-widgets` exits 0 (the dashboard bundle still builds and the lazy chunk boundary is intact).
    - `npm test` exits 0 with no pre-existing test regressed.
  </acceptance_criteria>
  <done>Chart theming lives in exactly one place, importable by the Records and Trends chart modules without pulling Chart.js into the main bundle, and Phase 17's y-gutter fix is now reusable rather than trapped in one file.</done>
</task>

<task type="auto">
  <name>Task 2: Add the shared effort-duration formatter and export the badge helpers</name>
  <files>src/dashboard/views/list.ts, src/dashboard/views/list.test.ts</files>
  <read_first>
    - `src/dashboard/views/list.ts` lines 44-140 — `formatActivityDate` (the Z-suffix rule and its defect comment), `formatDurationHms`, `formatPace` (the single-rounding-step rule and its 11-row defect comment), the private `appendBadge`, and `appendStatusBadges`
    - `src/dashboard/views/list.test.ts` — the existing colocated suite for this file and its structure
    - `18-UI-SPEC.md` § 6 (badge vocabulary table, especially the `Low confidence` row and its `sr-only` + `title` + `aria-describedby` requirement) and § 14 (`formatEffortDuration` contract)
  </read_first>
  <action>
Three additions to `src/dashboard/views/list.ts`, all in the existing formatter/badge block so the single-source-of-truth discipline is preserved:

1. `export function formatEffortDuration(totalSeconds: number): string` — renders `m:ss` when under an hour and `h:mm:ss` at or above one hour. `formatDurationHms(1179)` returns `"0:19:39"`, which is wrong for a 5K PR; this returns `"19:39"`. Use a **single** rounding step feeding both components, exactly as `formatPace`'s JSDoc mandates (`Math.round` once, then `floor(/60)` and `% 60`), so the `5:60` defect class cannot reappear. Return `'—'` for non-finite or negative input. Give it a JSDoc block explaining why it exists alongside `formatDurationHms` rather than replacing it, and naming its four consumers (PR tables, evolution progression tables, Riegel matrix, best-efforts panel).

2. Change `function appendBadge` to `export function appendBadge` — no behaviour change. 18-UI-SPEC § 5 requires `detail.ts` reuse it rather than duplicating a 5-line DOM builder.

3. `export function appendLowConfidenceBadge(container: HTMLElement, idPrefix: string): void` — the fix 18-UI-SPEC § 6 calls out as "this phase closes a real gap". It appends a `<span class="badge">Low confidence</span>` carrying `title="GPS-reconstructed distance; treat this time with caution"`, plus a sibling `<span class="sr-only">` with the **same** text and a unique `id` built from `idPrefix`, and sets `aria-describedby` on the badge to that id — so the explanation is reachable by keyboard and assistive tech, not only by hover. Then change `appendStatusBadges` to call this instead of `appendBadge(container, 'Low confidence')`, so the list and overview views inherit the fix at the same time as the two new Records surfaces get it. Use `textContent` for both spans (T-16-VW-01).

Extend `src/dashboard/views/list.test.ts` with `formatEffortDuration` cases: `1179` → `"19:39"`; `44` → `"0:44"`; `3600` → `"1:00:00"`; `5211` → `"1:26:51"`; `2388.9` rounds in one step (assert the result is not `"39:60"`); `59.6` → `"1:00"` not `"0:60"`; `-1`, `NaN` and `Infinity` all → `'—'`. Do not add DOM tests for the badge helpers — `vitest.config.ts` sets `environment: 'node'` with no jsdom; badge rendering is verified in the manual browser checkpoint (plan 18-16).
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/dashboard/views/list.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/views/list.test.ts` exits 0 and includes passing assertions for all seven `formatEffortDuration` inputs above.
    - `grep -c "export function appendBadge" src/dashboard/views/list.ts` returns `1`.
    - `grep -c "export function formatEffortDuration" src/dashboard/views/list.ts` returns `1`.
    - `grep -c "aria-describedby" src/dashboard/views/list.ts` returns at least `1` and `grep -c "sr-only" src/dashboard/views/list.ts` returns at least `1`.
    - `grep -c "appendBadge(container, 'Low confidence')" src/dashboard/views/list.ts` returns `0` (the raw call was replaced by the accessible helper).
    - `grep -c "innerHTML" src/dashboard/views/list.ts` returns `0`.
    - `npm test` exits 0 with no pre-existing test regressed.
  </acceptance_criteria>
  <done>One duration formatter, one badge builder, and one low-confidence explanation — all exported, all tested where testable, and the pre-existing list/overview badges inherit the accessibility fix.</done>
</task>

<task type="auto">
  <name>Task 3: Add every Phase 18 CSS class and token to the stylesheet</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/styles.css` lines 15-95 — the `:root` token block, the two `[data-theme]` blocks, and the `--zone-1..5` theme-invariant precedent these new tokens follow
    - `src/dashboard/styles.css` lines 340-400 (`.activity-table`, `.activity-table__row--highlight` and its `color-mix` + transition), 460-560 (`.preset-chip`, `.chip-row`, `.pagination__button` 32px min-size, `.empty-state`), 600-680 (`.calendar-day--tint-1..4` `color-mix` percentages), 713-800 (`.chart-band__canvas-wrap` responsive height, `.segmented`/`.segmented__option(--active)`, `.splits-scroll` overflow technique)
    - `18-UI-SPEC.md` § 17 (the complete class inventory), § Color (the token table), § 1, § 3, § 8 (the exact CSS blocks quoted for the jump list, evolution grid and year heatmap)
  </read_first>
  <action>
Add to `src/dashboard/styles.css` only. **No new spacing scale, no fifth type role, no new `--destructive` usage** — 18-UI-SPEC is explicit that this phase reuses the existing scales verbatim.

**Tokens**, declared once at `:root` and **not** repeated in the two `[data-theme]` blocks (they are theme-invariant, following the `--zone-1..5` precedent, not the `--chart-*` per-theme precedent):
`--load-ctl: #3b82f6;` `--load-atl: #ef4444;` `--load-tsb: #9ca3af;` `--cat-1: #4E79A7;` `--cat-2: #F28E2B;` `--cat-3: #E15759;` `--cat-4: #76B7B2;` `--cat-5: #59A14F;` `--cat-6: #EDC948;` `--cat-7: #B07AA1;` `--cat-8: #9C755F;`
Comment the block: `--load-ctl`/`--load-atl` are deliberate literal reuses of `--zone-1`/`--zone-5`'s hex values; `--cat-1..8` are used **only** by the Gear tab's top-8 bar chart and never to colour-code the seven PR distances.

**Classes** (every one from § 17):
- `.records-jump` — `position: sticky;` with **no** `top` value in CSS (18-UI-SPEC § 1 requires `top` be set inline at runtime from the live `.app-nav` height and recomputed on resize; a hardcoded value is the documented landmine), `z-index` above page content, `background: var(--surface)`, `display: flex`, `gap: var(--space-sm)`, `padding: var(--space-sm)`, plus the `.splits-scroll` horizontal-scroll technique verbatim (`overflow-x: auto; -webkit-overflow-scrolling: touch;`) and `white-space: nowrap` so it never wraps to a second line.
- `.records-jump__link` — a `<button>` reset styled as `.text-label`, `min-height: 32px; min-width: 32px` (the existing pagination touch-target exception, reused not re-invented), transparent background, `border: none`, `cursor: pointer`. Plus `.records-jump__link[aria-current="location"]` giving the same treatment as `.app-nav__link[aria-current="page"]`: `color: var(--accent); border-bottom: 2px solid var(--accent);`
- `.config-notice` — a `.text-body`-sized notice block: `--surface` background, `1px solid var(--border)`, `border-radius: 4px`, `padding: var(--space-md)`, `margin-bottom: var(--space-md)`. Not accent-filled — it is informational, not an error.
- `.sr-only` — the standard visually-hidden utility (`position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip-path: inset(50%); white-space: nowrap; border: 0;`). It does not exist in this stylesheet today.
- `.pr-table` — layered on `.activity-table`'s base rules; do not restate border-collapse, cell padding or `thead` background. Add only what differs. `.pr-table__numeric { text-align: right; font-variant-numeric: tabular-nums; }`. `.pr-table__row--pr { background: color-mix(in srgb, var(--accent) 15%, transparent); }` — the **identical** formula as `.activity-table__row--highlight` but with **no** `transition` property (§ 5: a permanent state marker, not a you-just-arrived flash).
- `.pr-evolution-grid` — exactly the three-breakpoint block quoted in § 3 (`1fr`; `repeat(2, 1fr)` at `min-width: 640px`; `repeat(3, 1fr)` at `min-width: 1000px`; `gap: var(--space-lg)`). Use those literal breakpoints, not `auto-fit`, so the manual checkpoint is a column count.
- `.pr-evolution-card` — composes with `.card`; add only `display: flex; flex-direction: column; gap: var(--space-sm);`
- `.pr-evolution-card__canvas-wrap` — `height: 160px; position: relative;` with a `@media (max-width: 380px)` override to `112px`, mirroring `.chart-band__canvas-wrap`'s existing responsive rule.
- `.pr-evolution-card__details` — spacing for the native `<details>`; no custom marker, no animation.
- `.year-heatmap` and `.year-heatmap__cell` — exactly the blocks quoted in § 8. Then `.year-heatmap__cell--tint-0 { background: var(--surface); }` and `--tint-1..4` reusing the **same four `color-mix` percentages** already used by `.calendar-day--tint-1..4` (12 % / 25 % / 40 % / 55 %). Tint-0 carries **no accent at all** — a rest day must look empty, not faintly active (§ 8, D-15 principle).
- `.trends-totals-strip` — § 17 flags this as possibly unnecessary. Add it **only** if `.card` + `.stat-grid` genuinely do not suffice; if they do, omit the class entirely and note that decision in the summary rather than shipping a dead rule.

Do **not** add any class for the Trends tab bar — § 7 is explicit that it reuses `.segmented`/`.segmented__option(--active)` verbatim and differs only in ARIA attributes.
  </action>
  <verify>
    <automated>npm run build-widgets && node -e "const c=require('fs').readFileSync('src/dashboard/styles.css','utf8'); const need=['--load-ctl','--load-atl','--load-tsb','--cat-1','--cat-8','.records-jump','.records-jump__link','.config-notice','.sr-only','.pr-table','.pr-table__numeric','.pr-table__row--pr','.pr-evolution-grid','.pr-evolution-card','.pr-evolution-card__canvas-wrap','.pr-evolution-card__details','.year-heatmap','.year-heatmap__cell','.year-heatmap__cell--tint-0']; const miss=need.filter(n=>!c.includes(n)); if(miss.length){console.error('missing:',miss);process.exit(1);} console.log('all Phase 18 classes and tokens present');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `all Phase 18 classes and tokens present` and exits 0.
    - `grep -A3 '\.pr-table__row--pr' src/dashboard/styles.css | grep -c transition` returns `0`.
    - `grep -A4 '^\.records-jump {' src/dashboard/styles.css | grep -cE '^\s*top:'` returns `0` (no hardcoded sticky offset).
    - `grep -c 'repeat(2, 1fr)' src/dashboard/styles.css` and `grep -c 'repeat(3, 1fr)' src/dashboard/styles.css` each return at least `1`.
    - `grep -c '\--load-ctl' src/dashboard/styles.css` returns `1` (declared once, not per theme).
    - `grep -cE '^\s*--space-[a-z0-9]+:' src/dashboard/styles.css` returns exactly `7` (the pre-existing scale is unchanged — no new spacing token).
    - `npm run build-widgets` exits 0.
  </acceptance_criteria>
  <done>Every class and token the remaining Phase 18 view plans reference exists, declared once, with the sticky offset and the permanent-highlight transition both deliberately left out per the UI contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| athlete-authored strings → DOM | Activity names and (once filled in) gear names are free text rendered by helpers this plan owns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-XSS-01 | Tampering / XSS | `appendBadge`, `appendLowConfidenceBadge` | mitigate | Both helpers write with `textContent` only; `grep -c innerHTML src/dashboard/views/list.ts` must return `0` (T-16-VW-01 convention). Gear names are blank today but will contain athlete-typed text once filled in (D-19), so this must not regress |
| T-18-A11Y-01 | Information Disclosure (of meaning, to AT users) | low-confidence badge | mitigate | `sr-only` span + `aria-describedby` makes the explanation reachable without hover, closing the gap 18-UI-SPEC § 6 identifies in the already-shipped list/overview badge |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs authorized. The heatmap uses a plain CSS grid; `chartjs-chart-matrix` is explicitly rejected. An executor that concludes an install is necessary must stop and run the Package Legitimacy Gate |
</threat_model>

<verification>
- `npm run build`, `npm test`, and `npm run build-widgets` all exit 0.
- No pre-existing detail-view chart behaviour changed (the extraction is a move, not a rewrite).
</verification>

<success_criteria>
Every downstream Phase 18 plan can import one chart-theme module, one duration formatter, one badge builder, and reference every CSS class it needs — with no plan needing to invent or fork any of them.
</success_criteria>

<output>
Create `.planning/phases/18-records-trends-differentiators/18-04-SUMMARY.md` when done. State explicitly whether `.trends-totals-strip` was added or deliberately omitted, and why.
</output>
