---
phase: 16-dashboard-shell-data-contract
plan: 08
type: execute
wave: 4
depends_on: [16-04, 16-07]
files_modified:
  - src/pages/widgets.html
  - dist/widgets/index.html
  - .gitignore
  - README.md
  - vite.config.pages.ts
  - scripts/build-widgets.mjs
  - .github/workflows/daily-refresh.yml
autonomous: true
requirements: [DASH-01, DASH-02]

must_haves:
  truths:
    - "D-08: The dashboard SPA is the generated index.html of the published site, and the widget showcase still exists at its own URL"
    - "A local build produces dist/widgets/index.html from src/dashboard/ without wiping the widget bundles or the standalone pages"
    - "D-11: The published directory contains the dashboard index manifest and every per-activity detail and stream file the SPA can request"
    - "The nightly workflow regenerates the dashboard index before building, isolating its failure from the rest of the pipeline"
  artifacts:
    - path: "src/pages/widgets.html"
      provides: "The relocated widget showcase / embed documentation page"
      contains: "iife.js"
    - path: "scripts/build-widgets.mjs"
      provides: "Dashboard Vite entry plus the extended data copy"
      contains: "src/dashboard"
    - path: ".github/workflows/daily-refresh.yml"
      provides: "compute-dashboard-index CI stage"
      contains: "compute-dashboard-index"
  key_links:
    - from: "scripts/build-widgets.mjs"
      to: "src/dashboard/index.html"
      via: "a Vite build entry writing dist/widgets/index.html with emptyDir false"
      pattern: "src/dashboard"
    - from: "scripts/build-widgets.mjs copyDataFiles"
      to: "dist/widgets/data/{dashboard,activities,streams}"
      via: "extended dataDirs list"
      pattern: "data/activities"
    - from: ".github/workflows/daily-refresh.yml"
      to: "node dist/index.js compute-dashboard-index"
      via: "a dedicated workflow step with continue-on-error and a warning"
      pattern: "compute-dashboard-index"
---

<objective>
Make the dashboard the published site root without losing the widget showcase, wire its Vite entry into the existing build, and put the index manifest plus every detail and stream file inside the publish directory.

Purpose: D-08 (dashboard at the site root), D-11 (CI copies data into the publish dir), D-12 (index generated in CI). RESEARCH.md Pitfalls 2 and 3 make this the riskiest plan in the phase: `dist/widgets/index.html` is a hand-committed file today, and `data/activities/` and `data/streams/` have never been in the publish path.
Output: a relocated `widgets.html`, an untracked-and-generated `dist/widgets/index.html`, an extended build script, and a new CI compute stage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dashboard-shell-data-contract/16-CONTEXT.md
@.planning/phases/16-dashboard-shell-data-contract/16-RESEARCH.md
@.planning/phases/16-dashboard-shell-data-contract/16-04-SUMMARY.md
@.planning/phases/16-dashboard-shell-data-contract/16-07-SUMMARY.md

<interfaces>
<!-- The exact current state of every file this plan changes. -->

`.gitignore` (12 lines, verbatim lines 1-7):
```
node_modules/
dist/*
!dist/widgets/
dist/widgets/*
!dist/widgets/index.html
!dist/widgets/test.html
```
`git ls-files dist/` currently returns exactly `dist/widgets/index.html` and `dist/widgets/test.html`. `dist/widgets/index.html` is a 226-line hand-authored widget-showcase page (title "Strava Analytics Widgets", embed-code documentation), NOT a build artifact — nothing in `build-widgets.mjs` or `vite.config.pages.ts` writes it.

`vite.config.pages.ts` (19 lines) — `root: 'src/pages'`, `outDir: '../../dist/widgets'`, `emptyDir: false  // CRITICAL: Do not delete widget bundles`, `rollupOptions.input: { heatmap, pinmap, routes }`, `target: 'es2020'`, `minify: 'terser'`.

`scripts/build-widgets.mjs`:
- `copyDataFiles()` (lines 134-152) — a `dataDirs` array of `{src, dest}` for `data/stats`, `data/geo`, `data/routes`, `data/heatmap`, then a single-level (non-recursive) `readdirSync` loop copying `*.json`.
- `buildPages()` (lines 154-174) — `await build({ root: 'src/pages', build: { outDir: '../../dist/widgets', emptyDir: false, rollupOptions: { input: { heatmap, pinmap, routes } }, target: 'es2020', minify: 'terser' }, logLevel: 'warn' })`
- `buildAllWidgets()` (lines 176-191) — 11 widget builds, then `copyDataFiles()`, then `buildPages()`.

`.github/workflows/daily-refresh.yml` — the step sequence is: Fetch → Compile TypeScript (`npm run build`) → Process statistics → Compute geographic statistics (`continue-on-error: true` + warn step) → Compute best efforts (`continue-on-error: true` + warn step) → Build widgets (`npm run build-widgets`) → Commit updated data and stats (`git-auto-commit-action@v7`, `file_pattern: 'data/activities/*.json data/sync-state.json data/geo/*.json data/streams/*.json'`) → Deploy widgets to GitHub Pages (`peaceiris/actions-gh-pages@v4`, `publish_dir: ./dist/widgets`).

Measured local data volumes: `data/streams` 142MB / 1,843 files, `data/activities` 7.5MB / 1,867 files, `data/geo` 15MB, `data/heatmap` 13MB, `data/routes` 2.3MB.

`README.md` lines 22-24 link the three standalone pages at `https://bacilo.github.io/strava-widgets/{heatmap,pinmap,routes}.html`; the widget embed snippets throughout reference `.iife.js` and `data/...` URLs that are unaffected by what lives at `/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the widget showcase and free up the site root</name>
  <files>src/pages/widgets.html, dist/widgets/index.html, .gitignore, README.md</files>
  <read_first>
    - dist/widgets/index.html (the full 226-line showcase page — its content moves verbatim; read it before moving so nothing is lost)
    - src/pages/heatmap.html (the sibling standalone page whose head/nav conventions the relocated page should adopt)
    - .gitignore (lines 1-7 — you remove exactly one exception line; `!dist/widgets/test.html` stays)
    - README.md (lines 18-30 — the "Live pages" style list where the new widgets.html link belongs)
    - .planning/phases/16-dashboard-shell-data-contract/16-RESEARCH.md (Pitfall 2 and Assumption A1 — the showcase must move rather than be deleted, and the move must be visible in the README so the embed documentation is not silently lost)
  </read_first>
  <action>
    Move the widget-showcase page out of the site root so the dashboard can take it (D-08).

    1. Create `src/pages/widgets.html` carrying the existing showcase content from `dist/widgets/index.html` verbatim — the embed-code documentation is the only place those snippets exist and must survive the move byte-for-byte apart from the changes below. Update its `<title>` to `Widget Gallery — Strava Analytics`, and add a nav link back to the dashboard root (`<a href="./">Dashboard</a>`) alongside links to `./heatmap.html`, `./pinmap.html`, and `./routes.html`, matching how `src/pages/heatmap.html` cross-links.

    2. Untrack the old file: `git rm --cached dist/widgets/index.html` and delete it from the working tree. It becomes generated build output in task 2, exactly like `heatmap.html` already is.

    3. In `.gitignore`, delete the single line `!dist/widgets/index.html`. Leave `dist/*`, `!dist/widgets/`, `dist/widgets/*`, and `!dist/widgets/test.html` untouched — `test.html` remains a hand-committed file and is out of scope. Add a short comment above the remaining exception noting that `index.html` was removed from this list in Phase 16 because the dashboard SPA now generates it.

    4. Update `README.md`: add `- **[Widget Gallery](https://bacilo.github.io/strava-widgets/widgets.html)**: Embed code and live previews for all 11 widgets` to the live-pages list beside the existing heatmap/pinmap/routes entries, and add `- **[Training Dashboard](https://bacilo.github.io/strava-widgets/)**: Browse the full running archive` as the first entry. Do not touch any embed snippet URL — the `.iife.js` and `data/...` URLs are unaffected by what serves at `/`.

    Do not add `src/pages/widgets.html` to any Vite input map yet; that happens in task 2 alongside the dashboard entry, so both build-wiring changes land together.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),cp=require('child_process');if(!fs.existsSync('src/pages/widgets.html'))throw new Error('widgets.html not created');const w=fs.readFileSync('src/pages/widgets.html','utf8');if(!w.includes('iife.js'))throw new Error('showcase embed snippets lost in the move');if(!/Widget Gallery/.test(w))throw new Error('title not updated');if(!/href=\"\.\/\"/.test(w))throw new Error('missing link back to the dashboard root');const gi=fs.readFileSync('.gitignore','utf8');if(gi.includes('!dist/widgets/index.html'))throw new Error('.gitignore still excepts dist/widgets/index.html');if(!gi.includes('!dist/widgets/test.html'))throw new Error('test.html exception must be preserved');const tracked=cp.execSync('git ls-files dist/').toString().trim().split('\n').filter(Boolean);if(tracked.includes('dist/widgets/index.html'))throw new Error('dist/widgets/index.html still tracked');if(!tracked.includes('dist/widgets/test.html'))throw new Error('test.html should remain tracked');const rd=fs.readFileSync('README.md','utf8');if(!rd.includes('widgets.html'))throw new Error('README missing the relocated gallery link');if(!rd.includes('Training Dashboard'))throw new Error('README missing the dashboard link');console.log('relocation OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/widgets.html` exists, retains the embed-code snippets (`grep -c 'iife.js' src/pages/widgets.html` is greater than 5), has the title `Widget Gallery — Strava Analytics`, and links back to `./`
    - `git ls-files dist/widgets/index.html` produces no output (untracked)
    - `git ls-files dist/widgets/test.html` still produces a match (untouched)
    - `.gitignore` no longer contains `!dist/widgets/index.html` and still contains `!dist/widgets/test.html`
    - `README.md` links both `https://bacilo.github.io/strava-widgets/` (Training Dashboard) and `https://bacilo.github.io/strava-widgets/widgets.html` (Widget Gallery)
    - No embed snippet URL in `README.md` changed (`git diff README.md` shows only added lines)
  </acceptance_criteria>
  <done>The showcase content lives at a buildable source path, the site root is free for the dashboard, and `dist/widgets/index.html` is no longer a hand-committed file.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dashboard and gallery entries to the Vite build</name>
  <files>vite.config.pages.ts, scripts/build-widgets.mjs</files>
  <read_first>
    - scripts/build-widgets.mjs (full file — especially `buildPages()` at lines 154-174 and `buildAllWidgets()` at lines 176-191; note `emptyDir: false` is flagged CRITICAL because the 11 widget IIFE bundles are already in `dist/widgets/` by the time pages build)
    - vite.config.pages.ts (all 19 lines — the standalone-config twin of `buildPages()`; keep the two in sync)
    - src/dashboard/index.html (plan 06 — the entry whose `<script type="module" src="./main.ts">` and `<link href="./styles.css">` Vite must resolve and rewrite)
    - .planning/phases/16-dashboard-shell-data-contract/16-PATTERNS.md ("Analog (new Vite page entry)" under `scripts/build-widgets.mjs` — the `root`/`outDir`/`emptyDir` shape to copy)
  </read_first>
  <action>
    Wire two new entries into the build.

    In `vite.config.pages.ts`, add `widgets: resolve(__dirname, 'src/pages/widgets.html')` to the existing `rollupOptions.input` map beside `heatmap`, `pinmap`, and `routes`. Leave `root`, `outDir`, `emptyDir: false`, `target`, and `minify` unchanged.

    In `scripts/build-widgets.mjs`:

    1. Add the same `widgets: resolve(__dirname, '../src/pages/widgets.html')` entry to `buildPages()`'s `rollupOptions.input`, and update its trailing `console.log` to name the fourth page.

    2. Add a new `async function buildDashboard()` modelled on `buildPages()` but rooted at the dashboard source:
       - `root: 'src/dashboard'`
       - `build.outDir: '../../dist/widgets'`
       - `build.emptyDir: false` — CRITICAL for the same reason it is on `buildPages()`: by this point `dist/widgets/` holds 11 IIFE bundles, four page HTML files, and the copied data tree. Emptying it would destroy all of them.
       - `build.rollupOptions.input: { index: resolve(__dirname, '../src/dashboard/index.html') }` — the `index` key is what makes the output land at `dist/widgets/index.html`, taking over the site root per D-08.
       - `build.target: 'es2020'`, `build.minify: 'terser'`, `logLevel: 'warn'` — identical to the other builds.
       - A `console.log('✓ Built dashboard SPA (index.html)')` on completion.

    3. In `buildAllWidgets()`, call `await buildDashboard()` AFTER `await buildPages()`, so the SPA is the last thing written and any `dist/widgets/index.html` left over from the pre-Phase-16 committed file is definitively replaced. Update the closing summary log to mention the dashboard.

    Do not create a separate Vite config file for the dashboard — `build-widgets.mjs` is already the single build orchestrator for widgets and pages, and adding a fourth config would fragment it. `vite.config.pages.ts` is kept in sync only because it already exists as a standalone twin.

    Note for the executor: the dashboard entry contains a `<script type="module" src="./main.ts">`. Vite resolves the TypeScript entry, bundles it, and rewrites the tag to the hashed asset — no separate `tsc` step is involved for the browser bundle. `npm run build` (`tsc`) still type-checks these files because `tsconfig.json` includes `src/**/*`; it emits unused `dist/dashboard/*.js`, which is harmless and gitignored.
  </action>
  <verify>
    <automated>npm run build-widgets && node -e "const fs=require('fs');const idx='dist/widgets/index.html';if(!fs.existsSync(idx))throw new Error('dashboard index.html not built');const h=fs.readFileSync(idx,'utf8');if(h.includes('Strava Analytics Widgets'))throw new Error('site root still serves the old showcase page');if(!h.includes('data-theme'))throw new Error('built index.html lost the theme bootstrap');if(!/<script type=\"module\"[^>]*src=\"[^\"]*\.js\"/.test(h))throw new Error('module entry not rewritten to a built asset');if(h.includes('main.ts'))throw new Error('unbundled main.ts reference survived the build');for(const f of ['heatmap.html','pinmap.html','routes.html','widgets.html','test.html'])if(!fs.existsSync('dist/widgets/'+f))throw new Error('missing '+f+' after build');const iife=fs.readdirSync('dist/widgets').filter(n=>n.endsWith('.iife.js'));if(iife.length!==11)throw new Error('expected 11 widget bundles, found '+iife.length+' (emptyDir regression)');console.log('build OK, bundles='+iife.length);"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build-widgets` exits 0
    - `dist/widgets/index.html` exists and is the dashboard entry (contains the `data-theme` bootstrap, does NOT contain the string `Strava Analytics Widgets`)
    - The built `index.html` references a hashed `.js` asset, not `./main.ts`
    - `dist/widgets/` still contains all 11 `*.iife.js` bundles plus `heatmap.html`, `pinmap.html`, `routes.html`, `test.html`, and the new `widgets.html`
    - `vite.config.pages.ts` and `buildPages()` both list four inputs including `widgets`
    - `buildDashboard()` sets `emptyDir: false` (`grep -c 'emptyDir: false' scripts/build-widgets.mjs` returns at least 2)
    - `buildAllWidgets()` calls `buildDashboard()` after `buildPages()`
  </acceptance_criteria>
  <done>One `npm run build-widgets` produces the widget bundles, four standalone pages, and the dashboard SPA at the site root, with nothing overwritten.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the dashboard data families and add the CI compute stage</name>
  <files>scripts/build-widgets.mjs, .github/workflows/daily-refresh.yml</files>
  <read_first>
    - scripts/build-widgets.mjs (lines 134-152, `copyDataFiles()` — the `dataDirs` array and the non-recursive `*.json` loop you extend)
    - .github/workflows/daily-refresh.yml (the full step list — you insert one step pair modelled on "Compute best efforts" + "Warn on best-effort failure", and you must NOT touch the commit step's `file_pattern`)
    - .planning/phases/16-dashboard-shell-data-contract/16-RESEARCH.md (Pitfall 3 — `data/activities/` and `data/streams/` are absent from the publish path today; Open Question 1 — the recommendation to use a dedicated workflow step rather than folding into `compute-all-stats`; Assumptions A2 and A3)
    - .planning/phases/16-dashboard-shell-data-contract/16-PATTERNS.md ("Commit-file-pattern note" — `data/dashboard/index.json` must NOT be added to the commit step, since D-12 makes it gitignored)
  </read_first>
  <action>
    Extend the publish copy and add the CI stage.

    In `scripts/build-widgets.mjs`'s `copyDataFiles()`, append three entries to the `dataDirs` array:
    - `{ src: 'data/dashboard', dest: 'dist/widgets/data/dashboard' }` — the generated index manifest
    - `{ src: 'data/activities', dest: 'dist/widgets/data/activities' }` — 1,867 files, 7.5MB
    - `{ src: 'data/streams', dest: 'dist/widgets/data/streams' }` — 1,843 files, 142MB; the whole-directory copy also carries `manifest.json`, which lives inside that directory

    The existing loop is single-level and filters to `*.json`, which is correct for all three (every one is a flat directory of JSON files). Keep the `if (!existsSync(src)) continue` guard so a fresh clone without a `data/dashboard/` build still succeeds.

    Add one efficiency guard, because copying 150MB on every local iteration is a real cost: skip `copyFileSync` when the destination already exists and its `mtimeMs` is greater than or equal to the source's. Use `statSync` for the comparison, wrapped so a missing destination falls through to the copy. CI runs on a fresh checkout so it always performs the full copy; local rebuilds become near-instant. Change the per-directory `console.log` to report the number of files copied and the number skipped.

    In `.github/workflows/daily-refresh.yml`, insert a step pair immediately AFTER "Warn on best-effort failure" and BEFORE "Build widgets", copying the exact shape of the best-efforts pair:

    - `- name: Compute dashboard index` with `id: dashboard-index`, `continue-on-error: true`, and `run: node dist/index.js compute-dashboard-index`
    - `- name: Warn on dashboard-index failure` with `if: steps.dashboard-index.outcome == 'failure'` and `run: echo "::warning::Dashboard index computation failed, the dashboard will serve a stale index"`

    Position matters: the generator reads `data/stats/best-efforts.json` for PR counts and record exclusions, so it must run after "Compute best efforts"; and its output must exist before "Build widgets" copies `data/dashboard/` into the publish directory.

    Add a comment above the new step, matching the existing comment style, stating that `data/dashboard/` is gitignored and regenerated every run (like `data/stats/`) and therefore deliberately absent from the commit step's `file_pattern`. Do NOT modify `file_pattern` — `data/activities/*.json` and `data/streams/*.json` are already listed there, and adding the gitignored dashboard path would make `git add` fail on an ignored path and skip the deploy, which is exactly the failure mode the existing comment on that step warns about.

    Leave the `peaceiris/actions-gh-pages@v4` step unchanged. Its default publish mode force-pushes a single commit to `gh-pages` rather than accumulating history (RESEARCH.md Assumption A2), and the existing step sets no `keep_files` override — so the ~150MB payload does not compound day over day. Record that in the plan summary as a monitored assumption rather than adding a size gate; a CI size budget is explicitly noted as out of scope for Phase 16 (RESEARCH.md Open Question 2).
  </action>
  <verify>
    <automated>node dist/index.js compute-dashboard-index && npm run build-widgets && node -e "const fs=require('fs');const p='dist/widgets/data/';if(!fs.existsSync(p+'dashboard/index.json'))throw new Error('index manifest not published');if(!fs.existsSync(p+'streams/manifest.json'))throw new Error('stream manifest not published');const acts=fs.readdirSync(p+'activities').filter(f=>f.endsWith('.json'));const strs=fs.readdirSync(p+'streams').filter(f=>f.endsWith('.json'));if(acts.length<1800)throw new Error('too few published activities: '+acts.length);if(strs.length<1800)throw new Error('too few published streams: '+strs.length);const idx=JSON.parse(fs.readFileSync(p+'dashboard/index.json','utf8'));const sample=idx.activities.find(r=>r.streams.available);if(!fs.existsSync(p+'activities/'+sample.id+'.json'))throw new Error('index row has no published activity file: '+sample.id);if(!fs.existsSync(p+'streams/'+sample.id+'.json'))throw new Error('index row has no published stream file: '+sample.id);for(const d of ['stats','geo','routes','heatmap'])if(!fs.existsSync(p+d))throw new Error('regression: '+d+' no longer published');console.log('publish OK activities='+acts.length+' streams='+strs.length);" && node -e "const y=require('fs').readFileSync('.github/workflows/daily-refresh.yml','utf8');if(!y.includes('compute-dashboard-index'))throw new Error('CI step missing');const iBE=y.indexOf('Compute best efforts'),iDI=y.indexOf('Compute dashboard index'),iBW=y.indexOf('Build widgets');if(!(iBE<iDI&&iDI<iBW))throw new Error('dashboard-index step must sit between best-efforts and build-widgets');if(!y.includes(\"steps.dashboard-index.outcome == 'failure'\"))throw new Error('missing warn step');if(!/continue-on-error: true[\s\S]{0,120}compute-dashboard-index/.test(y))throw new Error('dashboard-index step must be continue-on-error');if(/file_pattern:.*data\/dashboard/.test(y))throw new Error('gitignored data/dashboard must not be in the commit file_pattern');console.log('workflow OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `dist/widgets/data/dashboard/index.json` exists after a build
    - `dist/widgets/data/activities/` contains at least 1,800 JSON files and `dist/widgets/data/streams/` contains at least 1,800 JSON files plus `manifest.json`
    - For a sampled index row with `streams.available === true`, both `dist/widgets/data/activities/<id>.json` and `dist/widgets/data/streams/<id>.json` exist — the D-07 proving slice cannot 404 on the live site
    - `dist/widgets/data/{stats,geo,routes,heatmap}` still exist (no regression to the four pre-existing copies)
    - A second consecutive `npm run build-widgets` reports skipped (unchanged) files rather than recopying 150MB
    - `.github/workflows/daily-refresh.yml` contains a `Compute dashboard index` step with `continue-on-error: true` and a matching warn step, positioned after `Compute best efforts` and before `Build widgets`
    - The commit step's `file_pattern` is unchanged and contains no `data/dashboard` entry
    - The `peaceiris/actions-gh-pages@v4` step is unchanged
  </acceptance_criteria>
  <done>The published directory is self-contained: index manifest, per-activity detail, and per-activity streams are all same-origin reachable, and CI regenerates the index every night in an isolated stage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → published gh-pages branch | The build copies ~150MB of committed data into a directory that is force-pushed to a public branch |
| CI pipeline stage → deploy | A failing compute stage could publish stale or empty content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-BD-01 | Denial of Service | `buildDashboard()` / `buildPages()` output directory handling | mitigate | Both builds set `emptyDir: false`, grep-asserted, and the verify step counts 11 IIFE bundles plus all five HTML pages after a full build. Without this, one Vite entry would wipe every previously-built artifact from `dist/widgets/` |
| T-16-BD-02 | Information Disclosure | Publishing `data/activities/` and `data/streams/` over HTTP for the first time | accept | These are the athlete's own Strava/intervals.icu-derived records, the same trust level as `data/stats/*.json` already served publicly since v1.0 (RESEARCH.md State of the Art). No credentials, no third-party data. `data/tokens.json` and `export_data/` remain gitignored and are outside every copied directory |
| T-16-BD-03 | Denial of Service | Unbounded `gh-pages` branch growth from a ~150MB daily payload | accept | `peaceiris/actions-gh-pages@v4` force-pushes a single commit by default and the existing step sets no `keep_files` override, so history does not accumulate (Assumption A2). A CI size budget is explicitly deferred (Open Question 2); the assumption is recorded in the plan summary for Phase 17/18 authors |
| T-16-BD-04 | Denial of Service | A failing `compute-dashboard-index` stage blocking the nightly pipeline | mitigate | The step is `continue-on-error: true` with an explicit `::warning::`, matching how `compute-geo-stats` and `compute-best-efforts` are already isolated; a failure serves a stale index instead of taking down the whole refresh and deploy |
| T-16-BD-05 | Tampering | Adding a gitignored path to the commit step's `file_pattern` | mitigate | `data/dashboard/` is deliberately excluded from `file_pattern`; the acceptance criteria assert its absence. The existing in-file comment documents that listing an ignored path makes `git add` fail and skips the deploy — the exact regression that froze gh-pages from Feb to Aug 2026 |
</threat_model>

<verification>
- `npm run build-widgets` exits 0 and produces 11 IIFE bundles, five HTML pages, and the full published data tree
- `dist/widgets/index.html` is the dashboard SPA, not the old showcase
- `git ls-files dist/` returns only `dist/widgets/test.html`
- Sampled index rows resolve to published activity and stream files
- The workflow's new step sits between best-efforts and build-widgets with `continue-on-error: true`
- `npm test` and `npx tsc --noEmit` still green
</verification>

<success_criteria>
- The dashboard SPA is the generated site root; the widget showcase survives at `widgets.html` and is linked from the README
- `dist/widgets/index.html` is untracked build output with its `.gitignore` exception removed
- The build produces the SPA without wiping widget bundles or standalone pages
- The publish directory contains the index manifest plus every activity and stream file the SPA can request
- CI regenerates the index in an isolated, non-blocking stage before the build
</success_criteria>

<output>
Create `.planning/phases/16-dashboard-shell-data-contract/16-08-SUMMARY.md` when done
</output>
