---
phase: 16-dashboard-shell-data-contract
plan: 14
type: execute
wave: 2
depends_on: [16-10, 16-11, 16-12, 16-13]
gap_closure: true
closes_gaps: ["SC1", "P08"]
closes_findings: []
files_modified: []
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "origin/master contains src/dashboard/ — the branch the publishing workflow builds from carries the Phase 16 code"
    - "git rev-list --count origin/master..master is 0 — no phase work remains unpushed"
    - "A daily-refresh workflow run completed with conclusion success, and its step list includes a successful Run test suite and a successful Verify dashboard publish contract"
    - "origin/gh-pages root index.html is the dashboard SPA, not the pre-Phase-16 static widget showcase"
    - "origin/gh-pages contains widgets.html carrying the widget showcase, satisfying D-08's second clause"
    - "origin/gh-pages data/ contains dashboard/, activities/ and streams/ in addition to the pre-existing geo/, heatmap/, routes/ and stats/"
    - "The live origin serves 200 for /, /widgets.html, /data/dashboard/index.json, and for the detail and stream JSON of the index's first (i-prefixed) row"
  artifacts:
    - path: "origin/master"
      provides: "The deploy source branch, now containing the whole phase"
      contains: "src/dashboard"
    - path: "origin/gh-pages:index.html"
      provides: "The published dashboard SPA at the site root (D-08)"
      contains: "app-nav-root"
    - path: "origin/gh-pages:widgets.html"
      provides: "The relocated widget showcase at its own URL (D-08)"
  key_links:
    - from: "master (local)"
      to: "origin/master"
      via: "git push origin master"
      pattern: "src/dashboard"
    - from: "origin/master"
      to: "origin/gh-pages"
      via: "daily-refresh.yml build + peaceiris/actions-gh-pages deploy of ./dist/widgets"
      pattern: "assets"
---

<objective>
Land the phase on GitHub Pages: push the unpushed `master` commits, run the publishing workflow, and prove against the real remote that the deployed site is the SPA with its full data tree.

Purpose: this is the phase goal's headline clause and the single largest divergence the verifier found between the phase narrative and reality. `origin/master` does not contain `src/dashboard/` at all — roughly 148 commits containing the entire phase sit unpushed on the local branch. Every nightly `daily-refresh` run, including the successful one at 2026-08-11T05:43:50Z after all phase work was committed locally, built the pre-Phase-16 tree. The live `gh-pages` branch (HEAD `7a1aa8a`, deployed 2026-08-11T05:45:56Z) has no `assets/` bundle, no `widgets.html`, and a root `index.html` that is still the old static showcase; its `data/` holds only `geo`, `heatmap`, `routes` and `stats`. No SUMMARY mentions this, because plan 16-09 narrowed its deployment truth to "resolves over HTTP from the publish directory" and the human UAT ran against a local server.

Output: a pushed `master`, a green gated workflow run, and a verified deployed tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dashboard-shell-data-contract/16-CONTEXT.md

<interfaces>
<!-- Concrete facts about the remote, the workflow and the site. -->

Remote:            origin  https://github.com/bacilo/strava-widgets.git
Deploy source:     origin/master  (workflow trigger: schedule 05:00 UTC + workflow_dispatch)
Publish target:    origin/gh-pages  (peaceiris/actions-gh-pages@v4, publish_dir ./dist/widgets)
Live origin:       https://bacilo.github.io/strava-widgets/
Stale deploy seen by the verifier: gh-pages HEAD 7a1aa8a, 2026-08-11T05:45:56Z

Workflow file:     .github/workflows/daily-refresh.yml, job id `refresh-and-deploy`
  Steps that are continue-on-error by design (their failure must NOT be treated
  as a deploy blocker): Fetch new activities from intervals.icu, Compute
  geographic statistics, Compute best efforts, Compute dashboard index,
  Commit updated data and stats.
  Steps that are blocking (added by plan 16-13): Run test suite, Verify
  dashboard publish contract. Also blocking: Compile TypeScript, Process
  statistics, Build widgets.
  Required repo secret for the fetch step: INTERVALS_API_KEY (its absence is
  tolerated — the step is continue-on-error and the pipeline rebuilds from
  committed data).

SPA markers to assert in the deployed root index.html (from src/dashboard/index.html):
  id="app-nav-root"   (line 41)   and   id="app"   (line 42)
  The pre-Phase-16 showcase has neither.

Showcase marker to assert in the deployed widgets.html (from src/pages/widgets.html):
  references to built widget bundles ending in .iife.js

Gitignore facts relevant to the publish-safety scan:
  .env, data/tokens.json, export_data/, data/stats/, data/dashboard/, dist/* are ignored.
  data/provenance.json IS tracked and indexes into the gitignored export_data/.

CLI available: gh 2.86.0 is installed and on PATH.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full local gate and a publish-safety scan over the unpushed commits</name>
  <files>none — verification only</files>
  <read_first>
    - .github/workflows/daily-refresh.yml — the post-16-13 step order, so the local gate mirrors what CI will run
    - .gitignore — which paths are excluded from the push
    - scripts/verify-dashboard-publish.mjs — what the probe actually asserts, so a pass can be interpreted
    - .planning/phases/16-dashboard-shell-data-contract/16-VERIFICATION.md lines 8-21 — the SC1 gap block, which is the specification for this whole plan
  </read_first>
  <action>
    Run the local gate in this exact order and record each command's exit code in the SUMMARY. All five must exit 0 before Task 2 runs: `npx tsc --noEmit`, `npm test`, `npm run build`, `npm run build-widgets`, `npm run verify-dashboard`. This mirrors the CI gate plan 16-13 added, so a local failure here means the first post-push run would be blocked before the deploy — fix it rather than pushing. Record the `verify-dashboard` check count (expected: 15 checks passed, 0 failures) verbatim.

    Then scan what the push will expose on a public remote. Record the exact commit count with `git rev-list --count origin/master..master` (the verifier observed 148; report the real current number rather than repeating that figure).

    List every file the push touches with `git diff --name-only origin/master..master` and confirm none of them is `.env`, any `.env.*`, `data/tokens.json`, or a path under `export_data/`. Confirm the ignore rules are actually in effect with `git check-ignore -v .env data/tokens.json export_data` — each must report a matching `.gitignore` line. If any of those paths is NOT ignored, stop and report instead of proceeding to Task 2.

    Scan the diff content for credential-shaped strings, excluding the planning directory to avoid matching prose: run `git diff origin/master..master -- . ':(exclude).planning' | grep -nE 'INTERVALS_API_KEY[[:space:]]*=|STRAVA_CLIENT_SECRET|client_secret|refresh_token|access_token|Bearer [A-Za-z0-9._-]{20,}'` and require it to produce no output. A match in a workflow file referencing `secrets.INTERVALS_API_KEY` is expected and acceptable — GitHub Actions secret interpolation is not a literal credential; anything else must stop the plan.

    Finally, check `data/provenance.json`, which is tracked and indexes into the gitignored `export_data/`: run `grep -c '/Users/' data/provenance.json`. If it returns anything other than 0, absolute home-directory paths would be published — stop, report the finding, and do not push. If it returns 0, note that in the SUMMARY and continue.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npm run build && npm run build-widgets && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - All five gate commands exit 0, with each exit code recorded in the SUMMARY.
    - `npm run verify-dashboard` output contains `15 checks passed` and `0 failures`.
    - `git rev-list --count origin/master..master` printed a number greater than 0, recorded verbatim in the SUMMARY.
    - `git diff --name-only origin/master..master` contains no `.env`, `.env.*`, `data/tokens.json`, or `export_data/` path.
    - `git check-ignore -v .env data/tokens.json export_data` reports a matching rule for each of the three.
    - The credential-shaped-string grep produced no output other than expected `secrets.INTERVALS_API_KEY` interpolations in workflow YAML.
    - `grep -c '/Users/' data/provenance.json` prints `0`.
    - `git status --short` is empty — nothing uncommitted is about to be left behind.
  </acceptance_criteria>
  <done>Every gate the CI pipeline will run passes locally, and nothing credential-shaped or home-path-shaped is about to become public.</done>
</task>

<task type="auto">
  <name>Task 2: Push master to origin and run the publishing workflow to completion</name>
  <files>none — repository state and CI</files>
  <read_first>
    - .github/workflows/daily-refresh.yml — the trigger block (`workflow_dispatch` is already declared) and the full step list, so a failing step can be attributed
    - .planning/phases/16-dashboard-shell-data-contract/16-VERIFICATION.md lines 8-21 — what "missing" items this task discharges
  </read_first>
  <action>
    The developer has explicitly authorized this push. Do NOT insert a checkpoint, do NOT ask for confirmation, and do NOT stop to summarize before pushing — Task 1's gate is the only precondition and it has already run.

    Push with `git push origin master`. This publishes the unpushed commits containing all of `src/dashboard/`, the dashboard index generator, the relocated `src/pages/widgets.html`, the build wiring, and the plan 16-13 workflow gate. Confirm the push landed with `git rev-list --count origin/master..master` returning `0` and `git ls-tree origin/master src/ --name-only` listing `src/dashboard`.

    Then trigger the publishing workflow immediately rather than waiting for the 05:00 UTC cron: `gh workflow run daily-refresh.yml --ref master`. Wait a few seconds, then capture the run id with `gh run list --workflow=daily-refresh.yml --limit 1 --json databaseId,status,headSha` and block on it with `gh run watch <id> --exit-status`.

    Interpret the result carefully rather than retrying blindly:
    - A failure of `Fetch new activities from intervals.icu` is NOT a blocker — that step is `continue-on-error: true` and the pipeline rebuilds from committed data. It emits a `::warning::` and the job continues.
    - A failure of `Run test suite` or `Verify dashboard publish contract` IS a blocker and means something diverged between the local gate and CI. Capture the failing step's log with `gh run view <id> --log-failed`, record it verbatim in the SUMMARY, and stop. Under no circumstances remove, skip, or add `continue-on-error` to either gate step to force a green deploy — that would re-create exactly the WR-06 hole this pass just closed.
    - A failure of `Compile TypeScript`, `Process statistics` or `Build widgets` is also a blocker; capture and report the same way.

    Record in the SUMMARY: the pushed commit count, the run id, the run URL, the conclusion, and the per-step conclusions for `Run test suite`, `Verify dashboard publish contract` and `Deploy widgets to GitHub Pages` (obtainable from `gh run view <id> --json jobs`).
  </action>
  <verify>
    <automated>test "$(git rev-list --count origin/master..master)" = "0" && git ls-tree origin/master src/ --name-only | grep -qx 'src/dashboard' && echo PUSH_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --count origin/master..master` prints `0`.
    - `git ls-tree origin/master src/ --name-only` includes the line `src/dashboard`.
    - `gh run view <id> --json conclusion -q .conclusion` prints `success`.
    - The run's job step list includes `Run test suite` with conclusion `success` and `Verify dashboard publish contract` with conclusion `success` — the gate ran and passed, it was not skipped.
    - The run's job step list includes `Deploy widgets to GitHub Pages` with conclusion `success`.
    - `git diff HEAD -- .github/workflows/daily-refresh.yml` is empty — the workflow was not edited to force the run green.
    - The SUMMARY records the run id and URL.
  </acceptance_criteria>
  <done>origin/master carries the whole phase, and a gated daily-refresh run completed successfully with both new gate steps passing and the Pages deploy executing.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the deployed gh-pages tree and the live origin, not the local publish directory</name>
  <files>none — verification only</files>
  <read_first>
    - src/dashboard/index.html lines 38-50 — the `app-nav-root` and `app` container ids that identify the SPA in the built output
    - src/pages/widgets.html — the showcase markers to assert in the deployed copy
    - scripts/build-widgets.mjs lines 140-200 — which data directories `copyDataFiles` publishes, so the deployed `data/` assertion matches the intent
    - .planning/phases/16-dashboard-shell-data-contract/16-VERIFICATION.md lines 64-73 — the P08/D-08 gap block, which specifies exactly what "true of the deployed site" means
  </read_first>
  <action>
    Every assertion in this task runs against the REMOTE, never against `dist/widgets/`. The 16-09 pass failed precisely by asserting the local publish directory and calling it deployment.

    Run `git fetch origin gh-pages`, then assert against `origin/gh-pages`:
    - `git log -1 --format='%H %cI' origin/gh-pages` reports a commit newer than `7a1aa8a` / `2026-08-11T05:45:56Z` (the stale deploy the verifier found). Record the new sha and timestamp.
    - `git ls-tree origin/gh-pages --name-only` includes `assets`, `index.html`, `widgets.html`, `heatmap.html`, `pinmap.html`, `routes.html` and `data`.
    - `git show origin/gh-pages:index.html` contains both `id="app-nav-root"` and `id="app"` — proving the site root is the SPA and no longer the pre-Phase-16 static showcase.
    - `git show origin/gh-pages:widgets.html` contains at least one reference ending in `.iife.js` — proving the showcase survived at its own URL (D-08's second clause).
    - `git ls-tree origin/gh-pages:data --name-only` includes `dashboard`, `activities` and `streams` in addition to the pre-existing `geo`, `heatmap`, `routes` and `stats`.
    - `git show origin/gh-pages:data/dashboard/index.json` piped into a `node -e` reader reports `schemaVersion` 1 and a row count above 1,800; capture the first row's `id` into a shell variable for the HTTP checks below and confirm it begins with `i`.

    Then verify the live origin over HTTP at `https://bacilo.github.io/strava-widgets/`. GitHub Pages can lag the branch push by a minute or two, so poll rather than failing on the first attempt: retry each URL for up to five minutes before declaring failure. Using `curl -sS -o /dev/null -w '%{http_code}'`, require `200` for:
    - `/` (the SPA root)
    - `/widgets.html`
    - `/heatmap.html`, `/pinmap.html`, `/routes.html` (the pre-existing pages must not have been displaced by D-08)
    - `/data/dashboard/index.json`
    - `/data/activities/<first-row-id>.json` and `/data/streams/<first-row-id>.json`, using the actual first id read from the DEPLOYED index — this is the exact pair the browser will request for the first list row, and the pair the verifier proved works locally but that has never existed on the remote.
    Also fetch `/` with `curl -sS` and confirm the returned HTML contains `id="app-nav-root"`, so a cached old page is not mistaken for a successful deploy.

    Record every URL and its status code in the SUMMARY as a table. If any check still fails after five minutes of polling, report it as an open gap with the exact URL and status rather than declaring the plan done.
  </action>
  <verify>
    <automated>git fetch origin gh-pages && git show origin/gh-pages:index.html | grep -q 'id="app-nav-root"' && git ls-tree origin/gh-pages:data --name-only | grep -qx dashboard && test "$(curl -sS -o /dev/null -w '%{http_code}' https://bacilo.github.io/strava-widgets/)" = "200" && echo DEPLOY_VERIFIED</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%H %cI' origin/gh-pages` reports a sha other than `7a1aa8a` and a timestamp later than `2026-08-11T05:45:56Z`.
    - `git ls-tree origin/gh-pages --name-only` includes `assets`, `widgets.html` and `data`.
    - `git show origin/gh-pages:index.html | grep -c 'id="app-nav-root"'` prints at least `1`.
    - `git show origin/gh-pages:widgets.html | grep -c '.iife.js'` prints at least `1`.
    - `git ls-tree origin/gh-pages:data --name-only` includes `dashboard`, `activities` and `streams`.
    - The deployed `data/dashboard/index.json` reports `schemaVersion` 1 and more than 1,800 rows, with a first row id beginning with `i`.
    - `curl` returns `200` for every one of the eight URLs listed in the action, recorded as a table in the SUMMARY.
    - `curl -sS https://bacilo.github.io/strava-widgets/ | grep -c 'id="app-nav-root"'` prints at least `1`.
  </acceptance_criteria>
  <done>The published site — not the local build — is the dashboard SPA at the root, with the showcase at widgets.html, the three pre-existing pages intact, and every data URL the SPA can request returning 200.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repository to public remote | `git push` makes every byte of the unpushed commits publicly readable and irrevocable |
| CI runner to public gh-pages branch | The workflow publishes `dist/widgets/` — including a copied ~150MB data tree — to a public origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-14-01 | Information Disclosure | The unpushed commits about to become public | mitigate | Task 1 enumerates the pushed file set, asserts `.env`, `data/tokens.json` and `export_data/` are gitignored and absent from the diff, greps the diff content for credential-shaped strings, and requires `data/provenance.json` to contain no absolute home-directory paths. Any hit stops the plan before the push. |
| T-16-14-02 | Information Disclosure | The published activity archive on a public origin | accept | The archive is intentionally public — the site already publishes `data/geo`, `data/heatmap`, `data/routes` and `data/stats`, and D-11 knowingly accepts ~150MB of activity and stream JSON on `gh-pages`. Plan 16-16's human check includes a look at whether any location string is more precise than intended. |
| T-16-14-03 | Tampering | The CI gate steps added in plan 16-13 | mitigate | Task 2 forbids removing, skipping or adding `continue-on-error` to `Run test suite` or `Verify dashboard publish contract` to force a green run, and the acceptance criteria assert `git diff HEAD -- .github/workflows/daily-refresh.yml` is empty and both steps concluded `success` rather than `skipped`. |
| T-16-14-04 | Spoofing | Mistaking a cached or stale page for a successful deploy | mitigate | Task 3 asserts against `origin/gh-pages` directly and additionally requires the live `/` response body to contain `id="app-nav-root"`, plus a gh-pages HEAD newer than the stale `7a1aa8a` deploy. |
| T-16-14-SC | Tampering | npm installs | accept | This plan installs no packages. CI runs `npm ci` against the unchanged lockfile. No package legitimacy gate applies. |
</threat_model>

<verification>
1. `git rev-list --count origin/master..master` prints `0`.
2. `git ls-tree origin/master src/ --name-only` includes `src/dashboard`.
3. The dispatched `daily-refresh` run concluded `success`, with `Run test suite` and `Verify dashboard publish contract` both `success`.
4. `origin/gh-pages` HEAD is newer than `7a1aa8a` and its root `index.html` contains `id="app-nav-root"`.
5. All eight live URLs return 200, including the detail and stream JSON for the deployed index's first (`i`-prefixed) row.
</verification>

<success_criteria>
- The branch the publishing workflow builds from contains the phase's source, and nothing phase-related remains unpushed.
- The publishing run was gated by the test suite and the publish-contract probe, and both passed on the real runner.
- The live site root is the dashboard SPA, the widget showcase lives at `widgets.html`, and the three standalone pages still resolve.
- The deployed `data/` tree carries `dashboard/`, `activities/` and `streams/`, and the browser's first-row detail and stream URLs both return 200 from the live origin.
</success_criteria>

<output>
Create `.planning/phases/16-dashboard-shell-data-contract/16-14-SUMMARY.md` when done. Include the pushed commit count, the workflow run id and URL, the new gh-pages sha and timestamp, and the full live-URL status table.
</output>
