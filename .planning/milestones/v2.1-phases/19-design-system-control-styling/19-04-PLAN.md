---
phase: 19-design-system-control-styling
plan: 04
type: execute
wave: 4
depends_on: ["19-01", "19-02", "19-03"]
files_modified:
  - src/dashboard/styles.css
  - src/dashboard/styles.test.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  decisions: [D-04, D-09, D-10, D-11, D-12]
  truths:
    - "One global, unscoped focus ring exists: an inner --bg halo plus an --accent outer ring, with the UA outline suppressed in the same rule"
    - "The segmented control no longer clips its children, and its rounded silhouette is reproduced by end-child radii instead"
    - "The splits scroller has room for the 4px ring to paint inside its scroll box, and the records jump list was not given redundant padding"
    - "Five describe blocks assert that every Phase 19 rule exists in the stylesheet source, so a later phase cannot delete them silently"
    - "No luminance or contrast helper was added to the test suite — the two ratios are discharged by closed-form computation"
  artifacts:
    - path: "src/dashboard/styles.css"
      provides: "two-tone :focus-visible ring and the two container clipping fixes"
      contains: "box-shadow: 0 0 0 2px var(--bg), 0 0 0 4px var(--accent)"
    - path: "src/dashboard/styles.test.ts"
      provides: "five Phase 19 describe blocks guarding the element-level rules against silent deletion"
      contains: "styles.css — Phase 19 focus ring"
  key_links:
    - from: ":focus-visible box-shadow ring"
      to: ".segmented__option--active and .pagination__button--current"
      via: "the always---bg inner halo, which separates the ring from any fill including --accent-strong"
      pattern: "0 0 0 2px var\\(--bg\\)"
---

<objective>
Replace the global `outline` focus ring with a two-tone `box-shadow` ring that is visible on any fill including the two `--accent-strong` filled active states, fix the two containers that would clip it, and lock every Phase 19 rule against silent deletion with five new text-assertion describe blocks.

Purpose: today's `:focus-visible { outline: 2px solid var(--accent) }` is orange-on-orange where it matters most — an `--accent` ring on an `--accent-strong` fill (`#fc4c02` on `#b3390a` light, `#ff6b35` on `#c2410c` dark) on the current pagination page and the active segmented option. The two-tone ring's inner stop is always `--bg`, which reduces UI-02's open-ended contrast requirement to exactly two precomputed pairs. And because an ancestor's `overflow` clips `box-shadow` exactly as it clipped `outline`, the ring alone is necessary but not sufficient.
Output: one replaced rule, one removed declaration, three added rules in `styles.css`; five describe blocks in `styles.test.ts`.
</objective>

<contrast_record>
## UI-02 non-text contrast — discharged by computation, not by test (D-11)

Threshold: **3:1** (WCAG 2.1 SC 1.4.11). Because the ring's inner stop is always `--bg`, the accent ring is adjacent to exactly one colour per theme, so the requirement reduces to two fixed pairs. Both were computed independently in `19-RESEARCH.md` from the raw hex token values via the W3C relative-luminance formula and agree with `19-UI-SPEC.md`:

| Theme | Ring colour | Adjacent | Contrast ratio | Result |
|-------|-------------|----------|----------------|--------|
| Light | `--accent` `#fc4c02` | `--bg` `#ffffff` | **3.40:1** | clears 3:1, margin 0.40 (~13%) |
| Dark | `--accent` `#ff6b35` | `--bg` `#1a1a2e` | **6.02:1** | clears 3:1 comfortably |

The numeric claim is **closed**. Per D-11 no luminance or contrast-ratio helper is added to `styles.test.ts` — these values change only if `--accent` or `--bg` themselves change, which is out of this phase's scope. The human checkpoint verifies only that the ring actually *renders*, with light theme scrutinised first because its margin is the narrower one.
</contrast_record>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-design-system-control-styling/19-CONTEXT.md
@.planning/phases/19-design-system-control-styling/19-UI-SPEC.md
@.planning/phases/19-design-system-control-styling/19-PATTERNS.md
@.planning/phases/19-design-system-control-styling/19-VALIDATION.md
@.planning/phases/19-design-system-control-styling/19-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the focus ring and fix both clipping containers — one commit</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/styles.css` lines 330-336 (the `/* Global focus ring */` comment and the `:focus-visible` rule being replaced), 779-797 (`.segmented`, `.segmented__option`, `.segmented__option--active`), 801-804 (`.splits-scroll`), 952-962 (`.records-jump`, which must NOT be changed), and 936-946 (`.sr-only`, whose `overflow: hidden` at line 942 is the visually-hidden clip and is not a focus-ring concern)
    - `.planning/phases/19-design-system-control-styling/19-UI-SPEC.md` § Color § "NEW: Focus ring" and § Focus Ring Contract — the exact rule, why `outline: none` is load-bearing, the three-container enumeration, and the D-12 inheritance contract
    - `.planning/phases/19-design-system-control-styling/19-PATTERNS.md` § 7 (`.segmented` quoted verbatim, and the note that its rounded corners today come **entirely** from `overflow: hidden` clipping square children), **Landmine 6** (`outline: none` is the highest-risk one-line omission in the phase) and **Landmine 7** (why D-09 and D-10 must land in the same commit)
    - `.planning/phases/19-design-system-control-styling/19-RESEARCH.md` § "Overflow-clipping ancestors — D-10 completeness check" — the four-selector table proving the enumeration is exhaustive, and **Pitfall 1** and **Pitfall 3**
    - `src/dashboard/views/detail-charts.ts` — read only far enough to confirm the x-axis toggle is the single `.segmented` instance and that no markup change is needed. Do not modify this file.
  </read_first>
  <action>
All of the following land in **one commit**. Per PATTERNS Landmine 7, removing `.segmented`'s `overflow: hidden` without the end-child radii in the same commit leaves the segmented control rendering with square inner corners; and per RESEARCH Pitfall 3, shipping the ring without the container fix would demo a ring that is still completely invisible on the one control it was built to fix.

1. **Replace the `:focus-visible` rule in place** at its existing location (lines 332-335, under the `/* Global focus ring */` comment). Do not move it into the Phase 19 block and do not add a second focus rule. Remove both `outline: 2px solid var(--accent)` and `outline-offset: 2px`, and declare instead:
   - `outline` → `none`
   - `box-shadow` → `0 0 0 2px var(--bg), 0 0 0 4px var(--accent)`

   `outline: none` is load-bearing, not cosmetic: once the custom `outline` declaration is gone, browsers that supply a default UA focus outline paint it *in addition to* the box-shadow, producing a visible double ring. It must be in the same rule.

   Keep the selector bare and unscoped (D-12). Do not scope it to controls. Leaving it unscoped is what makes links and the clickable rows Phase 20 adds inherit the ring with no opt-in work, which is what ROADMAP's Phase 20 dependency on this phase requires. Extend the `/* Global focus ring */` comment to record the two-tone rationale (the inner stop is always `--bg`, so the ring never sits accent-on-accent against an `--accent-strong` fill), the two precomputed contrast ratios from this plan's `<contrast_record>` block, and that `outline: none` must not be removed.

2. **Remove `overflow: hidden` from `.segmented`** (line 783). Leave its `display: inline-flex`, `border: 1px solid var(--border)` and literal `border-radius: 4px` exactly as they are — the container's own radius still draws the outer border sliver, and it stays on its literal value per the narrow token back-substitution scope.

3. **Add two end-child radius rules** immediately after the existing `.segmented__option` rules (either side of `.segmented__option--active` is fine; they target different pseudo-classes on the same element and do not overlap declarations):
   - `.segmented__option:first-child` → `border-radius: var(--radius-control) 0 0 var(--radius-control)`
   - `.segmented__option:last-child` → `border-radius: 0 var(--radius-control) var(--radius-control) 0`

   `.segmented__option` declares no `border-radius` today, so these two rules plus the container's own radius must visually reproduce the previous silhouette. This is a real change of visual mechanism, not a refactor — record it in the summary as a named checkpoint call-out alongside the D-08 row-hover deviation.

4. **Add `padding: var(--space-xs)` to `.splits-scroll`** (line 801). It currently declares only `overflow-x: auto` and `-webkit-overflow-scrolling: touch`, i.e. zero padding, so a focused element at either scroll edge would have no room for the 4px ring. There is no focusable element inside this wrapper today; the guard exists for consistency and for anything added later.

5. **Do not change `.records-jump`.** It already declares `padding: var(--space-sm)` (8px), which exceeds the 4px the ring needs. 19-UI-SPEC calls this out explicitly so the executor does not add redundant padding. Confirm the existing 8px is still there and leave it.

Per RESEARCH's completeness check, `grep -n overflow` returns exactly six lines on four selectors, and the fourth — `.sr-only` at line 942 — is the visually-hidden 1px-clip utility, never itself focusable and never clipping a focusable descendant. **No action on `.sr-only`.** Its `overflow: hidden` must survive this task untouched.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const c=require('fs').readFileSync('src/dashboard/styles.css','utf8');const n=c.replace(/\/\*[\s\S]*?\*\//g,'');const m=n.match(/:focus-visible\s*\{([^}]*)\}/);if(!m)throw new Error('no :focus-visible rule');if(!m[1].includes('outline: none'))throw new Error('outline: none missing — double-ring risk');if(!m[1].includes('box-shadow: 0 0 0 2px var(--bg), 0 0 0 4px var(--accent)'))throw new Error('two-tone ring missing');if(m[1].includes('outline-offset'))throw new Error('outline-offset survived');console.log('focus ring OK')"` exits 0 and prints `focus ring OK`.
    - The **comment-stripped** `box-shadow` count is `1` — this is the file's only `box-shadow` declaration, so a second focus-ring variant cannot have been added: `node -e "const c=require('fs').readFileSync('src/dashboard/styles.css','utf8').replace(/\/\*[\s\S]*?\*\//g,'');const n=(c.match(/box-shadow/g)||[]).length;if(n!==1)throw new Error('expected 1 box-shadow occurrence, found '+n);console.log('one box-shadow OK')"` exits 0. Comment-stripped rather than a raw `grep -c`: this task's own extended `/* Global focus ring */` comment discusses the box-shadow in prose, and a raw count would score that prose. Task 2's Block 4 uses the same comment-stripped form (`cssNoComments.match(/box-shadow/g)`), so the two gates agree. (Today's raw count is `0`.)
    - The **comment-stripped** `outline` count is `1` — only the `outline: none` in the ring rule remains, and `outline-offset` (which contains the substring `outline`) is gone: `node -e "const c=require('fs').readFileSync('src/dashboard/styles.css','utf8').replace(/\/\*[\s\S]*?\*\//g,'');const n=(c.match(/outline/g)||[]).length;if(n!==1)throw new Error('expected 1 outline occurrence, found '+n);console.log('one outline OK')"` exits 0. This one **must** be comment-stripped: item 1 of this task's action requires the extended comment to state that `outline: none` must not be removed, so a raw `grep -c 'outline'` would return `2` and fail for the wrong reason — a self-invalidating gate. Today's raw count is `2`, both of them declarations (lines 333-334), with zero comment occurrences.
    - The **comment-stripped** `overflow: hidden` count is `1`, down from `2` today (`.segmented` line 783, `.sr-only` line 942) — `.segmented`'s is gone, `.sr-only`'s survives: `node -e "const c=require('fs').readFileSync('src/dashboard/styles.css','utf8').replace(/\/\*[\s\S]*?\*\//g,'');const n=(c.match(/overflow: hidden/g)||[]).length;if(n!==1)throw new Error('expected 1 overflow: hidden occurrence, found '+n);console.log('one overflow: hidden OK')"` exits 0. Comment-stripped for the same reason as the two counts above: any comment written about the removal would otherwise inflate it. Note that a raw whole-file `not.toContain('overflow: hidden')` check is **impossible** regardless, because `.sr-only` legitimately keeps its own; the test-suite assertion must be scoped to `.segmented`.
    - `node -e "const c=require('fs').readFileSync('src/dashboard/styles.css','utf8');const n=c.replace(/\/\*[\s\S]*?\*\//g,'');const seg=n.match(/\.segmented\s*\{([^}]*)\}/);if(!seg)throw new Error('.segmented rule missing');if(seg[1].includes('overflow'))throw new Error('.segmented still clips');if(!seg[1].includes('border-radius: 4px'))throw new Error('.segmented lost its own radius');const sr=n.match(/\.sr-only\s*\{([^}]*)\}/);if(!sr||!sr[1].includes('overflow: hidden'))throw new Error('.sr-only was disturbed');console.log('segmented + sr-only OK')"` exits 0.
    - `grep -c 'border-radius: var(--radius-control) 0 0 var(--radius-control)' src/dashboard/styles.css` returns `1` and `grep -c 'border-radius: 0 var(--radius-control) var(--radius-control) 0' src/dashboard/styles.css` returns `1`.
    - `node -e "const c=require('fs').readFileSync('src/dashboard/styles.css','utf8');const n=c.replace(/\/\*[\s\S]*?\*\//g,'');const s=n.match(/\.splits-scroll\s*\{([^}]*)\}/);if(!s||!s[1].includes('padding: var(--space-xs)'))throw new Error('.splits-scroll padding missing');const r=n.match(/\.records-jump\s*\{([^}]*)\}/);if(!r||!r[1].includes('padding: var(--space-sm)'))throw new Error('.records-jump padding changed');if((r[1].match(/padding/g)||[]).length!==1)throw new Error('redundant padding added to .records-jump');console.log('scroll containers OK')"` exits 0.
    - D-09 and D-10 landed in the **same commit**, proven by inspecting the committed hunk's *content*. `git log -1 --name-only` cannot prove this and must not be used for it: every edit in this task is in `src/dashboard/styles.css`, so a name-only listing is byte-identical whether or not the two end-child radius rules shipped. Assert on the diff instead: `node -e "const{execSync}=require('child_process');const d=execSync('git show HEAD -- src/dashboard/styles.css').toString();const added=d.split('\n').filter(l=>l.startsWith('+')&&!l.startsWith('+++')).join('\n');const removed=d.split('\n').filter(l=>l.startsWith('-')&&!l.startsWith('---')).join('\n');for(const x of ['outline: none','box-shadow: 0 0 0 2px var(--bg), 0 0 0 4px var(--accent)','border-radius: var(--radius-control) 0 0 var(--radius-control)','border-radius: 0 var(--radius-control) var(--radius-control) 0','padding: var(--space-xs)'])if(!added.includes(x))throw new Error('not among this commit\'s added lines: '+x);if(!removed.includes('overflow: hidden'))throw new Error(\"the .segmented 'overflow: hidden' removal is not in this commit\");console.log('D-09 + D-10 co-located in one commit')"` exits 0 and prints `D-09 + D-10 co-located in one commit`. Co-location is also gated by the four Node criteria above all having to pass against the same single commit; this bullet is what makes the *commit boundary* itself observable.
    - `git status --porcelain src/dashboard/views src/dashboard/nav.ts` is empty — the `.segmented` fix is CSS-only, no `detail-charts.ts` markup change.
    - `npx vitest run src/dashboard/styles.test.ts` and `npx tsc --noEmit -p tsconfig.json` both exit 0.
  </acceptance_criteria>
  <done>One unscoped `:focus-visible` rule declares `outline: none` plus the two-tone box-shadow and is the file's only `box-shadow`; `.segmented` no longer clips and its silhouette is carried by the two end-child radius rules; `.splits-scroll` has 4px of room and `.records-jump` is unchanged; `.sr-only` is untouched; and all of it is one commit.</done>
</task>

<task type="auto">
  <name>Task 2: Add the five Phase 19 describe blocks to styles.test.ts</name>
  <files>src/dashboard/styles.test.ts</files>
  <read_first>
    - `src/dashboard/styles.test.ts` — the whole 142-line file: the header comment explaining why assertions are over stylesheet text, the `css` / `cssNoComments` split and why negative assertions use raw `css`, the `declarationsFor()` and `selectorListDeclares()` helpers with their doc comments, and the three existing describe blocks (lines 59, 95, 109) for the naming and grouping convention
    - `.planning/phases/19-design-system-control-styling/19-PATTERNS.md` § 6 — the exact helper behaviour, in particular that `declarationsFor()` does a literal-selector regex match and will **not** find a multi-line comma-separated head like `input, select, textarea`, so combined heads must use `selectorListDeclares()`; and the `describe` title convention `'styles.css — Phase <N> <short topic>'`
    - `.planning/phases/19-design-system-control-styling/19-UI-SPEC.md` § Phase-Specific Notes — the five `describe` block titles to use verbatim
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` — the two-mechanism constraint, so no assertion is written that implies a rendering outcome
    - `src/dashboard/styles.css` — the final state of every rule being asserted, so each needle string is copied from the actual file rather than from a spec document
  </read_first>
  <action>
Append five new `describe` blocks to `src/dashboard/styles.test.ts`, using the titles from 19-UI-SPEC verbatim and the `it('...')` phrasing style of the existing Phase 17 block. Reuse the existing `declarationsFor()` and `selectorListDeclares()` helpers; do not redefine either.

**One new module-level helper is required and is the only addition permitted.** `selectorListDeclares()` splits a rule head on `,`, so it cannot match plan 19-03's hover selector, whose head contains commas *inside* `:where(:not(…))`. Add a small helper alongside the existing two — name it `ruleWithHeadContaining(needle: string): string` — that scans `cssNoComments` with the same `/([^{}]+)\{([^}]*)\}/g` idiom already used by `selectorListDeclares()` and returns `head + body` for the first rule whose head contains the needle, throwing a named error when not found so a deleted rule fails loudly. Give it a doc comment in the same style as the existing two, stating why `selectorListDeclares()` cannot serve this case.

**Block 1 — `'styles.css — Phase 19 control baseline'`** (D-01 to D-03). Use `selectorListDeclares()`, never `declarationsFor('input, select, textarea')` — the latter cannot match that head. Assert `selectorListDeclares('input', …)` is true for `border: 1px solid var(--border)`, `background: var(--surface)`, `padding: var(--space-xs) var(--space-sm)`, `border-radius: var(--radius-control)` and `min-height: 32px`; that `selectorListDeclares('select', 'min-height: 32px')` and `selectorListDeclares('textarea', 'font: inherit')` are true; and that `selectorListDeclares('input[type="checkbox"]', 'border: none')` is true. Add one D-02 negative assertion using the house raw-`css` idiom: `expect(css).not.toContain('::-webkit-')`. That needle is safe on raw text — the file has zero `::-webkit-` occurrences and `-webkit-overflow-scrolling` is a property, not a `::` pseudo-element.

**Block 2 — `'styles.css — Phase 19 button baseline'`** (D-05, D-06, D-08). Assert `selectorListDeclares('button', …)` is true for `font: inherit`, `min-height: 32px`, `cursor: pointer` and `border-radius: var(--radius-control)`. Assert the hover rule via `ruleWithHeadContaining(':where(:not(')` and check its text contains the declaration `color-mix(in srgb, var(--surface) 92%, var(--text))` and all six exclusion tokens: `:disabled`, `[aria-disabled="true"]`, `.pagination__button--current`, `.segmented__option--active`, `.calendar-day--tint-1` and `.calendar-day--tint-4`. Assert that no bare hover rule exists with `expect(selectorListDeclares('button:hover', '')).toBe(false)` — an empty declaration needle makes that helper return true only when a rule head contains exactly `button:hover`, so this is a precise negative. Assert `selectorListDeclares('.cta:hover', 'color-mix(in srgb, var(--accent) 92%, var(--text))')` is true, and that `declarationsFor('.activity-table tbody tr:hover')` contains `color-mix(in srgb, var(--surface) 92%, var(--text))`. Add the negative `expect(css).not.toContain('92%, black)')` for the retired literal-black mix.

**Block 3 — `'styles.css — Phase 19 disabled treatment'`** (D-07). Use `selectorListDeclares()` for both arms — `declarationsFor(':disabled')` would **throw**, because the head is `:disabled,` followed by a comma rather than `{`. Assert `selectorListDeclares(':disabled', …)` is true for `color: var(--text-secondary)`, `opacity: 0.6` and `cursor: default`, and that `selectorListDeclares('[aria-disabled="true"]', 'opacity: 0.6')` is true — both selectors are required, since `calendar.ts:112` uses the `disabled` property and `calendar.ts:131` uses `aria-disabled`.

**Block 4 — `'styles.css — Phase 19 focus ring'`** (D-09, D-10, D-12). Assert `declarationsFor(':focus-visible')` contains `outline: none` and `box-shadow: 0 0 0 2px var(--bg), 0 0 0 4px var(--accent)`, and does **not** contain `outline-offset`. Assert the ring is the file's only one: `expect((cssNoComments.match(/box-shadow/g) ?? []).length).toBe(1)`. For the `.segmented` clipping fix use a **scoped** negative — `expect(declarationsFor('.segmented')).not.toContain('overflow')` — and add an inline comment stating why the house raw-`css` idiom cannot be used here: `.sr-only` legitimately declares `overflow: hidden` and `.records-jump`/`.splits-scroll` declare `overflow-x`, so a whole-file negative would fail for the wrong reason. Also assert `declarationsFor('.sr-only')` still contains `overflow: hidden`, so the scoped fix cannot have been over-applied. Assert `selectorListDeclares('.segmented__option:first-child', 'border-radius: var(--radius-control) 0 0 var(--radius-control)')` and `selectorListDeclares('.segmented__option:last-child', 'border-radius: 0 var(--radius-control) var(--radius-control) 0')` are true, that `declarationsFor('.splits-scroll')` contains `padding: var(--space-xs)`, and that `declarationsFor('.records-jump')` still contains `padding: var(--space-sm)`.

**Block 5 — `'styles.css — Phase 19 radius tokens'`** (D-13). Assert `declarationsFor(':root')` contains `--radius-control: 4px` and `--radius-panel: 8px`; that neither `declarationsFor(':root[data-theme="light"]')` nor `declarationsFor(':root[data-theme="dark"]')` contains `--radius-`, proving the theme-invariant shape; that `selectorListDeclares(s, 'border-radius: var(--radius-panel)')` and `selectorListDeclares(s, 'padding: var(--space-lg)')` are both true for each of `.error-state`, `.empty-state`, `.calendar-picker` and `.config-notice`; that `selectorListDeclares('.stat-grid', 'gap: var(--space-lg)')` is true; and the negatives `expect(css).not.toContain('padding: var(--space-2xl)')` and `expect(css).not.toContain('gap: var(--space-xl)')`.

**Do not add a luminance or contrast-ratio helper** (D-11). The two ratios are closed-form and recorded in this plan's `<contrast_record>` block, in `19-UI-SPEC.md` and in `19-RESEARCH.md`; they change only if `--accent` or `--bg` change, which is out of scope.

**Do not write any test name or comment that implies a rendering outcome.** Per 19-VALIDATION.md these assertions prove that a rule exists in the stylesheet source and nothing else — not computed styles, not rendered boxes, not native picker chrome, not perceptual contrast, not click behaviour. Phrase every `it()` as an assertion about the stylesheet declaring something, in the style of the existing `'.theme-toggle pins color: var(--text)'`, never as `'inputs look styled'` or similar. Add a one-line comment at the head of the five blocks restating that the human checkpoint in plan 19-05 is the sole proof of rendering.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npm test && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/styles.test.ts` exits 0 with all five new describe blocks reported and zero skipped tests.
    - `npm test` exits 0 and the total passing count is strictly greater than the pre-Phase-19 baseline of 884.
    - `grep -c "describe('styles.css — Phase 19" src/dashboard/styles.test.ts` returns `5`.
    - The five titles match 19-UI-SPEC verbatim: `grep -o "styles.css — Phase 19 [a-z ]*" src/dashboard/styles.test.ts | sort -u` lists exactly `control baseline`, `button baseline`, `disabled treatment`, `focus ring`, `radius tokens`.
    - `grep -ci 'luminance\|contrastRatio\|relativeLuminance' src/dashboard/styles.test.ts` returns `0` — D-11 forbids a contrast helper.
    - `grep -c "declarationsFor(':disabled')" src/dashboard/styles.test.ts` returns `0` and `grep -c "declarationsFor('input" src/dashboard/styles.test.ts` returns `0` — neither combined-selector head is queried with the wrong helper.
    - `grep -c "expect(css).not.toContain('overflow" src/dashboard/styles.test.ts` returns `0` — the `.segmented` negative is scoped, not whole-file.
    - `grep -c 'function ruleWithHeadContaining' src/dashboard/styles.test.ts` returns `1`, and `grep -c 'function declarationsFor\|function selectorListDeclares' src/dashboard/styles.test.ts` returns `2` — the existing helpers were reused, not redefined.
    - **Mutation check, recorded in the summary:** temporarily delete the `input, select, textarea` rule from `styles.css`, confirm `npx vitest run src/dashboard/styles.test.ts` exits non-zero, then restore the rule and confirm it exits 0 again. Repeat for `outline: none` in the `:focus-visible` rule. Record both observed failure messages verbatim — an assertion that cannot fail is not a guard.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>Five correctly-titled describe blocks assert every Phase 19 rule using the right helper for each selector shape, one new helper handles the comma-bearing hover head, no contrast helper was added, no test name implies a rendering outcome, and the mutation check proves two of the assertions actually fail when their rule is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard user → focus indication | The only channel telling a keyboard user where they are. A suppressed outline with a non-working replacement makes the whole dashboard keyboard-unusable |
| ancestor `overflow` → descendant ring paint | `overflow` clips `box-shadow` exactly as it clipped `outline`; the clipping ancestor is often several levels above the focused element |
| green test suite → phase gate | Text assertions are cheap and near-meaningless about rendering; treating green as proof is this project's documented recurring failure |

**Not applicable, stated explicitly rather than padded:** no input handling (V5), no auth (V2), no session (V3), no access control (V4), no cryptography (V6). Zero network calls, zero data files, zero build-script changes, zero new dependencies — including zero new test dependencies, which 19-VALIDATION.md explicitly does not authorize.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-A11Y-14 | Denial of Service (keyboard access lost) | `outline: none` shipping without a working replacement ring | mitigate | `outline: none` and the `box-shadow` are required in the *same* rule, asserted by a Node check in Task 1 and by Block 4's `declarationsFor(':focus-visible')` assertions; the mutation check in Task 2 proves removing `outline: none` turns the suite red |
| T-19-A11Y-15 | Denial of Service (ring invisible where it matters most) | `.segmented`'s `overflow: hidden` clipping the ring on the active option | mitigate | Removed in the same commit as the ring, with the end-child radii replacing the clipping mechanism; asserted by a scoped `.segmented` negative rather than an unscoped whole-file one, which is impossible because `.sr-only` legitimately declares `overflow: hidden` |
| T-19-A11Y-16 | Denial of Service (double ring) | UA default outline painting alongside the box-shadow | mitigate | RESEARCH Pitfall 1; `outline: none` in the same rule, plus the comment-stripped `outline` occurrence count asserted to be exactly `1` (comment-stripped because this task's own comment names `outline: none`, which would inflate a raw `grep -c`) |
| T-19-CASCADE-17 | Tampering (over-applied fix) | a whole-file `overflow` removal also stripping `.sr-only`'s clip | mitigate | Task 1 asserts `.sr-only` still declares `overflow: hidden` and that the comment-stripped `overflow: hidden` count is exactly `1`; Block 4 re-asserts it in the suite |
| T-19-FALSEGREEN-18 | Repudiation (assertion that cannot fail) | text assertions written against a spec instead of the file | mitigate | Every needle is copied from the actual final `styles.css`, and Task 2's mutation check requires an observed red run for two representative assertions with the failure messages recorded verbatim |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs authorized, including no jsdom, happy-dom, @testing-library, puppeteer or playwright. 19-VALIDATION.md states the two-mechanism constraint is not to be escaped by adding a dependency. RESEARCH.md § Package Legitimacy Audit records `Disposition: N/A` |
</threat_model>

<verification>
- `npx vitest run src/dashboard/styles.test.ts` exits 0; `npm test` exits 0 above the 884 baseline.
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- Comment-stripped occurrence counts in `src/dashboard/styles.css`: `box-shadow` is `1`, `outline` is `1`, `overflow: hidden` is `1`. All three are comment-stripped rather than raw `grep -c`, because this plan's own extended `/* Global focus ring */` comment discusses `outline: none` and the box-shadow in prose — a raw count would fail for the wrong reason.
- The mutation check produced an observed red run for two assertions, with messages recorded.
- **Not proven by any of the above:** that the ring renders at all; that it renders unclipped on `.segmented`, `.records-jump`, `.splits-scroll` or on the two `--accent-strong` fills; that no *other* ancestor clips it; that the segmented control's silhouette still matches; or that the light-theme 3.40:1 ring is perceptually clear rather than merely technically passing. There is no CSSOM and no rendering engine in this repo. All of it is deferred to plan 19-05's human checkpoint.
</verification>

<success_criteria>
One global, unscoped `:focus-visible` rule declares `outline: none` plus the two-tone `--bg`/`--accent` box-shadow and is the file's only `box-shadow`; `.segmented` no longer clips and reproduces its silhouette via end-child radii, landed in the same commit; `.splits-scroll` has ring room and `.records-jump` and `.sr-only` are untouched; five correctly-titled describe blocks guard every Phase 19 rule with the correct helper per selector shape and are proven capable of failing; and no luminance helper or test dependency was added.
</success_criteria>

<output>
Create `.planning/phases/19-design-system-control-styling/19-04-SUMMARY.md` when done. Record: the `:focus-visible` rule before and after, confirmation that D-09 and D-10 landed in one commit with its hash, the `.segmented` silhouette-mechanism change flagged as a checkpoint call-out, the two contrast ratios restated with their source, the five describe block titles with their assertion counts, the new helper's signature and why it was needed, and both mutation-check failure messages verbatim.
</output>
