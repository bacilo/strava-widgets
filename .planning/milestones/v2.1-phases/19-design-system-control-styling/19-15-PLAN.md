---
phase: 19-design-system-control-styling
plan: 15
type: execute
wave: 15
depends_on: [19-14]
files_modified:
  - src/dashboard/styles.test.ts
autonomous: true
requirements: [UI-02]
user_setup: []

must_haves:
  truths:
    - "A helper can no longer silently return the body of a rule nested inside an @media block"
    - "Both single-match helpers resolve last-wins, matching how CSS actually resolves the cascade"
    - "The shared hover rule's guard fails when :hover is deleted and when the button scope is widened"
    - "Every claimed guarantee in the helper-audit comment matches what the helpers actually do"
  artifacts:
    - path: "src/dashboard/styles.test.ts"
      provides: "At-rule-range rejection by brace matching, last-wins resolution, an anchored and escaped numeric extractor, one shared rule scanner, and a head-shape hover assertion"
      contains: "AT_RULE_RANGES"
  key_links:
    - from: "assertNotAtRuleScoped"
      to: "bodyForSelectorListToken and ruleWithHeadContaining call sites"
      via: "match.index passed as the offset argument"
      pattern: "assertNotAtRuleScoped\\("
    - from: "the helper-audit comment"
      to: "the helper implementations"
      via: "each documented guarantee restated to match the code after this plan"
      pattern: "bodyForSelectorListToken:"
    - from: "the shared hover assertion"
      to: "splitTopLevelSelectors"
      via: "the :not() argument parsed out of the head and compared as an ordered list"
      pattern: "splitTopLevelSelectors\\("
  decisions: [D-05, D-06, D-07, D-09, D-10, D-12, D-13]
---

<objective>
Close the guard-layer findings from `19-REVIEW-round3.md`: the at-rule hole that
`assertNotAtRuleHead` does not actually close (R3-CR-01), the hover assertion that survives
deletion of both the pseudo-class and the element scope (R3-CR-02), the first-wins helpers
that read a value the cascade does not use (R3-WR-02), and the substrate duplication and
unescaped regex that keep the whole class alive (R3-IN-01, R3-IN-03).

Purpose: this phase's gate is open because of false greens. Waves 10-12 shipped three new
guards sold as closing holes; the reviewer proved two of them green under mutation. A guard
that cannot fail is worse than a missing guard, because the file now documents the hole as
closed.

Output: one test file whose helpers model at-rule nesting and the cascade correctly, whose
audit comment states only what is true, and whose two headline assertions have been watched
failing under the exact mutations the review executed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-design-system-control-styling/19-REVIEW-round3.md
@src/dashboard/styles.test.ts
@src/dashboard/styles.css
</context>

<constraints>
This plan changes `src/dashboard/styles.test.ts` only. `git status --porcelain
src/dashboard/styles.css` must be empty at the end of every task — no CSS declaration and
no CSS comment may be edited here. Plan 19-16 owns the stylesheet's comments.

Every acceptance criterion below is mutation-based. `19-REVIEW-round3.md` proved each of
these assertions green while the defect it names was present, so "the assertion exists" and
"the suite is green" prove nothing. The required evidence is a recorded terminal failure
under a named mutation, followed by a recorded pass after reverting it.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden the helper substrate — at-rule ranges, last-wins, anchored extraction, one scanner</name>
  <files>src/dashboard/styles.test.ts</files>
  <read_first>
    - `src/dashboard/styles.test.ts` lines 30-50 — `declarationsFor` and its existing regex escaping, the precedent R3-IN-01 says `extractNumericDeclaration` should have followed
    - `src/dashboard/styles.test.ts` lines 60-90 — `splitTopLevelSelectors` and its depth handling
    - `src/dashboard/styles.test.ts` lines 89-198 — `selectorListDeclares`, `assertNotAtRuleHead`, `bodyForSelectorListToken`, `extractNumericDeclaration`, `ruleWithHeadContaining`, and the three copies of the `/([^{}]+)\{([^}]*)\}/g` scanner
    - `src/dashboard/styles.test.ts` lines 200-230 — the `splitTopLevelSelectors` self-test block, the pattern the new self-tests must match
    - `.planning/phases/19-design-system-control-styling/19-REVIEW-round3.md` § R3-CR-01, § R3-WR-02, § R3-IN-01, § R3-IN-03 — the executed mutation outputs and the proposed `AT_RULE_RANGES` and `extractNumericDeclaration` implementations
    - `src/dashboard/styles.css` lines 420-450 — the `@media (max-width: 640px)` block containing `.app-nav[data-open="true"] .app-nav__links`, the media-nested-only selector the review used as its proof
  </read_first>
  <behavior>
    - `bodyForSelectorListToken('.app-nav[data-open="true"] .app-nav__links')` THROWS. Today it resolves and returns the `@media` body — that is R3-CR-01's executed proof.
    - `bodyForSelectorListToken('.app-nav__toggle')` still resolves to the top-level rule's body, not the media-nested one, because the top-level rule exists and the media-nested match is rejected by offset.
    - Given synthetic CSS declaring a token's rule twice, `bodyForSelectorListToken` returns the LAST rule's body.
    - Given a body declaring `z-index` twice, `extractNumericDeclaration` returns the LAST value.
    - `extractNumericDeclaration` does not match a property name occurring as a suffix of another declaration, and does not throw or misbehave on a property containing regex metacharacters.
    - The whole existing suite stays green: every current needle resolves to exactly one rule today, so last-wins is behaviour-preserving for them.
  </behavior>
  <action>
Replace the three duplicated copies of `/([^{}]+)\{([^}]*)\}/g` with a single factory
`RULE_SCANNER()` returning a fresh `RegExp` per call (`lastIndex` is stateful, so a shared
instance would be a new bug), and route `selectorListDeclares`, `bodyForSelectorListToken`
and `ruleWithHeadContaining` through it. Extract the
`body.split(';').map(trim).filter(Boolean)` idiom, currently repeated at three test sites,
into `declarationFragments(body)` and route those three callers through it.

Add `AT_RULE_RANGES`, computed once by brace matching over `cssNoComments`: for every match
of `/@[a-z-]+[^{]*\{/g`, walk forward tracking depth until the block closes, and record the
`[start, end]` offsets. Replace `assertNotAtRuleHead` with `assertNotAtRuleScoped(offset,
head, needle)` which throws when the head begins with `@` OR when the offset falls strictly
inside any recorded range. Pass `match.index` at both existing call sites in
`bodyForSelectorListToken` and `ruleWithHeadContaining`. Keep it deliberately out of
`selectorListDeclares` for the reason already documented — that helper walks every rule and
returns a boolean, so throwing would fire on the first at-rule it passes.

Make both single-match helpers last-wins, matching CSS. `bodyForSelectorListToken` collects
every non-at-rule-scoped match whose selector list contains the token and returns the last
one's body, throwing only when none matched. `extractNumericDeclaration` escapes `property`
with the same `.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')` guard `declarationsFor` already
uses, anchors the match to a declaration boundary (start of body, or a preceding `;` or
whitespace), uses `matchAll`, and returns the final match — throwing when there are none.

Give both helpers an optional trailing `source: string = cssNoComments` parameter so the new
self-tests can exercise them against synthetic stylesheets instead of only the real one.
Do not change any existing call signature at the call sites.

Add a `describe` block of self-tests, matching the style of the existing
`splitTopLevelSelectors` self-tests, covering: the media-nested-only token now throwing;
`.app-nav__toggle` still resolving to its top-level rule; last-wins for a token declared
twice in synthetic CSS; last-wins for a property declared twice inside one body; an anchored
non-match where the property name is a suffix of a longer property; and a property
containing a regex metacharacter.

Before committing, enumerate every needle any assertion in this file passes to
`bodyForSelectorListToken` and report, in the summary, how many rules each resolves against
today — the set where first-wins and last-wins differ is exactly the set of assertions whose
meaning changed. Today that set is expected to be empty; if it is not, say which needle and
what the value changed from and to.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npx tsc --noEmit -p tsconfig.json && node -e "const t=require('fs').readFileSync('src/dashboard/styles.test.ts','utf8');if(!/AT_RULE_RANGES/.test(t))throw new Error('AT_RULE_RANGES missing');if(!/assertNotAtRuleScoped/.test(t))throw new Error('assertNotAtRuleScoped missing');if(/assertNotAtRuleHead/.test(t))throw new Error('the dead assertNotAtRuleHead guard is still present');const scanners=(t.match(/\(\[\^\{\}\]\+\)\\\{/g)||[]).length;if(scanners>1)throw new Error('rule scanner regex still duplicated in '+scanners+' places');if(!/RULE_SCANNER/.test(t))throw new Error('RULE_SCANNER factory missing');if(!/declarationFragments/.test(t))throw new Error('declarationFragments helper missing');if(!/matchAll/.test(t))throw new Error('extractNumericDeclaration is not last-wins');if(!/replace\(\/\[\.\*\+\?\^\\\$\{\}\(\)\|\[\\\]\\\\\]\/g/.test(t))throw new Error('property is still interpolated into RegExp unescaped');console.log('substrate hardened: single scanner, at-rule ranges, last-wins, escaped extractor')"</automated>
  </verify>
  <acceptance_criteria>
    - **Mutation, executed and recorded:** a self-test calling `bodyForSelectorListToken('.app-nav[data-open="true"] .app-nav__links')` expects a throw. Before this task it resolved and returned `display: flex; flex-direction: column;` — quote the review's executed output and the new observed pass in the summary.
    - **Mutation, executed and recorded:** append `.app-nav { z-index: 0; }` (or, after 19-14, the equivalent override on whichever selector carries rung 4) to the end of `src/dashboard/styles.css`, run `npx vitest run src/dashboard/styles.test.ts`, observe the ladder test FAIL because the helper now reads the cascade winner, revert, observe green. Quote both terminal outputs. Under first-wins the review executed this exact mutation and the test stayed green.
    - **Mutation, executed and recorded:** duplicate a `z-index` declaration inside one rule body with a different value, observe `extractNumericDeclaration` return the second value, revert.
    - `bodyForSelectorListToken('.app-nav__toggle')` still resolves to the top-level rule — the fuller guard must reject media-nested matches without breaking the top-level ones.
    - The `/([^{}]+)\{([^}]*)\}/g` literal appears at most once in the file; `RULE_SCANNER` and `declarationFragments` exist and all previous callers route through them.
    - `extractNumericDeclaration` escapes its `property` argument and anchors the match to a declaration boundary; `assertNotAtRuleHead` no longer exists under that name.
    - The full suite is green: `npx vitest run src/dashboard/styles.test.ts` passes with a count equal to or greater than the pre-task count, and `npx tsc --noEmit` exits 0.
    - The summary lists every `bodyForSelectorListToken` needle with its match count, identifying any assertion whose value changed under last-wins.
    - `git status --porcelain src/dashboard/styles.css` is empty.
  </acceptance_criteria>
  <done>The helper substrate models at-rule nesting by offset and the cascade by last-wins, the scanner exists once, and each correction was watched failing under the review's own executed mutation before it passed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the hover assertion with a head-shape assertion that fails when :hover or the button scope is removed</name>
  <files>src/dashboard/styles.test.ts</files>
  <read_first>
    - `src/dashboard/styles.test.ts` lines 433-472 — the button-baseline describe block, the eight-token hover assertion at `:457-468` and its limitation comment at `:441-456`
    - `src/dashboard/styles.css` lines 1340-1360 — the shared hover rule `button:where(:not(…)):hover` and its `color-mix` declaration
    - `.planning/phases/19-design-system-control-styling/19-REVIEW-round3.md` § R3-CR-02 — the two executed mutations (removed `:hover`, removed `button` scope) with both staying green, and the proposed replacement assertion
    - `src/dashboard/styles.test.ts` lines 60-90 — `splitTopLevelSelectors`, which the replacement uses to compare the `:not()` argument as a list
  </read_first>
  <behavior>
    - Deleting `:hover` from the shared hover rule's head makes the test FAIL. Today it stays green while the surface-mix background applies to every non-excluded button unconditionally.
    - Widening the scope from `button` to `*` makes the test FAIL. Today it stays green while the hover mix applies to every element on the page.
    - Deleting any one of the eight exclusion tokens still makes the test FAIL — the WR-01 coverage this replaces must not be lost.
    - Reordering the eight tokens is a deliberate failure: the assertion compares the parsed list, so a reorder is visible in the diff rather than silently accepted.
  </behavior>
  <action>
Replace the assertion at `:457-468` with one that asserts the head's shape rather than
substrings of `head + body`. Resolve the rule with `ruleWithHeadContaining(':where(:not(')`,
slice off the head at the first `{`, normalise runs of whitespace to single spaces, then
assert the normalised head starts with `button:where(:not(` and ends with `)):hover`,
extract the `:not()` argument between those two anchors, and compare
`splitTopLevelSelectors` of that argument against the eight expected tokens as an ordered
list: `:disabled`, `[aria-disabled="true"]`, `.pagination__button--current`,
`.segmented__option--active`, `.calendar-day--tint-1`, `.calendar-day--tint-2`,
`.calendar-day--tint-3`, `.calendar-day--tint-4`. Keep the existing
`color-mix(in srgb, var(--surface) 92%, var(--text))` body check.

Rename the test so its title states what it now guards — that the rule is button-scoped,
hover-gated and excludes all eight tokens.

Delete the limitation comment at `:441-456` that documents the head-versus-body
indistinguishability, and replace it with a short comment stating what this assertion now
proves and what it still cannot: the tokens are extracted from the head only, so the old
limitation is retired, but the assertion still says nothing about whether the rule renders.

Apply R3-IN-04 in the same pass: the CR-02 test at `:618-626` repeats
`selectorListDeclares('button', 'border-radius: var(--radius-control)')`, which already
exists at `:438`. Keep the coupling — it is deliberate — but add one clause to the CR-02
comment stating that it duplicates the button-baseline assertion on purpose, so a reader
seeing two failures knows they are one fact.

Apply R3-IN-02 in the same pass: point all four ladder rungs at `bodyForSelectorListToken`
rather than leaving `.records-jump` alone on `declarationsFor`. `.records-jump` resolves as
an exact top-level selector token, so the switch is behaviour-preserving; confirm that by
running the suite. If any rung cannot use the shared helper, leave it and add one sentence
saying why, rather than leaving the split unexplained.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npx tsc --noEmit -p tsconfig.json && node -e "const t=require('fs').readFileSync('src/dashboard/styles.test.ts','utf8');const blk=t.split(/it\('the shared hover rule/)[1];if(!blk)throw new Error('renamed hover test not found');const body=blk.split('});')[0];if(!/startsWith\('button:where\(:not\('\)/.test(body))throw new Error('scope anchor assertion missing');if(!/endsWith\('\)\):hover'\)/.test(body))throw new Error('hover-gate anchor assertion missing');if(!/splitTopLevelSelectors/.test(body))throw new Error('exclusion list is not compared as a parsed list');if(/head-vs-body|cannot distinguish a selector token appearing in the HEAD/.test(t))throw new Error('the retired limitation comment is still present');const lad=t.split(/it\('the sticky-layer ladder/)[1].split('});')[0];if(/declarationsFor/.test(lad))throw new Error('ladder still mixes declarationsFor');console.log('hover assertion pins scope and hover gate; ladder uses one helper')"</automated>
  </verify>
  <acceptance_criteria>
    - **Mutation 1, executed and recorded:** change `button:where(:not(…)):hover` to `button:where(:not(…))` in `src/dashboard/styles.css`, run `npx vitest run src/dashboard/styles.test.ts`, observe FAIL, revert, observe green. Quote both outputs. The review executed this mutation against the old assertion and it stayed green.
    - **Mutation 2, executed and recorded:** change the `button` scope to `*`, observe FAIL, revert, observe green. Quote both outputs. The review executed this one too and it stayed green.
    - **Mutation 3, executed and recorded:** delete `.calendar-day--tint-3` from the exclusion list, observe FAIL, revert — proving the WR-01 eight-token coverage was not lost in the rewrite.
    - The test title names all three properties it guards (button scope, hover gate, eight exclusions), and the retired head-versus-body limitation comment is gone, replaced by a statement of what the new assertion does and does not prove.
    - The CR-02 test comment states in one clause that its `button` radius assertion deliberately duplicates the button-baseline test.
    - The ladder test uses one helper for all four rungs, or names the reason a rung cannot.
    - `npx vitest run src/dashboard/styles.test.ts` green, `npx tsc --noEmit` exit 0, `git status --porcelain src/dashboard/styles.css` empty after the mutations are reverted.
  </acceptance_criteria>
  <done>The shared hover rule's guard now fails on both mutations the review proved it survived, without losing the eight-token coverage it was widened to provide.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the helper-audit comment so it documents what the helpers actually guarantee</name>
  <files>src/dashboard/styles.test.ts</files>
  <read_first>
    - `src/dashboard/styles.test.ts` lines 330-400 — the whole helper-audit comment block, including the `bodyForSelectorListToken: … guarded` claim at `:349-353` and the `assertNotAtRuleHead now makes the two single-match helpers … fail loudly` claim at `:368-384`
    - `src/dashboard/styles.test.ts` lines 89-200 as rewritten by Task 1 — the actual coverage the comment must now describe
    - `.planning/phases/19-design-system-control-styling/19-REVIEW-round3.md` § R3-CR-01 closing paragraph and § R3-WR-02 closing paragraph — why documented-but-false safety is rated BLOCKER-tier here, and the two blind spots the audit omits
  </read_first>
  <action>
Rewrite the audit block so every sentence in it is true of the code after Tasks 1 and 2.
Specifically:

Restate the at-rule paragraph: brace-matched ranges are computed once over `cssNoComments`
and any match resolving at an offset inside one is rejected, so nested rules at every
position are caught — not only the prelude pseudo-rule, which was the unreachable case.
State that `selectorListDeclares` remains deliberately unguarded and its blind spot is still
a silent `false`, and say why (it walks every rule and returns a boolean).

Add the two blind spots R3-WR-02 found the audit omitting, then state their resolution:
`bodyForSelectorListToken` was first-rule-wins and `extractNumericDeclaration` was
first-declaration-wins while CSS resolves both last-wins; both are now last-wins. Name the
concrete case the review used — a later `.segmented__option { border-radius: var(--radius-control) }`
cancelling the `border-radius: 0` cancellation — so the next reader understands why this
mattered rather than only that it changed.

Note the substrate consolidation: one `RULE_SCANNER` factory and one `declarationFragments`
helper, so a future correction lands in one place instead of three.

Leave the closing paragraph about text-level assertions never observing a rendered page
intact — it is the one claim in this block that has held up all phase — and extend it with
one sentence recording that Round 4 added a fourth false-green mechanism to the record: a
guard that was documented as closing a hole it did not close, proven by mutation rather than
by reading. Cite `19-REVIEW-round3.md`.

Do not soften or delete the existing record of any earlier mechanism. This block is the
inheritance a future phase reads.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && node -e "const t=require('fs').readFileSync('src/dashboard/styles.test.ts','utf8');const start=t.indexOf('// - selectorListDeclares');const audit=t.slice(start,start+6000);if(/guarded — throws rather than silently returning the wrong block's body/.test(audit))throw new Error('the false guarantee is still documented');if(!/last-wins/.test(audit))throw new Error('audit does not document the cascade blind spot resolution');if(!/AT_RULE_RANGES|brace/.test(audit))throw new Error('audit does not describe the offset-based at-rule rejection');if(!/RULE_SCANNER|declarationFragments/.test(audit))throw new Error('audit does not note the substrate consolidation');if(!/19-REVIEW-round3/.test(t))throw new Error('the Round 4 mechanism is not cited');if(!/GAP 1/.test(audit))throw new Error('the earlier mechanisms were dropped from the record');console.log('audit comment matches the implementation')"</automated>
  </verify>
  <acceptance_criteria>
    - The string `guarded — throws rather than silently returning the wrong block's body` no longer appears; the at-rule paragraph describes offset-based rejection over brace-matched ranges and states that only `selectorListDeclares` remains unguarded, with its reason.
    - The audit documents both R3-WR-02 blind spots and states that both helpers are now last-wins, naming the `.segmented__option` cancellation case.
    - The audit names `RULE_SCANNER` and `declarationFragments` as the single points of correction.
    - The closing text-versus-rendered paragraph is intact, still references GAP 1, and gains one sentence recording the documented-but-false-guard mechanism with a citation to `19-REVIEW-round3.md`.
    - **Traceability check, executed:** for each of the three helpers, the summary quotes one sentence of the new audit text and the line of the implementation that makes it true. A claim with no corresponding line is a defect of exactly the kind this task closes.
    - `npx vitest run src/dashboard/styles.test.ts` green; `git status --porcelain src/dashboard/styles.css` empty.
  </acceptance_criteria>
  <done>Every guarantee the audit block states is one a reader can verify against a line of the implementation, and the phase's running record of false-green mechanisms is intact and extended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a guard's stated guarantee → what it actually catches | The boundary R3-CR-01 found breached: the file documents a closed hole that is open. A future author writing a claim behind that helper inherits the falsehood. |
| a green assertion → a defended invariant | R3-CR-02 and R3-WR-01 both stayed green under mutations that would ship severe, immediately visible regressions. Mutation-based acceptance is the control. |
| a source-text helper → the cascade the browser resolves | First-wins helpers read a value the browser does not use. Closed by last-wins in both helpers. |
| a test-file change → the stylesheet | This plan may not edit CSS; mutations are temporary and every task asserts the stylesheet is clean afterwards. |

**Application-security surface: not applicable, stated rather than padded.** This plan
changes one test file that reads a stylesheet as text in a Node process. No new data flow,
no authentication, session, access-control or cryptography surface, no user input handling,
no network egress, no runtime code shipped to a browser. The assessment would change if a
helper ever executed stylesheet content rather than parsing it as text, or if a browser
automation dependency were introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-G-01 | Repudiation (documented-but-false safety) | the helper-audit comment | mitigate | Task 3 rewrites every claim to match the implementation and its gate rejects the specific false sentence by exact string; Task 3's acceptance requires quoting the implementing line for each claim. |
| T-19-G-02 | Repudiation (an existence-based guard passing as a regression guard) | the hover assertion and the ladder assertion | mitigate | Every acceptance criterion names a specific mutation, requires it to be executed, and requires the failing and passing terminal output to be quoted. |
| T-19-G-03 | Tampering (a temporary mutation left in the stylesheet) | `src/dashboard/styles.css` | mitigate | Every task asserts `git status --porcelain src/dashboard/styles.css` is empty after the mutations are reverted, and this plan's `files_modified` excludes the stylesheet entirely. |
| T-19-G-04 | Tampering (last-wins silently changing what an existing assertion means) | all `bodyForSelectorListToken` call sites | mitigate | Task 1 requires enumerating every needle with its match count and naming any assertion whose value changed; the set is expected to be empty today and must be stated either way. |
| T-19-G-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized. No parser, no browser automation, no assertion library beyond the vitest already present. |
</threat_model>

<verification>
- `npx vitest run src/dashboard/styles.test.ts` green with a count at or above the pre-plan baseline; `npx tsc --noEmit -p tsconfig.json` exit 0.
- Seven mutations executed across the three tasks, each with its failing and passing terminal output quoted in the summary.
- `git status --porcelain src/dashboard/styles.css` empty after every task.
- `npm run build-widgets` exit 0 with zero `css-syntax-error` (the stylesheet is untouched, so this is a no-drift check).
</verification>

<success_criteria>
Every guard this round touches fails under the mutation the review proved it survived, the
helpers resolve at-rule nesting by offset and the cascade by last-wins, and the audit
comment documents only what the code does.
</success_criteria>

<output>
Create `.planning/phases/19-design-system-control-styling/19-15-SUMMARY.md` when done.
Record: each of the seven mutations with its command, its observed failure output and its
observed pass after reverting; the needle-to-match-count enumeration and whether any
assertion's value changed under last-wins; the three audit claims quoted alongside the
implementation line that makes each true; the vitest count before and after; and
confirmation that the stylesheet is byte-identical to its state at the end of plan 19-14.
</output>
