---
phase: 19-design-system-control-styling
plan: 16
type: execute
wave: 16
depends_on: [19-15]
files_modified:
  - src/dashboard/styles.css
  - .planning/phases/19-design-system-control-styling/deferred-items.md
autonomous: true
requirements: [UI-02]
user_setup: []

must_haves:
  truths:
    - "No comment in the shipped stylesheet states a rendered behaviour the project has measured to be false"
    - "The sticky-layer ladder comment describes the ordering it actually governs, and says plainly what it does not govern"
    - "Every claim about rendering in the ladder comment is either backed by a dated probe or marked as spec-derived and unobserved"
    - "No comment cites a line number in another source file"
    - "Everything consciously not done this round is written down with its reasoning"
  artifacts:
    - path: "src/dashboard/styles.css"
      provides: "A ladder comment whose rung 4 names the real sticky rung, whose stickiness claim carries its evidence, and whose scope is limited to sibling ordering in the root stacking context"
      contains: "Sticky-layer ladder"
    - path: ".planning/phases/19-design-system-control-styling/deferred-items.md"
      provides: "Dated, reasoned deferral entries for WR-03 radius literals and the remainder of R3-IN-05"
      contains: "WR-03"
  key_links:
    - from: "the ladder comment's stickiness claim"
      to: "19-GAP7-DIAGNOSIS.md § Fix confirmation (Round 4)"
      via: "a dated citation of Probe F rather than an unhedged assertion"
      pattern: "Probe F|GAP 7"
    - from: "the ladder comment's scope sentence"
      to: "the descendant-containment case"
      via: "an explicit statement that a focused descendant of a sticky rung is outside the ladder, marked spec-derived"
      pattern: "descendant"
  decisions: [D-09, D-10, D-12]
---

<objective>
Bring the stylesheet's own prose back into agreement with what this project has measured:
correct the ladder comment's rung-4 label and its stickiness claim (R3-WR-04), scope the
ladder to the ordering it actually governs and name the descendant case it does not
(R3-WR-03), strip the foreign line-number citations that rot on every unrelated edit
(R3-IN-05), and write down what this round is deliberately not doing.

Purpose: `styles.css` outlives the phase directory. R3-WR-04 is an instance of the phase's
own prose already being wrong — the comment asserts as settled fact that "every route
scrolls its content underneath" a nav that Probe D showed does not stay on screen. A
maintainer reading that will mis-diagnose the next stacking problem with it, which is the
same failure shape as CR-01 itself.

Output: a stylesheet whose comments state only what is true and say which of their claims
were observed and which are spec-derived, plus a reasoned deferral record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-design-system-control-styling/19-REVIEW-round3.md
@.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md
@.planning/phases/19-design-system-control-styling/19-VERIFICATION.md
@src/dashboard/styles.css
@src/dashboard/styles.test.ts
</context>

<constraints>
**Comments only.** No CSS declaration, selector or at-rule may change in this plan. The
guard is `git diff` plus the existing parse-level tests: the stylesheet must still contain
no `*/` after comment stripping, and `esbuild transformSync` must still report zero
warnings. GAP 1 — a stray `*/` inside comment prose that silently discarded a custom
property from the parsed stylesheet while every text assertion stayed green — was created
by exactly this kind of edit, so the parse-level check is not ceremony here.

**Do not introduce a new unverified rendering claim.** Every sentence this plan writes about
what the page does must either cite a dated probe or be explicitly marked as spec-derived
and unobserved. That distinction is the whole point of the exercise.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Correct the sticky-layer ladder comment — rung 4, the stickiness claim, and the scope of the ordering</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/styles.css` lines 157-205 — the full sticky-layer ladder comment and the rule it sits above, in whatever state plan 19-14 left it
    - `src/dashboard/styles.css` lines 940-965 — the `.splits-table__km` rule and its own ladder comment, whose two named futures R3-WR-03 says the descendant case does not cover
    - `src/dashboard/styles.css` lines 393-420 — the `:focus-visible` rule's own comment, which explains the promoted stacking context the ladder orders against
    - `.planning/phases/19-design-system-control-styling/19-REVIEW-round3.md` § R3-WR-03 and § R3-WR-04 — the proposed wording for both, and R3-WR-03's explicit INFERRED-ONLY marking with the two conditions it says would have to hold
    - `.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md` § Confirmed Root Cause and § Fix confirmation (Round 4) — the hypothesis id and the two Probe F outputs, which decide whether the stickiness claim can be stated or must be hedged
    - `src/dashboard/nav.ts` lines 125-160 — that `.app-nav__link` elements are DOM descendants of the nav element, the half of R3-WR-03 that was verified in source
  </read_first>
  <action>
Rewrite the ladder comment so it says three things it currently gets wrong.

**Rung 4's label.** Name whichever selector actually carries `position: sticky` and
`z-index: 20` after plan 19-14, and say in one clause why the rung moved if it did — that
the sticky rung must be an element whose containing block gives it room to travel.

**The stickiness claim (R3-WR-04).** The current sentences assert unhedged that
`.app-nav` "is `position: sticky; top: 0` … and every route scrolls its content underneath
it" and that "with no `z-index` here, a focused control scrolled under the nav painted OVER
the opaque global chrome on every route." Both are claims about rendering. Resolve them
against plan 19-14's Probe F:

- If both Probe F runs met the numeric pass condition, state the claim with its evidence
  attached and dated: that the nav remains on screen while content scrolls beneath it,
  confirmed on Activities and Records on {date} by the probe recorded in
  `19-GAP7-DIAGNOSIS.md`. Naming the evidence is not optional politeness — an unattributed
  rendering claim in this file is exactly what R3-WR-04 is.
- If either run did not, hedge instead: the nav is intended to remain on screen, as of
  {date} it does not, see `19-VALIDATION.md` GAP 7, and this z-index therefore guards a
  state that is currently unreachable.

The second sentence — that a focused control painted OVER the nav before `z-index: 20` —
must be hedged in either case until plan 19-17's row 21 observes it, because that behaviour
has never been seen on a rendered page: GAP 6 records the declaration as shipped and its
effect as unconfirmed. Say so, and say which row will settle it.

**The scope of the ordering (R3-WR-03).** `z-index` totally orders elements only within a
shared stacking context, and three of the four rungs establish their own. Scope the written
claim to what it governs: sibling and cousin ordering, within the root stacking context,
between these four specific rules. Then add one explicit sentence stating that a
`:focus-visible` ring on a DESCENDANT of a sticky rung is contained by that rung's own
stacking context and is deliberately outside the ladder — and mark that sentence as
spec-derived and not observed on a rendered page, citing R3-WR-03's own INFERRED-ONLY
marking. Do not enshrine it as measured. The DOM-descendant half is verified in source
(`nav.ts` builds `.app-nav__link` inside the nav element); the containment half is a
consequence of the positioned-layout spec that this project has not measured.

Correspondingly narrow the `.splits-table__km` comment: the `1`-to-`2` bump covers the
sibling-cell case only and does nothing for a focusable element added inside
`.splits-table__km` itself, which is one of the two futures that comment currently claims
it protects against.

Keep the rationale that is still true and still useful — why the gaps are 8-10x, why
`.records-jump` sits at 10 and computes its `top` from the live nav height, why
`:focus-visible` was promoted at all. Do not shorten the comment for its own sake; correct
it.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && rm -f /tmp/bw16.log && npm run build-widgets > /tmp/bw16.log 2>&1; BW=$?; cat /tmp/bw16.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw16.log && node -e "const fs=require('fs');const css=fs.readFileSync('src/dashboard/styles.css','utf8');const i=css.indexOf('Sticky-layer ladder');if(i<0)throw new Error('ladder comment not found');const c=css.slice(i,css.indexOf('*/',i));if(/and every route scrolls its content underneath it/.test(c))throw new Error('the disproven unhedged stickiness claim is still present');if(!/descendant/i.test(c))throw new Error('the descendant-containment case is not stated');if(!/(spec-derived|not observed|INFERRED)/i.test(c))throw new Error('the containment claim is not marked as unobserved');if(!/(Probe F|GAP 7)/.test(c))throw new Error('the stickiness claim carries no evidence citation');const stripped=css.replace(/\/\*[\s\S]*?\*\//g,'');if(stripped.includes('*/'))throw new Error('a stray */ was introduced - this is the GAP 1 mechanism');console.log('ladder comment corrected, scoped, and evidence-attributed')" && git diff --unified=0 src/dashboard/styles.css | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -vE '^[+-]\s*(\*|/\*|.*\*/)' | grep -E '^[+-]\s*[a-zA-Z#.:@-]+\s*[:{]' | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - The exact string `and every route scrolls its content underneath it` no longer appears in the stylesheet.
    - The stickiness claim cites either Probe F with a date or GAP 7 with a date, matching plan 19-14's actual outcome; the summary states which branch was taken and quotes the Probe F values that decided it.
    - The paint-order sentence (a focused control painting over the nav before `z-index: 20`) is hedged and names the row of `19-VALIDATION.md` that will settle it.
    - The ladder's scope is stated as sibling and cousin ordering within the root stacking context between the four named rules, and one sentence states the descendant case is outside it, explicitly marked spec-derived and not observed.
    - Rung 4 names the selector that actually carries `position: sticky` and `z-index: 20` after plan 19-14.
    - The `.splits-table__km` comment is narrowed to the sibling-cell case.
    - **No declaration changed:** `git diff --unified=0 src/dashboard/styles.css` contains no added or removed line that parses as a selector or declaration — asserted by the gate's filter, which counts zero.
    - `cssNoComments` still contains no `*/` and `esbuild transformSync` still reports zero warnings — both asserted by the existing parse-level tests, which must stay green.
    - `npm run build-widgets` exits 0 with zero `css-syntax-error` occurrences, established with the redirect-and-capture form.
  </acceptance_criteria>
  <done>The ladder comment describes the ordering it governs, attributes its rendering claims to dated evidence or marks them unobserved, and no longer states as fact a behaviour this project measured to be false.</done>
</task>

<task type="auto">
  <name>Task 2: Strip the rotting foreign line-number citations from the stylesheet</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/styles.css` lines 880-915 — the `.segmented` / `.segmented__option` comment block citing `trends.ts` line numbers for the three affected groups
    - `src/dashboard/styles.css` lines 1285-1320 — the `:disabled` comment block citing `calendar.ts` and `trends.ts` line numbers
    - `src/dashboard/styles.css` lines 1365-1420 — the CR-03 comment blocks citing `list.ts` and `calendar.ts` line numbers
    - `.planning/phases/19-design-system-control-styling/19-REVIEW-round3.md` § R3-IN-05 — what to keep (the rationale) and what rots (the cross-file line citations)
  </read_first>
  <action>
Remove every `{file}.ts:{number}` style citation from `src/dashboard/styles.css`, keeping the
file name and the descriptive identifier that makes it findable. A citation like
"the 5-tab Trends view group (`trends.ts:1110`, 3 middle options)" becomes "the 5-tab Trends
view group (`trends.ts`, 3 middle options)". Where a line number is the only thing
identifying the referenced construct, replace it with the construct's name — the function,
the class, the element being built — because a name survives an edit and a line number does
not.

Keep every rationale sentence. This task removes coordinates, not reasoning. Do not shorten
a comment because it is long; R3-IN-05's length observation is handled by the deferral in
Task 3, not by deleting explanation.

Do not write any new `.ts:` followed by digits anywhere in the file, including inside prose
describing this change — the gate counts the pattern file-wide and self-referential prose
would invalidate it.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && rm -f /tmp/bw16b.log && npm run build-widgets > /tmp/bw16b.log 2>&1; BW=$?; cat /tmp/bw16b.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw16b.log && node -e "const css=require('fs').readFileSync('src/dashboard/styles.css','utf8');const hits=css.match(/[a-zA-Z-]+\.ts:\d+/g)||[];if(hits.length)throw new Error('foreign line citations remain: '+hits.join(' '));const names=css.match(/[a-zA-Z-]+\.ts/g)||[];if(names.length<5)throw new Error('file-name references were deleted along with the line numbers, found only '+names.length);const stripped=css.replace(/\/\*[\s\S]*?\*\//g,'');if(stripped.includes('*/'))throw new Error('a stray */ was introduced - this is the GAP 1 mechanism');console.log('line citations removed, '+names.length+' file-name references kept')" && git diff --unified=0 src/dashboard/styles.css | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -E '^[+-]\s*[a-zA-Z#.:@-]+\s*[:{]' | grep -vE '^\s*[+-]\s*\*' | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -oE '[a-zA-Z-]+\.ts:[0-9]+' src/dashboard/styles.css` returns nothing.
    - At least five `{file}.ts` file-name references survive — the citations were de-coordinated, not deleted.
    - Every rationale sentence present before this task is still present; the summary states the comment line count before and after and confirms the delta is accounted for by removed coordinates alone.
    - **No declaration changed:** the gate's selector-and-declaration filter over `git diff` counts zero added or removed CSS lines.
    - `cssNoComments` still contains no `*/`; the esbuild parse-level test is green; `npm run build-widgets` exits 0 with zero `css-syntax-error`.
    - `npx vitest run src/dashboard/styles.test.ts` green.
  </acceptance_criteria>
  <done>No comment in the stylesheet points at a line number in a file it cannot see change, and every reason those comments exist is still written down.</done>
</task>

<task type="auto">
  <name>Task 3: Record what this round is deliberately not doing, with reasons</name>
  <files>.planning/phases/19-design-system-control-styling/deferred-items.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/deferred-items.md` — the existing 19-10 entry's shape: found during, observation, root cause, scope decision
    - `.planning/phases/19-design-system-control-styling/19-VERIFICATION.md` § Gaps Summary WR-03 — the ten selectors hardcoding literal `4px`/`8px`/`6px` and the verifier's own finding that the values currently match the tokens
    - `.planning/phases/19-design-system-control-styling/19-REVIEW-round3.md` § R3-IN-05 — the roughly 110 lines of review-round narrative and the proposal to move it to the phase directory
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` row 16 — the "some more separation wouldn't hurt" UX remark logged as a backlog note during Round 3
  </read_first>
  <action>
Add three dated entries to `deferred-items.md`, each in the existing entry shape (found
during, observation, reasoning, scope decision). State a judgement in every one — a
deferral without a stated reason is a silent drop, which this phase's rules forbid.

**WR-03 — ten selectors hardcode literal `4px`/`8px`/`6px` instead of the radius tokens.**
Deferred out of Round 4, with the reasoning stated: it is not a rendering defect today
because the literal values equal the token values, as `19-VERIFICATION.md` independently
confirmed; Round 4 is scoped to GAP 7 and the guard layer; and changing ten selectors'
radii immediately before the phase-gate checkpoint would add ten rendered changes that each
need their own checkpoint row to be honestly discharged, against a gate that is open
precisely because rendered changes were discharged in bulk. Name the trigger that should
pull it back in: the next phase that retunes `--radius-control` or `--radius-panel`, at
which point the literals silently split the control set.

**R3-IN-05 remainder — the review-round narrative in a production asset.** Round 4 removed
the rotting cross-file line citations and corrected the false claims; the wholesale
relocation of the narrative prose to the phase directory is deferred as maintainability
work, because moving roughly 110 lines of comment out of the stylesheet is a large,
low-risk-but-high-churn edit landing in the same wave as the gate checkpoint.

**Row 16 spacing remark.** The developer's Round 3 observation that the training-load window
group and the TRIMP model group sit close enough to read as one control at first glance
("some more separation wouldn't hurt") was explicitly not counted against that row's verdict
and is not a Phase 19 requirement. Carry it forward as UX backlog so it is not lost when the
phase directory stops being read.

If plan 19-13's diagnosis or plan 19-14's fix produced any STOP outcome, or if any finding
from `19-REVIEW-round3.md` was consciously left unaddressed by plans 19-14 through 19-16,
add an entry for it too, naming the finding id. Cross-check all eleven Round 3 finding ids
(R3-CR-01, R3-CR-02, R3-WR-01 through R3-WR-04, R3-IN-01 through R3-IN-05) against what
this round actually shipped and state, in the summary, the disposition of each: closed in
which plan, or deferred in which entry. A finding with neither disposition is a silent drop.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/19-design-system-control-styling/deferred-items.md','utf8');for(const k of ['WR-03','R3-IN-05'])if(!s.includes(k))throw new Error('missing deferral entry: '+k);if(!/separation/i.test(s))throw new Error('the row 16 spacing remark was not carried forward');if(!/radius-control|radius-panel/.test(s))throw new Error('the WR-03 entry does not name the trigger tokens');const entries=(s.match(/^##\s/gm)||[]).length;if(entries<4)throw new Error('expected at least 4 entries (1 pre-existing + 3 new), found '+entries);console.log(entries+' deferral entries recorded')" && npx vitest run src/dashboard/styles.test.ts && git status --porcelain src/dashboard/styles.test.ts | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - `deferred-items.md` carries at least four entries: the pre-existing 19-10 worktree entry plus WR-03, the R3-IN-05 remainder, and the row 16 spacing remark.
    - The WR-03 entry states the reasoning (values currently match, round scope, checkpoint-surface argument) and names `--radius-control` and `--radius-panel` as the trigger for pulling it back in.
    - **Disposition audit, in the summary:** all eleven Round 3 finding ids are listed with a disposition — closed in plan 19-14, 19-15 or 19-16, or deferred in a named entry. No id appears with neither.
    - `npx vitest run src/dashboard/styles.test.ts` green and `git status --porcelain src/dashboard/styles.test.ts` empty — this task changes no test file.
  </acceptance_criteria>
  <done>Every Round 3 finding has a stated disposition, and every item this round chose not to do is written down with the reason and the condition that should bring it back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a source comment → a maintainer's mental model | The comment outlives the phase directory. R3-WR-04 is an instance of it already carrying a disproven runtime claim; the next maintainer would mis-diagnose a stacking problem with it. |
| a measured claim → a spec-derived claim | Both appear in the same comment. Mixing them is how CR-01 was diagnosed and shipped without ever being observed. Every rendering sentence must be marked as one or the other. |
| comment prose → the CSS parser | GAP 1: a stray `*/` inside comment prose silently discarded `--radius-control` from the parsed stylesheet while every text assertion stayed green. Any comment edit re-enters this boundary. |
| a line-number citation → the file it cites | The citation rots on the next unrelated edit to that file, with nothing to detect it. |

**Application-security surface: not applicable, stated rather than padded.** This plan edits
CSS comments and one planning document. No declaration, selector or at-rule changes; no new
data flow, authentication, session, access-control or cryptography surface; no user input
handling; no network egress; no new dependency. The assessment would change only if a
comment edit altered a declaration — which the `git diff` filter in both code tasks exists
to prevent.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-C-01 | Repudiation (a disproven rendering claim shipped as settled fact) | the ladder comment | mitigate | The gate rejects the exact disproven sentence by string and requires either a Probe F citation or a GAP 7 hedge, plus an explicit unobserved marking on the containment claim. |
| T-19-C-02 | Tampering (a comment edit silently changing a declaration) | `src/dashboard/styles.css` | mitigate | Both code tasks run a `git diff` filter that counts added or removed lines parsing as a selector or declaration and requires zero; the esbuild parse-level test and the `*/` test remain green. |
| T-19-C-03 | Tampering (re-creating the GAP 1 mechanism with a stray comment terminator) | comment prose | mitigate | Every task's gate re-runs the comment-stripping check for `*/` and `npm run build-widgets` for `css-syntax-error`, the build-time signature GAP 3 misclassified. |
| T-19-C-04 | Repudiation (a review finding silently dropped) | `deferred-items.md` | mitigate | Task 3 requires a stated disposition for all eleven Round 3 finding ids and a written reason plus re-entry trigger for each deferral. |
| T-19-C-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized. No formatter, linter or comment tooling may be added to perform these edits. |
</threat_model>

<verification>
- `npx vitest run src/dashboard/styles.test.ts` green after every task, including the `*/` and esbuild parse-level checks.
- `npm run build-widgets` exit 0 with zero `css-syntax-error` occurrences.
- `git diff src/dashboard/styles.css` contains no added or removed selector or declaration line.
- `grep -oE '[a-zA-Z-]+\.ts:[0-9]+' src/dashboard/styles.css` returns nothing, with at least five file-name references surviving.
- `deferred-items.md` carries a reasoned entry per deferred item and the summary carries a disposition for all eleven Round 3 finding ids.
</verification>

<success_criteria>
The stylesheet's prose states only what is true, distinguishes what was measured from what
is spec-derived, points at names rather than line numbers, and nothing this round chose not
to do has been dropped without a written reason.
</success_criteria>

<output>
Create `.planning/phases/19-design-system-control-styling/19-16-SUMMARY.md` when done.
Record: which branch the stickiness claim took and the Probe F values that decided it; the
before and after wording of the three corrected claims; the comment line count before and
after with the delta accounted for; the disposition of all eleven Round 3 finding ids; and
confirmation that no CSS declaration changed.
</output>
