---
phase: 19-design-system-control-styling
plan: 17
type: execute
wave: 17
depends_on: [19-16]
files_modified:
  - .planning/phases/19-design-system-control-styling/19-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [UI-01, UI-02, UI-03, ACT-01]
user_setup: []

must_haves:
  truths:
    - "A developer confirms in a real browser, on a production-shaped URL, that the top nav stays on screen while two different routes scroll"
    - "The CR-01 paint-order question — does a focused control scrolled under the nav paint over it — is finally observed, now that its premise is reachable"
    - "The Records jump bar still paints above a focused control elsewhere and is still parked correctly beneath the nav"
    - "A focused nav link's own ring is observed rather than reasoned about"
    - "UI-01, UI-03 and ACT-01 are re-confirmed unregressed by their own verdicted row"
    - "Every row carries its own PASS/FAIL verdict token and its own controls named; rows 20-23 additionally record both themes"
  artifacts:
    - path: ".planning/phases/19-design-system-control-styling/19-VALIDATION.md"
      provides: "Rows 20-24 with independent verdicts, the Round 4 probe outputs verbatim, and resolved GAP 6 and GAP 7 entries"
      contains: "Round 4"
    - path: ".planning/REQUIREMENTS.md"
      provides: "UI-02's status agreeing with the five Round 4 verdicts, and dated confirmed-unregressed notes on UI-01, UI-03 and ACT-01"
      contains: "UI-02"
  key_links:
    - from: "rows 20-24 verdicts"
      to: "REQUIREMENTS.md UI-02 checkbox and phase-status table row"
      via: "a clean sweep of the five Round 4 rows being the only condition that ticks it"
      pattern: "UI-02"
    - from: "Probe G output"
      to: "row 20 and row 21's premise"
      via: "a probe run and recorded verbatim before either row is judged on appearances"
      pattern: "Probe G"
    - from: "GAP 6 and GAP 7"
      to: "their dated Round 4 resolutions"
      via: "a verbatim quote of the observation that closes each, or a new unpatched entry"
      pattern: "GAP 7"
  decisions: [D-01, D-03, D-05, D-09, D-10, D-12, D-13]
---

<objective>
Close the Phase 19 gate — or record precisely why it stays open — by putting five
independently-verdicted claims in front of a developer in a real browser, on a
production-shaped URL, in both themes for the four rendering rows, with the questionable premises probed before they
are judged.

Purpose: UI-02 is the last open requirement. Row 18 failed in Round 3 because its premise
was never probed until mid-session; GAP 6's paint-order question has never been observed at
all because that premise failed first. Both become answerable only if plan 19-14's fix
holds on a rendered page.

Output: rows 20-24 of `19-VALIDATION.md`, each with its own verdict and its own controls
named, rows 20-23 additionally recording both themes; two probe outputs verbatim; resolved
or re-entered GAP 6 and GAP 7; and requirement statuses that agree with the verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-design-system-control-styling/19-VALIDATION.md
@.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md
@.planning/phases/19-design-system-control-styling/19-12-PLAN.md
@.planning/phases/19-design-system-control-styling/19-12-SUMMARY.md
</context>

<gate_mechanics>
Read these before writing a single word into `19-VALIDATION.md`. Plan 19-12's summary
records a deviation in which the plan's own descriptive prose broke the plan's own gate.

1. **The verdict token is counted file-wide.** The gate regex matches the literal string
   `VERDICT` followed by a colon and PASS or FAIL, anywhere in the file. Six such tokens
   already exist, from Round 3 rows 14-19, and they stay. Round 4 adds exactly five. The
   count is 6 before Task 2 and 11 after. **Never write that literal token in prose** —
   headers, instructions, gap entries, lesson notes, approval addenda. Write "PASS/FAIL
   verdict token" instead. Round 3 caught four self-inflicted instances of this, one of them
   in text written before the gate had ever been run.
2. **A literal `|` inside a table cell breaks row parsing.** Observation cells are extracted
   by splitting the row on `|`. Use a semicolon or the word "and". Round 3 substituted
   semicolons for pipes when quoting a probe output and said so in the cell.
3. **Rows 1-19 are history and stay byte-identical.** Row 18's Round 3 FAIL is not rewritten,
   re-judged or softened. Rows 20 and 21 supersede it, in the same way the file already
   supersedes its own "Next step" and Approval sections rather than replacing them.
4. **The clean-sweep test is row-scoped, not file-scoped.** Row 18 carries a permanent FAIL,
   so a file-wide count of failures will never be zero. Every gate below evaluates the five
   Round 4 rows only.
</gate_mechanics>

<tasks>

<task type="auto">
  <name>Task 1: Run the gate, stage the build, and write the five Round 4 agenda rows</name>
  <files>.planning/phases/19-design-system-control-styling/19-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` § Manual-Only Verifications rows 14-19 — the five-column shape, the per-row anchor style, and the note above the table stating that a single verdict covering several rows is not sufficient
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` § Round 3 Probe Outputs — the recording format the Round 4 probes must match
    - `.planning/phases/19-design-system-control-styling/19-12-PLAN.md` Task 1 — the staging sequence with the absolute source path, the `MOUNT_PREFIX` reason, and the gate-command hygiene warning about `tee` pipelines
    - `.planning/phases/19-design-system-control-styling/19-12-SUMMARY.md` § Deviations — the self-inflicted verdict-token count failure, in full
    - `.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md` — the confirmed root cause, the fix applied, and both Probe F outputs, so the agenda describes the shipped state rather than the planned one
    - `.planning/phases/19-design-system-control-styling/19-14-SUMMARY.md`, `19-15-SUMMARY.md`, `19-16-SUMMARY.md` — what actually shipped in this round
    - `src/dashboard/views/records.ts` lines 168-182 — `updateJumpOffset`, the live-height coupling row 22 checks
  </read_first>
  <action>
Run the full gate from a clean working tree and record every exit code and count:
`npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets`,
`npm run verify-dashboard`. All four must exit 0 before the checkpoint may open, and
`build-widgets` must emit zero `css-syntax-error` occurrences. Use the redirect-and-capture
form: do not rewrite this into `cmd | tee log; ! grep -q needle log`, where the `;` makes
the grep the sole determinant of exit status and `tee` masks the build's own status.
`PIPESTATUS` is not the fix either — the developer's shell is zsh, where it is `pipestatus`
and 1-indexed. If `verify-dashboard` fails on stale data artifacts rather than a stylesheet
problem, run `npm run build` and `node dist/index.js compute-all-stats` first and record
that you did. If `npm test` shows the five known live-data file-level failures logged in
`deferred-items.md`, record the counts and state that
`npx vitest run src/dashboard/styles.test.ts` is this phase's actual surface.

Stage the built output under the production-shaped path: capture the repository root into a
variable while still inside the repository, create `/tmp/gh-pages`, create the symlink
`/tmp/gh-pages/strava-widgets` pointing at the absolute `dist/widgets` path under that
captured root, confirm `/tmp/gh-pages/strava-widgets/index.html` resolves, then change
directory to `/tmp/gh-pages` and serve port 8099 with `python3 -m http.server`. A bare
server root is not acceptable.

**Write five new agenda rows** into § Manual-Only Verifications, appended after row 19, in
the existing five-column shape (Behavior, Requirement, Why Manual, Test Instructions,
Observation). Leave rows 1-19 byte-identical. Each new Observation cell contains exactly the
sentinel for Round 4 and nothing else; Task 2 fills them. Add a short note above the new
rows stating that rows 20-24 are Round 4, that rows 20 and 21 supersede row 18 rather than
replacing it, and that each row needs its own independent verdict — without using the
literal verdict token anywhere in that note.

Write the rows so a reader who has never seen this phase can execute them:

- **Row 20 — the top nav stays on screen while the page scrolls, on two routes.** UI-02.
  Why manual: whether a sticky element holds is a rendered-layout fact; `styles.test.ts`
  runs in vitest's `node` environment with no DOM and no CSSOM, so no assertion in this
  repository can observe it. Instructions: on `#/list` (Activities), scroll to the top, then
  scroll down a full screen or more — the top nav must remain visible, pinned against the
  top of the window, while the content moves behind it. Then the same on `#/records`.
  Both themes, both routes. This is the row Round 3's Probe D disproved; the developer's own
  words then were that the navbar disappears upwards.
- **Row 21 — a focused control scrolled under the nav stays underneath it.** UI-02. Why
  manual: paint order between a sticky header and a promoted focused element; no automated
  check in this repository can observe it, and this is the original CR-01 question that has
  never been observable. Instructions: on `#/list`, Tab to a control partway down the page —
  a column-header sort button, a filter control, a pagination button — then scroll so that
  focused control travels up and passes beneath the nav. The nav must stay opaque and on
  top; no part of the focused control or its orange ring may appear over the header. Repeat
  on `#/records`. Both themes, both routes.
- **Row 22 — the Records jump bar still paints above a focused control elsewhere, and is
  still parked correctly beneath the nav.** UI-02. Why manual: the jump bar's `top` is
  computed at runtime from the live rendered nav height, so a change to the nav's sticky rung
  can mis-park it, and its paint ordering against a focused control is not observable
  automatically. Instructions: on `#/records`, confirm the jump bar sits flush beneath the
  nav with no gap and no overlap while scrolling; click a jump link, then Tab to a control
  in the section below it and confirm the jump bar still paints above that focused control.
  Both themes.
- **Row 23 — a focused nav link's own ring is fully visible.** UI-02. Why manual: the ring on
  a descendant of the sticky rung is contained by that rung's stacking context, which this
  project has not measured; the code review marked the claim inferred-only. Instructions:
  from the top of any route, Tab until a nav link is focused. Its orange ring must be fully
  visible on all four sides, not clipped by the nav's bottom border and not painted over by
  the nav's own background. Both themes.
- **Row 24 — the no-regression re-confirmation.** UI-01, UI-02, UI-03, ACT-01. Why manual:
  this round moved a sticky rung, rewrote guard assertions and edited comments across the
  stylesheet, so the things that must look unchanged need their own verdict rather than an
  assumption. Five sub-checks: (a) the detail view's two-option x-axis toggle, Distance and
  Time, still shows rounded outer corners and a square joint; (b) the input, select and
  textarea baselines across Activities and Calendar still render their intended border,
  padding, background and 4px corners, none reading as a raw browser default; (c) the card
  and panel rhythm on Overview and Records reads as it did; (d) an Activities row click still
  navigates to the detail view; (e) the nav itself looks unchanged — same height, same
  padding, same border beneath it, links in the same place. **Not theme-gated**, and say so
  in the row: this is the no-regression row and every sub-check is a geometry, structure or
  navigation claim rather than a colour one, matching the Round 3 precedent where row 19 was
  deliberately excluded from the both-theme requirement while rows 14-18 carried it. The
  automated gate's anchor set for this row therefore carries no light or dark token; that
  omission is the decision, not an oversight.

**Probe letters.** Round 3 used A through D; plan 19-13 used E1 and E2; plan 19-14 used F.
This round's checkpoint uses **G** (plan 19-14's Probe F snippet, re-run in this session on
both routes) and **H** (the stacking declaration read, defined in Task 2). State that
allocation in the note above the new rows so the operator does not go hunting for a snippet
labelled G inside `19-GAP7-DIAGNOSIS.md`, where it is labelled F.

Do not resolve the Approval line and do not touch `.planning/REQUIREMENTS.md` in this task.
Do not add any verdict token to the file: the count must still be exactly 6 when this task
ends.

Record the verbatim staging command sequence in the summary.
  </action>
  <verify>
    <automated>rm -f /tmp/bw17.log && npm run build-widgets > /tmp/bw17.log 2>&1; BW=$?; cat /tmp/bw17.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw17.log && npx tsc --noEmit -p tsconfig.json && npm run verify-dashboard && test -f /tmp/gh-pages/strava-widgets/index.html && readlink /tmp/gh-pages/strava-widgets | grep -q '^/' && node -e "const s=require('fs').readFileSync('.planning/phases/19-design-system-control-styling/19-VALIDATION.md','utf8');const t=s.split('## Manual-Only Verifications')[1].split(String.fromCharCode(35,35,35))[0];const rows=t.split(String.fromCharCode(10)).filter(l=>l.startsWith('|')&&!/^\|\s*-+/.test(l)&&!/^\|\s*Behavior/.test(l));if(rows.length!==24)throw new Error('expected 24 rows, found '+rows.length);const p=(s.match(/Round 4[^|]*_pending_/g)||[]).length;if(p!==5)throw new Error('expected 5 Round 4 pending sentinels, found '+p);const v=(s.match(/VERDICT:\s*(PASS|FAIL)/g)||[]).length;if(v!==6)throw new Error('verdict token count must still be 6 before the checkpoint, found '+v);if(!/nyquist_compliant: false/.test(s.split(String.fromCharCode(45,45,45))[1]||''))throw new Error('frontmatter verdict is not false');rows.slice(19).forEach((r,i)=>{const cells=r.split('|');if(cells.length<6)throw new Error('row '+(20+i)+' does not have five columns')});console.log('24 rows, 5 pending Round 4 sentinels, verdict count still 6')"</automated>
  </verify>
  <acceptance_criteria>
    - All four gate commands exit 0, with each exit code, the vitest passing count and the `verify-dashboard` assertion count recorded in the summary; any known live-data file-level failures are named and separated from this phase's surface.
    - `npm run build-widgets` emits zero `css-syntax-error` occurrences, established with the redirect-and-capture form whose exit status reflects the build itself.
    - `test -f /tmp/gh-pages/strava-widgets/index.html` succeeds and `readlink /tmp/gh-pages/strava-widgets` prints an absolute path beginning with the repository root.
    - The Manual-Only Verifications table has exactly 24 data rows, exactly 5 Round 4 pending sentinels, and the file-wide verdict-token count is still exactly 6.
    - Rows 1-19 are byte-identical: `git diff` on the validation file shows no change inside any of the first 19 data rows, and row 18's Round 3 FAIL is untouched.
    - Each of the five new rows names its own controls in its Test Instructions — row 20 names both routes and the scroll action; row 21 names a specific focusable control and both routes; row 22 names the jump bar, the flush-beneath-nav check and the paint-order check; row 23 names the nav link and the four sides of the ring; row 24 names all five sub-checks.
    - No literal `|` appears inside any cell of the five new rows.
    - `git status --porcelain src scripts .planning/REQUIREMENTS.md` is empty — this task changes no source file and no requirement status.
    - The verbatim staging command sequence, using an absolute source path, is in the summary.
  </acceptance_criteria>
  <done>The gate is green, a build carrying this round's fix is served under `http://localhost:8099/strava-widgets/`, and the validation record carries five staged Round 4 rows whose instructions name the exact controls to look at.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify rows 20-24 in a real browser, one named verdict per row, both themes on rows 20-23</name>
  <files>.planning/phases/19-design-system-control-styling/19-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` § Manual-Only Verifications rows 20-24 as staged by Task 1, and § Gap-Closure Record GAP 6 and GAP 7
    - `.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md` — the confirmed root cause, both Probe F outputs, and the Probe F snippet itself, which this session re-runs and records as Probe G
    - `.planning/phases/19-design-system-control-styling/19-12-SUMMARY.md` § Round 3 checkpoint outcome — the verbatim recording standard, including how a terse both-theme answer was recorded as given rather than expanded
    - `.planning/STATE.md` § Accumulated Context — the 16-09 / 17-15 / 19-05 / 19-09 / 19-12 wording precedent this result must match
  </read_first>
  <action>
Write or change no file under `src/` or `scripts/` in this task. Run the two probes, walk
the five rows, and record a verbatim observation plus an explicit PASS or FAIL verdict token
in each of the five Observation cells. Set nothing else.

**Run both probes first, and record them verbatim before judging anything.** Rows 20 and 21
depend on premises that failed in Round 3. Row 18 was judged FAIL last round only because a
probe was added mid-session to test its own premise; do not repeat the sequence where the
premise is discovered late. If a probe returns something other than its expected value,
stop — do not judge the rows that depend on it, record the output verbatim, and mark the
checkpoint PARTIAL.

**One verdict per row.** A single verdict covering several rows will not pass this task's
gate: each Observation cell is checked independently for its own verdict token, for content
naming that row's own controls, for a minimum length, and for not being a duplicate of
another cell. This is not bureaucracy — `19-VERIFICATION.md` reopened UI-02 precisely
because a blanket approval could not be traced to any single observation.

**Both themes, rows 20 through 23 only.** Those four are rendering-order and ring-visibility
claims, where a theme flip can change the answer. Row 24 is the no-regression row and is not
theme-gated — the same scoping 19-12 applied when it required both themes on rows 14-18 and
excluded row 19. Its gate anchor set carries no light or dark token, so widening the prose
here without widening `anchors[4]` would recreate exactly the prose-versus-gate mismatch this
round exists to close. If the developer gives
one terse answer covering several rows in response to an explicit theme prompt, record that
answer as given in each row and say that it was given once to a prompt spanning those rows.
Do not expand it into invented per-theme detail — Round 3 set this precedent deliberately.

**Checkpoint result protocol — house rule since 16-09.** If any row fails, record it verbatim
in the developer's own words, mark that row FAIL, mark the checkpoint PARTIAL, and leave
`nyquist_compliant: false`. Fix nothing in this task. Record no suggested fix and no
root-cause theory for a failing row — the next planning round diagnoses it, as this round
diagnosed GAP 7. Do not paraphrase a failure into something smaller than it is.

**Do not write the literal verdict token anywhere except inside the five Observation cells,
and do not put a `|` character inside any cell.** Both broke Round 3's gate from the plan's
own prose.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/19-design-system-control-styling/19-VALIDATION.md','utf8');if(/Round 4[^|]*_pending_/.test(s))throw new Error('a Round 4 sentinel is still pending');const t=s.split('## Manual-Only Verifications')[1].split(String.fromCharCode(35,35,35))[0];const rows=t.split(String.fromCharCode(10)).filter(l=>l.startsWith('|')&&!/^\|\s*-+/.test(l)&&!/^\|\s*Behavior/.test(l));if(rows.length!==24)throw new Error('expected 24 rows, found '+rows.length);const obs=rows.slice(19).map(r=>r.split('|').slice(-2)[0]);if(obs.length!==5)throw new Error('expected 5 Round 4 observation cells');const anchors=[[['Activities','list'],['Records','records'],['scroll'],['light'],['dark']],[['ring','focus'],['nav','header'],['Activities','list'],['Records','records'],['light'],['dark']],[['jump'],['above','over','paint'],['light'],['dark']],[['nav','link'],['ring','focus'],['light'],['dark']],[['x-axis','toggle'],['input'],['panel','card','rhythm'],['navigat','click'],['nav','header']]];obs.forEach((o,i)=>{if(!/VERDICT:\s*(PASS|FAIL)/.test(o))throw new Error('row '+(20+i)+' has no verdict token');for(const alts of anchors[i]){if(!alts.some(x=>o.toLowerCase().includes(x.toLowerCase())))throw new Error('row '+(20+i)+' observation does not name any of: '+alts.join(' / '))}if(o.replace(/[^a-zA-Z]/g,'').length<80)throw new Error('row '+(20+i)+' observation is too short to be a real report')});if(new Set(obs.map(o=>o.trim())).size!==5)throw new Error('two Round 4 observations are identical - a blanket verdict was pasted');const v=(s.match(/VERDICT:\s*(PASS|FAIL)/g)||[]).length;if(v!==11)throw new Error('expected 11 verdict tokens file-wide (6 Round 3 + 5 Round 4), found '+v);if(!/Probe G/.test(s)||!/Probe H/.test(s))throw new Error('a Round 4 probe output is not recorded');const a=(s.split('Approval:').pop()||'').split(String.fromCharCode(10))[0];console.log('five independent Round 4 verdicts recorded, all anchors present')" && git status --porcelain src scripts | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - Both probe outputs are recorded verbatim in a new `### Round 4 Probe Outputs` subsection and referenced from the rows that depend on them, alongside their expected values. If either returned an unexpected value, the checkpoint is PARTIAL and the dependent rows were not judged.
    - **Rendered-evidence criterion:** row 20's verdict rests on Probe G's numbers and on what the developer saw while scrolling — not on `styles.test.ts` being green, which cannot observe stickiness and must not be cited as if it could.
    - Each of rows 20-24 carries its own PASS or FAIL verdict token. Five rows, five tokens; eleven file-wide.
    - No two Round 4 observation cells are identical, and each names its own row's controls and one token per sub-check the row asks for — the gate asserts one anchor per sub-check with alternatives accepted, so reasonable phrasing is not punished, but a row 24 observation covering only the toggle and the inputs fails, because it silently drops sub-checks (c), (d) and (e).
    - Rows 20-23 each record both themes explicitly. A row observed in one theme only is FAIL for insufficient evidence, not a pass. Row 24 is deliberately excluded — it is the no-regression row, its sub-checks are geometry, structure and navigation claims, and `anchors[4]` in this task's gate carries no light or dark token to match. This mirrors 19-12, which required both themes on rows 14-18 and excluded row 19.
    - Row 21's observation states separately what happened on each of the two routes.
    - Row 22's observation states separately whether the jump bar was parked correctly beneath the nav and whether it still painted above a focused control.
    - Row 24's observation states a result for each of its five sub-checks — this is the row carrying UI-01, UI-03 and ACT-01's confirmed-unregressed coverage.
    - Rows 1-19 are unchanged: `git diff` touches only rows 20-24, the new probe subsection, the Approval line and the frontmatter.
    - The Approval line records `approved {date}` or `PARTIAL {date}` for Round 4, appended to the existing prose rather than replacing it.
    - No literal `|` inside any Observation cell; the verdict token appears nowhere outside the five cells.
    - `git status --porcelain src scripts` is empty and the session ran against a URL beginning `http://localhost:8099/strava-widgets/`.
  </acceptance_criteria>
  <what-built>
Since the last session, one thing was diagnosed and fixed, and a layer of test guards was
repaired.

- **The nav that disappeared upwards.** You reported in Round 3: "If i scroll down the navbar
  disappears upwards." Nothing was patched on the spot. Instead the cause was measured — a
  probe walked the page structure and watched what actually moved during a scroll — and the
  fix that measurement pointed at was applied to `src/dashboard/styles.css`. One stylesheet
  changed. No TypeScript, no markup.
- **A question that has never been answerable.** Three rounds ago a fix was written for a
  paint-order problem: a focused control, with its orange ring, scrolling under the top nav
  and appearing *over* it. The fix shipped and its declaration is confirmed present in the
  bundle. Whether it works has never once been seen, because the nav never stayed on screen
  long enough for anything to scroll under it. If row 20 passes, row 21 finally asks that
  question.
- **Guards that could not fail.** A code review proved by mutation that three of the test
  assertions this phase added were green even with the defect they name present — a hover
  rule guard survived deleting the hover, and the stacking-order guard survived deleting the
  positioning that makes the order mean anything. Those are fixed and each was watched
  failing before it passed. None of that is visible on screen; it is why the suite being
  green is not offered as evidence for anything below.

Five rows. Each needs its own answer.
  </what-built>
  <how-to-verify>
**Staging.** Task 1 ran the four gate commands, rebuilt and created the symlink. Confirm the
URL bar contains `/strava-widgets/`. If it does not, stop and say so — a root-served build is
how Phase 16 shipped a black page behind a 15/15 green gate.

Open with devtools Console visible for the whole session:

- `http://localhost:8099/strava-widgets/#/` (Overview)
- `http://localhost:8099/strava-widgets/#/list` (Activities)
- `http://localhost:8099/strava-widgets/#/calendar` (Calendar)
- `http://localhost:8099/strava-widgets/#/records` (Records)
- `http://localhost:8099/strava-widgets/#/trends` (Trends)
- a detail view reached by clicking any Activities row

---

**Probe G — does the nav actually hold? Run on `#/list` and again on `#/records`, before
judging rows 20, 21 or 22.**

Probe G is the same single-paste snippet plan 19-14 recorded as **Probe F** in
`19-GAP7-DIAGNOSIS.md` § Fix confirmation (Round 4) — the letter advances because this is a
second, independent run in a different session, recorded in a different file. Do not go
looking for a snippet labelled G in that section; there is none, and there should not be.

Scroll to the very top, paste that snippet, press Enter. The page scrolls itself down by 600 pixels and prints a result
about half a second later.

Expected: `sy` around 600, and the nav's `top` value at 0 or within a pixel of it — the nav
pinned to the top of the window. If `sy` comes back under 400 the page was too short and the
run does not count; say so and we re-run. If the `top` value comes back around minus 600,
the nav travelled off the top with the page and rows 20-22 are FAIL — record it and stop.

**Probe H — is the nav's stacking declaration live in this bundle? Run once, before judging
row 21.**

Read the computed `position` and `zIndex` of the element that carries the sticky rung, and
whether the nav element contains the nav links. Expected: `sticky`, `"20"`, and `true`. If
`zIndex` reads `auto`, the fix is not in the bundle you are looking at — stop and restage.

---

**Row 20 — does the nav stay on screen?** On `#/list`, scroll to the top, then scroll down a
full screen or more, slowly. The nav must stay put, pinned against the top of the window,
while the table scrolls behind it. Then the same on `#/records`. Switch theme and do it
again on both. Say what you saw on each route.

**Row 21 — does a focused control stay underneath the nav?** On `#/list`, click somewhere in
the page, then press Tab until a control partway down the page has an orange ring around it —
a column-header sort button, a filter control, a pagination button. Now scroll the page so
that ringed control travels upward and passes beneath the nav.

The question: as it passes under, does any part of it — the control itself or its orange
ring — show *on top of* the nav? It should not. The nav should be solid, and the control
should disappear behind it cleanly. Then the same on `#/records`. Both themes, both routes.
Say what you saw on each route.

If the control simply scrolls past without going under the nav, that means row 20 did not
really pass — say so rather than guessing.

**Row 22 — the Records jump bar.** On `#/records`, look at the jump bar (the row of section
links: superlatives, PR tables and so on). Two separate questions:
- Does it sit flush beneath the nav while you scroll — no gap opening between them, no
  overlap, not sliding underneath the nav's bottom edge?
- Click one of the jump links, then Tab to a control in the section below it. Does the jump
  bar still paint *above* that focused control when they overlap?

Both themes. Note: those jump links turning orange as you reach each section is the jump
bar's own current-section highlighting, not a focus ring — that is expected and is not what
this row is about.

**Row 23 — a focused nav link's own ring.** From the top of any route, press Tab until one of
the nav links themselves is focused. Its orange ring should be fully visible all the way
around — not cut off along the bottom by the nav's border, not faded behind the nav's own
background. Both themes.

**Row 24 — the things that must NOT have changed.** Five sub-checks, each needing its own
answer:
- **(a)** On a detail view, the two-option x-axis toggle (Distance / Time): rounded outer
  corners, square joint down the middle, exactly as before.
- **(b)** Inputs across Activities and Calendar — date inputs, number inputs, the search box:
  still bordered, padded, 4px rounded corners, none reading as a raw browser default.
- **(c)** Overview and Records: the card and panel rhythm reads as it did — no panel gained
  or lost a corner radius or padding.
- **(d)** Click an Activities row: it still navigates to the detail view.
- **(e)** The nav itself: same height, same padding, same border beneath it, links in the
  same place. The sticky behaviour moved in this round, so its appearance is worth a
  deliberate look rather than an assumption.

No theme flip is needed for row 24 — every sub-check above is about geometry, structure or
whether a click still navigates, none of which changes with the theme. Rows 20 through 23 are
the ones that need light and dark.

**Known issues that are not Phase 19's.** A `SyntaxError` in the console on page load;
Records "View Activity" links not navigating; the Calendar month picker being a plain wide
text box. All three are logged in `19-VALIDATION.md` § Non-Phase-19 Issues. Say whether each
still occurs; none counts against any row.

**Then record the outcome.** Fill each of the five Observation cells with what you actually
saw in that row, ending each with its own PASS or FAIL verdict. Five cells, five verdicts.
If any row fails, it is recorded in your words as gap-closure work and nothing is patched in
this session — the 16-09 / 17-15 / 19-05 / 19-12 precedent.
  </how-to-verify>
  <resume-signal>Give a separate verdict for each of rows 20, 21, 22, 23 and 24 — five answers, not one. For each, say what you saw and whether it passes; rows 20 through 23 need light and dark, row 24 does not. A single overall "approved" cannot be recorded against these rows. If a row fails, describe it in your own words and name the row number; nothing will be fixed during this session.</resume-signal>
  <done>Rows 20-24 each carry a dated verbatim observation naming that row's own controls, rows 20-23 in both themes, ending in its own PASS or FAIL verdict; both Round 4 probe outputs are on the record; and the Approval line carries an explicit Round 4 verdict with every failure logged unpatched.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve the sign-off, the gap register and the requirement statuses</name>
  <files>.planning/phases/19-design-system-control-styling/19-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` as filled by Task 2 — the five observations, the Approval line, the Validation Sign-Off checklist, and the Gap-Closure Record with GAP 1 through GAP 7
    - `.planning/REQUIREMENTS.md` lines 14-16, 47 and 75-78 — the UI-01/UI-02/UI-03/ACT-01 entries with their Round 3 annotations, and the phase status table rows
    - `.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md` — the confirmed root cause, the fix, and Probe F, which GAP 7's resolution must cite
    - `.planning/STATE.md` § Accumulated Context and § Blockers/Concerns — the Phase 19 entries that must reflect the new outcome and the wording precedent for recording a PARTIAL
    - `.planning/ROADMAP.md` Phase 19 line — the running annotation that must be extended, not rewritten
  </read_first>
  <action>
Resolve the record from Task 2's outcome. Fix no defect in this task.

1. **Sign-off.** Tick the Validation Sign-Off items actually delivered and append a dated
   Round 4 resolution line. Set `nyquist_compliant: true` in the frontmatter **only** if all
   five of rows 20-24 carry a PASS verdict and both probes returned their expected values;
   leave it `false` otherwise. Evaluate rows 20-24 only — row 18's Round 3 FAIL is permanent
   history and is not counted against Round 4.

2. **Gap register.** Resolve GAP 7 with a dated entry naming the confirmed root cause
   hypothesis, the fix applied, the plan that applied it, and a verbatim quote of row 20's
   observation plus Probe G's output as the rendered evidence. Resolve GAP 6 with a dated
   entry quoting row 21's observation — GAP 6 has been open since Round 3 as
   "declaration shipped, effect unconfirmed", and row 21 is the first observation of the
   effect. Do not delete or rewrite GAP 1 through GAP 5. If any row still fails, add its own
   numbered entry (GAP 8 onward) with a verbatim quote, the blocked row, the blocked
   requirement, the implicated decision ids, and **no** suggested fix and **no** root-cause
   theory.

3. **Requirement statuses in `REQUIREMENTS.md`.** UI-02 depends on rows 3-6, 10-17 and 19
   (already clean) plus the new rows 20-24. On a clean sweep of rows 20-24: tick UI-02,
   replace its "Round 3 — still open" annotation with a dated Round 4 note naming GAP 7's
   root cause and the rows evidencing its closure, and set its phase-status table row to
   `Complete`. On anything less: leave it unticked and update the parenthetical reason so it
   names the row that is still open. Never tick a requirement whose mapped rows did not all
   pass.

   UI-01, UI-03 and ACT-01 are already Complete and are not reopened by this round — row 24's
   sub-checks (b), (c) and (d) are their confirmed-unregressed evidence. Append that evidence
   to each entry as a dated note. If row 24 fails on any of those sub-checks, reopen the
   corresponding requirement and record it as a new gap rather than absorbing it.

4. **Record what this round teaches.** Extend the lessons note. The phase's running count of
   false-green mechanisms stands at four; state whether Round 4 adds a fifth — a guard
   documented as closing a hole it did not close, proven by mutation rather than by reading
   (`19-REVIEW-round3.md` R3-CR-01 and R3-CR-02) — and name the mitigation now in place:
   mutation-based acceptance criteria, where the required evidence is a recorded terminal
   failure under a named mutation rather than the existence of an assertion. Also record the
   mitigation this round introduced for the diagnosis itself: a separate diagnosis plan that
   must exclude every competing hypothesis by a cited probe field before any fix may be
   written, with an INCONCLUSIVE stop rather than a plausible guess.

5. **Roadmap.** Extend the Phase 19 line in `.planning/ROADMAP.md` with the Round 4 outcome,
   appended to the existing annotation rather than replacing it, and add plans 19-13 through
   19-17 to the phase's plan list with their waves.

6. If the verdict is PARTIAL, state plainly in the summary that the phase gate does not close
   and that another `/gsd-plan-phase 19 --gaps` pass is required.

Write the literal verdict token nowhere outside the five Observation cells, and put no `|`
inside any table cell.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/19-design-system-control-styling/19-VALIDATION.md';const s=fs.readFileSync(p,'utf8');const t=s.split('## Manual-Only Verifications')[1].split(String.fromCharCode(35,35,35))[0];const rows=t.split(String.fromCharCode(10)).filter(l=>l.startsWith('|')&&!/^\|\s*-+/.test(l)&&!/^\|\s*Behavior/.test(l));if(rows.length!==24)throw new Error('expected 24 rows, found '+rows.length);const obs=rows.slice(19).map(r=>r.split('|').slice(-2)[0]);const r4=obs.map(o=>(o.match(/VERDICT:\s*(PASS|FAIL)/)||[])[1]);if(r4.filter(Boolean).length!==5)throw new Error('expected 5 Round 4 verdicts, found '+r4.filter(Boolean).length);const v=(s.match(/VERDICT:\s*(PASS|FAIL)/g)||[]).length;if(v!==11)throw new Error('expected 11 verdict tokens file-wide, found '+v);const clean=!r4.includes('FAIL');if(clean&&!/nyquist_compliant: true/.test(s))throw new Error('clean Round 4 sweep but nyquist_compliant is not true');if(!clean&&!/nyquist_compliant: false/.test(s))throw new Error('a Round 4 row failed but nyquist_compliant is true');for(const g of ['GAP 6','GAP 7'])if(!new RegExp(g+'[\\s\\S]{0,4000}?Round 4').test(s))throw new Error('missing Round 4 resolution for '+g);const req=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const ui2=/- \[x\] \*\*UI-02\*\*/.test(req);if(clean&&!ui2)throw new Error('clean sweep but UI-02 is not ticked');if(!clean&&ui2)throw new Error('a Round 4 row failed but UI-02 is ticked');const rm=fs.readFileSync('.planning/ROADMAP.md','utf8');if(!/19-17-PLAN\.md/.test(rm))throw new Error('roadmap plan list not extended to 19-17');console.log('sign-off, gap register, requirements and roadmap agree with the Round 4 verdicts:',clean?'clean sweep':'PARTIAL')" && npm test && rm -f /tmp/bw17c.log && npm run build-widgets > /tmp/bw17c.log 2>&1; BW=$?; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw17c.log && git status --porcelain src scripts | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - `nyquist_compliant` is `true` only when all five Round 4 rows carry PASS and both probes returned their expected values; `false` otherwise. The evaluation is scoped to rows 20-24; row 18's permanent FAIL is not counted against it.
    - Exactly 11 verdict tokens exist file-wide: 6 from Round 3, 5 from Round 4.
    - GAP 7 carries a dated Round 4 resolution naming the confirmed hypothesis, the fix, the plan that applied it, and quoting both row 20's observation and Probe G's output. GAP 6 carries a dated Round 4 resolution quoting row 21's observation. GAP 1 through GAP 5 are unchanged.
    - If PARTIAL: a new numbered gap entry exists per still-failing row, each with a verbatim quote, the blocked row and requirement, the implicated decision ids, and no suggested fix and no root-cause theory.
    - UI-02 is ticked in `REQUIREMENTS.md` only on a clean sweep of rows 20-24, and its phase-status table row agrees with its checkbox.
    - UI-01, UI-03 and ACT-01 each carry a dated note citing row 24's relevant sub-check as confirmed-unregressed evidence, and remain Complete unless row 24 failed on their sub-check.
    - The lessons note states the running count of false-green mechanisms and names both mitigations this round introduced: mutation-based acceptance criteria, and a separate diagnosis plan with a cited-field exclusion requirement and an INCONCLUSIVE stop.
    - `.planning/ROADMAP.md`'s Phase 19 entry is extended with the Round 4 outcome and lists plans 19-13 through 19-17 with their waves.
    - `git status --porcelain src scripts` is empty — no source file was modified by this plan at all.
    - `npm test` exits 0 and `npm run build-widgets` emits no `css-syntax-error`, confirming nothing slipped in during the checkpoint session.
  </acceptance_criteria>
  <done>The validation record, `REQUIREMENTS.md` and `ROADMAP.md` all state exactly what a human confirmed row by row, `nyquist_compliant` matches the five Round 4 verdicts, GAP 6 and GAP 7 carry dated resolutions on rendered evidence or a new unpatched entry, and no defect was patched under checkpoint pressure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a row's premise → the row's verdict | Row 18 was judged only after a probe was improvised mid-session to test its own premise. Rows 20-22 are probe-gated by design, with the probes run and recorded before any appearance is judged. |
| a blanket verdict → a per-requirement claim | A verdict covering several rows cannot be traced to any one of them and cannot close a requirement. `19-VERIFICATION.md` found exactly this and reopened UI-02. |
| a green automated gate → phase sign-off | 921 tests were green with GAP 7 present. Green is the precondition for opening this checkpoint, never a substitute for it, and no row's verdict may cite the suite as evidence. |
| local build → production URL shape | Serving at the bare server root reproduces the class of defect that shipped Phase 16's black page behind a green gate. |
| checkpoint pressure → defect handling | Fixing a small defect mid-session is how a phase absorbs a gap instead of recording it. House rule since 16-09. |
| the plan's own prose → the plan's own gate | Round 3's descriptive text contained the counted verdict token and broke the count before the gate had ever run. The mechanics section exists for this. |

**Application-security surface: not applicable, stated rather than padded.** This plan
changes three planning documents and no source file. No input handling (ASVS V5),
authentication (V2), session management (V3), access control (V4) or cryptography (V6)
surface is involved. No package is installed. No private data file is staged; the served
directory is `dist/widgets` only. The assessment would change if the checkpoint ever
required serving a directory containing `data/` credentials or `.env`, or if a browser
automation dependency were introduced to run the probes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-R4-01 | Repudiation (a row judged under an unverified premise) | rows 20, 21, 22 | mitigate | Probes G and H are run and recorded before those rows are judged, with stated expected values and an explicit stop condition; row 21's instructions tell the developer to report a premise failure rather than guess. |
| T-19-R4-02 | Repudiation (a blanket verdict standing in for per-row evidence) | the checkpoint recording | mitigate | Five independent verdict tokens, five row-specific anchor sets asserted one anchor per sub-check, a minimum observation length and a pairwise-distinctness check, all in Task 2's gate. |
| T-19-R4-03 | Repudiation (a source-level green cited as rendered evidence) | row 20's verdict | mitigate | The acceptance criterion names `styles.test.ts` explicitly as inadmissible for this claim, and the row's Why Manual column states the vitest `node` environment reason in the record itself. |
| T-19-R4-04 | Tampering (a self-inflicted gate break from the plan's own prose) | `19-VALIDATION.md` | mitigate | The gate mechanics section pins the verdict-token arithmetic at 6 before and 11 after, forbids the literal token outside the five cells, and forbids `|` inside cells — both failure modes recorded in `19-12-SUMMARY.md`. |
| T-19-R4-05 | Elevation of privilege (a requirement ticked on partial evidence) | `.planning/REQUIREMENTS.md` UI-02 | mitigate | Task 3's gate ties the UI-02 checkbox and its phase-status row to a row-scoped clean sweep of rows 20-24 and fails if they disagree in either direction. |
| T-19-R4-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized. The server is `python3 -m http.server`, already present. Do not install jsdom, puppeteer or playwright to automate any row here — no automated substitute for these five rows exists, and this phase does not accept one. |
</threat_model>

<verification>
- All four gate commands exit 0 before the checkpoint opens; `build-widgets` emits zero `css-syntax-error`.
- The build is served under `http://localhost:8099/strava-widgets/` from a symlink proven to resolve.
- 24 table rows; 5 Round 4 sentinels before the checkpoint; 11 verdict tokens after it.
- Both Round 4 probes recorded verbatim before the rows depending on them were judged.
- `nyquist_compliant`, UI-02's checkbox and UI-02's phase-status row all agree with the five Round 4 verdicts.
- `git status --porcelain src scripts` empty throughout.
</verification>

<success_criteria>
Five claims — the nav holds, a focused control stays under it, the jump bar still orders and
parks correctly, a focused nav link's ring is whole, and nothing else moved — each carry
their own verdict from a human looking at a real page, the four rendering rows in both
themes, and the phase's
requirement statuses say exactly what that human confirmed and nothing more.
</success_criteria>

<output>
Create `.planning/phases/19-design-system-control-styling/19-17-SUMMARY.md` when done.
Record: the four gate exit codes with the vitest and `verify-dashboard` counts and explicit
confirmation of zero `css-syntax-error`; the verbatim staging commands with the absolute
source path; both probe outputs verbatim alongside their expected values; all five Round 4
observations verbatim with their verdicts; whether each of the three known non-Phase-19
issues still occurs; the resulting `nyquist_compliant` value; the UI-01/UI-02/UI-03/ACT-01
statuses with the row 24 sub-check evidencing each; the GAP 6 and GAP 7 resolutions; the
updated false-green mechanism count with this round's two mitigations; and — if PARTIAL —
that the phase gate does not close and another gap-closure pass is required.
</output>
