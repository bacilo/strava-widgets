---
phase: 20-row-click-interaction-pattern
plan: 14
type: execute
wave: 10
depends_on: ["20-11"]
files_modified:
  - src/dashboard/styles.test.ts
autonomous: true
gap_closure: true
requirements: [UX-03]
must_haves:
  decisions: [D-06, D-09, D-10, D-15]
  truths:
    - "All seven Phase 20 CSS assertions read the cascade winner, not any-rule-wins — a later top-level override turns them red"
    - "All seven Phase 20 CSS assertions also fail when an at-rule-scoped rule redeclares the guarded property, closing the @media blind spot"
    - "Each fix carries an in-suite proof of the exact blind spot it closes, executed against synthetic CSS rather than by editing styles.css"
    - "The helper audit paragraph no longer claims the whole Phase 20 block was migrated when three assertions were not"
  artifacts:
    - path: "src/dashboard/styles.test.ts"
      provides: "cascade-aware Phase 20 assertions, an assertNoAtRuleOverride companion helper, and blind-spot proofs for WR-02 and WR-03"
      contains: "assertNoAtRuleOverride"
  key_links:
    - from: "the seven Phase 20 CSS assertions"
      to: "assertNoAtRuleOverride"
      via: "a companion call alongside each cascade-winner assertion"
      pattern: "assertNoAtRuleOverride\\("
---

<objective>
Close two of the three guard-layer WARNINGs D-15 names, both of them false-green mechanisms this
phase's own work introduced: WR-02 (three Phase 20 CSS assertions still on any-rule-wins
`selectorListDeclares`) and WR-03 (`cascadeWinningBodyDeclaring` and its siblings skip every
at-rule-scoped rule, so a `@media` override leaves the converted assertions green).

Purpose: plan 20-10 was raised specifically to close the any-rule-wins class and closed four of
seven assertions. `20-VERIFICATION.md` confirmed the remaining three by direct grep at
`styles.test.ts:1263/1267/1292`, and `20-REVIEW.md` proved by executed mutation that appending
`.activity-row { text-decoration: underline; }` to `styles.css` leaves the guard green — the
precise regression the rule's own comment says it exists to prevent. WR-03 is the same class one
level deeper: appending `@media (max-width: 720px) { .activity-row { display: block; } }` leaves
even the converted `display: flex` assertion green, and `.activity-row`'s presentation is
genuinely governed by the 720px breakpoint in this very file.

Output: seven cascade-aware assertions, each paired with an at-rule companion, each backed by an
executed in-suite proof of the blind spot it closes — matching plan 20-10's established shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md
@.planning/phases/20-row-click-interaction-pattern/20-VERIFICATION.md
@.planning/phases/20-row-click-interaction-pattern/20-REVIEW.md

<interfaces>
Helpers already in `src/dashboard/styles.test.ts`, extracted so no exploration is needed:

- `splitTopLevelSelectors(head: string): string[]` (`:86`) — parenthesis-aware comma split of a
  rule head into exact selector tokens.
- `selectorListDeclares(needle: string, declaration: string): boolean` (`:139-153`) — returns
  `true` as soon as ANY rule whose selector list contains `needle` has `declaration` as a body
  substring. Any-rule-wins. This is the helper WR-02 is about.
- `isAtRuleScoped(offset: number, head: string): boolean` (`:202`) and
  `assertNotAtRuleScoped(offset, head, needle)` (`:224`) — the at-rule range machinery, computed
  once by brace matching over `cssNoComments`.
- `bodyForSelectorListToken(needle, source = cssNoComments): string` (`:269`) — LAST
  non-at-rule-scoped body for the token; throws naming the at-rule block when the token was seen
  only inside one, or the generic "no rule found" message when never seen.
- `bodiesForSelectorListToken(needle, source = cssNoComments): string[]` — every non-at-rule-scoped
  body in source order; `continue`s on `isAtRuleScoped`.
- `cascadeWinningBodyDeclaring(needle, property, source = cssNoComments): string` — the last body
  among those that actually declares `property`; throws naming both when none does.

Both `bodyForSelectorListToken` and `bodiesForSelectorListToken` accept an optional `source`
argument that defaults to the real stylesheet, added by plan 20-10 precisely so self-tests can
exercise synthetic CSS strings without editing `styles.css`. Use that argument for every proof in
this plan — no proof may write to `src/dashboard/styles.css`.

The Phase 20 describe block is `styles.css - Phase 20 row-click interaction pattern`
(`styles.test.ts:1249` onward). Its seven positive assertions and their current helpers:

| Line | Assertion | Helper today |
|------|-----------|--------------|
| 1263 | the bare `a` rule declares `color: inherit` | `selectorListDeclares` |
| 1267 | the bare `a` rule declares `text-decoration: underline` | `selectorListDeclares` |
| 1288 | `.activity-row` keeps `display: flex` | `cascadeWinningBodyDeclaring` |
| 1292 | `.activity-row` declares `text-decoration: none` | `selectorListDeclares` |
| 1296 | `.activity-row:hover` mixes from `--surface` toward `--text` | `bodyForSelectorListToken` |
| 1302 | `.activity-table__row--navigable` declares `cursor: pointer` | `bodyForSelectorListToken` |
| 1306 | `.activity-table__row--navigable:hover` mixes with the same formula | `bodyForSelectorListToken` |

The residual arithmetic, re-derived from the file rather than asserted — get this right, because
Task 1's verify depends on it. The Phase 20 block contains **six** `selectorListDeclares` calls in
total: **three positive** (the WR-02 targets at `:1263`, `:1267` and `:1292`, all converting in
this plan) and **three negative**, which stay:

| Line | Negative assertion | Why it stays |
|------|--------------------|--------------|
| ~1275 | `selectorListDeclares('a', 'color: var(--accent)')` is `false` | D-15 does not name it. `20-REVIEW.md` logs its vacuity-on-deletion as IN-10, which stays open. |
| ~1315 | `selectorListDeclares('.activity-table tbody tr', 'cursor: pointer')` is `false` | D-10's negative half — the four non-activity tables no longer inherit a pointer. |
| ~1319 | `selectorListDeclares('.activity-table tbody tr:hover', 'color-mix')` is `false` | D-10's other negative half. |

So: **6 total, 3 convert, 3 remain — and the 3 that remain are exactly the three selectors in the
table above.** The Phase 20 describe block is the last one in the file, so a slice from its title
to end-of-file is exactly the block and nothing else. All three negatives are **out of scope for
this plan**; do not convert them, and do not add a fourth negative assertion to satisfy a count.
</interfaces>

<binding_decision>
D-15, locked in `20-CONTEXT.md` on 2026-08-17. This plan implements the WR-02 and WR-03 halves:

> three of seven Phase 20 CSS assertions (`styles.test.ts:1263, 1267, 1292`) are still on
> any-rule-wins `selectorListDeclares` — the exact mechanism plan 20-10 was raised to close and
> only half-closed (WR-02); and `cascadeWinningBodyDeclaring` skips every at-rule-scoped rule, so
> a `@media` override of `.activity-row` leaves all four already-converted assertions green
> (WR-03). Each fix carries an in-suite proof of the blind spot it closes, matching 20-10's
> established shape.

The WR-01 half (`row-semantics.test.ts`'s role allowlist) is plan 20-15's, not this plan's.
</binding_decision>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-02 — move the three remaining Phase 20 assertions onto the cascade winner, with an executed proof</name>
  <files>src/dashboard/styles.test.ts</files>
  <read_first>
    - `src/dashboard/styles.test.ts` lines 130-160 — `selectorListDeclares`, so the any-rule-wins
      early `return true` is in view before it is replaced.
    - `src/dashboard/styles.test.ts` lines 255-378 — `bodyForSelectorListToken`,
      `bodiesForSelectorListToken` and `cascadeWinningBodyDeclaring`, including their optional
      `source` parameter and the exact wording of their throw messages.
    - `src/dashboard/styles.test.ts` lines 1249-1320 — the whole Phase 20 describe block with its
      long explanatory comments about why `declarationsFor` must not be used on the bare `a`
      selector.
    - `src/dashboard/styles.css` lines 1500-1552 — the shipped Phase 20 rules (`a`,
      `.activity-row`, `.activity-row:hover`, `.activity-table__row--navigable` and its `:hover`)
      so the assertions match real declaration text.
    - `.planning/phases/20-row-click-interaction-pattern/20-REVIEW.md` section "WR-02" — the
      executed mutation and the two rewritten assertions it drafts.
  </read_first>
  <action>
Convert the three named assertions to read the cascade winner, keeping each `it` title's substance
and adding the helper change to the title where it clarifies:

- `D-06: the bare a rule declares color: inherit` becomes an expectation that
  `cascadeWinningBodyDeclaring('a', 'color')` contains `color: inherit`.
- `D-06: the bare a rule declares text-decoration: underline` becomes an expectation that
  `cascadeWinningBodyDeclaring('a', 'text-decoration')` contains `text-decoration: underline`.
- `.activity-row declares text-decoration: none` becomes an expectation that
  `cascadeWinningBodyDeclaring('.activity-row', 'text-decoration')` contains
  `text-decoration: none`.

`cascadeWinningBodyDeclaring` is the correct helper for all three even though `a` is declared only
once today: `bodyForSelectorListToken` returns the last body whether or not it declares the
property under test, which is wrong the moment a second `a` rule is added that does not carry
`color`. Say that in a short comment above the two `a` assertions.

Keep the existing long comment about `declarationsFor`'s unanchored `a {` regex matching `.cta {`
first — it is still true and still load-bearing. Extend it with one sentence explaining that
`selectorListDeclares` was not the right replacement either, because it is any-rule-wins.

Add one new `it` immediately after the three, titled as the WR-02 blind-spot proof, that executes
the review's mutation against a synthetic stylesheet string rather than against `styles.css`.
Build a small CSS string containing `.activity-row { text-decoration: none; }` followed by
`.activity-row { text-decoration: underline; }` and assert both halves:

- `selectorListDeclares` cannot be given a `source` argument, so reproduce its any-rule-wins
  behaviour inline over the synthetic string (a scan that returns `true` on the first matching
  rule) and assert that it reports `true` for `text-decoration: none` — documenting the miss.
- `cascadeWinningBodyDeclaring('.activity-row', 'text-decoration', synthetic)` returns the body
  carrying `underline`, so an expectation that it contains `text-decoration: none` would fail —
  assert that directly, for example by asserting the returned body contains `underline` and does
  not contain `none`.

Comment above it that the first half documents the defect the old guard shipped with and must not
be "fixed" to a passing value, in the same register `row-semantics.test.ts`'s WR-02 blind-spot
proof uses.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e "import {readFileSync} from 'node:fs'; const t=readFileSync('src/dashboard/styles.test.ts','utf8'); const block=t.slice(t.indexOf('Phase 20 row-click interaction pattern')); const positives=[[\"cascadeWinningBodyDeclaring('a', 'color')\",'bare a color'],[\"cascadeWinningBodyDeclaring('a', 'text-decoration')\",'bare a text-decoration'],[\"cascadeWinningBodyDeclaring('.activity-row', 'text-decoration')\",'.activity-row text-decoration']]; for(const [needle,label] of positives){ if(!block.includes(needle)) throw new Error('not converted to the cascade winner: '+label); } const sld=(block.match(/selectorListDeclares\(/g)||[]).length; if(sld!==3) throw new Error('expected exactly 3 remaining selectorListDeclares calls in the Phase 20 block (6 before, 3 positives converted), found '+sld); const negatives=[\"selectorListDeclares('a', 'color: var(--accent)')\",\"selectorListDeclares('.activity-table tbody tr', 'cursor: pointer')\",\"selectorListDeclares('.activity-table tbody tr:hover', 'color-mix')\"]; for(const neg of negatives){ if(!block.includes(neg)) throw new Error('a negative assertion that must stay was removed or rewritten: '+neg); } if(!/WR-02/.test(block)) throw new Error('the WR-02 blind-spot proof is missing from the Phase 20 block'); console.log('three assertions converted, negatives untouched, blind-spot proof present');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/styles.test.ts` exits 0; `npx tsc --noEmit -p tsconfig.json`
      emits zero diagnostics.
    - The Phase 20 describe block contains the literal calls
      `cascadeWinningBodyDeclaring('a', 'color')`,
      `cascadeWinningBodyDeclaring('a', 'text-decoration')` and
      `cascadeWinningBodyDeclaring('.activity-row', 'text-decoration')`.
    - The residual count checks out at **6 before, 3 converted, 3 remaining**:
      `selectorListDeclares(` occurs exactly three times in the Phase 20 block after this task, and
      those three are, by literal match, `selectorListDeclares('a', 'color: var(--accent)')`,
      `selectorListDeclares('.activity-table tbody tr', 'cursor: pointer')` and
      `selectorListDeclares('.activity-table tbody tr:hover', 'color-mix')` — all negative, all
      out of scope. No positive assertion uses the helper, no negative assertion was converted, and
      no new negative assertion was invented to satisfy the count.
    - A WR-02 blind-spot proof `it` exists, executes against a synthetic CSS string passed as the
      helper's `source` argument, asserts the any-rule-wins scan reports the stale declaration as
      present, and asserts the cascade winner resolves to the overriding declaration instead.
    - `git status --porcelain src/dashboard/styles.css` is empty — no mutation was written to the
      real stylesheet.
    - The existing `declarationsFor` / `.cta {` explanatory comment survives and gains one sentence
      about `selectorListDeclares` being any-rule-wins.
  </acceptance_criteria>
  <done>All seven positive Phase 20 assertions read the cascade winner, and the mechanism plan 20-10 half-closed is closed with an executed proof.</done>
</task>

<task type="auto">
  <name>Task 2: WR-03 — an at-rule companion assertion, applied to all seven, with an executed proof</name>
  <files>src/dashboard/styles.test.ts</files>
  <read_first>
    - `src/dashboard/styles.test.ts` lines 196-240 — `isAtRuleScoped` and `assertNotAtRuleScoped`,
      the offset-range machinery the new helper must reuse rather than re-derive.
    - `src/dashboard/styles.test.ts` lines 255-378 — the three selector-token helpers and their
      JSDoc, especially `cascadeWinningBodyDeclaring`'s claim that its return value is "the
      cascade winner … matching how CSS resolves a selector declared more than once", which is
      the sentence WR-03 says is unqualified.
    - `src/dashboard/styles.test.ts` lines 785-820 — the helper-audit paragraph that presents the
      Phase 20 migration as complete and must be corrected.
    - `src/dashboard/styles.css` lines 540-556 — the 720px media block that already scopes
      `.activity-list--cards` and `.activity-table-wrapper`, so the `@media` risk for
      `.activity-row` is concrete rather than theoretical.
    - `.planning/phases/20-row-click-interaction-pattern/20-REVIEW.md` section "WR-03" — the two
      executed mutations and the drafted `assertNoAtRuleOverride` signature.
  </read_first>
  <action>
Add `function assertNoAtRuleOverride(needle: string, property: string, source: string =
cssNoComments): void` beside the other selector-token helpers. It scans `source` with the same
`RULE_SCANNER()` walk the other helpers use, splits each head with `splitTopLevelSelectors`, keeps
only the candidates whose selector list contains `needle` as an exact token AND for which
`isAtRuleScoped` is true, and throws when any such body declares `property` — anchoring the
property match to a declaration boundary (start of body, or a preceding `;` or whitespace) and
escaping `property` with the same `.replace` guard the other helpers use, so `index` can never
match inside `z-index`. The throw message must name the selector, the property and the at-rule
head, so a failure says which breakpoint kills the rule.

Give it a JSDoc stating plainly that it is the companion for the blind spot
`cascadeWinningBodyDeclaring`, `bodyForSelectorListToken` and `bodiesForSelectorListToken` cannot
see by construction, because all three `continue` on `isAtRuleScoped`.

Add one sentence to each of those three helpers' JSDoc stating the exclusion explicitly: the
returned body is the cascade winner **among top-level rules only**, at-rule-scoped rules are
skipped, and a guard built on it must pair with `assertNoAtRuleOverride` to be complete. Do not
rename the helpers — the review offers `topLevelCascadeWinningBodyDeclaring` as an option, and
renaming would churn every Phase 16-19 call site for no behavioural gain; state that choice in the
JSDoc so the naming question is recorded rather than left open.

Pair every one of the seven positive Phase 20 assertions with an `assertNoAtRuleOverride` call for
the same selector and property, added as a second statement inside the same `it`:

- `a` with `color`, and `a` with `text-decoration`
- `.activity-row` with `display`, and `.activity-row` with `text-decoration`
- `.activity-row:hover` with `background`
- `.activity-table__row--navigable` with `cursor`
- `.activity-table__row--navigable:hover` with `background`

Add one new `it` titled as the WR-03 blind-spot proof, executed against synthetic CSS strings via
the `source` argument, covering both of the review's mutations:

- A string containing top-level `.activity-row { display: flex; }` followed by
  `@media (max-width: 720px) { .activity-row { display: block; } }`. Assert
  `cascadeWinningBodyDeclaring('.activity-row', 'display', synthetic)` still returns the
  `display: flex` body — documenting the miss — and that `assertNoAtRuleOverride('.activity-row',
  'display', synthetic)` throws.
- A string containing top-level `.activity-table__row--navigable { cursor: pointer; }` followed by
  a `@media` block redeclaring `cursor: default` for the same selector. Assert
  `bodyForSelectorListToken` still reports `cursor: pointer` and that `assertNoAtRuleOverride`
  throws.

Comment above the proof that the first expectation in each pair documents the helper's structural
blind spot and must not be "fixed" into a failing value; the second is the closure.

Finally, correct the helper-audit paragraph at `styles.test.ts:790-814`: it currently reads as if
the whole Phase 20 block was migrated by plan 20-10. Rewrite the Phase 20 paragraph to state that
20-10 converted four of seven, that plan 20-14 converted the remaining three (WR-02), that all
seven now also carry an `assertNoAtRuleOverride` companion (WR-03), and that the block's negative
assertions deliberately stay on `selectorListDeclares` with IN-10's vacuity noted as open.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e "import {readFileSync} from 'node:fs'; const t=readFileSync('src/dashboard/styles.test.ts','utf8'); if(!/function assertNoAtRuleOverride\(/.test(t)) throw new Error('assertNoAtRuleOverride is not defined'); const block=t.slice(t.indexOf('Phase 20 row-click interaction pattern')); const pairs=[[\"'a', 'color'\"],[\"'a', 'text-decoration'\"],[\"'.activity-row', 'display'\"],[\"'.activity-row', 'text-decoration'\"],[\"'.activity-row:hover', 'background'\"],[\"'.activity-table__row--navigable', 'cursor'\"],[\"'.activity-table__row--navigable:hover', 'background'\"]]; for(const [p] of pairs){ if(!block.includes('assertNoAtRuleOverride('+p)) throw new Error('missing at-rule companion for '+p); } const n=(block.match(/assertNoAtRuleOverride\(/g)||[]).length; if(n<7) throw new Error('expected at least 7 companion calls in the Phase 20 block, found '+n); if(!/WR-03/.test(block)&&!/WR-03/.test(t)) throw new Error('the WR-03 blind-spot proof is missing'); const audit=t.slice(t.indexOf('Phase 20 cascade migration')); if(!/20-14/.test(audit.slice(0,3000))) throw new Error('the helper-audit paragraph was not corrected to name plan 20-14 and the four-of-seven split'); console.log('companion helper defined, seven pairs wired, audit corrected');" && test -z "$(git status --porcelain src/dashboard/styles.css)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/styles.test.ts` exits 0; `npx tsc --noEmit -p tsconfig.json`
      emits zero diagnostics.
    - `assertNoAtRuleOverride(needle, property, source = cssNoComments)` exists, reuses
      `RULE_SCANNER`, `splitTopLevelSelectors` and `isAtRuleScoped` rather than re-deriving them,
      anchors the property match to a declaration boundary, escapes the property, and throws a
      message naming the selector, the property and the offending at-rule head.
    - All seven positive Phase 20 assertions call `assertNoAtRuleOverride` with the matching
      selector and property listed in this task's action.
    - The JSDoc of `cascadeWinningBodyDeclaring`, `bodyForSelectorListToken` and
      `bodiesForSelectorListToken` each state the top-level-only exclusion and name
      `assertNoAtRuleOverride` as the companion; each records that the rename the review proposed
      was declined and why.
    - A WR-03 blind-spot proof `it` exists, runs both of the review's mutations against synthetic
      CSS strings via the `source` argument, asserts the old helpers stay green on each, and
      asserts `assertNoAtRuleOverride` throws on each.
    - The helper-audit paragraph names the four-of-seven split, plan 20-14's completion of it, the
      new companion, and IN-10's remaining vacuity on the negative assertions.
    - `git status --porcelain src/dashboard/styles.css` is empty — this plan changes no stylesheet.
  </acceptance_criteria>
  <done>A `@media` override of any Phase 20 selector now turns the suite red, and the helpers' structural blind spot is stated in their own docs instead of contradicted by their names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stylesheet text -> asserted truth | Every claim this suite makes about rendering is a text assertion over `styles.css`. This repository has shipped rendering defects behind a green gate four times; the guard layer is the only automated line of defence and it has now produced two false-green mechanisms of its own. |
| top-level cascade -> at-rule cascade | The helpers resolve only the top-level cascade. A `@media` block can kill a guarded rule at one breakpoint with every assertion still green — the exact case `.activity-row` is exposed to at 720px. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20G4-P14-01 | Spoofing | an assertion that reports a dead rule as live | mitigate | The three any-rule-wins assertions move to `cascadeWinningBodyDeclaring`, which throws when no top-level body declares the property and returns the last one that does; Task 1's proof executes the review's mutation to show the old form green and the new form resolving to the override. |
| T-20G4-P14-02 | Spoofing | a `@media` override invisible to every helper | mitigate | `assertNoAtRuleOverride` is added and paired with all seven positive assertions; Task 2's proof executes both of the review's mutations and asserts it throws on each. |
| T-20G4-P14-03 | Tampering | a guard "closed" by editing the real stylesheet to suit the test | mitigate | Every proof runs against synthetic CSS strings through the helpers' existing optional `source` argument, and both tasks assert `git status --porcelain src/dashboard/styles.css` is empty. |
| T-20G4-P14-04 | Repudiation | an audit comment claiming more coverage than exists | mitigate | Task 2 rewrites the helper-audit paragraph to state the four-of-seven split, the companion, and IN-10's remaining vacuity, so the next reader is not told the block is fully migrated when it is not. |
| T-20G4-P14-05 | Denial of Service | a helper rename churning forty Phase 16-19 call sites | accept | The rename `20-REVIEW.md` suggests is declined; the exclusion is stated in the JSDoc instead, and the choice is recorded there so the question is closed rather than rediscovered. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized; no `## Package Legitimacy Audit` is required because no package-manager install task exists. Do not add a CSS parser dependency to resolve at-rules properly — the brace-matching machinery already in this file is sufficient for the companion assertion. |
</threat_model>

<verification>
- `npx vitest run src/dashboard/styles.test.ts` exits 0 with the two new blind-spot proofs passing.
- `npx tsc --noEmit -p tsconfig.json` emits zero diagnostics.
- The Phase 20 block contains zero positive assertions on `selectorListDeclares` and seven
  `assertNoAtRuleOverride` companions matching the selector/property table in Task 2.
- `git status --porcelain src/dashboard/styles.css` is empty; `git diff --stat src/` lists only
  `src/dashboard/styles.test.ts`.
</verification>

<success_criteria>
The two false-green mechanisms this phase's own guard layer introduced are closed, each with an
executed proof rather than a claim: a later top-level override and a `@media`-scoped override both
turn the suite red for every Phase 20 rule, and the helpers' documentation now states the boundary
their names imply they do not have.
</success_criteria>

<output>
Create `.planning/phases/20-row-click-interaction-pattern/20-14-SUMMARY.md` when done.
</output>
