---
phase: 23-trends-zoom-pan-taller-bands
plan: 11
type: execute
wave: 7
gap_closure: true
depends_on: ["23-08", "23-09", "23-10"]
files_modified:
  - .planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TRN-01, TRN-02, TRN-03, TRN-04]
closes_rows: [R8, R11, R15, R16]

must_haves:
  decisions: [D-02, D-06, D-11, D-13, D-14, D-17, D-19, D-20, D-21, D-22, D-23, D-25]
  truths:
    - "Every Round 1 FAIL (R8, R11, R15, R16) is re-asked in a real browser against a provably fresh build"
    - "Every Round 1 PASS that the three fix plans could plausibly regress is re-asked, not assumed"
    - "TRN-01, TRN-02, TRN-03 and TRN-04 are each gated strictly on their own Round 2 row map"
    - "Findings 7 and 8 each get their own row with its own stated observation"
  artifacts:
    - path: ".planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md"
      provides: "Per-Task Verification Map rows for plans 23-08/09/10, a recomputed Round 2 expected-value table, and the Round 2 checkpoint record"
      contains: "Round 2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TRN-01..TRN-04 re-gated strictly on the Round 2 row map"
  key_links:
    - from: "Round 2 row verdicts"
      to: ".planning/REQUIREMENTS.md TRN-01..TRN-04"
      via: "the requirement-gating map at the end of Task 2"
      pattern: "TRN-0"
---

<objective>
Hold the BLOCKING human browser checkpoint that Round 1's four FAILs require, against a provably
fresh build carrying plans 23-08, 23-09 and 23-10, and gate TRN-01 through TRN-04 strictly on the
result.

Round 1 closed 2026-08-26 with 16 PASS / 4 FAIL / 0 BLOCKED. The four FAILs — R8 (step magnitude),
R11 (no gesture ever settles), R15 (year heatmap overflows every phone width) and R16 (D-02 lockstep
breaks on the gesture path) — are exactly what plans 23-08 and 23-09 fix. Plan 23-10 additionally
changes what every x-axis tick renders, which means Round 1's PASSes on R2, R4, R5 and R18 rest on
tick strings that no longer apply, and their expected values must be recomputed before Round 2 opens
rather than re-used.

`nyquist_compliant: true` requires a clean sweep. It is not reachable on a partial round.

Purpose: three of four TRN requirements are Pending on located, reproducible defects, and only a
real browser can discharge them.

Output: an updated Per-Task Verification Map, a recomputed Round 2 expected-value table, a Round 2
checkpoint record, and re-gated requirement states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-CONTEXT.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-07-SUMMARY.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-08-SUMMARY.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-09-SUMMARY.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the build is fresh, recompute the expected read-back values, and extend the verification map</name>
  <files>.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md` — the whole file: § Test Infrastructure, § Per-Task Verification Map, § "Expected read-back values (Round 1)", § "Lazy chunk boundary proof", § "Staged build (Round 1)", the Round 1 row table and § Findings
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-07-PLAN.md` Task 1 — the exact gate, lazy-chunk-proof and advance-computation discipline this task repeats, and the D-25 requirement that every expected value is computed BEFORE a browser is opened
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-08-SUMMARY.md`, `23-09-SUMMARY.md`, `23-10-SUMMARY.md` — each records the automated commands it added and, for 23-10, every expected tick string it invalidated
    - `src/dashboard/views/trends-zoom-logic.ts` and `src/dashboard/views/trends-tick-format.ts` — the CURRENT formulas the recomputation script must re-implement; do not re-implement the pre-23-08 arithmetic
    - `scripts/build-widgets.mjs` and `dist/widgets/index.html` — where the entry asset filename is read from
  </read_first>
  <action>
    Run this in order and record every output verbatim into `23-VALIDATION.md`.

    **(a) Clear the port before trusting anything.** Check whether a process is already bound to
    port 8099 and, if so, report its working directory before killing it. Round 1 found a stale
    `python3 -m http.server` from an unrelated session already bound there, serving `/private/tmp/gh-pages`.
    Record what you found, even if the answer is "nothing was bound".

    **(b) Clean rebuild and full gate.** `rm -rf dist/widgets`, then `npm test`, then
    `npx tsc --noEmit`, then `npm run build-widgets`, then `npm run verify-dashboard`. All four must
    exit 0. Record the test-file and test counts, the tsc exit code, and `verify-dashboard`'s check
    count against its 37-check Round 1 baseline.

    **(c) Prove the build is NEW, not merely rebuilt.** Read the module `<script src>` out of
    `dist/widgets/index.html` and record the `assets/index-*.js` filename, and the `assets/index-*.css`
    filename from the stylesheet link. Round 1's entry asset was **`assets/index-D2l-GZfl.js`** and
    reproduced byte-identically across two separate clean rebuilds, which is exactly why an unchanged
    hash cannot be read as proof of anything. Three plans changed `src/dashboard/` this round, so the
    entry hash MUST differ from `index-D2l-GZfl.js`. If it does not, stop and investigate before
    staging anything — an identical hash means the fixes are not in the artifact.

    **(d) Re-prove the lazy chunk boundary (T-23-LAZY).** `grep -c "Hammer"` on the entry chunk must
    be `0`; on the newest `dist/widgets/assets/trends-charts-*.js` chunk it must be non-zero. Record
    both filenames and both counts, and re-state the minifier caveat Round 1 recorded.

    **(e) Recompute every expected read-back value, BEFORE any browser is opened (D-25).** Write a
    standalone Node script that re-implements the CURRENT formulas against the real archive JSON in
    `data/stats/` — `computeArchiveBounds`, `computeFullRange`, `computeDefaultWindow`,
    `loadWindowRange`, `formatRangeLabel`, and this round's new `zoomStepRange`, `panStepRange`,
    `stepMsFromTicks`, `tickGranularityForStep` and `formatTimeAxisTick`. Do not copy Round 1's table
    forward; recompute it. First re-confirm the archive is unchanged since Round 1 (last weekly bucket
    `2026-08-10T00:00:00.000Z`, last monthly `2026-08-01T00:00:00.000Z`, last yearly
    `2026-01-01T00:00:00.000Z`, last training-load day `2026-08-11`, last commit touching `data/stats/`
    = `fe53914`) and record any drift explicitly rather than silently absorbing it.

    Publish the result as a new `## Expected read-back values (Round 2)` section with one row per
    state, carrying the expected canvas `aria-label` verbatim, the expected FIRST and LAST x-axis tick
    labels, and a **"changed since Round 1"** column with the Round 1 value beside the new one wherever
    they differ. At minimum the table must cover: Volume weekly default; Volume monthly default;
    Volume yearly default; Volume weekly after one `+`; Volume weekly after one `−`; Volume weekly
    after one `←`; Volume weekly at full zoom-out; Training Load 12mo default; and Training Load at an
    ≈11-day window. `23-10-SUMMARY.md` lists every tick string that plan invalidated — cross-check
    your computed values against that list and reconcile any disagreement before continuing.

    **(f) Extend the Per-Task Verification Map.** Add one row per new automated command from
    `23-08-SUMMARY.md`, `23-09-SUMMARY.md` and `23-10-SUMMARY.md`, keyed `23-08/T1`, `23-08/T2`,
    `23-08/T3`, `23-09/T1`, `23-09/T2`, `23-10/T1`, `23-10/T2`, `23-10/T3`, each with its Wave (6),
    Requirement, Threat Ref, Secure Behavior, Test Type, Automated Command, File Exists and Status —
    Status set from this session's actual run, never anticipated. Re-key the now-stale `23-01/T3` row
    whose filter reads `-t "panDeltaPx|zoomFactor"` to `-t "zoomStepRange and panStepRange"`, since
    plan 23-08 retired `panDeltaPx`. Add a row for this plan's own Task 2 (`23-11/T2`), test type
    `manual-only (blocking checkpoint)`, status `⬜ pending`.

    **(g) Stage the build.** Serve `dist/widgets` from the MAIN CHECKOUT (not a worktree — Round 1's
    2026-08-20 staging was a symlink into the 23-07 worktree and was destroyed when that worktree
    merged) under a path shape matching production, so the URL is
    `http://127.0.0.1:8099/strava-widgets/`. Confirm with `curl -sI` that it returns 200 and with
    `curl -s ... | grep -o 'assets/index-[A-Za-z0-9_-]*\.js'` that the served entry filename matches
    step (c) verbatim. Record both.

    Write all of (a) through (g) into `23-VALIDATION.md` as a `## Round 2 — build, staging and
    expected values` section placed above the Round 2 checkpoint record Task 2 will add.

    Change no source file in this task.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md','utf8');const need=['23-08/T1','23-08/T2','23-08/T3','23-09/T1','23-09/T2','23-10/T1','23-10/T2','23-10/T3','23-11/T2','Expected read-back values (Round 2)'];const missing=need.filter(k=>!s.includes(k));if(s.includes('panDeltaPx'))missing.push('stale panDeltaPx row not re-keyed');if(missing.length){console.error('missing: '+missing.join(', '));process.exit(1)}console.log('validation map extended')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx tsc --noEmit`, `npm run build-widgets` and `npm run verify-dashboard` were each run in this session and each exited 0, with their actual counts recorded in `23-VALIDATION.md`.
    - `23-VALIDATION.md` records an `assets/index-*.js` filename that is NOT `assets/index-D2l-GZfl.js`, and states plainly that it differs from Round 1's.
    - `23-VALIDATION.md` records the entry-chunk `Hammer` count as `0` and a non-zero count on a named `trends-charts-*.js` chunk.
    - A `## Expected read-back values (Round 2)` section exists with at least the nine states listed in the action, each carrying a verbatim `aria-label`, a first tick, a last tick, and a "changed since Round 1" annotation.
    - The Per-Task Verification Map contains rows keyed `23-08/T1`, `23-08/T2`, `23-08/T3`, `23-09/T1`, `23-09/T2`, `23-10/T1`, `23-10/T2`, `23-10/T3` and `23-11/T2`.
    - The string `panDeltaPx` no longer appears anywhere in `23-VALIDATION.md`.
    - `curl -sI http://127.0.0.1:8099/strava-widgets/` returned `200` and the served entry filename matches the built one verbatim, both recorded.
    - `git status --porcelain src/` reports no modification — this task changes no source file.
  </acceptance_criteria>
  <done>The gate is green, the served artifact is provably new, the expected values are recomputed from the current formulas against the live archive, and the verification map covers every command this round added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Round 2 blocking human browser checkpoint (R21-R42), then record and re-gate</name>
  <files>.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/STATE.md` — the Phase 22 record on substituted checkpoint evidence, including 22-16's two BLOCKED verdicts held against three rounds of orchestrator-relayed pressure to record PASS. It governs how every row below is scored
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md` — the Round 1 row table and its four FAILs, § Findings, and the `## Expected read-back values (Round 2)` table Task 1 just wrote
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-07-PLAN.md` Task 2 — the row wording, house rules and recording format this round continues
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-07-SUMMARY.md` — how Round 1 actually ran, including the emulation cross-checks and the clamped-direction false negative on R6
    - `.planning/REQUIREMENTS.md` line 67 — the standing rule that no automated test discharges any TRN requirement
  </read_first>
  <action>
    Hold the blocking human browser checkpoint below, then record it. This task performs no code
    change of any kind. Present the 22-row agenda to the developer, collect a verdict and the row's
    own stated evidence for each row, write a `## Round 2 — checkpoint record` section into
    `23-VALIDATION.md` carrying every verdict verbatim, set that file's `status` and
    `nyquist_compliant` frontmatter from the result, and gate `REQUIREMENTS.md` strictly on the
    row-to-requirement map at the end of the instructions.
  </action>
  <what-built>
    Three gap-closure fixes on top of the shipped Phase 23 feature. Plan 23-08 nested the settle
    callbacks inside the plugin's `zoom` and `pan` option objects so a real gesture now runs
    `settle()`, and rerouted the four on-screen buttons through the unit-tested pure range math so
    `+` divides the span by 1.5 and `←` moves it by 25%. Plan 23-09 wrapped the year consistency
    heatmap in a contained horizontal-scroll container so its unavoidable 634px grid can no longer
    push the document wide at phone widths. Plan 23-10 made every Trends x-axis pick its label
    granularity from its own tick step, and attached an explicit ResizeObserver to every chart so a
    narrowing viewport re-fits the canvas.
  </what-built>
  <how-to-verify>
    House rules for this checkpoint, non-negotiable:

    1. Every row below is NON-WAIVABLE. Record PASS only when the row's own stated observation was
       made, at the row's own stated width, on the row's own stated control. Evidence gathered at a
       different width, on a different control, or summarised as "it works" does NOT discharge a row.
    2. Developer or orchestrator authority does NOT substitute for the stated observation. If the
       observation was not made, record BLOCKED, not PASS. `workflow.auto_advance` does not apply.
    3. Use the four-verdict vocabulary from Phase 20: PASS / FAIL / BLOCKED / NOT EXERCISABLE. Every
       verdict must be backed by its own stated observation.
    4. HOUSE RULE 4: do not patch anything found here. Record it verbatim as a finding for a later
       gap-closure round, per the house rule since checkpoint 16-09.
    5. All rows run against `http://127.0.0.1:8099/strava-widgets/` in a real browser, against the
       full 1,868-activity archive. No staged fixture is expected or permitted (D-25).
    6. GESTURE ROWS REQUIRE A REAL HUMAN GESTURE. A synthetic `WheelEvent` or `PointerEvent`
       dispatched from the console does not produce a gesture end and cannot discharge R22, R26, R31,
       R36 or R41's gesture half. State the trusted-event evidence the way Round 1 did — e.g. the
       count of `wheel` events observed with `isTrusted: true` and `metaKey: true`, or the measured
       pixel distance and `pointermove` count of the drag.
    7. WIDTH- AND HEIGHT-SPECIFIC ROWS must state THAT ROW'S OWN width or height and the method used
       to reach it. Under DevTools emulation `window.innerWidth` / `innerHeight` read STALE — Round 1
       recorded `matchMedia('(max-width: 430px)')` true at an `innerWidth` of 682. Use
       `document.documentElement.clientWidth` / `clientHeight` and `100dvh`, cross-checked against
       `matchMedia`, and say which you used.
    8. Quote actual observed strings and numbers — `aria-label` values in the order they were read,
       rendered tick labels, `scrollWidth`/`clientWidth` pairs. Never summarise.
    9. HOUSE RULE 14, NON-WAIVABLE: a row's literal Required detail cannot be substituted by developer
       or orchestrator authority. `23-VALIDATION.md` and `.planning/STATE.md` both record that this
       exact substitution is what reopened Phase 22's Gap 1 (CAL-02) and shipped its Gap 2 (CR-01) as
       a Critical, and that in plan 22-16 two BLOCKED verdicts had to be held against three rounds of
       orchestrator-relayed pressure to record PASS on incomplete evidence. This round re-asks the
       same overflow class of row at the same phone widths, so the rule is restated here in full
       rather than referenced.
    10. BUILD FRESHNESS IS NOT ASSUMED. Serving on `127.0.0.1` alone is NOT sufficient — this project
        has already been bitten in this phase by a stale server on port 8099 and by a staged tree
        symlinked into a merged worktree. R21 gates every other row, and any fixture or rebuild during
        the round requires a hard reload before the next observation.

    ---

    R21 — BUILD FRESHNESS (prerequisite for every other row). In DevTools, quote the
    `assets/index-*.js` filename the served page actually loaded, read back two ways: the module
    `<script src>` and the `performance` resource entry. It must match the filename Task 1 recorded
    from the fresh build AND must differ from Round 1's `assets/index-D2l-GZfl.js`. If it matches
    Round 1's, every row below is BLOCKED — the fixes are not in the artifact.

    R22 — TRN-01, real wheel zoom changes the rendered range (re-ask of R2). Volume tab, weekly
    granularity. Quote the first and last x-axis tick labels. Then hold ⌘/Ctrl and scroll over the
    plot area. Quote them again. They must differ. State the trusted-wheel-event evidence per rule 6.

    R23 — TRN-01/D-14, bare wheel still scrolls the page (re-ask of R3). With the cursor OVER a plot
    area, scroll without the modifier. State that the page scrolled (quote `scrollY` before and
    after) AND quote the first/last tick to show the chart range did not change.

    R24 — TRN-01/D-06, the opening picture (re-ask of R4, against the ROUND 2 expected table). On a
    fresh load of Volume at weekly, quote the canvas `aria-label` verbatim and the first and last
    x-axis tick labels. All three must match the Round 2 expected table. Note the tick strings are
    expected to have CHANGED shape since Round 1 (plan 23-10) — compare against the Round 2 table,
    not against Round 1's row.

    R25 — TRN-01/D-06, the other two granularities (re-ask of R5). Switch to Monthly, quote the
    `aria-label` and the first/last ticks. Switch to Yearly, quote the same three. All must match the
    Round 2 expected table.

    R26 — TRN-02/D-15, drag-to-pan and the cursor (re-ask of R6). Drag RIGHTWARD across the plot area
    — Round 1 recorded that a leftward drag from the D-06 default correctly moves nothing, because
    the later-dates direction is already clamped there and `Pan to later dates` reads
    `disabled: true`. Quote first/last tick before and after, state the measured drag distance and
    `pointermove` count, and state the observed cursor at rest and during the drag (expected `grab`
    then `grabbing`, read via `getComputedStyle`, not eyeballed).

    R27 — TRN-02/D-11, all four buttons with NO pointing device at all (re-ask of R7). Using only the
    keyboard, Tab to each of `←` `→` `−` `+` and activate with Enter or Space. For EACH OF THE FOUR
    INDIVIDUALLY, quote its `aria-label` and the canvas `aria-label` before and after its activation.
    A single combined "all four work" statement does not discharge this row.

    R28 — TRN-02, THE STEP MAGNITUDE (re-ask of R8, Finding 1). From a fresh Volume weekly load, each
    of the four sub-cases below is required individually, each returning to the default first:
    (a) press `←` once — the canvas `aria-label` must read the Round 2 table's after-`←` value
        (Round 1 observed `Weekly distance chart, Apr 2025 to Apr 2026`, a 4-month step, against the
        designed 3-month step);
    (b) from that state press `→` once — it must return exactly to the weekly default;
    (c) from the default press `+` once — the `aria-label` must read the Round 2 table's after-`+`
        value (Round 1 observed `Nov 2025 to May 2026`, a 6-month window, against the designed
        8-month window);
    (d) from the default press `−` once — the `aria-label` must read the Round 2 table's after-`−`
        value.
    Quote all four resulting `aria-label`s verbatim. A row that quotes fewer than four does not
    discharge this row.

    R29 — TRN-02/D-11, buttons disable at their clamps (re-ask of R9). (a) On a fresh weekly load,
    state whether `→` is disabled and whether `←` is enabled. (b) Press `−` until it stops changing
    the range; quote the resulting `aria-label` (expected: the Round 2 full-zoom-out row) and state
    the `disabled` / `aria-disabled` state of `−`, `←` and `→`. Also state how many `−` presses it
    took — Round 1 took 14 with the halving bug, and the count is expected to change now that the
    step is ÷1.5.

    R30 — TRN-02/D-11, Reset appears only when zoomed and restores the DEFAULT (re-ask of R10, with
    the gesture half Round 1 could not exercise). (a) Confirm Reset is absent on a fresh weekly load.
    (b) Press `+` once; confirm Reset appears. (c) Press Reset; quote the resulting `aria-label` — it
    must equal the Round 2 weekly DEFAULT, not the full-zoom-out row. (d) NEW AND REQUIRED: reload,
    then zoom with a real ⌘/Ctrl + wheel GESTURE rather than the button, and confirm Reset appears.
    Round 1 recorded that after a gesture zoom Reset never appeared at all; (d) is what proves
    Finding 10 is closed on the reveal path.

    R31 — TRN-02/D-13, the `aria-label` updates after BOTH a gesture and a button (re-ask of R11, the
    row Finding 10 blocked). All three sub-cases are required individually:
    (a) after a real ⌘/Ctrl + wheel zoom, quote the canvas `aria-label` verbatim AND quote the first
        and last rendered tick labels — the label must NAME THE SAME RANGE the ticks show. Round 1's
        failure was precisely a label reading `Weekly distance chart, Aug 2025 to Aug 2026` while the
        ticks read `11 Aug 2011` … `13 Aug 2026`;
    (b) after a real drag-pan, quote the `aria-label` and the first/last ticks the same way;
    (c) after a `+` button press, quote the `aria-label`.
    Quoting fewer than all three does not discharge this row.

    R32 — TRN-02/D-17, the modifier hint is visible (re-ask of R12). Quote the hint text as rendered,
    verbatim, and state which band header it appeared in.

    R33 — TRN-03/D-19, bands are taller and viewport-relative (re-ask of R13). Keep the window WIDER
    than 430px throughout so the phone override is never in play — that is R35's row. At EACH of the
    two viewport heights below, both required and each observed individually, report the authoritative
    viewport height per rule 7, report
    `getComputedStyle(document.querySelector('.chart-band__canvas-wrap--tall')).height`, and state how
    you reached that height.
    (a) viewport height 600 (±10): 34dvh ≈ 204px, inside the clamp, so the computed height must read
        ≈204px and must NOT read 420px.
    (b) viewport height 1400 (±20): 34dvh ≈ 476px, above the 420px ceiling, so the computed height
        must read exactly `420px`.
    Two nearby heights do not discharge this row. If (b) is unreachable, record it BLOCKED rather
    than substituting a shorter window.

    R34 — TRN-03/D-20, both Cadence & HR bands get the full height (re-ask of R14). On that tab,
    report the computed height of BOTH bands as two numbers, and state whether the tab scrolls.

    R35 — TRN-03/D-21, THE PHONE FLOOR AND NO HORIZONTAL OVERFLOW (re-ask of R15, Finding 9). At EACH
    of 390px, 393px, 412px and 430px — all four, each named individually, each with its own stated
    emulation method and its own `document.documentElement.clientWidth` read-back cross-checked
    against `matchMedia` — report ALL FIVE of:
    (a) the computed `.chart-band__canvas-wrap--tall` height (expected 240px at an 800px emulated
        height, inside `clamp(160px, 30dvh, 260px)`);
    (b) `document.documentElement.scrollWidth` and `document.documentElement.clientWidth` — these must
        now be EQUAL. Round 1 measured `scrollWidth` 682 against every one of the four widths;
    (c) `.year-heatmap` `scrollWidth` and `clientWidth` — the grid is EXPECTED to still measure ~634
        internally; that is not the defect and must not be read as one;
    (d) `.year-heatmap-scroll` `scrollWidth` and `clientWidth` — the wrapper's `clientWidth` must be
        within the viewport while its `scrollWidth` is ~634, which is what proves the overflow is
        CONTAINED and scrollable rather than deleted;
    (e) that the heatmap's rightmost weeks are actually reachable by scrolling the wrapper — state how
        you reached them.
    Naming a width other than the required one does not discharge the row. This is the exact
    substitution that reopened CAL-02 in Phase 22; see house rule 14.

    R36 — TRN-04/D-02, the Cadence & HR pair stays in lockstep (re-ask of R16, Finding 10/4). On that
    tab: (a) zoom using the button cluster, then quote BOTH canvases' `aria-label`s — they must name
    the SAME range. (b) REQUIRED AND SEPARATE: perform a real ⌘/Ctrl + wheel zoom over the HR (LOWER)
    band specifically, wait at least one second, then quote BOTH `aria-label`s again — they must name
    the same range. Round 1's developer observation was that a wheel zoom "zooms in/out only the chart
    we're on" and the other band "doesn't seem to catch up"; (b) is what proves that is closed.

    R37 — TRN-04/D-25, the canvas-lifecycle discriminator (re-ask of R17). (a) Zoom the Volume tab to
    a specific range and write the `aria-label` down; do the same on Training Load. (b) Cycle all five
    Trends tabs at least three full passes WITHOUT touching the granularity toggle, then return to
    both and quote both `aria-label`s again — both must still read the ranges from (a). (c) Toggle
    granularity weekly → monthly → weekly and quote the Volume `aria-label`: it must read the Round 2
    weekly DEFAULT (D-23). (d) Paste the browser console output for the whole sequence; it must
    contain no errors, and specifically no "Canvas is already in use". (e) Paste the output of
    `[...document.querySelectorAll('[role=tabpanel]')].map(p => p.id + ':' + p.querySelectorAll('canvas').length).join(', ')`
    and of `document.querySelectorAll('.chart-zoom-controls').length`. Expected per-panel counts:
    Volume 1, Year-over-Year 1, Cadence & HR 2, Training Load 1, Gear 1; cluster count exactly 1.
    This row now also carries the new ResizeObserver's teardown: a leaked observer calling `resize()`
    on a destroyed chart would surface here as a console error.

    R38 — TRN-01/D-03(b), decimation at deep zoom (re-ask of R18). On Training Load, zoom to roughly
    a two-week span. State whether the series resolves to near-daily detail, quote the `aria-label`
    at that zoom, and quote two adjacent tooltip readings so the day spacing is on the record.

    R39 — TRN-04/18-D15, thin-HR-coverage shading at every zoom level (re-ask of R19). On Training
    Load, zoom into a span with visible shading and state whether the shaded region still covers the
    same dates it did when zoomed out. Quote the `aria-label` at both zoom levels.

    R40 — TRN-04/D-22, zoom resets on a full remount (re-ask of R20). Zoom Volume to a stated range,
    navigate to `#/records` and back to `#/trends`, and quote the Volume `aria-label`. It must read
    the Round 2 weekly DEFAULT, not the range you left it at.

    R41 — NEW, Finding 7: x-axis tick labels adapt below month granularity. This row gates no TRN
    requirement; it is required for the clean sweep. All three sub-cases:
    (a) On Training Load, zoom to an ≈11-day window (quote the `aria-label` so the span is on the
        record) and quote EVERY rendered x-axis tick label in left-to-right order. Round 1 recorded
        all eight reading `Feb 2026`; they must now read distinct dates in `D MMM YYYY` form.
    (b) On Volume weekly at the D-06 default, quote the first and last tick — expected `MMM YYYY`
        form, matching the Round 2 table.
    (c) On Volume weekly at full zoom-out, quote the first and last tick — expected `YYYY` form,
        matching the Round 2 table.

    R42 — NEW, Finding 8: the canvas re-fits when the viewport NARROWS. This row gates no TRN
    requirement; it is required for the clean sweep. It MUST be a resize of an already-loaded page,
    NEVER a fresh load at the narrow width — Round 1 established that a fresh load at 500px sizes the
    canvas correctly at 370 × 223, so a fresh load would produce a false PASS.
    Load the Trends Volume tab at a 1200px-wide viewport. Record the canvas CSS width and height and
    the `.chart-band__canvas-wrap` width. WITHOUT reloading, narrow the viewport to 500px wide. Then
    report: the canvas CSS width and height, the wrapper width, and
    `document.documentElement.scrollWidth` against `clientWidth`. Round 1 measured the canvas stuck at
    770 × 206 in a 370px wrapper with `scrollWidth` 835 against `clientWidth` 500. The canvas width
    must now sit inside the wrapper, and `scrollWidth` must equal `clientWidth`. State explicitly that
    no reload occurred between the two readings.

    ---

    RECORDING. Write a `## Round 2 — checkpoint record` section into `23-VALIDATION.md` with one entry
    per row, each carrying: the verdict, the developer's own words, and the quoted values the row
    required. Add a tally line (PASS / FAIL / BLOCKED / NOT EXERCISABLE) and a requirement-gating
    table. Record any new defect verbatim as a numbered finding continuing from Finding 10, left
    unpatched.

    Then gate `REQUIREMENTS.md` strictly on this map:
    - TRN-01 needs R22, R23, R24, R25, R38 all PASS.
    - TRN-02 needs R26, R27, R28, R29, R30, R31, R32 all PASS.
    - TRN-03 needs R33, R34, R35 all PASS.
    - TRN-04 needs R36, R37, R39, R40 all PASS.
    R41 and R42 gate no requirement — they close recorded findings the user elected to include — but
    they DO count toward the clean sweep.

    A requirement with any mapped row not PASS stays Pending, and the specific blocking row is named
    in `REQUIREMENTS.md`. TRN-01 currently carries a tick from Round 1: if any of its Round 2 rows
    fails, REVERT it to Pending and name the row, following Phase 19's revert precedent and 22-16's.
    Set `23-VALIDATION.md`'s `nyquist_compliant` to `true` and `status` to `complete` only if EVERY
    row R21 through R42 PASSED; otherwise `status: partial` and `nyquist_compliant: false`.
  </how-to-verify>
  <resume-signal>Reply with a verdict and its stated evidence for each of R21 through R42, or type "approved" only if every one of the 22 rows was observed with its own stated proof.</resume-signal>
  <verify>
    <human-check>Each of R21 through R42 recorded with its own stated observation quoted verbatim, or recorded BLOCKED. No row marked PASS on a summary, on evidence from a different width, height or control, on a synthetic event where a real gesture is required, or on developer/orchestrator authority.</human-check>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md','utf8');const r=s.slice(s.indexOf('Round 2 — checkpoint record'));const missing=[];for(let i=21;i<=42;i++){if(!new RegExp('\\\\bR'+i+'\\\\b').test(r))missing.push('R'+i)}if(!/Round 2 — checkpoint record/.test(s))missing.push('Round 2 section');if(missing.length){console.error('rows never recorded: '+missing.join(','));process.exit(1)}console.log('all 22 Round 2 rows recorded')"</automated>
  </verify>
  <acceptance_criteria>
    - `23-VALIDATION.md` contains a `## Round 2 — checkpoint record` section with all 22 rows (R21-R42), each carrying a verdict from PASS / FAIL / BLOCKED / NOT EXERCISABLE.
    - R21 quotes the served `assets/index-*.js` filename and states explicitly whether it differs from `assets/index-D2l-GZfl.js`.
    - R28 gives four individual sub-case observations (a, b, c, d), each quoting a canvas `aria-label` verbatim.
    - R31 quotes all three sub-cases, and for (a) and (b) quotes BOTH the `aria-label` and the first/last tick labels so label-versus-render agreement is on the record.
    - R35 names all four widths (390, 393, 412, 430) individually and reports all five required measurements at each, including the `.year-heatmap` and `.year-heatmap-scroll` `scrollWidth`/`clientWidth` pairs.
    - R36 records the button case and the real-gesture-over-the-lower-band case separately, quoting both canvases' `aria-label`s in each.
    - R42 states explicitly that no reload occurred between the 1200px and 500px readings.
    - R22, R26, R30(d), R31(a), R31(b), R36(b) and R41(a) each state real-gesture evidence per house rule 6; none is discharged on a synthetic event.
    - `REQUIREMENTS.md` ticks a TRN requirement only when every row in its Round 2 map PASSED, names the blocking row for any that stays Pending, and reverts TRN-01 to Pending if any of its Round 2 rows failed.
    - `23-VALIDATION.md` frontmatter `nyquist_compliant` is `true` only when every one of R21-R42 PASSED.
    - Any defect found is recorded verbatim as a numbered finding continuing from Finding 10 and left unpatched.
    - `git status --porcelain src/` reports no modification — this task changes no source file.
  </acceptance_criteria>
  <done>Every Round 2 row carries a verdict backed by its own stated observation, and each of the four TRN requirements reflects exactly what its row map produced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built artifact → served page | The evidence in this round is only as good as the bytes the browser loaded; a stale server or a stale staged tree silently invalidates every row |
| human observation → recorded verdict | The checkpoint record is the only durable evidence that a rendering claim was ever tested; a summarised or substituted observation is indistinguishable from a real one once written down |

This plan changes no source file and adds no runtime surface. Its threats are evidentiary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-STALE-R2 | Spoofing | the staged build at `127.0.0.1:8099` | mitigate | Round 1 hit both known variants: a stale `http.server` from an unrelated session already bound to 8099, and a staged tree symlinked into a worktree that was later merged away. Task 1 checks and reports what is bound to the port before killing it, rebuilds after `rm -rf dist/widgets`, serves from the main checkout, and requires the entry hash to DIFFER from Round 1's `assets/index-D2l-GZfl.js`. R21 re-reads it live two ways from the browser and gates every other row on the match |
| T-23-EVIDENCE-R2 | Repudiation | the Round 2 checkpoint record | mitigate | Each of the 22 rows states its own required observation, in its own required units, at its own required width. House rules 1, 2, 8 and 14 forbid substitution, and the `<automated>` verify fails if any row number is absent from the record. Verdicts use the four-state vocabulary so a hardware or dataset limit is recorded as BLOCKED or NOT EXERCISABLE rather than laundered into a PASS |
| T-23-PRESSURE | Repudiation | orchestrator-relayed pressure on a BLOCKED verdict | mitigate | `.planning/STATE.md` records that plan 22-16's two BLOCKED verdicts had to be held against three separate rounds of orchestrator-relayed pressure to record PASS on incomplete evidence. House rule 14 is restated in this plan in full rather than referenced, and house rule 2 states that `workflow.auto_advance` does not apply to this checkpoint even though `config.json` sets `mode: yolo` |
| T-23-FALSEPASS-42 | Spoofing (of a test condition) | R42's resize row | mitigate | A fresh load at 500px already renders correctly today and would produce a false PASS for Finding 8. R42 requires an explicit statement that no reload occurred between the 1200px and 500px readings, and requires both readings to be quoted |
| T-23-LAZY-R2 | Information disclosure (bundle weight, not data) | entry chunk | mitigate | Plan 23-10 adds a new import to `trends-charts.ts`. `trends-tick-format.ts` is pure and imports nothing, so it cannot drag Chart.js or Hammer across the lazy boundary — but that is an argument, not a measurement. Task 1 re-runs the `grep -c "Hammer"` proof on the freshly built entry and trends chunks rather than carrying Round 1's result forward |
| T-23-SC-11 | Tampering (supply chain) | package manager | accept | This plan installs nothing and modifies no source. `package.json` and `package-lock.json` are not in `files_modified`. The Package Legitimacy Gate does not apply |
</threat_model>

<verification>
- Task 1's four gate commands (`npm test`, `npx tsc --noEmit`, `npm run build-widgets`, `npm run verify-dashboard`) all exit 0 in this session, with their real counts recorded.
- The served entry asset differs from `assets/index-D2l-GZfl.js`, confirmed by `curl` and again live in the browser at R21.
- The `<automated>` checks on both tasks: the verification map carries rows for all three fix plans and no longer mentions `panDeltaPx`; the Round 2 record carries all 22 row numbers.
- `git status --porcelain src/` is empty for both tasks.

The substance of this plan is not automatable by construction. `vitest.config.ts` runs
`environment: 'node'` with no jsdom, no headless browser and no canvas polyfill, so no command in
this repository can observe a rendered chart, a gesture, a computed height or a document scroll
width. That is the entire reason this checkpoint exists.
</verification>

<success_criteria>
- A `## Round 2 — build, staging and expected values` section records a green gate, an entry asset hash that differs from Round 1's, a re-proved lazy-chunk boundary, and an expected-value table recomputed from the current formulas against the live archive with every changed tick string annotated.
- The Per-Task Verification Map carries a row for every automated command plans 23-08, 23-09 and 23-10 added, and no stale `panDeltaPx` row survives.
- A `## Round 2 — checkpoint record` section records all 22 rows R21-R42 with verdicts backed by their own stated observations.
- `REQUIREMENTS.md` reflects each TRN requirement's Round 2 row map exactly, including reverting TRN-01 to Pending if any of its rows failed.
- `23-VALIDATION.md` frontmatter is `status: complete` / `nyquist_compliant: true` if and only if every one of R21-R42 PASSED.
- No source file is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/23-trends-zoom-pan-taller-bands/23-11-SUMMARY.md` when done.

Record in it: the tally, which of Round 1's four FAILs closed and which did not, the final state of
each TRN requirement with the row that decided it, and any new finding raised during the round with
its number continuing from Finding 10.
</output>
