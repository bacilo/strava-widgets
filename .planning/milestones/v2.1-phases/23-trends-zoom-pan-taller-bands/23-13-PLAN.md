---
phase: 23-trends-zoom-pan-taller-bands
plan: 13
type: execute
wave: 9
gap_closure: true
depends_on: ["23-12"]
files_modified:
  - .planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TRN-01, TRN-02, TRN-03, TRN-04]
closes_findings: [11]
closes_rows: [R35]

must_haves:
  decisions: [D-02, D-06, D-11, D-13, D-14, D-17, D-19, D-20, D-21, D-22, D-23, D-25]
  truths:
    - "R35's failing clause is re-asked in full, at all four phone widths, against a provably fresh build"
    - "The no-horizontal-overflow clause is scored on EQUALITY, never on improvement"
    - "TRN-01, TRN-02 and TRN-04 are confirmed unregressed by rows of their own, not assumed"
    - "Every row states which of the three evidence methods produced it — real trusted gesture, scripted click, or iframe-emulated viewport"
    - "Finding 12 has a stated disposition on the record and no Round 3 row of its own"
  artifacts:
    - path: ".planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md"
      provides: "A Round 3 build/staging/expected-values section and a Round 3 checkpoint record covering R43-R54"
      contains: "Round 3"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TRN-01..TRN-04 re-gated strictly on the Round 3 row map"
  key_links:
    - from: "Round 3 row verdicts"
      to: ".planning/REQUIREMENTS.md TRN-01..TRN-04"
      via: "the requirement-gating map at the end of Task 2"
      pattern: "TRN-0"
---

<objective>
Hold the BLOCKING Round 3 human browser checkpoint that R35's FAIL requires, against a provably
fresh build carrying plan 23-12, and gate TRN-03 — Phase 23's only open requirement — strictly on
the result.

Round 2 (plan 23-11, closed 2026-08-26/27 against `assets/index-D01ardNQ.js`) returned 21 PASS /
1 FAIL / 0 BLOCKED across R21-R42. TRN-01, TRN-02 and TRN-04 all tick Complete. TRN-03 stays
Pending, blocked solely by R35, whose sub-case (b) measured
`documentElement.scrollWidth/clientWidth` = `460/390`, `460/393`, `460/412`, `460/430` — not equal at
any width. Finding 11 located the sole remaining driver as the five-tab Trends navigation strip
(`div.segmented` carrying `role="tablist"`), 412px wide at `left: 48, right: 460`, with
`overflow-x: visible` on itself and its parent. Plan 23-12 contains it in a `.trends-tablist-scroll`
wrapper, the third instance of the shipped `.splits-scroll` pattern.

Nothing in 23-12 touches zoom, pan, tick formatting, chart lifecycle or the tooltip. But 23-12 DOES
change the DOM structure directly above the five tab buttons — the element the whole Trends view's
navigation hangs off — so TRN-01, TRN-02 and TRN-04 are re-asked here as REGRESSION CONFIRMATION,
with rows of their own, not assumed intact.

`nyquist_compliant: true` requires a clean sweep and is not reachable on a partial round.

Purpose: TRN-03 is Phase 23's only open requirement and only a real browser can discharge it. This
repo has no jsdom, no headless browser, no CSSOM and no canvas polyfill.

Output: a Round 3 build/staging/expected-values section, a Round 3 checkpoint record covering
R43-R54, and re-gated requirement states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-CONTEXT.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-11-PLAN.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-11-SUMMARY.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-12-SUMMARY.md
@.planning/phases/23-trends-zoom-pan-taller-bands/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the build is fresh and carries 23-12's bytes, re-affirm the expected read-back values, publish the predicted phone-width geometry, and extend the verification map</name>
  <files>.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md` — the whole file: § Test Infrastructure and its "Hard constraint on what automation can observe here" paragraph, § Per-Task Verification Map, § "Round 2 — build, staging and expected values" (all seven sub-sections, (a) through (g)), the Round 2 row table, and § Findings 11 and 12
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-11-PLAN.md` Task 1 — the exact port-clearing, gate, freshness, lazy-chunk and advance-computation discipline this task repeats
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-12-SUMMARY.md` — the automated commands 23-12 added, the `className = 'segmented'` before/after counts, and the grep string it names for proving the fix is in the served bytes
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-12-PLAN.md` — its `<objective>` "Expected post-fix geometry" paragraph, whose four predicted `clientWidth` values and their arithmetic this task must publish rather than re-derive from scratch
    - `src/dashboard/views/trends-zoom-logic.ts` and `src/dashboard/views/trends-tick-format.ts` — the CURRENT formulas the re-affirmation script must re-implement
    - `scripts/build-widgets.mjs` and `dist/widgets/index.html` — where the entry script and stylesheet filenames are read from
  </read_first>
  <action>
    Run this in order and record every output verbatim into `23-VALIDATION.md` under a new
    `## Round 3 — build, staging and expected values` section, placed above the Round 3 checkpoint
    record Task 2 will add.

    **(a) Clear the port before trusting anything.** Check whether a process is bound to port 8099
    and, if so, report its pid, its command line and its working directory BEFORE killing it. Round 1
    found a stale `http.server` from an unrelated session; Round 2 found this session's own server
    still bound, serving `dist/widgets` through a symlink and therefore serving stale bytes until a
    fresh build landed under it. Record what you found even if the answer is "nothing was bound", and
    confirm with `lsof -i :8099` that the port is free before continuing.

    **(b) Clean rebuild and full gate.** `rm -rf dist/widgets`, then `npm test`, `npx tsc --noEmit`,
    `npm run build-widgets`, `npm run verify-dashboard`. All four must exit 0. Record the test-file
    and test counts against Round 2's `55/55 files, 1348/1348 tests` baseline (23-12 adds tests, so
    the test count is expected to RISE — record the actual delta rather than asserting it), the tsc
    exit code, and `verify-dashboard`'s check count against its 37-check baseline.

    **(c) Prove the build is NEW, not merely rebuilt.** Read the module `<script src>` and the
    stylesheet `<link href>` out of `dist/widgets/index.html`. Record both filenames.
    - The stylesheet MUST differ from Round 2's `assets/index-Duibt5wO.css`. This is the sharper of
      the two checks for this round: 23-12's substance is a CSS rule, so an unchanged stylesheet hash
      means the fix is not in the artifact.
    - The entry script MUST differ from Round 2's `assets/index-D01ardNQ.js` and from Round 1's
      `assets/index-D2l-GZfl.js`.
    If either hash is unchanged, STOP and investigate before staging anything.

    **(d) Prove 23-12's own bytes are in the artifact, not just that the hash moved.** Grep the built
    assets for the literal string `trends-tablist-scroll`:
    - it must appear in exactly one `dist/widgets/assets/index-*.css` file — record the filename and
      the count;
    - it must appear in at least one `dist/widgets/assets/*.js` chunk — record which chunk and the
      count. `trends.ts` is behind the lazy route boundary, so expect it in a `trends-*` chunk rather
      than the entry chunk; record where it actually landed rather than where you expected it.
    A hash that moved without this string present would mean an unrelated change moved the hash.

    **(e) Re-prove the lazy chunk boundary (T-23-LAZY).** `grep -c "Hammer"` on the entry chunk must
    be `0`; on the newest `dist/widgets/assets/trends-charts-*.js` chunk it must be non-zero. Record
    both filenames and both counts, and restate the minifier caveat Round 2 recorded. 23-12 adds no
    import, so this is expected to hold — re-measure it rather than carrying Round 2's result
    forward.

    **(f) Re-affirm the expected read-back values (D-25).** Nothing in 23-12 touches the zoom, pan or
    tick-format math, so the Round 2 expected-value table should re-affirm unchanged — but that is an
    argument, not a measurement, and D-25 requires every expected value to be computed before a
    browser is opened.
    - First re-run the archive-unchanged check: last weekly bucket `2026-08-10T00:00:00.000Z`, last
      monthly `2026-08-01T00:00:00.000Z`, last yearly `2026-01-01T00:00:00.000Z`, last training-load
      day `2026-08-11`, last commit touching `data/stats/` = `fe53914`. Record any drift EXPLICITLY
      rather than absorbing it silently.
    - Then re-run the standalone Node recomputation script Round 2 described (re-implementing the
      CURRENT `computeArchiveBounds`, `computeFullRange`, `computeDefaultWindow`, `computeLimits`,
      `zoomStepRange`, `panStepRange`, `formatRangeLabel`, `tickGranularityForStep`,
      `formatTimeAxisTick` against the real `data/stats/*.json`), and compare its output row by row
      against Round 2's § (e) table.
    - Publish a `## Expected read-back values (Round 3)` section that states, per row, either
      "re-affirmed identical to Round 2" or the new value with the Round 2 value beside it. Do not
      copy Round 2's table forward without re-running the computation. Cover at minimum: Volume
      weekly default; Volume monthly default; Volume yearly default; Volume weekly after one `+`;
      Volume weekly after one `−`; Volume weekly after one `←`; Volume weekly at full zoom-out;
      Training Load 12mo default. Also re-affirm the `7 presses` zoom-out count R29 measured.

    **(g) Publish the PREDICTED phone-width geometry for R46, with its arithmetic.** This is the
    round's new expected-value table and must be written BEFORE any browser is opened, so R46 is
    scored against a prediction rather than rationalised after the fact. Publish, as a table keyed by
    the four widths 390 / 393 / 412 / 430 at `clientHeight` 800:

    | Measurement | 390 | 393 | 412 | 430 |
    |---|---|---|---|---|
    | `documentElement.scrollWidth` must EQUAL `clientWidth` | 390 | 393 | 412 | 430 |
    | `.trends-tablist-scroll` `clientWidth` (= viewport − 96) | 294 | 297 | 316 | 334 |
    | `.trends-tablist-scroll` `scrollWidth` | > `clientWidth`, ≈416-420 | ≈416-420 | ≈416-420 | ≈416-420 |
    | `[role=tablist]` rect width (min-content floor, NOT a defect) | ≈412 | ≈412 | ≈412 | ≈412 |
    | `.chart-band__canvas-wrap--tall` computed height | 240px | 240px | 240px | 240px |
    | `.year-heatmap` `scrollWidth`/`clientWidth` (expected, NOT a defect) | 634/286 | 634/289 | 634/308 | 634/326 |
    | `.year-heatmap-scroll` `scrollWidth`/`clientWidth` | 638/294 | 638/297 | 638/316 | 638/334 |

    State the arithmetic beside it: `.view` is declared once on `index.html`'s
    `<main id="app" class="view">` and again on `trends.ts`'s inner `view` div, each with
    `padding: var(--space-lg)` (24px), giving a `viewport − 96` content width — confirmed, not
    asserted, because R35(d) measured `.year-heatmap-scroll` `clientWidth` at exactly 294 / 297 / 316
    / 334. The `.trends-tablist-scroll` `scrollWidth` band is 412 (the strip's min-content floor)
    plus the wrapper's own 4px leading padding, plus the trailing 4px depending on the engine's
    end-padding treatment — so state it as a range and score R46(f) on `scrollWidth > clientWidth`,
    never on an exact number. The last three rows are carried from R35's own PASSing sub-cases and
    are expected UNCHANGED; a change in them is itself a finding.

    **(h) Extend the Per-Task Verification Map.** Add one row per automated command from
    `23-12-SUMMARY.md`, keyed `23-12/T1`, `23-12/T2`, `23-12/T3`, each with its Wave (8),
    Requirement, Threat Ref, Secure Behavior, Test Type, Automated Command, File Exists and Status —
    Status set from this session's actual run, never anticipated. Add a row for this plan's own
    Task 2 (`23-13/T2`), test type `manual-only (blocking checkpoint)`, status `⬜ pending`. Set
    `23-11/T2`'s Status from `⬜ pending` to reflect Round 2's actual closure.

    **(i) Stage the build.** Serve `dist/widgets` from the MAIN CHECKOUT (not a worktree) under a
    path shape matching production, so the URL is `http://127.0.0.1:8099/strava-widgets/`. Confirm
    with `curl -sI` that it returns 200; with
    `curl -s http://127.0.0.1:8099/strava-widgets/ | grep -o 'assets/index-[A-Za-z0-9_-]*\.js'` that
    the served entry filename matches step (c) verbatim; and with
    `curl -s http://127.0.0.1:8099/strava-widgets/assets/<the css filename from (c)> | grep -c "trends-tablist-scroll"`
    that the SERVED stylesheet — not just the built one — contains the new rule. Record all three.

    Change no source file in this task.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md','utf8');const need=['Round 3 — build, staging and expected values','Expected read-back values (Round 3)','23-12/T1','23-12/T2','23-12/T3','23-13/T2','trends-tablist-scroll'];const missing=need.filter(k=>!s.includes(k));if(s.includes('index-Duibt5wO.css')&&!/differs from Round 2/i.test(s))missing.push('Round 2 css hash mentioned without a stated comparison');if(missing.length){console.error('missing: '+missing.join(', '));process.exit(1)}console.log('round 3 build section complete')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx tsc --noEmit`, `npm run build-widgets` and `npm run verify-dashboard` were each run in this session and each exited 0, with their actual counts recorded and the test-count delta against Round 2's 1348 stated explicitly.
    - `23-VALIDATION.md` records an `assets/index-*.css` filename that is NOT `assets/index-Duibt5wO.css` and states plainly that it differs from Round 2's.
    - `23-VALIDATION.md` records an `assets/index-*.js` filename that is neither `assets/index-D01ardNQ.js` nor `assets/index-D2l-GZfl.js`.
    - `23-VALIDATION.md` records a non-zero `grep -c "trends-tablist-scroll"` count against a named built CSS asset AND against a named built JS chunk.
    - `23-VALIDATION.md` records the entry-chunk `Hammer` count as `0` and a non-zero count on a named `trends-charts-*.js` chunk.
    - An `## Expected read-back values (Round 3)` section exists covering at least the eight named states, each marked either re-affirmed-identical or carrying its new value beside the Round 2 value, and states the archive-unchanged result including any drift.
    - A predicted phone-width geometry table exists naming all four widths individually, with the `viewport − 96` arithmetic and its R35(d) confirmation stated beside it, and with the `.trends-tablist-scroll` `scrollWidth` expressed as a range scored on `> clientWidth`.
    - The Per-Task Verification Map contains rows keyed `23-12/T1`, `23-12/T2`, `23-12/T3` and `23-13/T2`.
    - `curl -sI http://127.0.0.1:8099/strava-widgets/` returned `200`; the served entry filename matches the built one verbatim; and the served stylesheet contains `trends-tablist-scroll` — all three recorded.
    - `git status --porcelain src/` reports no modification — this task changes no source file.
  </acceptance_criteria>
  <done>The gate is green, the served artifact is provably new AND provably carries 23-12's own bytes, the zoom/tick expected values are re-affirmed by re-running the computation rather than by argument, and R46 has a written prediction to be scored against.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Round 3 blocking human browser checkpoint (R43-R54), then record and re-gate</name>
  <files>.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/STATE.md` — the Phase 22 record on substituted checkpoint evidence, including 22-16's two BLOCKED verdicts held against three rounds of orchestrator-relayed pressure to record PASS. It governs how every row below is scored
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md` — the Round 2 row table (especially R35 in full), the Round 2 method-disclosure block, § Findings 11 and 12, and the `## Round 3` sections Task 1 just wrote
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-11-PLAN.md` Task 2 — the row wording, house rules and recording format this round continues
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-11-SUMMARY.md` — how Round 2 actually ran, including which rows used the iframe technique and which needed the human
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-12-PLAN.md` — the `<objective>`'s chosen-approach and rejected-alternatives reasoning, so a row that contradicts a stated prediction is recognised as a finding rather than absorbed
    - `.planning/phases/23-trends-zoom-pan-taller-bands/deferred-items.md` — Finding 12's disposition, which this round cites rather than re-litigates
    - `.planning/phases/23-trends-zoom-pan-taller-bands/23-RESEARCH.md` line 877 — the standing note that DevTools/iframe emulation is a PARTIAL fallback for D-21's phone-width claims and that the gap must be flagged in the checkpoint rows rather than silently substituted
    - `.planning/REQUIREMENTS.md` line 67 — the standing rule that no automated test discharges any TRN requirement
  </read_first>
  <action>
    Hold the blocking human browser checkpoint below, then record it. This task performs no code
    change of any kind. Present the 12-row agenda to the developer, collect a verdict and the row's
    own stated evidence for each row, write a `## Round 3 — checkpoint record` section into
    `23-VALIDATION.md` carrying every verdict verbatim plus a mandatory method-disclosure block, set
    that file's `status` and `nyquist_compliant` frontmatter from the result, and gate
    `REQUIREMENTS.md` strictly on the row-to-requirement map at the end of the instructions.
  </action>
  <what-built>
    One gap-closure fix on top of the Phase 23 feature Round 2 already validated. Plan 23-12 added
    `.trends-tablist-scroll`, a contained horizontal-scroll wrapper (the third instance of the
    shipped `.splits-scroll` pattern, after `.records-jump` and `.year-heatmap-scroll`), placed
    around — never inside — the five-tab Trends navigation strip, so that strip's 412px min-content
    floor can no longer push the document wide at phone widths. The tablist element itself is
    byte-unchanged: same `role="tablist"`, same `aria-label="Trends views"`, same `.segmented` class,
    same five buttons, same roving-tabindex keyboard model. `.segmented` and `.segmented__option` are
    untouched, so all six `.segmented` instances across the app render identically. Nothing in the
    change touches zoom, pan, tick formatting, chart lifecycle or the tooltip.
  </what-built>
  <how-to-verify>
    House rules for this checkpoint, non-negotiable:

    1. Every row below is NON-WAIVABLE. Record PASS only when the row's own stated observation was
       made, at the row's own stated width, on the row's own stated control. Evidence gathered at a
       different width, on a different control, or summarised as "it works" does NOT discharge a row.
    2. Developer or orchestrator authority does NOT substitute for the stated observation. If the
       observation was not made, record BLOCKED, not PASS. `workflow.auto_advance` does not apply to
       this checkpoint.
    3. Use the four-verdict vocabulary from Phase 20: PASS / FAIL / BLOCKED / NOT EXERCISABLE. BLOCKED
       and NOT EXERCISABLE are DISTINCT and neither is a PASS. Every verdict must be backed by its own
       stated observation.
    4. HOUSE RULE 4: do not patch anything found here. Record it verbatim as a numbered finding
       CONTINUING FROM FINDING 12 — so a new defect is Finding 13, the next Finding 14 — and leave it
       unpatched for a later plan. `git status --porcelain src/` must be empty at the end of this
       task.
    5. All rows run against `http://127.0.0.1:8099/strava-widgets/` in a real browser, against the
       full 1,868-activity archive. No staged fixture is expected or permitted (D-25).
    6. GESTURE ROWS REQUIRE A REAL HUMAN GESTURE. A synthetic `WheelEvent` or `PointerEvent`
       dispatched from the console does not produce a gesture end and cannot discharge R48, R50(a) or
       R51(b). Round 2 established that the browser-automation input channel could not deliver real
       pointer events to this page at all — a click on a button's exact coordinates produced zero
       click events — so these rows must be performed by the developer, with in-page
       `isTrusted`/`metaKey` counters quoted as proof (Round 2's shape: "19 wheel events, ALL
       `isTrusted: true`, ALL `metaKey: true`"; "135 `pointermove` events, all trusted, dx +255px").
    7. WIDTH- AND HEIGHT-SPECIFIC ROWS must state THAT ROW'S OWN width or height and the method used
       to reach it. Under emulation `window.innerWidth` / `innerHeight` read STALE — Round 1 recorded
       `matchMedia('(max-width: 430px)')` true at an `innerWidth` of 682. Use
       `document.documentElement.clientWidth` / `clientHeight`, cross-checked against `matchMedia`,
       and say which you used.
    8. Quote actual observed strings and numbers — `aria-label` values in the order they were read,
       rendered tick labels, `scrollWidth`/`clientWidth` pairs, bounding rects. Never summarise.
    9. HOUSE RULE 14, NON-WAIVABLE: a row's literal Required detail cannot be substituted by developer
       or orchestrator authority. `23-VALIDATION.md` and `.planning/STATE.md` both record that this
       exact substitution is what reopened Phase 22's Gap 1 (CAL-02) and shipped its Gap 2 (CR-01) as
       a Critical, and that in plan 22-16 two BLOCKED verdicts had to be held against three rounds of
       orchestrator-relayed pressure to record PASS on incomplete evidence. This round re-asks the
       same overflow class of row at the same phone widths for the THIRD time, so the rule is restated
       here in full rather than referenced.
    10. BUILD FRESHNESS IS NOT ASSUMED. Serving on `127.0.0.1` alone is NOT sufficient — this phase has
        already been bitten by a stale server on port 8099, by a staged tree symlinked into a merged
        worktree, and by a symlinked server serving pre-fix bytes. R43 gates every other row, and any
        fixture edit or rebuild during the round requires a HARD reload before the next observation.
    11. NO PARTIAL CREDIT, and this is the rule this round exists for. R46(b) requires
        `document.documentElement.scrollWidth` to EQUAL `document.documentElement.clientWidth`. Round 1
        measured 682; Round 2 measured 460 against clientWidths of 390/393/412/430 and was correctly
        recorded a FAIL, because "narrower" is not "closed". A Round 3 reading of, say, 415 against 390
        is a FAIL, not a partial PASS, and must be recorded as one. The clause is satisfied at a width
        only by equality at that width, and the row is satisfied only when all four widths are equal.
    12. METHOD DISCLOSURE IS MANDATORY. The record must carry a method-disclosure block, in the shape
        Round 2 used, stating for EVERY row which of three evidence channels produced it: (i) a real
        trusted human gesture with its `isTrusted`/`metaKey`/`pointermove` counters; (ii) a scripted
        `element.click()` or dispatched event (`isTrusted: false`); or (iii) an iframe-emulated
        viewport. A round without this block is incomplete regardless of its verdicts.

    ENVIRONMENT CONSTRAINTS, carried forward because they have already cost prior rounds real time:

    - **Chrome refuses to size a real window below 500px wide.** Rounds 1 and 2 measured the phone
      widths inside a SAME-ORIGIN IFRAME sized to the exact target width and height. Media queries and
      `dvh` resolve against the iframe viewport, so layout evaluation is faithful — but it is NOT a
      real device viewport: no mobile UA, no touch emulation, no browser chrome, and specifically not
      the `vh`-toolbar-collapse behaviour 23-RESEARCH.md flags as unreproducible under emulation. If
      Round 3 uses the same technique, say so per row and disclose it under rule 12.
    - **A real phone is stronger evidence and is OFFERED, NOT REQUIRED.** If the developer can serve
      the staged build to an actual phone at one of the four widths and read R46(b) there, record it
      as an additional confirming observation beside the iframe reading. Its absence is not a BLOCKED
      — the iframe reading remains the row's primary evidence, exactly as in Rounds 1 and 2.
    - **Console noise.** Round 2 captured 64 console messages that ALL originated from an unrelated
      third-party Chrome extension (`chrome-extension://ffpmdofklpmnbkgiocdcofknfjckhbjl`). A clean
      browser profile makes the console-clean assertion in R52(d) cleaner; if the extension is present
      again, state its origin explicitly rather than counting its messages against the app.
    - **Canvas-count caveat.** R52(e)'s `tabpanel-cadence-hr:2` reading only reproduces while Cadence
      & HR is the ACTIVE tab; read from Volume it returns `0`, because the pair unmounts when
      inactive. State which tab was active when the count was read.

    ---

    R43 — BUILD FRESHNESS (prerequisite for every other row). In DevTools, quote the
    `assets/index-*.js` filename AND the `assets/index-*.css` filename the served page actually
    loaded, each read back live two ways: from the module `<script src>` / stylesheet `<link href>`,
    and from the `performance` resource entries. Both must match what Task 1 recorded from the fresh
    build. The stylesheet MUST differ from Round 2's `assets/index-Duibt5wO.css` and the entry script
    MUST differ from Round 2's `assets/index-D01ardNQ.js`. Additionally, confirm live in the page that
    the rule shipped: quote the output of
    `getComputedStyle(document.querySelector('.trends-tablist-scroll')).overflowX` — it must read
    `auto`, and the element must exist. If the hashes match Round 2's, or the element is absent, every
    row below is BLOCKED — the fix is not in the artifact.

    R44 — TRN-03/D-19, bands are taller and viewport-relative (re-ask of R33). Keep the viewport WIDER
    than 430px throughout so the phone override is never in play — that is R46's row. At EACH of the
    two viewport heights below, both required and each observed individually, report the authoritative
    viewport height per rule 7, report
    `getComputedStyle(document.querySelector('.chart-band__canvas-wrap--tall')).height`, and state how
    you reached that height.
    (a) viewport height 600 (±10): 34dvh ≈ 204px, inside the clamp, so the computed height must read
        ≈204px and must NOT read 420px.
    (b) viewport height 1400 (±20): 34dvh ≈ 476px, above the 420px ceiling, so the computed height
        must read exactly `420px`.
    Two nearby heights do not discharge this row. If (b) is unreachable, record it BLOCKED rather than
    substituting a shorter window.

    R45 — TRN-03/D-20, both Cadence & HR bands get the full height (re-ask of R34). On that tab, report
    the computed height of BOTH bands as two numbers, state the authoritative viewport height they were
    read at, and state whether the tab scrolls vertically (quote
    `documentElement.scrollHeight/clientHeight`). Round 2 read `272px` and `272px` at 900×800 with
    `1105/800`.

    R46 — TRN-03/D-21, THE PHONE FLOOR AND NO HORIZONTAL OVERFLOW (re-ask of R35, Finding 11). THIS IS
    THE ROW THE ROUND EXISTS FOR. At EACH of 390px, 393px, 412px and 430px — all four, each named
    individually, each with its own stated emulation method and its own
    `document.documentElement.clientWidth` read-back cross-checked against
    `matchMedia('(max-width: 430px)')`, at `clientHeight` 800 — report ALL SEVEN of:
    (a) the computed `.chart-band__canvas-wrap--tall` height (expected `240px`, per Task 1's predicted
        table; Round 2 measured 240px at all four and PASSED);
    (b) `document.documentElement.scrollWidth` and `document.documentElement.clientWidth` — these must
        be EQUAL. Round 1 measured `scrollWidth` 682 at every width; Round 2 measured 460 at every
        width. See house rule 11: equality or FAIL, at every one of the four widths, no partial
        credit;
    (c) `.year-heatmap` `scrollWidth` and `clientWidth` — the grid is EXPECTED to still measure 634
        internally (Round 2: `634/286`, `634/289`, `634/308`, `634/326`); that is not the defect and
        must not be read as one. A CHANGE here is itself a finding;
    (d) `.year-heatmap-scroll` `scrollWidth` and `clientWidth` plus its computed `overflow-x` — Round 2
        measured `638/294`, `638/297`, `638/316`, `638/334` with `overflow-x: auto`. Unchanged values
        confirm 23-12 did not disturb 23-09's fix;
    (e) that the heatmap's rightmost weeks are actually reachable by scrolling ITS wrapper — state how
        you reached them;
    (f) NEW — `.trends-tablist-scroll` `scrollWidth`, `clientWidth` and computed `overflow-x`,
        alongside `document.querySelector('[role=tablist]').getBoundingClientRect()` (quote `left`,
        `right` and `width`). Task 1's predicted table gives `clientWidth` 294 / 297 / 316 / 334 and a
        `scrollWidth` of roughly 416-420; score this sub-case on `scrollWidth > clientWidth` and
        `overflow-x: auto`, NOT on an exact `scrollWidth`. The tablist's own width staying ≈412 is
        EXPECTED and is not a defect — that is its min-content floor, exactly as `.year-heatmap`'s 634
        is. What must have changed is that its overflow is now contained by the wrapper instead of
        reaching the document;
    (g) NEW — all five tabs are still REACHABLE at this width, in three parts, all required:
        (i) scroll the wrapper horizontally to bring the `Gear` tab fully into view; quote
            `document.getElementById('tab-gear').getBoundingClientRect()` before and after, and the
            wrapper's `scrollLeft` at both points;
        (ii) with the KEYBOARD ONLY and REAL TRUSTED INPUT (rule 6), focus the active tab and press
             ArrowRight four times; after each press quote `document.activeElement.textContent`, that
             element's `aria-selected`, and the wrapper's `scrollLeft`, so the scroll-into-view
             behaviour is on the record. All five tab labels (`Volume`, `Year-over-Year`,
             `Cadence & HR`, `Training Load`, `Gear`) must appear in that sequence;
        (iii) with the trailing tab focused, state whether its two-tone focus ring is visibly clipped
              by the wrapper's edge — quote the focused button's `getBoundingClientRect()` and the
              wrapper's, so the 4px `padding: var(--space-xs)` allowance can be checked against the
              numbers rather than eyeballed.
    Naming a width other than the required one does not discharge the row. Reporting fewer than all
    seven sub-cases does not discharge the row. This is the exact substitution that reopened CAL-02 in
    Phase 22; see house rule 14.
    IF (b) STILL FAILS AT ANY WIDTH: do not patch, and do not stop at "still 460". Record the failing
    numbers, then enumerate the remaining drivers for the next round by pasting the output of a
    console expression that lists every element whose `getBoundingClientRect().right` exceeds
    `document.documentElement.clientWidth` — with each one's tag, class list and rect — and record
    that list verbatim as Finding 13. Round 2's own Finding 11 was produced exactly this way, and it
    is what made this round narrow.

    R47 — TRN-01/D-06 REGRESSION, the opening picture on all three granularities (re-ask of R24 and
    R25). On a fresh load of Volume at weekly, quote the canvas `aria-label` verbatim and the first and
    last x-axis tick labels. Switch to Monthly and quote the same three. Switch to Yearly and quote the
    same three. All nine values must match Task 1's `## Expected read-back values (Round 3)` table.
    Quoting fewer than all three granularities does not discharge this row.

    R48 — TRN-01/D-14 REGRESSION, the wheel gestures still behave (re-ask of R22 and R23). Both halves
    required, both with real trusted input per rule 6:
    (a) Volume weekly. Quote the first and last x-axis tick labels; hold ⌘/Ctrl and scroll over the
        plot area; quote them again. They must differ. State the trusted-wheel-event evidence.
    (b) With the cursor OVER a plot area, scroll WITHOUT the modifier. Quote `scrollY` before and after
        (it must change) AND quote the first/last tick to show the chart range did NOT change.

    R49 — TRN-02/D-11 REGRESSION, the four buttons and their step magnitudes (re-ask of R27 and R28).
    This row matters more than usual for this round, because 23-12 changes the DOM immediately above
    the tab strip and the Tab order runs through it. Both halves required:
    (a) Using ONLY the keyboard, quote the observed Tab order from the theme toggle through to the last
        zoom control (Round 2 observed `Theme: dark` → `Volume` → `Weekly` → `Monthly` → `Yearly` →
        `Pan to earlier dates` → `Pan to later dates` → `Zoom out` → `Zoom in`). A NEW Tab stop
        appearing before or after the tab strip is a defect — the wrapper must contribute none — and
        must be recorded as a finding.
    (b) Still keyboard-only, activate each of `←` `→` `−` `+` INDIVIDUALLY with Enter, and for each of
        the four quote its own `aria-label` and the canvas `aria-label` before and after. From the
        weekly default the four results must match the Round 3 expected table (Round 2's values:
        `←` → `Weekly distance chart, May 2025 to May 2026`; `→` back to
        `Weekly distance chart, Aug 2025 to Aug 2026`; `+` → `Weekly distance chart, Oct 2025 to Jun 2026`;
        `−` → `Weekly distance chart, Feb 2025 to Aug 2026`). A single combined "all four work"
        statement does not discharge this row.

    R50 — TRN-02/D-13/D-17 REGRESSION, the label agrees with the render, and the hint is visible
    (re-ask of R31 and R32). All three required:
    (a) after a real trusted ⌘/Ctrl + wheel zoom, quote the canvas `aria-label` verbatim AND the first
        and last rendered tick labels — the label must NAME THE SAME RANGE the ticks show;
    (b) after a `+` button press, quote the `aria-label`;
    (c) quote the modifier hint text as rendered, verbatim, and state which band header it appeared in
        (Round 2: `⌘ + scroll to zoom · drag or pinch to pan`, in the "Distance" band header).

    R51 — TRN-04/D-02 REGRESSION, the Cadence & HR pair stays in lockstep (re-ask of R36). Both halves
    required and separate: (a) zoom using the button cluster, then quote BOTH canvases' `aria-label`s —
    they must name the SAME range. (b) perform a REAL trusted ⌘/Ctrl + wheel zoom over the HR (LOWER)
    band specifically, wait at least one second, then quote BOTH `aria-label`s again — they must name
    the same range.

    R52 — TRN-04/D-25 REGRESSION, the canvas-lifecycle discriminator (re-ask of R37). This is the
    highest-value regression row for 23-12, because a mis-wired wrapper would surface as broken tab
    switching. (a) Zoom the Volume tab to a specific range and write the `aria-label` down; do the same
    on Training Load. (b) Cycle all five Trends tabs at least three full passes WITHOUT touching the
    granularity toggle — using the tab buttons, at a width where the strip does NOT scroll — then
    return to both and quote both `aria-label`s again; both must still read the ranges from (a).
    (c) Toggle granularity weekly → monthly → weekly and quote the Volume `aria-label`: it must read
    the Round 3 weekly DEFAULT (D-23). (d) Paste the browser console output for the whole sequence; it
    must contain no app-origin errors, and specifically no "Canvas is already in use"; state the origin
    of any extension-sourced messages rather than counting them against the app. (e) Paste the output
    of
    `[...document.querySelectorAll('[role=tabpanel]')].map(p => p.id + ':' + p.querySelectorAll('canvas').length).join(', ')`
    and of `document.querySelectorAll('.chart-zoom-controls').length`, AND state which tab was active
    when you read them. Expected with Cadence & HR active: Volume 1, Year-over-Year 1, Cadence & HR 2,
    Training Load 1, Gear 1; cluster count exactly 1.

    R53 — TRN-04/D-22 REGRESSION, zoom resets on a full remount (re-ask of R40). Zoom Volume to a
    stated range, navigate to `#/records` and back to `#/trends`, and quote the Volume `aria-label`. It
    must read the Round 3 weekly DEFAULT, not the range you left it at.

    R54 — NEW, the tab strip is unchanged where there was no defect. This row gates no TRN requirement;
    it is required for the clean sweep. At a viewport of at least 1000px wide, report all five of:
    (a) `document.querySelectorAll('.trends-tablist-scroll').length` — must be exactly `1`. More than
        one would mean the wrapper leaked onto another `.segmented` instance;
    (b) `.trends-tablist-scroll` `scrollWidth` and `clientWidth` — at this width they must be EQUAL, so
        no scrollbar is introduced where the strip already fits;
    (c) the five tabs render on ONE row with the joined-pill silhouette: quote each tab button's
        `getBoundingClientRect()` `top` (all five must be equal, proving no wrap) and state whether any
        internal join shows a gap or a rounded notch — the CR-02 defect class 19-REVIEW recorded;
    (d) the 3-button granularity `.segmented` (weekly/monthly/yearly) is untouched: quote its
        `getBoundingClientRect()` width and confirm it is NOT inside a `.trends-tablist-scroll`
        (`document.querySelector('.trends-tablist-scroll').contains(<that element>)` must be `false`);
    (e) the active tab's two-tone focus ring is fully visible when focused via keyboard at this width —
        state the observation, and state whether the 4px wrapper padding is visible as extra space
        above and below the strip compared to your memory of the previous build (a small, expected,
        deliberate visual delta from `padding: var(--space-xs)`, recorded here so it is not later
        mistaken for a regression).

    ---

    RECORDING. Write a `## Round 3 — checkpoint record` section into `23-VALIDATION.md` with:
    - a header stating the date, the served entry AND stylesheet asset filenames, and the URL;
    - a MANDATORY method-disclosure block per house rule 12, grouping the rows by evidence channel
      (real trusted human gesture / scripted `element.click()` with `isTrusted: false` / iframe-emulated
      viewport), in the shape Round 2's block used;
    - one entry per row R43 through R54, each carrying the verdict, the developer's own words, and the
      quoted values the row required;
    - a tally line (PASS / FAIL / BLOCKED / NOT EXERCISABLE);
    - a requirement-gating table;
    - a one-line statement that Finding 12 remains DEFERRED with its disposition recorded in
      `deferred-items.md` (dated 2026-08-27, plan 23-12), that this round deliberately adds no row for
      it, and that it therefore does not count against the sweep;
    - any new defect recorded verbatim as a numbered finding continuing from Finding 12, left unpatched.

    Then gate `REQUIREMENTS.md` strictly on this map:
    - **TRN-03 needs R44, R45 and R46 all PASS.** This is the requirement the round exists to close.
      R46 is non-waivable and is scored on equality at all four widths per house rule 11.
    - **TRN-01 regression-confirmed by R47 and R48.**
    - **TRN-02 regression-confirmed by R49 and R50.**
    - **TRN-04 regression-confirmed by R51, R52 and R53.**
    - R54 gates no requirement but counts toward the clean sweep.

    State plainly in the record that TRN-01, TRN-02 and TRN-04's ticks rest on Round 2's FULL row maps
    (R22-R25/R38, R26-R32, R36/R37/R39/R40) and that Round 3's rows for them are REGRESSION
    CONFIRMATION over an already-ticked requirement, not a fresh full discharge. Do not silently
    re-describe them as freshly discharged.

    A requirement with any mapped row not PASS stays Pending — or REVERTS to Pending if it currently
    ticks — with the specific blocking row named in `REQUIREMENTS.md`, following Phase 19's revert
    precedent and 22-16's. Set `23-VALIDATION.md`'s `nyquist_compliant` to `true` and `status` to
    `complete` only if EVERY row R43 through R54 PASSED; otherwise `status: partial` and
    `nyquist_compliant: false`.
  </how-to-verify>
  <resume-signal>Reply with a verdict and its stated evidence for each of R43 through R54, or type "approved" only if every one of the 12 rows was observed with its own stated proof — including all seven of R46's sub-cases at all four of 390/393/412/430.</resume-signal>
  <verify>
    <human-check>Each of R43 through R54 recorded with its own stated observation quoted verbatim, or recorded BLOCKED / NOT EXERCISABLE. No row marked PASS on a summary, on evidence from a different width, height or control, on a synthetic event where a real gesture is required, or on developer/orchestrator authority. R46(b) marked PASS only on `scrollWidth === clientWidth` at every one of the four widths.</human-check>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md','utf8');const i=s.indexOf('Round 3 — checkpoint record');if(i<0){console.error('Round 3 record section absent');process.exit(1)}const r=s.slice(i);const missing=[];for(let n=43;n<=54;n++){if(!new RegExp('\\\\bR'+n+'\\\\b').test(r))missing.push('R'+n)}for(const w of ['390','393','412','430']){if(!r.includes(w))missing.push('width '+w)}if(!/[Mm]ethod disclosure/.test(r))missing.push('method disclosure block');if(!/Finding 12/.test(r))missing.push('Finding 12 disposition statement');if(missing.length){console.error('missing from the Round 3 record: '+missing.join(', '));process.exit(1)}console.log('all 12 Round 3 rows, all four widths, method disclosure and the Finding 12 statement are recorded')"</automated>
  </verify>
  <acceptance_criteria>
    - `23-VALIDATION.md` contains a `## Round 3 — checkpoint record` section with all 12 rows (R43-R54), each carrying a verdict from PASS / FAIL / BLOCKED / NOT EXERCISABLE.
    - The record contains a method-disclosure block naming, for every row, which of the three evidence channels produced it.
    - R43 quotes BOTH the served `assets/index-*.js` and `assets/index-*.css` filenames, states explicitly whether each differs from Round 2's `assets/index-D01ardNQ.js` / `assets/index-Duibt5wO.css`, and quotes the live `getComputedStyle(...).overflowX` of `.trends-tablist-scroll`.
    - R46 names all four widths (390, 393, 412, 430) individually and reports all seven required sub-cases at each, including the `.trends-tablist-scroll` and `[role=tablist]` measurements and the three reachability parts of (g).
    - R46(b) is recorded PASS only where `document.documentElement.scrollWidth` EQUALS `clientWidth` at that width; any non-equal reading is recorded FAIL with both numbers quoted, and the overflow-driver enumeration is recorded as Finding 13.
    - R46(g)(ii) states real trusted keyboard evidence and quotes all five tab labels in the observed arrow-key sequence with each one's `aria-selected` and the wrapper's `scrollLeft`.
    - R49(a) quotes the observed Tab order and states explicitly whether the wrapper introduced a new Tab stop.
    - R52(e) states which tab was active when the per-panel canvas counts were read.
    - R48, R50(a) and R51(b) each state real-gesture evidence per house rule 6; none is discharged on a synthetic event.
    - R54 reports all five sub-cases, including `document.querySelectorAll('.trends-tablist-scroll').length === 1` and the granularity strip's non-containment check.
    - The record states that TRN-01/TRN-02/TRN-04's Round 3 rows are regression confirmation over Round 2's full row maps, not a fresh discharge.
    - The record states Finding 12's DEFERRED disposition and cites `deferred-items.md`.
    - `REQUIREMENTS.md` ticks TRN-03 only when R44, R45 and R46 ALL passed, names the blocking row otherwise, and reverts any currently-ticked TRN requirement to Pending if one of its Round 3 regression rows failed.
    - `23-VALIDATION.md` frontmatter `nyquist_compliant` is `true` only when every one of R43-R54 PASSED.
    - Any defect found is recorded verbatim as a numbered finding continuing from Finding 12 and left unpatched.
    - `git status --porcelain src/` reports no modification — this task changes no source file.
  </acceptance_criteria>
  <done>Every Round 3 row carries a verdict backed by its own stated observation and its own disclosed evidence method, and TRN-03 reflects exactly what R44/R45/R46 produced — closed on equality at all four phone widths, or still Pending with the blocking row and the next round's overflow-driver list already on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built artifact → served page | The evidence in this round is only as good as the bytes the browser loaded; a stale server or a stale staged tree silently invalidates every row. This phase has already been bitten three times |
| human observation → recorded verdict | The checkpoint record is the only durable evidence that a rendering claim was ever tested; a summarised or substituted observation is indistinguishable from a real one once written down |
| emulated viewport → phone-width claim | The four phone widths are measured inside an iframe, not on a device. A claim written as if it were a device measurement would overstate the evidence |

This plan changes no source file and adds no runtime surface. Its threats are evidentiary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-STALE-R3 | Spoofing | the staged build at `127.0.0.1:8099` | mitigate | All three known variants have already occurred in this phase: a stale `http.server` from an unrelated session, a staged tree symlinked into a since-merged worktree, and this session's own symlinked server serving pre-fix bytes. Task 1 reports what is bound to the port before killing it, rebuilds after `rm -rf dist/widgets`, serves from the main checkout, requires BOTH the stylesheet and entry hashes to differ from Round 2's, and additionally greps the built AND served CSS for `trends-tablist-scroll`. R43 re-reads both hashes live two ways from the browser and also reads the rule's computed `overflow-x` off the live element, gating every other row |
| T-23-HASHMOVE | Spoofing (of a freshness proof) | the asset-hash check itself | mitigate | A moved hash proves only that SOMETHING changed, not that this round's fix shipped. Task 1 § (d) therefore greps the literal `trends-tablist-scroll` string out of the built CSS asset and a named JS chunk, and § (i) greps it out of the SERVED stylesheet over HTTP. R43 confirms it a third way, from the rendered page's own computed style |
| T-23-EVIDENCE-R3 | Repudiation | the Round 3 checkpoint record | mitigate | Each of the 12 rows states its own required observation, in its own required units, at its own required width. House rules 1, 2, 8 and 14 forbid substitution; house rule 12 requires a per-row method disclosure so an iframe reading can never be mistaken for a device reading; and the `<automated>` verify fails if any row number, any of the four widths, the disclosure block, or the Finding 12 statement is absent from the record |
| T-23-PARTIAL-R3 | Repudiation (of a failing measurement) | R46(b) | mitigate | The specific failure mode this round guards against is scoring an improvement as a pass. 682 → 460 was already recorded a FAIL for exactly this reason. House rule 11 is stated as its own rule, R46(b) restates the equality requirement inline, and the acceptance criteria bind PASS to equality at every one of the four widths |
| T-23-PRESSURE-R3 | Repudiation | orchestrator-relayed pressure on a BLOCKED or FAIL verdict | mitigate | `.planning/STATE.md` records that plan 22-16's two BLOCKED verdicts had to be held against three separate rounds of orchestrator-relayed pressure to record PASS on incomplete evidence. House rule 14 is restated in this plan in full rather than referenced, and house rule 2 states that `workflow.auto_advance` does not apply to this checkpoint even though `config.json` sets `mode: yolo` |
| T-23-A11Y-SILENT | Tampering (of the accessibility tree) | tablist ↔ tab ownership after 23-12's wrapper | mitigate | A severed tablist/tab relation is invisible to every automated check in this repo. R46(g)(ii) exercises the roving-focus arrow-key path with real trusted keyboard input and requires all five labels with their `aria-selected` values quoted; R49(a) requires the Tab order to be quoted so a new Tab stop from the wrapper is caught; R54(c) requires the five tabs' rect `top` values to be equal so a silent wrap is caught |
| T-23-LAZY-R3 | Information disclosure (bundle weight, not data) | entry chunk | mitigate | 23-12 adds no import, so the boundary should hold — but that is an argument, not a measurement. Task 1 § (e) re-runs the `grep -c "Hammer"` proof on the freshly built entry and trends chunks rather than carrying Round 2's result forward |
| T-23-SC-13 | Tampering (supply chain) | package manager | accept | This plan installs nothing and modifies no source. `package.json` and `package-lock.json` are not in `files_modified`. The Package Legitimacy Gate does not apply |
</threat_model>

<verification>
- Task 1's four gate commands (`npm test`, `npx tsc --noEmit`, `npm run build-widgets`, `npm run verify-dashboard`) all exit 0 in this session, with their real counts recorded and the test-count delta against Round 2's 1348 stated.
- The served stylesheet differs from `assets/index-Duibt5wO.css` and the served entry differs from `assets/index-D01ardNQ.js`, each confirmed by `curl` and again live in the browser at R43.
- The literal string `trends-tablist-scroll` is present in the built CSS asset, in a named built JS chunk, and in the SERVED stylesheet over HTTP.
- The `<automated>` checks on both tasks: the verification map carries rows for 23-12 and for this plan's own Task 2; the Round 3 record carries all 12 row numbers, all four phone widths, the method-disclosure block and the Finding 12 statement.
- `git status --porcelain src/` is empty for both tasks.

The substance of this plan is not automatable by construction. `vitest.config.ts` runs
`environment: 'node'` with no jsdom, no headless browser, no CSSOM and no canvas polyfill, so no
command in this repository can observe a rendered chart, a gesture, a computed height, a bounding
rect or a document scroll width. That is the entire reason this checkpoint exists, and it is why
23-12's green test suite cannot tick TRN-03 on its own.
</verification>

<success_criteria>
- A `## Round 3 — build, staging and expected values` section records a green gate, a stylesheet hash that differs from Round 2's, a re-proved lazy-chunk boundary, the `trends-tablist-scroll` string proven present in the built and served bytes, a re-affirmed (re-computed, not copied) expected-value table, and a predicted phone-width geometry table with its arithmetic.
- The Per-Task Verification Map carries a row for every automated command plan 23-12 added, plus this plan's own Task 2.
- A `## Round 3 — checkpoint record` section records all 12 rows R43-R54 with verdicts backed by their own stated observations, plus the mandatory method-disclosure block.
- R46 is reported at all four phone widths individually with all seven sub-cases, and is scored on equality rather than improvement.
- `REQUIREMENTS.md` reflects each TRN requirement's Round 3 row map exactly, describes TRN-01/02/04's rows as regression confirmation rather than fresh discharge, and reverts any requirement whose regression row failed.
- Finding 12's DEFERRED disposition is stated on the record and cited to `deferred-items.md`.
- `23-VALIDATION.md` frontmatter is `status: complete` / `nyquist_compliant: true` if and only if every one of R43-R54 PASSED.
- No source file is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/23-trends-zoom-pan-taller-bands/23-13-SUMMARY.md` when done.

Set `requirements-completed` to list only what this round's own rendered evidence actually
discharges — TRN-03 if and only if R44, R45 and R46 all PASSED. If TRN-01, TRN-02 and TRN-04 are
recorded as regression-confirmed by their Round 3 rows, list them and say in the same line that they
were confirmed unregressed, not freshly discharged.

Record in it: the tally; whether R35's clause is finally closed and at which of the four widths;
the final state of each TRN requirement with the row that decided it; whether Phase 23's gate is now
closed; and any new finding raised during the round with its number continuing from Finding 12.
</output>
