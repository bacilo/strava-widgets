---
phase: 24-local-curation-mode
plan: 16
type: execute
wave: 9
depends_on: ["24-14"]
files_modified:
  - src/dashboard/views/detail-best-efforts-logic.ts
  - src/dashboard/views/detail-best-efforts-logic.test.ts
  - src/dashboard/curation-seam.test.ts
autonomous: true
gap_closure: true
closes_gaps: [GAP-24-05]
closes_findings: [WR-17]
requirements: [CUR-01]

must_haves:
  decisions: [D-03, D-05, D-06, D-07, D-11]
  truths:
    - "There is exactly ONE definition of 'is this effort excluded right now' in detail-best-efforts-logic.ts — an exported resolveExcluded helper — and both buildPrBadgeLabels and buildBestEffortsPanelRows call it rather than repeating the ternary"
    - "isExcluded( appears exactly once in detail-best-efforts-logic.ts, inside that single helper"
    - "A source-structure guard pins detail.ts's buildPrBadgeLabels call site to the literal buildPrBadgeLabels(bestEffortsEntry, liveExclusions), not to any-two-arguments"
    - "A source-structure guard pins that both call sites in mountBestEffortsAndBadges receive the SAME final-argument identifier, so buildPrBadgeLabels(entry, null) alongside a correctly-wired panel call fails the suite"
    - "A behavioural guard asserts the PR set from buildPrBadgeLabels equals the isPr set from buildBestEffortsPanelRows across every combination of wasPRAtTheTime, excludedFromRecords and live-index state"
    - "The WR-17 divergence mutation (buildPrBadgeLabels(bestEffortsEntry, null)) is observed RED against the new guards and observed to pass tsc --noEmit unchanged — proving the type checker cannot catch it (D-11)"
    - "Every pre-existing GAP-24-01 and WR-05 test in detail-best-efforts-logic.test.ts stays green with no edit to its expectations, including the R19 mirror-image case"
  artifacts:
    - path: "src/dashboard/views/detail-best-efforts-logic.ts"
      provides: "The single exported resolveExcluded definition both derivations call"
      contains: "export function resolveExcluded"
    - path: "src/dashboard/curation-seam.test.ts"
      provides: "WR-17 structural pins on both call sites and their shared liveExclusions binding"
      contains: "buildPrBadgeLabels(bestEffortsEntry, liveExclusions)"
    - path: "src/dashboard/views/detail-best-efforts-logic.test.ts"
      provides: "resolveExcluded unit cases plus the exhaustive header-vs-panel non-divergence table"
      contains: "resolveExcluded"
  key_links:
    - from: "buildPrBadgeLabels and buildBestEffortsPanelRows"
      to: "one shared resolveExcluded definition"
      via: "a single call each, replacing two verbatim copies of the same ternary"
      pattern: "resolveExcluded\\("
    - from: "detail.ts's mountBestEffortsAndBadges"
      to: "both derivations receiving the same liveExclusions binding"
      via: "curation-seam.test.ts's WR-17 identifier-equality pin"
      pattern: "buildPrBadgeLabels\\(bestEffortsEntry, liveExclusions\\)"
---

<objective>
Close **WR-17** (`24-REVIEW.md` § Wave 7 Review, line 1152; item 3 of the amended **GAP-24-05** in
`24-VALIDATION.md`): nothing structurally pins `buildPrBadgeLabels`'s call site, nor that it
receives the same `liveExclusions` binding as the panel.

Purpose: plan 24-13's fix is correct today, but its correctness rests on two verbatim copies of the
same four-line ternary staying verbatim — and copy-paste is the mechanism by which they diverge.
The original WR-05 defect was precisely one of these two sites reading a different source. The test
layer does not close the gap either: `curation-seam.test.ts:129` pins only
`buildBestEffortsPanelRows`'s arity, through a regex
(`/buildBestEffortsPanelRows\([^)]*,[^)]*,[^)]*\)/`) that accepts literally any three arguments.
There is no equivalent pin for `buildPrBadgeLabels`'s call site and nothing at all asserting the
same value is handed to both. `tsc` cannot help: a future `buildPrBadgeLabels(bestEffortsEntry, null)`
alongside a correct `buildBestEffortsPanelRows(bestEffortsEntry, ageGrading, liveExclusions)` type-checks
cleanly and silently reinstates the exact header-vs-panel contradiction Round 2's R15 quoted
(`PRExcluded — {reason}` in one cell). And per GAP-24-05, no checkpoint row driven through the
curate UI's own Save→Recompute→Untick sequence can observe that reinstatement — so the code and its
tests are the only available evidence, and today they cover the fixed behaviour without covering
the divergence mode.

Output: one exported `resolveExcluded` helper both derivations call, a behavioural non-divergence
table, and source-structure pins on both call sites and their shared binding — with the divergence
mutation observed RED first, and observed to pass `tsc` unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-local-curation-mode/24-CONTEXT.md
@.planning/phases/24-local-curation-mode/24-VALIDATION.md
@src/dashboard/views/detail-best-efforts-logic.ts
@src/dashboard/curation-seam.test.ts
</context>

<interfaces>
<!-- Extracted from live source 2026-09-02. The executor needs no codebase exploration for these. -->

`src/dashboard/views/detail-best-efforts-logic.ts` current exports:

```ts
export const DISTANCE_DISPLAY_NAMES: Record<TargetDistanceKey, string>;

export function buildPrBadgeLabels(
  entry: ActivityBestEfforts | null,
  liveExclusions: ExclusionIndex | null
): string[];

export interface BestEffortPanelRow {
  distance: TargetDistanceKey;
  display: string;
  durationSec: number;
  paceSecPerKm: number;
  isPr: boolean;
  lowConfidence: boolean;
  excluded: boolean;
  agePercent: number | null;
  ageDerived: boolean;
}

export function buildBestEffortsPanelRows(
  entry: ActivityBestEfforts | null,
  ageGrading: AgeGradingDocument | null,
  liveExclusions: ExclusionIndex | null
): BestEffortPanelRow[];
```

The duplicated four lines (present verbatim at **both** call sites, `:65-68` inside
`buildPrBadgeLabels` and `:152-155` inside `buildBestEffortsPanelRows`):

```ts
const excluded =
  liveExclusions !== null
    ? isExcluded(liveExclusions, entry.activityId, distance)
    : effort.excludedFromRecords;
```

Consumers of that local `excluded` value, which must not change behaviour:
- `buildPrBadgeLabels`: `if (excluded) continue;` — then pushes `PR — ${DISTANCE_DISPLAY_NAMES[distance]}`.
- `buildBestEffortsPanelRows`: `isPr: effort.wasPRAtTheTime && !excluded` and `excluded,` on the row.

From `src/analytics/best-effort-exclusions.ts`:

```ts
export function isExcluded(index: ExclusionIndex, activityId: string, distance: TargetDistanceKey): boolean;
// entry missing -> false; entry === 'all' -> true; otherwise entry.has(distance)
export type ExclusionIndex = Map<string, 'all' | Set<TargetDistanceKey>>;
```

From `src/dashboard/views/detail.ts` (`mountBestEffortsAndBadges`, lines 533-554) — the seam this
plan pins, quoted here so the executor does not have to guess the identifier names:

```ts
const [bestEffortsEntry, ageGrading, liveExclusionState] = await Promise.all([...]);
const exclusionReason = liveExclusionState.reason;
const liveExclusions = liveExclusionState.index;
for (const label of buildPrBadgeLabels(bestEffortsEntry, liveExclusions)) { appendBadge(...); }
const rows = buildBestEffortsPanelRows(bestEffortsEntry, ageGrading, liveExclusions);
```

From `src/dashboard/curation-seam.test.ts` — the idioms to reuse, not reinvent:
- `stripComments(...)` imported from `./row-semantics.test.js`; module-level `detailStripped`,
  `detailBestEffortsLogicStripped` constants already exist.
- `countOccurrences(haystack, needle)` helper (line 39).
- The function-body slice idiom used at line 73-76:
  `const fnStart = detailStripped.indexOf('async function mountBestEffortsAndBadges'); const fnBody = detailStripped.slice(fnStart);`
</interfaces>

<house_rules>
Non-waivable for this plan:

1. **Behaviour must not change.** This is a refactor plus guards. Every existing case in
   `detail-best-efforts-logic.test.ts` — including `buildPrBadgeLabels`'s and
   `buildBestEffortsPanelRows`'s own describes, the `GAP-24-01` describe (line 194) and the `WR-05`
   describe (line 264, which contains the `R19 mirror-image` case round 4 surfaced) — must stay
   green with **no edit to any existing expectation**. If an existing test needs editing to pass,
   the refactor is wrong; fix the refactor.
2. **D-11 — observe RED first.** A guard that would stay green against the defect it names is worse
   than no guard. The WR-17 divergence mutation must be applied to a working copy, the new guards
   observed to FAIL against it, and the failure output recorded verbatim, before the mutation is
   reverted.
3. **`liveExclusions` stays REQUIRED.** No default value, no `?` optional marker, on either
   function. That is plan 24-13's forgotten-call-site discipline and the reason `tsc` at least
   catches a *missing* argument even though it cannot catch a *wrong* one.
4. **`liveExclusions === null` means UNKNOWN, never NOT-EXCLUDED.** The fallback to
   `effort.excludedFromRecords` is load-bearing (D-07's Save-vs-Recompute divergence window) and
   must survive the extraction unchanged.
5. **Do not widen scope.** WR-15 and IN-13 are out of scope for this round. Do not touch
   `detail.ts`, `detail-sections.ts` or any other production file — this plan's `files_modified` is
   exactly three files, one of them production.
</house_rules>

<tasks>

<task type="auto" id="T1" tdd="true">
  <name>Task 1: Extract resolveExcluded as the single definition, and pin non-divergence behaviourally</name>
  <files>src/dashboard/views/detail-best-efforts-logic.ts, src/dashboard/views/detail-best-efforts-logic.test.ts</files>
  <read_first>
    - `src/dashboard/views/detail-best-efforts-logic.ts` in full — both docblocks (the `buildPrBadgeLabels` one at lines ~25-51 and the `buildBestEffortsPanelRows` one at ~88-133). The shape-sameness justification that WR-17 rejects lives in the `buildPrBadgeLabels` docblock only (~37-40); the panel docblock carries no such claim but does carry its own illustrative `isExcluded(...)` snippet (~103). Both snippets are in scope for task 1(c) — read both before editing rather than assuming symmetric wording.
    - `src/dashboard/views/detail-best-efforts-logic.test.ts` in full — the `activity(...)` and `effort(...)` fixture builders, and the four describes at lines 32, 83, 194 and 264. The `R19 mirror-image` case at line 286 is the one round-4 re-verification surfaced; it must survive untouched.
    - `src/analytics/best-effort-exclusions.ts` `isExcluded` (line 70) and the `ExclusionIndex` type — the three-branch semantics (`missing → false`, `'all' → true`, `Set.has(distance)`) the helper wraps.
    - `.planning/phases/24-local-curation-mode/24-REVIEW.md` § `#### WR-17` (line 1152) — the reviewer's stated fix, including the exact proposed `resolveExcluded` signature this task implements.
    - `.planning/phases/24-local-curation-mode/24-CONTEXT.md` D-05 (line 120), D-06 (line 131) and D-07 (line 142) — the per-distance read tolerance, the one-reason-per-activity contract, and the instant-mirror/deliberate-recompute split that makes the live document authoritative for badge state.
  </read_first>
  <behavior>
    New unit cases for `resolveExcluded(liveExclusions, activityId, distance, effort)`:

    - `resolveExcluded(null, 'a1', '5k', { excludedFromRecords: true })` → `true` — UNKNOWN falls back to the precomputed flag.
    - `resolveExcluded(null, 'a1', '5k', { excludedFromRecords: false })` → `false`.
    - `resolveExcluded(new Map(), 'a1', '5k', { excludedFromRecords: true })` → `false` — a LOADED-and-empty live index overrides a stale-true precomputed flag. This is the R19/R26 mirror direction at the helper level.
    - `resolveExcluded(new Map([['a1', 'all']]), 'a1', '5k', { excludedFromRecords: false })` → `true`.
    - `resolveExcluded(new Map([['a1', new Set(['5k'])]]), 'a1', '10k', { excludedFromRecords: false })` → `false` — D-05's per-distance read tolerance survives.
    - `resolveExcluded(new Map([['a2', 'all']]), 'a1', '5k', { excludedFromRecords: false })` → `false` — the index is keyed on the passed activityId, not hardcoded.

    New exhaustive non-divergence table — for a single-effort entry at distance `5k`, across all
    **12** combinations of `wasPRAtTheTime ∈ {true, false}` × `excludedFromRecords ∈ {true, false}`
    × `liveExclusions ∈ {null, new Map(), new Map([['a1','all']])}`:

    - the set of distances implied by `buildPrBadgeLabels(entry, live)` (parsed back from each
      `PR — {Display}` label via `DISTANCE_DISPLAY_NAMES`) EQUALS the set of distances where
      `buildBestEffortsPanelRows(entry, null, live)` yields `isPr === true`.
    - the assertion is made per combination with the combination named in the failure message, so a
      divergence identifies which of the 12 states broke.
    - additionally: for every combination, `rows[0].excluded === resolveExcluded(live, 'a1', '5k', effortInput)`
      — the row's own reported `excluded` field is the shared helper's answer, not a re-derivation.

    Unchanged: every existing expectation in this file.
  </behavior>
  <action>
    (a) In `src/dashboard/views/detail-best-efforts-logic.ts`, add an exported helper immediately
    above `buildPrBadgeLabels`, with exactly this signature (quoted from `24-REVIEW.md` § WR-17's
    stated fix):

    `export function resolveExcluded(liveExclusions: ExclusionIndex | null, activityId: string, distance: TargetDistanceKey, effort: { excludedFromRecords: boolean }): boolean`

    Its body is the ternary that currently exists in duplicate: when `liveExclusions !== null`,
    return `isExcluded(liveExclusions, activityId, distance)`; otherwise return
    `effort.excludedFromRecords`. Give it a docblock stating it is THE single definition of "is this
    effort excluded right now", that `null` means UNKNOWN and never NOT-EXCLUDED, and that it exists
    because WR-17 found two verbatim copies whose sameness was asserted by comment rather than by
    construction. The `effort` parameter is typed structurally (`{ excludedFromRecords: boolean }`)
    rather than as `BestEffort` so the helper stays callable from a test with a minimal literal.

    (b) Replace BOTH copies of the ternary — the one inside `buildPrBadgeLabels`'s loop and the one
    inside `buildBestEffortsPanelRows`'s loop — with
    `const excluded = resolveExcluded(liveExclusions, entry.activityId, distance, effort);`. Change
    nothing else in either loop: `if (excluded) continue;` stays, `isPr: effort.wasPRAtTheTime && !excluded`
    stays, `excluded,` on the row stays, and `wasPRAtTheTime`'s early `continue` in
    `buildPrBadgeLabels` stays exactly where it is (it is what GAP-24-05's row analysis depends on).

    (c) Amend both docblocks in place. They do NOT carry identical wording — check each separately:

      - `buildPrBadgeLabels`'s docblock (lines ~25-51) justifies non-divergence by shape sameness
        ("the exact same ternary shape `buildBestEffortsPanelRows` uses for its `excluded` field, so
        the two derivations cannot diverge", lines ~37-40) AND contains an illustrative
        `isExcluded(liveExclusions, entry.activityId, distance)` reference at line ~37.
      - `buildBestEffortsPanelRows`'s docblock (lines ~88-133) has NO "cannot diverge" claim, but it
        does contain its own separate illustrative `isExcluded(liveExclusions, ...)` reference at
        line ~103.

    Replace the shape-sameness justification with the structural one: both call the single exported
    `resolveExcluded`, so there is one definition rather than two copies, and `curation-seam.test.ts`'s
    WR-17 pins assert that `detail.ts` hands both functions the same `liveExclusions` binding.

    **Both illustrative `isExcluded(...)` snippets must be replaced with `resolveExcluded(...)` —
    not just the one in the `buildPrBadgeLabels` docblock.** After this task, the only surviving
    `isExcluded(` occurrence in the file is the single call inside `resolveExcluded`'s body. Leaving
    the panel docblock's snippet in place leaves 2 occurrences and fails this task's own
    `grep -c 'isExcluded(' … returns 1` acceptance criterion.

    Do not delete the surrounding D-05/D-06/D-07/WR-05 rationale or the BADGE-STATE-only caveat
    about Records rankings — this is an amendment, not a rewrite.

    (d) Add the `resolveExcluded` unit cases and the 12-combination non-divergence table from
    `<behavior>` to `src/dashboard/views/detail-best-efforts-logic.test.ts`, as a new
    `describe('WR-17 — one definition of excluded, and the two derivations cannot diverge', …)`
    block. Import `resolveExcluded` alongside the existing imports. Do not edit any existing
    expectation in the file.

    (e) **Observe RED before wiring the fix in, per D-11.** Run the new describe block against the
    PRE-extraction module by temporarily giving `resolveExcluded` a deliberately wrong body — return
    `effort.excludedFromRecords` unconditionally, ignoring `liveExclusions` — and record verbatim
    which of the new cases fail (expect at least the loaded-and-empty-index case and several of the
    12 table rows). Then restore the correct body. This proves the new table is load-bearing rather
    than tautological. Record the mutation, the failure output, and the revert in the SUMMARY.

    (f) Run `npx vitest run src/dashboard/views/detail-best-efforts-logic.test.ts` (green, with the
    pre-existing cases including `R19 mirror-image` still passing), then `npx tsc --noEmit` and
    `npm test`; all exit 0.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/detail-best-efforts-logic.test.ts && npx tsc --noEmit && grep -c 'export function resolveExcluded' src/dashboard/views/detail-best-efforts-logic.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function resolveExcluded' src/dashboard/views/detail-best-efforts-logic.ts` returns `1`.
    - `grep -c 'isExcluded(' src/dashboard/views/detail-best-efforts-logic.ts` returns `1` — the only remaining call is inside `resolveExcluded` (count taken on the raw file; the import line reads `import { isExcluded }` without a following paren and does not match).
    - `grep -c 'resolveExcluded(' src/dashboard/views/detail-best-efforts-logic.ts` returns `3` — one declaration plus exactly two call sites.
    - `grep -c 'liveExclusions !== null' src/dashboard/views/detail-best-efforts-logic.ts` returns `1` — the ternary exists in exactly one place.
    - `grep -c 'liveExclusions: ExclusionIndex | null' src/dashboard/views/detail-best-efforts-logic.ts` returns `3` (both public functions plus the helper), and `grep -c 'liveExclusions?:' src/dashboard/views/detail-best-efforts-logic.ts` returns `0` — the parameter is still required.
    - `grep -c 'isPr: effort.wasPRAtTheTime && !excluded' src/dashboard/views/detail-best-efforts-logic.ts` returns `1`, and `grep -c 'if (!effort.wasPRAtTheTime) continue;' src/dashboard/views/detail-best-efforts-logic.ts` returns `1` — neither consumer of `excluded` changed.
    - `git diff src/dashboard/views/detail-best-efforts-logic.test.ts` contains no deletion of an existing `expect(` line — only additions.
    - `npx vitest run src/dashboard/views/detail-best-efforts-logic.test.ts` exits 0, and the run includes a passing test whose name contains `R19 mirror-image`.
    - The SUMMARY records the verbatim failure output from the step-(e) wrong-body mutation and confirms the revert.
    - `npx tsc --noEmit` and `npm test` both exit 0.
  </acceptance_criteria>
  <done>One exported `resolveExcluded` is the sole definition of exclusion state, both derivations call it, and a 12-combination table proves the header PR set and the panel `isPr` set cannot disagree.</done>
</task>

<task type="auto" id="T2">
  <name>Task 2: Pin both call sites and their shared liveExclusions binding, observed red against the WR-17 divergence mutation</name>
  <files>src/dashboard/curation-seam.test.ts</files>
  <read_first>
    - `src/dashboard/curation-seam.test.ts` in full — the file docblock's explicit disclaimer that a green run is SOURCE TEXT SHAPE coverage only (this task's additions inherit that limitation and must not overstate), the `countOccurrences` helper, the `stripComments`-derived module constants, the `fnStart`/`fnBody` slice idiom at lines 73-76, and the `GAP-24-01` describe at line 121 whose line-129 any-three-arguments regex this task tightens.
    - `src/dashboard/views/detail.ts` lines 525-560 — `mountBestEffortsAndBadges`'s body, so the pinned literals match the shipped identifiers exactly (`bestEffortsEntry`, `ageGrading`, `liveExclusions`).
    - `.planning/phases/24-local-curation-mode/24-REVIEW.md` § `#### WR-17` — specifically the reviewer's stated seam assertion (`expect(detailStripped).toContain('buildPrBadgeLabels(bestEffortsEntry, liveExclusions)')`) and the claim that `buildPrBadgeLabels(bestEffortsEntry, null)` type-checks cleanly, which step (d) below verifies rather than assumes.
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` § `#### GAP-24-05 — AMENDED 2026-09-02` item 3 — the statement that no checkpoint row can observe this divergence, which is why these source-structure pins are the only available evidence.
  </read_first>
  <action>
    Add a new `describe('WR-17 — both derivations are pinned to the same liveExclusions binding', …)`
    block to `src/dashboard/curation-seam.test.ts`. Leave the existing `GAP-24-01` describe and its
    line-129 regex in place — the new assertions are strictly additive and stricter.

    (a) `it('detail.ts contains exactly one buildPrBadgeLabels( call site, in the literal two-argument form')`:
    `expect(countOccurrences(detailStripped, 'buildPrBadgeLabels(')).toBe(1)` and
    `expect(detailStripped).toContain('buildPrBadgeLabels(bestEffortsEntry, liveExclusions)')`.

    (b) `it('detail.ts pins buildBestEffortsPanelRows to the literal three-argument form, not merely to three arguments')`:
    `expect(detailStripped).toContain('buildBestEffortsPanelRows(bestEffortsEntry, ageGrading, liveExclusions)')`.

    (c) `it('both call sites inside mountBestEffortsAndBadges receive the SAME final-argument identifier')`:
    slice `mountBestEffortsAndBadges`'s body with the file's existing `fnStart`/`fnBody` idiom, then
    capture the final argument of each call with an anchored regex — for `buildPrBadgeLabels` capture
    group 2 of `/buildPrBadgeLabels\(\s*([A-Za-z_$][\w$]*)\s*,\s*([A-Za-z_$][\w$]*)\s*\)/`, and for
    `buildBestEffortsPanelRows` capture group 3 of
    `/buildBestEffortsPanelRows\(\s*([A-Za-z_$][\w$]*)\s*,\s*([A-Za-z_$][\w$]*)\s*,\s*([A-Za-z_$][\w$]*)\s*\)/`.
    Assert both matches are non-null and the two captured identifiers are strictly equal, with a
    failure message naming both. Because the capture class is an IDENTIFIER, a literal `null`,
    `undefined` or `new Map()` in either position produces a null match and fails the test — which
    is exactly the WR-17 mutation. Also assert both captured first arguments are equal, so the two
    functions cannot be handed different best-efforts entries either.

    (d) `it('detail-best-efforts-logic.ts has one definition of exclusion state, called from both derivations')`:
    on `detailBestEffortsLogicStripped`, assert `countOccurrences(..., 'resolveExcluded(') === 3`,
    `countOccurrences(..., 'isExcluded(') === 1`, and `countOccurrences(..., 'liveExclusions !== null') === 1`.
    Then slice each function body — `indexOf('export function buildPrBadgeLabels')` to
    `indexOf('export interface BestEffortPanelRow')`, and `indexOf('export function buildBestEffortsPanelRows')`
    to end — and assert each slice contains `resolveExcluded(`. Include a comment in the test naming
    WR-17 and stating that this pins the single-definition property the two docblocks now claim.

    (e) `it('neither function makes liveExclusions optional or defaulted')`:
    `countOccurrences(detailBestEffortsLogicStripped, 'liveExclusions?:') === 0` and
    `countOccurrences(detailBestEffortsLogicStripped, 'liveExclusions: ExclusionIndex | null =') === 0`.

    (f) **Observe RED, and prove `tsc` cannot (D-11).** Copy `src/dashboard/views/detail.ts` to
    `$SCRATCH/detail.ts.orig` (a directory OUTSIDE the repository) and install a shell
    `trap 'cp "$SCRATCH/detail.ts.orig" src/dashboard/views/detail.ts' EXIT INT TERM`. Then apply the
    WR-17 mutation in place: change `buildPrBadgeLabels(bestEffortsEntry, liveExclusions)` to
    `buildPrBadgeLabels(bestEffortsEntry, null)`, leaving the panel call untouched. Record, verbatim:
      1. `npx tsc --noEmit` → **exit 0** (this is WR-17's central claim: the type checker is blind to it);
      2. `npx vitest run src/dashboard/curation-seam.test.ts` → **non-zero exit**, with assertions
         (a) and (c) both failing, quoted verbatim;
      3. `npx vitest run src/dashboard/views/detail-best-efforts-logic.test.ts` → still **exit 0**,
         because the mutation lives in `detail.ts`, which that file does not read. Record this
         explicitly: it is why the seam pins are needed in addition to Task 1's behavioural table.
    Then restore `detail.ts` from the scratch copy and confirm with
    `git status --porcelain src/dashboard/views/detail.ts` → no output, and `cmp` against the scratch
    copy → byte-identical.

    (g) Re-run `npx vitest run src/dashboard/curation-seam.test.ts` (green), then `npm test`,
    `npx tsc --noEmit` and `npm run build`; all exit 0.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/curation-seam.test.ts && git status --porcelain src/dashboard/views/detail.ts | grep -c . ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "buildPrBadgeLabels(bestEffortsEntry, liveExclusions)" src/dashboard/curation-seam.test.ts` returns at least `1`.
    - `grep -c "buildBestEffortsPanelRows(bestEffortsEntry, ageGrading, liveExclusions)" src/dashboard/curation-seam.test.ts` returns at least `1`.
    - `npx vitest run src/dashboard/curation-seam.test.ts` exits 0 and reports a `WR-17` describe block with 5 passing tests.
    - The SUMMARY quotes the step-(f) `npx tsc --noEmit` result against the mutated `detail.ts` as **exit 0**, and states in words that this confirms WR-17's claim that the type checker cannot catch the divergence.
    - The SUMMARY quotes the verbatim failing assertions from `curation-seam.test.ts` under the mutation, naming at least the exactly-one-call-site pin and the same-identifier pin.
    - The SUMMARY records that `detail-best-efforts-logic.test.ts` stayed green under the mutation, and why that matters.
    - `git status --porcelain src/dashboard/views/detail.ts` produces no output — the mutation was reverted; `find . -name 'detail.ts.orig' -not -path './node_modules/*'` returns nothing.
    - `git status --porcelain` lists only `src/dashboard/views/detail-best-efforts-logic.ts`, `src/dashboard/views/detail-best-efforts-logic.test.ts` and `src/dashboard/curation-seam.test.ts` as changed by this plan.
    - `npm test`, `npx tsc --noEmit` and `npm run build` all exit 0.
  </acceptance_criteria>
  <done>`buildPrBadgeLabels(bestEffortsEntry, null)` now fails the suite where it previously type-checked and shipped, and the failure was observed rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/best-effort-exclusions.json` (live, developer-authored) → the rendered detail view | The live document is the authority for BADGE STATE. It is fetched at render time and can legitimately disagree with the precomputed `best-efforts.json` until a Recompute runs (D-07). A derivation that reads the wrong document renders a false claim about the developer's curation intent. |
| the precomputed `excludedFromRecords` flag → PR claims on screen | Stale-true and stale-false are both reachable states. Which document wins is a correctness decision, not a style preference. |
| two independent derivations → one rendered `<td>` | `buildPrFlagsCell` (`detail-sections.ts:339-353`) paints `isPr` and `excluded` into the SAME cell, so a disagreement between the two derivations is not merely inconsistent — it renders the self-contradicting `PRExcluded — {reason}` string Round 2's R15 quoted. |
| a future edit to `detail.ts` → the shipped bundle | `tsc` accepts `buildPrBadgeLabels(entry, null)`, so the type system is not a boundary guard here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-16-01 | Tampering | A future edit passing a different exclusion source to `buildPrBadgeLabels` than to `buildBestEffortsPanelRows` in `detail.ts:550/554`, silently reinstating WR-05 | mitigate | `curation-seam.test.ts`'s WR-17 pin captures the final-argument IDENTIFIER of both calls and asserts equality; a literal (`null`, `undefined`, `new Map()`) fails the identifier regex outright. Observed red against the exact `buildPrBadgeLabels(bestEffortsEntry, null)` mutation in Task 2(f). |
| T-24-16-02 | Tampering | The two copies of the exclusion ternary drifting apart under future maintenance — the copy-paste mechanism WR-17 names | mitigate | The ternary is deleted from both call sites and exists once, in the exported `resolveExcluded`. Pinned by `resolveExcluded(` occurring exactly 3 times, `isExcluded(` exactly once, and `liveExclusions !== null` exactly once. |
| T-24-16-03 | Repudiation | A green suite being read as proof of rendered behaviour, when `curation-seam.test.ts` covers SOURCE TEXT SHAPE only under `environment: 'node'` with no DOM | accept | The file's existing docblock already states this limitation explicitly and the new describe inherits it; the plan requires the SUMMARY to record that `detail-best-efforts-logic.test.ts` stayed GREEN under the mutation, making the structural-vs-behavioural split visible. Rendered proof is plan 24-17's browser row, not this plan's. |
| T-24-16-04 | Information Disclosure | Rendering a `PR` claim for a run the developer has excluded — a false public statement about the archive, and the exact contradiction R15 recorded | mitigate | The 12-combination non-divergence table asserts the header PR set equals the panel `isPr` set in every reachable state, including the stale-true precomputed / empty-live-index mirror. |
| T-24-16-05 | Elevation of Privilege | Treating `liveExclusions === null` (fetch failed / parse failed) as NOT-EXCLUDED, which would silently clear a real exclusion badge | mitigate | `resolveExcluded`'s `null` branch returns `effort.excludedFromRecords`, preserved verbatim from both originals; two unit cases pin it, and an acceptance criterion asserts the parameter is neither optional nor defaulted. |
| T-24-16-06 | Tampering | Task 2's `detail.ts` mutation being left in the working tree, shipping the very defect this plan closes | mitigate | A `trap … EXIT INT TERM` restores the scratch copy on every exit path; acceptance criteria require `git status --porcelain src/dashboard/views/detail.ts` empty and a byte-identical `cmp`, plus no `detail.ts.orig` anywhere in the repo. |
| T-24-16-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. `git diff --name-only` must not list `package.json` or `package-lock.json`. |
</threat_model>

<verification>
- `npx vitest run src/dashboard/views/detail-best-efforts-logic.test.ts` and `npx vitest run src/dashboard/curation-seam.test.ts` both exit 0.
- `npm test`, `npx tsc --noEmit` and `npm run build` all exit 0.
- `grep -c 'liveExclusions !== null' src/dashboard/views/detail-best-efforts-logic.ts` returns 1; `grep -c 'resolveExcluded(' ...` returns 3.
- `git status --porcelain src/dashboard/views/detail.ts` produces no output.
- `git diff --stat` lists exactly the three files in `files_modified`.
</verification>

<success_criteria>
- The exclusion derivation has ONE definition, called from both places, replacing two verbatim copies whose sameness was previously asserted only by comment (WR-17).
- `buildPrBadgeLabels(bestEffortsEntry, null)` fails `curation-seam.test.ts` while still passing `npx tsc --noEmit` — both observed and quoted, confirming the review's claim rather than restating it (D-11).
- A 12-combination behavioural table proves the header PR set and the panel `isPr` set agree in every reachable state, including the stale-true-precomputed / loaded-and-empty-live mirror direction.
- Every pre-existing GAP-24-01 and WR-05 expectation, including the `R19 mirror-image` case, is green with no edit.
- `liveExclusions` remains a REQUIRED parameter on both public functions, and `null` still means UNKNOWN (D-07).
</success_criteria>

<output>
Create `.planning/phases/24-local-curation-mode/24-16-SUMMARY.md` when done. It MUST carry: the
verbatim failure output from Task 1(e)'s wrong-body mutation and its revert; the three verbatim
results from Task 2(f) — `tsc --noEmit` exit 0 under the mutation, `curation-seam.test.ts`
non-zero with the failing assertions quoted, and `detail-best-efforts-logic.test.ts` still exit 0 —
with a statement in words of why the third result is the reason the seam pins are needed; and
confirmation that no existing expectation in `detail-best-efforts-logic.test.ts` was edited.
</output>
