---
phase: 21-overview-rebuild
plan: 07
type: execute
wave: 4
depends_on: ["21-01", "21-02", "21-03", "21-04", "21-05", "21-06"]
files_modified:
  - .planning/phases/21-overview-rebuild/21-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [OVR-01, OVR-02, OVR-03, OVR-04, FIX-01]
must_haves:
  decisions: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-11, D-13, D-14, D-15, D-16]
  truths:
    - "D-16: the ended-streak branch is observed against a staged-build fixture, because the live archive's streak is active and the branch is unreachable on organic data"
    - "D-16: the fixture edit lands ONLY in `dist/widgets/data/stats/streaks.json`; the repository's `data/stats/streaks.json` is proven unmodified by a content assertion, not by `git status` — `data/stats/` is gitignored and a git check there is vacuous"
    - "D-16: the build is served and browsed from `127.0.0.1`, never `localhost` — stale `index.html` and data files have been served from `localhost` in this project's staged checkpoints before"
    - "The `ended {date}` rows assert the date's VALUE against the fixture's injected `currentStreakEnd`, not merely that a sub-label appeared — a plausible wrong fix renders the streak's START date and would pass a presence check"
    - "The Activities mobile card gets its own rows: it is named in no success criterion but D-05/D-06 change it, so it is observed rather than assumed"
    - "Overview rows and Records/Activities rows are compared side by side, because the phase goal is stated as Overview reaching the same standard as those screens"
    - "Every row names one verdict, its own required detail and its own observer; a failing row is recorded verbatim and left unpatched"
  artifacts:
    - path: ".planning/phases/21-overview-rebuild/21-VALIDATION.md"
      provides: "a thirteen-row checkpoint agenda with one verdict, one required detail and one named observer per row, plus the fixture disclosure"
      contains: "R21-VERDICT"
    - path: ".planning/REQUIREMENTS.md"
      provides: "OVR-01..04 and FIX-01 status gated on the row-to-requirement map"
      contains: "OVR-01"
  key_links:
    - from: "dist/widgets/data/stats/streaks.json fixture"
      to: "rows R12 and R13"
      via: "currentStreakEnd injected as 2026-08-03, currentStreakStart left at 2026-08-10"
      pattern: "ended Aug 3, 2026"
    - from: "the shared renderActivityRow"
      to: "rows R1, R2, R3"
      via: "three surfaces observed in one session against one bundle"
      pattern: "R21-VERDICT"
---

<objective>
Run the phase's mandatory human browser checkpoint (ROADMAP success criterion 6) against a served,
production-shaped build, with a staged fixture that makes FIX-01's ended-streak branch observable
at all.

Purpose: every requirement in milestone v2.1 is visual or interactive, this repository has no jsdom
and no headless browser, and a fully green automated gate has masked real breakage three times
(Phase 16's black page, Phase 17's two rendering gaps, Phase 19's dead `--radius-control` token,
which passed 909/909 tests, clean `tsc`, clean `build-widgets` and 37/37 verify-dashboard checks).
Nothing this phase built is discharged by `npm test`.

Two things make this checkpoint different from a generic one. The live archive's streak is ACTIVE
(`streaks.json`: `withinCurrentStreak: true`, `currentStreak: 2`), so without D-16's fixture the
session would verify the active path and report FIX-01 satisfied. And the `ended {date}` rows must
read the date's VALUE back, because `records-logic.ts`'s layer-2 bug would have rendered a
plausible, confidently-wrong start date that a does-it-render row would have passed.

Output: thirteen recorded rows, a stated disposition for each, and REQUIREMENTS.md updated only
where the rows earn it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-overview-rebuild/21-CONTEXT.md
@.planning/phases/21-overview-rebuild/21-VALIDATION.md
@.planning/phases/21-overview-rebuild/21-01-SUMMARY.md
@.planning/phases/21-overview-rebuild/21-02-SUMMARY.md
@.planning/phases/21-overview-rebuild/21-03-SUMMARY.md
@.planning/phases/21-overview-rebuild/21-04-SUMMARY.md
@.planning/phases/21-overview-rebuild/21-05-SUMMARY.md
@.planning/phases/21-overview-rebuild/21-06-SUMMARY.md

<house_rules>
Non-negotiable, established across checkpoints 16-09, 17-15, 19-05, 19-12, 19-17, 20-05, 20-11,
20-18 and 20-20:

- **One named verdict per row, and the row's own required detail with it.** A blanket statement
  covering several rows is not evidence.
- **Rows are presented ONE AT A TIME**, never as a numbered list of thirteen.
- **Every row names its observer**, using exactly one of: "observed by developer", "observed by
  agent (browser automation against the staged build)", or "observed by developer, corroborated by
  agent".
- **Rows marked "Observer required: developer's own eyes" cannot be answered by agent automation.**
  If the developer declines, the row is recorded BLOCKED with the decline quoted.
- **A theme-sensitive row can only PASS if both the light and the dark theme are named.**
- **A failing row is recorded verbatim and left unpatched.** No suggested fix, no root-cause theory
  for a still-failing row. The next planning round owns diagnosis.
- **Never cite an automated result as evidence for a manual row.** `npm test` cannot observe a
  layout, a wrap, a re-rank, a theme or a rendered date.
- **Serve under `/strava-widgets`, never at the server root**, and browse **`127.0.0.1`, never
  `localhost`**.
- **The four-state verdict vocabulary is kept:** PASS, FAIL, BLOCKED, NOT EXERCISABLE. BLOCKED
  means the observation could not be produced for a dataset or environment reason; NOT EXERCISABLE
  means the gesture itself could not be performed. Neither is a defect verdict.
- **"I could not tell" and "I would rather not" are legitimate answers**, recorded as given.
</house_rules>

<fixture_disclosure>
The archive cannot produce an ended streak. Rows R12 and R13 are observed against a deliberate,
disclosed edit to the STAGED build only:

| Field | Repository `data/stats/streaks.json` | Staged `dist/widgets/data/stats/streaks.json` |
|-------|--------------------------------------|-----------------------------------------------|
| `withinCurrentStreak` | `true` | `false` |
| `currentStreak` | `2` | `0` |
| `currentStreakStart` | `2026-08-10T00:00:00.000Z` | `2026-08-10T00:00:00.000Z` (left as-is, deliberately) |
| `currentStreakEnd` | absent (no compute run yet) | `2026-08-03T00:00:00.000Z` |

`currentStreakStart` is left at its real value ON PURPOSE. It is the discriminator: the correct fix
renders **`ended Aug 3, 2026`**; the plausible wrong fix `21-RESEARCH.md` Pitfall 1 describes would
render **`ended Aug 10, 2026`**. Rows R12 and R13 are not answerable by "a sub-label appeared".

The next `npm run build-widgets` overwrites the staged copy. Nothing is committed.
</fixture_disclosure>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate, production-shaped staging, the ended-streak fixture, and the thirteen-row agenda</name>
  <files>.planning/phases/21-overview-rebuild/21-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/21-overview-rebuild/21-VALIDATION.md` (full) — its frontmatter, the
      Per-Task Verification Map, the Wave 0 Requirements section naming this fixture task, and the
      Manual-Only Verifications table whose five rows this agenda expands into thirteen.
    - `.planning/phases/20-row-click-interaction-pattern/20-18-PLAN.md` Task 1 — the
      fixture-edit-and-verify script shape: run the gate first, assert the staged file has the
      PRE-EDIT value, apply the edit, assert the repository copy is untouched, assert `git status
      --porcelain` is clean for the source trees.
    - `.planning/phases/21-overview-rebuild/21-CONTEXT.md` D-16, including the `127.0.0.1` trap.
    - `scripts/build-widgets.mjs` around its `copyDataFiles` mapping — confirm
      `{ src: 'data/stats', dest: 'dist/widgets/data/stats' }` still holds before relying on the
      staged path.
    - `.gitignore` line 11 — `data/stats/` is gitignored. This is load-bearing for the guard design
      and is stated in the action below.
  </read_first>
  <action>
    Run the established gate first, so the fixture is applied to freshly-built output rather than a
    stale `dist/`: `npm test`, then `npx tsc --noEmit -p tsconfig.json`, then `npm run build-widgets`
    with its log captured and grepped for `css-syntax-error` (the Phase 19 GAP 1/GAP 3 signature),
    then `npm run verify-dashboard`.

    Stage the build the way GitHub Pages serves it — under the `/strava-widgets` project path, never
    at the server root, which is the shape that hid Phase 16's black page:
    `mkdir -p /tmp/gh-pages && ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets`, then assert
    `/tmp/gh-pages/strava-widgets/index.html` exists and the symlink target is absolute.

    Apply the D-16 fixture to `dist/widgets/data/stats/streaks.json` ONLY, with four guards:
    1. BEFORE the edit, assert the staged file reads `withinCurrentStreak === true` and
       `currentStreak === 2`. A staged file already carrying the fixture means `dist/` is stale and
       the whole session would be verifying the wrong bundle.
    2. Apply exactly three field changes: `withinCurrentStreak` to `false`, `currentStreak` to `0`,
       and `currentStreakEnd` to `'2026-08-03T00:00:00.000Z'`. Leave `currentStreakStart` at its
       existing `'2026-08-10T00:00:00.000Z'` — it is the discriminator R12/R13 depend on. Write the
       JSON back.
    3. Assert the REPOSITORY copy `data/stats/streaks.json` still reads `withinCurrentStreak === true`
       and `currentStreak === 2`. This content assertion is the real guard and it is not optional:
       `data/stats/` is gitignored, so `git status --porcelain data` is vacuously empty there and
       would report clean even if the repository copy had been edited. Record that reasoning in the
       plan output.
    4. Assert `git status --porcelain src scripts` is empty — no source file changes during the
       checkpoint.

    Then serve: `python3 -m http.server 8099 --directory /tmp/gh-pages` in the background, and
    confirm `http://127.0.0.1:8099/strava-widgets/index.html` responds 200. Use `127.0.0.1`
    everywhere, never `localhost` — D-16's stated trap and the user's own recorded
    `staged-build-browser-cache-trap` note.

    Append a `## Round 1` section to `21-VALIDATION.md` below the existing Manual-Only table,
    carrying, in order:
    - A preamble stating: the served URL prefix `http://127.0.0.1:8099/strava-widgets/`, the four
      route URLs the session uses (`#/`, `#/list`, `#/records`, and one activity detail), the
      bundle's `assets/index-*.js` filename read from the staged `index.html`, and the full fixture
      disclosure table from this plan's `<fixture_disclosure>` block including the
      `ended Aug 3, 2026` versus `ended Aug 10, 2026` discriminator.
    - The `<house_rules>` block verbatim.
    - A thirteen-row table with columns `Row | Behavior | Requirement | Instructions | Observation`,
      one row per entry in `<checkpoint_rows>` below, each Instructions cell ending with its own
      `Required detail:` clause and its own `Observer required:` clause, and each Observation cell
      left EMPTY.
    - A `### Row-to-requirement map` listing: OVR-01 → R1, R4, R5; OVR-02 → R2, R3; OVR-03 → R6,
      R7, R8, R9, R10; OVR-04 → R11; FIX-01 → R12, R13. State that a requirement is ticked only
      when every row mapped to it is PASS.

    Set `21-VALIDATION.md`'s frontmatter `nyquist_compliant: false`, add `round: 1` and a
    `round1_staged` date key, and leave `status: draft`. Do NOT write any verdict token in this
    task — the agenda is staged, not answered.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json && rm -f /tmp/bw21.log && npm run build-widgets > /tmp/bw21.log 2>&1; BW=$?; cat /tmp/bw21.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw21.log && npm run verify-dashboard && mkdir -p /tmp/gh-pages && ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets && test -f /tmp/gh-pages/strava-widgets/index.html && readlink /tmp/gh-pages/strava-widgets | grep -q '^/' && node --input-type=module -e 'import {readFileSync,writeFileSync} from "node:fs"; const p="dist/widgets/data/stats/streaks.json"; const s=JSON.parse(readFileSync(p,"utf8")); if(s.withinCurrentStreak!==true||s.currentStreak!==2) throw new Error("staged streaks.json is not the freshly-built archive state (withinCurrentStreak="+s.withinCurrentStreak+", currentStreak="+s.currentStreak+") - dist/ may be stale or already fixture-edited"); s.withinCurrentStreak=false; s.currentStreak=0; s.currentStreakEnd="2026-08-03T00:00:00.000Z"; if(s.currentStreakStart!=="2026-08-10T00:00:00.000Z") throw new Error("currentStreakStart is not the expected discriminator value: "+s.currentStreakStart); writeFileSync(p, JSON.stringify(s,null,2)); const back=JSON.parse(readFileSync(p,"utf8")); if(back.withinCurrentStreak!==false||back.currentStreak!==0||back.currentStreakEnd!=="2026-08-03T00:00:00.000Z") throw new Error("the staged fixture edit did not land"); if(back.currentStreakStart!=="2026-08-10T00:00:00.000Z") throw new Error("currentStreakStart was altered - the R12/R13 discriminator is gone"); const repo=JSON.parse(readFileSync("data/stats/streaks.json","utf8")); if(repo.withinCurrentStreak!==true||repo.currentStreak!==2) throw new Error("the REPOSITORY copy of streaks.json was modified - only the staged copy may be edited (git status cannot catch this: data/stats/ is gitignored)"); if("currentStreakEnd" in repo) throw new Error("the repository copy gained currentStreakEnd - no compute run was authorised in this plan"); console.log("fixture landed on the staged copy only; repository copy intact");' && test -z "$(git status --porcelain src scripts)" && node --input-type=module -e 'import {readFileSync} from "node:fs"; const p=".planning/phases/21-overview-rebuild/21-VALIDATION.md"; const s=readFileSync(p,"utf8"); const parts=s.split("## Round 1"); if(parts.length!==2) throw new Error("expected exactly one Round 1 heading"); const t=parts[1]; const rows=t.split("\n").filter(l=>l.startsWith("|")&&!/^\|\s*-+/.test(l)&&!/^\|\s*Row/.test(l)&&!/^\|\s*Field/.test(l)); const ids=["R1","R2","R3","R4","R5","R6","R7","R8","R9","R10","R11","R12","R13"]; const agenda=rows.filter(r=>ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(r))); if(agenda.length!==13) throw new Error("expected 13 agenda rows, found "+agenda.length); ids.forEach((id,i)=>{ if(!new RegExp("^\\|\\s*"+id+"\\.").test(agenda[i])) throw new Error("agenda row "+(i+1)+" should begin with "+id+"."); }); agenda.forEach((r,i)=>{ if(r.split("|").length<6) throw new Error("row "+ids[i]+" does not have five columns"); }); const rd=(t.match(/Required detail/g)||[]).length; if(rd!==13) throw new Error("expected one Required detail clause per row, found "+rd); const ob=(t.match(/Observer required/g)||[]).length; if(ob!==13) throw new Error("expected one Observer required clause per row, found "+ob); if((s.match(/R21-VERDICT/g)||[]).length!==0) throw new Error("verdict tokens must be zero before the checkpoint"); const pre=t.split("| Row")[0]; if(!/127\.0\.0\.1:8099\/strava-widgets\//.test(pre)) throw new Error("the served 127.0.0.1 URL prefix is missing from the preamble"); if(/localhost:8099/.test(s)) throw new Error("localhost:8099 appears - D-16 requires 127.0.0.1"); for(const n of ["2026-08-03","2026-08-10","ended Aug 3, 2026","ended Aug 10, 2026"]) if(!pre.includes(n)) throw new Error("the fixture discriminator disclosure is missing: "+n); if(!/Row-to-requirement map/.test(t)) throw new Error("the row-to-requirement map is missing"); const fm=s.split("---")[1]||""; if(!/nyquist_compliant: false/.test(fm)) throw new Error("frontmatter verdict is not false"); if(!/round: 1/.test(fm)) throw new Error("frontmatter round is not 1"); if(!/round1_staged/.test(fm)) throw new Error("round1_staged date missing"); console.log("13 staged rows, 0 verdicts, fixture disclosed, 127.0.0.1 enforced");'</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets` and
      `npm run verify-dashboard` all exit 0, and the build log contains zero `css-syntax-error`.
    - `/tmp/gh-pages/strava-widgets/index.html` exists and `readlink /tmp/gh-pages/strava-widgets`
      is an absolute path.
    - The staged `dist/widgets/data/stats/streaks.json` read `withinCurrentStreak: true` /
      `currentStreak: 2` BEFORE the edit and reads `false` / `0` /
      `currentStreakEnd: "2026-08-03T00:00:00.000Z"` after it.
    - The staged file's `currentStreakStart` is still `"2026-08-10T00:00:00.000Z"`.
    - `data/stats/streaks.json` still reads `withinCurrentStreak: true` / `currentStreak: 2` and has
      no `currentStreakEnd` key.
    - `git status --porcelain src scripts` is empty.
    - `http://127.0.0.1:8099/strava-widgets/index.html` responds 200; the string `localhost:8099`
      appears nowhere in `21-VALIDATION.md`.
    - `21-VALIDATION.md` has exactly one `## Round 1` heading, thirteen agenda rows R1..R13 in
      order, five columns each, one `Required detail` and one `Observer required` clause per row,
      zero `R21-VERDICT` tokens, the fixture disclosure naming both dates and both rendered strings,
      and a row-to-requirement map.
    - Frontmatter: `nyquist_compliant: false`, `round: 1`, a `round1_staged` date.
  </acceptance_criteria>
  <done>A production-shaped build is served from 127.0.0.1 with a disclosed ended-streak fixture, and thirteen unanswered rows are staged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The thirteen-row browser checkpoint</name>
  <what-built>
    Six plans landed: the shared two-line row renderer with per-surface element-id scoping
    (21-02), its CSS (21-03), Overview's retirement of its private row renderer (21-04), the
    Records year-scope control with a per-distance empty state (21-05), the two this-year Headline
    Stats tiles and Overview's Current Streak sub-label (21-06), and FIX-01's two-layer producer
    fix (21-01). Task 1 ran the full gate, staged the build under `/strava-widgets`, injected the
    ended-streak fixture into the staged data only, and served it on `127.0.0.1:8099`.
  </what-built>
  <how-to-verify>
    Open `http://127.0.0.1:8099/strava-widgets/#/`. **Confirm the URL bar reads `127.0.0.1`, not
    `localhost`, before judging any row** — stale `index.html` and data files have been served from
    `localhost` in this project's checkpoints before, which would make the fixture edit invisible
    and rows R12/R13 falsely PASS on the active-streak path.

    Routes used this session:
    - `http://127.0.0.1:8099/strava-widgets/#/` — Overview (R1, R2, R4, R5, R11, R13)
    - `http://127.0.0.1:8099/strava-widgets/#/list` — Activities (R3, R9)
    - `http://127.0.0.1:8099/strava-widgets/#/records` — Records (R6, R7, R8, R9, R10, R12)

    Present the thirteen rows from `21-VALIDATION.md`'s Round 1 table **one at a time**, in order,
    following every rule in the `<house_rules>` block. For each row, record the developer's own
    words in the Observation cell together with one `R21-VERDICT: PASS|FAIL|BLOCKED|NOT EXERCISABLE`
    token, the row's own required detail, and its observer attribution.

    Theme-sensitive rows (R1, R6, R11, R13) require BOTH themes named before they can be PASS. Use
    the existing theme toggle in the nav.

    Rows R12 and R13 are not answerable by "a sub-label appeared". Read the rendered sub-label
    string back verbatim. `ended Aug 3, 2026` is correct. `ended Aug 10, 2026` is the wrong-date
    failure `21-RESEARCH.md` Pitfall 1 predicts and must be recorded FAIL with the string quoted.
  </how-to-verify>
  <action>
    Present the thirteen Round 1 rows from `.planning/phases/21-overview-rebuild/21-VALIDATION.md`
    to the developer ONE AT A TIME, in R1..R13 order, quoting each row's Instructions cell including
    its `Required detail:` and `Observer required:` clauses. Before row R1, confirm with the
    developer that the URL bar reads `http://127.0.0.1:8099/strava-widgets/` — not `localhost`, and
    not a bare server root. Capture each answer in the developer's own words and hold them for
    Task 3, which writes them into the Observation cells. Do not summarise, do not merge answers
    across rows, do not substitute an agent computed-style read for a row whose observer is the
    developer's own eyes, and do not edit any source file if a row fails.
  </action>
  <read_first>
    - `.planning/phases/21-overview-rebuild/21-VALIDATION.md` § Round 1 — the staged agenda, the
      preamble's fixture disclosure and the `<house_rules>` block Task 1 wrote into it.
    - This plan's `<house_rules>` and `<fixture_disclosure>` blocks.
  </read_first>
  <verify>
    <human-check>All thirteen rows have an answer from the developer, each with the row's own required detail and a named observer; the URL bar was confirmed to read 127.0.0.1 and /strava-widgets/ before any row was judged.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer confirmed the URL bar reads `http://127.0.0.1:8099/strava-widgets/` before R1 was judged.
    - Each of R1..R13 has an answer captured in the developer's own words, or an explicit decline.
    - R1, R6, R11 and R13 each have both light and dark theme named, or are not claimed as PASS.
    - R12 and R13 each have the rendered sub-label string quoted verbatim.
    - No source file was edited during the session: `git status --porcelain src scripts` is empty.
  </acceptance_criteria>
  <done>Thirteen developer answers are in hand, each attributable to a named observer.</done>
  <resume-signal>Per-row verdicts recorded. Type "done" when all thirteen rows have an answer, or name the rows you are declining.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the outcome and gate the requirements on it</name>
  <files>.planning/phases/21-overview-rebuild/21-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/21-overview-rebuild/21-VALIDATION.md` — the Round 1 table as answered in
      Task 2, and its Per-Task Verification Map, whose `⬜ pending` status cells are updated here.
    - `.planning/REQUIREMENTS.md` `:26-29` and `:52` — the OVR-01..04 and FIX-01 entries and their
      checkbox state; `:83-87` — the requirement-to-phase status table.
    - `.planning/STATE.md` — the recorded precedent that a PARTIAL checkpoint ticks only the
      requirements whose rows all passed (16-09, 17-15, 19-05, 20-05 all did exactly this).
  </read_first>
  <action>
    Write the verdicts into the Round 1 Observation cells, then add three sections below the table:

    - `## Checkpoint Outcome (Round 1)` — the count of PASS / FAIL / BLOCKED / NOT EXERCISABLE, and
      one sentence per non-PASS row naming what was not established. No fix, no root-cause theory.
    - `## Evidence Quality (Round 1)` — which rows were observed by the developer's own eyes, which
      by agent automation, whether both themes were genuinely named on each of R1, R6, R11 and R13,
      and an explicit note that rows R12 and R13 rest on a fixture rather than organic archive data,
      naming the file, the three edited fields and the discriminator value.
    - `## Round 1 Gap-Closure Record` — one subsection per FAIL or BLOCKED row, quoting the
      developer's words verbatim, with the requirement it blocks. Omit this section entirely if
      there are no FAIL and no BLOCKED rows.

    Update the Per-Task Verification Map's Status column from `⬜ pending` to the state each row's
    evidence supports.

    Set frontmatter `nyquist_compliant: true` and `status: passed` ONLY on a clean sweep of all
    thirteen rows. Any FAIL, BLOCKED or NOT EXERCISABLE keeps `nyquist_compliant: false` and
    `status: partial`.

    In `REQUIREMENTS.md`, tick a requirement's checkbox and set its status-table row to `Complete`
    only when EVERY row mapped to it is PASS, per the map Task 1 wrote: OVR-01 needs R1, R4, R5;
    OVR-02 needs R2, R3; OVR-03 needs R6, R7, R8, R9, R10; OVR-04 needs R11; FIX-01 needs R12 and
    R13. Leave every other requirement exactly as it stands. If FIX-01 passes, add a parenthetical
    to its entry recording that its `ended` branch was confirmed against a staged-build fixture, not
    organic archive data, and that the field is absent from the published `streaks.json` until a
    compute run regenerates it.

    Do not edit any source file. Do not patch a failing row.
  </action>
  <verify>
    <automated>node --input-type=module -e 'import {readFileSync} from "node:fs"; const p=".planning/phases/21-overview-rebuild/21-VALIDATION.md"; const s=readFileSync(p,"utf8"); const parts=s.split("## Round 1"); if(parts.length!==2) throw new Error("expected exactly one Round 1 heading"); const t=parts[1]; const ids=["R1","R2","R3","R4","R5","R6","R7","R8","R9","R10","R11","R12","R13"]; const rows=t.split("\n").filter(l=>l.startsWith("|")).filter(r=>ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(r))); if(rows.length!==13) throw new Error("expected 13 rows, found "+rows.length); const obs=rows.map(r=>r.split("|")[5]||""); const v=obs.map(o=>(o.match(/R21-VERDICT:\s*(PASS|FAIL|BLOCKED|NOT EXERCISABLE)/)||[])[1]); v.forEach((x,i)=>{ if(!x) throw new Error("row "+ids[i]+" has no R21-VERDICT token"); }); for(let i=0;i<obs.length;i++){ const body=obs[i].replace(/R21-VERDICT:\s*(PASS|FAIL|BLOCKED|NOT EXERCISABLE)/,"").trim(); if(body.length<60) throw new Error("row "+ids[i]+" observation is too thin ("+body.length+" chars)"); if(!/observed by (developer|agent)/i.test(obs[i])) throw new Error("row "+ids[i]+" does not name its observer"); for(let j=i+1;j<obs.length;j++) if(obs[i].trim()===obs[j].trim()) throw new Error("rows "+ids[i]+" and "+ids[j]+" share an identical observation"); } const at=(id)=>ids.indexOf(id); const pass=(id)=>v[at(id)]==="PASS"; const cell=(id)=>obs[at(id)]; for(const n of ["R1","R6","R11","R13"]) if(pass(n) && !(/light/i.test(cell(n)) && /dark/i.test(cell(n)))) throw new Error("theme-sensitive row "+n+" is PASS without naming both themes"); for(const n of ["R12","R13"]){ if(pass(n)){ if(!/ended Aug 3, 2026/.test(cell(n))) throw new Error(n+" is PASS without quoting the exact rendered sub-label"); if(/ended Aug 10, 2026/.test(cell(n))) throw new Error(n+" quotes the WRONG date - that is a FAIL, not a PASS"); if(!/0 days/.test(cell(n))) throw new Error(n+" is PASS without confirming the big number still reads 0 days (D-14)"); } } if(pass("R7") && !/\b20\d\d\b/.test(cell("R7"))) throw new Error("R7 is PASS without a read-back date proving the year scope"); if(pass("R10") && /No marathon efforts yet/.test(cell("R10"))) throw new Error("R10 quotes the old hardcoded marathon copy - that is a FAIL"); if(pass("R9") && !/All time/i.test(cell("R9"))) throw new Error("R9 is PASS without stating the scope on re-arrival"); if(pass("R5") && !/#\/activity\//.test(cell("R5"))) throw new Error("R5 is PASS without the navigated URL"); const fails=v.filter(x=>x==="FAIL").length; const nonpass=v.filter(x=>x!=="PASS").length; const fm=s.split("---")[1]||""; const claimed=/nyquist_compliant: true/.test(fm); if(claimed && nonpass>0) throw new Error("nyquist_compliant is true without a clean sweep"); if(claimed && !/status: passed/.test(fm)) throw new Error("nyquist_compliant true requires status: passed"); if(!claimed && nonpass===0) throw new Error("all rows pass but nyquist_compliant was not set true"); if(nonpass>0 && !/## Round 1 Gap-Closure Record/.test(s)) throw new Error(nonpass+" non-PASS rows but no Round 1 Gap-Closure Record"); if(!/## Evidence Quality \(Round 1\)/.test(s)) throw new Error("missing Evidence Quality (Round 1)"); if(!/## Checkpoint Outcome \(Round 1\)/.test(s)) throw new Error("missing Checkpoint Outcome (Round 1)"); if(!/dist\/widgets\/data\/stats\/streaks\.json/.test(s)) throw new Error("the Evidence Quality section does not name the fixture file"); if(/⬜ pending/.test(s)) throw new Error("the Per-Task Verification Map still has pending rows"); const req=readFileSync(".planning/REQUIREMENTS.md","utf8"); const ticked=(id)=>new RegExp("\\[x\\][^\\n]*"+id+"\\b").test(req); const map={"OVR-01":["R1","R4","R5"],"OVR-02":["R2","R3"],"OVR-03":["R6","R7","R8","R9","R10"],"OVR-04":["R11"],"FIX-01":["R12","R13"]}; for(const [rq,rs] of Object.entries(map)){ const all=rs.every(pass); if(ticked(rq)!==all) throw new Error(rq+" checkbox state does not match its row map (all rows pass: "+all+")"); } console.log("13 verdicts recorded, "+fails+" FAIL, requirements gated on the row map"); ' && test -z "$(git status --porcelain src scripts)"</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the thirteen rows carries exactly one `R21-VERDICT` token, an observation of at
      least 60 characters excluding that token, and a named observer.
    - No two rows share an identical observation cell.
    - R1, R6, R11 and R13 name both light and dark themes if PASS.
    - R12 and R13, if PASS, quote `ended Aug 3, 2026`, do not contain `ended Aug 10, 2026`, and
      confirm the big number still reads `0 days`.
    - R7, if PASS, contains a read-back date; R10, if PASS, does not quote `No marathon efforts yet`;
      R9, if PASS, states the scope on re-arrival; R5, if PASS, contains the navigated `#/activity/` URL.
    - `nyquist_compliant: true` and `status: passed` appear only on a clean sweep; otherwise
      `false` / `partial` and a `## Round 1 Gap-Closure Record` exists.
    - `## Checkpoint Outcome (Round 1)` and `## Evidence Quality (Round 1)` both exist, and the
      latter names `dist/widgets/data/stats/streaks.json`.
    - The Per-Task Verification Map has no `⬜ pending` rows left.
    - Each of OVR-01, OVR-02, OVR-03, OVR-04 and FIX-01 is ticked in `REQUIREMENTS.md` if and only
      if every row mapped to it is PASS.
    - `git status --porcelain src scripts` is empty — no source file was touched during the checkpoint.
  </acceptance_criteria>
  <done>Thirteen rows have a stated disposition, and no requirement is ticked on evidence that was not given.</done>
</task>

</tasks>

<checkpoint_rows>
Thirteen rows for Task 1's agenda. Each Instructions cell must end with its own `Required detail:`
and `Observer required:` clauses, worded as given.

**R1. Overview Recent PRs — the two-line hierarchy (OVR-01, D-06).** On `#/`, look at the Recent
PRs card. Each row should be two lines: the activity name on the first line with its badges pushed
to the right edge of the row, and a second line reading date · distance · duration · pace. It should
not read as three stacked blocks. Toggle the theme and look again.
*Required detail:* the first row's name, the badge text you see on it, and the full second line, in
both themes. *Observer required:* developer's own eyes.

**R2. Overview Recent Activities — the same structure (OVR-02, D-05).** On the same page, scroll to
Recent Activities. Compare a row here against a Recent PRs row directly above it.
*Required detail:* whether the two rows have the same two-line shape and the same badge placement,
stated as a comparison, plus any difference you can see. *Observer required:* developer's own eyes.

**R3. Activities mobile card — the third surface (OVR-02, D-05).** Narrow the window below 720px
and open `#/list` so the mobile card layout renders instead of the table. Compare a card against the
Overview rows from R1/R2.
*Required detail:* whether the card has the same two-line shape as the Overview rows, and the name
and second line of one card. *Observer required:* developer's own eyes.

**R4. Narrow-width degradation (OVR-01, D-06).** With the window at roughly 360px wide, look at a
row that carries at least one badge on Overview or `#/list`.
*Required detail:* whether any badge has wrapped down onto the metrics line, and what the row looks
like at that width. *Observer required:* developer's own eyes.

**R5. The row is still a link (OVR-01, Phase 20 non-regression).** Back at full width on `#/`, Tab
to a Recent PRs row and confirm it takes exactly one Tab stop and shows a focus ring, then press
Enter.
*Required detail:* the number of Tab presses to move past the row, whether the focus ring was
visible, and the `#/activity/...` URL you landed on. *Observer required:* developer's own eyes.

**R6. The Records scope control (OVR-03, D-01, D-02).** Open `#/records` and scroll to the PR
Tables section. A two-option control should sit between the section heading and the first distance
table, reading All time and This year, with All time selected. Toggle the theme.
*Required detail:* the two option labels as written, which one is selected on arrival, and whether
the control is legible in both themes. *Observer required:* developer's own eyes.

**R7. The year scope re-ranks (OVR-03, D-01).** Click This year. Pick one distance table that still
has rows and read its top row.
*Required detail:* the distance you picked, the rank shown in its first row, and the date in that
row. *Observer required:* developer's own eyes.

**R8. The scope governs only the PR tables (OVR-03, D-03).** With This year still selected, scroll
through Superlatives, PR Evolution and Race Predictions.
*Required detail:* whether each of those three changed when you toggled, named individually.
*Observer required:* developer's own eyes.

**R9. The scope does not persist (OVR-03, D-04).** With This year selected, navigate to `#/list`,
then back to `#/records`.
*Required detail:* which scope is selected on arrival. *Observer required:* developer's own eyes.

**R10. The empty state names its own distance (OVR-03).** With This year selected, find a distance
table showing an empty state (if none is empty this year, say so and the row is BLOCKED).
*Required detail:* the empty state's heading text, quoted exactly. *Observer required:* developer's
own eyes.

**R11. This-year Headline Stats tiles (OVR-04, D-09, D-11).** Back on `#/`, count the tiles in the
Headline Stats card and read the last two. Toggle the theme.
*Required detail:* the number of tiles, the last two tiles' labels and values, and whether both read
correctly in both themes. *Observer required:* developer's own eyes.

**R12. Records Current Streak sub-label (FIX-01, D-13, D-14, D-16).** On `#/records`, find the
Current Streak tile in the Superlatives grid. The staged fixture has set the streak to ended.
*Required detail:* the tile's big number and its sub-label, both quoted exactly as rendered. A
sub-label reading `ended Aug 10, 2026` is the wrong date and must be recorded FAIL.
*Observer required:* developer's own eyes.

**R13. Overview Current Streak sub-label (FIX-01, D-15).** On `#/`, find the Current Streak tile in
Headline Stats. Toggle the theme.
*Required detail:* the tile's big number and its sub-label, both quoted exactly as rendered, in both
themes. `ended Aug 10, 2026` is the wrong date and must be recorded FAIL. *Observer required:*
developer's own eyes.
</checkpoint_rows>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged `dist/` -> served origin | A local HTTP server serves the build under the production path prefix |
| fixture data -> recorded evidence | Edited data is presented in a session whose verdicts become the requirement gate |
| checkpoint session -> repository | A checkpoint must not silently change source or committed data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07-01 | Spoofing | a root-served or `localhost`-cached build standing in for the production path | mitigate | Task 1 asserts `/tmp/gh-pages/strava-widgets/index.html` resolves and the symlink target is absolute; the agenda and the checkpoint instructions require confirming `127.0.0.1` and `/strava-widgets/` in the URL bar before any row is judged; Task 1's check fails if the string `localhost:8099` appears anywhere in `21-VALIDATION.md` |
| T-21-07-02 | Spoofing | fixture data presented as archive data | mitigate | Task 1 asserts the staged file held the pre-edit archive values, that the edit landed, and that the repository copy is unchanged BY CONTENT (a `git status` check is vacuous — `data/stats/` is gitignored); the preamble discloses the file, the three fields, both dates and both possible rendered strings; Task 3 requires the Evidence Quality section to name the fixture and requires FIX-01's REQUIREMENTS entry to record it |
| T-21-07-03 | Repudiation | a manual row recorded as passed on evidence never given | mitigate | Task 3's automated check requires one four-state verdict token per row, a named observer, at least 60 characters of observation, no two identical cells, both theme names on the four theme-sensitive rows, and a per-row required-detail predicate on every PASS row |
| T-21-07-04 | Repudiation | the `ended` sub-label passing on presence rather than value | mitigate | R12 and R13 cannot be recorded PASS unless their cell quotes `ended Aug 3, 2026`; quoting `ended Aug 10, 2026` is rejected by the automated check as a FAIL condition, closing `21-RESEARCH.md` Pitfall 1 |
| T-21-07-05 | Tampering | patching a defect during the checkpoint | mitigate | `git status --porcelain src scripts` must be empty, asserted in both Task 1 and Task 3. The house rule since 16-09 is that a failing row is recorded verbatim and left unpatched |
| T-21-07-06 | Tampering (XSS / output encoding) | this plan's surface | accept | This plan writes only planning Markdown and edits one staged JSON data file; it adds no rendering code. The output-encoding controls for every surface observed here were established and asserted in plans 21-02, 21-04, 21-05 and 21-06 |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized; no `## Package Legitimacy Audit` is required because no package-manager install task exists. The server is `python3 -m http.server`, already present. Do not install jsdom, happy-dom, puppeteer, playwright or any accessibility-audit tool to automate these rows — no automated substitute exists in this repository and this phase does not accept one |
</threat_model>

<verification>
The gate for this plan is Task 1's script (full suite, `tsc`, `build-widgets` with no
`css-syntax-error`, `verify-dashboard`, production-shaped staging, the four fixture guards and the
staged-agenda shape check) followed by Task 3's script (per-row verdict, observer, substance,
theme, discriminator and requirement-map checks).

No automated result in this repository is evidence for any of the thirteen rows. `npm test` cannot
observe a layout, a wrap, a re-rank, a theme, a Tab stop or a rendered date string.

After the checkpoint the staged fixture stays in `dist/` until the next `npm run build-widgets`
overwrites it. Nothing is committed and the repository copy of `streaks.json` is never touched.
</verification>

<success_criteria>
- The build is served from `127.0.0.1` under `/strava-widgets` from a symlink proven to point at `dist/widgets`.
- The ended-streak fixture exists only in the staged copy, with the repository copy proven unchanged by content.
- Thirteen rows each carry one verdict, one required detail and one named observer.
- FIX-01's rows assert the date's value against the fixture's injected `currentStreakEnd`.
- `REQUIREMENTS.md` ticks exactly those requirements whose every mapped row passed.
- No source file changed during the checkpoint.
</success_criteria>

<output>
Create `.planning/phases/21-overview-rebuild/21-07-SUMMARY.md` when done.
</output>
