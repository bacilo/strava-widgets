---
phase: 25-ci-hardening-light-theme-verification
plan: 09
type: execute
wave: 6
depends_on: ["25-08"]
files_modified:
  - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md
autonomous: true
requirements: [VER-01, FIX-02, CI-02, CI-01]
gap_closure: true
closes_gaps: [GAP-25-01, GAP-25-02]

must_haves:
  decisions: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13]
  truths:
    - "Checkpoint Row Discipline rule 3: every Round 2 row is fully drafted — discriminator, evidence requirement, pass and fail conditions — in THIS plan, which completes strictly before plans 25-10 and 25-11 run any of them"
    - "GAP-25-02's recommended split is applied as three independent rows, one requirement each: R6a maps to FIX-02 only, R6b to CI-02 only, R6c to CI-01 only, so a missing item withholds only its own requirement"
    - "R7 replaces the BLOCKED R2 and is drafted against the mechanism plan 25-08 measured and selected, never against a mechanism assumed to work"
    - "Every row carries a reachability proof in BOTH directions: it can fail on a broken implementation (not vacuous, the D-05 defect class) and it can pass on a working one (not unpassable, the D-04 defect class)"
    - "Every row asserts reachable extent against an independently-derived value — the eight step names read from src/compute-all-stats-steps.ts, the six document names from D-09, the eight FIX-02 cases from D-12's four shapes times two functions — never against the artefact agreeing with itself"
    - "GAP-25-01's four numbered clauses are carried into R7 verbatim in substance: dark OS, dashboard-theme in the null-or-auto class, a frame provably at or before that navigation's first-paint, and a top-level navigation to the production URL rather than a proxy context"
    - "The ordering argument (inline head script complete at domInteractive 287.9 ms plus 25-05's node:vm parity pin, D-06) is named in R7 as explicitly NOT acceptable evidence"
    - "No row is scored in this plan; every Verdict cell reads pending when the plan completes"
  artifacts:
    - path: ".planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md"
      provides: "Section 'Round 2 Checkpoint (R6a, R6b, R6c, R7)' with the governing rules, the row-to-requirement map, and four fully drafted rows each carrying a bidirectional reachability proof and a pending verdict"
      contains: "Round 2 Checkpoint (R6a, R6b, R6c, R7)"
  key_links:
    - from: "Round 2 row R6c"
      to: "src/compute-all-stats-steps.ts COMPUTE_ALL_STATS_STEPS"
      via: "the eight step names the collapsed Actions step's log must carry, enumerated from source rather than read off the log"
      pattern: "compute-training-load"
    - from: "Round 2 row R7"
      to: "the GAP-25-01 capture-mechanism reachability proof section"
      via: "the selected mechanism and its measured margin against the 612 ms first paint"
      pattern: "SELECTED MECHANISM"
---

<objective>
Draft every Round 2 checkpoint row — R6a, R6b, R6c and R7 — with its discriminator, its evidence
requirement, its pass and fail conditions, and a reachability proof in both directions, BEFORE any
of them is run.

Purpose: Checkpoint Row Discipline rule 3. Round 1 deliberately did NOT split R6 mid-round to
harvest the two ticks whose evidence was already green, because redesigning a row after seeing its
outcome is the exact failure mode the rule exists to prevent (Phase 24 R19/R26 precedent). The split
is legitimate only if it is drafted first. This plan is that drafting. Plan 25-10 runs R7; plan 25-11
runs R6a/R6b/R6c.

Output: a `## Round 2 Checkpoint (R6a, R6b, R6c, R7)` section in `25-VALIDATION.md` with four rows,
all verdicts `pending`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-08-SUMMARY.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-07-SUMMARY.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-06-SUMMARY.md

<interfaces>
<!-- Everything below is already established. Do NOT re-derive it by exploration. -->

ROW-TO-REQUIREMENT MAP for Round 2 (one row per requirement, per GAP-25-02's recommended shape):
  VER-01  <- R7   (R1, R3, R4 and R5 already PASSED in Round 1 and are NOT re-run)
  FIX-02  <- R6a
  CI-02   <- R6b
  CI-01   <- R6c

THE EIGHT COMPUTE STEP NAMES, read from `src/compute-all-stats-steps.ts`'s exported
`COMPUTE_ALL_STATS_STEPS` array, in array order — this is R6c's independently-derived value:
  compute-stats (mandatory), compute-advanced-stats (mandatory), compute-geo-stats,
  compute-best-efforts, compute-age-grading, compute-dashboard-index, compute-gear-aggregate,
  compute-training-load
`src/index.ts:303-310` wraps each step to print a line of the form "> NAME" before it runs, and
`src/index.ts:313` prints "All statistics generated successfully!" after the walk. The end-of-run
"DEGRADED STEPS (N)" summary (`src/index.ts:322`) is printed ONLY when a tolerated step failed, so
its presence is conditional and must not be part of any pass condition.

THE WORKFLOW SIDE (`.github/workflows/daily-refresh.yml`): the collapsed step is named "Compute all
statistics" at :96 and runs `node dist/index.js compute-all-stats --ci` at :97. The pushed copy on
`origin/master` does NOT yet carry it — `git show origin/master:.github/workflows/daily-refresh.yml
| grep -c "compute-all-stats --ci"` returned 0 at planning time (2026-09-04), while local `master`
returns 1. That gap is what makes R6c's failing direction concrete rather than hypothetical.

CI-02's INDEPENDENTLY-DERIVED EXTENT (D-09/D-10): six documents, named — weekly-distance.json,
monthly-stats.json, yearly-stats.json, year-over-year.json, best-efforts.json, and one per-activity
best-effort shard sampled at runtime (no pinned ids, per `verify-dashboard-publish.mjs:430-455`).
The check total went 40/40 at the end of Phase 24 to `56 check(s) passed, 0 failure(s).` — 16
net-new checks.

FIX-02's INDEPENDENTLY-DERIVED EXTENT (D-12/D-13): four shapes (absent `gearName` key, `undefined`,
empty string, non-string) times two functions (`buildGearAggregate`, `buildGearCoverage`) = eight
regression cases in `src/analytics/gear-aggregate-logic.test.ts`, matching the +8 test delta
attributed to plan 25-01 in the Wave 1 Integration Gate table. D-13's type change is proven by
`npx tsc --noEmit` exiting 0 with `gearName` optional on the row type.

THE WAVE 1 GATE NUMBERS to reconcile against: 62 test files / 1596 tests (up from the 60/1560
pre-planning baseline by exactly +2 files and +36 tests: 8 from 25-01, 10 from 25-02, 1 from 25-04,
17 from 25-05), five commands all exit 0, `verify-dashboard` final line `56 check(s) passed, 0
failure(s).`

THE FAILING-DIRECTION EVIDENCE THAT ALREADY EXISTS, citable rather than re-derivable — this file's
own `## D-11 RED Observation Log`: eight verbatim vitest failures for FIX-02 against the unwidened
predicate (`TypeError: Cannot read properties of undefined (reading 'toLowerCase')` at
`gear-aggregate-logic.ts:43:6`, and the four `expected 1 to be +0` coverage assertions), and six
break/restore cycles for CI-02, each quoting the failure line that names its own document (for
example `✗ /data/stats/weekly-distance.json expected a non-empty array, got an array of length 0`).

D-04 AS AMENDED, and D-05, are already disclosed in this file's Round 1 section. Round 2 rows that
depend on them cross-reference those disclosures explicitly rather than restating them loosely, and
R7 carries its own disclosure if 25-08 selected the throttled mechanism.

VERDICT VOCABULARY, established Phase 20 and used since: PASS / FAIL / BLOCKED / NOT EXERCISABLE.
BLOCKED means the evidence offered was not the evidence the row required. It is not a softer PASS.

HOUSE RULE since plan 16-09: nothing is patched during a round. A defect observed while running a
row is recorded, not fixed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Round 2 preamble — governing rules, row-to-requirement map, and the constraints every row inherits</name>
  <files>.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</files>
  <read_first>
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (the "Checkpoint Row Discipline (inherited precedents — non-negotiable)" section; the whole "Round 1 Checkpoint (R1-R6)" section for structure, including its D-04 amendment disclosure and its Execution provenance block; and "Gaps opened by Round 1")
    - .planning/phases/24-local-curation-mode/24-VALIDATION.md (the Round 4 Checkpoint section — the structural template these rounds follow)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-08-SUMMARY.md (which mechanism 25-08 selected, and whether it HALTED)
  </read_first>
  <action>
FIRST, check plan 25-08's outcome. If 25-08 recorded a HALT (no mechanism produced a frame at or
before first paint), then R7 CANNOT be drafted: draft R6a, R6b and R6c only, state plainly in the
preamble that VER-01 has no Round 2 row because GAP-25-01 remains open at the mechanism level, and
do not invent a weaker R7. Everything below about R7 applies only if 25-08 selected a mechanism.

Open a new section `## Round 2 Checkpoint (R6a, R6b, R6c, R7)` in `25-VALIDATION.md`, placed after
the Round 1 section and before `## Validation Sign-Off`. Write its preamble containing:

1. WHY THIS ROUND EXISTS, in two sentences: Round 1 returned R2 BLOCKED (no capture mechanism beat
   first paint) and R6 BLOCKED (CI-01's live-run evidence absent), and R6's unsplittability withheld
   FIX-02 and CI-02 whose own evidence was green. GAP-25-01 and GAP-25-02 are the scope.

2. THE ROW-TO-REQUIREMENT MAP, one row per requirement: VER-01 to R7, FIX-02 to R6a, CI-02 to R6b,
   CI-01 to R6c. State explicitly that this is GAP-25-02's recommended split, drafted BEFORE it is
   run, and that Round 1 deliberately declined to apply it mid-round — cite R6's own verdict text
   for why.

3. WHAT ROUND 1 ALREADY SETTLED AND IS NOT RE-RUN: R1 (light-OS legibility), R3 and R4 (live-follow
   in both directions), R5 (cache-trap exclusion) all PASSED. State that VER-01's Round 2 tick
   depends on R7 PASSING while those four Round 1 PASSes stand. State the one caveat honestly: R5's
   asset-identity evidence was taken against `origin/gh-pages` @ `b2b05a4` (built from source commit
   `623046363`, which was `origin/master`'s HEAD), and plan 25-11 will push this phase's work and
   dispatch a workflow that redeploys the Pages site — so R5's specific hashed asset may no longer be
   what production serves afterwards. That is why R7 carries its own asset-identity clause rather
   than leaning on R5's.

4. THE GOVERNING RULE, unchanged and non-waivable: a requirement ticks ONLY if every row mapped to it
   is PASS. Under the split, that now means one row per requirement rather than one row for three.

5. CHECKPOINT ROW DISCIPLINE, restated as binding for this round: (a) a row is drafted, with its
   reachability proof, before it is run — this plan is the drafting and plans 25-10/25-11 are the
   running; (b) reachability is proved in BOTH directions, because this phase has already been bitten
   in both (D-05 caught a vacuous row, D-04 caught an unpassable one); (c) rows assert reachable
   extent against an independently-derived value, never internal agreement (Phase 23 CR-01, Phase 24
   R32). Add the explicit pledge: if a row turns out unsatisfiable while it is being run, it is
   HALTED and a new numbered gap is opened — it is NOT edited to yield a tick.

6. THE VERDICT VOCABULARY (PASS / FAIL / BLOCKED / NOT EXERCISABLE, with BLOCKED defined as evidence
   that was not the evidence the row required) and the house rule that nothing is patched during a
   round (since plan 16-09).

7. EXECUTION SPLIT AND ENVIRONMENTAL HAZARDS carried forward: D-07 hybrid execution, the developer
   performs every macOS appearance change by hand and `osascript` is rejected for any recorded row;
   D-08 target `https://bacilo.github.io/strava-widgets/`; the cleared-site-data procedure for R7 is
   the harness's throwaway `--user-data-dir` (an empty profile and empty HTTP cache by construction,
   which is stronger than a hard reload, and must be NAMED in the row's own evidence); and the Round
   1 environment note that the Chrome window must be frontmost during capture, since a backgrounded
   tab made both live-follow rows initially read as false failures.

8. DISCLOSURE OBLIGATIONS carried into this round: D-04's amendment disclosure and D-05's dark-OS
   deviation disclosure both already exist in the Round 1 section and are cross-referenced by
   section name; if plan 25-08 selected the throttled mechanism, R7 must additionally carry a
   slowed-load disclosure of the same weight. State that the absence of a required disclosure is a
   defect in the round independent of any row's verdict.
  </action>
  <verify>
    <automated>grep -c "Round 2 Checkpoint (R6a, R6b, R6c, R7)" .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `25-VALIDATION.md` contains a `## Round 2 Checkpoint (R6a, R6b, R6c, R7)` heading positioned after the Round 1 section and before `## Validation Sign-Off`.
    - The preamble states the row-to-requirement map with exactly one row per requirement, naming VER-01/R7, FIX-02/R6a, CI-02/R6b, CI-01/R6c.
    - The preamble states that R1, R3, R4 and R5 PASSED in Round 1 and are not re-run, AND discloses that plan 25-11's push and dispatch will redeploy production, which is why R7 carries its own asset-identity clause.
    - The preamble restates all three Checkpoint Row Discipline rules and adds the pledge that an unsatisfiable row is HALTED and gapped rather than edited.
    - The preamble names the four-value verdict vocabulary and defines BLOCKED as evidence that was not the evidence the row required.
    - The preamble names D-07 (developer's hand, no `osascript`), D-08 (the production URL), the throwaway-profile cleared-storage procedure, and the frontmost-window requirement.
    - If plan 25-08 HALTED, the preamble says so and no R7 is drafted anywhere in the section.
  </acceptance_criteria>
  <done>The Round 2 section exists with its governing rules, its one-row-per-requirement map, and its inherited constraints written down before any row text.</done>
</task>

<task type="auto">
  <name>Task 2: Draft R7 — the replacement first-paint row — with a bidirectional reachability proof against 25-08's selected mechanism</name>
  <files>.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</files>
  <read_first>
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (the "GAP-25-01 capture-mechanism reachability proof" section written by plan 25-08; the Round 1 R2 row, whose capture-fidelity arithmetic R7 must beat; and GAP-25-01's four numbered clauses)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-08-SUMMARY.md (the selected mechanism, its measured margin, and the negative-control colours)
    - scripts/first-paint-capture.mjs (the exact invocation R7's evidence requirement will name, including which flags)
    - src/dashboard/styles.css (lines 10-25 and 100-110 — the two `--bg` values that make the discriminator)
  </read_first>
  <action>
Draft row R7 into the Round 2 section. Do not run it. Its Verdict cell reads `pending`.

R7 — First-paint flash, observed (VER-01, ROADMAP criterion 4 clause 2). It replaces the BLOCKED R2.

DISCRIMINATOR: the sampled background colour of a captured raster frame whose timestamp is provably
at or before that same navigation's `first-paint`. `rgb(26, 26, 46)` (dark `--bg: #1a1a2e`,
`styles.css:105`) is PASS. `rgb(255, 255, 255)` is FAIL. A frame that cannot be shown to be at or
before first paint is BLOCKED, not PASS — that is precisely R2's outcome and repeating it is not a
closure.

EVIDENCE REQUIREMENT, carrying GAP-25-01's four clauses in substance:
  1. macOS Appearance in DARK, quoted from `defaults read -g AppleInterfaceStyle` returning `Dark`,
     set by the developer's own hand (D-07) or disclosed as already-dark. D-05's reasoning is
     cross-referenced: on a light OS `--bg: #ffffff` (`styles.css:18`) makes white the correct final
     state, so the row would be vacuous.
  2. `localStorage.getItem('dashboard-theme')` quoted at the instant of observation, in the
     `null`-or-`'auto'` class per D-04 as amended. A quoted `'light'` or `'dark'` records the row
     BLOCKED, because the in-page control would be overriding the OS setting under test. Also quote
     `matchMedia('(prefers-color-scheme: dark)').matches` (must be `true`) and
     `document.documentElement.getAttribute('data-theme')` (must be `'dark'`).
  3. The captured frame's timestamp, the navigation's `performance.timeOrigin`, and that
     navigation's `first-paint` `startTime`, all three quoted, with the arithmetic shown:
     `frame_timestamp - timeOrigin` must be at or below the `first-paint` startTime. Comparing the
     frame to wall-clock proximity to the reload is explicitly NOT acceptable — that attribution is
     what GAP-25-01 clause 3 forbids.
  4. A top-level navigation to `https://bacilo.github.io/strava-widgets/` (D-08). The local
     127.0.0.1 control served in 25-08 Part B does NOT satisfy this clause; it is mechanism proof
     only, and the row must say so, so a future reader cannot mistake one for the other.
  5. Cache-trap exclusion in the row's own right (D-08, T-25-21): quote the captured document's
     `script[type=module][src]` hashed asset filename, a cache-busted refetch of `index.html` shown
     byte-identical to the document the browser used (SHA-256 and byte length for both), and the
     matching asset name in the `origin/gh-pages` tree at the commit deployed at that moment — the
     independently-derived value, per Checkpoint Row Discipline rule 2. Also name the throwaway
     `--user-data-dir` as the cleared-storage mechanism.
  6. The developer's own judgment of the captured frame, recorded verbatim (D-07 keeps the human
     judgment human). State the granularity actually given; do not expand a blanket approval into
     per-clause detail it did not contain (plan 19-09 precedent).

REACHABILITY PROOF, both directions, written into the row before it is run:
  - CAN FAIL (not vacuous): cite plan 25-08 Part B's negative control by frame path and sampled
    colour — the stripped-bootstrap copy of the same build sampled `rgb(255, 255, 255)` at first
    paint under the same mechanism and the same dark OS. A broken pre-paint bootstrap is therefore
    demonstrably detectable by this row's own instrument.
  - CAN PASS (not unpassable): cite plan 25-08 Part A's production runs by number — at least one
    frame with `beats_first_paint` true against the real 612 ms first paint, with the measured
    margin — and Part B's intact copy sampling `rgb(26, 26, 46)`. This is the clause R2 could not
    satisfy; it is what makes R7 a different row rather than a retry.
  - State the residual risk honestly: if, on the day, no frame beats first paint, the row is BLOCKED
    and a new numbered gap is opened. It is not re-scored against a later frame.

DISCLOSURES the row must carry:
  - Cross-reference D-05's dark-OS deviation disclosure and D-04's amendment disclosure by section
    name (both already written in the Round 1 section).
  - If plan 25-08 selected the throttled mechanism, R7 carries a slowed-load disclosure of the same
    weight as those two: what was throttled, the exact `Network.emulateNetworkConditions`
    parameters, that this is a deliberate deviation from the row's natural conditions, and the
    argument that a widened navigation-to-first-paint window can only make a real flash more visible
    and cannot manufacture a pass.

EXPLICITLY NOT ACCEPTABLE EVIDENCE, stated in the row so the running agent cannot drift into it: the
ordering argument — inline head script complete by `domInteractive` `287.9 ms`, `324 ms` before
`first-paint` `612 ms`, plus plan 25-05's `node:vm` parity pin (D-06). GAP-25-01 rejects it by name
as a closure route. It may be restated as context; it may not decide the verdict.
  </action>
  <verify>
    <automated>grep -c "^\*\*R7 " .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md; grep -c "beats_first_paint" .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The Round 2 section contains a row labelled R7 whose Verdict cell reads `pending`.
    - R7's text states the discriminator as `rgb(26, 26, 46)` PASS versus `rgb(255, 255, 255)` FAIL, and states that a frame not provably at or before first paint is BLOCKED rather than PASS.
    - R7's evidence requirement enumerates all six items above, including the explicit arithmetic form `frame_timestamp - timeOrigin` compared against the `first-paint` startTime.
    - R7 states that the 127.0.0.1 control from 25-08 does not satisfy the top-level-navigation clause.
    - R7 contains a reachability proof with a CAN FAIL paragraph citing 25-08's negative control by frame path and sampled colour, and a CAN PASS paragraph citing a production run with `beats_first_paint` true and its measured margin.
    - R7 names the ordering argument and states it may not decide the verdict.
    - If 25-08 selected the throttled mechanism, R7 contains a slowed-load disclosure naming the exact emulation parameters.
    - R7 carries a cache-trap clause requiring the hashed asset name, a byte-identical cache-busted refetch, and a match against the deployed `origin/gh-pages` tree.
  </acceptance_criteria>
  <done>R7 is fully drafted with a bidirectional reachability proof grounded in 25-08's measurements, and it has not been run.</done>
</task>

<task type="auto">
  <name>Task 3: Draft R6a, R6b and R6c — one row per requirement — each with a bidirectional reachability proof</name>
  <files>.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</files>
  <read_first>
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (the "D-11 RED Observation Log" — the failing-direction evidence these rows cite; the "Wave 1 Integration Gate" — the numbers R6a and R6b reconcile against; R6's Round 1 verdict, including its three-clause table; and GAP-25-02's four numbered clauses)
    - src/compute-all-stats-steps.ts (the exported COMPUTE_ALL_STATS_STEPS array — the eight names and their order, which is R6c's independently-derived value)
    - src/index.ts (lines 289-340 — the per-step "> NAME" announcement, the success line, and the conditional DEGRADED STEPS summary)
    - .github/workflows/daily-refresh.yml (lines 1-45 for the push/schedule/workflow_dispatch triggers and the push `paths` list; lines 88-145 for the collapsed compute step, the test/verify gates, and the unguarded Commit and Deploy steps)
    - src/analytics/gear-aggregate-logic.test.ts (the eight FIX-02 regression case names R6a enumerates)
  </read_first>
  <action>
Draft rows R6a, R6b and R6c into the Round 2 section, each mapped to exactly one requirement, each
with a bidirectional reachability proof, all three with Verdict `pending`. Do not run them.

R6a — FIX-02's evidence, on the tree that is actually pushed.
  DISCRIMINATOR: all five gate commands exit 0 on the exact commit plan 25-11 pushes to
  `origin/master` — `npm test`, `npx tsc --noEmit`, `npm run build`, `npm run build-widgets`,
  `npm run verify-dashboard` — AND all eight FIX-02 regression cases are present and passing in
  `src/analytics/gear-aggregate-logic.test.ts`, enumerated by name.
  INDEPENDENTLY-DERIVED EXTENT (rule 2): the eight cases are D-12's four shapes — absent `gearName`
  key, `undefined`, empty string, non-string — times two functions, `buildGearAggregate` and
  `buildGearCoverage`. The row asserts eight named cases, not "the suite is green". It also
  reconciles the `npm test` tally against 62 files / 1596 tests; if merging `origin/master` changes
  the tally, every unit of delta must be attributed by name, with no unexplained residue, in the
  same form the Wave 1 Integration Gate used.
  D-13's half: `npx tsc --noEmit` exits 0 with `gearName` optional on the row type.
  CAN FAIL: cite this file's D-11 RED Observation Log verbatim — the eight failures observed against
  the unwidened predicate, including `TypeError: Cannot read properties of undefined (reading
  'toLowerCase')` at `gear-aggregate-logic.ts:43:6` and the four `expected 1 to be +0`
  `buildGearCoverage` assertions. The row also fails if any of the five commands exits non-zero or
  if the tally carries an unexplained delta.
  CAN PASS: the identical five-command gate already ran green on the wave-1/2 merged tree (this
  file's Wave 1 Integration Gate, three consecutive identical `npm test` tallies). Nothing about
  merging `origin/master`'s nightly data auto-commits is expected to change it.
  NOTE the row must state: this is a FRESH run on the pushed commit, not a citation of the wave-1
  numbers, because the tree changes when `origin/master` is merged in.

R6b — CI-02's evidence, on the same tree.
  DISCRIMINATOR: `npm run verify-dashboard` exits 0, its final line reads `56 check(s) passed, 0
  failure(s).` (or a higher passed count with `0 failure(s).`, if the shard sample count differs),
  AND each of the six by-name documents appears among the passing checks.
  INDEPENDENTLY-DERIVED EXTENT (rule 2): the six names come from D-09's list — weekly-distance.json,
  monthly-stats.json, yearly-stats.json, year-over-year.json, best-efforts.json, and one
  runtime-derived per-activity best-effort shard (D-10; no pinned ids) — and from the 40/40 total at
  the end of Phase 24 versus 56 now, i.e. 16 net-new checks. The row asserts the six names are
  individually present; a bare total is not sufficient, because a total can rise for unrelated
  reasons.
  CAN FAIL: cite the D-11 RED Observation Log's six break/restore cycles verbatim, each quoting the
  failure line naming its own document, for example `✗ /data/stats/weekly-distance.json expected a
  non-empty array, got an array of length 0` and `✗ GET /data/stats/best-efforts/i182358139.json
  expected 200, got 404`. Each assertion has been observed RED naming its own document, satisfying
  D-11.
  CAN PASS: the same command already returned `56 check(s) passed, 0 failure(s).` on the merged tree.

R6c — CI-01's evidence, which does not exist yet.
  DISCRIMINATOR, all four clauses required together (GAP-25-02's numbered contract):
    1. `origin/master` confirmed to carry the collapsed step, verified by reading the PUSHED copy:
       `git show origin/master:.github/workflows/daily-refresh.yml | grep -c "compute-all-stats
       --ci"` returns 1. At planning time it returned 0, and the local copy returned 1.
    2. A dispatched run id and its conclusion, quoted from `gh run view`. The conclusion must be
       `success`.
    3. The collapsed "Compute all statistics" step's own log excerpt from that run, carrying a
       "> NAME" line for each of the eight steps in `COMPUTE_ALL_STATS_STEPS` order —
       compute-stats, compute-advanced-stats, compute-geo-stats, compute-best-efforts,
       compute-age-grading, compute-dashboard-index, compute-gear-aggregate, compute-training-load —
       from ONE invocation where the workflow previously had eight hand-maintained steps, plus the
       "All statistics generated successfully!" line. The eight names are enumerated from
       `src/compute-all-stats-steps.ts`, NOT read off the log, per rule 2. State explicitly that the
       conditional "DEGRADED STEPS (N)" summary is NOT part of the pass condition — it prints only
       when a tolerated step failed (D-03) — but that if it does appear, it must name every degraded
       step and the row records that fact.
    4. The dispatch performed from a normal single-context execution with the developer's explicit
       authorisation, NOT from a parallel worktree and NOT opportunistically during a checkpoint.
       Evidence: the authorisation exchange quoted verbatim, and `git rev-parse --show-toplevel`
       shown to be the main checkout rather than a path under `.claude/worktrees/`.
  CAN FAIL, and concretely so today: the pushed copy currently has `grep -c` returning 0, so a
  dispatch performed right now would run the OLD eight-step workflow and its log would show no
  single collapsed invocation carrying all eight names. Clause 1 would fail and clause 3 would fail
  with it. The row also fails if the run's conclusion is not `success`, or if any one of the eight
  names is missing from the collapsed step's log. This is a currently-true failing state, not a
  hypothetical one — which is what makes the row non-vacuous.
  CAN PASS: local `master`'s `daily-refresh.yml:96-97` already carries the collapsed step and the
  `--ci` flag, `src/compute-all-stats-steps.ts` is unit-tested by 10 cases in
  `src/compute-all-stats-steps.test.ts`, `gh` 2.86.0 is installed and authenticated with `repo` and
  `workflow` scopes, and `workflow_dispatch` is already configured on the workflow
  (`daily-refresh.yml:37-43`). Once the push lands, the dispatch produces exactly this log.
  THE ROW MUST ALSO STATE, up front, that gathering its evidence has production side effects, so the
  running plan gates on an authorisation checkpoint: the push itself matches the workflow's own push
  trigger `paths` (`src/**`, `scripts/**`, `.github/workflows/**` among others), so pushing alone
  starts a production run; and `daily-refresh.yml`'s `Commit updated data and stats` and `Deploy
  widgets to GitHub Pages` steps carry no `github.ref` branch guard, so any run deploys to the live
  Pages site and commits data back to `origin`.
  </action>
  <verify>
    <automated>grep -c "^\*\*R6a \|^\*\*R6b \|^\*\*R6c " .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md; grep -c "compute-training-load" .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The Round 2 section contains three separate rows R6a, R6b and R6c, each naming exactly one requirement (FIX-02, CI-02, CI-01 respectively), each with Verdict `pending`.
    - R6a enumerates eight FIX-02 regression cases derived as four D-12 shapes times two functions, and requires a fresh five-command run on the pushed commit rather than a citation of the wave-1 numbers.
    - R6b enumerates the six document names from D-09/D-10 individually and states that a bare check total is not sufficient.
    - R6c lists all four GAP-25-02 clauses, including the `git show origin/master:...` grep returning 1 and the `git rev-parse --show-toplevel` non-worktree check.
    - R6c enumerates the eight compute step names in `COMPUTE_ALL_STATS_STEPS` order and states they are derived from `src/compute-all-stats-steps.ts` rather than read off the log.
    - R6c states that the conditional `DEGRADED STEPS` summary is not part of the pass condition.
    - Each of the three rows contains a CAN FAIL paragraph and a CAN PASS paragraph; R6a's and R6b's CAN FAIL paragraphs quote the D-11 RED Observation Log verbatim, and R6c's cites the currently-0 grep on the pushed workflow copy.
    - R6c states the production side effects of both the push and the dispatch, and that its evidence-gathering is gated on an authorisation checkpoint.
    - `grep -c "pending" .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md` is at least 4 after this task: no Round 2 row has been scored.
  </acceptance_criteria>
  <done>R6a, R6b and R6c are drafted as three independent one-requirement rows, each proved reachable in both directions, none of them run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A drafted row -> the requirement tick it will later authorise | A row written loosely today becomes a false tick tomorrow. This is the boundary the whole Checkpoint Row Discipline exists to police. |
| Evidence cited from a prior round -> evidence asserted about the tree that will actually be pushed | R6a and R6b could be written as citations of wave-1 numbers; the tree changes when `origin/master` is merged, so a citation would be an assertion about a different artefact than the one shipped. |
| The row author -> the row runner | These are different plan executions. Anything the runner has to infer is a place the row can drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-27 | Repudiation (row redesigned to yield a tick) | The R6a/R6b/R6c split | mitigate | The split is drafted in this plan, which completes and commits before plans 25-10 and 25-11 run anything. The preamble records the pledge that an unsatisfiable row is HALTED and gapped, never edited after its outcome is seen (Phase 24 R19/R26 precedent, and Round 1's own refusal to split R6 mid-round). |
| T-25-28 | Repudiation (vacuous row) | R7 and R6c | mitigate | Every row carries a CAN FAIL paragraph citing concrete evidence: R7 cites 25-08's stripped-bootstrap control sampling `rgb(255, 255, 255)`; R6a and R6b cite the D-11 RED Observation Log verbatim; R6c cites the currently-0 grep for `compute-all-stats --ci` on the pushed workflow copy, which is a failing state that is true right now. |
| T-25-29 | Repudiation (unpassable row) | R7 | mitigate | R7's CAN PASS paragraph must cite a measured production run with `beats_first_paint` true and its margin against 612 ms. If plan 25-08 HALTED, R7 is not drafted at all rather than drafted unpassably — the D-04 defect class, caught before a round is burned rather than after. |
| T-25-30 | Tampering (evidence asserts internal agreement) | R6b and R6c | mitigate | Extent is asserted against independently-derived values: the six document names from D-09/D-10 and the 40-to-56 check delta for R6b; the eight step names read from `src/compute-all-stats-steps.ts` for R6c. A bare total or a "log looks right" reading is explicitly insufficient. |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan; it modifies one Markdown file. `25-RESEARCH.md`'s Package Legitimacy Audit records that this phase installs no new external packages. |

Not applicable, one line each:
- **V2 Authentication / V3 Session Management / V4 Access Control / V5 Input Validation / V6 Cryptography** — this plan writes documentation; it touches no runtime code path, no input parser, and no credential.
</threat_model>

<verification>
- `25-VALIDATION.md` contains `## Round 2 Checkpoint (R6a, R6b, R6c, R7)` with a preamble, a one-row-per-requirement map, and four drafted rows (three if 25-08 HALTED).
- Every Round 2 row has a stated discriminator, an enumerated evidence requirement, a CAN FAIL paragraph and a CAN PASS paragraph.
- Every Round 2 Verdict cell reads `pending`; nothing was run or scored.
- R6c states the production side effects of the push and the dispatch and defers them to an authorisation checkpoint.
</verification>

<success_criteria>
- GAP-25-02's recommended R6a/R6b/R6c split exists on paper, drafted before it is run, so a missing item can withhold only its own requirement.
- R7 exists as a genuinely different row from R2 — grounded in a measured mechanism, provable in both directions — rather than a retry of the same capture.
- A different agent could run all four rows from this text alone, and could not accidentally score a post-first-paint frame, a bare check total, or a log-shaped impression as a PASS.
</success_criteria>

<output>
Create `.planning/phases/25-ci-hardening-light-theme-verification/25-09-SUMMARY.md` when done.
It MUST include: `## Rows drafted` (each row, its requirement, its discriminator in one line),
`## Reachability proofs` (the CAN FAIL and CAN PASS citation for each row), `## Disclosures carried`
(D-04 amendment, D-05 dark-OS, and the slowed-load disclosure if applicable), and
`## Not run` (an explicit statement that every verdict is still pending), and
`## Row baseline commit` — the output of `git rev-parse HEAD` taken IMMEDIATELY AFTER the commit
that writes the Round 2 rows into `25-VALIDATION.md`, recorded as a full 40-character sha on its own
line prefixed `ROW_BASELINE_SHA: `. Plans 25-10 and 25-11 diff against this exact sha to prove no row
was edited after seeing its outcome (Checkpoint Row Discipline rule 3); without it they have no
concrete ref and the anti-tamper check degrades into an unverifiable assertion.
`requirements-completed` MUST be empty — drafting a row ticks nothing.
</output>
