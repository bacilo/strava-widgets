---
phase: 25-ci-hardening-light-theme-verification
plan: 04
type: execute
wave: 2
depends_on: ["25-03"]
files_modified:
  - scripts/lib/curation-guard.mjs
  - scripts/lib/curation-guard.test.mjs
  - .planning/todos/pending/2026-09-02-wr19-curation-guard-directory-eacces.md
  - .planning/todos/completed/2026-09-02-wr19-curation-guard-directory-eacces.md
  - dist/widgets  # gitignored build tree; Task 2 plants and removes a mode-000 directory in it
autonomous: true
requirements: [CI-02]

must_haves:
  decisions: [D-11]
  truths:
    - "An unreadable (mode-000) directory under dist/widgets is reported as an attributed violation naming that directory, not thrown as an uncaught EACCES"
    - "The guard stays fail-CLOSED: the catch pushes a violation and returns without descending — it never swallows the error and continues as 'clean'"
    - "The directory-list failure message is worded in parallel with the existing file-read failure message, so a future reader recognises them as one defensive pattern applied twice"
    - "D-11 precedent: the new mode-000-directory fixture was observed RED against the unfixed guard (uncaught EACCES) before the fix landed"
    - "The new fixture restores the directory's mode before teardown, so a failed test cannot leave an unremovable directory behind"
    - "The WR-19 todo file is moved to .planning/todos/completed/ as part of this work"
  artifacts:
    - path: "scripts/lib/curation-guard.mjs"
      provides: "try/catch around walk()'s readdirSync, converting EACCES into a reported violation"
      contains: "could not be listed"
    - path: "scripts/lib/curation-guard.test.mjs"
      provides: "mode-000-directory fixture with its own chmod-restore afterEach"
      contains: "WR-19"
  key_links:
    - from: "scripts/lib/curation-guard.mjs walk()"
      to: "the violations array returned by findCurationArtifacts"
      via: "violations.push({ path: dir, reason: 'could not be listed (...)' }) then return"
      pattern: "could not be listed"
    - from: "scripts/lib/curation-guard.test.mjs"
      to: "scripts/lib/curation-guard.mjs"
      via: "findCurationArtifacts(tmpDir) over a planted mode-000 directory"
      pattern: "findCurationArtifacts"
---

<objective>
Close the folded todo WR-19: `findCurationArtifacts`'s `readdirSync` at
`scripts/lib/curation-guard.mjs:82-83` is unguarded, so a mode-000 directory under
`dist/widgets` throws an uncaught `EACCES` instead of a reported violation — the
directory-shaped sibling of the file case plan 24-15 fixed.

Purpose: this is literally CI hardening and sits in the guard family this phase already
touches. It fails CLOSED today (the build still aborts, nothing unsafe ships), so the cost is
operator experience: it reintroduces the unattributed `Widget build failed: EACCES` that 24-15
set out to replace.
Output: the same try/catch-to-violation shape applied at the directory-list site, a mode-000
directory fixture observed RED first, and the todo closed.

**Wave note:** this plan depends on 25-03 for one reason only — both plans plant and remove
fixtures in the same real `dist/widgets` tree, and Phase 24's Wave 7 found a cross-plan defect
of exactly that shape (24-11's whole-tree read racing 24-05's fixture planting; resolved with
`fileParallelism: false` in commit `e01ea8c`). There is no logical coupling between WR-19 and
CI-02; the ordering exists solely to keep the shared build tree single-writer.

**Traceability note:** this plan's `requirements: [CI-02]` frontmatter exists because the
frontmatter field cannot be empty and this is the closest requirement family (publish-gate
hardening). WR-19 is a folded todo, NOT part of CI-02. This plan does not discharge CI-02 and
MUST NOT tick it; `requirements-completed` in this plan's SUMMARY is empty. This mirrors the
Phase 24 precedent where plans 24-15 and 24-16 closed WR-14/WR-17 under `requirements: [CUR-01]`
with an empty `requirements-completed`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-RESEARCH.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-PATTERNS.md
@.planning/todos/pending/2026-09-02-wr19-curation-guard-directory-eacces.md

<interfaces>
<!-- The exact sibling pattern to mirror, and the exact fixture to clone. Do not re-derive. -->

The defect site — scripts/lib/curation-guard.mjs:82-83 (unguarded):
  function walk(dir) {
    for (const entry of readdirSync(dir, { withFileTypes: true })) {
      const entryPath = resolve(dir, entry.name);
      ...

The sibling pattern to mirror — the readFileSync case in the SAME file (lines 156-165, verbatim):
  let content;
  try {
    content = readFileSync(entryPath, 'latin1');
  } catch (error) {
    violations.push({
      path: entryPath,
      reason: `could not be read for scanning (${error.code ?? error.message}) — an unscannable file cannot be certified free of the "${CURATE_MARKER}" marker`,
    });
    continue;
  }

The fixture to clone — curation-guard.test.mjs:227-242, the WR-14 case (c) FILE analog (verbatim):
  it('(c) mode-000 regular file: reported via the read try/catch, citing EACCES (WR-14) — exercises the catch, not the isFile gate', () => {
    if (process.getuid?.() === 0 || process.platform === 'win32') {
      // root defeats mode bits; win32 has no POSIX chmod semantics — skip.
      return;
    }
    const target = path.join(tmpDir, 'wr14-secret.js');
    writeFileSync(target, 'x');
    chmodSync(target, 0o000);

    const violations = findCurationArtifacts(tmpDir);
    expect(
      violations.some(
        (v) => v.path === target && v.reason.includes('could not be read for scanning') && v.reason.includes('EACCES')
      )
    ).toBe(true);
  });

The cleanup hazard — the file's existing afterEach (lines 37-49, verbatim). The new fixture needs
its own analogous restore, with 0o700 (directories need the execute bit to be listable/removable),
not 0o600:
  afterEach(async () => {
    // WR-14 case (c) plants a mode-000 fixture. Recursive removal can itself
    // fail on some platforms unless the mode is restored first.
    const mode000Path = path.join(tmpDir, 'wr14-secret.js');
    if (existsSync(mode000Path)) {
      try {
        chmodSync(mode000Path, 0o600);
      } catch {
        // best-effort; fall through to rm below regardless
      }
    }
    await fs.rm(tmpDir, { recursive: true, force: true });
  });

The WR-14 describe block that hosts cases (a)-(e) spans curation-guard.test.mjs:181-275 and has
its own beforeEach/afterEach pair. The new directory fixture belongs in that block (or a new
sibling describe with its own pair) — NOT in the top `describe('findCurationArtifacts', ...)`
block, whose afterEach only restores the file-shaped `wr14-secret.js` path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the mode-000-directory fixture and observe it RED against the unfixed guard (D-11 precedent)</name>
  <files>scripts/lib/curation-guard.test.mjs</files>
  <read_first>
    - scripts/lib/curation-guard.test.mjs (whole file — the top-level beforeEach/afterEach at 33-49, the shared `writeFile` helper at 51-55 and 200-204, and the WR-14 describe block with cases (a)-(e) at 181-275)
    - scripts/lib/curation-guard.mjs (lines 40-110 — `walk()`, `CURATE_MARKER`, `CURATE_DIR_NAME`, and the docblock stating that unreadable entries are "reported as violations rather than read")
    - .planning/phases/25-ci-hardening-light-theme-verification/25-PATTERNS.md § 3 (the fixture clone instructions and the chmod-restore hazard)
    - .planning/todos/pending/2026-09-02-wr19-curation-guard-directory-eacces.md
  </read_first>
  <behavior>
    Given a directory `<tmpDir>/wr19-locked-dir` created with `mkdirSync`, containing one
    ordinary file planted BEFORE the chmod (so the EACCES demonstrably fires on listing, not on
    content), then `chmodSync(dir, 0o000)`:
    - `findCurationArtifacts(tmpDir)` returns normally — it does NOT throw.
    - The returned violations array contains an entry whose `path` is that directory and whose
      `reason` includes both the directory-list phrase and `EACCES`.
    - The guard remains fail-closed: a non-empty violations array is what aborts the build.
    Skipped when `process.getuid?.() === 0` or `process.platform === 'win32'`, matching case (c).
  </behavior>
  <action>
Do NOT touch `scripts/lib/curation-guard.mjs` in this task.

Add the directory-shaped sibling of WR-14 case (c) to `curation-guard.test.mjs`, inside the
WR-14 describe block at 181-275 (or a new sibling `describe('WR-19 — unreadable directories', ...)`
with its own `beforeEach`/`afterEach` pair). Name it so the failure output identifies it, e.g.
`'(f) mode-000 directory: reported via the readdir try/catch, citing EACCES (WR-19) — the directory-shaped sibling of case (c)'`.

Clone case (c) exactly, changing only the shape:
  - root/win32 skip guard as the FIRST line of the test body, with the same explanatory comment
  - `mkdirSync(target)` instead of `writeFileSync(target, 'x')`
  - plant one ordinary file inside the directory BEFORE the chmod (reuse the shared
    `writeFile(relativePath, contents)` helper) so the failure is provably on listing
  - `chmodSync(target, 0o000)` immediately after
  - assertion shape unchanged:
    `violations.some((v) => v.path === target && v.reason.includes('could not be listed') && v.reason.includes('EACCES'))`

Cleanup (MANDATORY — this is the hazard PATTERNS.md § 3 calls out explicitly): the hosting
describe's `afterEach` must restore the directory's mode with `chmodSync(dirPath, 0o700)` — note
`0o700`, not `0o600`, because a directory needs the execute bit to be listable and removable —
inside a `try {} catch {}` best-effort block guarded by `existsSync`, BEFORE
`await fs.rm(tmpDir, { recursive: true, force: true })`. Without this, a failing run leaves an
unremovable directory behind and poisons every subsequent run. Mirror the existing afterEach's
comment wording so the reason is legible.

Then run the file and RECORD THE RED. Per D-11's precedent (a guard only counts once observed
red), capture the exact pre-fix behaviour: expect the test to fail with an UNCAUGHT exception
whose text matches `EACCES: permission denied, scandir` — not a clean assertion failure. Quote
the verbatim vitest output in the SUMMARY, including the thrown error's message and the fact
that it is a throw rather than an assertion mismatch. That distinction is the whole point of
WR-19: the guard currently dies instead of reporting.

Also run the whole file to confirm the new fixture is the ONLY failure and that cases (a)-(e)
still pass — a cleanup bug in the new afterEach would surface as collateral failures.
  </action>
  <verify>
    <automated>npx vitest run scripts/lib/curation-guard.test.mjs 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/lib/curation-guard.test.mjs` exits NON-ZERO at the end of this task.
    - The recorded failure output contains the literal substring `EACCES` AND indicates a thrown error (`scandir` appears in the message), not a plain `expected true to be false` assertion mismatch. Quote it verbatim in the SUMMARY.
    - Exactly one test in the file fails — cases (a)-(e) and every other test in the file still pass. Quote the vitest tally line.
    - `grep -c "WR-19" scripts/lib/curation-guard.test.mjs` returns at least 1.
    - `grep -n "0o700" scripts/lib/curation-guard.test.mjs` shows the directory-mode restore in an `afterEach`.
    - `git diff --name-only` shows `scripts/lib/curation-guard.mjs` is NOT modified by this task.
    - After the run, `ls -la /tmp` contains no leftover mode-000 `vitest`/`curation-guard` temp directory (the afterEach restore worked even though the test failed). State the check performed.
  </acceptance_criteria>
  <done>The mode-000-directory fixture exists, was observed throwing an uncaught EACCES against the unfixed guard, and its cleanup does not leave an unremovable directory behind.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap walk()'s readdirSync in the sibling try/catch, keeping the guard fail-closed</name>
  <files>scripts/lib/curation-guard.mjs</files>
  <read_first>
    - scripts/lib/curation-guard.mjs (lines 40-110 for `walk()` and the constants; lines 150-170 for the verbatim `readFileSync` try/catch being mirrored; lines 43-47 for the docblock stating unreadable entries are "reported as violations rather than read")
    - scripts/lib/curation-guard.test.mjs (Task 1's fixture, which must go green)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-RESEARCH.md § "Pitfall 6" (why a catch-and-continue-as-clean fix would turn a fail-closed defect into a fail-open one)
  </read_first>
  <action>
Restructure `walk(dir)` (`scripts/lib/curation-guard.mjs:82-83`) to hoist the `readdirSync` out
of the `for...of` header and guard it, mirroring the file-read site's shape exactly:

  let entries;
  try {
    entries = readdirSync(dir, { withFileTypes: true });
  } catch (error) { ...push a violation..., return; }
  for (const entry of entries) { /* body unchanged */ }

The violation object's `reason` must be worded in parallel with the existing file-read message
so the two read as one defensive pattern applied twice:
  `could not be listed (${error.code ?? error.message}) — an unreadable directory cannot be certified free of the "${CURATE_MARKER}" marker`
(the file-read site says `could not be read for scanning (...) — an unscannable file cannot be
certified free of the "..." marker`). Use `path: dir`.

FAIL-CLOSED IS NON-NEGOTIABLE (RESEARCH Pitfall 6). The catch MUST `violations.push(...)` and
then `return` (stop descending into a directory it cannot list). It must NEVER swallow the error
and continue as if the directory were clean — that would convert a fail-closed defect into a
fail-open hole, which is strictly worse than the bug being fixed. Do not add a `console.warn`
in place of the push.

Do not change: `SCANNED_EXTENSIONS`/`UNSCANNED_EXTENSIONS`, the `CURATE_DIR_NAME` /
`.curate-dist` directory-name checks, the `entry.isFile()` gate added by 24-15, the content
scan, or `assertNoCurationArtifacts`'s abort behaviour. Extend the module docblock's existing
"reported as violations rather than read" sentence to cover the directory-list case too, citing
WR-19.

Then run the fixture and confirm it flips to green, and run the full file plus a whole-tree
scan against the real `dist/widgets` to confirm nothing regressed.
  </action>
  <verify>
    <automated>npx vitest run scripts/lib/curation-guard.test.mjs && npm run build-widgets</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/lib/curation-guard.test.mjs` exits 0 with every case including the new WR-19 fixture passing. Quote the tally line and compare it to Task 1's.
    - `grep -c "could not be listed" scripts/lib/curation-guard.mjs` returns 1, and it is inside `walk()`.
    - `grep -n -A3 "could not be listed" scripts/lib/curation-guard.mjs` shows a `return;` (or `continue;`) after the push — prove the guard stops descending rather than proceeding.
    - `grep -c "readdirSync(dir" scripts/lib/curation-guard.mjs` returns 1 and it is NOT inside a `for (` header any more.
    - `npm run build-widgets` exits 0 and prints the existing green `✓ Curation-artifact scan` line over the real `dist/widgets` tree (22 `.d.ts` files still scanned, zero violations) — proving the restructure did not change whole-tree behaviour.
    - Fail-open negative check, recorded in the SUMMARY: plant a mode-000 directory under `dist/widgets`, run `npm run build-widgets`, and show it exits NON-ZERO with `✗ Curation-artifact guard failed:` naming that directory (not an unattributed `Widget build failed: EACCES`). Then `chmod 0700` and remove it, and show `npm run build-widgets` back to exit 0. This is the operator-experience improvement WR-19 exists for; quote both outputs.
  </acceptance_criteria>
  <done>The directory-list site reports an attributed violation, the guard is still fail-closed, and the whole-tree scan is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Close the WR-19 todo</name>
  <files>.planning/todos/pending/2026-09-02-wr19-curation-guard-directory-eacces.md, .planning/todos/completed/2026-09-02-wr19-curation-guard-directory-eacces.md</files>
  <read_first>
    - .planning/todos/pending/2026-09-02-wr19-curation-guard-directory-eacces.md (the full todo, including the "Also open from the same review" trailer naming IN-17 and IN-18)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md § "Deferred Ideas" (IN-17 and IN-18 are explicitly NOT folded)
    - .planning/todos/completed/ (confirm the naming convention used by already-closed todos)
  </read_first>
  <action>
Move `.planning/todos/pending/2026-09-02-wr19-curation-guard-directory-eacces.md` to
`.planning/todos/completed/` with `git mv`, preserving the filename. Before moving, append a
dated resolution section to the file body recording: the plan that closed it (25-04), the commit
or file:line of the fix in `curation-guard.mjs`, the name of the new fixture in
`curation-guard.test.mjs`, and the verbatim pre-fix RED observation from Task 1.

CRITICAL — the todo's trailer lists IN-17 and IN-18 from the same wave-9 review, and
CONTEXT.md's Deferred Ideas section says both stay pending and are explicitly NOT folded into
this phase. Moving this file must not silently close them. Before the move, extract that trailer
into a NEW pending todo file
`.planning/todos/pending/2026-09-02-in17-in18-curation-guard-cosmetics.md` carrying the same
frontmatter shape (`created`, `source: 24-REVIEW.md § Wave 9 Review`, `severity: info`,
`area: scripts/lib/curation-guard.mjs`) and both items' text verbatim, plus one line noting they
were deliberately deferred at Phase 25 planning per `25-CONTEXT.md` § Deferred Ideas. Then remove
the trailer from the WR-19 file so the completed file is about WR-19 only.
  </action>
  <verify>
    <automated>test ! -f .planning/todos/pending/2026-09-02-wr19-curation-guard-directory-eacces.md && test -f .planning/todos/completed/2026-09-02-wr19-curation-guard-directory-eacces.md && test -f .planning/todos/pending/2026-09-02-in17-in18-curation-guard-cosmetics.md && grep -q "IN-17" .planning/todos/pending/2026-09-02-in17-in18-curation-guard-cosmetics.md && echo TODO_STATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/todos/pending/ | grep -c wr19` returns 0.
    - `ls .planning/todos/completed/ | grep -c wr19` returns 1.
    - `git log --follow --oneline -1 -- .planning/todos/completed/2026-09-02-wr19-curation-guard-directory-eacces.md` shows the move was made with `git mv` (history preserved), not delete-and-recreate.
    - The completed file contains a dated resolution section naming plan `25-04`, the fixed `curation-guard.mjs` site, the new fixture name, and the verbatim pre-fix RED text.
    - `grep -c "IN-17\|IN-18" .planning/todos/completed/2026-09-02-wr19-curation-guard-directory-eacces.md` returns 0 — the deferred items did not ride along into completed.
    - `.planning/todos/pending/2026-09-02-in17-in18-curation-guard-cosmetics.md` exists and quotes both IN-17 and IN-18 verbatim plus the deferral note citing `25-CONTEXT.md` § Deferred Ideas.
  </acceptance_criteria>
  <done>WR-19 is closed in `completed/`, and IN-17/IN-18 remain open in `pending/` as their own todo, per CONTEXT.md's Deferred Ideas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem entries under `dist/widgets` → `findCurationArtifacts` | The guard walks a build-output tree whose entries it does not control. A hostile or accidentally-broken entry (mode-000 directory, symlink, FIFO) must not be able to change the guard's verdict. |
| `findCurationArtifacts` → `assertNoCurationArtifacts` → `build-widgets` exit code | A non-empty violations array is the only thing that aborts the publish build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Tampering / Repudiation | `walk()`'s unguarded `readdirSync` (`curation-guard.mjs:82-83`) | mitigate | ASVS V1 Architecture (fail-closed error handling): wrap the call and convert the exception into a REPORTED violation carrying `path: dir` and `error.code`, then `return` without descending. Today the throw escapes as an unattributed `Widget build failed: EACCES` — safe but unattributable, so an operator cannot tell a permissions accident from a real curation-artifact leak. |
| T-25-10 | Elevation of privilege (fail-open regression) | The fix itself | mitigate | The catch must never swallow-and-continue-as-clean, which would convert a fail-CLOSED defect into a fail-OPEN hole — strictly worse than the bug. Pinned by an acceptance criterion that greps for the `return` after the push, and by a negative check that plants a mode-000 directory under the real `dist/widgets` and requires `npm run build-widgets` to exit non-zero naming it. A test asserting only "does not throw" is explicitly insufficient. |
| T-25-11 | Denial of service (self-inflicted) | The new mode-000 fixture's teardown | mitigate | The fixture's `afterEach` restores `0o700` before `fs.rm(..., { recursive: true })`; without it a failed run leaves an unremovable directory that poisons every subsequent run. The existing file-shaped fixture already carries this hazard note at `curation-guard.test.mjs:37-49`. |

Not applicable (one line each):
- **V2 Authentication** — no auth surface; this is a build-time filesystem walk.
- **V3 Session Management** — no sessions.
- **V4 Access Control** — the curate server's Origin/Host gating (Phase 24) is untouched; WR-19 is availability and attribution, not access control.
- **V5 Input Validation** — no parsed user input; the input is directory entries, handled by the try/catch above.
- **V6 Cryptography** — none touched.

**Supply chain:** no `npm install` in this plan; no new package is introduced anywhere in Phase 25, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `npx vitest run scripts/lib/curation-guard.test.mjs` exits 0 with the new WR-19 fixture green and cases (a)-(e) unregressed.
- `npm run build-widgets` exits 0 over the real `dist/widgets` tree, printing the green `✓ Curation-artifact scan` line.
- The planted-mode-000-directory negative check exits non-zero with `✗ Curation-artifact guard failed:` naming that directory, and returns to exit 0 after cleanup.
- `.planning/todos/pending/` no longer lists WR-19; `.planning/todos/completed/` does; IN-17/IN-18 survive as their own pending todo.
</verification>

<success_criteria>
- An unreadable directory under `dist/widgets` produces an attributed violation instead of an uncaught `EACCES`, and the guard remains fail-closed.
- The new fixture was observed RED (uncaught throw) before the fix, per this repo's D-11 precedent.
- The WR-19 todo is closed and IN-17/IN-18 remain deferred, as CONTEXT.md requires.
</success_criteria>

<output>
Create `.planning/phases/25-ci-hardening-light-theme-verification/25-04-SUMMARY.md` when done.
`requirements-completed` MUST be empty — this plan closes a folded todo, not CI-02.
It MUST include: `## D-11 RED observation` (Task 1's verbatim pre-fix throw, with the `scandir`
text), and `## Fail-closed proof` (the planted-directory `build-widgets` exit codes and output
lines, before and after cleanup).
</output>
