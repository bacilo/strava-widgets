---
phase: 22-calendar-week-start-totals
plan: 05
type: execute
wave: 5
depends_on: ["22-04"]
files_modified:
  - .planning/phases/22-calendar-week-start-totals/22-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CAL-01, CAL-02, CAL-03]
must_haves:
  decisions: [D-02, D-03, D-04, D-10, D-15, D-16]
  truths:
    - "D-16: the checkpoint READS VALUES BACK from the live archive, it does not confirm presence. October 2025 is the discriminator month — exactly one Sunday-dated activity (Oct 19, 24.0 km) — and both the Sunday-start and Monday-start week-total tables are written into the agenda verbatim so the developer reads numbers against a stated expectation"
    - "D-16: the tell is rows 3 and 4 turning from `56.0 km / 5h 27m / 4 runs` and `104.1 km / 10h 14m / 7 runs` into `80.0 km / 7h 58m / 5 runs` and `80.0 km / 7h 42m / 6 runs`. A toggle that repaints without re-grouping leaves 56.0/104.1 in place; the two Monday rows landing on the SAME 80.0 km with DIFFERENT run counts (5 vs 6) is the second tell"
    - "The known rounding artifact is disclosed in the preamble so it is not misreported as a bug: Monday-start rows display-sum to 357.2 km against the month total's displayed 357.3 km, from independent per-row `toFixed(1)`. Unrounded metres reconcile exactly at 357.349 km under BOTH week starts"
    - "D-03: the first-load re-flow to Monday-first is a named row, observed consciously as expected behaviour rather than silently absorbed — the 19-CONTEXT D-08 hover-change precedent"
    - "D-02: `.calendar-header`'s `align-items: baseline` across five mixed-height controls is a named row — the flagged visual snag"
    - "D-10: eight columns at a narrow viewport is a named row — RESEARCH's one MEDIUM-confidence recommendation (A4), with the `.splits-scroll`-style wrapper as the documented fallback if it fails"
    - "D-04: focus staying on the pressed segmented button, and an open day picker being cleared, are observed rows — no unit test in this repository can see either"
    - "T-22-WK-01 is observed, not just unit-tested: a devtools-tampered `'MONDAY'` value renders Monday-first with no console error AND is not rewritten by the app (D-07 forbids repair)"
    - "D-15: the agenda states that Trends, biggest-week and streak surfaces stay Monday-fixed by design, so the checkpoint does not open a row against them and a verifier does not read the inconsistency as a gap"
    - "House rule: no automated result is cited as evidence for any row. Every row has a human observer and quotes what was actually rendered"
  artifacts:
    - path: ".planning/phases/22-calendar-week-start-totals/22-VALIDATION.md"
      provides: "a Round 1 section — served-URL preamble, cache-trap paragraph, rounding-artifact disclosure, both October 2025 tables, an eleven-row agenda, and the recorded outcome"
      contains: "R22-VERDICT"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CAL-01, CAL-02 and CAL-03 ticked only if their full row maps passed"
      contains: "CAL-01"
  key_links:
    - from: "the served build at 127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10"
      to: "rows R3 and R4"
      via: "the developer reading five week-total rows back per week start"
      pattern: "80.0 km"
    - from: "each answered row"
      to: "a REQUIREMENTS.md checkbox"
      via: "the Row-to-requirement map"
      pattern: "R22-VERDICT"
---

<objective>
Run the phase's mandatory human browser checkpoint — ROADMAP success criterion 5 — and record the
result.

Purpose: `vitest.config.ts` is `environment: 'node'` with no jsdom, and PROJECT.md line 49 records
that automated gates have missed rendering defects in this project three times. Criterion 2's grid
math is the only thing this phase can discharge by unit test. Criteria 1, 3, 4 and 5 all have a
mandatory human-observation component, and D-16 requires the checkpoint to read VALUES back rather
than confirm presence — "the numbers changed" is not proof, because a wrong-grouping bug also
changes the numbers.

Output: a Round 1 section in `22-VALIDATION.md` carrying eleven answered rows with verdicts, and
CAL-01/CAL-02/CAL-03 ticked in `REQUIREMENTS.md` only where their row maps passed.

**No staged fixture is needed or permitted.** Unlike Phase 21, the live archive supplies the
discriminator directly: October 2025 has exactly one Sunday-dated run. Nothing in `dist/` or `data/`
may be edited for this checkpoint.

**Explicitly NOT in scope (D-15).** No row is opened against Trends' weekly volume,
`records-logic.ts`'s biggest week, or the streak logic. Those read the Monday-fixed `weekStartISO`
from the pipeline and are deliberately untouched by this phase. A user who selects Sunday will still
see Monday-based weeks there; that is knowingly shipped, and a verifier must score this phase against
the Calendar view alone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md
@.planning/phases/22-calendar-week-start-totals/22-RESEARCH.md
@.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md
@.planning/phases/21-overview-rebuild/21-07-PLAN.md
@.planning/phases/21-overview-rebuild/21-08-PLAN.md
</context>

<house_rules>
Carried forward from checkpoint 16-09 and every v2.1 phase since. These bind Task 2.

1. **Never cite an automated result as evidence for a manual row.** A green `npm test`, a source
   grep, or an agent's own DOM read is not an answer to a row whose observer is the developer's eyes.
2. **Present rows ONE AT A TIME**, in order, quoting each row's instructions including its
   `Required detail:` and `Observer required:` clauses.
3. **Read values back, do not confirm presence.** A row answered "the totals are there" is not
   answered. Quote the rendered text.
4. **Record the developer's own words.** Do not summarise, do not merge answers, do not fill a cell
   with what the implementation ought to produce.
5. **Do not fix anything during the session.** A failing row is recorded and the session continues to
   the next row; no source file may be edited. `git status --porcelain src scripts` must be empty at
   the end.
6. **R1 gates everything.** If R1 is not PASS, every subsequent row is recorded BLOCKED naming R1.
7. **Do not write a failure's wrong values into a PASS cell.** Task 3's checks treat that as a
   contradiction.
</house_rules>

<cache_trap>
`127.0.0.1` alone is NOT sufficient. This project has been bitten twice: Phase 21 Round 1's R13 only
passed after a hard reload cleared a stale cached `streaks.json`, and the staged-build trap recurs
with a stale `index.html` / `index.json` in the observing tab.

Every checkpoint session must: browse `127.0.0.1`, never `localhost`; serve under the
`/strava-widgets` project path, never the server root; and hard-reload (Cmd+Shift+R, or DevTools open
with "Disable cache" ticked, then reload) before judging any row. R1 exists solely to record that
this happened.

This phase adds a second cache surface: `localStorage`. Rows R2 and R8 require a specific storage
state, so each names the exact devtools command that establishes it, and each is followed by a hard
reload before it is judged.
</cache_trap>

<discriminator_data>
Copied verbatim from `22-RESEARCH.md` § D-16 Discriminator Month and independently recomputed against
the committed `data/dashboard/index.json` (1,868 activities, `generatedAt: 2026-08-12`) during
planning. These tables go into the agenda unaltered.

**Why October 2025 qualifies:** exactly one activity in the month falls on a Sunday — Oct 19, 2025,
"Morning Run", 24.0 km, raw moving time 2h 31m 37s (9,097 s). October 1, 2025 is a Wednesday
(`getUTCDay() === 3`), so leading padding is 3 under Sunday-start and 2 under Monday-start. That
single Sunday activity moves from ending one week row to starting the next, changing exactly two
adjacent rows — the cleanest possible single-variable discriminator.

### Sunday-start week totals (October 2025)

| Row | Days (Oct) | Distance | Time | Runs |
|-----|------------|----------|------|------|
| 1 | 1–4 | 59.1 km | 5h 42m | 5 |
| 2 | 5–11 | 80.0 km | 7h 53m | 6 |
| **3** | **12–18** | **56.0 km** | **5h 27m** | **4** |
| **4** | **19–25** | **104.1 km** | **10h 14m** | **7** |
| 5 | 26–31 | 58.1 km | 5h 32m | 5 |

### Monday-start week totals (October 2025)

| Row | Days (Oct) | Distance | Time | Runs |
|-----|------------|----------|------|------|
| 1 | 1–5 | 59.1 km | 5h 42m | 5 |
| 2 | 6–12 | 80.0 km | 7h 53m | 6 |
| **3** | **13–19** | **80.0 km** | **7h 58m** | **5** |
| **4** | **20–26** | **80.0 km** | **7h 42m** | **6** |
| 5 | 27–31 | 58.1 km | 5h 32m | 5 |

**The tell.** Toggling Sunday → Monday must turn rows 3 and 4 from `56.0 km / 5h 27m / 4 runs` and
`104.1 km / 10h 14m / 7 runs` into `80.0 km / 7h 58m / 5 runs` and `80.0 km / 7h 42m / 6 runs`. A
toggle that re-paints the grid without actually re-grouping the weeks leaves 56.0 / 104.1 in place.
The two Monday rows landing on the *same* 80.0 km display value while carrying *different* run counts
(5 vs 6) is the second tell against a partially-correct regrouping.

**Known rounding artifact — NOT a bug.** The month header renders `357.3 km` under both week starts
(`monthTotalM` does not depend on the week start). The Sunday-start rows display-sum to 357.3 km; the
Monday-start rows display-sum to **357.2 km**, 0.1 km lower, purely from independent per-row
`toFixed(1)`. The exact unrounded metres are identical under both groupings at 357.349 km. Do not
record this as a computation error, and do not ask the developer to verify that the rows sum to the
header.

**Rest-week month (D-12), also read from the live archive during planning:** June 2025 under
Monday-start has a full zero-run week covering **June 16–22** (7 days, no runs), and its first row is
**June 1 alone** (1 day, no runs). Both must render the en-dash `–` with no time line and no `×N`
line.
</discriminator_data>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the full gate, serve a production-shaped build under /strava-widgets, and stage the eleven-row Round 1 agenda</name>
  <files>.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` — the existing Test
      Infrastructure, Sampling Rate, Per-Task Verification Map, Wave 0 Requirements and Manual-Only
      Verifications sections. The Round 1 section is appended below them; nothing above is rewritten
      except the Per-Task Verification Map's Status column.
    - `.planning/phases/21-overview-rebuild/21-07-PLAN.md` lines 140-200 — the precedent for the gate
      command, the symlink staging under `/tmp/gh-pages/strava-widgets`, the
      `python3 -m http.server 8099 --directory /tmp/gh-pages` serve step, and the preamble contents.
    - `.planning/phases/21-overview-rebuild/21-08-PLAN.md` lines 260-340 — the precedent for the
      agenda row shape (five columns, one `Required detail:` and one `Observer required:` clause per
      row) and for the automated staging checks.
    - This plan's `<discriminator_data>`, `<cache_trap>` and `<house_rules>` blocks — all three are
      copied into the Round 1 preamble.
    - `.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md` D-02, D-03, D-04, D-10, D-12,
      D-16 — the wording each row is measured against.
  </read_first>
  <action>
    First re-run the full phase gate and fail the task if any part is not clean: `npm test`,
    `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets` (whose output must contain no
    `css-syntax-error`), and `npm run verify-dashboard`.

    Then stage the build the way GitHub Pages serves it, under the `/strava-widgets` project path and
    never at the server root: `mkdir -p /tmp/gh-pages` and
    `ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets`, then assert
    `/tmp/gh-pages/strava-widgets/index.html` exists and that `readlink` on the symlink returns an
    absolute path. Serve with `python3 -m http.server 8099 --directory /tmp/gh-pages` in the
    background and confirm `http://127.0.0.1:8099/strava-widgets/index.html` responds 200 and that
    `http://127.0.0.1:8099/strava-widgets/data/dashboard/index.json`, fetched over HTTP rather than
    read from disk, responds 200. Capture the bundle's `assets/index-*.js` filename — the agenda
    quotes it so a later reader knows which build was observed.

    Do NOT edit anything under `dist/` or `data/`. This checkpoint uses organic archive data; there
    is no fixture.

    Then append a `## Round 1` section to `22-VALIDATION.md` containing, in this order:

    A **preamble** stating: the served URL prefix `http://127.0.0.1:8099/strava-widgets/`, the one
    route this session uses (`http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10`), the
    observed bundle filename, and the four gate commands with their results. Then the whole
    `<cache_trap>` paragraph including its `localStorage` note, the whole `<house_rules>` list, the
    whole `<discriminator_data>` block with BOTH October 2025 tables verbatim, the tell, the rounding
    artifact, and the June 2025 rest-week facts. Then one sentence recording the D-15 scope fence:
    Trends, biggest-week and streak surfaces stay Monday-fixed by design and no row is opened against
    them.

    Then an **eleven-row agenda table** with columns `Row | Requirement | Instructions | Observation |
    Verdict`. Each Instructions cell ends with its own `Required detail:` clause naming exactly what
    must be quoted back, and an `Observer required:` clause naming the developer's own eyes. Leave
    every Observation cell empty and every Verdict cell empty — zero `R22-VERDICT` tokens may exist
    before the checkpoint.

    | Row | Requirement | What it asks |
    |-----|-------------|--------------|
    | R1 | precondition | The URL bar reads `127.0.0.1` and includes `/strava-widgets/`; a hard reload was performed and the method is named; the bundle filename matches the one in the preamble. **Required detail:** the URL as typed, the reload method, the `assets/index-*.js` filename |
    | R2 | CAL-01, D-03 | Run `localStorage.removeItem('dashboard-calendar-week-start')` in devtools, hard-reload, then read back the seven weekday headings left to right plus the eighth heading, and say which of the two segmented options is the filled one. Expected `Mon Tue Wed Thu Fri Sat Sun` then `Total`, with `Monday` filled. **Also confirm explicitly** that the calendar now starting weeks on Monday is understood as the intended D-03 change and not a regression. **Required detail:** the eight heading texts in order, and which option is filled |
    | R3 | CAL-02, D-16 | On `#/calendar?month=2025-10` under Monday-start, read back all five week-total cells: day range implied by the row, distance, time and run count. Compare against the Monday-start table. **Required detail:** five triples of distance / time / run count, quoted as rendered |
    | R4 | CAL-02, D-16 | Click `Sunday`. Read back all five week-total cells again. Expected the Sunday-start table. Rows 3 and 4 must now read `56.0 km / 5h 27m / 4 runs` and `104.1 km / 10h 14m / 7 runs`; still reading `80.0` on both is the failure the discriminator exists to expose and must be recorded FAIL with the numbers quoted. **Required detail:** five triples, and an explicit statement of what rows 3 and 4 read |
    | R5 | CAL-01, D-04 | Tab to a segmented option and activate it with the keyboard. Confirm focus stays on the option just pressed (the focus ring is still on it; optionally confirm `document.activeElement` in the console). Then open a multi-run day's picker, toggle the week start, and confirm the picker panel is cleared rather than left beside the reorganised grid. **Required detail:** where the focus ring was immediately after activation, and what happened to the open picker |
    | R6 | CAL-02, D-12 | Navigate to `#/calendar?month=2025-06` under Monday-start. Read back the total cell for the week covering June 16–22 and the total cell for the first row (June 1 alone). Expected: an en-dash `–` only, with no time line and no `×N` line, in both. **Required detail:** exactly what text appears in each of those two cells |
    | R7 | CAL-01 | With `Sunday` selected, hard-reload the page. Confirm `Sunday` is still the filled option and the weekday row still begins with `Sun`. **Required detail:** which option is filled after the reload, and the first weekday heading |
    | R8 | CAL-01, T-22-WK-01 | In devtools run `localStorage.setItem('dashboard-calendar-week-start','MONDAY')`, hard-reload, and confirm three things: the calendar renders Monday-first, the console shows no error or warning from the app, and `localStorage.getItem('dashboard-calendar-week-start')` STILL returns `'MONDAY'` — the app must not repair or rewrite a tampered key (D-07). **Required detail:** the first weekday heading, the console state, and the value the getItem call returned |
    | R9 | CAL-03, D-02 | Look along `.calendar-header`: the month total, the two month-nav buttons, the `Jump to month` input and the segmented group. Confirm they sit on a coherent baseline and nothing is visibly misaligned or overlapping, in BOTH light and dark themes. **Required detail:** a description of the alignment and any control that sits noticeably off |
    | R10 | CAL-03 | On the segmented control confirm all four of: hover feedback on the inactive option, the two-tone `:focus-visible` ring when keyboard-focused, sufficient contrast between the active and inactive options, and that all of it holds in BOTH themes. **Required detail:** one sentence per item, per theme |
    | R11 | CAL-02, D-10 | Narrow the viewport to roughly 380px (or use a phone emulation preset). Confirm the eight-column grid does not crush the day columns into illegibility and does not overflow the panel. **Required detail:** what happens to the day columns and the total column at that width, and whether anything overflows |

    Then a `### Row-to-requirement map (Round 1)` listing which rows gate which requirement:
    CAL-01 → R2, R5, R7, R8; CAL-02 → R3, R4, R6, R11; CAL-03 → R9, R10. R1 gates all of them.

    Set the file's frontmatter `round: 1` and a `round1_staged` date. Leave `nyquist_compliant: false`
    and `status: draft` until Task 3.

    Finally, fill in the existing **Per-Task Verification Map**'s `Task ID` / `Plan` / `Wave` columns
    with the concrete assignments made during planning, and set the Status column for the four
    automated rows to ✅ (they are green as of this task's gate run):

    | Existing row | Task ID | Plan | Wave |
    |---|---|---|---|
    | CAL-01 / T-22-WK-01 allow-list, unit | 22-02-T1 | 22-02 | 2 |
    | CAL-01 / T-22-WK-02 throwing storage, unit | 22-02-T1 | 22-02 | 2 |
    | CAL-02 / leading padding both week starts, unit | 22-01-T1, 22-01-T2 | 22-01 | 1 |
    | CAL-02 / week-total derivation, unit | 22-01-T2 | 22-01 | 1 |
    | CAL-01 / control renders, toggles, survives reload, manual | 22-05-T2 (rows R2, R5, R7, R8) | 22-05 | 5 |
    | CAL-02 / total cell renders with correct boundary numbers, manual | 22-05-T2 (rows R3, R4, R6, R11) | 22-05 | 5 |
    | CAL-03 / Phase 19 styling in both themes, manual | 22-05-T2 (rows R9, R10) | 22-05 | 5 |

    Add one row to that map for the new pure-helper coverage: CAL-02, no threat ref, "the week-total
    duration formatter and accessible-name builder are pinned to archive-derived values", unit,
    `npx vitest run src/dashboard/views/calendar.test.ts`, task `22-03-T1`, wave 3.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json && rm -f /tmp/bw22.log && npm run build-widgets > /tmp/bw22.log 2>&1; BW=$?; cat /tmp/bw22.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw22.log && npm run verify-dashboard && mkdir -p /tmp/gh-pages && ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets && test -f /tmp/gh-pages/strava-widgets/index.html && readlink /tmp/gh-pages/strava-widgets | grep -q '^/' && curl -sf -o /dev/null http://127.0.0.1:8099/strava-widgets/index.html && curl -sf -o /dev/null http://127.0.0.1:8099/strava-widgets/data/dashboard/index.json && test -z "$(git status --porcelain src scripts data dist)" && node --input-type=module -e 'import {readFileSync} from "node:fs"; const p=".planning/phases/22-calendar-week-start-totals/22-VALIDATION.md"; const s=readFileSync(p,"utf8"); if((s.match(/## Round 1/g)||[]).length!==1) throw new Error("expected exactly one Round 1 heading"); const t=s.split("## Round 1")[1]; const ids=["R1","R2","R3","R4","R5","R6","R7","R8","R9","R10","R11"]; const rows=t.split("\n").filter(l=>l.startsWith("|")).filter(r=>ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(r))); if(rows.length!==11) throw new Error("expected 11 agenda rows, found "+rows.length); ids.forEach((id,i)=>{ if(!new RegExp("^\\|\\s*"+id+"\\.").test(rows[i])) throw new Error("agenda row "+(i+1)+" should begin with "+id+"."); if(rows[i].split("|").length<6) throw new Error("row "+id+" does not have five columns"); }); const rd=(t.match(/Required detail/g)||[]).length; if(rd!==11) throw new Error("expected one Required detail clause per row, found "+rd); const ob=(t.match(/Observer required/g)||[]).length; if(ob!==11) throw new Error("expected one Observer required clause per row, found "+ob); if((t.match(/R22-VERDICT/g)||[]).length!==0) throw new Error("verdict tokens must be zero before the checkpoint"); const pre=t.split("| Row")[0]; for(const n of ["127.0.0.1:8099/strava-widgets/","2025-10","59.1 km","80.0 km","56.0 km","104.1 km","5h 27m","10h 14m","7h 58m","7h 42m","357.2 km","357.349","2025-06","June 16","dashboard-calendar-week-start","MONDAY"]) if(!pre.includes(n)) throw new Error("the Round 1 preamble is missing: "+n); if(!/hard reload/i.test(pre)) throw new Error("the preamble does not carry the hard-reload cache-trap paragraph"); if(!/D-15/.test(pre)) throw new Error("the preamble does not carry the D-15 scope fence"); if(/localhost:8099/.test(s)) throw new Error("localhost:8099 appears - D-16 requires 127.0.0.1"); if(!/Row-to-requirement map \(Round 1\)/.test(t)) throw new Error("the row-to-requirement map is missing"); const fm=s.split("---")[1]||""; if(!/round: 1/.test(fm)) throw new Error("frontmatter round is not 1"); if(!/round1_staged/.test(fm)) throw new Error("round1_staged date missing"); if(!/nyquist_compliant: false/.test(fm)) throw new Error("nyquist_compliant must stay false until the checkpoint is answered"); if(/TBD/.test(s.split("## Round 1")[0])) throw new Error("the Per-Task Verification Map still contains TBD task ids"); console.log("11 staged rows, 0 verdicts, no fixture, 127.0.0.1 enforced, task ids assigned");'</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets` (no `css-syntax-error`
      in its output) and `npm run verify-dashboard` all exit 0.
    - `/tmp/gh-pages/strava-widgets/index.html` exists, `readlink` on the symlink returns an absolute
      path, and both `http://127.0.0.1:8099/strava-widgets/index.html` and
      `.../data/dashboard/index.json` respond 200 over HTTP.
    - `git status --porcelain src scripts data dist` is empty — no fixture was staged and no source
      was touched.
    - `22-VALIDATION.md` has exactly one `## Round 1` heading with exactly eleven agenda rows R1..R11
      in order, five columns each, one `Required detail:` and one `Observer required:` clause per row,
      and **zero** `R22-VERDICT` tokens.
    - The preamble contains the served URL prefix, `2025-10`, every distance and time value from both
      October tables, `357.2 km`, `357.349`, the June 2025 rest-week facts, the storage key
      `dashboard-calendar-week-start`, the tampered value `MONDAY`, a hard-reload paragraph, and the
      D-15 scope fence.
    - The string `localhost:8099` appears nowhere in the file.
    - Frontmatter has `round: 1` and a `round1_staged` date; `nyquist_compliant` is still `false`.
    - The Per-Task Verification Map contains no remaining `TBD` in its Task ID or Plan columns.
  </acceptance_criteria>
  <done>A clean, production-shaped build is served from 127.0.0.1 under /strava-widgets with organic archive data, and eleven unanswered rows are staged with both discriminator tables in the preamble.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] The eleven-row browser checkpoint — read the October 2025 values back under both week starts</name>
  <what-built>
    Plans 22-01 through 22-04 shipped the whole phase: `buildMonthGrid` now takes a required
    `weekStart` and derives per-week totals over in-month days only; a calendar-scoped
    `calendar-preferences.ts` persists the choice under `dashboard-calendar-week-start` with an
    allow-list parse and try/catch on both storage paths; `calendar.ts` renders a week-start-aware
    weekday row, a `Total` header and a non-focusable, screen-reader-named total cell after every
    week row; and a `.segmented` Sunday/Monday control in `.calendar-header` persists the choice and
    rebuilds the grid in place without moving focus.

    Task 1 re-ran the full gate and served that build under `/strava-widgets` on 127.0.0.1 with
    **organic archive data — no fixture of any kind was staged.**

    What no automated gate in this repository can see: whether the grid actually re-flows, whether
    the totals actually render, whether the numbers are the RIGHT numbers, whether focus stayed put,
    whether the control inherits Phase 19's styling, whether the header baseline holds, or whether
    eight columns survive a phone. `vitest.config.ts` is `environment: 'node'` with no jsdom and
    there is no headless browser in this project.
  </what-built>
  <how-to-verify>
    Open `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10`. **Confirm the URL bar reads
    `127.0.0.1`, not `localhost`, and includes `/strava-widgets/`, before judging any row.**

    **Then hard-reload before judging anything** (Cmd+Shift+R, or DevTools open with "Disable cache"
    ticked, then reload). R1 exists solely to record that this happened and which build was served.
    R1 must be PASS before any other row may be judged; if it is not, record every remaining row
    BLOCKED naming R1 and stop.

    Present the eleven rows from `22-VALIDATION.md`'s Round 1 table **one at a time**, in R1..R11
    order, following every rule in this plan's `<house_rules>` block. For each row record the
    developer's own words in the Observation cell together with one
    `R22-VERDICT: PASS|FAIL|BLOCKED|NOT EXERCISABLE` token, the row's own required detail, and its
    observer attribution.

    R3 and R4 are the D-16 discriminator pair and are not answerable by "the totals are there". Read
    the five distance / time / run-count triples back verbatim under each week start. The tell is
    rows 3 and 4 turning from `80.0 km / 7h 58m / 5 runs` and `80.0 km / 7h 42m / 6 runs` under
    Monday into `56.0 km / 5h 27m / 4 runs` and `104.1 km / 10h 14m / 7 runs` under Sunday. Rows that
    do not change are the wrong-grouping failure the discriminator exists to expose.

    Do not let the developer chase the rounding artifact: the Monday-start rows display-sum to
    357.2 km against the header's 357.3 km, and that is expected per the preamble. Do not open a row
    about it and do not record it as a defect.

    Rows R2 and R8 each require a specific `localStorage` state set from devtools, followed by a hard
    reload, before they are judged. The exact commands are in the agenda cells.
  </how-to-verify>
  <action>
    Present the eleven Round 1 rows from
    `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` to the developer ONE AT A TIME,
    in R1..R11 order, quoting each row's Instructions cell including its `Required detail:` and
    `Observer required:` clauses.

    Before R1, confirm with the developer that the URL bar reads
    `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10` — not `localhost`, not a bare
    server root — and ask them to perform a hard reload, naming the method used.

    For R2, ask them to run `localStorage.removeItem('dashboard-calendar-week-start')` in the
    devtools console and hard-reload before reading the weekday headings back. For R8, ask them to
    run `localStorage.setItem('dashboard-calendar-week-start','MONDAY')` and hard-reload, then to
    read `localStorage.getItem('dashboard-calendar-week-start')` back after the page has rendered —
    a value that has changed means the app repaired the key, which D-07 forbids, and is a FAIL.

    For R3 and R4, ask for the five triples as rendered, not as summarised. If a row's numbers match
    neither table, record FAIL and quote what was seen. Do not compute the expected values on the
    developer's behalf, do not read the DOM yourself as a substitute, and do not accept "looks right".

    For R6, the two June 2025 cells must contain an en-dash and nothing else. A cell reading
    `0.0 km` or `0h 0m` or `×0` is a D-12 FAIL and the exact text must be quoted.

    For R11, if the narrow-viewport result is unacceptable, record FAIL with what was seen and note
    that the documented fallback is the `.splits-scroll`-style horizontal-scroll wrapper (DISC-6b) —
    but **do not implement it during this session.** No source file may be edited while the
    checkpoint is running.

    When writing a PASS observation, quote only what was actually rendered. Do not write a failure's
    wrong values (`56.0` and `104.1` in a Monday-start cell, or vice versa) into a PASS cell — Task
    3's automated check treats their presence in a PASS cell as a contradiction and will reject it.

    Capture each answer in the developer's own words and hold them for Task 3, which writes them into
    the Observation cells. Do not summarise, do not merge answers, do not substitute an agent DOM
    read for a row whose observer is the developer's own eyes, and do not edit any source file if a
    row fails.
  </action>
  <read_first>
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Round 1 — the staged agenda
      and the preamble Task 1 wrote, including both October 2025 tables, the tell, the rounding
      artifact, the June 2025 rest-week facts and the D-15 scope fence.
    - This plan's `<house_rules>`, `<cache_trap>` and `<discriminator_data>` blocks.
    - `.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md` D-16 — why reading a value back
      is the requirement and why "the numbers changed" is not proof.
    - `.planning/PROJECT.md` line 49 — the house rule this whole task exists to honour.
  </read_first>
  <verify>
    <human-check>All eleven rows have an answer from the developer, each with the row's own required detail and a named observer; the URL bar was confirmed to read 127.0.0.1 and /strava-widgets/ and a hard reload was performed before R1 was judged; no row after R1 was judged unless R1 was PASS; no source file was edited during the session.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer confirmed the URL bar reads
      `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10` before R1 was judged.
    - A hard reload was performed and the method used is named in R1's answer.
    - All eleven rows have an answer, or an explicit decline / BLOCKED with the reason.
    - R3 and R4 each quote five distance / time / run-count triples as rendered.
    - R2 and R8 each name the devtools command that was run and the hard reload that followed.
    - R8's answer states the value `localStorage.getItem('dashboard-calendar-week-start')` returned
      after the reload.
    - R6's answer quotes the exact text of both June 2025 cells.
    - Every row after R1 was judged only because R1 was PASS; otherwise those rows read BLOCKED
      naming R1.
    - No row outside R1..R11 was asked; in particular no row was opened against Trends, biggest week
      or streaks (D-15).
    - No source file was edited during the session: `git status --porcelain src scripts` is empty.
  </acceptance_criteria>
  <done>Eleven developer answers are in hand, each attributable to a named observer, with the rendered week-total values quoted verbatim under both week starts.</done>
  <resume-signal>Per-row verdicts recorded. Type "done" when all eleven rows have an answer, or name the rows you are declining.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the Round 1 outcome and gate CAL-01, CAL-02 and CAL-03 on their row maps</name>
  <files>.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Round 1 as answered in Task
      2, its `Row-to-requirement map (Round 1)`, and the Per-Task Verification Map whose manual-only
      Status cells this task updates.
    - `.planning/phases/21-overview-rebuild/21-08-PLAN.md` Task 3 — the precedent for the three
      recording sections and for gating `REQUIREMENTS.md` strictly on the row map rather than on an
      overall impression.
    - `.planning/REQUIREMENTS.md` lines 33-35 (CAL-01, CAL-02, CAL-03 entries) and lines 88-90
      (`| CAL-01 | Phase 22 | Pending |` and its two siblings in the status table).
    - `.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md` D-15 — the wording to reuse when
      recording that the Trends/biggest-week/streak inconsistency is knowingly shipped.
  </read_first>
  <action>
    Write the eleven verdicts into the Round 1 Observation cells in the developer's own words, then
    add three sections below the Round 1 table:

    - `## Checkpoint Outcome (Round 1)` — the count of PASS / FAIL / BLOCKED / NOT EXERCISABLE across
      R1..R11, and one sentence per non-PASS row naming what was not established. No fix, no
      root-cause theory, no remediation plan — a gap-closure round is a separate decision.
    - `## Known and Accepted` — the rounding artifact (Monday-start rows display-sum to 357.2 km vs
      the header's 357.3 km; unrounded metres reconcile at 357.349 km under both week starts), and
      the D-15 scope fence stated as knowingly shipped: a user who selects Sunday still sees
      Monday-based weeks on Trends, in the biggest-week figure and in the streak logic, because those
      read the pipeline's Monday-fixed `weekStartISO`. A verifier must not score either as a gap.
    - `## Settled Discretion (phase 22)` — a short table recording the nine choices the planner
      settled so a later reader does not reopen them: the storage key
      `dashboard-calendar-week-start`; the `Xh Ym` round-to-nearest-minute duration format; wrapping
      both storage paths in try/catch; the Monday-first weekday label rotation plus the `Total`
      header; `aria-label="Week start"` with `Sunday`/`Monday` option text; the
      `repeat(7, 1fr) auto` eighth track with 380px compaction and the horizontal-scroll wrapper as
      documented fallback; clearing `pickerHost` on toggle; not adding `monthTotalTimeSec`; and the
      `.sr-only` accessible-name mechanism instead of `aria-label` on a role-less `div`.

    Update the Per-Task Verification Map's three manual-only Status cells with the verdict of their
    mapped rows (✅ only if every mapped row is PASS; otherwise ❌ or ⚠️ with the failing row named).

    Then update `.planning/REQUIREMENTS.md`, strictly on the row map and nothing else:
    - CAL-01 is ticked only if R2, R5, R7 and R8 are ALL PASS.
    - CAL-02 is ticked only if R3, R4, R6 and R11 are ALL PASS.
    - CAL-03 is ticked only if R9 and R10 are ALL PASS.
    - Any requirement whose rows did not all pass stays `[ ]` with a one-line note naming the failing
      rows and pointing at `22-VALIDATION.md` Round 1.
    - Update the matching status-table rows at lines 88-90 from `Pending` to `Complete` for exactly
      the requirements that were ticked, leaving the others `Pending`.
    - Append to each ticked requirement's line a `**Closed 2026-08-18 (plan 22-05, Round 1)**` note
      naming the rows that closed it and what was read back, following the wording style already used
      by the OVR-03 and OVR-04 entries.

    Finally set the file's frontmatter: `status: complete` and `nyquist_compliant: true` only if
    every one of R1..R11 is PASS; otherwise `status: partial` and `nyquist_compliant: false`, and add
    a `round1_answered` date either way.

    Do not touch `ROADMAP.md` — closing the phase checkbox belongs to verification and
    `/gsd-progress`, not to this plan.
  </action>
  <verify>
    <automated>node --input-type=module -e 'import {readFileSync} from "node:fs"; const p=".planning/phases/22-calendar-week-start-totals/22-VALIDATION.md"; const s=readFileSync(p,"utf8"); const t=s.split("## Round 1")[1]; const ids=["R1","R2","R3","R4","R5","R6","R7","R8","R9","R10","R11"]; const rows=t.split("\n").filter(l=>l.startsWith("|")).filter(r=>ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(r))); if(rows.length!==11) throw new Error("expected 11 agenda rows, found "+rows.length); const v=(t.match(/R22-VERDICT/g)||[]).length; if(v!==11) throw new Error("expected exactly 11 verdicts, found "+v); for(const r of rows){ if(!/R22-VERDICT:\s*(PASS|FAIL|BLOCKED|NOT EXERCISABLE)/.test(r)) throw new Error("a row has no well-formed verdict: "+r.slice(0,40)); const cells=r.split("|"); if(cells.length<6) throw new Error("a row lost a column"); const obs=cells[4]||""; if(obs.trim().length<10) throw new Error("an Observation cell is empty or near-empty: "+r.slice(0,40)); } const pass=rows.filter(r=>/R22-VERDICT:\s*PASS/.test(r)); for(const r of pass){ const id=(r.match(/^\|\s*(R\d+)\./)||[])[1]; if((id==="R3") && /56\.0/.test(r)) throw new Error("R3 PASS cell quotes a Sunday-start value"); if((id==="R4") && /7h 58m/.test(r)) throw new Error("R4 PASS cell quotes a Monday-start value"); } for(const h of ["## Checkpoint Outcome (Round 1)","## Known and Accepted","## Settled Discretion (phase 22)"]) if(!s.includes(h)) throw new Error("missing section: "+h); if(!/357\.349/.test(s.split("## Known and Accepted")[1].split("##")[0])) throw new Error("Known and Accepted does not record the rounding artifact"); if(!/D-15/.test(s.split("## Known and Accepted")[1].split("##")[0])) throw new Error("Known and Accepted does not record the D-15 fence"); const fm=s.split("---")[1]||""; if(!/round1_answered/.test(fm)) throw new Error("round1_answered date missing"); const all=rows.every(r=>/R22-VERDICT:\s*PASS/.test(r)); if(all && !/nyquist_compliant: true/.test(fm)) throw new Error("all rows PASS but nyquist_compliant is not true"); if(!all && !/nyquist_compliant: false/.test(fm)) throw new Error("not all rows PASS but nyquist_compliant is true"); const req=readFileSync(".planning/REQUIREMENTS.md","utf8"); const map={ "CAL-01":["R2","R5","R7","R8"], "CAL-02":["R3","R4","R6","R11"], "CAL-03":["R9","R10"] }; for(const [id,need] of Object.entries(map)){ const ok=need.every(n=>rows.some(r=>new RegExp("^\\|\\s*"+n+"\\.").test(r)&&/R22-VERDICT:\s*PASS/.test(r))); const line=req.split("\n").find(l=>l.includes("**"+id+"**")); if(!line) throw new Error("no requirement line for "+id); const ticked=line.startsWith("- [x]"); if(ticked!==ok) throw new Error(id+" tick state ("+ticked+") does not match its row map ("+ok+")"); const st=req.split("\n").find(l=>l.startsWith("| "+id+" ")); if(!st) throw new Error("no status row for "+id); if(ok && !/Complete/.test(st)) throw new Error(id+" status row is not Complete"); if(!ok && !/Pending/.test(st)) throw new Error(id+" status row is not Pending"); } console.log("11 verdicts recorded, three sections present, requirement ticks match their row maps");' && test -z "$(git status --porcelain src scripts)" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `22-VALIDATION.md` Round 1 has exactly eleven `R22-VERDICT` tokens, one per row, each a
      well-formed `PASS` / `FAIL` / `BLOCKED` / `NOT EXERCISABLE`, and no Observation cell is empty.
    - No PASS cell for R3 quotes a Sunday-start-only value, and no PASS cell for R4 quotes a
      Monday-start-only value — the contradiction guard.
    - The three new sections exist; `## Known and Accepted` records both the 357.349 km rounding
      artifact and the D-15 fence.
    - `## Settled Discretion (phase 22)` lists all nine settled choices.
    - Each of CAL-01, CAL-02 and CAL-03 is ticked in `REQUIREMENTS.md` if and only if every row in
      its map passed, and its status-table row reads `Complete` in exactly the same cases.
    - Frontmatter carries `round1_answered`, and `nyquist_compliant: true` if and only if all eleven
      rows passed.
    - `ROADMAP.md` is not modified by this plan.
    - `git status --porcelain src scripts` is empty and `npm test` exits 0.
  </acceptance_criteria>
  <done>Round 1 is recorded verbatim, the rounding artifact and the D-15 fence are on record as accepted, the nine settled discretion choices are documented, and each Calendar requirement is ticked strictly on its row map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer devtools → `localStorage` | Rows R2 and R8 deliberately cross this boundary to exercise the tamper path in a real browser |
| local HTTP server → browser | A loopback-only static server on 127.0.0.1 serving the built bundle and the already-public published JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-WK-01 | Tampering | the persisted week start | mitigate (observed) | Row R8 exercises the allow-list in a real browser: a devtools-written `'MONDAY'` must render Monday-first, produce no console error, and NOT be repaired by the app. This is the only place in the phase where the tamper mitigation is observed rather than unit-simulated |
| T-22-VER-01 | Repudiation | the checkpoint record itself | mitigate | Every row carries a named observer and the developer's own words; automated results are explicitly barred as evidence for a manual row (`<house_rules>` 1). Task 3's contradiction guard rejects a PASS cell quoting the other week start's values, so a mis-transcribed answer cannot silently pass |
| T-22-VER-02 | Tampering | the observed build | mitigate | Task 1 asserts `git status --porcelain src scripts data dist` is empty, so no fixture or source edit can be present. The bundle's `assets/index-*.js` filename is recorded in the preamble and confirmed by R1, so the artifact the developer looked at is identified. R1 also gates on a hard reload, closing the stale-cache trap that has bitten this project twice |
| T-22-SRV-01 | Information disclosure | the local static server | accept | `python3 -m http.server` serves `/tmp/gh-pages` on loopback only, and its contents are the published dashboard bundle plus already-public activity JSON. The server is a transient checkpoint fixture, not a deployed artifact |
| T-22-SC | Tampering | npm/pip/cargo installs | accept (not applicable) | Zero new packages in this phase; RESEARCH.md § Package Legitimacy Audit records "Not applicable". No install task exists in any phase-22 plan |
</threat_model>

<verification>
- Task 1's staging check proves eleven rows are staged with zero verdicts, both discriminator tables
  in the preamble, no fixture, and `127.0.0.1` enforced.
- Task 2's human-check proves every row has a developer answer with its required detail.
- Task 3's recording check proves eleven verdicts, non-empty observations, the contradiction guard,
  the three sections, and that each requirement's tick matches its row map exactly.

This is the ONLY plan in phase 22 that discharges a visual or interaction claim. Plans 22-01 through
22-04 each state explicitly that they do not.
</verification>

<success_criteria>
- Eleven rows asked one at a time, answered by the developer, recorded verbatim with verdicts.
- The October 2025 week totals were read back under both week starts and compared against tables
  computed from the live archive — not merely confirmed present.
- The rounding artifact and the D-15 scope fence are recorded as knowingly accepted.
- CAL-01, CAL-02 and CAL-03 are ticked strictly on their row maps.
- No source file was edited and no fixture was staged during the checkpoint.
</success_criteria>

<output>
Create `.planning/phases/22-calendar-week-start-totals/22-05-SUMMARY.md` when done.
</output>
