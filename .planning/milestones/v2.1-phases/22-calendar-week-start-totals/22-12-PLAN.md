---
phase: 22-calendar-week-start-totals
plan: 12
type: execute
wave: 11
depends_on: ["22-09", "22-11"]
gap_closure: true
round: 3
files_modified:
  - .planning/phases/22-calendar-week-start-totals/22-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CAL-01, CAL-02, CAL-03]
must_haves:
  decisions: [D-03, D-10, D-12, D-15, D-16, GC-4, GC-5, GC-6]
  truths:
    - "GC-6: `22-VALIDATION.md` is APPEND-ONLY. Round 3 adds a `## Round 3` section and rows R18..R23; the Round 1 (R1-R11) and Round 2 (R12-R17) sections, their verdicts and their frontmatter dates are not rewritten. The file ends with 23 `R22-VERDICT` tokens (11 + 6 + 6)"
    - "GC-6 / NO WAIVER, HARD FAIL: house rule 1 CANNOT be waived for the two decisive rows R19 (the R13/R11 re-ask) and R22 (the blocked-site-data browser configuration). Rounds 1 and 2 both recorded PASSes under mid-session waivers and `22-VERIFICATION.md` scored that as a real weakening of the evidence. If an agent observes R19 or R22, that row is recorded **BLOCKED**, never PASS — and the executor's checkpoint prompt must refuse the observation rather than perform it"
    - "GC-6 / R22 IS MANDATORY AND MAY NOT BE DECLINED. It was declined in Round 2 ('decline it') and the blocked-site-data path has never been exercised in a real browser this phase. If the developer declines it, the row is recorded BLOCKED and Gap 2 is recorded STILL OPEN — it may not be recorded as informational, and it may not be closed on static analysis or on R15's isolated devtools evidence"
    - "GC-6: R22's expected outcome CHANGES this round because the app-wide guard landed. With blocked site data the app must BOOT: nav renders, the Calendar route renders its grid AND its five week-total cells, the week start defaults to Monday, and the console carries no application error. A blank page (no nav, no view) is a FAIL of the Gap 2 fix, not a `NOT EXERCISABLE`; the generic `Something went wrong` panel is also a FAIL. There is no `NOT EXERCISABLE` disposition for R22 this round"
    - "R19 states a PIXEL WIDTH. Round 2's R13 could not state one (the agent's resize attempt left `matchMedia('(max-width: 380px)').matches` false and the row reverted to the developer without a stated width), which is why its FAIL was hard to compare against R11's. R19's PASS cell must carry the width used, down to at least 380px, and quoted rendered text from at least three distance-carrying day cells and one week-total cell, in BOTH themes"
    - "The staged build is proven FRESH before any row is judged — the served JS filename differs from BOTH Round 1's `assets/index-YqJHQsHW.js` AND Round 2's `assets/index-Dlom2BM3.js`, the served CSS filename differs from Round 2's `assets/index-aaEmW9us.css`, and the served CSS carries the Round 3 declarations (`minmax(0, max-content)`, a single-column `grid-template-areas` and `overflow-wrap` inside a 380px block). Every rendering row is judged only after a cache-bypassing hard reload"
    - "D-16 discipline holds in Round 3: row R20 READS THE OCTOBER 2025 MONDAY-START WEEK TOTALS BACK against the table recorded in the Round 1 preamble, so a regression introduced by the BL-01/BL-02 CSS or the app-wide storage refactor cannot hide behind `the totals are there`"
    - "R23 is the regression row the app-wide storage refactor requires: theme persistence and week-start persistence must both still work NORMALLY, with storage unblocked, after the refactor moved six call sites onto a shared resolver"
    - "No source file is edited during the checkpoint session; `git status --porcelain src scripts` is empty at the end"
    - "Requirement ticks follow the Round 3 row map exactly and nothing else: CAL-02 ticks only if R18, R19 and R20 are ALL PASS; CAL-01's existing tick survives only if R23 is PASS (Phase 19's UI-01/UI-02 precedent for reverting a ticked requirement when a gap-closure row fails); CAL-03 is confirm-unregressed by R21 and is not re-gated. `.planning/REQUIREMENTS.md` is not pre-ticked"
  artifacts:
    - path: ".planning/phases/22-calendar-week-start-totals/22-VALIDATION.md"
      provides: "an appended `## Round 3` section — freshness preamble, house rules incl. the non-waivable clause, a six-row agenda R18..R23, the recorded outcome and a Gap Closure Record"
      contains: "## Round 3"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CAL-01/CAL-02/CAL-03 states matching the Round 3 row map"
      contains: "CAL-02"
  key_links:
    - from: "the re-served build at 127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10"
      to: "row R19"
      via: "the developer narrowing to a stated width down to at least 380px and reading day-cell values back"
      pattern: "R22-VERDICT"
    - from: "a real browser-level blocked-site-data setting"
      to: "row R22"
      via: "a full reload of the app with the setting active"
      pattern: "R22-VERDICT"
    - from: "each answered Round 3 row"
      to: "a REQUIREMENTS.md checkbox"
      via: "the Row-to-requirement map (Round 3)"
      pattern: "R22-VERDICT"
---

<objective>
Run the **blocking Round 3 browser checkpoint** and record the result. This is the plan that decides
whether Phase 22's two remaining gaps are actually closed, after two rounds that did not close them.

Purpose: `22-VERIFICATION.md` returns `gaps_found` at 4/7. Gap 1 (CAL-02, the ~380px day-cell
overflow) has now FAILED TWICE by the developer's own eyes — R11 in Round 1, R13 in Round 2 against
a provably fresh, fixed build. Gap 2's fix is locally sound but its documentation overclaimed, and
the one row that would have exercised the real browser configuration (R16) was declined in Round 2
and has never been run. Plans `22-09`, `22-10` and `22-11` wrote the fixes; only a human looking at
this build can close them.

Output: a `## Round 3` section in `22-VALIDATION.md` carrying six answered rows R18..R23 with
verdicts, a Gap Closure Record dispositioning both `22-VERIFICATION.md` gaps, and `REQUIREMENTS.md`
updated strictly on the Round 3 row map.

**No staged fixture is needed or permitted.** The archive supplies everything: October 2025's
Monday-start week totals are already recorded verbatim in the Round 1 preamble and are re-read at
R20 as a regression check. Nothing under `dist/` or `data/` may be edited.

**Explicitly NOT in scope (D-15).** No row is opened against Trends' weekly volume,
`records-logic.ts`'s biggest week, or the streak logic — those read the pipeline's Monday-fixed
`weekStartISO` and are deliberately untouched by this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md
@.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md
@.planning/phases/22-calendar-week-start-totals/22-REVIEW.md
@.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md
@.planning/phases/22-calendar-week-start-totals/22-08-PLAN.md
@.planning/phases/22-calendar-week-start-totals/22-05-PLAN.md
</context>

<house_rules>
Carried forward from `22-08-PLAN.md` (and from checkpoint 16-09 before it), with three Round 3
additions that are **not negotiable mid-session**. These bind Task 2.

1. **Never cite an automated result as evidence for a manual row.** A green `npm test`, a source
   grep, or an agent's own DOM read is not an answer to a row whose observer is the developer's eyes.
2. **Present rows ONE AT A TIME**, in order, quoting each row's instructions including its
   `Required detail:` and `Observer required:` clauses in full.
3. **Read values back, do not confirm presence.** A row answered "the totals are there" is not
   answered. Quote the rendered text.
4. **Record the developer's own words.** Do not summarise, do not merge answers, do not fill a cell
   with what the implementation ought to produce.
5. **Do not fix anything during the session.** A failing row is recorded and the session continues to
   the next row; no source file may be edited. `git status --porcelain src scripts` must be empty at
   the end.
6. **R18 gates everything.** If R18 is not PASS, every subsequent row is recorded BLOCKED naming R18.
7. **Do not write a failure's wrong values into a PASS cell.** Task 3's checks treat that as a
   contradiction.
8. **Four-state verdict vocabulary:** `PASS` / `FAIL` / `BLOCKED` / `NOT EXERCISABLE`.
   `NOT EXERCISABLE` is for a row whose claim could not be reached because something outside this
   round's scope failed first. It is not a soft FAIL and it is not a PASS. **R22 has no
   `NOT EXERCISABLE` disposition this round** — see rule 12.
9. **Restore the browser.** R22 changes a browser-wide privacy setting. It must be reverted before
   the session ends, and the revert confirmed by reloading the Calendar and seeing the week-start
   preference work again (that confirmation is R23).
10. **(Round 3) The waiver is not carried forward AND cannot be granted for R19 or R22.** Round 1
    waived house rule 1 for R6/R7/R8/R10; Round 2's preamble stated the waiver was NOT pre-granted
    and the developer re-granted it mid-session for R12/R14/R15/R17 anyway. `22-VERIFICATION.md`
    scored both as a real weakening of the evidence behind ticked requirements. For Round 3:
    - **R19 and R22 are non-waivable.** If the developer offers or asks for a waiver on either, the
      executor **declines**, records the request and the developer's words verbatim in that row's
      Observation cell, and records the row **BLOCKED** — never PASS, never FAIL. An agent-observed
      result on those rows is not evidence.
    - For R18, R20, R21 and R23 a mid-session waiver remains the developer's to grant. It is recorded
      verbatim in that row's Observation cell and written into the Checkpoint Outcome as a stated
      deviation.
11. **(Round 3) The executor must not offer to observe R19 or R22 on the developer's behalf**, must
    not resize a window or change a browser setting to observe them, and must not fill either
    Observation cell from a DOM read, a computed-style dump, a screenshot it took itself, or static
    analysis. The only admissible content in those two cells is the developer's own reported
    observation.
12. **(Round 3) R22 is mandatory and may not be declined.** It was declined in Round 2 ("decline
    it"). If it is declined again, the row is recorded **BLOCKED**, `Gap 2` is recorded **STILL
    OPEN** in the Gap Closure Record, and the Checkpoint Outcome states plainly that the app-level
    blocked-site-data claim remains unobserved for a third round. It may not be recorded as
    informational and it may not be closed on R15's isolated devtools evidence or on source reading.
13. **(Round 3) Hard-reload before judging any rendering row.** `127.0.0.1` alone is not sufficient
    — see `<cache_trap>`. Every rendering row is judged only after a cache-bypassing reload, and R18
    exists to record that it happened against a build whose filename is neither Round 1's nor
    Round 2's.
</house_rules>

<cache_trap>
`127.0.0.1` alone is NOT sufficient. This project has been bitten repeatedly: Phase 21 Round 1's R13
only passed after a hard reload cleared a stale cached `streaks.json`, and the staged-build trap
recurs with a stale `index.html` / `index.json` in the observing tab.

Every checkpoint session must: browse `127.0.0.1`, never `localhost`; serve under the
`/strava-widgets` project path, never the server root; and hard-reload (Cmd+Shift+R, Cmd+Option+R in
Safari, or DevTools open with "Disable cache" ticked, then reload) before judging any row.

**Round 3's build-freshness bar is the strictest yet, because two builds have already been observed.**
Round 1 was judged against `assets/index-YqJHQsHW.js`. Round 2 was judged against
`assets/index-Dlom2BM3.js` and `assets/index-aaEmW9us.css`. If Round 3 is judged against either, the
Round 3 fixes are not in the observed artifact and every verdict is worthless. Task 1 asserts the
served JS filename differs from BOTH, that the served CSS filename differs from Round 2's, and that
the served CSS actually carries the Round 3 declarations — `minmax(0, max-content)`, a
single-column `grid-template-areas` and an `overflow-wrap` declaration, all inside a
`max-width: 380px` block. R18 then makes the developer read the filename off the page after a hard
reload and compare it against the preamble.

The `localStorage` surface persists too: R22 requires a specific browser-level storage state and R23
requires it restored, and each is judged only after a reload.
</cache_trap>

<round_carryover>
Facts the Round 3 agenda quotes. All are already recorded in `22-VALIDATION.md`'s Round 1 and Round 2
sections and are restated here so the executor does not re-derive them.

**The failing row this round re-asks, twice over.**
Round 1 R11 (FAIL), developer's words: *"Total column values seem fine. Day cell values can slightly
overflow once width to narrow (not a big issue for me, can just be documented if not fixable very
quickly)"* and *"behavior similar in light and dark theme"*. They were explicitly offered a
documented-PASS framing and chose FAIL.
Round 2 R13 (FAIL), the re-ask against the fixed build, developer's words: *"Days still overflow.
Total colum remains wide (wider than any other column) and all text fits. But other columns (day
columns) become narrow and distance text overflows."* No width was stated on that row, because the
agent's attempted narrow-viewport method never engaged the media query
(`matchMedia('(max-width: 380px)').matches` returned `false`) and the row reverted to the developer.
**R19 must state a width.**

**The row that was declined and has never been run.** Round 2 R16 (BLOCKED), developer's word:
*"decline it"*. Nothing has been established in a real browser about the browser-configuration-level
blocked-storage path in this phase. `22-REVIEW.md` BL-03 predicted a blank page from static
analysis; that prediction has never been observed.

**October 2025, Monday-start week totals** (recomputed from the live archive during Round 1 planning;
`data/dashboard/index.json`, 1,868 activities):

| Wk | Days (Oct) | Distance | Time | Runs |
|-----|------------|----------|------|------|
| 1 | 1–5 | 59.1 km | 5h 42m | 5 |
| 2 | 6–12 | 80.0 km | 7h 53m | 6 |
| 3 | 13–19 | 80.0 km | 7h 58m | 5 |
| 4 | 20–26 | 80.0 km | 7h 42m | 6 |
| 5 | 27–31 | 58.1 km | 5h 32m | 5 |

**Known rounding artifact — NOT a bug.** The month header renders `357.3 km`; the Monday-start rows
display-sum to `357.2 km`, purely from independent per-row `toFixed(1)`. Unrounded metres reconcile
at 357.349 km under both week starts. Do not open a row about it and do not record it as a defect.

**The Sunday-start values, for the contradiction guard only:** rows 3 and 4 read `56.0 km / 5h 27m /
4 runs` and `104.1 km / 10h 14m / 7 runs` under Sunday. R20 is a Monday-start row; those values must
not appear in its PASS cell.

**Bundles already observed:** Round 1 `assets/index-YqJHQsHW.js`; Round 2
`assets/index-Dlom2BM3.js` + `assets/index-aaEmW9us.css`.
</round_carryover>

<what_this_round_ships>
Quoted into the Round 3 preamble so the developer knows what they are looking at.

**Plan 22-09 (GC-4, closing `22-REVIEW.md` BL-01 + BL-02 together).** The `@media (max-width: 380px)`
calendar block now also declares:
`.calendar-grid { grid-template-columns: repeat(7, minmax(0, 1fr)) minmax(0, max-content) }` — so
the eighth (Total) track can finally be squeezed instead of holding a hard content floor that the
seven day columns paid for;
`.calendar-week-total { min-width: 0; white-space: normal; overflow-wrap: anywhere }` — so
`10h 14m` may wrap rather than pinning the track wide;
`.calendar-day { grid-template-areas: "number" "distance" "count"; grid-template-columns: 1fr }` plus
`justify-self: start` on all three children — so the distance value gets the cell's FULL width
instead of a centred third of it; and `.calendar-day__distance { overflow-wrap: anywhere }` so a
numeric token that still cannot fit breaks rather than spilling past the border. The `line-height:
1.5` that made the total cell taller at this breakpoint (WR-07) was dropped. The eight-column
contract is unchanged at the default breakpoint — only the 380px track SIZING changed.
**The `.splits-scroll`-style horizontal-scroll wrapper (DISC-6b) is STILL deliberately not
implemented** and remains the documented fallback if this round fails too.

**Plans 22-10 and 22-11 (GC-5, closing `22-REVIEW.md` BL-03 app-wide — a locked user decision).**
A new `src/dashboard/storage.ts` exports `resolveStorage()`, which wraps the `globalThis.localStorage`
PROPERTY GETTER in try/catch and returns `null` when it throws or is absent. It is now the ONLY place
in `src/dashboard/` that dereferences a storage global, and a repo-wide test proves it. Six
previously-unguarded sites were wired to it: `main.ts:19`, `nav.ts:186`, `nav.ts:206`, `theme.ts:93`,
`theme.ts:130` and `detail-charts.ts:218`. Two of those (`main.ts:19` and, via `main.ts:22`'s
`createNav`, `nav.ts:186`) ran at MODULE SCOPE, which is why blocked site data previously killed the
whole dashboard module graph and rendered a blank page before any view could mount. Every reader now
tolerates a null handle explicitly: the theme falls back to `auto`, the week start to Monday, the
overlay config to its default. `calendar-preferences.ts`'s header and `calendar.ts`'s rationale
comment — which claimed a calendar-scoped guard closed the app-level threat, and claimed a "generic
error panel" would catch it — were corrected to describe what actually happens.

**What this round did NOT touch:** `index.html`'s inline pre-paint script (already try/catch-wrapped
since Phase 16 — it is why the theme ATTRIBUTE has always survived blocked storage even while the
module script died), the `.splits-scroll` fallback, WR-05's 8px header/value offset (R17 read the
current alignment as correct), and the D-15 scope fence.
</what_this_round_ships>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the gate, serve a PROVABLY FRESH Round 3 build under /strava-widgets, and stage the six-row R18..R23 agenda</name>
  <files>.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` — the whole file: the
      Per-Task Verification Map (63-84), the Round 1 section (from 190) with its preamble, both
      October tables, eleven answered rows, `## Checkpoint Outcome (Round 1)`,
      `## Known and Accepted` and `## Settled Discretion (phase 22)`; then the Round 2 section (from
      345) with its preamble, ten house rules, six answered rows R12..R17,
      `## Checkpoint Outcome (Round 2)`, `## Gap Closure Record (Round 2)` and
      `## Known and Accepted (Round 2)`. The Round 3 section is APPENDED below all of it; nothing
      above is rewritten except the Per-Task Verification Map's Status column.
    - `.planning/phases/22-calendar-week-start-totals/22-08-PLAN.md` Task 1 (lines 201-322) — the
      staging precedent this task follows: the gate commands, the
      `/tmp/gh-pages/strava-widgets` symlink, the `python3 -m http.server 8099 --directory
      /tmp/gh-pages` serve step, the served-artifact freshness assertions, the preamble contents and
      the five-column agenda row shape with one `Required detail:` and one `Observer required:`
      clause per row.
    - This plan's `<house_rules>`, `<cache_trap>`, `<round_carryover>` and
      `<what_this_round_ships>` blocks — all four are copied into the Round 3 preamble.
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` — the two gaps as worded
      and its `human_verification` frontmatter, which the Round 3 rows are measured against.
    - `.planning/phases/22-calendar-week-start-totals/22-09-SUMMARY.md`,
      `22-10-SUMMARY.md` and `22-11-SUMMARY.md` — what actually shipped, so
      `<what_this_round_ships>` is quoted accurately rather than from this plan's expectations.
  </read_first>
  <action>
    First re-run the full phase gate and fail the task if any part is not clean: `npm test`,
    `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets` (whose captured output must contain
    no `css-syntax-error`), and `npm run verify-dashboard`.

    Then stage the build exactly as GitHub Pages serves it, under the `/strava-widgets` project path
    and never at the server root: `mkdir -p /tmp/gh-pages` and
    `ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets`; assert
    `/tmp/gh-pages/strava-widgets/index.html` exists and that `readlink` on the symlink returns an
    absolute path. Serve with `python3 -m http.server 8099 --directory /tmp/gh-pages` in the
    background and confirm `http://127.0.0.1:8099/strava-widgets/index.html` and
    `http://127.0.0.1:8099/strava-widgets/data/dashboard/index.json` both respond 200 over HTTP.

    **Then prove the served artifact is THIS round's build.** Read the `assets/index-*.js` and
    `assets/index-*.css` filenames out of the served `index.html`. The JS filename must be neither
    `assets/index-YqJHQsHW.js` (Round 1) nor `assets/index-Dlom2BM3.js` (Round 2); the CSS filename
    must not be `assets/index-aaEmW9us.css` (Round 2). Fetch the CSS over HTTP and assert that one of
    its `max-width: 380px` blocks contains ALL of: `minmax(0, max-content)`, a `grid-template-areas`
    declaration, and an `overflow-wrap` declaration. Record both filenames — the agenda quotes them
    so a later reader knows exactly which build was observed.

    Note in the preamble, honestly, that the JS-side Round 3 change (the shared `resolveStorage`)
    cannot be discriminated in a minified bundle by string match, so its freshness rests on the
    changed filename plus the behavioural rows R22 and R23 rather than on a served-text assertion.

    Do NOT edit anything under `dist/` or `data/`. This checkpoint uses organic archive data.

    Then append a `## Round 3` section to `22-VALIDATION.md` containing, in this order:

    A **preamble** stating: the served URL prefix `http://127.0.0.1:8099/strava-widgets/`; the two
    calendar routes this session uses
    (`http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10` and, for R21 only, the same
    route under Sunday-start); the observed JS and CSS filenames plus an explicit sentence that the
    JS filename differs from BOTH `assets/index-YqJHQsHW.js` and `assets/index-Dlom2BM3.js`; and the
    four gate commands with their results. Then the whole `<cache_trap>` block, the whole
    `<house_rules>` list including rules 10, 11, 12 and 13, the whole `<what_this_round_ships>` block,
    and the whole `<round_carryover>` block including both R11's and R13's verbatim words, R16's
    decline, the Monday-start table, the rounding artifact and the Sunday-start values named as
    contradiction-guard material. Then one sentence recording the D-15 scope fence.

    The preamble must NOT contain the literal phrases `Required detail` or `Observer required` —
    those belong to agenda rows only, and Task 1's check counts them.

    Then a **six-row agenda table** with columns `Row | Requirement | Instructions | Observation |
    Verdict`, rows labelled `R18.` through `R23.` in that order. Each Instructions cell ends with its
    own `Required detail:` clause naming exactly what must be quoted back, and an
    `Observer required:` clause. For R19 and R22 that clause must read **the developer's own eyes —
    NON-WAIVABLE (house rules 10 and 11)**. Leave every Observation and Verdict cell empty — zero new
    `R22-VERDICT` tokens may exist in the Round 3 section before the checkpoint.

    | Row | Requirement | What it asks |
    |-----|-------------|--------------|
    | R18 | precondition | The URL bar reads `127.0.0.1` and includes `/strava-widgets/`; a hard reload was performed and the method is named; the `assets/index-*.js` filename read from the page matches the preamble AND is neither `index-YqJHQsHW.js` nor `index-Dlom2BM3.js`. **Required detail:** the URL as typed, the reload method, the bundle filename as read |
    | R19 | CAL-02, D-10, GC-4 | **The R13/R11 re-ask, third time, against BOTH the BL-01 and BL-02 fixes.** Narrow the viewport to a STATED pixel width, down to at least 380px (responsive-design mode or a phone emulation preset — name which, and name the width). Confirm the 380px rules are actually engaged before judging (`matchMedia('(max-width: 380px)').matches` should read `true`; Round 2's attempt failed exactly here). At that width read back the rendered text of at least three day cells that carry a distance, and the rendered text of one week-total cell. Then answer: does ANY value still overflow its cell? Does the eight-column grid overflow the panel or the viewport? Are the day columns still legible? Is the behaviour the same in the other theme? **Required detail:** the stated width, the `matchMedia` result, the quoted text of at least three day cells and one total cell, an explicit yes/no on overflow, an explicit clipped-versus-overflowing statement, and the both-themes comparison. A value that is CLIPPED or TRUNCATED rather than overflowing is ALSO a FAIL — say which it is. A value that WRAPS onto two lines inside its cell is NOT a failure; that is the intended Round 3 behaviour |
    | R20 | CAL-02, D-16 | Back at normal width on `#/calendar?month=2025-10` under Monday-start, read back all five week-total cells: distance, time and run count. Compare against the Monday-start table in the preamble. This is the regression check on the BL-01/BL-02 CSS and on the app-wide storage refactor — the grid math and the total cell must be unchanged by both. **Required detail:** five triples of distance / time / run count, quoted as rendered |
    | R21 | CAL-03, IN-05 | Confirm-unregressed. At normal width, look along `.calendar-header` (month total, two nav buttons, the `Jump to month` input, the segmented group) and confirm nothing is misaligned or overlapping; then find a wide week-total cell (October 2025 under Sunday-start has `104.1 km / 10h 14m / ×7`) and confirm the `Total` header still sits over the values it labels rather than centred away from them. Both themes. **Required detail:** a description of the header alignment, where the `Total` header sits relative to the values, the week used, and the theme comparison. This row does not re-gate CAL-03 |
    | R22 | CAL-01, T-22-WK-02, GC-5, BL-03 | **Gap 2, real browser configuration. MANDATORY — this row may not be declined (house rule 12) and may not be waived (house rules 10, 11).** Block site data for the origin — Firefox: Settings → Privacy & Security → Cookies and Site Data → Custom → Cookies → **All cookies**; Chrome: `chrome://settings/content/siteData` → *Don't allow sites to save data*. Then RELOAD `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10` (a full reload, not a hash navigation — the whole point is to re-run the app bootstrap under the blocked configuration) and describe what the page shows. **Disposition rules, applied by the executor when recording:** (a) the app boots — the nav renders, the Calendar route renders its grid AND its five week-total cells, the week start reads Monday, and the console carries no application error → **PASS**; (b) the page is blank, with no nav and no view content → **FAIL** of the Gap 2 fix (this is the outcome `22-REVIEW.md` BL-03 predicted for the PRE-fix build; the app-wide guard exists to prevent it); (c) the generic `Something went wrong` panel → **FAIL**. **There is no `NOT EXERCISABLE` disposition for this row this round.** **Required detail:** the browser and the exact setting used, what the page rendered (nav present? grid present? how many week-total cells? which week start?), any console error text, and which of (a)/(b)/(c) it matches |
    | R23 | CAL-01, GC-5 | **Restore and regression-check.** Restore the browser setting changed at R22 and confirm it is restored. Then, with storage working normally: hard-reload the Calendar, select `Sunday`, hard-reload again and confirm `Sunday` is still the filled option with `Sun` as the first weekday heading; then cycle the header theme toggle once, reload, and confirm the theme mode stuck. This is the regression row the app-wide storage refactor requires — six call sites moved onto a shared resolver and both persistence paths must still work for a normal user. **Required detail:** confirmation the browser setting was restored, which option was filled and the first weekday heading after the reload, and what the theme was before and after its reload |

    Then a `### Row-to-requirement map (Round 3)` stating: **R18 gates all rows.**
    **CAL-02 → R19, R20 (both gating).** **CAL-01 → R23 (gating); R22 gates Gap 2's disposition and
    is mandatory, but does not by itself re-gate CAL-01's persistence claim.**
    **CAL-03 → R21 (confirm-unregressed, not gating).**

    Set the file's frontmatter `round: 3` and add a `round3_staged` date. Leave
    `nyquist_compliant: false` and `status: partial` until Task 3. Do not alter `round1_staged`,
    `round1_answered`, `round2_staged` or `round2_answered`.

    Finally, update the Per-Task Verification Map: add two rows for this round's automated coverage —
    one for CAL-02 / no threat ref, "the BL-01/BL-02 380px overrides are asserted BY VALUE via
    `atRuleBodiesFor`, not merely proven to exist", unit,
    `npx vitest run src/dashboard/styles.test.ts`, task `22-09-T2`, wave 9; and one for CAL-01 /
    T-22-WK-02, "`resolveStorage` guards the property getter app-wide, both its failing and its
    live-and-working branches are covered, and a repo-wide source guard proves it is the only
    dereference site", unit, `npx vitest run src/dashboard/storage.test.ts`, task `22-11-T3`,
    wave 10.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json && rm -f /tmp/bw22r3c.log && npm run build-widgets > /tmp/bw22r3c.log 2>&1; BW=$?; tail -3 /tmp/bw22r3c.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw22r3c.log && npm run verify-dashboard && mkdir -p /tmp/gh-pages && ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets && test -f /tmp/gh-pages/strava-widgets/index.html && readlink /tmp/gh-pages/strava-widgets | grep -q '^/' && curl -sf -o /dev/null http://127.0.0.1:8099/strava-widgets/index.html && curl -sf -o /dev/null http://127.0.0.1:8099/strava-widgets/data/dashboard/index.json && test -z "$(git status --porcelain src scripts data dist)" && node --input-type=module -e 'const base="http://127.0.0.1:8099/strava-widgets/"; const html=await (await fetch(base+"index.html")).text(); const js=(html.match(/assets\/index-[A-Za-z0-9_-]+\.js/)||[])[0]; const cssref=(html.match(/assets\/index-[A-Za-z0-9_-]+\.css/)||[])[0]; if(!js||!cssref) throw new Error("could not read the asset filenames from the served index.html"); for(const stale of ["assets/index-YqJHQsHW.js","assets/index-Dlom2BM3.js"]) if(js===stale) throw new Error("the served bundle is a PREVIOUS rounds build ("+stale+") - the Round 3 fixes are not in the observed artifact"); if(cssref==="assets/index-aaEmW9us.css") throw new Error("the served stylesheet is Round 2s - the BL-01/BL-02 fixes are not in the observed artifact"); const css=await (await fetch(base+cssref)).text(); const blocks=[...css.matchAll(/@media\s*\(max-width:\s*380px\)\s*\{/g)].map(m=>{let i=m.index+m[0].length,d=1; while(d>0&&i<css.length){ if(css[i]==="{")d++; else if(css[i]==="}")d--; i++; } return css.slice(m.index,i);}); if(!blocks.some(b=>b.includes("minmax(0,max-content)")||b.includes("minmax(0, max-content)"))) throw new Error("no served 380px block carries the BL-01 track fix - stale CSS"); if(!blocks.some(b=>/grid-template-areas/.test(b))) throw new Error("no served 380px block carries the BL-02 day-cell stack - stale CSS"); if(!blocks.some(b=>/overflow-wrap/.test(b))) throw new Error("no served 380px block carries the overflow-wrap floors - stale CSS"); console.log("served build is fresh for Round 3: "+js+" "+cssref);' && node --input-type=module -e 'import {readFileSync} from "node:fs"; const p=".planning/phases/22-calendar-week-start-totals/22-VALIDATION.md"; const s=readFileSync(p,"utf8"); if((s.match(/## Round 3/g)||[]).length!==1) throw new Error("expected exactly one Round 3 heading"); const t=s.split("## Round 3")[1]; const ids=["R18","R19","R20","R21","R22","R23"]; const rows=t.split("\n").filter(l=>l.startsWith("|")).filter(r=>ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(r))); if(rows.length!==6) throw new Error("expected 6 agenda rows, found "+rows.length); ids.forEach((id,i)=>{ if(!new RegExp("^\\|\\s*"+id+"\\.").test(rows[i])) throw new Error("agenda row "+(i+1)+" should begin with "+id+"."); if(rows[i].split("|").length<6) throw new Error("row "+id+" does not have five columns"); }); const rd=(t.match(/Required detail/g)||[]).length; if(rd!==6) throw new Error("expected exactly one Required detail clause per row, found "+rd); const ob=(t.match(/Observer required/g)||[]).length; if(ob!==6) throw new Error("expected exactly one Observer required clause per row, found "+ob); for(const id of ["R19","R22"]){ const r=rows.find(x=>new RegExp("^\\|\\s*"+id+"\\.").test(x)); if(!/NON-WAIVABLE/i.test(r)) throw new Error(id+" does not carry the NON-WAIVABLE observer clause"); } const r22=rows.find(x=>/^\|\s*R22\./.test(x)); if(/NOT EXERCISABLE/.test(r22)) throw new Error("R22 must have no NOT EXERCISABLE disposition this round"); if(!/may not be declined/i.test(r22)) throw new Error("R22 does not state that it may not be declined"); const r19=rows.find(x=>/^\|\s*R19\./.test(x)); if(!/matchMedia/.test(r19)) throw new Error("R19 does not require proof that the 380px rules are engaged"); if(!/380px/.test(r19)) throw new Error("R19 does not state the target width"); if((t.match(/R22-VERDICT/g)||[]).length!==0) throw new Error("no verdict token may exist in Round 3 before the checkpoint"); if((s.match(/R22-VERDICT/g)||[]).length!==17) throw new Error("Rounds 1+2 verdict count changed - those sections must not be rewritten"); const pre=t.split("| Row")[0]; for(const n of ["127.0.0.1:8099/strava-widgets/","2025-10","index-YqJHQsHW.js","index-Dlom2BM3.js","59.1 km","80.0 km","7h 58m","7h 42m","58.1 km","357.349","minmax(0, max-content)","grid-template-areas","overflow-wrap","resolveStorage","main.ts:19","nav.ts:186","splits-scroll","D-15","decline it"]) if(!pre.includes(n)) throw new Error("the Round 3 preamble is missing: "+n); if(!/hard reload/i.test(pre)) throw new Error("the preamble does not carry the hard-reload cache-trap paragraph"); if(!/non-waivable/i.test(pre)) throw new Error("the preamble does not carry the non-waivable clause (house rules 10/11)"); if(!/may not be declined/i.test(pre)) throw new Error("the preamble does not carry house rule 12"); if(/Required detail/.test(pre)||/Observer required/.test(pre)) throw new Error("the preamble must not use the row-only clause phrases"); if(/localhost:8099/.test(s)) throw new Error("localhost:8099 appears - 127.0.0.1 is required"); if(!/Row-to-requirement map \(Round 3\)/.test(t)) throw new Error("the Round 3 row-to-requirement map is missing"); const fm=s.split("---")[1]||""; if(!/round: 3/.test(fm)) throw new Error("frontmatter round is not 3"); if(!/round3_staged/.test(fm)) throw new Error("round3_staged date missing"); for(const keep of ["round1_answered","round2_answered"]) if(!fm.includes(keep)) throw new Error(keep+" was dropped from the frontmatter"); if(!/nyquist_compliant: false/.test(fm)) throw new Error("nyquist_compliant must stay false until Task 3"); console.log("6 staged rows R18..R23, 0 new verdicts, Rounds 1+2 intact at 17, fresh Round 3 build, non-waivable clauses present");'</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets` (zero
      `css-syntax-error` in its output) and `npm run verify-dashboard` all exit 0.
    - `/tmp/gh-pages/strava-widgets/index.html` exists, the symlink resolves to an absolute path, and
      both the served `index.html` and `data/dashboard/index.json` respond 200 over HTTP from
      `127.0.0.1:8099`.
    - The served `assets/index-*.js` filename is neither `assets/index-YqJHQsHW.js` nor
      `assets/index-Dlom2BM3.js`; the served CSS filename is not `assets/index-aaEmW9us.css`; and a
      served `max-width: 380px` block contains `minmax(0, max-content)`, a `grid-template-areas`
      declaration and an `overflow-wrap` declaration.
    - `git status --porcelain src scripts data dist` is empty.
    - `22-VALIDATION.md` has exactly one `## Round 3` heading with exactly six agenda rows
      `R18.`..`R23.` in order, five columns each, exactly six `Required detail:` and six
      `Observer required:` clauses in the section, and **zero** `R22-VERDICT` tokens inside Round 3.
    - R19's and R22's `Observer required:` clauses both carry `NON-WAIVABLE`; R22's Instructions cell
      states it may not be declined and contains no `NOT EXERCISABLE` disposition; R19's Instructions
      cell names `380px` and requires a `matchMedia` confirmation.
    - The file still contains exactly 17 `R22-VERDICT` tokens (Rounds 1 and 2 untouched).
    - The Round 3 preamble contains every one of: the served URL prefix, `2025-10`,
      `index-YqJHQsHW.js`, `index-Dlom2BM3.js`, all five Monday-start distances/times, `357.349`,
      `minmax(0, max-content)`, `grid-template-areas`, `overflow-wrap`, `resolveStorage`,
      `main.ts:19`, `nav.ts:186`, `splits-scroll`, `D-15`, `decline it`, a hard-reload paragraph, the
      non-waivable clause, and house rule 12's may-not-be-declined clause.
    - The preamble contains neither `Required detail` nor `Observer required`.
    - `localhost:8099` appears nowhere in the file.
    - Frontmatter carries `round: 3` and `round3_staged`, retains `round1_answered` and
      `round2_answered`, and keeps `nyquist_compliant: false`.
  </acceptance_criteria>
  <done>A provably fresh Round 3 production-shaped build is served from 127.0.0.1 under /strava-widgets against organic archive data, and six unanswered rows are staged with both prior FAILs, the declined row, and the fix inventory quoted in the preamble.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] The six-row Round 3 checkpoint — the third narrow-viewport ask, and the blocked-site-data row that has never been run</name>
  <what-built>
    Plan `22-09` fixed BOTH root causes `22-REVIEW.md` found for the overflow that failed at R11 and
    again at R13, in the same block: the eighth (Total) grid track can now be squeezed
    (`minmax(0, max-content)` plus `min-width: 0` and `white-space: normal` on the total cell)
    instead of holding a hard floor the seven day columns paid for; and each day cell collapses from
    a three-column grid with a CENTRED distance to a single-column stack with everything
    start-aligned, so the distance value gets the cell's full width instead of a third of it. Both
    values also got `overflow-wrap: anywhere`, so a number that still cannot fit wraps rather than
    spilling past the border. The `.splits-scroll` horizontal-scroll fallback is still deliberately
    not built.

    Plans `22-10` and `22-11` implemented the user's locked app-wide decision on `22-REVIEW.md`
    BL-03. A shared `resolveStorage()` in a new `src/dashboard/storage.ts` wraps the
    `localStorage` property getter in try/catch, and six previously-unguarded sites now go through
    it — `main.ts:19`, `nav.ts:186`, `nav.ts:206`, `theme.ts:93`, `theme.ts:130`,
    `detail-charts.ts:218`. Two of those ran at MODULE SCOPE, which is why blocked site data used to
    kill the entire dashboard module graph and render a blank page before any view mounted. The two
    source comments that claimed a calendar-scoped guard closed the app-level threat, and that a
    "generic error panel" would catch it, were corrected to describe what actually happens.

    Task 1 re-ran the full gate and served that build under `/strava-widgets` on 127.0.0.1 with
    **organic archive data — no fixture of any kind was staged** — and proved the served bundle is
    neither Round 1's `assets/index-YqJHQsHW.js` nor Round 2's `assets/index-Dlom2BM3.js`, and that
    the served CSS actually carries the Round 3 declarations.

    What no automated gate in this repository can see: whether the day-cell values still overflow at
    a narrow width (R11 and R13 were both the developer's own visual judgement, and both FAILED),
    and whether a real browser with site data blocked now boots the app instead of rendering blank.
    `vitest.config.ts` is `environment: 'node'` with no jsdom and there is no headless browser in
    this project.
  </what-built>
  <how-to-verify>
    Open `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10`. **Confirm the URL bar reads
    `127.0.0.1`, not `localhost`, and includes `/strava-widgets/`, before judging any row.**

    **Then hard-reload before judging anything** (Cmd+Shift+R, Cmd+Option+R in Safari, or DevTools
    open with "Disable cache" ticked, then reload). R18 exists to record that this happened AND that
    the bundle filename on the page is this round's — neither `index-YqJHQsHW.js` (Round 1) nor
    `index-Dlom2BM3.js` (Round 2). R18 must be PASS before any other row may be judged; if it is not,
    record every remaining row BLOCKED naming R18 and stop.

    Present the six rows from `22-VALIDATION.md`'s Round 3 table **one at a time**, in R18..R23
    order, following every rule in this plan's `<house_rules>` block.

    **The two non-negotiable rules for this session (house rules 10, 11 and 12):**

    - **R19 and R22 cannot be waived.** Do not offer to observe either on the developer's behalf. Do
      not resize a window, change a browser privacy setting, take a screenshot, dump computed styles,
      or reason from source in order to fill either Observation cell. If the developer offers a
      waiver or asks you to do the row, **decline**, record their words verbatim, and record that row
      **BLOCKED**. A BLOCKED decisive row is a better outcome than a fourth round built on evidence
      the verifier will discount — Rounds 1 and 2 both recorded PASSes under waivers and
      `22-VERIFICATION.md` scored that as weakening the evidence behind two ticked requirements.
    - **R22 may not be declined.** It was declined in Round 2 ("decline it") and the browser-
      configuration path has never been exercised in this phase. Say plainly, before asking it, that
      this is the row the entire app-wide storage fix exists for, that the expected outcome has
      CHANGED since Round 2 (the app should now BOOT under blocked site data rather than going
      blank), that the setting is reverted at R23, and that declining it leaves Gap 2 open for a
      third round. If it is still declined, record BLOCKED and say so in the outcome — do not
      substitute R15's Round 2 devtools evidence for it.

    R19 is the row this entire gap-closure round exists for, asked for the third time. Do not accept
    "looks better". The row asks for a STATED width, a `matchMedia('(max-width: 380px)').matches`
    confirmation that the rules are actually engaged (Round 2's attempt failed exactly here and
    produced a width-less FAIL), the quoted rendered text of at least three distance-carrying day
    cells and one total cell, an explicit yes/no on overflow, a clipped-versus-overflowing statement,
    and the both-themes comparison. Tell them explicitly that a value WRAPPING onto two lines inside
    its cell is the intended Round 3 behaviour and is NOT a failure — only overflowing the cell,
    being clipped, or being truncated is. If they offer a documented-PASS framing, do not encourage
    it: R11 and R13 were both recorded FAIL precisely because they chose accuracy over convenience,
    and CAL-02's tick depends on this verdict being honest.

    For R22, read the disposition rules aloud before they start so they know exactly what PASS looks
    like: nav rendered, Calendar grid rendered, five week-total cells rendered, week start reading
    Monday, no application console error. A blank page is a FAIL this round, not an out-of-scope
    finding. **Make sure the setting is restored at R23** (house rule 9).

    Do not let the developer chase the rounding artifact: the Monday-start rows display-sum to
    357.2 km against the header's 357.3 km, and that is expected per the preamble.
  </how-to-verify>
  <action>
    Present the six Round 3 rows from
    `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` to the developer ONE AT A TIME,
    in R18..R23 order, quoting each row's Instructions cell including its `Required detail:` and
    `Observer required:` clauses in full.

    Before R18, confirm with the developer that the URL bar reads
    `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10`, ask them to perform a hard
    reload and name the method, and ask them to read the `assets/index-*.js` filename off the page
    (devtools Network or Sources panel) and compare it against the preamble — a filename equal to
    `index-YqJHQsHW.js` or `index-Dlom2BM3.js` means a previous round's build is being observed and
    R18 is FAIL.

    For R19, ask for the width they used and the `matchMedia('(max-width: 380px)').matches` result
    before anything else; if the media query is not engaged, the row cannot be judged at that width
    and they must narrow further or use a phone emulation preset. Then ask for the quoted rendered
    text of at least three distance-carrying day cells and one week-total cell, the explicit overflow
    yes/no, the clipped-versus-overflowing distinction, and the other-theme comparison. **Do not read
    the DOM yourself as a substitute and do not accept a waiver on this row** (house rules 10, 11) —
    if one is offered, decline, record their words, and record the row BLOCKED.

    For R20, ask for the five triples as rendered, not as summarised, and compare them against the
    Monday-start table in the preamble. A row whose numbers match neither the Monday-start table nor
    anything else is a FAIL with the seen values quoted.

    For R21, ask about `.calendar-header`'s alignment, then where the `Total` header sits relative to
    the values beneath it and which week they used, in both themes. This row confirms IN-05 and does
    not re-gate CAL-03.

    For R22, give them the exact settings path for their browser and state that the next step is a
    FULL RELOAD, not a hash navigation — re-running the bootstrap under the blocked configuration is
    the entire point. Ask what the page rendered (nav present? grid present? how many week-total
    cells? which week start?), any console error text, and which of (a)/(b)/(c) it matches. Apply
    the three disposition rules literally: (a) PASS, (b) FAIL, (c) FAIL. **Do not accept a waiver on
    this row and do not accept a decline** — if it is declined anyway, record BLOCKED with their
    words verbatim, and do not patch anything (house rule 5).

    For R23, confirm the R22 setting was restored, then walk the two persistence checks: the
    Sunday-selection-survives-a-reload check and the theme-toggle-survives-a-reload check. This is
    the regression row for the app-wide storage refactor.

    Capture each answer in the developer's own words and hold them for Task 3, which writes them into
    the Observation cells. Do not summarise, do not merge answers, do not substitute an agent
    observation for any row, and do not edit any source file if a row fails.
  </action>
  <read_first>
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Round 3 — the staged agenda
      and preamble Task 1 wrote, including both prior FAILs' verbatim words, R16's decline, the
      Monday-start table, the rounding artifact and the fix inventory.
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Round 1 row R11 (line 305)
      and § Round 2 row R13 (line 478) — the exact judgements this round re-asks, in the developer's
      own words, and § Round 2 row R16 (line 481), the declined row R22 re-asks.
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Checkpoint Outcome
      (Round 2) — the recorded house-rule-1 waivers, verbatim, that rules 10 and 11 exist to stop
      repeating.
    - This plan's `<house_rules>`, `<cache_trap>`, `<round_carryover>` and
      `<what_this_round_ships>` blocks.
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` — both gaps as worded and
      its `human_verification` frontmatter, so the questions asked match the gaps being closed.
    - `.planning/PROJECT.md` line 49 — the house rule this whole task exists to honour.
  </read_first>
  <verify>
    <human-check>All six rows have an answer, each with the row's own required detail; the URL bar was confirmed to read 127.0.0.1 and /strava-widgets/ and the bundle filename was confirmed to be neither prior round's before R18 was judged; no row after R18 was judged unless R18 was PASS; R19 and R22 were answered by the developer's own eyes with no waiver granted or exercised, or are recorded BLOCKED; R19's answer states a viewport width and a matchMedia confirmation; R22 was actually run against a real browser-level blocked-site-data setting, or is recorded BLOCKED; the setting was restored and the restoration confirmed at R23; no source file was edited during the session.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer confirmed the URL bar reads
      `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10` and named the hard-reload
      method before R18 was judged.
    - R18's answer quotes the `assets/index-*.js` filename as read from the page, and it is neither
      `index-YqJHQsHW.js` nor `index-Dlom2BM3.js`.
    - R19's answer quotes the viewport width used, the `matchMedia('(max-width: 380px)').matches`
      result, the rendered text of at least three day cells and one week-total cell, an explicit
      yes/no on overflow, a clipped-versus-overflowing statement, and the both-themes comparison —
      **observed by the developer**. If an agent observed it, or a waiver was exercised on it, the
      row reads BLOCKED and quotes the request verbatim.
    - R20's answer quotes five distance / time / run-count triples as rendered.
    - R21's answer describes the header alignment and the `Total` header's position relative to the
      values, names the week used, and compares both themes.
    - R22's answer names the browser and the exact setting, states what rendered (nav / grid / count
      of week-total cells / week start), reports any console error text, and states which of
      (a)/(b)/(c) it matches — **observed by the developer**. If it was declined or waived, the row
      reads BLOCKED with the developer's words verbatim, and no `NOT EXERCISABLE` verdict is used.
    - R23's answer confirms the R22 browser setting was restored, states which option was filled and
      the first weekday heading after the reload, and states the theme before and after its reload.
    - Every row after R18 was judged only because R18 was PASS; otherwise those rows read BLOCKED
      naming R18.
    - No row outside R18..R23 was asked; in particular no row was opened against Trends, biggest week
      or streaks (D-15).
    - No source file was edited during the session: `git status --porcelain src scripts` is empty.
  </acceptance_criteria>
  <done>Six developer answers are in hand, the two decisive rows are attributable to the developer's own eyes or are honestly recorded BLOCKED, and the narrow-viewport rendering and the real blocked-site-data behaviour are quoted verbatim.</done>
  <resume-signal>Per-row verdicts recorded. Type "done" when all six rows have an answer, or name the rows you are declining — noting that declining R19 or R22 records that row BLOCKED and leaves its gap open.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the Round 3 outcome, disposition both verification gaps, and re-gate the Calendar requirements</name>
  <files>.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Round 3 as answered in
      Task 2, its `Row-to-requirement map (Round 3)`, and the Per-Task Verification Map whose
      manual-only Status cells this task updates.
    - `.planning/phases/22-calendar-week-start-totals/22-08-PLAN.md` Task 3 — the recording
      precedent: verdict tokens in Verdict cells only, the three closing sections, and gating
      `REQUIREMENTS.md` strictly on the row map rather than on an overall impression.
    - `.planning/REQUIREMENTS.md` — the CAL-01/CAL-02/CAL-03 bullet entries with their Round 1 and
      Round 2 closure notes, and the CAL-01/CAL-02/CAL-03 rows of the status table.
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` — both gap entries, whose
      dispositions this task records, and the third failed truth (#7, CR-01's documentation
      accuracy).
    - `.planning/STATE.md` § Decisions — the Phase 19 precedent (UI-01/UI-02 reverted from complete
      to blocked when a gap-closure row failed), which is the rule this task applies to CAL-01 if
      R23 does not pass.
  </read_first>
  <action>
    Write the six verdicts into the Round 3 Observation cells in the developer's own words, each with
    one `R22-VERDICT: PASS|FAIL|BLOCKED|NOT EXERCISABLE` token, the row's required detail, and its
    observer attribution. Do not summarise and do not merge answers. **R22 must not carry a
    `NOT EXERCISABLE` verdict this round** (house rule 12).

    For R19 and R22, the Observation cell must state explicitly who observed it. If either was
    observed by an agent, or a waiver was granted and exercised on it, the verdict is `BLOCKED` and
    the cell quotes the waiver request verbatim (house rules 10 and 11).

    None of the prose sections below may contain the literal token `R22-VERDICT` — it belongs to
    Verdict cells only, and this task's check counts exactly six of it in the Round 3 section and
    exactly twenty-three in the file.

    Then add three sections below the Round 3 table:

    - `## Checkpoint Outcome (Round 3)` — the count of PASS / FAIL / BLOCKED / NOT EXERCISABLE across
      R18..R23, and one sentence per non-PASS row naming what was not established. Record any
      mid-session waiver the developer granted, verbatim, as a stated deviation, and state explicitly
      whether it was granted on a non-waivable row (R19/R22) and therefore refused. No fix, no
      root-cause theory, no remediation plan.
    - `## Gap Closure Record (Round 3)` — one entry per gap in `22-VERIFICATION.md`, each stating
      CLOSED or STILL OPEN and the row that decided it.
      **Gap 1 (CAL-02, the ~380px overflow) is decided by R19**, with R20 recorded alongside as the
      regression check. If R19 is FAIL, record the developer's words verbatim, state that this is the
      THIRD consecutive failure of this claim (R11, R13, R19), and note that the DISC-6b
      `.splits-scroll` horizontal-scroll wrapper remains the documented, unimplemented fallback — but
      do NOT propose it, plan it, or implement it here (house rule 5).
      **Gap 2 (BL-03, the app-level blocked-site-data threat and its overclaiming documentation) is
      decided by R22.** If R22 is PASS, record that the app-wide `resolveStorage` guard was observed
      closing the threat end to end in a real browser configuration, and that
      `22-VERIFICATION.md` truth #7 ("CR-01's own documentation accurately describes what is and is
      not covered") is now backed by both the corrected comments and an observation. If R22 is FAIL
      or BLOCKED, record Gap 2 STILL OPEN and say which — a FAIL means the app-wide guard did not
      hold, a BLOCKED means it was declined or waived for a second round and remains unobserved.
    - `## Known and Accepted (Round 3)` — restate that the 357.349 km rounding artifact and the D-15
      scope fence remain knowingly shipped; that the DISC-6b `.splits-scroll` wrapper remains
      documented and unimplemented by deliberate choice (GC-1, carried into GC-4), not by omission;
      and that `22-REVIEW.md`'s WR-04, WR-05, WR-08, WR-09, WR-10 and WR-11 were scoped out of
      Round 3 and remain open code-review warnings for the user to disposition.

    Update the Per-Task Verification Map's manual-only Status cells to reflect the Round 3 verdicts
    of their mapped rows.

    Then update `.planning/REQUIREMENTS.md`, strictly on the Round 3 row map and nothing else:

    - **CAL-02** is ticked `[x]` and its status-table row set to `Complete` if and only if R18, R19
      and R20 are ALL PASS. Append a `**Closed 2026-08-18 (plan 22-12, Round 3)**` note naming those
      rows and quoting the width and the day-cell text read back at that width. If any of them did
      not pass, CAL-02 stays `[ ]` / `Pending` with a note naming the failing row, its verdict, and
      pointing at Round 3.
    - **CAL-01** keeps its tick if and only if R23 is PASS. Append a note recording R23's persistence
      result and R22's disposition (including, if R22 was FAIL or BLOCKED, that the app-level
      blocked-site-data claim is not established). **If R23 is not PASS, revert CAL-01 to `[ ]` and
      its status-table row to `Pending`**, with a note naming R23 and citing the Phase 19
      UI-01/UI-02 precedent for reverting a ticked requirement when a gap-closure row fails.
    - **CAL-03** is not re-gated. Leave its tick and status row exactly as they are. If R21 did not
      pass, record it in `## Checkpoint Outcome (Round 3)` as an open finding and say plainly in the
      CAL-03 line's note that R21 is confirm-unregressed and does not change the requirement's state.

    Finally set the file's frontmatter: `round3_answered` with today's date either way;
    `status: complete` and `nyquist_compliant: true` only if every one of R18..R23 is PASS; otherwise
    `status: partial` and `nyquist_compliant: false`. Do not alter the Round 1 or Round 2 sections,
    their verdicts, or their frontmatter dates.

    Do not touch `ROADMAP.md` — its Phase 22 checkbox belongs to verification and `/gsd-progress`.
    `22-VERIFICATION.md` is not edited either; re-verification is `/gsd-verify-work`'s job.
  </action>
  <verify>
    <automated>node --input-type=module -e 'import {readFileSync} from "node:fs"; const p=".planning/phases/22-calendar-week-start-totals/22-VALIDATION.md"; const s=readFileSync(p,"utf8"); const t=s.split("## Round 3")[1]; const ids=["R18","R19","R20","R21","R22","R23"]; const rows=t.split("\n").filter(l=>l.startsWith("|")).filter(r=>ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(r))); if(rows.length!==6) throw new Error("expected 6 Round 3 rows, found "+rows.length); if((t.match(/R22-VERDICT/g)||[]).length!==6) throw new Error("expected exactly 6 Round 3 verdicts"); if((s.match(/R22-VERDICT/g)||[]).length!==23) throw new Error("expected 23 verdict tokens in the file (11 + 6 + 6)"); for(const r of rows){ if(!/R22-VERDICT:\s*(PASS|FAIL|BLOCKED|NOT EXERCISABLE)/.test(r)) throw new Error("a row has no well-formed verdict: "+r.slice(0,40)); const cells=r.split("|"); if(cells.length<6) throw new Error("a row lost a column"); if((cells[4]||"").trim().length<20) throw new Error("an Observation cell is empty or near-empty: "+r.slice(0,40)); } const row=(id)=>rows.find(x=>new RegExp("^\\|\\s*"+id+"\\.").test(x)); const verdict=(id)=>(row(id).match(/R22-VERDICT:\s*(PASS|FAIL|BLOCKED|NOT EXERCISABLE)/)||[])[1]; if(verdict("R22")==="NOT EXERCISABLE") throw new Error("R22 may not carry a NOT EXERCISABLE verdict this round (house rule 12)"); for(const id of ["R19","R22"]) if(verdict(id)==="PASS" && !/developer/i.test(row(id))) throw new Error(id+" PASS cell does not attribute the observation to the developer - non-waivable row"); if(verdict("R19")==="PASS"){ if(!/px/.test(row("R19"))) throw new Error("R19 PASS cell does not state the viewport width it was judged at"); if(!/km/.test(row("R19"))) throw new Error("R19 PASS cell quotes no rendered day-cell value"); } if(verdict("R20")==="PASS" && /(56\.0|104\.1|5h 27m|10h 14m)/.test(row("R20"))) throw new Error("R20 PASS cell quotes Sunday-start values - contradiction guard"); if(verdict("R22")==="PASS" && !/(nav|grid)/i.test(row("R22"))) throw new Error("R22 PASS cell does not state what actually rendered"); for(const h of ["## Checkpoint Outcome (Round 3)","## Gap Closure Record (Round 3)","## Known and Accepted (Round 3)"]) if(!s.includes(h)) throw new Error("missing section: "+h); const gcr=s.split("## Gap Closure Record (Round 3)")[1].split("\n## ")[0]; for(const n of ["R19","R22","BL-03","CAL-02"]) if(!gcr.includes(n)) throw new Error("the Gap Closure Record does not name "+n); if(verdict("R19")!=="PASS" && !/splits-scroll/.test(gcr)) throw new Error("R19 did not pass but the documented unimplemented fallback is not recorded"); const ka=s.split("## Known and Accepted (Round 3)")[1]; for(const n of ["357.349","D-15","splits-scroll","WR-05"]) if(!ka.includes(n)) throw new Error("Known and Accepted (Round 3) is missing: "+n); const fm=s.split("---")[1]||""; if(!/round3_answered/.test(fm)) throw new Error("round3_answered date missing"); for(const keep of ["round1_answered","round2_answered"]) if(!fm.includes(keep)) throw new Error(keep+" was dropped"); const all=ids.every(id=>verdict(id)==="PASS"); if(all!==/nyquist_compliant: true/.test(fm)) throw new Error("nyquist_compliant does not match the all-PASS state"); const req=readFileSync(".planning/REQUIREMENTS.md","utf8"); const line=(id)=>req.split("\n").find(l=>l.includes("**"+id+"**")); const status=(id)=>req.split("\n").find(l=>l.startsWith("| "+id+" ")); const cal2ok=["R18","R19","R20"].every(id=>verdict(id)==="PASS"); if(line("CAL-02").startsWith("- [x]")!==cal2ok) throw new Error("CAL-02 tick state does not match its Round 3 row map"); if(cal2ok!==/Complete/.test(status("CAL-02"))) throw new Error("CAL-02 status row does not match its Round 3 row map"); const cal1ok=verdict("R23")==="PASS"; if(line("CAL-01").startsWith("- [x]")!==cal1ok) throw new Error("CAL-01 tick state does not match R23 (Phase 19 revert precedent applies)"); if(cal1ok!==/Complete/.test(status("CAL-01"))) throw new Error("CAL-01 status row does not match R23"); if(!line("CAL-03").startsWith("- [x]")||!/Complete/.test(status("CAL-03"))) throw new Error("CAL-03 must be left as it was - R21 does not re-gate it"); if(!/22-12/.test(req)) throw new Error("REQUIREMENTS.md carries no Round 3 attribution"); console.log("6 Round 3 verdicts recorded, three sections present, requirement states match the Round 3 row map");' && test -z "$(git status --porcelain src scripts)" && npm test && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `22-VALIDATION.md`'s Round 3 section has exactly six `R22-VERDICT` tokens, one per row, each a
      well-formed `PASS` / `FAIL` / `BLOCKED` / `NOT EXERCISABLE`, with no Observation cell shorter
      than 20 characters; the file carries 23 verdict tokens in total (11 Round 1 + 6 Round 2 +
      6 Round 3).
    - **R22 does not carry a `NOT EXERCISABLE` verdict** (house rule 12).
    - A PASS on R19 or R22 attributes the observation to the developer in the Observation cell; if an
      agent observed either, or a waiver was exercised on either, the verdict is `BLOCKED` and the
      cell quotes the request verbatim.
    - Contradiction guards hold: an R19 PASS cell states a viewport width and quotes at least one
      rendered `km` value; an R20 PASS cell quotes no Sunday-start value (`56.0`, `104.1`, `5h 27m`,
      `10h 14m`); an R22 PASS cell states what actually rendered (nav / grid).
    - The three Round 3 sections exist. The Gap Closure Record names R19, R22, `BL-03` and `CAL-02`,
      and — if R19 did not pass — records the `splits-scroll` fallback as still documented and still
      unimplemented, without proposing or implementing it.
    - `## Known and Accepted (Round 3)` records the 357.349 km rounding artifact, the D-15 fence, the
      unimplemented `splits-scroll` fallback and the code-review warnings (`WR-05` among them) left
      open.
    - `REQUIREMENTS.md`: CAL-02 is ticked and `Complete` if and only if R18, R19 and R20 all PASS;
      CAL-01 is ticked and `Complete` if and only if R23 is PASS; CAL-03 is left ticked and
      `Complete` regardless of R21. The file names plan `22-12`.
    - Frontmatter carries `round3_answered`, retains `round1_answered` and `round2_answered`, and
      sets `nyquist_compliant: true` if and only if all six Round 3 rows passed.
    - `ROADMAP.md` and `22-VERIFICATION.md` are not modified by this plan.
    - `git status --porcelain src scripts` is empty; `npm test` and
      `npx tsc --noEmit -p tsconfig.json` both exit 0.
  </acceptance_criteria>
  <done>Round 3 is recorded verbatim and append-only, both verification gaps carry an explicit disposition tied to a named row, and each Calendar requirement's state follows its row map rather than an overall impression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser privacy configuration → the served app | R22 crosses this deliberately, blocking site data browser-wide — the realistic form of the T-22-WK-02 threat and the one form no round has yet exercised |
| local HTTP server → browser | A loopback-only static server on 127.0.0.1 serving the built bundle and already-public published JSON |
| the observed artifact → the recorded verdict | A stale bundle would make every verdict describe code that is not under test; two prior builds have already been observed in this phase |
| the observer's identity → the strength of the evidence | Rounds 1 and 2 both recorded PASSes under mid-session waivers, which `22-VERIFICATION.md` scored as weakening the evidence behind ticked requirements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-WK-02 | Denial of service | the dashboard bootstrap under blocked site data | mitigate (observed) | R22 blocks site data browser-wide and RELOADS, re-running the module-scope path that previously threw. This is the only place in the phase where the app-wide guard is observed rather than unit-simulated, and it is the exact row declined in Round 2. House rule 12 removes the option to skip it |
| T-22-VER-03 | Tampering | the observed build | mitigate | Task 1 asserts `git status --porcelain src scripts data dist` is empty and proves freshness four ways: the served JS filename differs from BOTH prior rounds', the served CSS filename differs from Round 2's, and the served CSS carries the BL-01, BL-02 and overflow-wrap declarations. R18 makes the developer read the filename off the page after a hard reload |
| T-22-VER-01 | Repudiation | the checkpoint record itself | mitigate | House rules 10 and 11 make R19 and R22 non-waivable and require an agent-observed result on either to be recorded BLOCKED. Task 3's checks reject a PASS on either row whose Observation cell does not attribute it to the developer, reject an R19 PASS with no stated width or rendered value, and reject an R20 PASS quoting Sunday-start values |
| T-22-VER-05 | Repudiation | a silently-skipped decisive row | mitigate | House rule 12 forbids declining R22 and forbids a `NOT EXERCISABLE` verdict on it; Task 3's check enforces the verdict vocabulary, and the Gap Closure Record must state STILL OPEN if it was declined. Round 2's outcome — a declined row recorded as informational — cannot recur |
| T-22-GC-BR-01 | Denial of service | the developer's browser configuration | mitigate | R22 changes a browser-wide privacy setting; house rule 9 requires it restored before the session ends, and R23 is the row that confirms the restoration by exercising both persistence paths again |
| T-22-SRV-01 | Information disclosure | the local static server | accept | `python3 -m http.server` serves `/tmp/gh-pages` on loopback only; its contents are the published dashboard bundle plus already-public activity JSON. Transient checkpoint fixture, not a deployed artifact |
| T-22-SC | Tampering | npm/pip/cargo installs | accept (not applicable) | Zero new packages. `22-RESEARCH.md` § Package Legitimacy Audit records "Not applicable"; no install task exists in any Phase 22 plan |
</threat_model>

<verification>
- Task 1's staging check proves six rows are staged with zero new verdicts, Rounds 1 and 2 intact at
  seventeen, no fixture, `127.0.0.1` enforced, the served artifact carrying this round's CSS and
  neither prior round's filenames, and the non-waivable / may-not-be-declined clauses present in both
  the preamble and the two decisive rows.
- Task 2's human-check proves every row has a developer answer with its required detail, that the two
  decisive rows were not observed by an agent, and that the browser setting was restored.
- Task 3's recording check proves six verdicts, non-empty observations, the observer attribution on
  the two non-waivable rows, all three contradiction guards, the three sections, both gap
  dispositions, and that each requirement's state matches the Round 3 row map exactly.

This is the ONLY plan in the Round 3 gap-closure set that discharges a visual or browser-behaviour
claim. Plans `22-09`, `22-10` and `22-11` each state explicitly that they do not.
</verification>

<success_criteria>
- Six rows asked one at a time, recorded verbatim with four-state verdicts under a `## Round 3`
  heading continuing at R18, appended without rewriting Rounds 1 or 2.
- The narrow-viewport question that failed as R11 and again as R13 was re-asked against a provably
  fresh build carrying BOTH the BL-01 and BL-02 fixes, at a stated pixel width with the media query
  confirmed engaged, and answered with quoted rendered values rather than an impression.
- The blocked-site-data browser configuration was actually exercised — or is honestly recorded
  BLOCKED, with Gap 2 recorded STILL OPEN and no substitution of Round 2's isolated devtools
  evidence.
- No waiver was granted or exercised on R19 or R22; any request for one is recorded verbatim and the
  row is BLOCKED.
- Both `22-VERIFICATION.md` gaps carry an explicit CLOSED / STILL OPEN disposition tied to a row.
- CAL-02 is ticked if and only if its row map passed; CAL-01's tick survives if and only if R23
  passed; CAL-03 is untouched. `REQUIREMENTS.md` is not pre-ticked.
- No source file was edited and no fixture was staged during the checkpoint.
</success_criteria>

<output>
Create `.planning/phases/22-calendar-week-start-totals/22-12-SUMMARY.md` when done.
</output>
