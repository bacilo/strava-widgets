---
phase: 22-calendar-week-start-totals
plan: 15
type: execute
wave: 12
depends_on: []
gap_closure: true
round: 4
files_modified:
  - src/dashboard/styles.css
  - src/dashboard/styles.test.ts
autonomous: true
requirements: [CAL-02]
user_setup: []
must_haves:
  decisions: [D-10, D-11, D-16, GC-4, GC-7]
  truths:
    - "GC-7 / SC3 COVERAGE BAND, STATED EXPLICITLY: the calendar's overflow compaction governs every viewport width from 0px through 640px inclusive. At 641px and above the default eight-column shape applies, where CR-02's own arithmetic gives a ~480px grid minimum against `641 - 2 x 24px = 593px` of available width inside `.view` — roughly 113px of headroom at the boundary, widening monotonically up to `.view`'s 1100px max-width. The claim this plan makes is therefore 'no overflow at any width from 320px to 1100px+', not 'no overflow at one tested pixel width'."
    - "GC-7: the three most common real phone CSS widths — 390px (iPhone 12/13/14/15), 393px (iPhone 15/16 Pro) and 412px (Pixel) — are INSIDE the compaction band for the first time in this phase. They sat in the untested 381-640px band through all three prior rounds."
    - "GC-7: the fix is a breakpoint move ONLY. Every declaration inside the calendar compaction block is byte-identical to what plan 22-09 shipped; only the at-rule prelude changes from `max-width: 380px` to `max-width: 640px`. No declaration is added, removed or revalued, so R19's PASS at 375px remains valid evidence for the sub-380px half of the band."
    - "GC-7 BREADTH GUARD (the anti-repeat test): a test parses the numeric `max-width` out of the calendar compaction block's own prelude and asserts it is at least 530px — CR-02's computed upper edge of the overflow band. Narrowing the breakpoint back toward 380px in a future edit turns this red. This is the guard Round 3 lacked, and its absence is why a fix that was correct at one width shipped as if it closed the whole truth."
    - "GC-7: no `@media (max-width: 380px)` block in `styles.css` mentions any `.calendar-` selector any more. The file's remaining two 380px blocks govern `.chart-band__canvas-wrap` and `.pr-evolution-card__canvas-wrap` only, and a test asserts that count is exactly 2."
    - "D-10 SURVIVES: `.calendar-grid`'s default-breakpoint track list is still `grid-template-columns: repeat(7, 1fr) auto` — the eight-column contract is unchanged at desktop widths, which is what plan 22-09 correctly protected and this plan must not undo. Only the SIZING FUNCTIONS change, and now they change at 640px instead of 380px."
    - "GC-4 SURVIVES: no calendar rule declares `overflow`, `overflow-x` or `text-overflow` at any breakpoint — the DISC-6b `.splits-scroll` horizontal-scroll wrapper is still deliberately not built, and truncation (which would render a WRONG number instead of a clipped one) is still forbidden."
    - "WR-06 SURVIVES: the `padding: var(--space-xs)` rule is still the FIRST rule inside the calendar compaction block, because `RULE_SCANNER` swallows an at-rule block's first nested rule into the prelude's pseudo-body and every other value assertion depends on sitting below it."
    - "`npx vitest run src/dashboard/styles.test.ts` is green, `npx tsc --noEmit -p tsconfig.json` exits 0, and `npm run build-widgets` exits 0 with zero `css-syntax-error` occurrences in its output."
  artifacts:
    - path: "src/dashboard/styles.css"
      provides: "the calendar overflow compaction governing 0-640px instead of 0-380px"
      contains: "@media (max-width: 640px)"
    - path: "src/dashboard/styles.test.ts"
      provides: "breakpoint-parsing breadth guard, updated block-count assertions, and a renamed breakpoint-agnostic block extractor"
      contains: "toBeGreaterThanOrEqual(530)"
  key_links:
    - from: "a 390/393/412px phone viewport"
      to: "the calendar's zero-floor grid tracks and single-column day stack"
      via: "the calendar compaction block's widened prelude"
      pattern: "@media \\(max-width: 640px\\)"
    - from: "a future edit narrowing the breakpoint back"
      to: "a red test"
      via: "the parsed-breakpoint >= 530px assertion"
      pattern: "toBeGreaterThanOrEqual\\(530\\)"
---

<objective>
Close `22-VERIFICATION.md`'s **Gap 1** (CAL-02 / SC3, reopened) / `22-REVIEW.md` **CR-02**: the
day-cell and week-total overflow fix is pinned to `@media (max-width: 380px)` while the rules that
cause the overflow are unconditional from 381px to at least 1000px.

Purpose: SC3's own wording is "legible at **every viewport the app is expected to render at**." Round
3's plan `22-09` deliberately scoped its fix to `@media (max-width: 380px)` and said so in its own
must-haves, and R19 — the row that closed the gap — observed exactly one width, 375px. Direct source
read confirms `.calendar-grid { grid-template-columns: repeat(7, 1fr) auto }` (line 743),
`.calendar-day { min-width: 32px }` (line 766) and `.calendar-week-total { white-space: nowrap }`
(line 832) all still apply unconditionally at 381px and above, with no `@media` rule between the
file's 465px, 544px, 640px, 720px and 1000px breakpoints touching `.view`, `.calendar-grid`,
`.calendar-day` or `.calendar-week-total`. CR-02's arithmetic gives a ~480px grid minimum against
`viewport - 48px` of available width, so the band roughly 381-530px overflows — and it contains 390px,
393px and 412px, the three most common phone CSS widths, none of which any checkpoint row in any of
the three rounds has ever observed.

Output: the calendar compaction block's prelude raised from `max-width: 380px` to `max-width: 640px`,
with every declaration inside it unchanged; the `styles.test.ts` assertions that encode the old
breakpoint updated; and a NEW breadth guard that parses the breakpoint out of the block's own prelude
and fails if it is ever narrowed below 530px — the guard whose absence let a one-width fix ship as a
whole-truth close.

**Explicitly NOT in scope, and deliberately so.**
- `.view { padding: var(--space-lg) }` is NOT touched. Reducing it would recover 48px but it is shared
  by all five screens (Overview, Activities, Calendar, Records, Trends) and would change every one of
  them; the 640px cutoff already clears CR-02's ~530px edge by roughly 113px without it.
- The DISC-6b `.splits-scroll` horizontal-scroll wrapper is still NOT built. It remains the documented,
  unimplemented fallback, for the original focus-order reason: this grid is full of focusable day
  buttons and a horizontal-scroll container with focusable content scroll-jumps on Tab.
- The `deferred-items.md` mid-number wrapping polish item (a distance breaking after its first digit at
  very narrow widths) is NOT addressed; it is recorded there as accepted, not-a-defect.
- No change to `nav.ts`, `theme.ts` or `storage.ts` (plans `22-13`/`22-14`). The D-15 fence stands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md
@.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md
@.planning/phases/22-calendar-week-start-totals/22-REVIEW.md
@.planning/phases/22-calendar-week-start-totals/22-09-PLAN.md
@.planning/phases/22-calendar-week-start-totals/deferred-items.md
</context>

<interfaces>
<!-- The exact CSS and test surface this plan touches, with line numbers as of the current file. -->

`src/dashboard/styles.css` — the DEFAULT-breakpoint rules that cause the overflow. All three stay
exactly as they are; this plan does not edit them.

  .view                  (308-312)  padding: var(--space-lg);  max-width: 1100px;   /* NOT touched */
  .calendar-grid         (741-745)  grid-template-columns: repeat(7, 1fr) auto;  gap: var(--space-xs);
  .calendar-day          (764-778)  min-width: 32px;  padding: var(--space-sm);
                                    grid-template-areas: "number . ." / ". distance ." / ". . count";
                                    grid-template-columns: 1fr 1fr 1fr;
  .calendar-week-total   (825-833)  padding: var(--space-sm);  white-space: nowrap;  (no min-width)

`src/dashboard/styles.css` — the calendar compaction block. ONLY its prelude changes.

  line 926   @media (max-width: 380px) {        <-- becomes  @media (max-width: 640px) {
  927-930      .calendar-day, .calendar-week-total { padding: var(--space-xs); }   <-- must stay FIRST
  932-934      .calendar-grid { grid-template-columns: repeat(7, minmax(0, 1fr)) minmax(0, max-content); }
  936-943      .calendar-day { min-width: 0; grid-template-areas: "number" "distance" "count";
                               grid-template-columns: 1fr; }
  945-949      .calendar-day__number, .calendar-day__distance, .calendar-day__count { justify-self: start; }
  951-954      .calendar-day__distance { font-size: 14px; overflow-wrap: anywhere; }
  956-960      .calendar-week-total { min-width: 0; white-space: normal; overflow-wrap: anywhere; }
  962-964      .calendar-week-total__distance { font-size: 14px; }
  966-969      .calendar-week-total__time, .calendar-week-total__count { font-size: 12px; }
  970        }

Every other `@media` block in `styles.css`, none of which mentions a `.calendar-` selector:

  465   @media (max-width: 640px)   .app-nav__links / .app-nav__toggle          (nav collapse)
  544   @media (max-width: 720px)   .activity-table-wrapper / .sort-select
  1019  @media (min-width: 721px)
  1052  @media (max-width: 380px)   .chart-band__canvas-wrap                    (stays 380px)
  1378  @media (min-width: 640px)
  1384  @media (min-width: 1000px)
  1401  @media (max-width: 380px)   .pr-evolution-card__canvas-wrap             (stays 380px)

`src/dashboard/styles.test.ts` helpers the assertions run through (do not rewrite these):

  RULE_SCANNER()                                          // swallows an at-rule's FIRST nested rule
  isAtRuleScoped(index, head, source): boolean            // "inside ANY at-rule" — breakpoint-agnostic
  splitTopLevelSelectors(head): string[]
  bodyForSelectorListToken(needle): string                // the DEFAULT-breakpoint body
  cascadeWinningBodyDeclaring(needle, property): string
  assertNoAtRuleOverride(needle, property): void          // THROWS iff an at-rule override exists
  atRuleBodiesFor(needle, property, source?): string[]    // at-rule-scoped bodies, source order
  calendar380Block(): string          (1949-1965)         // <-- renamed and re-needled by this plan
  cssNoComments                                           // the comment-stripped stylesheet
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Raise the calendar compaction block's prelude from max-width 380px to max-width 640px and rewrite its rationale comment</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/styles.css` lines 736-970 — the whole calendar block: the D-10 comment at 736-740,
      the default-breakpoint rules at 741-853 that this task does NOT edit, the 71-line rationale
      comment at 855-925 that this task rewrites, and the compaction block at 926-970 whose ONLY
      changed character range is its prelude.
    - `src/dashboard/styles.css` lines 306-312 — `.view`'s 24px padding and 1100px max-width, the two
      numbers CR-02's available-width arithmetic uses. Confirm for yourself that no `@media` rule
      anywhere in the file overrides `.view`'s padding; that is what makes the available width
      `viewport - 48px` at every breakpoint.
    - `src/dashboard/styles.css` lines 460-486, 544-556, 1019, 1052, 1378, 1384, 1401 — every other
      at-rule in the file, to confirm the new 640px calendar block collides with none of them by
      selector (the existing 640px block at 465 governs `.app-nav__*` only).
    - `22-REVIEW.md` CR-02 (lines 151-218) — the contributor-by-contributor arithmetic table, the
      ~480px grid minimum, the per-device available widths, the ~530px no-overflow crossover, and the
      two suggested fix shapes. This plan implements the first (single raised breakpoint).
    - `.planning/phases/22-calendar-week-start-totals/22-09-PLAN.md` — the Round 3 plan whose own
      must-have deliberately scoped the fix to 380px. Read it so you understand you are widening that
      decision, not reversing its content.
  </read_first>
  <action>
    Change exactly one line in `src/dashboard/styles.css`: the at-rule prelude at line 926, currently
    `@media (max-width: 380px) {`, becomes `@media (max-width: 640px) {`. Every declaration between
    that line and its closing `}` at line 970 stays byte-identical — same rules, same order, same
    values. In particular the `.calendar-day, .calendar-week-total { padding: var(--space-xs); }` rule
    must remain the FIRST rule inside the block (WR-06: `RULE_SCANNER` swallows an at-rule block's
    first nested rule into the prelude's pseudo-body, so every value assertion in `styles.test.ts`
    depends on sitting below it).

    Do NOT touch the two remaining `@media (max-width: 380px)` blocks at lines 1052 and 1401 — they
    govern `.chart-band__canvas-wrap` and `.pr-evolution-card__canvas-wrap`, are outside this phase's
    scope, and a test asserts their count is exactly 2 after this change.

    Do NOT touch `.view`'s padding, `.calendar-grid`'s default `repeat(7, 1fr) auto` track list,
    `.calendar-day`'s default `min-width: 32px` or `.calendar-week-total`'s default
    `white-space: nowrap`. D-10's eight-column contract survives as the default-breakpoint shape and
    plan 22-09 was right to protect it; only the widths at which the sizing functions change are moving.

    Then replace the 71-line rationale comment at lines 855-925 with a comment of at most 30 lines
    that describes the stylesheet as it now stands rather than the round history (`22-REVIEW.md` IN-06:
    several comment blocks in this file have become archived process narrative, and the
    `-SUMMARY.md`/`-VALIDATION.md` artifacts already hold it). The replacement must state, concretely:

    - The COVERAGE BAND and why it is 640px: the compaction governs 0-640px inclusive; above it, the
      default eight-column shape has a content-derived minimum of roughly 480px (7 day tracks with a
      definite 32px item min-width plus 8px padding either side and 1px borders = ~350px; 7 gaps at
      `--space-xs` = 28px; the `auto` Total track's `nowrap` min-content = ~100-105px) against
      `viewport - 2 x var(--space-lg)` of available width, so the first non-overflowing viewport is
      around 530px and 640px clears it with headroom. Name 390px, 393px and 412px explicitly as the
      real phone widths that sit inside the band.
    - Why the fix is a breakpoint move and not new declarations: BL-01 (the `nowrap` content floor on
      the 8th track winning the whole width negotiation) and BL-02 (the day cell's centred middle
      column giving the distance value a third of an already-collapsed cell) were correctly diagnosed
      and correctly fixed in Round 3; they were simply gated one pixel above where the defect lives.
    - Why `overflow-wrap: anywhere` and not `break-word`: only `anywhere` lowers an element's
      MIN-CONTENT contribution, and the grid track floor is computed from min-content, so `anywhere` is
      what makes the fix hold at the arithmetic edge.
    - Why no `overflow`/`overflow-x`/`text-overflow` appears anywhere in the block: that would be the
      DISC-6b `.splits-scroll` scroll-wrapper fallback, still deliberately unimplemented because this
      grid is full of focusable day buttons and a horizontal-scroll container with focusable content
      scroll-jumps on Tab; and truncation would render a WRONG number rather than a clipped one.
    - The WR-06 positional convention: the padding rule must stay first in the block, and a test
      enforces it.
    - That `styles.css` now contains exactly TWO `@media (max-width: 380px)` blocks, neither of them
      calendar-related.

    Do not introduce any of the strings `TBD`, `FIXME`, `XXX`, `TODO`, `HACK` or `PLACEHOLDER` — the
    verifier greps every phase-touched file for them.
  </action>
  <verify>
    <automated>node -e '
const fs=require("fs");
const css=fs.readFileSync("src/dashboard/styles.css","utf8");
const noc=css.replace(/\/\*[\s\S]*?\*\//g,"");
if(/TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER/.test(css)) throw new Error("a debt marker was introduced into styles.css");
function blocks(width){ const out=[]; const re=new RegExp("@media \\\\(max-width: "+width+"px\\\\)\\\\s*\\\\{","g"); let m; while((m=re.exec(noc))){ let i=m.index+m[0].length,d=1; while(d>0&&i<noc.length){ if(noc[i]==="{")d++; else if(noc[i]==="}")d--; i++; } out.push(noc.slice(m.index,i)); } return out; }
const b380=blocks(380), b640=blocks(640);
if(b380.length!==2) throw new Error("expected exactly two @media (max-width: 380px) blocks after the move, found "+b380.length);
if(b380.some(b=>b.includes(".calendar-"))) throw new Error("a 380px block still governs a .calendar- selector - the compaction was not moved");
if(b640.length!==2) throw new Error("expected exactly two @media (max-width: 640px) blocks (nav collapse + calendar), found "+b640.length);
const cal=b640.filter(b=>b.includes(".calendar-"));
if(cal.length!==1) throw new Error("expected exactly one 640px block mentioning a .calendar- selector, found "+cal.length);
const body=cal[0];
for(const needle of ["padding: var(--space-xs)","repeat(7, minmax(0, 1fr)) minmax(0, max-content)","min-width: 0","grid-template-columns: 1fr","justify-self: start","overflow-wrap: anywhere","white-space: normal","font-size: 14px","font-size: 12px"]) if(!body.includes(needle)) throw new Error("the moved calendar block lost the declaration: "+needle);
if(/(^|[^-\w])overflow\s*:/.test(body)||/overflow-x\s*:/.test(body)||/text-overflow\s*:/.test(body)) throw new Error("GC-4 breached: the calendar block declares an overflow property - the splits-scroll fallback was not authorised");
const first=body.slice(body.indexOf("{")+1); if(!first.slice(0,first.indexOf("}")).includes("padding: var(--space-xs)")) throw new Error("WR-06 breached: the padding rule is no longer the FIRST rule inside the calendar block");
const dflt=noc.slice(noc.indexOf(".calendar-grid {"), noc.indexOf(".calendar-weekday {"));
if(!dflt.includes("grid-template-columns: repeat(7, 1fr) auto")) throw new Error("D-10 breached: the default eight-column track list changed");
const day=noc.slice(noc.indexOf(".calendar-day {"), noc.indexOf(".calendar-day--outside"));
if(!day.includes("min-width: 32px")||!day.includes("grid-template-columns: 1fr 1fr 1fr")) throw new Error("the default .calendar-day rules were edited - this plan moves a breakpoint, it does not restyle the default");
const tot=noc.slice(noc.indexOf(".calendar-week-total {"), noc.indexOf(".calendar-week-total__distance"));
if(!tot.includes("white-space: nowrap")) throw new Error("the default .calendar-week-total nowrap was edited - out of scope for this plan");
if(!/padding: var\(--space-lg\)/.test(noc.slice(noc.indexOf(".view {"), noc.indexOf(".card {")))) throw new Error(".view padding was changed - explicitly out of scope, it is shared by all five screens");
console.log("GC-7 CSS move confirmed: calendar compaction now at max-width 640px, 380px blocks down to 2 (neither calendar), all declarations and D-10/GC-4/WR-06 invariants intact");
' && rm -f /tmp/bw22r4a.log && npm run build-widgets > /tmp/bw22r4a.log 2>&1; BW=$?; tail -3 /tmp/bw22r4a.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw22r4a.log && echo "build-widgets clean"</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/styles.css` contains exactly two `@media (max-width: 380px)` blocks and neither
      mentions a `.calendar-` selector.
    - It contains exactly two `@media (max-width: 640px)` blocks; exactly one of them mentions a
      `.calendar-` selector, and that one contains all nine declaration needles listed in the verify
      command, in the same order they had at 380px.
    - `.calendar-day, .calendar-week-total { padding: var(--space-xs); }` is the FIRST rule inside the
      calendar compaction block (WR-06).
    - The calendar compaction block declares no `overflow`, `overflow-x` or `text-overflow` (GC-4 —
      the `.splits-scroll` fallback is still unbuilt).
    - The default-breakpoint rules are untouched: `.calendar-grid` still declares
      `grid-template-columns: repeat(7, 1fr) auto` (D-10), `.calendar-day` still declares
      `min-width: 32px` and `grid-template-columns: 1fr 1fr 1fr`, `.calendar-week-total` still declares
      `white-space: nowrap`, and `.view` still declares `padding: var(--space-lg)`.
    - The rationale comment is at most 30 lines and states, in concrete terms: the 0-640px coverage
      band; the ~480px grid-minimum vs `viewport - 48px` arithmetic; 390px/393px/412px named as real
      phone widths inside the band; why `anywhere` rather than `break-word`; why no overflow property
      appears; the WR-06 padding-first convention; and that only two 380px blocks remain, neither
      calendar-related.
    - `styles.css` contains none of `TBD`, `FIXME`, `XXX`, `TODO`, `HACK`, `PLACEHOLDER`.
    - `npm run build-widgets` exits 0 with zero `css-syntax-error` occurrences in its captured output.
  </acceptance_criteria>
  <done>The calendar's overflow compaction governs every viewport up to 640px instead of only up to 380px, with every declaration unchanged and D-10's desktop eight-column contract intact.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point the styles.test.ts breakpoint assertions and add the breadth guard that fails if the fix is ever re-narrowed</name>
  <files>src/dashboard/styles.test.ts</files>
  <read_first>
    - `src/dashboard/styles.test.ts` lines 1871-1936 — the `Phase 22 gap closure (22-06)` describe.
      Six `it()` titles name "380px"; their BODIES use `bodyForSelectorListToken`,
      `cascadeWinningBodyDeclaring`, `assertNoAtRuleOverride` and `atRuleBodiesFor`, all of which are
      breakpoint-AGNOSTIC (`isAtRuleScoped` only asks "is this inside any at-rule"), so only the titles
      change. The one exception is the IN-06 case at 1927-1935, whose `toHaveLength(3)` is a real
      assertion about the file's structure and becomes 2.
    - `src/dashboard/styles.test.ts` lines 1938-1965 — the `calendar380Block()` helper and its JSDoc.
      It brace-walks from a `@media (max-width: 380px)` prelude and returns the first such block
      mentioning `.calendar-`. Both its needle and its name must change.
    - `src/dashboard/styles.test.ts` lines 1967-2063 — the `Phase 22 gap closure round 3 (22-09)`
      describe. Nine `it()` titles name "380px"; the two cases at 2045 and 2058 call
      `calendar380Block()` and must call the renamed helper.
    - `src/dashboard/styles.test.ts` lines 499-549 — `atRuleBodiesFor`'s JSDoc and implementation, so
      you can see for yourself that it filters on at-rule-scoped-ness and selector token, never on the
      at-rule's prelude text. This is why none of its call sites need editing.
    - `src/dashboard/styles.test.ts` lines 460-497 — `assertNoAtRuleOverride`, the throwing sibling,
      same breakpoint-agnostic property.
    - `22-VERIFICATION.md` frontmatter `gaps[0]` and its `missing[0]` — the wording this task's new
      breadth guard encodes as a test.
  </read_first>
  <action>
    Rename `calendar380Block()` (line 1949) to `calendarCompactionBlock()` and change its needle regex
    from `/@media \(max-width: 380px\)\s*\{/g` to `/@media \(max-width: 640px\)\s*\{/g`. The
    `.calendar-` body filter stays and is what disambiguates it from the nav-collapse 640px block at
    `styles.css:465`. Update its JSDoc to say it extracts the ONE `@media (max-width: 640px)` block
    that mentions a `.calendar-` selector, and to keep the existing explanation of why the positional
    assertions need it (RULE_SCANNER swallows an at-rule block's first nested rule into the prelude's
    pseudo-body). Update both call sites, at lines 2053 and 2060.

    Change the IN-06 case at 1927-1935: `expect(matches).toHaveLength(3)` becomes
    `toHaveLength(2)`, and rewrite its title and comment to state that the two remaining
    `@media (max-width: 380px)` blocks govern `.chart-band__canvas-wrap` and
    `.pr-evolution-card__canvas-wrap` only, the calendar's compaction having moved to 640px. Add an
    assertion in the same case that none of the two matched 380px blocks contains the substring
    `.calendar-`.

    Update every `it()` and `describe()` title in the two Phase 22 describes (lines 1871, 1872, 1877,
    1882, 1887, 1892, 1907, 1967, 1968, 1979, 1991, 2005, 2013, 2026, 2035, 2045, 2058) so that "380px"
    reads "the 640px calendar compaction breakpoint" or "at <=640px" as fits the sentence. Change no
    assertion body in any of them — the point of this rename is that the assertions were already
    breakpoint-agnostic and are still true.

    Then add a new `describe('styles.css — Phase 22 gap closure round 4 (22-15): GC-7, the compaction
    breakpoint COVERAGE BAND')` containing four cases:

    - **GC-7a, the breadth guard.** Parse the numeric `max-width` value out of the calendar compaction
      block's own prelude — locate the `@media (max-width: NNNpx) {` whose brace-matched body contains
      `.calendar-`, capture `NNN`, parse it as an integer — and assert
      `expect(breakpointPx).toBeGreaterThanOrEqual(530)`. Comment it with the reason in one paragraph:
      CR-02's arithmetic puts the grid's content-derived minimum at ~480px against `viewport - 48px` of
      available width, so the last overflowing viewport is around 530px; a compaction gated below that
      leaves real phone widths (390/393/412px) exposed, which is exactly how `22-VERIFICATION.md`
      reopened Gap 1 after Round 3's 380px-scoped fix passed a 375px observation. State that this case
      exists to make the fix's BREADTH, not merely its existence, a test-enforced invariant.
    - **GC-7b, the three phone widths.** Assert that each of 390, 393 and 412 is `<=` the parsed
      breakpoint, naming the devices in the assertion messages (iPhone 12-15, iPhone 15/16 Pro, Pixel).
      This is deliberately redundant with GC-7a and is written so that a reader of a future failure
      sees immediately which real devices fall out of coverage.
    - **GC-7c, no calendar rule left behind at 380px.** Assert that `calendarCompactionBlock()` returns
      a body containing all of `minmax(0, max-content)`, `min-width: 0`, `white-space: normal`,
      `overflow-wrap: anywhere`, `justify-self: start` and `grid-template-columns: 1fr`, and that no
      `@media (max-width: 380px)` block in `cssNoComments` contains `.calendar-`.
    - **GC-7d, D-10 survives at desktop.** Assert `bodyForSelectorListToken('.calendar-grid')` still
      contains `grid-template-columns: repeat(7, 1fr) auto`, that
      `bodyForSelectorListToken('.calendar-day')` still contains `min-width: 32px` and
      `grid-template-columns: 1fr 1fr 1fr`, and that `bodyForSelectorListToken('.calendar-week-total')`
      still contains `white-space: nowrap` — the eight-column contract and the default cell shape are
      unchanged above the breakpoint, which is what plan 22-09 correctly protected.

    Do not edit any helper other than `calendar380Block`. Do not weaken or delete any existing
    assertion.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npx tsc --noEmit -p tsconfig.json && node -e '
const fs=require("fs");
const t=fs.readFileSync("src/dashboard/styles.test.ts","utf8");
if(/calendar380Block/.test(t)) throw new Error("calendar380Block was not renamed - it still needles the wrong breakpoint");
if(!/calendarCompactionBlock/.test(t)) throw new Error("calendarCompactionBlock helper is missing");
if(!/toBeGreaterThanOrEqual\(530\)/.test(t)) throw new Error("GC-7a breadth guard missing - nothing prevents the breakpoint being re-narrowed");
for(const w of ["390","393","412"]) if(!t.includes(w)) throw new Error("GC-7b does not name the real phone width "+w+"px");
if(!/toHaveLength\(2\)/.test(t)) throw new Error("the IN-06 380px block count was not updated to 2");
if(/@media \\\(max-width: 380px\\\)\\s\*\\\{/.test(t) && !/@media \\\(max-width: 640px\\\)\\s\*\\\{/.test(t)) throw new Error("the block extractor still needles 380px");
console.log("styles.test.ts re-pointed to the 640px calendar compaction block with a >=530px breadth guard");
' && node -e '
// Non-vacuity proof: re-narrow the calendar breakpoint back to 380px and assert styles.test.ts goes RED.
const fs=require("fs"), cp=require("child_process");
const p="src/dashboard/styles.css", orig=fs.readFileSync(p,"utf8");
const noc=orig.replace(/\/\*[\s\S]*?\*\//g,"");
const re=/@media \(max-width: 640px\)\s*\{/g; let m, target=-1;
while((m=re.exec(noc))){ let i=m.index+m[0].length,d=1; while(d>0&&i<noc.length){ if(noc[i]==="{")d++; else if(noc[i]==="}")d--; i++; } if(noc.slice(m.index,i).includes(".calendar-")) target=m.index; }
if(target<0) throw new Error("could not locate the calendar 640px block to mutate");
const at=orig.indexOf("@media (max-width: 640px) {", orig.indexOf(".calendar-week-total__count"));
if(at<0) throw new Error("could not locate the calendar 640px prelude in the raw file");
fs.writeFileSync(p, orig.slice(0,at)+"@media (max-width: 380px) {"+orig.slice(at+"@media (max-width: 640px) {".length));
let failed=false;
try { cp.execSync("npx vitest run src/dashboard/styles.test.ts",{stdio:"pipe"}); } catch { failed=true; } finally { fs.writeFileSync(p,orig); }
if(!failed) throw new Error("VACUOUS GUARD: re-narrowing the calendar compaction back to 380px left styles.test.ts GREEN - Round 3s exact mistake would ship again unnoticed");
console.log("non-vacuity confirmed: re-narrowing the breakpoint turns styles.test.ts RED, and styles.css was restored");
'</automated>
  </verify>
  <acceptance_criteria>
    - `calendar380Block` no longer appears anywhere in `styles.test.ts`; `calendarCompactionBlock`
      exists, needles `@media (max-width: 640px)`, filters on `.calendar-`, and is called at both
      former call sites.
    - The IN-06 case asserts `toHaveLength(2)` for `@media (max-width: 380px)` and additionally
      asserts neither matched block contains `.calendar-`.
    - Every `it()`/`describe()` title in the two Phase 22 describes names the 640px breakpoint rather
      than 380px, and no assertion body in any of them was changed.
    - A new round-4 describe exists with GC-7a, GC-7b, GC-7c and GC-7d as specified. GC-7a parses the
      breakpoint out of the calendar block's own prelude and asserts `>= 530`. GC-7b names 390, 393 and
      412 with their devices. GC-7d asserts the three default-breakpoint declarations survive.
    - **Non-vacuity gate (mandatory):** rewriting the calendar compaction block's prelude back to
      `@media (max-width: 380px)` makes `npx vitest run src/dashboard/styles.test.ts` FAIL. The verify
      command performs the mutation, asserts the red, and restores `styles.css`.
    - `npx vitest run src/dashboard/styles.test.ts` is green and `npx tsc --noEmit -p tsconfig.json`
      exits 0.
  </acceptance_criteria>
  <done>The stylesheet's tests describe the breakpoint that actually ships, and a parsed-breakpoint floor makes the fix's coverage band — not merely its existence — an enforced invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewport width → rendered layout | an untrusted, unbounded input: the app cannot constrain the width a user's device reports, so every declared breakpoint is a claim about a band of inputs |
| future editor → shipped CSS | a regression here is invisible to type-checking and to every text-level assertion that does not read the at-rule prelude |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-R4-07 | Denial of Service | the calendar grid overflowing its panel or the viewport at 381-530px, making week-total and day-cell values unreadable — the whole of CAL-02's user-facing value at real phone widths | mitigate | the compaction's prelude is raised to `max-width: 640px`, covering the entire computed overflow band with ~113px of headroom at the boundary; developer-observed at a stated 390/393/412px width by non-waivable checkpoint row R25 |
| T-22-R4-08 | Tampering | a future edit narrowing the breakpoint back toward 380px, silently reopening the band behind a green suite — the literal Round 3 failure mode | mitigate | GC-7a parses the breakpoint out of the block's own prelude and asserts `>= 530`; the Task 2 verify command mutates the prelude back to 380px and FAILS the task unless the suite goes red |
| T-22-R4-15 | Denial of Service | a value CLIPPED or TRUNCATED rather than wrapped, which would render a numerically WRONG distance while looking clean | mitigate | GC-4 invariant retained and re-asserted: no `overflow`, `overflow-x` or `text-overflow` is declared in the calendar block at any breakpoint; row R25 requires an explicit clipped-versus-overflowing statement |
| T-22-R4-16 | Tampering | the widened breakpoint silently restyling the desktop grid, breaking D-10's eight-column contract that plan 22-09 protected | mitigate | GC-7d asserts the default-breakpoint `repeat(7, 1fr) auto`, `min-width: 32px`, `1fr 1fr 1fr` and `white-space: nowrap` declarations all survive; Task 1's verify re-reads all four from source |
| T-22-R4-17 | Denial of Service | the compaction now applying at 381-640px, a band no round has ever seen rendered, producing a newly-broken small-tablet layout | mitigate | non-waivable checkpoint row R26 requires a developer observation at a stated ~600px width with values read back and an explicit legible/not-legible judgement |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs nothing — no manifest is in `files_modified` and the change is two files of CSS and test source. `22-RESEARCH.md`'s Package Legitimacy Gate is not triggered; no `[ASSUMED]`/`[SUS]` package exists in this plan |

No `high`-severity threat is left undispositioned.
</threat_model>

<verification>
- `npx vitest run src/dashboard/styles.test.ts` is green; `npx vitest run src/dashboard` is fully green.
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- `npm run build-widgets` exits 0 with zero `css-syntax-error` in its captured output.
- Both non-vacuity mutation gates go red and restore their files; `git status --porcelain` shows only
  this plan's two files.
- The rendered result at a real phone width above 380px cannot be observed by any automated gate in
  this repository (`vitest.config.ts` is `environment: 'node'`, there is no jsdom and no headless
  browser). That is checkpoint rows **R25** (390/393/412px) and **R26** (~600px), staged in
  `22-VALIDATION.md` § Round 4 and run by plan `22-16`. Both are non-waivable (house rule 14).
</verification>

<success_criteria>
- The calendar compaction governs 0-640px; the two remaining 380px blocks are non-calendar.
- Every declaration inside the moved block is unchanged.
- D-10's eight-column default shape and GC-4's no-overflow-property rule both survive.
- A parsed-breakpoint `>= 530px` guard fails if the band is ever re-narrowed.
- `build-widgets` is clean.
</success_criteria>

<output>
Create `.planning/phases/22-calendar-week-start-totals/22-15-SUMMARY.md` when done.
</output>
