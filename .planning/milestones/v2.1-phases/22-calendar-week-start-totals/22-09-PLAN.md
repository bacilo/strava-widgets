---
phase: 22-calendar-week-start-totals
plan: 09
type: execute
wave: 9
depends_on: ["22-08"]
gap_closure: true
round: 3
files_modified:
  - src/dashboard/styles.css
  - src/dashboard/styles.test.ts
autonomous: true
requirements: [CAL-02, CAL-03]
must_haves:
  decisions: [D-09, D-10, D-11, GC-4]
  truths:
    - "GC-4 / BL-01: at 380px `.calendar-grid` redeclares `grid-template-columns: repeat(7, minmax(0, 1fr)) minmax(0, max-content)` AND `.calendar-week-total` declares `min-width: 0` + `white-space: normal`, so the 8th (Total) track no longer holds a hard content-based floor that wins 100% of the width negotiation while the seven day tracks absorb the entire shortfall"
    - "GC-4 / BL-02: at 380px `.calendar-day` collapses its inner three-column grid to a single-column stack (`grid-template-areas: \"number\" \"distance\" \"count\"`, `grid-template-columns: 1fr`) and `.calendar-day__number`/`__distance`/`__count` all take `justify-self: start`, so the distance string gets the cell's FULL width instead of a centred third of an already-collapsed cell"
    - "BL-01 and BL-02 land TOGETHER in this one plan with their `styles.test.ts` updates. `22-REVIEW.md` states in terms that verifying either alone reproduces another ambiguous R13-style result; splitting them across plans is forbidden"
    - "The min-content floor of the two strings that actually overflow is itself collapsed: `.calendar-day__distance` and `.calendar-week-total` declare `overflow-wrap: anywhere` at 380px. At 380px a day cell's content box is roughly 28px wide, which is at the edge of an unbreakable `42.2` token at 14px — `anywhere` (not `break-word`) is required because only `anywhere` lowers the element's MIN-CONTENT contribution, which is what the grid track floor is computed from"
    - "D-10's eight-column contract survives as the DEFAULT-breakpoint shape: the top-level `.calendar-grid` rule still declares `grid-template-columns: repeat(7, 1fr) auto` and the grid still has exactly eight tracks at 380px. Only the track SIZING FUNCTIONS change at 380px. This deliberately supersedes plan 22-06's must-have that the track list is 'unchanged at every breakpoint' — 22-CONTEXT.md's Claude's-Discretion clause assigns 'the exact CSS track for the 8th column (`auto` vs `minmax(...)`) and the mobile strategy' to the planner, and BL-01 is the exercise of exactly that discretion"
    - "`styles.test.ts:1858`'s bare `assertNoAtRuleOverride('.calendar-grid', 'grid-template-columns')` — the assertion that locks the FAILING shape in place by asserting the track list can never be overridden at any breakpoint — is inverted to a `.toThrow(/redeclares \"grid-template-columns\"/)` pairing plus a value read of the override"
    - "WR-03 is closed for every declaration the 380px block carries: a new `atRuleBodiesFor` helper (sibling of `assertNoAtRuleOverride`, sharing the same `RULE_SCANNER`/`isAtRuleScoped`/`splitTopLevelSelectors` substrate) RETURNS the at-rule-scoped bodies so each override is asserted BY VALUE, not merely by existence. Changing `min-width: 0` to `min-width: 200px` or `font-size: 14px` to `font-size: 40px` now goes red"
    - "WR-06 is closed by a test rather than a comment: the `.calendar-day, .calendar-week-total { padding }` rule is still the FIRST nested rule inside the calendar 380px block, and a positional assertion enforces it — every rule this round adds sits below it and is therefore visible to the guard machinery"
    - "WR-07: the 380px `.calendar-week-total__distance` override is reduced to `font-size: 14px` alone — the `line-height: 1.5` (which RAISED the tallest cell's line box at the one breakpoint where space is scarcest, diverging from the base 1.2 and from its `.calendar-day__distance` counterpart) and the no-op `font-weight: 600` are dropped"
    - "No horizontal-scroll wrapper is built: no rule in the calendar 380px block declares `overflow` or `overflow-x` (`overflow-wrap` is a different property and is required), and the DISC-6b fallback comment survives, updated rather than deleted"
    - "This plan discharges NO visual claim. Whether the ~380px overflow is actually gone is decided ONLY by plan 22-12's Round 3 row R19, observed by the developer's own eyes with no waiver (PROJECT.md line 49; R11 and R13 were both visual judgements)"
  artifacts:
    - path: "src/dashboard/styles.css"
      provides: "the BL-01 track-sizing fix, the BL-02 day-cell stack, the two `overflow-wrap: anywhere` floors, the WR-07 cleanup, and a DISC-6b comment that records Round 3"
      contains: "minmax(0, max-content)"
    - path: "src/dashboard/styles.test.ts"
      provides: "the `atRuleBodiesFor` value-reading helper, the inverted `.calendar-grid` assertion, and a Round 3 describe asserting every 380px override BY VALUE"
      contains: "atRuleBodiesFor"
  key_links:
    - from: ".calendar-grid's 8th track"
      to: "the 380px squeeze"
      via: "minmax(0, max-content) replacing auto inside the 380px block"
      pattern: "repeat\\(7, minmax\\(0, 1fr\\)\\) minmax\\(0, max-content\\)"
    - from: ".calendar-day__distance"
      to: "the day cell's full content width"
      via: "the single-column grid-template-areas stack plus justify-self: start"
      pattern: "grid-template-columns: 1fr;"
    - from: "each 380px override declaration"
      to: "a value assertion"
      via: "atRuleBodiesFor in styles.test.ts"
      pattern: "atRuleBodiesFor"
---

<objective>
Close **Gap 1** of `22-VERIFICATION.md` — the ~380px day-cell overflow, FAILED TWICE by the
developer's own eyes (Round 1 row R11, Round 2 row R13) — by fixing BOTH root causes
`22-REVIEW.md` names, in one plan, with their test substrate.

Purpose: `REQUIREMENTS.md` keeps **CAL-02** `Pending` solely because of R11/R13. Round 2's answer
(plan `22-06`) deepened type compaction and was re-asked at R13 against a provably fresh build; the
developer recorded FAIL again, verbatim: *"Days still overflow. Total colum remains wide (wider than
any other column) and all text fits. But other columns (day columns) become narrow and distance text
overflows."* `22-REVIEW.md` root-causes that sentence exactly:

- **BL-01** — the seven day tracks were floored to zero at 380px (`.calendar-day { min-width: 0 }`)
  but the 8th track kept `white-space: nowrap` with no `min-width` override, so it holds a hard
  content-based floor and the day columns absorb the entire width shortfall. 22-06 did not reduce
  the overflow; it RELOCATED it, from the page into the cells.
- **BL-02** — each `.calendar-day` is itself a three-column grid with `.calendar-day__distance`
  pinned to the CENTRED middle column, and the 380px block only ever touched its `font-size`. The
  string is laid out in roughly a third of an already-collapsed cell and spills symmetrically past
  both borders.

Output: both fixes in the same `@media (max-width: 380px)` block, two `overflow-wrap: anywhere`
floors that make the fix hold at the arithmetic edge, the WR-07 line-height cleanup, and a
`styles.test.ts` substrate that asserts each override's VALUE instead of its mere existence.

**Explicitly NOT in scope.** The DISC-6b `.splits-scroll`-style horizontal-scroll wrapper is still
NOT implemented — its comment is updated, never deleted, and it remains the documented fallback if
Round 3's checkpoint fails too. **WR-05** (the 8px `.calendar-weekday--total` header/value padding
offset) is NOT fixed — Round 2 row R17 read the current alignment as correct and the offset is cell
padding; touching it would add a second variable to R19's judgement. **WR-04**, **WR-08**–**WR-11**
are out of scope. `calendar.ts` is NOT edited by this plan (plan `22-10` owns it this round).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md
@.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md
@.planning/phases/22-calendar-week-start-totals/22-REVIEW.md
@.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md
@.planning/phases/22-calendar-week-start-totals/22-06-PLAN.md
</context>

<interfaces>
<!-- Current state extracted from source. The executor still reads the files (see read_first),
     but needs no exploration to know what it is walking into. -->

`src/dashboard/styles.css` — the calendar block as it stands after plan 22-06:

- `.calendar-grid` (741-745): `display: grid`, `grid-template-columns: repeat(7, 1fr) auto`,
  `gap: var(--space-xs)`
- `.calendar-weekday` (747-753) / `.calendar-weekday--total` (760-762): `text-align: center` /
  `text-align: right`
- `.calendar-day` (764-778): `display: grid`, `min-width: 32px`, `min-height: 32px`,
  `padding: var(--space-sm)`, `grid-template-areas: "number . ." ". distance ." ". . count"`,
  `grid-template-columns: 1fr 1fr 1fr`
- `.calendar-day__number` (791-797): `justify-self: start`
- `.calendar-day__distance` (799-805): `font-size: 20px`, `line-height: 1.2`,
  `justify-self: center`  ← BL-02's amplifier
- `.calendar-day__count` (807-813): `justify-self: end`
- `.calendar-week-total` (825-833): `justify-items: end`, `padding: var(--space-sm)`,
  `white-space: nowrap`  ← BL-01's unyielding floor
- `.calendar-week-total__distance` (835-839): `font-size: 20px`, `line-height: 1.2`
- `.calendar-week-total__time` (841-846) / `__count` (848-853): `font-size: 14px`
- the DISC-6b/GC-1/IN-06/WR-03 comment (855-877) and the calendar
  `@media (max-width: 380px)` block (878-902), whose five rules are, in source order:
  1. `.calendar-day, .calendar-week-total { padding: var(--space-xs); }`  ← position 1, structurally
     unguardable (WR-03/WR-06) — MUST stay first
  2. `.calendar-day { min-width: 0; }`
  3. `.calendar-day__distance { font-size: 14px; }`
  4. `.calendar-week-total__distance { font-size: 14px; font-weight: 600; line-height: 1.5; }`
  5. `.calendar-week-total__time, .calendar-week-total__count { font-size: 12px; }`
- the file's other two `@media (max-width: 380px)` blocks govern `.chart-band__canvas-wrap` and
  `.pr-evolution-card__canvas-wrap` and are NOT touched.

`src/dashboard/styles.test.ts` — module-scoped helpers (use these; the only new one is Task 2's):

- `const css: string` (14) — raw stylesheet, comments included
- `const cssNoComments: string` (31) — comments stripped
- `splitTopLevelSelectors(head): string[]` (86) — depth-aware comma split
- `RULE_SCANNER(): RegExp` (118) — `/([^{}]+)\{([^}]*)\}/g`, fresh instance per call. Its body class
  `[^}]*` permits an unmatched `{`, which is exactly why an at-rule block's FIRST nested rule is
  swallowed into the prelude's pseudo-body (WR-03/WR-06)
- `bodyForSelectorListToken(needle, source?): string` (309) — last TOP-LEVEL body; skips every
  at-rule-scoped rule by construction
- `cascadeWinningBodyDeclaring(needle, property, source?): string` (428) — same at-rule blind spot
- `isAtRuleScoped(index, head, source): boolean` — the predicate both halves share
- `assertNoAtRuleOverride(needle, property, source?): void` (470) — THROWS with a message matching
  `/redeclares "<property>"/` when an at-rule-scoped rule redeclares `property` for `needle`

Existing Phase 22 CSS describes in `styles.test.ts`:

- `describe('styles.css — Phase 22 calendar week totals')` (1751-1817), 15 `it(` cases — **NOT
  edited by this plan**, byte-identical, still 15 cases
- `describe('styles.css — Phase 22 gap closure (22-06): the 380px compaction and the Total header
  modifier')` (1819-1871), 8 `it(` cases — Task 2 edits exactly ONE of them (the `.calendar-grid`
  case at 1855-1860) and leaves the count at 8
</interfaces>

<settled_decisions>
The planner settled these; an executor implements them and does not reopen them.

| # | Choice | Decision | Rationale |
|---|--------|----------|-----------|
| GC-4a | Which fixes land | **BL-01 and BL-02 together, in this one plan, with their test updates** | `22-REVIEW.md` BL-02: "Both BL-01 and BL-02 should land in the same round; verifying either in isolation will produce another ambiguous R13-style result." Two rounds have already been spent on ambiguity |
| GC-4b | The 8th track's sizing function at 380px | `minmax(0, max-content)`, with `repeat(7, minmax(0, 1fr))` for the day tracks | Verbatim `22-REVIEW.md` BL-01's suggested fix, which `22-VERIFICATION.md` copies into Gap 1's `missing:` list. `minmax(0, 1fr)` makes the day tracks' zero floor explicit and independent of `.calendar-day { min-width: 0 }` rather than implied by it |
| GC-4c | D-10 vs. the track change | The 8th track's SIZING FUNCTION changes at 380px; the eight-track CONTRACT does not | 22-CONTEXT.md § Claude's Discretion assigns "the exact CSS track for the 8th column (`auto` vs `minmax(...)`) and the mobile strategy" to the planner. Plan 22-06's must-have that the track list is unchanged at every breakpoint was a Round-2 choice, not a locked user decision, and it is deliberately superseded here — it is the assertion at `styles.test.ts:1858` that has been locking the failing shape in place |
| GC-4d | The day cell at 380px | Single-column stack, `justify-self: start` on all three children | Verbatim `22-REVIEW.md` BL-02's suggested fix and `22-VERIFICATION.md`'s second `missing:` item |
| GC-4e | `overflow-wrap: anywhere` on the two overflowing elements | **Added**, beyond BL-01/BL-02's literal sketch | Arithmetic: at a 380px viewport with 7×4px gaps, 4px cell padding and a 1px border per side, a day cell's content box lands near 28px. `42.2` at 14px is roughly that wide, so the fix sits on the arithmetic edge without a break opportunity inside the token. `anywhere` — NOT `break-word` — is required: only `anywhere` lowers the element's min-content contribution, and the grid track floor is computed from min-content. This is a strengthening of the prescribed fix, never a substitute for either half of it |
| GC-4f | WR-07 (`line-height: 1.5` at 380px) | **Fixed** — the override becomes `font-size: 14px` alone | It is a declaration inside the exact block being edited, it raises height at the one breakpoint where space is scarcest, and `font-weight: 600` duplicates the base rule. Strictly reduces the 380px height budget; cannot make R19 worse |
| GC-4g | WR-05 (the 8px header/value edge offset) | **Not fixed** | Round 2 row R17 read the current alignment as correct ("the 8px difference is cell padding"). Changing it adds a second variable to R19/R21's judgement for no gap-closing benefit |
| GC-4h | Rule ORDER inside the 380px block | The padding rule stays in position 1; every new rule goes below it, and a TEST now enforces it | WR-03/WR-06: position 1 is swallowed by `RULE_SCANNER` and is structurally unguardable. 22-06 held this as a convention in a comment with no test behind it; Task 2 closes that |
| GC-4i | How WR-03's "existence, not value" gap closes | A new `atRuleBodiesFor` sibling helper, built on the SAME `RULE_SCANNER`/`isAtRuleScoped`/`splitTopLevelSelectors` substrate as `assertNoAtRuleOverride` | `22-REVIEW.md` WR-03's own suggested fix. Reusing the substrate means the returning half and the throwing half can never disagree about what a rule or a selector token is — the property the existing helper pair already documents as load-bearing |
</settled_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Fix BL-01 and BL-02 together in the 380px block, add the two overflow-wrap floors, and close WR-07</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/styles.css` lines 707-915 — the ENTIRE calendar block as it currently stands:
      `.calendar-grid`'s `repeat(7, 1fr) auto` (741-745), `.calendar-day`'s inner three-column grid
      and `grid-template-areas` (764-778), `.calendar-day__number`/`__distance`/`__count` and their
      three different `justify-self` values (791-813), `.calendar-week-total`'s `justify-items: end`
      + `white-space: nowrap` (825-833), the four total sub-line rules (835-853), the
      DISC-6b/GC-1/IN-06/WR-03 comment (855-877) and the five-rule 380px block (878-902). Read the
      CURRENT declarations before editing — this task reconciles with them, it does not paste over
      them.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § BL-01 and § BL-02 in full,
      including both "Fix:" code blocks and BL-02's closing sentence that the two must land in the
      same round. These two sections are the specification this task implements.
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` Gap 1's `missing:` list —
      the six items that define done for this gap, two of which are the BL-01/BL-02 fixes verbatim.
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` Round 2 row R13 (line 478) —
      the developer's verbatim FAIL, which is what this CSS answers. Also Round 1 row R11 (line
      305), the same judgement one round earlier.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § WR-06 and § WR-07 — why the
      first nested rule of the block is structurally unguardable (so new rules must NOT be placed in
      position 1), and the `line-height` divergence this task removes.
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` (skim the Gap-Closure
      Record only) — this project shipped a CSS comment with a stray `*/` that silently discarded a
      live custom property and passed every text-assertion gate. Any comment written here is proven
      by a clean `npm run build-widgets` with zero `css-syntax-error`, not by reading it back.
  </read_first>
  <action>
    Edit ONLY the calendar `@media (max-width: 380px)` block at `src/dashboard/styles.css:878-902`
    and its preceding comment (855-877). Do not edit any top-level calendar rule, and do not move,
    merge, reorder or touch the file's other two 380px blocks
    (`.chart-band__canvas-wrap`, `.pr-evolution-card__canvas-wrap`).

    **Rule 1 stays exactly where it is.** `.calendar-day, .calendar-week-total { padding:
    var(--space-xs); }` must remain the FIRST nested rule inside the block, byte-identical
    (GC-4h / WR-03 / WR-06). Every rule below is what the block must contain when this task is done,
    in this source order:

    2. Selector exactly `.calendar-grid`, declaring only
       `grid-template-columns: repeat(7, minmax(0, 1fr)) minmax(0, max-content);` — BL-01. Declare
       no `gap`, no `display`, no `overflow`.
    3. Selector exactly `.calendar-day`, declaring exactly three properties: the existing
       `min-width: 0;`, plus `grid-template-areas` set to the three single-column strings
       `"number"`, `"distance"`, `"count"` (one per line, in that order — the base rule's
       `"number . ."` / `". distance ."` / `". . count"` three-column form must NOT survive here),
       plus `grid-template-columns: 1fr;` — BL-02.
    4. Selector list exactly `.calendar-day__number, .calendar-day__distance, .calendar-day__count`,
       declaring only `justify-self: start;` — BL-02. This is what overrides `__distance`'s base
       `justify-self: center` (the centring that makes the overflow spill past BOTH borders) and
       `__count`'s base `justify-self: end`.
    5. Selector exactly `.calendar-day__distance`, declaring `font-size: 14px;` (unchanged from
       22-06) plus `overflow-wrap: anywhere;` — GC-4e. Use `anywhere`, never `break-word`: only
       `anywhere` lowers the element's min-content contribution, and the grid track floor is
       computed from min-content.
    6. Selector exactly `.calendar-week-total`, declaring `min-width: 0;`, `white-space: normal;`
       and `overflow-wrap: anywhere;` — BL-01's second half plus GC-4e. `white-space: normal` is
       what lets `10h 14m` wrap at its space instead of holding a one-line max-content floor.
    7. Selector exactly `.calendar-week-total__distance`, declaring ONLY `font-size: 14px;` — WR-07.
       Delete the `line-height: 1.5;` and the `font-weight: 600;` this rule carries today.
    8. Selector list exactly `.calendar-week-total__time, .calendar-week-total__count`, declaring
       only `font-size: 12px;` (unchanged from 22-06).

    Declare nothing else anywhere in the block. In particular: no `overflow`, no `overflow-x`, no
    `text-overflow`, no `display`, no `position` — `overflow-wrap` is a different property and is
    required, but a real `overflow:` declaration would be the scroll-wrapper fallback this round is
    still not building, and truncation would silently render a WRONG number instead of a clipped one.

    **Comment content.** Update the existing DISC-6b comment above the block; do not delete it and
    do not start a second comment. It must RETAIN every literal token it carries today —
    `DISC-6b`, `splits-scroll`, `GC-1`, `IN-06`, `WR-03`, `R11` — and gain these, each named
    literally: `R13`, `GC-4`, `BL-01`, `BL-02`, `WR-07`. The prose must state:

    1. that Round 2 row `R13` re-asked `R11` against the 22-06 build and recorded FAIL again, and
       that `22-REVIEW.md` root-caused it as two compounding causes;
    2. `BL-01`: the 8th track kept a `white-space: nowrap` content floor while only the day tracks
       were relaxed, so the day columns absorbed the whole shortfall — fixed by giving every track
       a zero floor and letting the Total track participate in the squeeze;
    3. `BL-02`: the day cell's own inner three-column grid gave the centred distance a third of an
       already-collapsed cell — fixed by stacking the cell into one column at this breakpoint only;
    4. that `overflow-wrap: anywhere` (not `break-word`) is deliberate, because only `anywhere`
       lowers min-content and the track floor is computed from min-content;
    5. `WR-07`: why the `line-height: 1.5` override was dropped (it raised the tallest cell's line
       box at the narrowest breakpoint and diverged from both the base 1.2 and its `.calendar-day__distance`
       counterpart);
    6. that the `splits-scroll` wrapper is STILL the documented, unimplemented fallback if `R19`
       fails too — keep GC-1's focusable-day-buttons/Tab-scroll-jump reason;
    7. that `IN-06` still holds (three disjoint 380px blocks, consolidation still declined) and
       `WR-03`/`WR-06` still hold (the padding rule is kept in position 1 deliberately, and Round 3
       now has a TEST enforcing it rather than only this sentence).

    Do not write the character sequence `*/` inside the comment body other than as its terminator,
    and do not nest a `/*` inside it.
  </action>
  <verify>
    <automated>node --input-type=module -e 'import {readFileSync} from "node:fs"; const css=readFileSync("src/dashboard/styles.css","utf8"); const s=css.replace(/\/\*[\s\S]*?\*\//g,""); const starts=[...s.matchAll(/@media \(max-width: 380px\)\s*\{/g)]; if(starts.length!==3) throw new Error("expected exactly 3 comment-stripped 380px blocks, found "+starts.length); const blocks=starts.map(m=>{let i=m.index+m[0].length,d=1; while(d>0&&i<s.length){ if(s[i]==="{")d++; else if(s[i]==="}")d--; i++; } return s.slice(m.index+m[0].length,i-1);}); const cal=blocks.filter(b=>b.includes(".calendar-")); if(cal.length!==1) throw new Error("expected exactly one calendar 380px block, found "+cal.length); const b=cal[0]; const firstBody=b.slice(b.indexOf("{")+1,b.indexOf("}")); if(!/padding/.test(firstBody)) throw new Error("WR-03/WR-06 ordering broken: the first nested rule must still be the padding rule"); const rules=[...b.matchAll(/([^{}]+)\{([^}]*)\}/g)].map(m=>({sels:m[1].split(",").map(x=>x.trim()),body:m[2]})); const decl=(sel,d)=>rules.some(r=>r.sels.includes(sel)&&r.body.includes(d)); const need=[[".calendar-grid","grid-template-columns: repeat(7, minmax(0, 1fr)) minmax(0, max-content)"],[".calendar-day","min-width: 0"],[".calendar-day","grid-template-columns: 1fr"],[".calendar-day","grid-template-areas"],[".calendar-day__number","justify-self: start"],[".calendar-day__distance","justify-self: start"],[".calendar-day__count","justify-self: start"],[".calendar-day__distance","font-size: 14px"],[".calendar-day__distance","overflow-wrap: anywhere"],[".calendar-week-total","min-width: 0"],[".calendar-week-total","white-space: normal"],[".calendar-week-total","overflow-wrap: anywhere"],[".calendar-week-total__distance","font-size: 14px"],[".calendar-week-total__time","font-size: 12px"],[".calendar-week-total__count","font-size: 12px"]]; for(const [sel,d] of need) if(!decl(sel,d)) throw new Error(sel+" does not declare "+d+" inside the 380px block"); const dayAreas=rules.find(r=>r.sels.includes(".calendar-day")&&r.body.includes("grid-template-areas")); if(/number\s*\.\s*\./.test(dayAreas.body)) throw new Error("BL-02 not applied: the 380px .calendar-day grid-template-areas is still the three-column shape"); const wtd=rules.find(r=>r.sels.includes(".calendar-week-total__distance")); if(/line-height/.test(wtd.body)) throw new Error("WR-07: drop line-height from the 380px .calendar-week-total__distance override"); if(/font-weight/.test(wtd.body)) throw new Error("WR-07: drop the no-op font-weight from the 380px .calendar-week-total__distance override"); const bodies=rules.map(r=>r.body).join(";"); if(/(^|[^-\w])overflow\s*:/.test(bodies)||/overflow-x\s*:/.test(bodies)||/text-overflow\s*:/.test(bodies)) throw new Error("the 380px block must declare no overflow / overflow-x / text-overflow (overflow-wrap is a different property and is required)"); for(const base of ["grid-template-columns: repeat(7, 1fr) auto","min-width: 32px","grid-template-columns: 1fr 1fr 1fr","font-size: 20px","white-space: nowrap","justify-self: center","justify-self: end"]) if(!s.includes(base)) throw new Error("a top-level base declaration was edited away: "+base); const ci=css.indexOf("DISC-6b"); if(ci===-1) throw new Error("the DISC-6b comment was deleted"); const comment=css.slice(ci,css.indexOf("*/",ci)); for(const t of ["splits-scroll","GC-1","IN-06","WR-03","R11","R13","GC-4","BL-01","BL-02","WR-07"]) if(!comment.includes(t)) throw new Error("the DISC-6b comment is missing the token: "+t); console.log("BL-01 + BL-02 landed together, overflow-wrap floors present, WR-07 closed, padding rule still first, base rules intact");' && rm -f /tmp/bw22r3a.log && npm run build-widgets > /tmp/bw22r3a.log 2>&1; BW=$?; tail -5 /tmp/bw22r3a.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw22r3a.log && echo "build clean, no css-syntax-error"</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/styles.css` still contains exactly three `@media (max-width: 380px)` blocks
      after comment stripping, exactly one of which mentions a `.calendar-` selector.
    - The first nested rule inside that calendar block still declares `padding`.
    - Inside that block: `.calendar-grid` declares
      `grid-template-columns: repeat(7, minmax(0, 1fr)) minmax(0, max-content)`; `.calendar-day`
      declares `min-width: 0`, `grid-template-columns: 1fr` and a `grid-template-areas` whose value
      does NOT match `number . .`; the selector list
      `.calendar-day__number, .calendar-day__distance, .calendar-day__count` declares
      `justify-self: start`; `.calendar-day__distance` declares `font-size: 14px` and
      `overflow-wrap: anywhere`; `.calendar-week-total` declares `min-width: 0`,
      `white-space: normal` and `overflow-wrap: anywhere`; `.calendar-week-total__distance` declares
      `font-size: 14px` and NEITHER `line-height` NOR `font-weight`; the selector list
      `.calendar-week-total__time, .calendar-week-total__count` declares `font-size: 12px`.
    - No declaration anywhere in that block matches `overflow:`, `overflow-x:` or `text-overflow:`.
    - The top-level base rules are unedited — the file still contains
      `grid-template-columns: repeat(7, 1fr) auto`, `min-width: 32px`,
      `grid-template-columns: 1fr 1fr 1fr`, `font-size: 20px`, `white-space: nowrap`,
      `justify-self: center` and `justify-self: end`.
    - The DISC-6b comment contains all ten literal tokens `splits-scroll`, `GC-1`, `IN-06`, `WR-03`,
      `R11`, `R13`, `GC-4`, `BL-01`, `BL-02`, `WR-07`.
    - `npm run build-widgets` exits 0 and its captured output contains zero occurrences of
      `css-syntax-error`.
  </acceptance_criteria>
  <done>Both of Gap 1's root causes are fixed in the same block: the Total track can now be squeezed, the day cell gives its distance the full cell width, and neither value has an unbreakable min-content floor larger than the cell it lives in.</done>
</task>

<task type="auto">
  <name>Task 2: Add the atRuleBodiesFor value-reading helper, invert the assertion that locked the failing shape, and guard every 380px override by value</name>
  <files>src/dashboard/styles.test.ts</files>
  <read_first>
    - `src/dashboard/styles.test.ts` lines 100-230 — `RULE_SCANNER`, `computeAtRuleRanges`,
      `isAtRuleScoped`, and `splitTopLevelSelectors`. The new helper reuses all of them; it must not
      introduce a second notion of what a rule or a selector token is.
    - `src/dashboard/styles.test.ts` lines 280-500 — `bodyForSelectorListToken`,
      `bodiesForSelectorListToken`, `cascadeWinningBodyDeclaring` and `assertNoAtRuleOverride`,
      including each JSDoc's explicit statement that the first three skip at-rule-scoped rules by
      construction. `assertNoAtRuleOverride` (470-497) is the exact structural sibling the new
      helper mirrors — read its body, not just its doc.
    - `src/dashboard/styles.test.ts` lines 1751-1817 — the 15-case
      `describe('styles.css — Phase 22 calendar week totals')` block. **Not edited.** Note case
      1786 (`.calendar-week-total declares white-space: nowrap`) and case 1752 (`.calendar-grid`
      declares `repeat(7, 1fr) auto`): both assert a base value that Task 1 now overrides at 380px,
      and both still pass because they read top-level rules only. Their override-aware companions go
      in the NEW describe, not by editing these.
    - `src/dashboard/styles.test.ts` lines 1819-1871 — the 8-case
      `describe('styles.css — Phase 22 gap closure (22-06)...')`. Task 2 edits exactly ONE case in
      it: the `.calendar-grid` case at 1855-1860, whose
      `expect(() => assertNoAtRuleOverride('.calendar-grid', 'grid-template-columns')).not.toThrow()`
      at line 1858 is the assertion `22-VERIFICATION.md` names as locking the failing shape in place.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § WR-03 — the finding
      ("existence proof only … changing `min-width: 0` to `min-width: 200px` keeps every one of
      these five tests green") and its suggested `atRuleBodyFor` shape.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § WR-06 — the positional guard
      this task turns from a comment into a test.
    - `src/dashboard/styles.css` as left by Task 1.
  </read_first>
  <action>
    **Helper.** Immediately after `assertNoAtRuleOverride` in `src/dashboard/styles.test.ts`, add
    `function atRuleBodiesFor(needle: string, property: string, source: string = cssNoComments):
    string[]`. It walks `RULE_SCANNER()` exactly as `assertNoAtRuleOverride` does, keeps only the
    candidates for which `isAtRuleScoped(match.index, head, source)` is true AND whose
    `splitTopLevelSelectors(head)` contains `needle` as an exact token AND whose body declares
    `property` at a declaration boundary (reuse the same `(?:^|;|\s)` anchored, metacharacter-escaped
    pattern the existing helpers build), and returns those bodies in source order. It THROWS, naming
    both `needle` and `property`, when there are none — so a deleted override fails loudly instead
    of silently comparing against `undefined`. Give it a JSDoc naming `WR-03` that states: this is
    the RETURNING sibling of the THROWING `assertNoAtRuleOverride`; the two share
    `RULE_SCANNER`/`isAtRuleScoped`/`splitTopLevelSelectors` so they can never disagree about what a
    rule is; and that a guard which needs to know an override's VALUE (not merely that one exists)
    must use this, because `assertNoAtRuleOverride`'s `toThrow` pairing is an existence proof
    satisfied by any override at any breakpoint with any value.

    **Edit exactly one existing case.** In the 22-06 describe, replace the case currently titled
    `D-10/GC-1: the eight-column contract survives the compaction and the scroll wrapper stays
    unimplemented` with one titled `D-10/GC-4/BL-01: the eight-column contract is the DEFAULT
    shape and is deliberately overridden at 380px`. Its body must: assert
    `bodyForSelectorListToken('.calendar-grid')` still contains
    `grid-template-columns: repeat(7, 1fr) auto`; assert
    `expect(() => assertNoAtRuleOverride('.calendar-grid', 'grid-template-columns'))
    .toThrow(/redeclares "grid-template-columns"/)`; assert the first entry of
    `atRuleBodiesFor('.calendar-grid', 'grid-template-columns')` contains
    `repeat(7, minmax(0, 1fr)) minmax(0, max-content)`; and assert the top-level body still does not
    match `/overflow/`. Add a comment recording that the previous form of this case asserted the
    track list is NEVER overridden, that `22-VERIFICATION.md` named that assertion as what locked
    the R13 failing shape in place, and that GC-4c inverted it deliberately under 22-CONTEXT.md's
    Claude's-Discretion clause for the 8th column's CSS track. Leave the describe at exactly 8 cases
    and change nothing else inside it.

    **New describe.** Append to the END of the file a describe titled exactly
    `styles.css — Phase 22 gap closure round 3 (22-09): BL-01/BL-02 and the 380px override VALUES`,
    holding exactly these nine cases, all reading values via `atRuleBodiesFor` where an override
    exists:

    1. `BL-01: .calendar-week-total is nowrap at the default breakpoint and wraps with a zero floor
       at 380px` — base body contains `white-space: nowrap`; the 380px body for `white-space`
       contains `white-space: normal`; the 380px body for `min-width` contains `min-width: 0`; the
       380px body for `overflow-wrap` contains `overflow-wrap: anywhere`.
    2. `BL-02: .calendar-day is a three-column grid at the default breakpoint and a single-column
       stack at 380px` — base body contains `grid-template-columns: 1fr 1fr 1fr`; the 380px body for
       `grid-template-columns` contains `grid-template-columns: 1fr` and does NOT match
       `/1fr 1fr 1fr/`; the 380px body for `grid-template-areas` does NOT match `/number\s*\.\s*\./`.
    3. `BL-02: all three day-cell children are start-aligned at 380px, overriding centre and end` —
       base `.calendar-day__distance` contains `justify-self: center` and base
       `.calendar-day__count` contains `justify-self: end`; the 380px `justify-self` body for each
       of `.calendar-day__number`, `.calendar-day__distance` and `.calendar-day__count` contains
       `justify-self: start`.
    4. `GC-4e: the two overflowing values declare overflow-wrap: anywhere, not break-word, at 380px`
       — the 380px `overflow-wrap` bodies for `.calendar-day__distance` and `.calendar-week-total`
       each contain `anywhere` and neither matches `/break-word/`.
    5. `WR-03: the 380px day-cell compaction is asserted by VALUE` — the 380px bodies contain
       `min-width: 0` for `.calendar-day` and `font-size: 14px` for `.calendar-day__distance`.
       Add a comment stating this is the case WR-03 says was missing: the pre-existing pairing proves
       only that an override exists, so mutating `min-width: 0` to `min-width: 200px` used to stay
       green and now does not.
    6. `WR-03: the 380px total-cell compaction is asserted by VALUE` — the 380px `font-size` bodies
       contain `font-size: 12px` for `.calendar-week-total__time` and for
       `.calendar-week-total__count`.
    7. `WR-07: the 380px .calendar-week-total__distance override is font-size only` — its 380px
       `font-size` body contains `font-size: 14px` and does not match `/line-height/` or
       `/font-weight/`; and the base body still contains `line-height: 1.2`.
    8. `WR-06: the padding rule is still the FIRST rule inside the calendar 380px block` — slice
       `cssNoComments` from the calendar 380px block's `@media` prelude, brace-match to its closing
       `}`, and assert the substring up to the first `}` contains `padding: var(--space-xs)`. Add a
       comment recording that this is the positional convention `styles.css`'s WR-03 paragraph has
       been holding in prose with no test behind it, that `RULE_SCANNER` swallows the block's first
       nested rule into the prelude pseudo-body, and that any future rule added in position 1 would
       be silently unguarded.
    9. `GC-4: no scroll wrapper was built — no calendar rule declares overflow at any breakpoint` —
       assert `atRuleBodiesFor('.calendar-grid', 'overflow')` THROWS (no such override exists) and
       that the calendar 380px block extracted in case 8 does not match `/(^|[^-\w])overflow\s*:/`.

    Do not edit the 15-case `Phase 22 calendar week totals` describe. Do not add or remove cases from
    the 22-06 describe. Do not delete `assertNoAtRuleOverride` or change its behaviour.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npm test && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e 'import {readFileSync} from "node:fs"; const t=readFileSync("src/dashboard/styles.test.ts","utf8"); if(!/function atRuleBodiesFor\(/.test(t)) throw new Error("atRuleBodiesFor was not added"); const helper=t.slice(t.indexOf("function atRuleBodiesFor(")); const hbody=helper.slice(0,helper.indexOf("\n}")); for(const dep of ["RULE_SCANNER()","isAtRuleScoped","splitTopLevelSelectors"]) if(!hbody.includes(dep)) throw new Error("atRuleBodiesFor does not reuse the shared substrate: "+dep); if(!/throw new Error/.test(hbody)) throw new Error("atRuleBodiesFor must throw when no override exists"); const A="styles.css — Phase 22 calendar week totals"; const B="styles.css — Phase 22 gap closure (22-06)"; const C="styles.css — Phase 22 gap closure round 3 (22-09)"; const ai=t.indexOf(A), bi=t.indexOf(B), ci=t.indexOf(C); if(ai===-1||bi===-1||ci===-1) throw new Error("one of the three Phase 22 describes is missing or misnamed"); if(!(ai<bi&&bi<ci)) throw new Error("the round 3 describe must be appended after both existing Phase 22 describes"); const count=(s)=>(s.match(/\bit\(/g)||[]).length; if(count(t.slice(ai,bi))!==15) throw new Error("the 15-case Phase 22 describe was modified"); if(count(t.slice(bi,ci))!==8) throw new Error("the 22-06 describe must stay at exactly 8 cases, found "+count(t.slice(bi,ci))); const nb=t.slice(ci); if(count(nb)!==9) throw new Error("expected exactly 9 cases in the round 3 describe, found "+count(nb)); if((nb.match(/atRuleBodiesFor/g)||[]).length<12) throw new Error("the round 3 describe reads too few override values through atRuleBodiesFor"); const b06=t.slice(bi,ci); if(/assertNoAtRuleOverride\(.\.calendar-grid., .grid-template-columns.\)\)\.not\.toThrow/.test(b06)) throw new Error("styles.test.ts still asserts .calendar-grid grid-template-columns is NEVER overridden - this is the assertion that locked the failing shape"); if(!/toThrow\(\/redeclares "grid-template-columns"\//.test(b06)) throw new Error("the .calendar-grid case was not inverted to a toThrow pairing"); if(!b06.includes("minmax(0, max-content)")) throw new Error("the .calendar-grid case does not read the override VALUE"); for(const tok of ["overflow-wrap","break-word","justify-self: start","min-width: 200px","padding: var(--space-xs)"]) if(!nb.includes(tok)) throw new Error("the round 3 describe does not reference "+tok); console.log("atRuleBodiesFor in place, .calendar-grid assertion inverted, 9 value-asserting cases, 15/8 pre-existing describes intact");'</automated>
  </verify>
  <acceptance_criteria>
    - `styles.test.ts` declares `function atRuleBodiesFor(`, whose body references `RULE_SCANNER()`,
      `isAtRuleScoped` and `splitTopLevelSelectors`, and which throws when no at-rule override
      declares the property for the selector.
    - The `styles.css — Phase 22 calendar week totals` describe still holds exactly 15 `it(` cases
      and is otherwise unmodified.
    - The `styles.css — Phase 22 gap closure (22-06)` describe still holds exactly 8 `it(` cases;
      its `.calendar-grid` case no longer contains a
      `assertNoAtRuleOverride('.calendar-grid', 'grid-template-columns')).not.toThrow` form, DOES
      contain a `toThrow(/redeclares "grid-template-columns"/)` pairing, and DOES read the override
      value `minmax(0, max-content)`.
    - A describe titled
      `styles.css — Phase 22 gap closure round 3 (22-09): BL-01/BL-02 and the 380px override VALUES`
      is appended after both existing Phase 22 describes and holds exactly 9 `it(` cases, with at
      least 12 `atRuleBodiesFor` reads between them.
    - `npx vitest run src/dashboard/styles.test.ts` exits 0, `npm test` exits 0 with no test
      regressed (baseline 1222 passing plus this plan's new cases), and
      `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>Every declaration the 380px block carries is now asserted by its actual value, the assertion that pinned the failing eight-track shape in place is inverted, and the deliberately-unguardable first rule is held in position by a test rather than by a paragraph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published stylesheet → browser rendering | The only untrusted-ish input crossing here is viewport width; the stylesheet is build-time content |
| stylesheet source → the test suite's regex substrate | `styles.test.ts` parses CSS with regexes carrying documented blind spots (WR-03, WR-06); a guard can silently claim more than it proves |
| archive values → the rendered day cell | A day-cell distance is real activity data of unbounded string length; the layout must degrade by wrapping, never by truncating a number |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-GC3-CSS-01 | Information disclosure | `.calendar-day__distance` / `.calendar-week-total` at ≤380px | mitigate | The fix relaxes floors and permits wrapping; it adds no `overflow: hidden` and no `text-overflow: ellipsis`, so a compacted value is never silently clipped into a DIFFERENT number than the archive holds. Task 1's automated check rejects `overflow:`, `overflow-x:` and `text-overflow:` anywhere in the block |
| T-22-GC3-CSS-02 | Tampering | the CSS guard suite | mitigate | `atRuleBodiesFor` turns five existence proofs into value assertions (WR-03), and the positional guard turns WR-06's prose convention into a test. A regression to any Round 3 declaration now fails loudly instead of leaving a false green — the exact defect class that let 22-06 ship an unmeasured compaction |
| T-22-GC3-CSS-03 | Denial of service | the parsed stylesheet | mitigate | Task 1 rewrites a large CSS comment. Phase 19 GAP 1 proved a malformed comment can silently discard live declarations while every text-assertion gate stays green, so Task 1 gates on `npm run build-widgets` exiting 0 with zero `css-syntax-error` — a real CSS parser, not a regex |
| T-22-GC3-CSS-04 | Elevation of privilege | grid focus order | accept (avoided by design) | GC-1's rejection of the horizontal-scroll wrapper stands: a scroll container full of focusable day buttons scroll-jumps on Tab. This plan introduces no focusable element, no `tabindex` and no scroll container; `calendar.ts` is not edited at all |
| T-22-SC | Tampering | npm/pip/cargo installs | accept (not applicable) | Zero new packages. `22-RESEARCH.md` § Package Legitimacy Audit records "Not applicable"; no install task exists in any Phase 22 plan |
</threat_model>

<verification>
- Task 1 proves BL-01 and BL-02 both landed in the SAME block, that the two `overflow-wrap: anywhere`
  floors are present, that WR-07's `line-height`/`font-weight` are gone, that the padding rule still
  occupies the unguardable position 1, that no `overflow`/`text-overflow` sneaked in, that the
  top-level base declarations were not edited, that the DISC-6b comment gained the Round 3 tokens
  while keeping the Round 2 ones, and that a real CSS parser accepts the file.
- Task 2 proves the value-reading helper exists and reuses the shared substrate, that the assertion
  which locked the failing shape is inverted and now reads the override's value, that the two
  pre-existing describes are untouched at 15 and 8 cases, and that nine new cases assert values
  rather than existence.

**This plan discharges no visual claim.** Whether the ~380px overflow is actually gone is settled
only by plan `22-12`'s Round 3 row **R19**, observed by the developer's own eyes with **no waiver**
(PROJECT.md line 49; R11 and R13 were both visual judgements, and both were FAIL).
</verification>

<success_criteria>
- The calendar 380px block carries BOTH of `22-VERIFICATION.md` Gap 1's prescribed fixes, plus the
  two `overflow-wrap: anywhere` floors that keep them holding at the arithmetic edge.
- `.calendar-grid`'s top-level `repeat(7, 1fr) auto` is intact; only the 380px block redeclares it.
- `styles.test.ts:1858`'s never-overridden assertion is inverted and now reads the override value.
- Every declaration in the 380px block is guarded by value, and the padding rule's position 1 is
  guarded by a test.
- The `.splits-scroll` fallback is still documented and still unimplemented.
- `npm test` and `npx tsc --noEmit -p tsconfig.json` exit 0; `npm run build-widgets` exits 0 with
  zero `css-syntax-error`.
- **CAL-02 is NOT ticked by this plan.**
</success_criteria>

<output>
Create `.planning/phases/22-calendar-week-start-totals/22-09-SUMMARY.md` when done.
</output>
