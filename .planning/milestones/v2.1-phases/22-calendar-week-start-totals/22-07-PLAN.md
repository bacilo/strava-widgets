---
phase: 22-calendar-week-start-totals
plan: 07
type: execute
wave: 7
depends_on: ["22-06"]
gap_closure: true
files_modified:
  - src/dashboard/views/calendar-preferences.ts
  - src/dashboard/views/calendar-preferences.test.ts
  - src/dashboard/views/calendar.ts
  - src/dashboard/views/calendar-logic.ts
  - src/dashboard/views/calendar-logic.test.ts
autonomous: true
requirements: [CAL-01]
must_haves:
  decisions: [D-05, D-06, D-07, D-08, GC-2]
  truths:
    - "GC-2 / CR-01: the `globalThis.localStorage` PROPERTY ACCESS is inside a try/catch. Where site data is blocked (Firefox `Block cookies and site data`, Chrome blocked-origin storage, a storage-partitioned iframe) the getter throws SecurityError before any `getItem` runs — after this plan that throw is absorbed and the Calendar renders with the Monday default instead of collapsing into `main.ts`'s generic `Something went wrong` panel"
    - "GC-2 / CR-01: `calendar.ts` obtains its storage handle ONLY through `resolveWeekStartStorage(deps.storage)`; the string `globalThis.localStorage` appears nowhere in `calendar.ts` after this plan"
    - "T-22-WK-02 is TRUE as documented: `calendar-preferences.ts`'s header no longer claims a `disabled cookies` mitigation its functions cannot deliver — it names the getter as the throwing operation and points at the resolver that guards it"
    - "A regression test exists whose stand-in throws from the `localStorage` PROPERTY GETTER itself, not from `getItem` — the one path none of Round 1's eleven checkpoint rows exercised (R8 tampered the stored VALUE, not the handle's accessibility)"
    - "GC-2 / WR-01: `buildMonthGrid` is total as its JSDoc claims — an off-union `weekStart` falls back to the D-03 Monday offset instead of producing NaN and `RangeError: Invalid array length` from `new Array(NaN)`. Verified by a test asserting the off-union grid deep-equals the `monday` grid"
    - "T-22-WK-01 keeps exactly one trust path: `parseWeekStart`'s allow-list is still the ONLY way a stored value reaches the grid math. The guarded resolver widens NOTHING it trusts — no normalisation, no repair, no write-back, no console output, no JSON parsing (D-07)"
    - "WR-04 counters are undisturbed: `calendar.ts` still contains exactly two `tabindex` occurrences and exactly two `.focus()` occurrences, and `setWeekStart`'s body still contains none of `focus`, `mount(`, `navigateTo`, `await`"
  artifacts:
    - path: "src/dashboard/views/calendar-preferences.ts"
      provides: "`resolveWeekStartStorage`, null-tolerant read/write, and a header whose T-22-WK-02 claim is true as shipped"
      contains: "resolveWeekStartStorage"
    - path: "src/dashboard/views/calendar-preferences.test.ts"
      provides: "a getter-throwing storage stand-in plus null-handle cases"
      contains: "defineProperty"
    - path: "src/dashboard/views/calendar.ts"
      provides: "the single guarded call site that replaces the unguarded `deps.storage ?? globalThis.localStorage`"
      contains: "resolveWeekStartStorage(deps.storage)"
    - path: "src/dashboard/views/calendar-logic.ts"
      provides: "a total week-start offset accessor so the module's `never throws` contract holds"
      contains: "weekStartOffset"
  key_links:
    - from: "globalThis.localStorage (property access)"
      to: "a try/catch"
      via: "resolveWeekStartStorage in calendar-preferences.ts"
      pattern: "resolveWeekStartStorage"
    - from: "calendar.ts mount()"
      to: "resolveWeekStartStorage"
      via: "the single storage-handle resolution site"
      pattern: "resolveWeekStartStorage\\(deps\\.storage\\)"
    - from: "an off-union weekStart"
      to: "the Monday offset"
      via: "weekStartOffset's total accessor"
      pattern: "weekStartOffset"
---

<objective>
Close **Gap 2** of `22-VERIFICATION.md` — code-review finding **CR-01 (Critical)** — and the WR-01
totality defect GC-2 puts in scope alongside it.

Purpose: `calendar.ts:424` reads `const storage = deps.storage ?? globalThis.localStorage;` outside
any try/catch. In a browser where site data is blocked, the `localStorage` **getter itself** throws
`SecurityError` before `readStoredWeekStart`'s try/catch is ever entered. `mount()` runs inside
`main.ts:53-77`'s try/catch, whose catch renders a generic "Something went wrong" panel — so an
optional cosmetic preference takes down the **entire Calendar view**. `calendar-preferences.ts`'s
header currently claims T-22-WK-02 covers "disabled cookies"; that claim is false as shipped, and
this plan makes it true rather than merely rewording it.

Separately, `buildMonthGrid([], {year:2024,month:3}, 'MONDAY')` throws
`RangeError: Invalid array length` at `calendar-logic.ts:219` — executed and confirmed by both
`22-REVIEW.md` (WR-01) and `22-VERIFICATION.md`'s behavioural spot-checks — contradicting the
module's own "Total function: never throws" JSDoc and `calendar-preferences.ts`'s T-22-WK-01 claim
that a tampered value "can never reach the grid math". GC-2 requires the code and its documentation
to agree; this plan makes the code match the documentation.

Output: a guarded storage resolver, a null-tolerant read/write path, corrected security notes, a
total offset accessor, and regression tests for both — including the getter-throwing stand-in that
none of Round 1's eleven checkpoint rows exercised.

**Explicitly NOT in scope.** `WR-05`'s separate disposition is not adjudicated here — the
`WeekStartStorage | null` widening this plan performs is part of CR-01's own prescribed fix in
`22-REVIEW.md`, and any residual WR-05 commentary stays open. `WR-04` (exact-count source guards),
`WR-06`, `IN-01`–`IN-04`, `IN-07`–`IN-13` are all out of scope; this plan only avoids tripping
`WR-04`'s counters. `theme.ts` carries the same unguarded shape and is **not** edited — it is
outside this phase's file scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md
@.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md
@.planning/phases/22-calendar-week-start-totals/22-REVIEW.md
@.planning/phases/22-calendar-week-start-totals/22-PATTERNS.md
@.planning/phases/22-calendar-week-start-totals/22-02-PLAN.md
</context>

<interfaces>
<!-- Current contracts the executor edits. Extracted from source; no exploration needed. -->

`src/dashboard/views/calendar-preferences.ts` (as shipped):

- `export const WEEK_START_STORAGE_KEY = 'dashboard-calendar-week-start'`
- `export interface WeekStartStorage { getItem(key: string): string | null; setItem(key: string, value: string): void }`
- `export function parseWeekStart(raw: unknown): WeekStart` — allow-lists exactly `'sunday'` /
  `'monday'`, otherwise `'monday'`. No console output, no repair, no write-back (D-07).
- `export function readStoredWeekStart(storage: WeekStartStorage): WeekStart` — try/catch around
  `storage.getItem(...)` only.
- `export function writeWeekStart(storage: WeekStartStorage, value: WeekStart): void` — try/catch
  around `storage.setItem(...)` only.

`src/dashboard/views/calendar.ts` (as shipped):

- line 33: `import { readStoredWeekStart, writeWeekStart, type WeekStartStorage } from './calendar-preferences.js';`
- line 349: `storage?: WeekStartStorage` on `CalendarViewDeps`
- line 424: `const storage = deps.storage ?? globalThis.localStorage;`  ← the defect
- line 426: `let weekStart = readStoredWeekStart(storage);`
- line 544: `writeWeekStart(storage, next);` inside `setWeekStart`

`src/dashboard/views/calendar-logic.ts` (as shipped):

- line 103: `export type WeekStart = 'sunday' | 'monday'`
- line 106: `const WEEK_START_OFFSET: Record<WeekStart, number> = { sunday: 0, monday: 1 };`
- line 175: `return (firstWeekdayOfMonth(m) - WEEK_START_OFFSET[weekStart] + 7) % 7;`
- line 216: `const weekCount = Math.max(MIN_WEEK_ROWS, Math.ceil(cellCount / 7));`
- line 219: `const flatCells: (DayCell | null)[] = new Array(totalSlots).fill(null);` ← where the
  `NaN` produced upstream becomes `RangeError: Invalid array length`
- `WEEK_START_OFFSET` has exactly two references in the repo, both in this file (lines 106 and 175).

`src/dashboard/views/calendar-preferences.test.ts` (existing stand-in shape to follow, lines 12-34):
`fakeStorage(initial, { throwOnGet, throwOnSet })` returns an object with `data`, `getItemCalls`,
`setItemCalls` plus `getItem`/`setItem`. It simulates a throwing METHOD; nothing in the file
simulates a throwing property GETTER.
</interfaces>

<settled_decisions>
| # | Choice | Decision | Rationale |
|---|--------|----------|-----------|
| GC-2a | Resolver shape | `export function resolveWeekStartStorage(override?: WeekStartStorage): WeekStartStorage \| null` in `calendar-preferences.ts`, returning `override` when given, else `globalThis.localStorage ?? null` inside try/catch, else `null` | Verbatim the fix `22-REVIEW.md` CR-01 prescribes. Keeping it in the preferences module preserves D-06: that module owns the whole persistence contract and `calendar.ts` never touches a storage global |
| GC-2b | Null handling | `readStoredWeekStart` / `writeWeekStart` take `WeekStartStorage \| null` and early-return on a falsy handle, before their try/catch | Also from CR-01's fix. It makes the fallback INTENTIONAL rather than an artefact of a `TypeError` being swallowed by a catch written for storage-subsystem failures |
| GC-2c | WR-01 fix direction | Fix the CODE, not the docs — make the offset lookup total | GC-2 lets the planner pick, but `22-VERIFICATION.md` records the JSDoc totality claim, `calendar-logic.ts`'s purity contract and `calendar-preferences.ts`'s T-22-WK-01 note all asserting totality. One code change makes three existing documents true; the alternative makes three documents weaker |
| GC-2d | Keep or delete `WEEK_START_OFFSET`? | **Keep the const**, add a total accessor `weekStartOffset(weekStart)` returning `WEEK_START_OFFSET.sunday` for `'sunday'` and `WEEK_START_OFFSET.monday` otherwise | Plan `22-01`'s recorded `must_haves.key_links` pattern is the literal string `WEEK_START_OFFSET`; deleting it would invalidate a shipped plan's verification pattern for no gain. An explicit ternary (not `?? fallback`) avoids a nullish-coalesce on a type-level non-nullable index expression |
| GC-2e | Off-union regression assertion | Assert the off-union grid **deep-equals the `'monday'` grid**, not merely `not.toThrow()` | `not.toThrow()` would also pass on a fallback to Sunday. Deep equality pins the D-03 default the fallback is supposed to be |
</settled_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard the localStorage property access end to end (CR-01) and make T-22-WK-02's claim true</name>
  <files>src/dashboard/views/calendar-preferences.ts, src/dashboard/views/calendar-preferences.test.ts, src/dashboard/views/calendar.ts</files>
  <read_first>
    - `src/dashboard/views/calendar-preferences.ts` — the whole file (68 lines): the header's
      T-22-WK-01 and T-22-WK-02 notes (lines 10-18), `WeekStartStorage` (25-28), `parseWeekStart`
      (36-39), `readStoredWeekStart` (46-52), `writeWeekStart` (60-67).
    - `src/dashboard/views/calendar-preferences.test.ts` — the whole file, especially the
      `fakeStorage` helper at lines 12-34 (the stand-in style to follow) and the `tamperedValues`
      loop, so the new cases match the file's existing shape rather than inventing a second style.
    - `src/dashboard/views/calendar.ts` lines 33, 340-355 and 404-430 — the import, the
      `CalendarViewDeps` interface, and the `mount()` region containing line 424's unguarded
      resolution plus the four-line comment above it explaining lazy resolution.
    - `src/dashboard/views/calendar.ts` lines 528-560 — `setWeekStart`, whose
      `writeWeekStart(storage, next)` call at 544 must keep compiling against the widened signature,
      and whose body is asserted by `calendar.test.ts:265-271` to contain none of `focus`, `mount(`,
      `navigateTo`, `await`.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § CR-01 — the finding and its
      prescribed fix, including the `Object.defineProperty(fake, 'storage', { get() { throw ... } })`
      test hint.
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` Gap 2 — the blast-radius
      analysis (`main.ts:53-77`'s catch renders a generic panel) that makes this Critical.
    - `src/dashboard/theme.ts` lines 85-110 — the sibling unguarded `options.storage ?? localStorage`
      shape. **Read for context only; do not edit `theme.ts`.**
    - `.planning/phases/22-calendar-week-start-totals/22-PATTERNS.md` § `calendar-preferences.ts` —
      the `theme.ts` discipline this module mirrors.
  </read_first>
  <behavior>
    Write these tests in `calendar-preferences.test.ts` FIRST, watch them fail, then implement:

    - `resolveWeekStartStorage(fake)` returns the exact `fake` object (identity), and does so even
      while a throwing `globalThis.localStorage` getter is installed — the override path must never
      touch the global.
    - With `globalThis.localStorage` defined as a property whose GETTER throws
      (`Object.defineProperty(globalThis, 'localStorage', { configurable: true, get() { throw new Error('SecurityError'); } })`),
      `resolveWeekStartStorage()` does not throw and returns `null`.
    - With no `localStorage` on `globalThis` (the repo's default `environment: 'node'` state),
      `resolveWeekStartStorage()` returns `null`.
    - `readStoredWeekStart(null)` returns `'monday'` and does not throw.
    - `writeWeekStart(null, 'sunday')` does not throw and performs no observable work.
    - A full resolve-then-read cycle under the throwing getter writes nothing to the console: spy on
      `console.warn` and `console.error` and assert zero calls (D-07's silence rule).
    - Every pre-existing case in the file still passes unchanged — in particular the
      `throwOnGet`/`throwOnSet` cases, which continue to exercise the method-throwing path the
      resolver does not replace.

    Restore `globalThis` in an `afterEach` with `Reflect.deleteProperty(globalThis, 'localStorage')`
    so the throwing getter cannot leak into any other test in the suite.
  </behavior>
  <action>
    **`calendar-preferences.ts`:**

    Add `export function resolveWeekStartStorage(override?: WeekStartStorage): WeekStartStorage | null`.
    It returns `override` immediately when one is supplied; otherwise it reads
    `globalThis.localStorage` **inside a `try` block**, returning that handle or `null` when it is
    absent, and returning `null` from the `catch`. Give it a JSDoc naming `CR-01` and stating
    plainly that the GETTER is the throwing operation when site data is blocked (Firefox "Block
    cookies and site data", Chrome blocked-origin storage, a storage-partitioned iframe), that this
    is why `readStoredWeekStart`'s own try/catch is not sufficient, and that this function is the
    ONLY place in `src/dashboard/` allowed to touch a storage global for this key (D-06).

    Widen `readStoredWeekStart` to `(storage: WeekStartStorage | null): WeekStart` with an explicit
    `if (!storage) return 'monday';` before the try/catch, and `writeWeekStart` to
    `(storage: WeekStartStorage | null, value: WeekStart): void` with an explicit early `return` on
    a falsy handle. Keep both existing try/catch blocks exactly as they are — they still cover a
    throwing `getItem` / `setItem` (Safari private mode, quota exceeded), which the resolver does
    not address.

    Correct the header's **T-22-WK-02** note. It currently claims that wrapping the read and the
    write covers "disabled cookies". Rewrite it to state: the read and the write are wrapped against
    a throwing `getItem`/`setItem` (Safari private mode, quota exceeded), AND — separately — the
    `globalThis.localStorage` property access is wrapped by `resolveWeekStartStorage`, because with
    site data blocked the getter throws before either function is called; cite `CR-01` and record
    that the claim as originally written was false as shipped in Round 1.

    Correct the header's **T-22-WK-01** note to state the defence in depth accurately: the
    allow-list is the only path by which a stored value reaches the grid math, AND
    `calendar-logic.ts`'s offset lookup is itself total (Task 2), so an off-union value that
    bypasses `parseWeekStart` degrades to the Monday default rather than reaching `new Array(NaN)`.
    Do not claim the value "can never reach the grid math" — WR-01 proved that wording false.

    **`calendar.ts`:**

    Change the import on line 33 to also import `resolveWeekStartStorage`. Replace line 424 with
    `const storage = resolveWeekStartStorage(deps.storage);`. Update the four-line comment above it:
    keep its existing point about lazy resolution keeping the module importable in the Node test
    environment, and add that the resolution is delegated to `calendar-preferences.ts` because the
    property access itself throws under blocked site data (CR-01) and would otherwise take the whole
    view down through `main.ts`'s generic error panel. Change nothing else in `calendar.ts` — line
    426's `readStoredWeekStart(storage)` and line 544's `writeWeekStart(storage, next)` compile
    against the widened signatures unchanged.

    **Hard constraints.** After this task, the string `globalThis.localStorage` must not appear
    anywhere in `calendar.ts`. Do not write the literal strings `tabindex` or `.focus()` in any
    comment or code you add to `calendar.ts` (`calendar.test.ts:280-284` and `:311-314` count
    whole-file occurrences and require exactly 2 of each; WR-04 is out of scope). Do not add a
    `console` call, a normalisation, a repair or a write-back on any of these paths — D-07 and
    T-22-WK-01 both forbid it, and the resolver must widen nothing the Calendar trusts.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/calendar-preferences.test.ts src/dashboard/views/calendar.test.ts && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e 'import {readFileSync} from "node:fs"; const p=readFileSync("src/dashboard/views/calendar-preferences.ts","utf8"); if(!/export function resolveWeekStartStorage/.test(p)) throw new Error("resolveWeekStartStorage is not exported"); const fn=p.slice(p.indexOf("export function resolveWeekStartStorage")); const body=fn.slice(0,fn.indexOf("\n}")); if(!/try\s*\{/.test(body)) throw new Error("resolveWeekStartStorage has no try block"); if(!/globalThis\.localStorage/.test(body)) throw new Error("resolveWeekStartStorage does not read globalThis.localStorage"); if(body.indexOf("globalThis.localStorage")<body.indexOf("try")) throw new Error("the globalThis.localStorage access is not inside the try block"); if(!/catch/.test(body)) throw new Error("resolveWeekStartStorage has no catch"); if(!/WeekStartStorage \| null/.test(p)) throw new Error("the read/write signatures were not widened to WeekStartStorage | null"); if(!/if \(!storage\)/.test(p)) throw new Error("no explicit falsy-handle early return"); for(const t of ["CR-01","T-22-WK-01","T-22-WK-02"]) if(!p.includes(t)) throw new Error("calendar-preferences.ts lost the marker: "+t); if(/console\./.test(p)) throw new Error("D-07 forbids console output on these paths"); const c=readFileSync("src/dashboard/views/calendar.ts","utf8"); if(c.includes("globalThis.localStorage")) throw new Error("calendar.ts still dereferences globalThis.localStorage"); if(!c.includes("resolveWeekStartStorage(deps.storage)")) throw new Error("calendar.ts does not resolve storage through the guarded resolver"); if((c.match(/tabindex/g)||[]).length!==2) throw new Error("calendar.ts tabindex count moved off 2 (WR-04 guard)"); if((c.match(/\.focus\(\)/g)||[]).length!==2) throw new Error("calendar.ts .focus() count moved off 2 (WR-04 guard)"); const t=readFileSync("src/dashboard/views/calendar-preferences.test.ts","utf8"); if(!/defineProperty\([^)]*globalThis/.test(t)) throw new Error("no getter-throwing globalThis stand-in in the test file"); if(!/get\s*\(\s*\)\s*\{[\s\S]{0,160}throw/.test(t)) throw new Error("the stand-in does not throw from a property GETTER"); if(!/Reflect\.deleteProperty/.test(t)) throw new Error("the throwing getter is not torn down - it will leak into other tests"); if(!/readStoredWeekStart\(null\)/.test(t)) throw new Error("no null-handle read case"); if(!/writeWeekStart\(null/.test(t)) throw new Error("no null-handle write case"); console.log("CR-01 closed: guarded resolver, widened signatures, getter-throwing regression test, WR-04 counters intact");'</automated>
  </verify>
  <acceptance_criteria>
    - `calendar-preferences.ts` exports `resolveWeekStartStorage`, whose body reads
      `globalThis.localStorage` **inside** a `try` and returns `null` from its `catch`.
    - `readStoredWeekStart` and `writeWeekStart` both accept `WeekStartStorage | null` and
      early-return on a falsy handle via an explicit `if (!storage)`; both keep their original
      try/catch for a throwing `getItem`/`setItem`.
    - The header's T-22-WK-02 note names `CR-01` and identifies the property getter (not
      `getItem`/`setItem`) as the throwing operation under blocked site data; the T-22-WK-01 note no
      longer asserts that a tampered value "can never reach the grid math".
    - `calendar-preferences.ts` contains zero `console.` calls.
    - `calendar.ts` contains `resolveWeekStartStorage(deps.storage)` and contains the string
      `globalThis.localStorage` **zero** times.
    - `calendar.ts` still contains exactly 2 occurrences of `tabindex` and exactly 2 of `.focus()`.
    - `calendar-preferences.test.ts` installs a `globalThis` property whose GETTER throws (via
      `Object.defineProperty` with a `get()` that throws), tears it down with
      `Reflect.deleteProperty`, and covers `readStoredWeekStart(null)` and
      `writeWeekStart(null, ...)`.
    - `npx vitest run src/dashboard/views/calendar-preferences.test.ts
      src/dashboard/views/calendar.test.ts` exits 0 and `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>A blocked-site-data browser can no longer take down the Calendar view through the week-start preference, and the module's own security notes describe what the code actually does.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make buildMonthGrid total for an off-union weekStart (WR-01) and re-run the full phase gate</name>
  <files>src/dashboard/views/calendar-logic.ts, src/dashboard/views/calendar-logic.test.ts</files>
  <read_first>
    - `src/dashboard/views/calendar-logic.ts` lines 100-240 — `MIN_WEEK_ROWS`, the `WeekStart` union,
      `WEEK_START_OFFSET` (106), `firstWeekdayOfMonth` (164-167), `leadingPaddingFor` (169-176),
      `buildMonthGrid`'s JSDoc with its "Total function: never throws" claim (178-191), and the
      `padding → cellCount → weekCount → totalSlots → new Array(totalSlots)` chain at 213-219 that
      turns a `NaN` offset into a `RangeError`.
    - `src/dashboard/views/calendar-logic.test.ts` — the existing Sunday/Monday padding cases, so the
      new regression case matches the file's established construction style and month choices.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § WR-01 — the executed proof
      (`RangeError: Invalid array length at buildMonthGrid (calendar-logic.ts:219:41)`), the exact
      `NaN` propagation chain, and the prescribed `weekStartOffset` fix.
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` § Behavioral Spot-Checks —
      the independent confirmation of the same throw.
    - `.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md` D-03 and D-08 — Monday is the
      default and `weekStart` is a required injected parameter, which together fix which value the
      fallback must be.
    - `.planning/phases/22-calendar-week-start-totals/22-01-PLAN.md` lines 30-40 — the recorded
      `key_links` whose `pattern` is the literal string `WEEK_START_OFFSET`, which is why GC-2d keeps
      the const rather than replacing it.
  </read_first>
  <behavior>
    Write these tests in `calendar-logic.test.ts` FIRST, watch the first one fail with
    `RangeError: Invalid array length`, then implement:

    - `buildMonthGrid([], { year: 2024, month: 3 }, 'MONDAY' as never)` does not throw
      (T-22-WK-01 defence in depth — the exact case `22-REVIEW.md` executed).
    - That same call deep-equals `buildMonthGrid([], { year: 2024, month: 3 }, 'monday')` — the
      off-union value degrades to the D-03 Monday default, not to Sunday.
    - The same pair holds for a second, differently-shaped off-union value (e.g.
      `undefined as never`) and for a second month whose leading padding differs, so the fallback is
      not accidentally month-specific.
    - Both legitimate week starts are unchanged: every existing `'sunday'` and `'monday'` padding
      expectation in the file still passes.
  </behavior>
  <action>
    In `src/dashboard/views/calendar-logic.ts`, keep `WEEK_START_OFFSET` exactly as it is (GC-2d) and
    add a module-private `function weekStartOffset(weekStart: WeekStart): number` returning
    `WEEK_START_OFFSET.sunday` when `weekStart === 'sunday'` and `WEEK_START_OFFSET.monday`
    otherwise. Use an explicit equality ternary, not a nullish-coalescing fallback on the index
    expression — `WEEK_START_OFFSET[weekStart]` is typed non-nullable, so `??` would read as dead
    code to a future reader while being the only live branch at runtime.

    Change `leadingPaddingFor` to call `weekStartOffset(weekStart)` instead of indexing
    `WEEK_START_OFFSET` directly. That is the only call-site change; the arithmetic
    `(firstWeekdayOfMonth(m) - offset + 7) % 7` is unchanged.

    Give `weekStartOffset` a JSDoc naming `WR-01` that states: the lookup is total; an off-union
    value is only reachable by bypassing `parseWeekStart` or TypeScript; and the fallback is the
    D-03 Monday offset rather than the `undefined → NaN → Math.ceil(NaN) → new Array(NaN)` chain
    that threw `RangeError` at line 219 as shipped.

    Update `buildMonthGrid`'s JSDoc so its "Total function: never throws" clause is now backed:
    state that totality now holds for an off-union `weekStart` too, via `weekStartOffset`, and cite
    `WR-01`. Do not weaken the D-08 framing — `weekStart` stays a REQUIRED parameter with no default,
    and the module stays pure, DOM-free and clock-free.

    Finally, re-run the whole phase gate (full suite, type-check, widget build with no
    `css-syntax-error`, dashboard verifier) so that plans 22-06 and 22-07 together are proven green
    before the Round 2 checkpoint plan stages a build from them.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/calendar-logic.test.ts && npm test && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e 'import {readFileSync} from "node:fs"; const l=readFileSync("src/dashboard/views/calendar-logic.ts","utf8"); if(!/function weekStartOffset/.test(l)) throw new Error("weekStartOffset was not added"); if(!/WEEK_START_OFFSET/.test(l)) throw new Error("WEEK_START_OFFSET was removed - plan 22-01 key_links pattern broken"); if(/WEEK_START_OFFSET\[weekStart\]/.test(l)) throw new Error("the unguarded WEEK_START_OFFSET[weekStart] index survives"); if(!/weekStartOffset\(weekStart\)/.test(l)) throw new Error("leadingPaddingFor does not use the total accessor"); if(!l.includes("WR-01")) throw new Error("the totality fix is not attributed to WR-01"); const t=readFileSync("src/dashboard/views/calendar-logic.test.ts","utf8"); if(!/as never/.test(t)) throw new Error("no off-union regression case"); if(!/MONDAY/.test(t)) throw new Error("the reviewed off-union value MONDAY is not covered"); console.log("WR-01 closed: total offset accessor in place, off-union regression covered");' && rm -f /tmp/bw22gc7.log && npm run build-widgets > /tmp/bw22gc7.log 2>&1; BW=$?; tail -3 /tmp/bw22gc7.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw22gc7.log && npm run verify-dashboard && echo "full phase gate green after 22-06 + 22-07"</automated>
  </verify>
  <acceptance_criteria>
    - `calendar-logic.ts` declares `function weekStartOffset(weekStart: WeekStart): number`, still
      declares `WEEK_START_OFFSET`, and contains **zero** occurrences of the raw index expression
      `WEEK_START_OFFSET[weekStart]`.
    - `leadingPaddingFor` calls `weekStartOffset(weekStart)`.
    - `weekStartOffset`'s JSDoc and `buildMonthGrid`'s JSDoc both cite `WR-01`; `buildMonthGrid`
      keeps `weekStart` as a required third parameter with no default (D-08).
    - `calendar-logic.test.ts` covers the reviewed off-union case with the literal `MONDAY` and an
      `as never` cast, asserting deep equality with the `'monday'` grid (not merely `not.toThrow()`).
    - `npm test` exits 0 with no test regressed (baseline 1203 passing plus this round's new cases),
      and `npx tsc --noEmit -p tsconfig.json` exits 0.
    - `npm run build-widgets` exits 0 with zero `css-syntax-error` occurrences in its output, and
      `npm run verify-dashboard` exits 0.
  </acceptance_criteria>
  <done>`buildMonthGrid` is total for every input its own JSDoc promises to survive, and the full phase gate is green on the combined 22-06 + 22-07 tree, ready for the Round 2 checkpoint to stage a build from it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser storage policy → `mount()` | The `localStorage` property getter is attacker- and configuration-controlled: with site data blocked it throws before any method call. This is the boundary CR-01 crosses unguarded today |
| `localStorage` value → grid math | `localStorage` is user- and extension-writable (T-16-TH-01's precedent). `parseWeekStart`'s allow-list is the only sanctioning step |
| `buildMonthGrid`'s public signature → array allocation | An exported, deliberately reusable pure function whose `weekStart` argument reaches `new Array(n)` — a future caller that bypasses `parseWeekStart` is untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-WK-02 | Denial of service | `globalThis.localStorage` property access in `calendar.ts` `mount()` | mitigate | `resolveWeekStartStorage` wraps the property access in try/catch and returns `null`; `readStoredWeekStart(null)` returns the D-03 Monday default. The whole Calendar view therefore survives blocked site data instead of collapsing into `main.ts`'s generic panel. Locked by a regression test whose stand-in throws from the GETTER, and observed in a real browser by plan `22-08` row R15 |
| T-22-WK-01 | Tampering | the persisted week-start value | mitigate | `parseWeekStart`'s exact allow-list stays the ONLY path from storage to grid math — the resolver returns a handle, never a value, and performs no parsing, normalisation, repair or write-back (D-07). Defence in depth added: `weekStartOffset` makes an off-union value degrade to Monday instead of reaching `new Array(NaN)` |
| T-22-GC-ST-01 | Denial of service | `buildMonthGrid`'s array allocation | mitigate | WR-01's executed `RangeError: Invalid array length` is closed at the source of the `NaN`, not by a try/catch at the call site — the module keeps its documented totality rather than acquiring an exception path |
| T-22-GC-ST-02 | Repudiation | silent storage failure | accept | The fallback is deliberately silent (no console output) per D-07 and `theme.ts`'s precedent. A user whose storage is blocked sees a working Calendar on the Monday default rather than a diagnostic; the trade is recorded here rather than left implicit |
| T-22-GC-ST-03 | Elevation of privilege | test-injected `globalThis` mutation | mitigate | The getter-throwing stand-in is installed with `configurable: true` and removed in `afterEach` via `Reflect.deleteProperty`, so it cannot leak into the other 50 test files in the suite (`npm test` running green is the check) |
| T-22-SC | Tampering | npm/pip/cargo installs | accept (not applicable) | Zero new packages. `22-RESEARCH.md` § Package Legitimacy Audit records "Not applicable"; no install task exists in any Phase 22 plan |
</threat_model>

<verification>
- Task 1 proves the property access sits inside a try/catch, that `calendar.ts` no longer names a
  storage global at all, that the two security notes were corrected rather than left orphaned, and
  that a getter-throwing stand-in (not a throwing `getItem`) locks the behaviour in.
- Task 2 proves the off-union path is total and pinned to the Monday default by deep equality, and
  re-runs the full phase gate across the combined 22-06 + 22-07 tree.

**This plan discharges no visual claim.** Whether a real blocked-site-data browser now renders the
Calendar instead of "Something went wrong" is settled only by plan `22-08`'s Round 2 row R15,
observed by the developer's own eyes (PROJECT.md line 49). Automated tests simulate the throw; they
do not prove the browser's real behaviour.
</verification>

<success_criteria>
- `globalThis.localStorage` is dereferenced in exactly one place in the codebase's calendar path —
  inside `resolveWeekStartStorage`'s `try`.
- `calendar-preferences.ts`'s T-22-WK-01 and T-22-WK-02 notes are true statements about the shipped
  code.
- A test with a getter-throwing storage stand-in fails if the guard is removed.
- `buildMonthGrid` with an off-union `weekStart` returns the Monday grid instead of throwing.
- `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets` (no `css-syntax-error`)
  and `npm run verify-dashboard` all exit 0.
- No requirement is ticked by this plan — CAL-01's robustness caveat closes on plan `22-08`'s R15.
</success_criteria>

<output>
Create `.planning/phases/22-calendar-week-start-totals/22-07-SUMMARY.md` when done.
</output>
