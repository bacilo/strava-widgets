---
phase: 30-elevation-quality-signal
plan: 05
type: execute
wave: 3
depends_on: ["30-03"]
files_modified:
  - src/dashboard/views/list.ts
  - src/dashboard/views/list.test.ts
  - src/dashboard/views/detail.ts
autonomous: true
requirements: [ELEV-01]

must_haves:
  decisions: [D-10, D-12, D-13]
  truths:
    - "D-10: an elevation-flagged activity carries exactly one elevation badge on every renderActivityRow surface, naming the fired mode(s) with the measured value"
    - "D-10: the badge text carries a number read off the row's own evidence fields, never recomputed and never an adjective alone"
    - "D-10: an activity whose elevation tier is not severe carries no elevation badge on any surface"
    - "D-12: the detail view's Elevation Gain stat card is badged when the activity is elevation-flagged, and no other stat card, split column or aggregate is caveated"
    - "D-13: no list filter, filter chip or URL parameter is added for elevation this phase"
    - "A row whose quality object is missing or whose elevation key is absent produces no badge and no TypeError"
  artifacts:
    - path: "src/dashboard/views/list.ts"
      provides: "the fourth qualityBadgeSpecs block and 'elevation' on the QualityBadgeSpec signal union"
      contains: "descriptionIdSuffix: 'elevation'"
    - path: "src/dashboard/views/detail.ts"
      provides: "the Elevation Gain stat card built as a variable and conditionally badged"
      contains: "elevationStatCard"
  key_links:
    - from: "src/dashboard/views/list.ts qualityBadgeSpecs"
      to: "row.quality.elevation"
      via: "a fourth if block feeding the existing appendQualityBadges loop, no per-surface branching"
      pattern: "elevation.tier === 'severe'"
    - from: "src/dashboard/views/detail.ts Elevation Gain stat card"
      to: "indexClient.getRow(detail.id)?.quality?.elevation"
      via: "appendAccessibleBadge on the card, mirroring the paceDisagreement precedent"
      pattern: "appendAccessibleBadge\\(elevationStatCard"
---

<objective>
Put the elevation flag where a reader meets the activity: one badge per row on all three list
surfaces, and a caveat on the one stat card whose number the same page flags as implausible.

Purpose: the roadmap's "UI hint: yes" and D-10/D-12. A signal computed in CI and published on the
index that nothing renders is a signal nobody sees. This is also the surface plan 30-08's checkpoint
reads numbers off, so every badge must carry a number that exists in a committed file.

Output: a fourth badge spec through machinery that already iterates generically, and a stat-card
badge copied from the pace-disputed precedent three lines above it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-elevation-quality-signal/30-CONTEXT.md
@.planning/phases/30-elevation-quality-signal/30-PATTERNS.md
@.planning/phases/30-elevation-quality-signal/30-03-SUMMARY.md
@src/dashboard/views/list.ts
@src/dashboard/views/detail.ts

<interfaces>
<!-- The existing third block, list.ts:450-460, the closest template: severe-tier gate AND a
     non-null evidence field, condition plus measured value, no adjectives. -->

```typescript
if (impossibleSamples.tier === 'severe' && impossibleSamples.count !== null) {
  const n = impossibleSamples.count;
  const noun = n === 1 ? 'sample' : 'samples';
  specs.push({
    signal: 'impossibleSamples',
    visibleText: `${n} ${noun} faster than the 100 m world record`,
    explanation:
      'these samples imply a speed no human sustains, so any effort drawn across them is not a measured time',
    descriptionIdSuffix: 'impossible-samples',
  });
}
```

<!-- The guard already at the head of qualityBadgeSpecs (list.ts:417-421), added by 27-12 for G-04.
     Destructure elevation through THIS guard, never before it. -->

```typescript
const quality = row.quality;
if (!quality) return specs;
const { decimation, gapProfile, impossibleSamples } = quality;
```

<!-- The stat-card badge precedent, detail.ts:673-691. The Elevation Gain card is currently built
     inline at :691 with no intermediate variable — pull it into one first. -->

```typescript
const paceStatCard = buildStatCard(formatPace(paceSecPerKm), 'Pace');
if (disagreement !== null) {
  appendAccessibleBadge(paceStatCard, `Pace disputed — …`, paceDisputedExplanation(disagreement), paceDisputedDescriptionId('detail-pace-stat'));
}
statGrid.appendChild(paceStatCard);
```

<!-- D-10's wording examples: `altitude −282 m below ground`; `altitude drift 198 m · spike 80 m/s`.
     Condition plus measured value, Phase 27's form, no adjectives — the checkpoint quotes a number. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: One severe-only elevation badge on every row surface</name>
  <files>src/dashboard/views/list.ts, src/dashboard/views/list.test.ts</files>
  <read_first>
    - `src/dashboard/views/list.ts:340-365` — `QualityBadgeSpec` and its doc comment, which currently
      says the union names "which of the three TIERING signals fired".
    - `src/dashboard/views/list.ts:414-465` — `qualityBadgeSpecs` in full, including the `if (!quality)`
      guard added by 27-12 for review finding G-04 and all three existing blocks.
    - `src/dashboard/views/list.ts:227-250` — `appendAccessibleBadge`, reused unchanged.
    - `src/dashboard/views/list.ts:540-620` — `appendQualityBadges` and `activityRowAriaLabel`, both
      of which iterate `qualityBadgeSpecs(row)` generically and need no change.
    - `src/dashboard/views/list.test.ts:580-640` — `qualityRow` and the existing per-signal badge
      tests, the convention the new cases follow.
    - `.planning/phases/30-elevation-quality-signal/30-03-SUMMARY.md` — the severe-elevation activity
      id and its committed shard values.
  </read_first>
  <action>
    Extend `QualityBadgeSpec['signal']` with `'elevation'` and update that interface's doc comment,
    which currently says the union names one of the three TIERING signals — elevation is a fourth
    badge source that is deliberately NOT a tiering signal (D-06), and the comment must say so rather
    than becoming quietly false.

    Add `elevation` to the destructure that already sits behind the `if (!quality) return specs;`
    guard, and add a fourth `if` block after the `impossibleSamples` one:
      - Gate on `elevation.tier === 'severe'` only. No badge for `'none'` and none for
        `'not-computable'` — the detail view is where absence is disclosed (D-11), not the row.
      - Build the visible text from the modes that actually fired, each with its measured value from
        the row's own evidence fields, never recomputed: sub-ground reads as altitude below ground
        with the measured minimum (e.g. `altitude −282 m below ground`); drift reads as an altitude
        drift with the ABSOLUTE value of the signed `deltaM` (e.g. `altitude drift 198 m`); vertical
        rate reads as a spike with the worst rate (e.g. `spike 80 m/s`). When two or three modes fire,
        join them into ONE badge with a separator, matching D-10's own example form — one badge per
        row, never one per mode, because 12 activities fire two or more and rows already carry pace,
        PR and gear badges.
      - Guard each mode's value field for `null` exactly as the three existing blocks guard theirs: a
        severe tier with a null measured value contributes no clause, and if that leaves no clause at
        all, push no spec rather than a numberless badge.
      - `descriptionIdSuffix: 'elevation'`.
      - The `explanation` carries why it matters in the sr-only slot: barometric and GPS altitude
        errors of this size mean the elevation figures for this activity are not measured ground
        truth, and this project flags rather than corrects them — no DEM lookup, no grade-adjusted
        pace. Exact wording is yours; keep Phase 27's register.

    Change nothing else in this file. `appendQualityBadges` and `activityRowAriaLabel` already iterate
    the spec list generically, which is what gives D-10's "no per-surface branching" for free across
    all three `renderActivityRow` surfaces.

    Add NOTHING to `list-logic.ts`: D-13 defers the elevation filter and URL parameter. Do not add a
    `FilterState` member, a chip, a checkbox or a query parameter.

    Extend `src/dashboard/views/list.test.ts` with cases for: a sub-ground-only severe row producing
    exactly one spec whose `visibleText` contains the measured minimum; a drift-only row whose text
    shows the absolute drift; a vertical-rate-only row; a row firing all three producing exactly ONE
    spec containing all three numbers; a `tier: 'none'` row producing no elevation spec; a
    `tier: 'not-computable'` row producing no elevation spec; a severe row whose measured values are
    all `null` producing no elevation spec rather than a numberless badge; and a row whose `quality`
    is `undefined` (the stale-index case 27-12's G-04 fixed) still returning an empty array with no
    TypeError. Assert against `qualityBadgeSpecs`'s returned specs, following the existing tests'
    plan-level convention.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/list.test.ts -t "elevation"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "descriptionIdSuffix: 'elevation'" src/dashboard/views/list.ts` returns 1 — exactly one elevation spec source, not one per mode.
    - `grep -n "signal: 'decimation' | 'gapProfile' | 'impossibleSamples' | 'elevation'" src/dashboard/views/list.ts` matches, and the interface doc comment no longer claims the union names only tiering signals.
    - `git diff src/dashboard/views/list.ts` shows no change inside `appendQualityBadges`, `activityRowAriaLabel` or `appendAccessibleBadge`.
    - `git diff --stat src/dashboard/views/list-logic.ts` is empty (D-13): `git diff --name-only | grep -c list-logic.ts` returns 0.
    - `npx vitest run src/dashboard/views/list.test.ts -t "elevation"` exits 0 with at least eight assertions across the listed cases.
    - A test proves the all-three-modes row yields exactly ONE spec: it asserts `specs.filter(s => s.signal === 'elevation').length === 1` and that the single `visibleText` contains all three numbers.
    - `npx tsc --noEmit` and `npm test` exit 0.
  </acceptance_criteria>
  <done>Every elevation-flagged row carries one badge naming its fired modes with their measured numbers, on all three surfaces, and no filter was added.</done>
</task>

<task type="auto">
  <name>Task 2: Caveat the Elevation Gain stat card, and nothing else</name>
  <files>src/dashboard/views/detail.ts</files>
  <read_first>
    - `src/dashboard/views/detail.ts:660-700` — the stat grid: where `disagreement` is read from
      `indexClient.getRow(detail.id)`, the Pace stat card's build-then-badge-then-append shape, and
      the inline Elevation Gain `buildStatCard` call at :691 with its `formatOrDash`/`numOrNull`
      formatting.
    - `src/dashboard/views/detail.ts:565-590` — `mountBestEffortsAndBadges`'s existing `Promise.all`
      including the `paceQualityClient.load(detail.id)` member wired in Phase 27, and the
      stale-navigation guard after it.
    - `src/dashboard/views/list.ts:227-250` — `appendAccessibleBadge`'s signature.
    - `.planning/phases/30-elevation-quality-signal/30-CONTEXT.md` § D-12.
  </read_first>
  <action>
    Pull the Elevation Gain stat card out of its inline `statGrid.appendChild(buildStatCard(...))`
    into an `elevationStatCard` variable, keeping its existing `formatOrDash` / `numOrNull` /
    `Math.round` formatting and its `'Elevation Gain'` label byte-for-byte. Then, mirroring the Pace
    card three lines above: when the row's `quality?.elevation.tier === 'severe'`, call
    `appendAccessibleBadge(elevationStatCard, visibleText, explanation, descriptionId)`; then append.

    Read the quality object from the row the render path already has —
    `indexClient.getRow(detail.id)?.quality ?? null`, the same optional-chain shape the
    `disagreement` read at :669 uses. Add no new client, no new `Promise.all` member, no new
    constructor option and no second fetch (D-05).

    The visible text names the fired mode(s) and their measured value in the same form as the row
    badge, so the checkpoint can compare the card against the row against the shard and find three
    agreeing numbers with one independent source. Reuse the row badge's text builder from
    `qualityBadgeSpecs` if it is exported or trivially extractable; if reusing means exporting a new
    helper from `list.ts`, export it rather than retyping the strings — two surfaces with separately
    maintained wording is the drift `EXPLANATION_PROBE_QUALITY` exists to prevent elsewhere in this
    tree.

    Caveat nothing else (D-12): do not touch the per-split `elevDeltaM` column, the elevation
    aggregates in `compute-stats.ts`, the overview totals, or any other stat card. The quality section
    sits on the same page; a caveat everywhere starts to look like correction, which this milestone
    forbids.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "elevationStatCard" src/dashboard/views/detail.ts` returns at least 3 (build, badge, append).
    - `grep -c "appendAccessibleBadge(elevationStatCard" src/dashboard/views/detail.ts` returns 1.
    - `grep -c "Promise.all" src/dashboard/views/detail.ts` is unchanged from before this task, and `grep -c "paceQualityClient.load" src/dashboard/views/detail.ts` is still 1 — no new fetch.
    - `git diff --name-only` contains neither `src/analytics/compute-stats.ts` nor `src/dashboard/views/overview.ts` (D-12: nothing else caveated).
    - The `'Elevation Gain'` label and the card's number formatting are byte-identical to before: `git diff src/dashboard/views/detail.ts` shows the `buildStatCard(...)` arguments unchanged, only relocated into a variable.
    - `npx tsc --noEmit` and `npm test` exit 0.
  </acceptance_criteria>
  <done>The one stat card whose number the same page flags as implausible carries the caveat, and no other surface does.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published index row → rendered badge text | Numbers from a published artifact reach the DOM on four surfaces |
| a stale or partially-parsed row → the render loop | A missing key here takes down the whole list render, as G-04 did in Phase 27 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-19 | Denial of Service | `qualityBadgeSpecs` destructuring `elevation` off an absent quality object | mitigate | The elevation destructure sits behind the existing `if (!quality) return specs;` guard added by 27-12 for G-04; tested with `quality: undefined` and with `elevation` absent. |
| T-30-20 | Tampering | a numberless or adjective-only badge | mitigate | Each mode clause requires a non-null measured value; a severe tier with all values null pushes no spec at all. Tested. |
| T-30-21 | Tampering | badge text drifting between the row and the stat card | mitigate | Both surfaces build their text from the same exported helper rather than separately maintained strings. |
| T-30-22 | Tampering | scope creep into a filter or a URL parameter | mitigate | D-13 defers both; asserted by `git diff --name-only` showing `list-logic.ts` untouched. |
| T-30-23 | Tampering | untrusted text reaching the DOM as markup | mitigate | Every string goes through `appendAccessibleBadge`, which writes `textContent` on both spans; no `innerHTML` is introduced. Elevation values are numbers derived from the committed stream — no new free-text field reaches the page. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `npx vitest run src/dashboard/views/list.test.ts -t "elevation"` exits 0.
- `npx tsc --noEmit` and `npm test` exit 0.
- `git diff --name-only` shows no change to `list-logic.ts`, `compute-stats.ts` or `overview.ts`.
- `grep -c "innerHTML" src/dashboard/views/list.ts src/dashboard/views/detail.ts` is unchanged from before this plan.
</verification>

<success_criteria>
- One severe-only elevation badge per row on all three `renderActivityRow` surfaces, carrying the
  fired modes and their measured numbers.
- The Elevation Gain stat card is caveated when flagged, and nothing else is.
- No new fetch, no new filter, no new URL parameter.
- A stale row renders no badge and no TypeError.
</success_criteria>

<output>
Create `.planning/phases/30-elevation-quality-signal/30-05-SUMMARY.md` when done.
Record: the exact badge text produced for the severe activity id from `30-03-SUMMARY.md`, copied from
the test output, so plan 30-08's checkpoint states the expected string before the developer looks at
the page; and whether the badge-text builder was exported for reuse by the stat card, naming it.
</output>
