---
phase: 27-per-activity-quality-signals
plan: 10
type: execute
wave: 6
depends_on: ["27-05", "27-08", "27-09"]
files_modified:
  - .planning/phases/27-per-activity-quality-signals/27-VALIDATION.md
autonomous: false
requirements: [QUAL-03, QUAL-04, QUAL-05, ERA-01, ERA-02]

must_haves:
  decisions: [D-03, D-08, D-09, D-16, D-18]
  truths:
    - "A human reads zero pace-quality shard requests on the activity-list view and exactly one on detail open, in a real browser network panel, against a digest-verified build"
    - "A human reads a severe activity's badge text off the rendered page and it names the condition and its measured value"
    - "The browser's filtered severe cohort count is compared against two independently produced figures, not against itself"
    - "Every checkpoint row is proven able to both pass and fail before it is run"
    - "A row that fails is recorded verbatim as a gap, not patched under checkpoint pressure"
  artifacts:
    - path: ".planning/phases/27-per-activity-quality-signals/27-VALIDATION.md"
      provides: "the drafted checkpoint rows with their bidirectional reachability proofs, and the recorded verdicts"
      contains: "Round 1 Checkpoint"
  key_links:
    - from: "27-VALIDATION.md checkpoint rows"
      to: "27-CALIBRATION.md section 4 and compute-pace-quality-recount.mjs stdout"
      via: "an independently-derived comparison value per row, never internal agreement"
      pattern: "independently"
---

<objective>
Close Phase 27 on rendered evidence: a human browser checkpoint covering Criterion 2 (network
panel), Criterion 3 (badge read-back), Criterion 4 (the composite compared against two
independent figures, plus the threshold-move demonstration) and Criterion 5 (device-family
differentiation and the no-device-name category), against a build whose served digest is
verified.

Purpose: every phase in this project closes on a human browser checkpoint because automated
gates have missed rendering defects here three times, and there is no jsdom or headless browser
in the tree. D-18 is explicit that the network-panel reading is the only evidence that catches
a stale bundle and the only one that cannot agree with itself.

Output: `27-VALIDATION.md` with drafted rows, bidirectional reachability proofs, and recorded
verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-per-activity-quality-signals/27-VALIDATION.md
@.planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md
@.planning/phases/27-per-activity-quality-signals/27-03-SUMMARY.md
@.planning/phases/27-per-activity-quality-signals/27-05-SUMMARY.md
@.planning/phases/27-per-activity-quality-signals/27-08-SUMMARY.md
@.planning/phases/27-per-activity-quality-signals/27-09-SUMMARY.md
@.planning/ROADMAP.md

<interfaces>
<!-- The three independently produced figures for ONE cohort. A checkpoint row must compare the
     browser against these, never against the browser's own other number. -->

| figure | produced by | plan |
|---|---|---|
| composite severe count, computed from streams via the classifier | `npm run compute-pace-quality-calibration` → `27-CALIBRATION.md` § 4 | 27-03 |
| composite severe count, recounted from the shipped index WITHOUT importing the classifier | `node scripts/compute-pace-quality-recount.mjs` | 27-05 |
| filtered row count shown in the browser with `?severe=1` | the shipped SPA | 27-08 |

Criterion 5's proof pair, both already pinned fixtures with verified sample counts:
- fēnix 6 Pro FIT, activity `10041312551` (939 samples)
- Suunto 9 FIT, activity `3480808722` (1,620 samples)
Same file format, different device family — that is the whole of ERA-01's point.

`27-VALIDATION.md`'s Manual-Only Verifications table already names the four behaviours this
checkpoint must cover. Its frontmatter carries `status: draft` and `nyquist_compliant: false`.

Project checkpoint hazards, each of which has cost a round here before:
- `build-widgets` skips work based on mtime and silently no-ops on a locally-edited `dist/`
  file. Verify the SERVED digest, never the build log.
- Serving from `127.0.0.1` alone is not sufficient — checkpoints have served a stale
  `index.html` / `index.json` from a staged build. Hard-reload after every fixture edit.
- The browser viewport clamps to 500..941; do not draft a row requiring a width outside that.
- Gestures need a human. Do not draft a row an agent could claim to have performed.
- A row must assert reachable extent against an INDEPENDENTLY-DERIVED value. Two code paths
  sharing an implementation agreeing with each other is not evidence.
- A row that cannot fail (vacuous) is as broken as one that cannot pass. Check both.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serve a digest-verified build and draft the checkpoint rows</name>
  <files>.planning/phases/27-per-activity-quality-signals/27-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/27-per-activity-quality-signals/27-09-SUMMARY.md` — the recorded built
      JS asset filename and its sha256 digest, and the two named activity ids (one severe, one
      healthy) with their expected badge numbers copied from the committed shard files.
    - `.planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md` § 1 and § 4 — the
      denominators and the composite this checkpoint compares against.
    - `.planning/phases/27-per-activity-quality-signals/27-05-SUMMARY.md` — the recount script's
      stdout against the live index.
    - `.planning/phases/27-per-activity-quality-signals/27-VALIDATION.md` — the existing
      Manual-Only Verifications table and frontmatter.
    - `.planning/STATE.md` § Accumulated Context — the Phase 24 R26 entry (a discriminator that
      went vacuous), the Phase 22 22-16 entry (BLOCKED held against three rounds of pressure to
      record PASS on incomplete evidence), and the Phase 16 16-09 precedent (failures logged
      verbatim rather than patched under checkpoint pressure).
  </read_first>
  <action>
    Rebuild and serve, then verify the served digest before drafting anything:
      1. `npm run build && npm run compute-dashboard-index && npm run build-widgets`.
      2. `shasum -a 256` the emitted JS asset and compare it to the digest recorded in
         `27-09-SUMMARY.md`. If they differ, state whether the difference is expected (source
         changed in plans 27-08/27-09 since) and re-record; if `build-widgets` reported "skipped"
         for that asset, delete the stale `dist/` output and rebuild.
      3. Serve the publish directory over HTTP and FETCH the asset URL, then `shasum -a 256` the
         fetched bytes. The SERVED digest is the one that matters. Record it.
      4. `curl` the served `/data/dashboard/index.json` and confirm its row count and its
         `totals.qualityAnySevere` match the local file — a stale staged `index.json` served
         alongside a fresh bundle is a failure mode this project has hit.
      5. `node scripts/compute-pace-quality-recount.mjs` and record its stdout. This is
         independent figure #2.

    Then append a `## Round 1 Checkpoint (R1-R8)` section to `27-VALIDATION.md` drafting the
    rows below. Every row must carry, BEFORE any verdict, an explicit two-line reachability
    proof — `CAN PASS:` naming the concrete state that would make it pass, and `CAN FAIL:`
    naming the concrete state that would make it fail. A row whose CAN FAIL line cannot be
    written is vacuous and must be redrafted or dropped. Every verdict reads `pending` until
    the developer answers.

    - **R1 — served digest.** The developer confirms the page they are testing was served from
      the digest recorded in step 3, by reading the asset filename in DevTools' Network panel
      and comparing it. Independently derived: the digest was computed from the fetched bytes,
      not from the build log.
      CAN FAIL: a stale bundle serves a different asset filename.

    - **R2 — Criterion 2, zero fetches on the list.** DevTools → Network, filter
      `pace-quality`, hard-reload `#/list`, scroll to the bottom of the first page.
      Expected: zero requests. Record the observed count.
      CAN FAIL: any request appears — which would mean a list-side code path exists despite the
      grep gate in plan 27-09.

    - **R3 — Criterion 2, exactly one fetch on open.** Without clearing the filter, open one
      activity's detail view. Expected: exactly one request to
      `data/stats/pace-quality/{that id}.json`, HTTP 200. Then navigate back to `#/list` and
      open the SAME activity again: expected still exactly one total (memoized), and then open a
      DIFFERENT activity: expected exactly two total.
      CAN FAIL: zero requests (the mount never fires), two on first open (a duplicate fetch), or
      a fresh request on re-open (memoization broken).

    - **R4 — Criterion 3, badge read-back on a severe activity.** Open the severe activity id
      recorded in `27-09-SUMMARY.md`. Quote the rendered badge's visible text VERBATIM,
      including its number, from the detail view. Independently derived: the expected number is
      read from that activity's committed `data/stats/pace-quality/{id}.json` before the row is
      run, and is stated in the row; the developer's quotation is compared against THAT, not
      against what the page also shows elsewhere.
      CAN FAIL: the badge names a condition without a number, or the number disagrees with the
      committed shard.

    - **R5 — D-08, healthy disclosure.** Open the healthy activity id recorded in
      `27-09-SUMMARY.md`. Quote all five Quality Signals rows verbatim. Expected: five rows, each
      with an explicit statement, none blank, none reading `undefined`/`null`/`NaN`, and the
      device-era row naming a real family.
      CAN FAIL: fewer than five rows, a blank value, or a healthy activity showing nothing at all
      — which is exactly the silence-as-good-news reading D-08 exists to prevent.

    - **R6 — Criterion 4, the filtered cohort against two independent figures.** Navigate to
      `#/list?severe=1`, read the result count the page shows. Compare it against
      `27-CALIBRATION.md` § 4's composite AND against the recount script's stdout from step 5.
      Three figures, one cohort, two of them produced without the browser. State the expected
      value in the row before it is run.
      CAN FAIL: the browser count differs from either independent figure by more than the
      archive drift between the two runs, which must be stated as a number, not as a hand-wave.
      Then untick the filter and confirm the count returns to the full row count.

    - **R7 — Criterion 4, threshold moved.** Present the Threshold Sensitivity table from
      `27-CALIBRATION.md` (produced by `npm run compute-pace-quality-calibration -- --sweep`).
      The developer confirms at least one row where a looser threshold strictly increased the
      composite and one where a stricter threshold strictly decreased it. This is a document
      read, not a browser action — draft it as such rather than pretending it is rendered
      evidence.
      CAN FAIL: no row moved in one of the two directions, or every DID-NOT-MOVE row lacks a
      stated reason.

    - **R8 — Criterion 5, device family and the no-device-name category.** Open activity
      `10041312551` (fēnix 6 Pro FIT) and activity `3480808722` (Suunto 9 FIT) and quote each
      one's device-era row verbatim. Expected: two different family names for two files of the
      same format. Then open a no-device-name activity (pick one from the index by
      `quality.deviceEra.family === 'no-device-name'`, and state which id before running) and
      quote its device-era row. Expected: an explicit no-device-name statement, never a
      fabricated device name.
      CAN FAIL: both FIT activities show the same family, or the no-device-name activity shows
      any device string at all.

    Add a `Reachability Audit` sub-section listing, for each of R1-R8, the CAN PASS and CAN FAIL
    lines together, so the audit can be read in one place. Any row missing either line is
    struck before the checkpoint runs.

    Do not run the browser rows yourself and do not pre-fill any verdict. Record the served
    digest, the recount stdout, the two named activity ids and their expected shard-derived
    numbers into the section so the developer is reading against stated values rather than
    forming an opinion.
  </action>
  <verify>
    <automated>node scripts/compute-pace-quality-recount.mjs && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `27-VALIDATION.md` gains a `## Round 1 Checkpoint (R1-R8)` section with eight rows, every
      verdict reading `pending`.
    - Every row carries both a `CAN PASS:` and a `CAN FAIL:` line, and the `Reachability Audit`
      sub-section repeats all sixteen lines together.
    - The served digest (computed from FETCHED bytes, not from the local file and not from the
      build log) is recorded, alongside the digest from `27-09-SUMMARY.md` and a statement of
      whether they match.
    - The recount script's stdout is recorded verbatim.
    - R4, R6 and R8 each state their expected value, derived from a source OTHER than the page
      being checked (the committed shard, the calibration report, the recount script, the
      index's own family field).
    - `node scripts/compute-pace-quality-recount.mjs` exits 0 and `npm run verify-dashboard` exits 0.
    - No verdict is pre-filled and no browser row is claimed as performed.
  </acceptance_criteria>
  <done>Eight rows drafted, each proven able to pass and to fail, each comparing against an independently derived value, against a build whose served digest is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>### CHECKPOINT — Task 2: Round 1 browser verification (R1-R8)</name>
  <files>.planning/phases/27-per-activity-quality-signals/27-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/27-per-activity-quality-signals/27-VALIDATION.md` § Round 1 Checkpoint —
      the eight drafted rows and the Reachability Audit, in full, before presenting anything.
    - `.planning/ROADMAP.md` § Phase 27 — the five Success Criteria each row maps to.
    - `.planning/phases/27-per-activity-quality-signals/27-03-SUMMARY.md` — the developer's
      recorded disposition on the composite rate, since R6 and R7 are scored against the
      criterion as that disposition left it, not as ROADMAP originally worded it.
  </read_first>
  <action>
    Confirm every automated gate is green before presenting anything: `npm run test`,
    `npx tsc --noEmit`, `npm run build-widgets` (grep its output for `css-syntax-error`, zero
    tolerance), `npm run verify-dashboard`, and `node scripts/compute-pace-quality-recount.mjs`.
    If any is red, stop — do not present a checkpoint against a red gate.

    Then present the eight rows drafted by Task 1 to the developer using the `<what-built>`,
    `<how-to-verify>` and `<resume-signal>` blocks below, with each row's stated expected value
    and its source filled in from `27-VALIDATION.md`. Give the developer the serve command and
    the served asset digest recorded in Task 1.

    Do not perform any browser row yourself and do not pre-fill any verdict. When the reply
    arrives, transcribe it verbatim into `27-VALIDATION.md` § Round 1 Checkpoint, apply the
    requirement→row map in the acceptance criteria to decide which requirements tick, and
    record any FAIL in a `Gap-Closure Record` sub-section without patching it.
  </action>
  <what-built>
    Five per-activity quality signals computed in CI (plans 27-01, 27-02, 27-04), landed
    additively on the dashboard index row with `DASHBOARD_INDEX_SCHEMA_VERSION` unchanged, one
    evidence shard per activity under `data/stats/pace-quality/`, a lazy shard client fetched
    once from the detail view's single mount point (27-06, 27-09), three severe-tier badges on
    all four activity-row surfaces (27-07), one "has any severe signal" filter (27-08), an
    always-on five-signal Quality Signals section on the detail view (27-09), an archive-wide
    calibration report with a threshold-sensitivity sweep (27-03), and an independent recount
    that does not import the classifier (27-05).

    Everything automatable has already been automated and is green: full vitest suite,
    `tsc --noEmit`, `npm run build-widgets` with zero `css-syntax-error`, `npm run verify-dashboard`
    including two demonstrated-failing publish checks, and the recount script against the live
    shipped index. What is left needs a real browser and a human.
  </what-built>
  <how-to-verify>
    Serve the publish directory and HARD-RELOAD before starting — `127.0.0.1` alone is not
    sufficient here; this project has served a stale `index.html` / `index.json` from a staged
    build during a checkpoint before. Hard-reload again after any navigation that changes the
    dataset. Keep the viewport between 500 and 941 px.

    Then work R1 through R8 as drafted in `27-VALIDATION.md` § Round 1 Checkpoint. Each row
    states what to do, what value to expect, and where that expected value came from.

    For each row reply with one of four verdicts, and please quote what you actually saw rather
    than summarizing:
      - **PASS** — observed matches the stated expectation. Quote the number or the text.
      - **FAIL** — observed contradicts it. Quote what you saw. Do not describe a fix.
      - **BLOCKED** — you could not gather the evidence the row asks for (the row asks for a
        width you cannot reach, the dataset has no qualifying activity, a browser refused).
        BLOCKED is not FAIL and is not PASS.
      - **NOT EXERCISABLE** — the row's precondition does not exist in this archive at all.

    A partial answer is BLOCKED, not PASS. If a row's evidence is at a different width, a
    different activity or a different theme than the row asks for, that is BLOCKED — the
    substitution is exactly how a green gate has shipped a defect here before.
  </how-to-verify>
  <resume-signal>
    Reply per row: `R1: PASS — "…"`, `R4: FAIL — "…"`, etc. A single blanket approval is
    accepted and will be recorded AS a blanket approval, with a note that no per-row detail was
    given — it will not be expanded into invented per-row observations.
  </resume-signal>
  <verify>
    <human-check>Eight verdicts recorded verbatim in 27-VALIDATION.md § Round 1 Checkpoint, each with the developer's own quoted words.</human-check>
  </verify>
  <acceptance_criteria>
    - Every verdict is transcribed VERBATIM into `27-VALIDATION.md`, not paraphrased. A blanket
      approval is recorded as a blanket approval and is not expanded per row.
    - Requirements are ticked in `REQUIREMENTS.md` ONLY after every row mapped to that
      requirement has PASSED. A BLOCKED row does not tick. This is the project's standing rule
      (Phase 19 UI-02, Phase 22 CAL-01/02, Phase 24 CUR-01 all reverted a premature tick), and
      the v2.1 retrospective specifically calls out ticking before verification as a process
      defect to stop repeating.
    - Requirement→row mapping, stated before ticking: QUAL-03 ← R2, R3, R6; QUAL-04 ← R4, R5;
      QUAL-05 ← R6, R7; ERA-01 ← R8; ERA-02 ← R8. QUAL-01 and QUAL-02 are discharged by the
      automated artifact inspection in plans 27-04 and 27-02 and are ticked on that evidence,
      cited by file and command.
    - Any FAIL is recorded verbatim as a gap in a `Gap-Closure Record` sub-section and is NOT
      patched in this plan. The house rule since 16-09: a failing row is logged, and the next
      planning round diagnoses it.
    - `27-VALIDATION.md` frontmatter is updated: `status` to `passed` only if every row is PASS,
      otherwise `partial`; `nyquist_compliant` to `true` only in the all-PASS case.
    - If R6's three figures disagree, the delta and its cause are recorded before any verdict is
      accepted — archive drift between runs is a legitimate cause and must be quantified; a
      threshold difference is not and is a FAIL.
  </acceptance_criteria>
  <done>Eight verdicts recorded verbatim; requirements ticked only where every mapped row passed; any failure logged as a gap rather than patched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built publish dir → local HTTP server → browser | The only place the assembled page is exercised as shipped |
| developer's observation → `27-VALIDATION.md` | Human evidence enters the record and drives requirement ticks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-33 | Tampering | stale served bundle | mitigate | R1 compares the SERVED asset digest (computed from fetched bytes) against the digest recorded in 27-09; the served `index.json` row count and totals are also re-checked. Hard-reload instructed explicitly. |
| T-27-34 | Repudiation | verdict fabrication | mitigate | Verdicts transcribed verbatim; a blanket approval is recorded as one rather than expanded; no browser row may be claimed as performed by an agent. |
| T-27-35 | Tampering | vacuous discriminator | mitigate | Every row carries a written CAN PASS / CAN FAIL pair, collected in a Reachability Audit; a row missing either is struck before the checkpoint runs. Directly targets the Phase 24 R26 failure mode. |
| T-27-36 | Tampering | premature requirement tick | mitigate | An explicit requirement→row map, an all-mapped-rows-PASS rule, and the recorded history of four prior reverts as the reason. |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `npm run test`, `npx tsc --noEmit`, `npm run build-widgets` (zero `css-syntax-error`),
  `npm run verify-dashboard` and `node scripts/compute-pace-quality-recount.mjs` all exit 0
  before the checkpoint is presented.
- The served asset digest is recorded and compared to 27-09's.
- All eight rows carry both reachability lines before any verdict is taken.
</verification>

<success_criteria>
- Criterion 2 is proven by a human network-panel reading against a digest-verified build, in
  addition to the plan 27-06 counter test.
- Criterion 3 is satisfied by a badge quotation read off the rendered page and compared to the
  committed shard.
- Criterion 4's satisfiable half is proven: the composite is measured, independently
  re-derived without importing the classifier, reported, and demonstrated responsive to a
  threshold move in both directions.
- Criterion 5 is proven on the fēnix/Suunto FIT pair and on a real no-device-name activity.
- No requirement is ticked on a BLOCKED or FAILED row.
</success_criteria>

<output>
Create `.planning/phases/27-per-activity-quality-signals/27-10-SUMMARY.md` when done.
Record: the served asset digest and whether it matched 27-09's; the three composite figures and
their deltas with stated causes; all eight verdicts verbatim; the requirement→row map and which
requirements were ticked; and any FAIL transcribed into the Gap-Closure Record unpatched.
</output>
