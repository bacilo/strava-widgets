---
phase: 27-per-activity-quality-signals
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - scripts/compute-pace-quality-calibration.mjs
  - scripts/compute-pace-quality-calibration.test.mjs
  - package.json
  - .planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md
autonomous: false
requirements: [QUAL-05]

must_haves:
  decisions: [D-01, D-02, D-03, D-04, D-05, D-06]
  truths:
    - "The composite severe rate is measured as the actual union across all three tiering signals over the full live archive, not estimated from per-signal marginals"
    - "Every reported rate names which of the two denominators it uses and reconciles them"
    - "The 26-RESIDUAL 14-activity residual is re-derived live and compared to the committed list"
    - "The measured overlap between the impossible-sample severe cohort and the severe-decimation cohort is reported, not hidden"
    - "Moving a threshold moves the measured composite rate, demonstrated in both directions, without editing any source constant"
    - "A composite rate materially above 5% is recorded as a reportable finding under D-02, never silently retuned away"
  artifacts:
    - path: "scripts/compute-pace-quality-calibration.mjs"
      provides: "the full-archive dry run, the union computation, the threshold-move discriminator"
      contains: "hasAnySevereSignal"
    - path: ".planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md"
      provides: "the committed calibration report Criterion 4 and plan 27-05 both read"
      min_lines: 60
    - path: "scripts/compute-pace-quality-calibration.test.mjs"
      provides: "guard test importing the script's pure functions without running the sweep"
  key_links:
    - from: "scripts/compute-pace-quality-calibration.mjs"
      to: "dist/analytics/pace-quality.js"
      via: "import of computePaceQualitySignals + hasAnySevereSignal"
      pattern: "dist/analytics/pace-quality.js"
    - from: "package.json"
      to: "scripts/compute-pace-quality-calibration.mjs"
      via: "compute-pace-quality-calibration npm script"
      pattern: "compute-pace-quality-calibration"
---

<objective>
Measure the ACTUAL composite severe rate — the union across all three tiering signals over the
full live archive — report it with its per-signal breakdown and both denominators, prove the
threshold knob is connected in both directions, and put the real number in front of the
developer for a disposition before any UI is built against it.

Purpose: research measured that D-04's locked severe-decimation cohort ALONE is 154/1,866 =
8.3%, already above ROADMAP Criterion 4's ~5% ceiling, before the other two signals contribute
anything. D-02 forbids retuning backward from the target and D-04 forbids narrowing the
decimation rule. So Criterion 4 as literally worded and D-01/D-02/D-04 may be jointly
unsatisfiable. This plan does not resolve that. It measures it and hands the number to the
person who can.

Output: `27-CALIBRATION.md`, a committed, regenerable report; and a recorded developer
disposition on the measured rate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-per-activity-quality-signals/27-CONTEXT.md
@.planning/phases/27-per-activity-quality-signals/27-RESEARCH.md
@.planning/phases/27-per-activity-quality-signals/27-02-SUMMARY.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-RESIDUAL.md
@scripts/compute-pace-residual.mjs

<interfaces>
<!-- Imported from the COMPILED module. `compute-pace-residual.mjs` sets the precedent:
     it imports `../dist/analytics/pace-derivation.js`, and its npm script is
     `npm run build && node scripts/compute-pace-residual.mjs`. Follow both. -->

```javascript
// from ../dist/analytics/pace-quality.js  (built by `npm run build` from 27-01/27-02)
computePaceQualitySignals(stream, metadata, options?) -> ActivityQualitySignals
hasAnySevereSignal(signals) -> boolean
resolveDeviceFamily(metadata) -> DeviceEraSignal
DECIMATION_SEVERE_ZERO_ADVANCE_FRACTION, DECIMATION_SEVERE_MIN_SAMPLES,
GAP_PROFILE_SEVERE_FRACTION, IMPOSSIBLE_SAMPLE_SEVERE_COUNT, WORLD_RECORD_100M_SPEED_MPS

// QualityThresholdOverrides — the knob, no source edit needed:
{ decimationZeroAdvanceFraction?, decimationMinSamples?, gapProfileSevereFraction?,
  impossibleSevereCount?, impossibleFloorMps? }
```

Script shape to copy — `scripts/compute-pace-residual.mjs`:
- header block stating what it reads, what it writes, and its only write target
- `import { readdirSync, readFileSync, writeFileSync } from 'fs'` / `join, dirname` /
  `fileURLToPath, pathToFileURL`
- per-file `try/catch` around `readFileSync`/`JSON.parse` that WARNS AND CONTINUES (T-26-01) —
  one unreadable stream must not abort a 1,890-file sweep
- pure exported functions (`zeroAdvanceFraction`, `renderResidualMarkdown`, …) plus a
  `main()` gated behind `if (import.meta.url === pathToFileURL(process.argv[1] ?? '').href)`,
  so a unit test can import the pure parts without running the sweep or writing the report

The two denominators, per D-06 — both must appear in the report and be reconciled:
- ALL activities in `data/activities/` (1,890 at last measurement)
- Activities with a committed stream in `data/streams/` (1,865-1,866 at last measurement)
The difference (24-25 activities) has no stream at all; those report `notComputableReason`,
are never counted in the severe numerator, and must be stated as an explicit line in the
report rather than absorbed into either denominator silently.

ROADMAP Criterion 4 says "1,864-activity archive" and "≈90 activities". CONTEXT D-06 says
1,890/1,866. Research measured 1,890/1,865. **All three are stale the moment they are written.**
Compute every denominator at run time. Do not hardcode any of these numbers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The full-archive calibration sweep and its committed report</name>
  <files>scripts/compute-pace-quality-calibration.mjs, package.json, .planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md</files>
  <read_first>
    - `scripts/compute-pace-residual.mjs` in full — the archive-sweep shape, the
      warn-and-continue per-file try/catch, the `main()`-behind-self-execution-check guard, and
      the "the report ships with the script that regenerates it" (D-19) discipline.
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-RESIDUAL.md` in
      full — the exact section structure to mirror (Cohort Definition, Window Formula, Summary,
      the per-activity table, Reconciliation, Regeneration) and the 14-activity residual list
      this task re-derives.
    - `.planning/phases/27-per-activity-quality-signals/27-02-SUMMARY.md` — the chosen threshold
      values and their written justifications, to be quoted into the report rather than
      re-derived.
    - `package.json` scripts block — the `compute-pace-residual` entry
      (`npm run build && node scripts/compute-pace-residual.mjs`) is the exact form to follow.
    - `.planning/phases/27-per-activity-quality-signals/27-CONTEXT.md` § D-01, § D-02, § D-06.
  </read_first>
  <action>
    Create `scripts/compute-pace-quality-calibration.mjs` sweeping the FULL live archive and
    writing `.planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md`. Add
    `"compute-pace-quality-calibration": "npm run build && node scripts/compute-pace-quality-calibration.mjs"`
    to `package.json` scripts. Its only write target is that one report file; it must never
    write into `data/`.

    Enumerate from `data/activities/*.json` (not from the stream manifest) so the stream-less
    population is visible rather than invisible. For each activity: read its activity JSON for
    `device_name`, `source_provider`, `elapsed_time`, `moving_time`; attempt to read
    `data/streams/{id}.json`, treating a missing file as `null` rather than an error; call
    `computePaceQualitySignals(stream, metadata)` with NO overrides.

    The report must contain, in this order:

    1. **Denominators, computed live.** Activity count, stream-file count, and the
       stream-less count as their arithmetic difference — each labelled with the command or
       expression that produced it. Then one sentence naming which denominator each rate below
       uses. Do not print 1,864, 1,866 or 1,890 as expected values anywhere; print what you
       measured, and add a line noting the ROADMAP/CONTEXT figures this run supersedes.

    2. **Thresholds in force**, quoted from `27-02-SUMMARY.md` with their justifications:
       the decimation rule (verbatim from D-04/26-RESIDUAL), the gap-profile cut, the
       impossible-sample cut, and the physical floor.

    3. **Per-signal severe cohorts** — count and percentage against BOTH denominators, one row
       per tiering signal, plus the minor-tier counts for context. Device era and
       elapsed-vs-moving appear as untiered distributions (a family census and a ratio
       quantile summary), explicitly labelled as contributing nothing to the composite per
       D-05/D-13/D-14.

    4. **THE COMPOSITE — the actual union.** Compute it as the size of the SET of activity ids
       for which `hasAnySevereSignal` is true. Not the sum of the three marginals, and not an
       inclusion-exclusion estimate. Report it against both denominators. Alongside it print a
       three-way overlap breakdown: how many activities are severe on exactly one signal, on
       exactly two, on all three, and the pairwise intersection sizes — in particular the
       impossible-sample-severe ∩ decimation-severe intersection, which research measured at
       90% for the "at least one impossible sample" form and which this run measures at the
       actual chosen cut.

    5. **The D-02 disposition paragraph.** If the composite rate lands materially above ~5%,
       write it as a FINDING, not a defect: state the measured rate, state that D-04's locked
       decimation cohort alone accounts for the bulk of it, state that D-02 forbids retuning
       backward from the target and D-04 forbids narrowing the decimation rule, and state that
       the disposition is the developer's under this plan's checkpoint. Do NOT propose a
       threshold change here, do not soften any threshold to bring the number down, and do not
       report the number as a failure. If it lands below ~5%, say so plainly and note that this
       was not the objective.

    6. **The D-04 boundary cross-check.** Run `npm run compute-pace-residual`, then compare the
       regenerated `26-RESIDUAL.md` residual list against the committed one as of Phase 26's
       close, and against this sweep's own severe-decimation cohort size. Report: whether the
       14-activity residual list still matches by id, and whether the severe-decimation cohort
       size has drifted from 154. A drift is a reportable finding about archive growth, not a
       failure — say which direction it moved and by how many.

    7. **Regeneration** — the npm command, mirroring `26-RESIDUAL.md`'s own closing section.

    Every number in the report is produced by this run. Nothing is transcribed from
    `27-RESEARCH.md`, `27-CONTEXT.md` or `26-RESIDUAL.md` except the threshold justifications
    in section 2, which are quoted and attributed.
  </action>
  <verify>
    <automated>npm run compute-pace-quality-calibration</automated>
  </verify>
  <acceptance_criteria>
    - `npm run compute-pace-quality-calibration` exits 0 and writes
      `.planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md`.
    - `git status --porcelain data/` shows no modification under `data/` after the run.
    - The report contains all seven numbered sections above.
    - `grep -c "1864\|1,864" .planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md`
      returns 0 outside the "figures this run supersedes" line.
    - The composite figure is derived from a Set of ids: `grep -n "new Set" scripts/compute-pace-quality-calibration.mjs`
      returns at least one match inside the composite computation, and the report states the
      composite is not the sum of the marginals.
    - Sanity gate on the composite, checkable without trusting the script: the reported
      composite count is greater than or equal to the largest single-signal severe count and
      less than or equal to the sum of the three. Assert this in the script and print it as a
      PASS/FAIL line in the report.
    - Section 6 reports the live `npm run compute-pace-residual` output, with the residual list
      compared by activity id, not by count alone.
    - Section 5 contains no proposed threshold change and no threshold was edited in
      `src/analytics/pace-quality.ts` during this task (`git diff --stat src/analytics/pace-quality.ts`
      is empty).
  </acceptance_criteria>
  <done>`27-CALIBRATION.md` exists, every figure in it was computed by this run against the live archive, and the composite is the true union.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the threshold knob is connected, in both directions</name>
  <files>scripts/compute-pace-quality-calibration.mjs, scripts/compute-pace-quality-calibration.test.mjs, .planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md</files>
  <read_first>
    - `scripts/compute-pace-quality-calibration.mjs` — the file as Task 1 left it.
    - `scripts/compute-pace-residual.mjs` lines 380-390 — the
      `if (import.meta.url === pathToFileURL(process.argv[1] ?? '').href)` self-execution guard
      that lets a test import the pure parts without running the sweep.
    - `.planning/phases/27-per-activity-quality-signals/27-VALIDATION.md` § Manual-Only
      Verifications, row 3 — the Criterion 4 demonstrated-failing requirement this task
      automates so the human checkpoint reads a recorded result rather than performing a sweep.
    - `.planning/phases/27-per-activity-quality-signals/27-CONTEXT.md` § D-02's closing
      sentence — the knob demonstration proves the knob is wired; it does not license turning
      it to reach a number.
  </read_first>
  <action>
    Add a `--sweep` mode to `scripts/compute-pace-quality-calibration.mjs` that re-runs the
    composite computation under a series of `QualityThresholdOverrides` and appends a
    **Threshold Sensitivity** section to `27-CALIBRATION.md`. No source constant is edited —
    the overrides go through the `options` parameter plan 27-02 added, exactly the way
    `classifyGaps(t, d, { pauseRule })` already takes its own knob.

    The sweep must demonstrate movement in BOTH directions for each of the two thresholds this
    phase chose, and must state the direction it expected before showing the number:
      - `gapProfileSevereFraction` set strictly looser than shipped → composite count strictly
        INCREASES (or the run reports "no movement", which is itself a finding to surface, not
        to paper over).
      - `gapProfileSevereFraction` set strictly stricter → composite count strictly DECREASES
        or stays equal at the floor set by the other two signals; if it stays equal, the report
        must SAY that the floor is D-04's locked decimation cohort and print that floor's size,
        because "the rate did not move" would otherwise read as a broken knob when it is
        actually the locked cohort dominating.
      - the same pair for `impossibleSevereCount`.
      - one run with `decimationZeroAdvanceFraction` moved, printed with an explicit label that
        this override exists ONLY to demonstrate the knob and that D-04 locks the shipped value
        — so no reader mistakes the demonstration for a proposal.

    This is the point of the exercise per this project's checkpoint history: a discriminator
    that cannot fail is as broken as one that cannot pass. Each row of the sensitivity table
    carries the override applied, the composite count, the delta from the shipped run, and a
    MOVED / DID-NOT-MOVE verdict with a stated reason when it did not move.

    Create `scripts/compute-pace-quality-calibration.test.mjs` — a vitest file importing the
    script's pure functions (the composite-from-signals reducer and the report renderer) and
    asserting them WITHOUT running the sweep or writing the report. Cover: the composite
    reducer returns the union size for hand-built signal sets including the all-three-severe
    and the none-severe cases; the sanity gate (max marginal ≤ composite ≤ sum of marginals)
    fires on a deliberately inconsistent hand-built input; and importing the module does not
    write `27-CALIBRATION.md` (assert the file's mtime is unchanged across the import).
  </action>
  <verify>
    <automated>npx vitest run scripts/compute-pace-quality-calibration.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/compute-pace-quality-calibration.test.mjs` exits 0.
    - `npm run compute-pace-quality-calibration -- --sweep` exits 0 and appends a
      **Threshold Sensitivity** section with at least six rows (two directions × three thresholds).
    - Every sensitivity row carries an explicit MOVED or DID-NOT-MOVE verdict; every
      DID-NOT-MOVE row carries a stated reason naming the floor that dominates.
    - At least one row shows the composite count strictly increasing and at least one shows it
      strictly decreasing relative to the shipped run — the discriminator is proven to work in
      both directions, not just one.
    - `git diff --stat src/analytics/pace-quality.ts` is empty for this task — no shipped
      threshold constant was edited to produce the demonstration.
    - `grep -c "process.env" scripts/compute-pace-quality-calibration.mjs` returns 0 — the knob
      is a function parameter, not an environment variable a CI run could accidentally set.
    - Importing the script in the test does not write the report: asserted by an mtime check in
      the test file.
  </acceptance_criteria>
  <done>Criterion 4's "demonstrated failing by moving a threshold" is executable, recorded in the report, and proven in both directions without touching a shipped constant.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>### CHECKPOINT — Task 3: Disposition on the measured composite severe rate</name>
  <files>.planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md</files>
  <read_first>
    - `.planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md` — the report Tasks 1
      and 2 produced. Read sections 1, 4, 5 and Threshold Sensitivity in full before presenting.
    - `.planning/ROADMAP.md` § Phase 27 Criterion 4 — the wording under discussion.
    - `.planning/phases/27-per-activity-quality-signals/27-CONTEXT.md` § D-01, § D-02, § D-04.
  </read_first>
  <action>
    Do not resolve this. Present it. Read `27-CALIBRATION.md` sections 1, 4, 5 and the Threshold
    Sensitivity table, then put the measured numbers in front of the developer using the
    `<decision>`, `<context>` and `<options>` blocks below, quoting the report verbatim rather
    than paraphrasing it. Fill every `{quote …}` placeholder below with the actual figure from
    the report before presenting; a placeholder reaching the developer is a defect.

    Do not edit any threshold constant in `src/analytics/pace-quality.ts`, do not amend
    `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md`, and do not propose a specific
    threshold value. Wait for the reply, then transcribe it verbatim into `27-03-SUMMARY.md`.
    Apply a ROADMAP/REQUIREMENTS wording amendment only if the developer's reply asks for one,
    in a follow-on commit, preserving the original wording in the summary.
  </action>
  <decision>
    ROADMAP Criterion 4 requires the top-tier flag count to be "under ~5%". The measured
    composite rate from this run is **{quote the actual number and both percentages from
    27-CALIBRATION.md section 4}**. D-04 locks the severe-decimation threshold verbatim at
    Phase 26's cohort rule; that cohort alone is **{quote its measured size and percentage}**.
    D-02 forbids retuning any threshold backward from the ~5% target.

    Under those three locked decisions the composite has a floor at the decimation cohort's
    size, so Criterion 4 as literally worded and D-01/D-02/D-04 may be jointly unsatisfiable.
    That is what the report measured. It is not a bug, and no threshold was moved to change it.
  </decision>
  <context>
    Present, in this order, quoting the report rather than paraphrasing:
      1. Both denominators and the stream-less count, so the percentages are unambiguous.
      2. The three per-signal severe cohorts.
      3. The composite union, with the exactly-one / exactly-two / all-three breakdown and the
         impossible-sample ∩ decimation intersection.
      4. The Threshold Sensitivity table, so the developer can see the knob is genuinely
         connected before deciding what to do with it.
      5. The D-04 boundary cross-check result (residual list match, cohort drift).

    Nothing downstream of this plan depends on the answer for its FIELD SHAPES — the index
    contract, shard, client, badges and filter are all shape-identical either way. What the
    answer settles is what Criterion 4 asserts at verification time, and therefore what plan
    27-10's checkpoint row can pass on.
  </context>
  <options>
    <option id="accept-and-amend">
      <name>Accept the measured rate; amend Criterion 4</name>
      <pros>Honours D-02 and D-04 exactly as written. The badge text "N% of recorded time in gaps or pauses" keeps meaning what it says. The rate becomes a reported finding, which is what QUAL-05's own wording asks for once the mechanism-first thresholds are fixed.</pros>
      <cons>ROADMAP Criterion 4's "under ~5%" clause has to be rewritten to "measured, independently re-derived, reported, and demonstrated responsive", and REQUIREMENTS QUAL-05's "≈90 of 1,864" figure has to be restated against a live denominator.</cons>
    </option>
    <option id="revisit-d04">
      <name>Revisit D-04's severe-decimation definition, with explicit sign-off</name>
      <pros>Could bring the composite under ~5% if the decimation severe tier were drawn differently.</pros>
      <cons>D-04 says Phase 26's cohort rule is reused VERBATIM, precisely so no second, differently-drawn line is invented for a mechanism Phase 26 already drew one for. Changing it here forks the definition from `26-RESIDUAL.md` and `compute-pace-residual.mjs`, and does it after seeing the number — which is the quota D-02 exists to prevent. Requires explicit sign-off recorded in the summary.</cons>
    </option>
    <option id="split-the-criterion">
      <name>Split Criterion 4 into a satisfiable half and a reported half</name>
      <pros>Keeps a real pass/fail gate on the parts that ARE satisfiable — the rate is measured against a live denominator, independently re-derived by a script that does not import the classifier (D-03), and demonstrated responsive to a threshold move in both directions — while the rate's VALUE becomes a reported finding rather than a threshold.</pros>
      <cons>Two clauses to track at verification instead of one.</cons>
    </option>
  </options>
  <resume-signal>
    Reply with one of: `accept-and-amend`, `revisit-d04`, `split-the-criterion`, or your own
    disposition in words. If `revisit-d04`, state the replacement rule explicitly — this plan
    will not infer one, and D-04 is not overridden by silence.
  </resume-signal>
  <verify>
    <human-check>Developer disposition recorded verbatim in 27-03-SUMMARY.md, including which option was chosen and any wording amendment to ROADMAP Criterion 4 / REQUIREMENTS QUAL-05.</human-check>
  </verify>
  <acceptance_criteria>
    - The measured composite rate is quoted verbatim to the developer, with both denominators.
    - The developer's reply is transcribed verbatim into `27-03-SUMMARY.md`, not paraphrased.
    - No threshold constant in `src/analytics/pace-quality.ts` is edited by this task under any
      option except an explicit `revisit-d04` sign-off, and if edited, the sign-off text is
      quoted in the same summary.
    - If the disposition amends ROADMAP Criterion 4 or REQUIREMENTS QUAL-05, the amendment is
      applied to those files in a follow-on commit and the original wording is preserved in the
      summary so the change is auditable.
  </acceptance_criteria>
  <done>A recorded developer disposition exists for the measured rate, and the phase proceeds against a criterion that is actually satisfiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/activities/`, `data/streams/` → calibration script | 1,890 externally-derived JSON files read in one sweep |
| calibration script → `.planning/…/27-CALIBRATION.md` | The script's only write target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | Denial of Service | archive sweep | mitigate | Per-file `try/catch` around `readFileSync`/`JSON.parse` that warns and continues (T-26-01) — one unreadable file cannot abort a 1,890-file run. Copied from `compute-pace-residual.mjs`. |
| T-27-09 | Tampering | write scope | mitigate | Only write target is `27-CALIBRATION.md`; asserted by a `git status --porcelain data/` acceptance check after the run. |
| T-27-10 | Repudiation | rate provenance | mitigate | Every figure computed by the run itself; no figure transcribed from research. The threshold-sensitivity table plus the max-marginal ≤ composite ≤ sum sanity gate make a fabricated composite detectable from the report alone. |
| T-27-11 | Tampering | silent retune | mitigate | `git diff --stat src/analytics/pace-quality.ts` asserted empty for Tasks 1 and 2; the knob is a function parameter, and `grep -c "process.env"` returning 0 rules out an ambient override. |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `npm run compute-pace-quality-calibration` and `... -- --sweep` both exit 0.
- `npx vitest run scripts/compute-pace-quality-calibration.test.mjs` exits 0.
- `git status --porcelain data/` is empty after both runs.
- `27-CALIBRATION.md` contains seven numbered sections plus Threshold Sensitivity, and the
  sanity gate line reads PASS.
- The developer's disposition is transcribed verbatim in the summary.
</verification>

<success_criteria>
- The composite severe rate is measured as a true union over the full live archive, against
  both denominators, with the stream-less population stated explicitly.
- The threshold knob is proven to move the rate in both directions without editing a shipped
  constant.
- A rate above 5% is recorded as a finding under D-02, and the developer has given a
  disposition with the real number in hand.
</success_criteria>

<output>
Create `.planning/phases/27-per-activity-quality-signals/27-03-SUMMARY.md` when done.
Record: the measured composite count and both percentages; the three per-signal cohorts; the
impossible ∩ decimation intersection; the D-04 residual cross-check result; the full Threshold
Sensitivity table; and the developer's disposition transcribed verbatim.
</output>
