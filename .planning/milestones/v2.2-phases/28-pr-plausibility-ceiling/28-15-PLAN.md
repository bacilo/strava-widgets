---
phase: 28-pr-plausibility-ceiling
plan: 15
type: execute
wave: 3
depends_on: ["28-13", "28-14"]
files_modified:
  - .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements: [PR-03, PR-04, PR-05]

must_haves:
  decisions: [D-03, D-04, D-09, D-10, D-13, D-14, D-15]
  truths:
    - "A human reads, on activity 4556693525's rendered detail view, a Demoted ceiling badge AND an Excluded badge on the 400m and 1K rows, and ONLY the Excluded badge on the 1 Mile, 5K and 10K rows. That is 2 of 5 rows demoted, matching the independently derived extent, and the page Round 1 recorded as PASS is now named as a FAIL state"
    - "A human reads activity 3475725513's 400m row still showing the world-record demotion, and its 1K row showing a ceiling demotion, both next to the owner's exclusion. This is the precedence rule observed in the browser"
    - "A human reads the 400m, 1K and 1 Mile Records notes verbatim, and each matches a string derived without the browser: 35 (8/17/10), 11 (7/1/3), 5 (3/0/2)"
    - "A human reads the demoted badge's computed colour in the dark theme as rgb(251, 146, 60), not the failing rgb(194, 65, 12)"
    - "The developer signs off the REGENERATED 28-DIFF.md. The sign-off is bound to its new sha256 and records the 31/31/31 reconciliation and the 13-row owner-excluded listing. The sign-off lives in 28-VALIDATION.md, never in the artifact (D-14)"
    - "Every row has CAN PASS and CAN FAIL lines checked for reachability in both directions before it runs. Count-bearing and string-bearing rows need a quoted observation; a blanket approval without one records those rows as BLOCKED"
    - "PR-03, PR-04 and PR-05 are re-ticked only if every Round 2 row PASSes. PR-01 and PR-02 are left for the verifier. ROADMAP and STATE are not marked complete by this plan"
  artifacts:
    - path: ".planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md"
      provides: "Round 2 Checkpoint rows, verdicts, Reachability Audit, fresh PR-04 sign-off"
      contains: "Round 2 Checkpoint"
  key_links:
    - from: "28-VALIDATION.md Round 2 rows"
      to: "Round 2 Evidence (plan 28-14) and the served shards"
      via: "expected strings and counts stated in each row before it runs"
      pattern: "Independently derived"
    - from: "28-VALIDATION.md PR-04 Sign-off (Round 2)"
      to: ".planning/phases/28-pr-plausibility-ceiling/28-DIFF.md"
      via: "sha256 recorded by plan 28-14 Task 2 and re-hashed after the checkpoint"
      pattern: "sha256"
---

<objective>
Run a Round 2 human checkpoint against a build served from verified bytes, and obtain a fresh
PR-04 sign-off on the regenerated diff.

Round 2 re-checks:
- R3: activity 4556693525 now carries a ceiling reason next to its exclusion.
- R6: the Records note is guard-accurate after CR-02.

It adds:
- a precedence row (3475725513)
- a dark-theme contrast row (WR-02)

Purpose: 28-VERIFICATION.md's "Human Verification Required" items 1-3, and ROADMAP criteria 4 and 5,
which require rendered evidence and a human sign-off that code re-derivation cannot supply.

Round 1's R3 certified the defect's own output. Its PASS line described exactly what CR-01
produced. So every Round 2 row names the Round 1 state as a FAIL where it applies, and it asserts
extent (which rows, how many, which guard) against values derived before the observation.

Output: Round 2 verdicts, the fresh sign-off, and requirement dispositions that match the results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/28-pr-plausibility-ceiling/28-VERIFICATION.md
@.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md
@.planning/phases/28-pr-plausibility-ceiling/28-DIFF.md
@.planning/phases/28-pr-plausibility-ceiling/28-09-PLAN.md
@.planning/phases/28-pr-plausibility-ceiling/28-09-SUMMARY.md
@.planning/phases/28-pr-plausibility-ceiling/28-13-SUMMARY.md
@.planning/phases/28-pr-plausibility-ceiling/28-14-SUMMARY.md

<interfaces>
Independent figures a row may compare the browser against. A row that compares the browser to
another number the same page shows is not evidence.

| figure | produced by |
|---|---|
| per-effort guard and reason for 4556693525 and 3475725513 | the served shards `data/stats/best-efforts/{id}.json`, curled and compared to the local files, plus plan 28-14's structural comparison |
| 13 owner-excluded ceiling demotions, 31 ceiling total, independent count 31 | `node scripts/compute-pr-ceiling-recount.mjs` (no classifier import) and plan 28-10's pre-fix sweep |
| per-guard, non-excluded counts per distance (400m 8/17/10, 1k 7/1/3, 1mi 3/0/2) | a throwaway node one-liner over `data/stats/best-efforts.json` written in Task 1, NOT `records-logic.ts` |
| demoted-badge token hex per theme | `src/dashboard/styles.css` read as text |
| the fresh diff's sha256 and predicted content | 28-VALIDATION.md § Round 2 Evidence (plan 28-14) |

Expected rendered strings, derived by the planner from the shipped document and the plan 28-13
templates. Task 1 must re-derive every one by its own arithmetic from the regenerated document
before drafting. If any differs, STOP: that is archive drift or a defect, and it must not be
absorbed into a row.

Activity 4556693525 detail view, Best Efforts panel, row by row. Only the Excluded badge should
appear wherever no Demoted badge is listed:
- 400m (45.2s): `Demoted — implied 8.85 m/s exceeds personal ceiling 5.11 m/s (1.28 x p90 3.99 m/s over 1825 filtered 400m efforts)`, then `Excluded — bad measurement`
- 1K (207.4s): `Demoted — implied 4.82 m/s exceeds personal ceiling 4.75 m/s (1.28 x p90 3.71 m/s over 1843 filtered 1k efforts)`, then `Excluded — bad measurement`
- 1 Mile (393.8s), 5K (1498.6s), 10K (3308s): `Excluded — bad measurement` only. Implied speeds 4.0867, 3.3364 and 3.0230 are under the ceilings 4.6323, 4.3458 and 4.2236.

Activity 3475725513 detail view:
- 400m (14.6s): `Demoted — implied 27.32 m/s exceeds world-record pace 9.30 m/s`, then `Excluded — Recorded with the same inaccurate GPS device class; its 1k time is not trusted as a genuine personal record.`
- 1K (148.9s): `Demoted — implied 6.72 m/s exceeds personal ceiling 4.75 m/s (1.28 x p90 3.71 m/s over 1843 filtered 1k efforts)`, then the same Excluded text
- 1 Mile, 5K, 10K, Half Marathon: Excluded only

Records screen, All time scope. Each note is the `<p class="text-label">` under its table:
- 400m: `35 400m efforts were demoted by a plausibility guard (8 by the personal ceiling, 17 by the world-record pace guard, 10 by the activity max-speed guard). Efforts the owner excluded are not counted here. See the activity detail view for each reason.`
- 1K: `11 1K efforts were demoted by a plausibility guard (7 by the personal ceiling, 1 by the world-record pace guard, 3 by the activity max-speed guard). Efforts the owner excluded are not counted here. See the activity detail view for each reason.`
- 1 Mile: `5 1 Mile efforts were demoted by a plausibility guard (3 by the personal ceiling, 2 by the activity max-speed guard). Efforts the owner excluded are not counted here. See the activity detail view for each reason.`
- 5K, 10K, Half Marathon: no note. Marathon: `No Marathon efforts yet` empty state. Take the label from `DISTANCE_DISPLAY_NAMES`.

Distinguishing values for R6: pre-fix Round 1 read `36 400m efforts were demoted by the plausibility
ceiling…`. CR-01 fixed without CR-02 would read 47 with the old ceiling wording. CR-02's copy
without the owner-exclusion filter would read 47 with breakdown 19/18/10. Only the correct build
reads 35 with breakdown 8/17/10. At 1K the pre-fix count is also 11, so there only the wording and
breakdown discriminate. A build missing the exclusion filter would read 13.

Dark-theme badge colour: `--demoted-text` `#fb923c`, which is `rgb(251, 146, 60)`. Pre-fix it was
`--accent-strong` `#c2410c`, `rgb(194, 65, 12)`. Light: `#b3390a`, `rgb(179, 57, 10)`.

Round 1 served-shard digest for 4556693525 (pre-fix): `82c467ac8de0344a7c9ad810c225a285bda429f2306fee71dbced0b8c96d1b39`. The Round 2 served digest MUST differ from it.

Checkpoint hazards in this project, each of which has cost a round before:
- `build-widgets` skips copying when the destination mtime >= the source mtime, and silently
  no-ops on a locally edited `dist/` file. Verify the SERVED bytes by digest, never from the build
  log.
- The browser cache serves a stale `index.html` / JSON. Use `http://127.0.0.1:<port>` AND
  hard-reload (cmd+shift+r) on every page, and prefer a cache-busting query on the page URL.
  `127.0.0.1` alone is not enough.
- The viewport clamps to widths 500..941; no row needs another width.
- Gestures need a human. These rows need navigation and reading only. An agent may read DOM text
  back through browser tooling if it is available, but must disclose that as agent readback, and
  the verdict words must still be the developer's.
- A native `confirm()` kills automation; no row triggers one.
- Verdict vocabulary: PASS, FAIL, BLOCKED, NOT EXERCISABLE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serve a digest-verified build, re-derive every expected value independently, and draft the Round 2 rows</name>
  <files>.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md</files>
  <read_first>
    - .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md (the Round 1 section, the AMENDED section, the Round 2 Evidence section and the Reachability Audit format)
    - .planning/phases/28-pr-plausibility-ceiling/28-09-PLAN.md Task 1 (the serving and digest recipe)
    - .planning/phases/28-pr-plausibility-ceiling/28-14-SUMMARY.md (the new 28-DIFF.md sha256 and reconciliation)
    - .planning/phases/28-pr-plausibility-ceiling/28-13-SUMMARY.md (the exact note templates and token values)
    - src/dashboard/views/detail-best-efforts-logic.ts `DISTANCE_DISPLAY_NAMES` (the rendered distance labels)
  </read_first>
  <action>
    Work in the primary working tree `/Users/pedf/workspace/strava-widgets`. Confirm that
    `git rev-parse HEAD:data/streams/manifest.json` is still
    `b8ac1a5494ae1384f9c5b67ce42752335fcbb532`, and that `data/stats/best-efforts.json` is the
    document plan 28-14 regenerated: `effortsDemoted` 65.

    1. Build and serve.
       - Run `npm run build && npm run build-widgets`. Do NOT rerun `compute-all-stats`; the data is
         pinned to plan 28-14.
       - For each of `data/stats/best-efforts.json`,
         `data/stats/best-efforts/4556693525.json`, `data/stats/best-efforts/3475725513.json` and
         `data/dashboard/index.json`, compare `shasum -a 256` of the source file with its
         `dist/widgets/data/...` copy. If any pair differs, the mtime skip ate the copy: `cp` from
         the source and re-verify.
       - Serve with `npx http-server dist/widgets -p 8917 -c-1`, or reuse the port if one is
         already serving that directory.
       - `curl` the emitted JS asset and the four JSON files. Record the sha256 of the FETCHED bytes
         and check each against its local file.
       - Assert that the fetched 4556693525 shard digest is NOT `82c467ac…8b1d39`, the Round 1
         pre-fix digest.
    2. Re-derive every expected value without the browser and without `records-logic.ts`:
       - With a throwaway node script, recompute the per-guard, non-excluded counts at every distance
         from `data/stats/best-efforts.json`.
       - Recompute each expected badge string from the shard's `durationSec`, `TARGET` meters
         (declared locally) and `doc.ceilings` using `toFixed(2)`, and compare it character for
         character with the shard's `demotion.reason`.
       - Read `--demoted-text` for both themes from `src/dashboard/styles.css` as text, and convert
         each hex value to `rgb()`.
       - Compare every value with the interfaces block. On any mismatch, STOP and report it; do not
         draft a row around it.
    3. Append `## Round 2 Checkpoint (R2-1..R2-6)` to `28-VALIDATION.md`, after Round 2 Evidence.
       Record the served digests first. Then draft the rows below.
       - Each row gets an `Independently derived, stated before the row runs:` line quoting the
         expected values, then `CAN PASS:` and `CAN FAIL:` lines, then `Verdict: pending`.
       - Each row also gets a `Reachability:` line that answers both questions: could this row pass
         against the pre-fix build, and could a correct build fail it? Name the concrete state for
         each answer.

    - **R2-1 served build.**
      - Ask: the developer opens `http://127.0.0.1:8917/?r2=<epoch>`, hard-reloads, and reads the JS
        asset filename in the Network panel.
      - CAN PASS: the filename equals the one recorded in step 1.
      - CAN FAIL: any other filename (a stale bundle).
      - Reachability: a stale cache fails it, and the fresh build passes it.

    - **R2-2 (R3 re-run): the pinned activity carries both claims, on exactly two of five rows.**
      - Ask: open 4556693525's detail view, hard-reload, and quote every badge on all five Best
        Efforts rows, in order.
      - Then open the Records screen (All time) and confirm the activity is absent from the 400m
        and 1K ranked tables.
      - CAN PASS:
        - the 400m and 1K rows each show the expected Demoted text followed by
          `Excluded — bad measurement`
        - the 1 Mile, 5K and 10K rows show only `Excluded — bad measurement`
        - the activity is absent from both ranked tables
      - CAN FAIL:
        - the 400m row shows only `Excluded — bad measurement` (the Round 1 PASS state, which is
          CR-01's output)
        - a Demoted badge appears on 1 Mile, 5K or 10K
        - the reason numbers differ from the expected text
        - the two claims render as one run-on string
        - the activity appears in a ranked table
      - Reachability: the pre-fix build fails it on the first CAN FAIL condition, and a correct build
        passes it.

    - **R2-3 precedence: an absolute-guard demotion is not overwritten.**
      - Ask: open 3475725513's detail view and quote the 400m and 1K rows' badges.
      - CAN PASS: 400m shows the world-record Demoted text and 1K shows the ceiling Demoted text,
        each followed by the Excluded text.
      - CAN FAIL:
        - 400m names the personal ceiling (the absolute guard was overwritten)
        - 1K shows only Excluded (the pre-fix state)
        - either row lacks the Excluded badge
      - Reachability: the pre-fix build fails it on the 1K row, and a correct build passes it.

    - **R2-4 (R6 re-run): guard-accurate Records notes.**
      - Ask: on Records (All time), quote the note under the 400m, 1K and 1 Mile tables verbatim,
        and confirm there is no note under 5K, 10K or Half Marathon.
      - CAN PASS: all three strings equal the expected text exactly, the three counts are 35, 11 and
        5, and there is no note at 5K, 10K or Half Marathon.
      - CAN FAIL:
        - any note says `demoted by the plausibility ceiling` for the whole count
        - the 400m count is 36 or 47, or anything other than 35, or the 1K count is 13
        - the breakdown is not 8/17/10
        - a note appears at 5K, 10K or Half Marathon
      - Reachability: pre-fix reads 36 and fails; a correct build reads 35 and passes.

    - **R2-5 dark-theme demoted-badge contrast (WR-02).**
      - Ask: with the theme set to dark, open 4556693525's detail view, and in DevTools read
        `getComputedStyle` `color` of the first `.badge--demoted` element. Then switch to light and
        read it again.
      - CAN PASS: dark reads `rgb(251, 146, 60)` and light reads `rgb(179, 57, 10)`.
      - CAN FAIL: dark reads `rgb(194, 65, 12)` (the pre-fix token), or any other value.
      - Reachability: this row is only reachable once R2-2 shows a demoted badge exists on that
        page. The pre-fix build has no demoted badge on 4556693525, so there R2-5 would be BLOCKED,
        not PASS. If that happens, use activity `3475712118`'s 400m row instead, which was demoted
        before the fix too.

    - **R2-6 fresh PR-04 sign-off (a document read, not a browser action).**
      - Ask: the developer reads the regenerated `28-DIFF.md` in full. Specifically:
        - the Summary table
        - the new `## Ceiling demotions on owner-excluded efforts` section (13 rows)
        - the Reconciliation
        Give the developer the machine diff against the signed-off version from plan 28-14, which
        shows the four sections as byte-identical.
      - State the independent figures:
        - ceiling-only 31 = recount byGuard.ceiling 31 = recount independentCeilingCount 31
        - the 13 labels match plan 28-10's pre-fix sweep
        - rankings, flags and retroactive promotions (3) are unchanged from the version signed in
          Round 1
      - Surface the D-04 observation: `3475726256@400m` is exactly the stale 44.0s / 9.09 m/s
        figure.
      - CAN PASS:
        - the developer confirms the three-way 31 reconciliation
        - the developer recognises all 13 owner-excluded rows as their own exclusions and finds none
          wrongly ceiling-demoted
        - the developer accepts that no ranking or flag changed relative to the Round 1 sign-off
      - CAN FAIL:
        - any of the three figures differ
        - a listed row is not an activity the developer excluded
        - a change appears outside the predicted sections
        - the developer rejects a demotion
      - Reachability: the pre-fix diff states 18 and has no listing, so it fails; the regenerated
        diff passes.

    Also record R4 once more as NOT EXERCISABLE with the unchanged reason, and record WR-01's
    browser path as NOT EXERCISABLE: no 2026 effort is in any top-10, so every This-year table
    renders its empty state. Neither gets a verdict request.

    Add a `### Round 2 Reachability Audit` repeating all CAN PASS and CAN FAIL lines together. Do
    not run any browser row, and do not pre-fill any verdict.

    Commit: `docs(28-15): draft Round 2 checkpoint rows against a digest-verified build`.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run verify-dashboard && node scripts/compute-pr-ceiling-recount.mjs --expect-demoted 65 && grep -c "Round 2 Checkpoint" .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 28-VALIDATION.md contains `## Round 2 Checkpoint (R2-1..R2-6)` with six rows, every verdict reading `pending`
    - Every row has `Independently derived`, `CAN PASS:`, `CAN FAIL:` and `Reachability:` lines, and `### Round 2 Reachability Audit` repeats the twelve CAN lines
    - R2-2's CAN FAIL line names `Excluded — bad measurement` shown alone on the 400m row as a failure state
    - R2-4 states the exact 400m expected string and names 36 and 47 as failing 400m counts
    - The served digests for the asset and the four JSON files are recorded and match the local files, and the fetched 4556693525 shard digest is recorded as different from `82c467ac8de0344a7c9ad810c225a285bda429f2306fee71dbced0b8c96d1b39`
    - The step-2 re-derivation output is recorded, with zero mismatches against the interfaces block
    - `npm test`, `npx tsc --noEmit`, `npm run verify-dashboard` and the recount with `--expect-demoted 65` all exit 0
  </acceptance_criteria>
  <done>Six rows are drafted against a verified build. Each can pass and can fail, and each compares against a value produced without the browser.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>### CHECKPOINT — Task 2: Round 2 browser verification (R2-1..R2-6) and the fresh PR-04 sign-off</name>
  <files>.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md § Round 2 Checkpoint and § Round 2 Evidence, in full
    - .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md (the regenerated file; be ready to explain the owner-excluded section)
    - .planning/ROADMAP.md Phase 28 success criteria 3, 4 and 5
    - .planning/REQUIREMENTS.md lines 56-60 (PR-01..PR-05 with their REOPENED notes)
  </read_first>
  <what-built>
    The personal ceiling is now applied to owner-excluded efforts too, so the 13 efforts that were over
    the ceiling but unflagged now carry a ceiling demotion alongside the owner's exclusion. The pinned
    activity 4556693525 is one of them. The Records notes now attribute each demotion to the guard
    that made it and leave out owner-excluded efforts. The demoted badge is readable in dark mode. The
    independent recount now fails on the defect's shape, and it did fail on the pre-fix archive. The
    archive-wide diff was regenerated: rankings and flags are unchanged, the ceiling-only total is 31
    (reconciled three ways), and the 13 owner-excluded demotions are listed.
  </what-built>
  <how-to-verify>
    Before presenting anything, re-run `npm test`, `npx tsc --noEmit`, `npm run verify-dashboard` and
    `node scripts/compute-pr-ceiling-recount.mjs --expect-demoted 65`, re-curl the served digests, and
    state each result.

    Present the rows one at a time, in order. For each, state the expected values BEFORE asking for
    the observation, and give the exact URL. Tell the developer to hard-reload each page.

    Say plainly up front that Round 1's R3 passed on the defect's own output, and that R2-2 is
    written so that the same page would now FAIL.

    For R2-2, R2-3, R2-4 and R2-5, the verdict needs the quoted rendered text or value.
    - The developer may paste it.
    - An agent may read it back from the DOM, disclosed as agent readback, and the developer
      confirms it.
    - A blanket "approved" with no quoted observation records those rows as BLOCKED, not PASS.
      Round 1's blanket approval is why the gap closure exists.

    For R2-6, record the developer's approval under `### PR-04 Sign-off (Round 2, D-14)`, with:
    - the regenerated 28-DIFF.md sha256
    - the 31/31/31 figures
    - the developer's verbatim words
    - the date
    Write nothing into 28-DIFF.md. Re-hash it after the checkpoint; it must be unchanged.

    House rules:
    - A failing row is recorded verbatim as a gap and is not patched during the checkpoint.
    - Evidence taken at the wrong condition (a stale build, the wrong theme, no hard reload) is
      BLOCKED.
    - Do not perform a browser row yourself beyond disclosed readback.
  </how-to-verify>
  <action>
    Transcribe each verdict verbatim, using the four-state vocabulary, into `### Round 2 Outcome`
    with a per-row table.

    If EVERY row R2-1..R2-6 is PASS:
    - In 28-VALIDATION.md, set frontmatter `status: approved` and `nyquist_compliant: true`, and
      update the Validation Sign-Off approval line. Append; do not delete the REOPENED history.
    - In `.planning/REQUIREMENTS.md`, tick PR-03, PR-04 and PR-05 (`[x]`). Append to each an italic
      note: `Re-ticked <date> on Round 2 (28-15), per-row PASS — see 28-VALIDATION.md § Round 2 Outcome; pending phase re-verification.`
    - Keep every earlier note, including the REOPENED text, legible and unedited. Set the
      traceability table rows for PR-03, PR-04 and PR-05 to match the ticks.
    - Leave PR-01 and PR-02 exactly as they are; the verifier decides those.

    If ANY row is FAIL or BLOCKED:
    - Leave PR-03, PR-04 and PR-05 unticked, and leave the frontmatter at `gaps_found` / `false`.
    - Open a `### Round 2 Gap-Closure Record` quoting the observation, with no suggested fix.

    Do not edit ROADMAP.md or STATE.md. Do not mark the phase complete. Phase completion follows
    re-verification.

    Commit: `docs(28-15): record Round 2 checkpoint verdicts and PR-04 sign-off`. Never put the
    skip-ci token in the message.
  </action>
  <resume-signal>Give a per-row verdict with the quoted observation for R2-2..R2-5 (or allow disclosed agent readback), and "approved" or objections for R2-6</resume-signal>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run verify-dashboard && node scripts/compute-pr-ceiling-recount.mjs --expect-demoted 65</automated>
    <human-check>Six Round 2 rows verdicted, with quoted observations, against a digest-verified build, and the fresh PR-04 sign-off bound to the regenerated 28-DIFF.md sha256</human-check>
  </verify>
  <acceptance_criteria>
    - All six rows carry a four-state verdict with the developer's own words. R2-2..R2-5 each quote the observed text or value, or are recorded BLOCKED.
    - `### PR-04 Sign-off (Round 2, D-14)` exists and names the regenerated sha256, and `shasum -a 256 .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md` still matches it after the checkpoint
    - PR-03, PR-04 and PR-05 are `[x]` in REQUIREMENTS.md if and only if all six rows are PASS; PR-01 and PR-02 lines are byte-unchanged (`git diff -- .planning/REQUIREMENTS.md` shows no change on lines starting `- [ ] **PR-01**` or `- [ ] **PR-02**`)
    - Every pre-existing REOPENED note in REQUIREMENTS.md is still present: `grep -c "REOPENED 2026-09-16" .planning/REQUIREMENTS.md` is at least 3
    - `git status --porcelain src scripts` is empty after the checkpoint
    - ROADMAP.md and STATE.md are not modified by this plan
  </acceptance_criteria>
  <done>Round 2's rendered evidence and the fresh sign-off are recorded, and the requirement dispositions match exactly what the rows showed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built bundle and data → served page | Stale bundles or skipped copies produce evidence about a build nobody shipped. |
| generated diff → sign-off record | An approval must name the exact bytes reviewed. |
| checkpoint row → requirement tick | A row that the defect itself satisfies certifies the defect (Round 1 R3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-15-A | Spoofing | stale served bytes | mitigate | Source-vs-dist digest check with `cp` fallback for the mtime skip; fetched-byte digests; shard digest must differ from Round 1's pre-fix digest; cache-busting URL and hard reload. |
| T-28-15-B | Repudiation | sign-off not bound to content | mitigate | D-14 sha256 recorded and re-hashed after the checkpoint. |
| T-28-15-C | Tampering | row blessing the defect | mitigate | Every row names the pre-fix state as a CAN FAIL and states its bidirectional reachability; expected values re-derived without the browser or `records-logic.ts`. |
| T-28-15-D | Repudiation | blanket approval standing in for observations | mitigate | String/count rows without a quoted observation are BLOCKED. |
| T-28-15-E | Tampering | premature tick | mitigate | Ticks only on all-PASS, annotated "pending phase re-verification"; PR-01/PR-02, ROADMAP and STATE untouched. |
| T-28-15-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — `npx http-server` was already used in Round 1; no new package is added to package.json. |
</threat_model>

<verification>
- All gates are green before and after the checkpoint.
- Served digests are recorded, and the 4556693525 shard digest differs from Round 1's.
- The 28-DIFF.md sha256 is unchanged across the checkpoint.
- `git status --porcelain src scripts` is empty.
</verification>

<success_criteria>
- A human has read both claims (Demoted and Excluded) on the pinned activity's exact two rows, the precedence rule on 3475725513, and the guard-accurate Records notes, each against values derived without the browser.
- The regenerated diff has been signed off against its hash, with 31 reconciled three ways.
- The requirement ticks follow the all-rows-PASS rule.
</success_criteria>

<output>
Create `.planning/phases/28-pr-plausibility-ceiling/28-15-SUMMARY.md` recording the served digests, all six verdicts with verbatim words and observations, the sign-off sha256 and date, the requirement dispositions, and any gap opened.
</output>
