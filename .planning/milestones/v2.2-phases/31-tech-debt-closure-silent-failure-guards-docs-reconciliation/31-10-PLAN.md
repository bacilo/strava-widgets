---
phase: 31-tech-debt-closure-silent-failure-guards-docs-reconciliation
plan: 10
type: execute
wave: 4
depends_on: ["31-08", "31-09"]
files_modified:
  - .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md
  - .planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TD-05]

must_haves:
  decisions: [D-12]
  truths:
    - "D-12: exactly one PR-04 re-sign happens in this phase, at the end, after every artifact-moving change has landed"
    - "D-12 / Phase 28 D-14: the sign-off is recorded in 28-VALIDATION.md, outside the generated artifact, bound to the new 28-DIFF.md sha256"
    - "The developer is shown the machine diff against the Round 3 signed version (cdf9d654…) and the three-way ceiling count BEFORE being asked for a verdict"
    - "The verdict is transcribed verbatim; a blanket approval is recorded as a blanket approval and is never expanded into invented per-row detail"
    - "TD-05 ticks only after the verdict, and only if every mapped row passed"
  artifacts:
    - path: ".planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md"
      provides: "the PR-04 Sign-off (Round 4) record, bound to the new sha256"
      contains: "PR-04 Sign-off (Round 4"
    - path: ".planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md"
      provides: "the completed Per-Task Verification Map and the phase's validation disposition"
      contains: "nyquist_compliant"
  key_links:
    - from: "the Round 4 sign-off record"
      to: "the committed 28-DIFF.md"
      via: "a sha256 computed from the committed bytes, with the superseded Round 3 hash named"
      pattern: "sha256"
---

<objective>
Close Phase 31 on the one thing only a human can do: re-sign PR-04 against the regenerated
`28-DIFF.md` (D-12, Phase 28 D-14). The developer sees the machine diff against the Round 3 signed
version and the three-way ceiling reconciliation before being asked for a verdict; the verdict is
transcribed verbatim; TD-05 ticks only afterwards.

Purpose: TD-04 changed the wording on every ceiling row of the shipped document, and the phase
regenerated all five artifacts of record. The Round 3 sign-off was bound to bytes that no longer
exist. A record that changed hands without the owner seeing it is, by this project's own standard, a
milestone failure.

Output: a Round 4 sign-off record, a completed Per-Task Verification Map, and TD-05's disposition.

**Run on the primary checkout.** The checkpoint's recount, `compute-pace-quality-recount`, and `verify-dashboard` runs all read gitignored `data/` and `dist/widgets/`; the served digest must be the build on this machine, per the staged-build cache-trap lesson.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-CONTEXT.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md
@.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md
@.planning/ROADMAP.md

<interfaces>
<!-- The figures a row may be compared against must come from somewhere other than the artifact
     being signed. A document agreeing with itself is not evidence. -->

| figure | produced by | source |
|---|---|---|
| the Round 3 signed bytes | `git show <rev>:.planning/phases/28-pr-plausibility-ceiling/28-DIFF.md`, verified to hash to `cdf9d65499d7ac62123ecc33d1e298ae08a0d07f6740ba7bdcc4cf5fa365b8dd` | git history, pre-31-08 |
| ceiling-only demoted, per distance and total | the regenerated `28-DIFF.md` | 31-08 |
| the same count, with no classifier import | `node scripts/compute-pr-ceiling-recount.mjs` → `byGuard.ceiling` and `independentCeilingCount` | Phase 28 D-15 |
| flagged activities, exclusions accounted | the same recount run | 31-08 |
| the composite, unmoved | `node scripts/compute-pace-quality-recount.mjs --expect 299` | Phase 27 script |
| elevation cohorts, unmoved | `node scripts/compute-elevation-recount.mjs` | Phase 30 script |
| Round 3 baseline to diff against | 1,899 / 1,874 considered; ceiling-only 32; flag flips 13; retroactive promotions 2; ranking rows moved 49; `totals.effortsDemoted` 66; flagged 47 | `28-VALIDATION.md` § PR-04 Sign-off (Round 3) |

| convention | rule |
|---|---|
| where the sign-off lives | `28-VALIDATION.md`, never inside `28-DIFF.md`; bound to a sha256 (Phase 28 D-14) |
| Round 4 placement | appended AFTER § PR-04 Sign-off (Round 3), following its structure: artifact reviewed, sha256, prior signed version superseded, material presented, developer's verdict, date, notes |
| expected content delta | TD-04's reason format on every ceiling row, plus any archive drift since 2026-09-19 | D-12 |
| verdict handling | transcribed verbatim; a blanket approval is recorded as one, not expanded; a FAIL is logged unpatched and diagnosed by the next round | house rule since 16-09 |
| tick discipline | requirements tick after verification, never before; a BLOCKED row does not tick | STATE.md, v2.1 retrospective |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the machine diff and the reconciliation, and draft the Round 4 rows unanswered</name>
  <files>.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md, .planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md` § PR-04 Sign-off (Round 3) in
      full — its exact structure is the template Round 4 follows, and its figures are the baseline.
    - `.planning/phases/28-pr-plausibility-ceiling/28-CONTEXT.md` § D-13/D-14/D-15 — the diff
      artifact, the sign-off living outside it, and the recount's independence.
    - `.planning/phases/31-.../31-08-SUMMARY.md` — the regeneration's recorded figures and the new
      sha256, which this task re-derives rather than trusts.
    - `.planning/phases/31-.../31-VALIDATION.md` — the Per-Task Verification Map, whose Status column
      this task completes, and the Manual-Only table describing this very sign-off.
    - `.planning/STATE.md` § Accumulated Context — the checkpoint precedents: a discriminator that
      went vacuous, a verdict held against pressure, failures logged verbatim rather than patched.
  </read_first>
  <action>
    Recover the Round 3 signed bytes: find the last commit that touched
    `.planning/phases/28-pr-plausibility-ceiling/28-DIFF.md` before 31-08's regeneration, extract that
    revision with `git show <rev>:<path>`, and verify with `shasum -a 256` that it hashes to
    `cdf9d65499d7ac62123ecc33d1e298ae08a0d07f6740ba7bdcc4cf5fa365b8dd`. If it does not, stop and
    report — the signed baseline must be identified by its hash, not by its position in history.

    Produce the machine diff between that revision and the committed file, and summarise it honestly:
    how many lines changed, how many of those are reason-string reformatting only, and what else
    moved. Compute `shasum -a 256` of the committed file and state it beside the superseded hash.

    Re-derive the three-way ceiling figure yourself rather than copying 31-08's: the diff's
    ceiling-only count, the recount's `byGuard.ceiling`, and the recount's `independentCeilingCount`.
    Record all three verbatim, plus the recount's flagged-activity count, its exclusions-accounted
    line, and its `--expect-demoted` verdict. Also run and record
    `node scripts/compute-pace-quality-recount.mjs --expect 299` and
    `node scripts/compute-elevation-recount.mjs`.

    Tabulate every headline figure against the Round 3 baseline in the interfaces table: unchanged, or
    changed with a stated cause and a number. If any figure moved for a reason other than TD-04's
    formatting or archive drift, say so plainly — that is a finding, not a footnote.

    Append a `## PR-04 Sign-off (Round 4 — post-TD-04 regeneration, D-12)` section to
    `28-VALIDATION.md` following Round 3's structure, with every field filled EXCEPT the verdict,
    which reads `pending`. Draft three rows for the developer, each carrying an explicit `CAN PASS:`
    line and a `CAN FAIL:` line written BEFORE the checkpoint runs — a row whose CAN FAIL line cannot
    be written is vacuous and must be struck or redrafted:

      - **R4-1 — the content delta is only what was predicted.** The developer reads the diff summary
        and confirms the changes are the reason-string reformatting plus any stated archive drift, and
        nothing else.
        CAN FAIL: a record changes hands that was not named; a rank table moves; a count moves with no
        stated cause.

      - **R4-2 — the numbers reconcile three ways.** The developer reads the three independently
        produced ceiling counts and confirms they agree, and that the flagged-activity count and the
        exclusions-accounted line match the recount.
        CAN FAIL: any two of the three disagree, or the flagged count moved without a cause.

      - **R4-3 — the margin is legible on a real row.** The developer reads one regenerated ceiling
        reason from `28-DIFF.md` — use the `3475730418@1mi` row if it is still present, since that is
        the record Round 3's note flagged — and confirms the implied speed, the ceiling and the margin
        are three distinct legible numbers, closing the "4.63 exceeds 4.63" observation.
        CAN FAIL: implied and ceiling still render identically; the margin reads `0.000`; the row is
        absent, in which case the row is struck as NOT EXERCISABLE with the reason recorded rather
        than substituted with a more convenient row.

    Add a short Reachability Audit repeating all six CAN PASS / CAN FAIL lines together. Do not
    pre-fill any verdict and do not answer any row yourself.

    Finally, complete `31-VALIDATION.md`: fill the Task ID / Plan / Wave columns for every row from
    the plans this phase actually emitted, set each row's Status from the recorded evidence, and
    record every `-t` filter's proven match count in its row. Leave the frontmatter alone; Task 2
    dispositions it.
  </action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/31-10-t1.log; ! grep -qE "[1-9][0-9]* failed" /tmp/31-10-t1.log && npx tsc --noEmit && node scripts/compute-pr-ceiling-recount.mjs && node scripts/compute-pace-quality-recount.mjs --expect 299 && node scripts/compute-elevation-recount.mjs && npm run verify-dashboard && grep -c "PR-04 Sign-off (Round 4" .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The recovered Round 3 revision is verified by hash: its `shasum -a 256` equals `cdf9d65499d7ac62123ecc33d1e298ae08a0d07f6740ba7bdcc4cf5fa365b8dd`; the revision and the command are recorded.
    - The new sha256 is computed from the committed bytes and recorded beside the superseded one.
    - The three-way ceiling figure is re-derived in this task (not copied from 31-08) and all three values are recorded verbatim, together with the flagged-activity count and the `--expect-demoted` verdict.
    - Every Round 3 baseline figure is tabulated as unchanged or changed-with-cause.
    - `grep -c "CAN PASS:" .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md` and `grep -c "CAN FAIL:"` each increase by exactly 6 (three rows plus the audit's repetition); all three verdicts read `pending`.
    - Nothing is written into `28-DIFF.md`: `git diff .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md` is empty.
    - `31-VALIDATION.md` has no `TBD` left in its Task ID, Plan or Wave columns: `grep -c "| TBD |" .planning/phases/31-*/31-VALIDATION.md` returns 0, and every `-t` row records its proven match count.
    - All gate commands exit 0 and their output is recorded verbatim.
  </acceptance_criteria>
  <done>The material is on the table, every number derived somewhere other than the document being signed, and no verdict pre-filled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>### CHECKPOINT — Task 2: PR-04 Round 4 re-sign (R4-1, R4-2, R4-3)</name>
  <files>.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md, .planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md` § PR-04 Sign-off (Round 4) — the
      drafted rows and the Reachability Audit, in full, before presenting anything.
    - `.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md` § PR-04 Sign-off (Round 3) — the
      previous verdict's wording and the three-option prompt shape it used.
    - `.planning/phases/31-.../31-VALIDATION.md` § Manual-Only Verifications — this checkpoint's own
      recorded instructions.
    - `.planning/STATE.md` § Carried into the next milestone — requirements are ticked after
      verification, never before.
    - `.planning/REQUIREMENTS.md` TD-01..TD-06 — the tick state each plan left behind, so this plan
      can state what remains and why.
  </read_first>
  <action>
    Confirm every gate is green before presenting anything: `npm test`, `npx tsc --noEmit`,
    `npm run build-widgets` (grep for `css-syntax-error`, zero tolerance),
    `npm run verify-dashboard`, `node scripts/compute-pr-ceiling-recount.mjs`,
    `node scripts/compute-pace-quality-recount.mjs --expect 299`, and
    `node scripts/compute-elevation-recount.mjs`. If any is red, stop — a checkpoint against a red
    gate is worthless.

    Present the three rows drafted in Task 1, with the machine diff summary, both sha256 values, and
    the three-way ceiling figure shown BEFORE the verdict is requested, exactly as Round 3 did. Offer
    the same shape of explicit choice Round 3 used — approve, approve with a named additional action,
    or hold — so the developer is choosing rather than confirming.

    Do not answer any row yourself and do not pre-fill any verdict. When the reply arrives, transcribe
    it verbatim into `28-VALIDATION.md` § PR-04 Sign-off (Round 4), then apply the disposition rules in
    the acceptance criteria. Record any FAIL as a gap, unpatched, with the row that produced it.
  </action>
  <what-built>
    Six tech-debt closures, each with a demonstrated-failing test. The four CR-01 regression tests no
    longer read the owner-editable exclusions file; one premise test does, and fails with re-pin
    instructions (31-01). `copyJsonTree` compares content, not mtime, so a doctored
    `dist/widgets/data/` file cannot survive a build, and says so by path when it is replaced (31-02).
    An unrecognized demotion guard is rendered as "N by another guard" instead of folded away, and the
    ceiling recount exits non-zero naming each malformed exclusions entry (31-03); the curation queue
    reports how many entries it ignored (31-04). The ceiling demotion reason now states its margin at
    three decimals, closing the "4.63 m/s exceeds personal ceiling 4.63 m/s" wording you flagged in the
    Round 3 sign-off (31-05). Two generator defects were fixed and `isStreamFile` given one owner
    (31-06, 31-07), and all five artifacts of record were regenerated twice each against the merged
    1,899-activity archive, each proven to reproduce itself apart from its timestamp line (31-08).
    Every stale hand-written figure was re-measured and corrected in place with a dated note, and
    `27-VALIDATION.md` flipped to passed (31-09).

    Everything automatable is automated and green. What is left is the one irreducible human step:
    the diff you signed on 2026-09-19 no longer exists byte-for-byte, so PR-04 needs your signature
    against the regenerated one.
  </what-built>
  <how-to-verify>
    Nothing to install or serve — this is a document read against machine-produced numbers.

    You will be shown, before being asked anything: the machine diff between the Round 3 signed
    `28-DIFF.md` (sha256 `cdf9d654…`, recovered from git and verified by hash) and the regenerated
    one; both sha256 values; the three-way ceiling count derived three independent ways; and each
    Round 3 headline figure marked unchanged or changed-with-cause.

    Then answer R4-1, R4-2 and R4-3 as drafted. Each row states what to check, what to expect, and
    where the expected value came from — compare against the stated value rather than against another
    part of the same document.

    For each row reply with one of four verdicts, quoting what you actually saw:
      - **PASS** — observed matches the stated expectation. Quote the number or the text.
      - **FAIL** — observed contradicts it. Quote what you saw. Do not describe a fix.
      - **BLOCKED** — the evidence the row asks for could not be gathered.
      - **NOT EXERCISABLE** — the row's precondition does not exist in this archive (R4-3 if the
        `3475730418@1mi` row is gone).

    A partial answer is BLOCKED, not PASS.
  </how-to-verify>
  <resume-signal>
    Reply per row: `R4-1: PASS — "…"`, or give an overall choice (approve / approve and also … /
    hold) in your own words. A single blanket approval is accepted and will be recorded AS a blanket
    approval, with a note that no per-row detail was given — it will not be expanded into invented
    per-row observations.
  </resume-signal>
  <verify>
    <human-check>Three verdicts recorded verbatim in 28-VALIDATION.md § PR-04 Sign-off (Round 4), in the developer's own quoted words, bound to the new sha256.</human-check>
  </verify>
  <acceptance_criteria>
    - The verdict is transcribed VERBATIM into `28-VALIDATION.md` § PR-04 Sign-off (Round 4), not paraphrased. A blanket approval is recorded as a blanket approval and is not expanded per row.
    - The record names: the artifact reviewed, the new sha256, the superseded Round 3 sha256 `cdf9d65499d7ac62123ecc33d1e298ae08a0d07f6740ba7bdcc4cf5fa365b8dd`, the material presented, the verdict, and the date. Nothing is written into `28-DIFF.md`.
    - Requirement→row mapping, stated before ticking: TD-05 ← R4-1, R4-2, R4-3 plus the automated evidence from 31-06/31-07/31-08/31-09 (the five idempotence proofs, the two calibration test groups, the manifest cross-check, and the five dated corrections), cited by plan and command.
    - TD-05 is ticked in `REQUIREMENTS.md` ONLY if all three rows PASS. A BLOCKED or NOT EXERCISABLE row does not tick. If R4-3 is NOT EXERCISABLE because the named row is gone, that is recorded and the tick decision is the developer's, stated explicitly rather than assumed.
    - The tick state of TD-01, TD-02, TD-03, TD-04 and TD-06 is audited and reported: each is either already ticked by its own plan with the evidence named, or left unticked with the reason. Nothing is ticked here on another plan's behalf without naming the green evidence.
    - Any FAIL is recorded verbatim as a gap in a Gap-Closure Record sub-section and is NOT patched in this plan.
    - `31-VALIDATION.md` frontmatter is dispositioned: `status: passed` and `nyquist_compliant: true` and `wave_0_complete: true` only if every row in its Per-Task map is green and all three checkpoint rows PASS; otherwise `status: partial` with the reason recorded.
    - `.planning/REQUIREMENTS.md` is the only file whose tick state changes, and only after the verdict.
    - The commit message contains no CI-skip token; nothing is pushed by this plan.
  </acceptance_criteria>
  <done>Round 4 signed or held on the developer's own words, TD-05 dispositioned by that verdict, and the phase's validation record closed honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regenerated `28-DIFF.md` → the developer's judgment → a signed record | The only place a record changing hands is caught before it ships |
| developer's words → `28-VALIDATION.md` → `REQUIREMENTS.md` tick state | Human evidence entering the record and driving a requirement's disposition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Repudiation | a sign-off bound to bytes other than the ones reviewed, or a baseline identified by position rather than content | mitigate | The Round 3 revision is recovered from git and verified by `shasum -a 256` against the recorded hash before any diff is produced; the new hash is computed from the committed bytes; both appear in the record |
| T-31-33 | Repudiation | verdict fabrication or a blanket approval expanded into invented per-row detail | mitigate | Verdicts transcribed verbatim; a blanket approval is recorded as one; no row may be claimed as answered by an agent |
| T-31-34 | Tampering | a vacuous or unsatisfiable checkpoint row | mitigate | Each row carries a written CAN PASS / CAN FAIL pair collected in a Reachability Audit, checked in both directions; R4-3 is struck as NOT EXERCISABLE rather than substituted with a more convenient row |
| T-31-35 | Repudiation | a premature or unevidenced requirement tick | mitigate | TD-05 ticks only on three PASSes; the tick state of the other five requirements is audited and each is justified or left open; ticks happen after verification, never before |
| T-31-36 | Tampering | a number presented to the developer that the document produced about itself | mitigate | The three-way ceiling figure is re-derived in Task 1 from the recount, which imports nothing it checks (Phase 28 D-15); the baseline comes from git history, not from the new file |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
- All gates green before presentation: `npm test`, `npx tsc --noEmit`, `npm run build-widgets` (zero `css-syntax-error`), `npm run verify-dashboard`, and the three recount scripts.
- The Round 3 baseline is verified by hash before the diff is produced.
- Three verdicts recorded verbatim with CAN PASS / CAN FAIL lines written beforehand.
- `28-DIFF.md` unmodified by this plan.
</verification>

<success_criteria>
- One re-sign, at the end of the phase, bound to the new sha256 and recorded outside the artifact.
- The developer saw the machine diff and the three-way reconciliation before being asked.
- TD-05's disposition follows the verdict, and any failure is logged rather than patched.
</success_criteria>

<output>
Create `.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-10-SUMMARY.md` when done.
Record: the recovered revision and its verified hash, the diff summary, both sha256 values, the
three-way ceiling figures, the verdict verbatim, the requirement→evidence map as applied, the audited
tick state of all six TD requirements, and any gap left unpatched with the row that produced it.
</output>
