---
phase: 31-tech-debt-closure-silent-failure-guards-docs-reconciliation
plan: 09
type: execute
wave: 3
depends_on: ["31-08"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/v2.2-MILESTONE-AUDIT.md
  - .planning/phases/27-per-activity-quality-signals/27-VALIDATION.md
autonomous: true
requirements: [TD-05, TD-06]

must_haves:
  decisions: [D-13, D-14]
  truths:
    - "D-13: every stale hand-written figure the audit names is corrected in place, each with a dated italic note naming the source record that measured it"
    - "D-13: each corrected figure is re-measured against the merged archive or read off the artifact 31-08 regenerated — never copied from the audit's own older measurement"
    - "D-13: the audit's own tech_debt list records 27 G-01 as CLOSED by plan 27-11, so the next reader does not re-open it"
    - "D-14 / TD-06: with G-02 closed, 27-VALIDATION.md's frontmatter reads status: passed and its G-02 record says so, dated"
    - "No v2.2 phase carries a pre-execution validation record into the archive"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PACE-06, ERA-02 and PR-03/04/05 corrected in place with dated notes"
      contains: "Corrected 2026-"
    - path: ".planning/phases/27-per-activity-quality-signals/27-VALIDATION.md"
      provides: "status: passed and a dated G-02 closure note"
      contains: "status: passed"
  key_links:
    - from: "the corrected figures in REQUIREMENTS.md and ROADMAP.md"
      to: "26-RESIDUAL.md, 27-VALIDATION.md G-02/G-03, 28-VERIFICATION.md, data/dashboard/index.json"
      via: "each dated note names the record or command the new figure was measured from"
      pattern: "Corrected 2026-"
---

<objective>
Reconcile every stale hand-written figure the v2.2 close-out audit names, in place, each with a dated
italic note naming its source record (D-13), and close TD-06 by flipping `27-VALIDATION.md` to
`status: passed` in the same plan that closes G-02 (D-14).

Purpose: the milestone cannot be completed on a record that contradicts its own artifacts. The
generators were fixed and the artifacts regenerated in 31-06 through 31-08, so the figures quoted by
hand in `REQUIREMENTS.md` and `ROADMAP.md` are now the last things out of step.

Output: corrected prose with dated provenance notes, an amended audit tech_debt list, and a v2.2
archive in which no phase carries a pre-execution validation record.

**Run on the primary checkout.** Task 1's device-family census reads the gitignored `data/dashboard/index.json`; the corrected ERA-02 figure must be measured on the merged archive, not on a worktree copy of unknown vintage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-CONTEXT.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-RESEARCH.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md
@.planning/v2.2-MILESTONE-AUDIT.md

<interfaces>
<!-- The exact edit sites, found by grep this session. Line numbers may shift; the strings will not. -->

| # | site | current text | correction |
|---|---|---|---|
| 1 | `.planning/REQUIREMENTS.md` PACE-06 (line ~33) | "**13 of the 154**" | the live figure read off the `26-RESIDUAL.md` that 31-08 committed; ROADMAP line 97 already says **14 of the 154, all marginal (0.51-2.44%)** — reconcile REQUIREMENTS to the artifact, not to ROADMAP's copy |
| 2 | `.planning/REQUIREMENTS.md` ERA-02 (line ~94) | "716 of 1,864 activities (38%)" plus a caveat citing "663 of 1,890" | the live figure re-measured now; research measured **663 of 1,899 (34.9%)** on 2026-09-19 — re-measure before editing, do not copy |
| 3 | `.planning/ROADMAP.md` Phase 27 Criterion 5 (line ~176) | "the 716-activity no-device-name cohort" | the same re-measured figure |
| 4 | `.planning/ROADMAP.md` Phase 26 Criterion 1 (line ~97) | "re-derived against the live committed archive on 2026-09-08 (1,866 streams scanned)" | the post-G-03 count from the regenerated `26-RESIDUAL.md` — this figure carries the manifest miscount 31-06 fixed, and is named as stale by `27-VALIDATION.md` G-03 |
| 5 | `.planning/REQUIREMENTS.md` PR-03, PR-04, PR-05 (lines ~58-60) and the traceability rows (~169-171) | "pending phase re-verification" | replaced by the re-verification reference: `28-VERIFICATION.md`, verified 2026-09-17T10:15Z, `status: passed`, 5/5, `previous_status: gaps_found` 2/5, gaps_remaining empty |
| 6 | `.planning/v2.2-MILESTONE-AUDIT.md` tech_debt list (line ~57) | "G-01 (open): 27-CALIBRATION.md is not regenerable byte-identical…" | marked CLOSED by plan 27-11 (2026-09-10); confirmed again by 31-08's regeneration, which differs only by timestamp and archive growth |
| 7 | `.planning/phases/27-per-activity-quality-signals/27-VALIDATION.md` | `status: partial`; G-02 "Status: OPEN"; the header note saying G-02 "remains open and is scheduled for Phase 31 D-13" | `status: passed`; G-02 CLOSED with today's date and the new figure; the header note updated to match |

| fact | value | source |
|---|---|---|
| house style for the note | the "corrected `<date>`" dated-parenthetical style already used in `26-RESIDUAL.md` and at ROADMAP line 97 (Phase 30 D-04) — read one before writing one | 31-PATTERNS § No Analog Found |
| the ERA-02 measurement | a device-family census over `data/dashboard/index.json`; `27-VALIDATION.md` G-02 records the full census shape to reproduce | G-02 |
| what must NOT be edited | ROADMAP § Phase 31's own Criterion 5 (line ~361), which states the pre-merge target "663/1,890" — it is this phase's own success criterion and stays as written; the live denominator difference is recorded in the note instead | planner decision, recorded here so it is not re-litigated |
| Nyquist backfill | already done before planning (commits `c5f0e1ef` 29, `faaf7c5e` 26, `4b89773f` 27); 26 and 29 are compliant. Do NOT re-run `/gsd-validate-phase` and do NOT hand-edit another phase's status table beyond the single G-02-gated flip D-14 authorises | D-14 |
| tick discipline | requirements are ticked after verification, never before; three were ticked, reverted and re-ticked during v2.1 for exactly this reason | STATE.md § Carried into the next milestone |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure each stale figure and correct it in place with a dated note</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-RESIDUAL.md` — as
      regenerated and committed by 31-08: the residual count, the cohort size and the scanned-archive
      figure all come from here, not from any older document.
    - `.planning/phases/27-per-activity-quality-signals/27-VALIDATION.md` § G-02 and § G-03 — the
      source records the notes must name, and G-02's device-family census method.
    - `.planning/phases/28-pr-plausibility-ceiling/28-VERIFICATION.md` frontmatter — the
      re-verification date, status and score the PR-03/04/05 wording will reference.
    - `.planning/REQUIREMENTS.md` lines 30-40, 55-62, 90-98 and 165-175 — every edit site, in full,
      before changing any of them.
    - `.planning/ROADMAP.md` lines 90-100 and 170-180 — Phase 26 Criterion 1 and Phase 27 Criterion 5.
    - `.planning/phases/31-.../31-CONTEXT.md` § D-13.
  </read_first>
  <action>
    Re-measure before editing. Run the device-family census over `data/dashboard/index.json` the way
    `27-VALIDATION.md` G-02 describes and record the full census and the row total verbatim; read the
    residual count, the 154-cohort size and the scanned-archive figure off the committed
    `26-RESIDUAL.md`. Every number written below must come from one of those two measurements taken in
    this session — never from the audit's own older figure, and never from research's 2026-09-19
    measurement on trust.

    Apply corrections 1-5 from the interfaces table. Each correction replaces the stale figure in the
    body text with the live one, and appends a dated italic note in the project's existing
    "corrected `<date>`" style that: states the previous figure, states the new one, names the source
    record or command it was measured from, and says in one clause why it moved (improved device
    classification for ERA-02; the manifest miscount 31-06 fixed for the scanned-archive figure; the
    cross-plan integration repair already recorded at ROADMAP line 97 for PACE-06). Keep the old
    figure visible INSIDE the note — a reader tracing the audit must be able to find it — but never
    leave it standing as an assertion in the body.

    For correction 5, replace the "pending phase re-verification" clause in PR-03, PR-04 and PR-05 and
    in their three traceability-table rows with a reference to `28-VERIFICATION.md`'s actual outcome:
    verified 2026-09-17T10:15Z, `passed`, 5/5, superseding the earlier `gaps_found` 2/5, with no gaps
    remaining. Do not change any tick state in this task — PR-03/04/05 are already Complete and stay
    Complete; only the stale wording moves.

    Do not edit ROADMAP § Phase 31's own Criterion 5. Do not touch any generated artifact. Do not
    tick or untick any requirement in this task.
  </action>
  <verify>
    <automated>test $(grep -n "716 of 1,864\|716-activity" .planning/REQUIREMENTS.md .planning/ROADMAP.md | grep -vi "corrected" | wc -l) -eq 0 && test $(grep -n "13 of the 154" .planning/REQUIREMENTS.md | grep -vi "corrected" | wc -l) -eq 0 && test $(grep -n "pending phase re-verification" .planning/REQUIREMENTS.md | grep -vi "corrected" | wc -l) -eq 0 && test $(grep -n "1,866 streams scanned" .planning/ROADMAP.md | grep -vi "corrected" | wc -l) -eq 0 && grep -c "Corrected 2026-" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The device-family census and the row total are recorded verbatim in the SUMMARY, with the command used, and the ERA-02 figure written into the documents equals that measurement.
    - The residual figure written into PACE-06 equals the figure in the committed `26-RESIDUAL.md`; both are quoted in the SUMMARY.
    - The scanned-archive figure in ROADMAP Phase 26 Criterion 1 equals the post-fix count and is quoted beside the pre-fix 1,866 in its note.
    - Each of the five corrections carries a dated italic note naming its source record; the five notes are quoted verbatim in the SUMMARY.
    - Every stale string survives ONLY inside a correction note: the four `grep | grep -vi corrected | wc -l` checks in the verify block all return 0.
    - `git diff .planning/ROADMAP.md` shows no change to the Phase 31 section.
    - No requirement's tick state changed: `git diff .planning/REQUIREMENTS.md | grep -E "^[-+]- \[" | wc -l` returns 0.
  </acceptance_criteria>
  <done>Five figures re-measured and corrected in place, each with provenance a later reader can follow.</done>
</task>

<task type="auto">
  <name>Task 2: Close G-01 in the audit, close G-02 in 27-VALIDATION, and flip it to passed</name>
  <files>.planning/v2.2-MILESTONE-AUDIT.md, .planning/phases/27-per-activity-quality-signals/27-VALIDATION.md</files>
  <read_first>
    - `.planning/v2.2-MILESTONE-AUDIT.md` § tech_debt (around line 50-60) — the item list, including
      the G-01 entry and the two docs entries Task 1 just discharged.
    - `.planning/phases/27-per-activity-quality-signals/27-VALIDATION.md` — the frontmatter, the header
      note (lines 14-25) that says G-02 is open and scheduled for Phase 31, the § Gap-Closure Record
      entries for G-01/G-02/G-03, and the retroactive-audit section at the end that states
      `status: partial` is retained until G-02 closes.
    - `.planning/phases/31-.../31-CONTEXT.md` § D-13 (the audit amendment) and § D-14 (the flip, and
      the explicit prohibition on hand-editing another phase's VALIDATION status table beyond this).
    - This plan's Task 1 output — the G-02 figure just measured is what the closure note records.
  </read_first>
  <action>
    In `.planning/v2.2-MILESTONE-AUDIT.md`, amend the tech_debt list in place: mark the 27 G-01 entry
    CLOSED, naming plan 27-11 (2026-09-10) as the fix and 31-08's regeneration as the confirmation
    that the report now reproduces apart from its timestamp and archive growth. Add the same kind of
    dated note to the two documentation entries Task 1 discharged (the PACE-06 "13 of the 154" entry
    and the G-02 "716 of 1,864" entry), pointing at this phase. Do not restructure the audit, do not
    re-run it, and do not change its overall `status`.

    In `27-VALIDATION.md`: update § G-02's "Status: OPEN" to CLOSED with today's date, the corrected
    figure, and the file and line where the correction landed; update the header note so it no longer
    says G-02 remains open and scheduled for Phase 31; update the retroactive-audit section's closing
    sentence, which currently states `status: partial` is retained until G-02 closes, to record the
    flip and its cause. Then set the frontmatter `status: partial` → `status: passed`.
    `nyquist_compliant` and `wave_0_complete` are already `true` and are not touched.

    Change nothing else in `27-VALIDATION.md` — no row verdicts, no Per-Task map entries, no
    requirement dispositions. D-14 authorises exactly this one G-02-gated flip.
  </action>
  <verify>
    <automated>grep -q "^status: passed" .planning/phases/27-per-activity-quality-signals/27-VALIDATION.md && test $(grep -c "G-02" .planning/phases/27-per-activity-quality-signals/27-VALIDATION.md) -ge 1 && test $(grep -n "G-01 (open)" .planning/v2.2-MILESTONE-AUDIT.md | wc -l) -eq 0 && test $(grep -n "remains open and is scheduled for Phase 31" .planning/phases/27-per-activity-quality-signals/27-VALIDATION.md | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep '^status:' .planning/phases/27-per-activity-quality-signals/27-VALIDATION.md` returns `status: passed`.
    - `grep -n "nyquist_compliant\|wave_0_complete" .planning/phases/27-per-activity-quality-signals/27-VALIDATION.md` shows both still `true` and unmodified in `git diff`.
    - § G-02 reads CLOSED, dated today, quoting the corrected figure and naming the file where the correction landed; the text is quoted in the SUMMARY.
    - The audit's G-01 entry reads CLOSED and names plan 27-11 plus 31-08's confirming regeneration; the entry is quoted in the SUMMARY.
    - `git diff .planning/phases/27-per-activity-quality-signals/27-VALIDATION.md` touches only the frontmatter status line, the header note, § G-02 and the retroactive-audit closing sentence — no row verdict, no Per-Task map row, no requirement disposition.
    - `npm test` and `npx tsc --noEmit` exit 0 (nothing here touches code, but the gate is cheap and the phase's record depends on it).
    - The commit message contains no CI-skip token; nothing is pushed.
  </acceptance_criteria>
  <done>G-01 recorded closed where the audit can see it, G-02 closed at its source, and 27's validation record no longer says partial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured value → hand-written prose in a document of record | A number typed by hand into a requirement is indistinguishable from a measured one unless its provenance is written beside it |
| one phase's validation record → the milestone archive | A `status: partial` left behind, or flipped too early, misreports the whole milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | Repudiation | a stale figure standing in a document of record as if measured | mitigate | Every figure is re-measured in this session from `data/dashboard/index.json` or the regenerated `26-RESIDUAL.md`; each correction carries a dated note naming its source record; the old strings survive only inside those notes, asserted by grep |
| T-31-30 | Repudiation | a correction copied from the audit's own older measurement, propagating the error it was meant to fix | mitigate | The action forbids copying research's or the audit's figures; the SUMMARY must quote the census command and its output |
| T-31-31 | Repudiation | flipping `27-VALIDATION.md` to passed before its gating gap actually closed | mitigate | D-14 gates the flip on G-02's closure, which is this same plan's Task 1; the diff is constrained to four locations and asserted by `git diff` scope |
| T-31-32 | Tampering | scope creep into another phase's validation record or into this phase's own success criteria | mitigate | Explicit prohibitions: no re-run of `/gsd-validate-phase`, no other status table edited, ROADMAP § Phase 31 untouched, no tick state changed in Task 1 |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
- The four stale-string greps (excluding correction notes) all return 0.
- `27-VALIDATION.md` frontmatter reads `status: passed`; `nyquist_compliant` and `wave_0_complete` unmodified.
- The audit's G-01 entry reads CLOSED.
- `npm test` and `npx tsc --noEmit` exit 0.
- No tick state changed in Task 1; nothing pushed.
</verification>

<success_criteria>
- Every stale figure the audit names is corrected in place, with provenance.
- The audit no longer lists a closed item as open.
- No v2.2 phase carries a pre-execution or gap-gated validation record into the archive.
</success_criteria>

<output>
Create `.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-09-SUMMARY.md` when done.
Record: the device-family census verbatim with its command, the figures read off `26-RESIDUAL.md`, the
five correction notes verbatim, the amended audit entry, the G-02 closure text, and the exact
`git diff --stat` for all four files.

**Tick rules.** TD-06 MAY be ticked by this plan — its remaining obligation was exactly the G-02
correction plus the `27-VALIDATION.md` flip, both landed here, and `26`/`29` were already backfilled
before planning. TD-05 must NOT be ticked here: it ticks in 31-10, after the PR-04 Round 4 verdict.
</output>
