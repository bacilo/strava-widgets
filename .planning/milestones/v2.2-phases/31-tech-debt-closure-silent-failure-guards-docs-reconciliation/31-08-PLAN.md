---
phase: 31-tech-debt-closure-silent-failure-guards-docs-reconciliation
plan: 08
type: execute
wave: 2
depends_on: ["31-05", "31-06", "31-07"]
files_modified:
  - .planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-RESIDUAL.md
  - .planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md
  - .planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md
  - .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md
  - .planning/phases/30-elevation-quality-signal/30-CALIBRATION.md
  - data/best-effort-ceiling.json
autonomous: true
requirements: [TD-05]

must_haves:
  decisions: [D-11]
  truths:
    - "D-11: all five artifacts of record are regenerated against the merged archive by their own generators, with the three generator defects already fixed"
    - "D-11: each artifact is regenerated twice and the second run is byte-identical to the first once the **Generated:** line is stripped — the project's prose-corrections-revert lesson, proven rather than asserted"
    - "TD-04's reason format is in data/stats/best-efforts.json before 28-DIFF.md is generated, so the signed diff carries the final wording"
    - "The three-way ceiling count (diff = recount byGuard.ceiling = independentCeilingCount) is re-derived and recorded before any human is asked for a verdict"
    - "data/best-effort-ceiling.json is committed only if git says it actually changed"
  artifacts:
    - path: ".planning/phases/28-pr-plausibility-ceiling/28-DIFF.md"
      provides: "the regenerated diff the PR-04 Round 4 sign-off will be bound to"
      contains: "Generated:"
    - path: ".planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md"
      provides: "the calibration report with WR-07's derived drift sentence and WR-08's reconciled columns"
      contains: "owner-excluded"
  key_links:
    - from: "src/analytics/best-effort-ceiling.ts's TD-04 reason format"
      to: "28-DIFF.md's per-effort reason text"
      via: "data/stats/best-efforts.json regenerated first, then compute-pr-ceiling-diff.mjs"
      pattern: "by [0-9]+\\.[0-9]{3} m/s"
---

<objective>
Regenerate the five artifacts of record against the merged 1,899-activity archive, in dependency
order, twice each, and prove the second run is byte-identical apart from its `**Generated:**` line
(TD-05, D-11).

Purpose: three generators were wrong (26 G-03, 28 WR-07, 28 WR-08) and one output format changed
(TD-04). Until these run, every figure the milestone's records cite is either stale or generated by
code that has since been fixed. Proving idempotence is what stops the next run silently reverting
this work.

Output: five regenerated artifacts, an idempotence proof for each, the new `28-DIFF.md` sha256, and
the three-way ceiling reconciliation — the material the Round 4 sign-off will be presented against.

**Run on the primary checkout.** Every command here reads `data/streams/`, `data/activities/` and
`data/stats/`, which are gitignored and absent from bare worktrees. Do NOT push; the orchestrator
pushes. Never put the literal CI-skip token in a commit message — quoting it anywhere in a commit
body silently suppresses the whole daily-refresh run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-CONTEXT.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-RESEARCH.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md
@.planning/v2.2-MILESTONE-AUDIT.md

<interfaces>
<!-- Established facts and the exact ordering constraints. Do not rediscover. -->

| fact | value | source |
|---|---|---|
| the five artifacts | `26-RESIDUAL.md`, `27-CALIBRATION.md`, `28-CEILING-CALIBRATION.md`, `28-DIFF.md`, `30-CALIBRATION.md` — all under `.planning/phases/`, never under `data/` | RESEARCH |
| their generators | `compute-pace-residual`, `compute-pace-quality-calibration`, `compute-pr-ceiling-calibration`, `compute-pr-ceiling-diff`, `compute-elevation-calibration` (each npm script runs `npm run build` first) | package.json |
| **hard ordering 1** | `compute-pace-quality-calibration.mjs:646` runs `execSync('npm run compute-pace-residual')` inside its own run. Run the residual generator FIRST in its own step, then the pace-quality calibration, then run the residual generator ONCE MORE so the committed `26-RESIDUAL.md` is unambiguously its own generator's last output rather than a side effect | RESEARCH Pitfall 3 |
| **hard ordering 2** | TD-04's reason format must already be in `data/stats/best-efforts.json` before `compute-pr-ceiling-diff.mjs` runs, or the signed diff carries the old wording and needs a second sign-off | RESEARCH, D-12 |
| **the idempotence trap** | every artifact opens with a `**Generated:** <ISO timestamp>` line; a naive byte-comparison of two runs ALWAYS fails. Strip that line from both before comparing and say so in the proof | RESEARCH Pitfall 2 |
| the current signed hash | `28-DIFF.md` sha256 `cdf9d65499d7ac62123ecc33d1e298ae08a0d07f6740ba7bdcc4cf5fa365b8dd` (Round 3, 2026-09-19) | `28-VALIDATION.md` |
| Round 3 baseline figures | 1,899 activities / 1,874 considered; ceiling-only demoted 32; flag flips 13; retroactive promotions 2; ranking rows moved 49; `totals.effortsDemoted` 66; flagged activities 47; three-way ceiling 32 = 32 = 32 | `28-VALIDATION.md` § PR-04 Sign-off (Round 3) |
| `data/best-effort-ceiling.json` | tracked, holds only numbers (no reason strings), rewritten by a local `compute-all-stats`; commit it ONLY if `git status --porcelain` shows it changed. Do not propose WR-06's write gate — out of scope | RESEARCH Open Question 1 |
| the signed-artifact rule | nothing is ever hand-written into a generated artifact; the sign-off lives outside it, bound to a sha256 (Phase 28 D-14) | 28-CONTEXT.md |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate all five artifacts in dependency order and prove idempotence</name>
  <files>.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-RESIDUAL.md, .planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md, .planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md, .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md, .planning/phases/30-elevation-quality-signal/30-CALIBRATION.md</files>
  <read_first>
    - `.planning/phases/31-.../31-RESEARCH.md` § Common Pitfalls 2 and 3 — the timestamp-stripping
      idiom and the subprocess ordering hazard; both are load-bearing for this task.
    - `.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md` § PR-04 Sign-off (Round 3) — the
      baseline figures every regenerated number is compared against.
    - `src/analytics/compute-best-efforts.test.ts`'s "byte-identical apart from generatedAt" test —
      the project's own idempotence idiom this proof mirrors.
    - `package.json` scripts — each `compute-*` script runs `npm run build` first; know what each one
      writes before running it.
    - The three predecessor summaries, `31-05-SUMMARY.md`, `31-06-SUMMARY.md`, `31-07-SUMMARY.md` —
      confirm all three generator/format fixes actually landed before regenerating anything.
  </read_first>
  <action>
    Confirm the tree is clean and the three predecessor plans are merged. If any of 31-05, 31-06 or
    31-07 is missing, stop — regenerating early wastes a human sign-off cycle, which is the specific
    thing D-12 exists to avoid.

    Refresh the computed data first, so TD-04's reason format is present in the inputs the reporters
    read: `npm run build` then `npm run compute-all-stats` (or, if narrower is provably sufficient,
    `compute-best-efforts` followed by `compute-dashboard-index` — state which you ran and why).
    Record `git status --porcelain data/` afterwards.

    Then regenerate in this exact order, capturing each command's stdout verbatim:
      1. `npm run compute-pace-residual`
      2. `npm run compute-pace-quality-calibration` (note in the log that this re-runs the residual
         generator as a subprocess — that is expected, not a stray write)
      3. `npm run compute-pace-residual` again, so the committed `26-RESIDUAL.md` is its own
         generator's last output
      4. `npm run compute-pr-ceiling-calibration`
      5. `npm run compute-pr-ceiling-diff`
      6. `npm run compute-elevation-calibration`

    Then prove idempotence for each of the five artifacts: copy the current file aside, run its own
    generator once more, and compare the two with the `**Generated:**` line stripped from both
    (e.g. `diff <(grep -v '^\*\*Generated:\*\*' A) <(grep -v '^\*\*Generated:\*\*' B)`). Every diff
    must be empty. State explicitly in the proof that the timestamp line is excluded and why — a bare
    byte-comparison would fail on day one for the wrong reason. Respect the same subprocess ordering
    when re-running the pace-quality calibration.

    If any diff is non-empty, do NOT hand-edit the artifact. Record the differing hunk verbatim, stop,
    and report it as a gap — a non-idempotent generator is a defect in the generator, and hand-fixing
    its output is precisely the failure mode this phase exists to close.

    Compare every regenerated headline figure against the Round 3 baseline in the interfaces table and
    record each as unchanged or changed-with-cause. Expected content changes: TD-04's reason format on
    every ceiling row, the residual manifest off-by-one, and WR-07/WR-08's prose and columns. Any
    other movement must be explained by archive drift with a number, not waved through.
  </action>
  <verify>
    <automated>for f in .planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-RESIDUAL.md .planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md .planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md .planning/phases/30-elevation-quality-signal/30-CALIBRATION.md; do test -s "$f" || exit 1; grep -q '^\*\*Generated:\*\*' "$f" || exit 1; done; grep -qE "by [0-9]+\.[0-9]{3} m/s" .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md && ! grep -q "400m shows the largest drift" .planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md && npm test 2>&1 | tee /tmp/31-08-t1.log; ! grep -qE "[1-9][0-9]* failed" /tmp/31-08-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - All five artifacts are regenerated; each command's stdout is recorded verbatim in the SUMMARY.
    - Five idempotence diffs, each empty, each performed with the `**Generated:**` line stripped from both sides; the exact diff command used is recorded once.
    - `26-RESIDUAL.md`'s final content is the output of run 3 (its own generator), not a subprocess side effect; state the ordering actually executed.
    - `28-DIFF.md` contains at least one `by <3dp> m/s` margin clause — TD-04's format reached the diff.
    - `28-CEILING-CALIBRATION.md` contains no `400m shows the largest drift` literal and does carry the owner-excluded reconciliation.
    - Every headline figure is tabulated against the Round 3 baseline (1,899 / 1,874 / 32 / 13 / 2 / 49 / 66 / 47) as unchanged or changed-with-cause.
    - `npm test` exits 0.
    - No artifact was hand-edited: `git diff` for the five files shows only generator-shaped changes, and the SUMMARY states that no manual edit was made.
  </acceptance_criteria>
  <done>Five artifacts regenerated in the right order, each proven to reproduce itself.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive the three-way ceiling count, hash the diff, and commit</name>
  <files>data/best-effort-ceiling.json, .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md</files>
  <read_first>
    - `.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md` § PR-04 Sign-off (Round 3) — the
      three-way reconciliation's exact form and the note about committing
      `data/best-effort-ceiling.json` alongside the record.
    - `.planning/phases/31-.../31-RESEARCH.md` § Open Questions 1 — the `git status --porcelain`
      decision rule for that file.
    - `.planning/phases/31-.../31-VALIDATION.md` § Sampling Rate — the pre-verify gate list this task
      runs.
    - `~/.claude/projects/-Users-pedf-workspace-strava-widgets/memory/` entries on the CI-skip token
      and on CI auto-commit push races.
  </read_first>
  <action>
    Run the phase gate and record every command's verbatim output: `npm test`, `npx tsc --noEmit`,
    `npm run build-widgets` (grep its output for `css-syntax-error`, zero tolerance, and for any
    `replaced stale` line from 31-02's new log), `npm run verify-dashboard`,
    `node scripts/compute-pr-ceiling-recount.mjs` against the real exclusions file,
    `node scripts/compute-elevation-recount.mjs`, and
    `node scripts/compute-pace-quality-recount.mjs --expect 299`.

    Derive the three-way ceiling figure three independent ways and record all three: `28-DIFF.md`'s
    ceiling-only demoted count, the recount's `byGuard.ceiling`, and the recount's
    `independentCeilingCount`. They must be equal. If they are not, stop and record the disagreement
    as a gap; do not proceed to a sign-off against numbers that do not reconcile. Also record the
    recount's flagged-activity count and its `--expect-demoted` verdict.

    Compute and record `shasum -a 256` of the regenerated `28-DIFF.md`, and state the Round 3 hash
    `cdf9d65499d7ac62123ecc33d1e298ae08a0d07f6740ba7bdcc4cf5fa365b8dd` beside it as the version being
    superseded. Do NOT write the hash or any sign-off text into `28-DIFF.md` — the artifact stays
    purely generated (Phase 28 D-14). The record goes into `28-VALIDATION.md`, which plan 31-10 owns.

    Check `git status --porcelain data/best-effort-ceiling.json`. If it is unchanged, say so and do
    not touch it. If it changed, diff it, state whether any `ceilingMps`/`p90Mps`/`populationN` value
    moved or only a timestamp did, and commit it alongside the artifacts following the Round 3
    precedent. Revert any other incidental `data/` churn (e.g. a timestamp-only
    `data/geo/geo-metadata.json`) with `git checkout --`, exactly as plan 28-14 did, rather than
    committing it.

    Commit the five artifacts (plus `data/best-effort-ceiling.json` if and only if it changed) in one
    commit. The commit message must NOT contain the CI-skip token in any form. Do not push. If a
    later push races with the nightly CI auto-commit, that is the orchestrator's problem and is
    resolved with `git merge origin/master`, never a rebase.
  </action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/31-08-t2a.log; ! grep -qE "[1-9][0-9]* failed" /tmp/31-08-t2a.log && npx tsc --noEmit && node scripts/compute-pr-ceiling-recount.mjs && node scripts/compute-elevation-recount.mjs && node scripts/compute-pace-quality-recount.mjs --expect 299 && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - Every gate command exits 0 and its output is recorded verbatim; `build-widgets` emits zero `css-syntax-error` occurrences, and any `replaced stale` line is quoted and explained.
    - The three-way ceiling figure is recorded three times from three sources and they are equal; the flagged-activity count and the `--expect-demoted` verdict are recorded.
    - The new `28-DIFF.md` sha256 is recorded, with the superseded Round 3 hash beside it.
    - `grep -c "cdf9d654" .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md` returns 0 — no sign-off text was written into the generated artifact.
    - The `data/best-effort-ceiling.json` decision is stated explicitly: unchanged and untouched, or changed with the numeric diff described and committed.
    - `git status --porcelain data/` after the commit shows no unintended tracked churn; any reverted file is named.
    - The commit message does not contain the CI-skip token; `git log -1 --format=%B` is quoted in the SUMMARY.
    - Nothing was pushed: `git status -sb` shows the branch ahead of origin, and the SUMMARY says so.
  </acceptance_criteria>
  <done>The artifacts are committed, the numbers reconcile three ways, and the diff has a hash to sign.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local archive (`data/`) → generator → committed artifact of record → human sign-off | The whole evidentiary chain for PR-04; a wrong or stale input here becomes a signed claim |
| local commit → origin/master → nightly CI deploy | A commit body can suppress the nightly run; a push can race the CI auto-commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Repudiation | a hand-corrected generated artifact that reverts on the next run | mitigate | Nothing is hand-edited; five idempotence proofs with the `**Generated:**` line stripped, and a non-empty diff is recorded as a gap rather than patched |
| T-31-26 | Tampering | a regeneration performed out of dependency order, so the committed `26-RESIDUAL.md` is a subprocess side effect or `28-DIFF.md` carries the pre-TD-04 wording | mitigate | The six-step ordering is fixed in the action, the residual generator runs last for its own file, and the diff is asserted to contain a 3-dp margin clause |
| T-31-27 | Repudiation | a sign-off bound to a hash that does not match the committed bytes | mitigate | The sha256 is computed from the committed file and recorded beside the superseded Round 3 hash; nothing is written into the artifact itself (Phase 28 D-14) |
| T-31-28 | Tampering | incidental `data/` churn committed as if it were a deliverable | mitigate | `git status --porcelain data/` is inspected; only `data/best-effort-ceiling.json` may be committed and only if it changed; anything else is reverted with `git checkout --`, per plan 28-14's precedent |
| T-31-29 | Denial of Service | a commit body containing the CI-skip token silently suppressing the nightly deploy | mitigate | Explicitly forbidden; the commit message is quoted in the SUMMARY for inspection |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
- Five artifacts regenerated; five empty idempotence diffs with the timestamp line stripped.
- `npm test`, `npx tsc --noEmit`, `npm run build-widgets` (zero `css-syntax-error`), `npm run verify-dashboard`, and all three recount scripts exit 0.
- Three-way ceiling figure equal across `28-DIFF.md`, `byGuard.ceiling` and `independentCeilingCount`.
- New `28-DIFF.md` sha256 recorded; Round 3 hash named as superseded.
- Nothing pushed; no CI-skip token in the commit body.
</verification>

<success_criteria>
- Every artifact of record is the output of a fixed generator run against the merged archive.
- Each reproduces itself byte-for-byte apart from its timestamp line.
- The material a human needs for the Round 4 sign-off exists and reconciles.
</success_criteria>

<output>
Create `.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-08-SUMMARY.md` when done.
Record: the exact regeneration order executed, all six stdout captures, the five idempotence diffs and
the command used, the headline-figure comparison against the Round 3 baseline, the three-way ceiling
figures, the new sha256, the `data/best-effort-ceiling.json` decision, and the commit message.

**TD-05 tick rule:** do NOT tick TD-05 here. It ticks only in 31-10, after the PR-04 Round 4 verdict.
</output>
