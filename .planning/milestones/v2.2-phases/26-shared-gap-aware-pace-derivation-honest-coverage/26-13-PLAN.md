---
phase: 26-shared-gap-aware-pace-derivation-honest-coverage
plan: 13
type: execute
wave: 9
depends_on: [26-12]
files_modified:
  - .planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements: [COV-01, COV-02]

must_haves:
  decisions: [D-06, D-08]
  truths:
    - "The coverage caption for activity 11865310195 — the activity that rendered no Pace Distribution section at all — is read in the browser at the moment of observation and reconciled against a hand sum taken straight from its committed stream file, not asserted from a unit test."
    - "The pinned exemplar 4556693525 still renders BOTH its caption and its histogram bars, so the fix is confirmed not to have inverted into caption-always/bars-never."
    - "The HR half of the breakdown section still renders nothing for an activity with no HR stream, now that the section itself renders where it previously did not (D-31)."
    - "COV-01 and COV-02 are closed in REQUIREMENTS.md only after the browser session records a PASS on every row — never before, per this project's verification-after-requirement-tick lesson."
  artifacts:
    - path: ".planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VALIDATION.md"
      provides: "Round 2 checkpoint with per-row pre-stated expectations, both-direction discriminators, and recorded verdicts"
      contains: "Round 2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "COV-01 and COV-02 closed with their disposition stated"
      contains: "COV-02"
  key_links:
    - from: "the served build"
      to: "data/streams/11865310195.json and data/dashboard/index.json"
      via: "a hard-reloaded production-shaped URL, verified by curl against the SERVED path rather than the repo file"
      pattern: "hard reload"
---

<objective>
Confirm the CR-01 fix on screen, and close COV-01 and COV-02.

Purpose: `26-VERIFICATION.md` found the blocking gap by code-path tracing and a live archive scan,
not by browser observation — Round 1's checkpoint could not have caught it because
`11865310195` was never one of its rows. Roadmap Criterion 3 requires a coverage percentage to be
*read in the browser at the moment of observation* and reconciled against a hand sum from the
committed stream file; the activity that most needs that reading is the one that rendered nothing.
This project has shipped rendering defects behind a fully green automated gate three times, and
plan 26-12's tests run in a node environment that cannot invoke a DOM builder at all — so the
rendered output is genuinely unverified until a human looks.

COV-01 (held open pending WR-01's adjudication, answered by plan 26-11) and COV-02 (reopened by
the verification) are then closed as the LAST action, gated on the session's verdicts.

Output: a recorded Round 2 checkpoint in `26-VALIDATION.md` and two closed requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VALIDATION.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-UI-SPEC.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VERIFICATION.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-11-SUMMARY.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-12-SUMMARY.md
</context>

<interfaces>
<!-- Facts measured by the planner against the live tree on 2026-09-09. The executor must
     re-derive every number independently (the archive grows nightly) but should not need
     to discover the shape of the problem. -->

**The subject activity is reachable and every render input is total.** `data/activities/11865310195.json`
and `data/streams/11865310195.json` both exist; the live `data/dashboard/index.json` row reads
`{"id":"11865310195","distanceM":0,"movingTimeSec":15,"paceSecPerKm":null,"streams":{"available":true,"hr":false,...},"prCount":0,"paceDisagreement":null}`.
Executed directly at HEAD: `computeSplits` → `[]`, `computePaceDistribution` → `[]`,
`computeHrZoneTimes` → `null`, none throws. Detail route: `#/activity/11865310195`.

**Pre-fix behaviour, so the rows are provably failable.** At HEAD before plan 26-12,
`buildBreakdownSection([], coverage, null)` returns `null` for this activity — no section, no
heading, no caption. Rows R2-1 and R2-2 therefore fail against pre-fix code and pass against
post-fix code; both directions are demonstrated, not assumed.

**Independent derivation inputs (hand-sum method, must NOT import `src/analytics/pace-derivation.ts`).**
`data/streams/11865310195.json` carries `t = [0, 1, 2, 14, 17, 18]`, `d = [0, 0, 0, 0, 0, 0]`.
Applying the classification rule as documented in `pace-derivation.ts`'s comments (read, not
imported): a segment is `recording-gap` when `dt > 10`; else `pause` when it belongs to a maximal
distance-flat run whose duration exceeds `5 × p90(advanceIntervals)`; else `covered`. Here
`advanceIntervals` is empty (`d` never advances), so the pause threshold is `Infinity` and nothing
is a pause. Segment `2 → 14` is `dt = 12 > 10` → recording gap. Remaining segments
`1 + 1 + 3 + 1 = 6` seconds are covered. `spanSec = t[5] - t[0] = 18` (D-06: the stream's own
span, never metadata `elapsed_time`). Sum identity: `6 + 12 + 0 = 18`. Rounded: **33% / 67% / 0%**.
Unbucketed covered time: `6` seconds → `formatEffortDuration(6)` → `0:06`.

**Round 1's convention this round must match** (`26-VALIDATION.md` § "Round 1 Checkpoint (R1-R6)"):
served at `http://127.0.0.1:4173/strava-widgets/` via `npm run curate`
(`nohup npm run curate > /tmp/gsd-26-serve.log 2>&1 &`), hard reload before every row, viewport
clamped to the 500-941 px band, every row carrying a pre-stated expected value derived *before*
observation plus an explicit failing-observation description and a both-direction satisfiability
note, and a verdict table filled in verbatim from the developer's quotations.

**REQUIREMENTS.md live state, read on 2026-09-09 (do not trust the verifier's older snapshot —
commit `73ab8cbf` already repaired most of the drift):** `PACE-01` … `PACE-07` and `ERA-03` are all
`[x]` / `Complete`. Only two lines remain open:
- checklist line ~41: `- [ ] **COV-01**: For any derived pace series, covered time plus excluded time — itemised by named exclusion category — sums exactly …`
- checklist line ~42: `- [ ] **COV-02**: Coverage is visible to the reader wherever a derived distribution is shown …`
- traceability table line ~132: `| COV-01 | Phase 26 | Pending (exactness verified; held open pending WR-01 adjudication, see 26-REVIEW.md) |`
- traceability table line ~133: `| COV-02 | Phase 26 | Pending (gaps_found — caption gated behind buckets.length, see 26-VERIFICATION.md) |`

**Out of scope, deliberately:** `F-26-01` (the Moving Time stat tile carrying no badge for the same
corrupted `moving_time` the Pace tile discloses) is correctly deferred to Phase 27 under QUAL-01's
"elapsed-vs-moving divergence" signal. Do not add a row for it and do not patch it here.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build, serve, and pre-derive every Round 2 expectation independently</name>
  <files>.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VALIDATION.md` — the whole file, especially § "Round 1 Checkpoint (R1-R6)" (the exact section shape, the pre-session gate table, the served-build block, the setup instructions, and Row 1's independent-derivation paragraph) and § "Demonstrated-Failing Cases (Nyquist negative staging)" (the eight-row table this task extends).
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-11-SUMMARY.md` and `26-12-SUMMARY.md` — the verbatim RED transcripts, the measured residue figures, and whether 26-12's second note branch was included.
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VERIFICATION.md` § "Human Verification Required" — the recommendation this round answers.
    - `src/dashboard/views/detail-sections.ts` — `coverageCaptionText` and `breakdownSectionPlan` as landed, to confirm the note's exact rendered copy before pre-stating it.
    - `scripts/curate-server.mjs` and `package.json` scripts — the serve command and port Round 1 used.
  </read_first>
  <action>
Run the full pre-session gate, in this order, and record each exit code in a table exactly as
Round 1 did: `npm run test`, `npx tsc --noEmit`, `npm run build`,
`npm run compute-dashboard-index`, `npm run build-widgets`, `npm run verify-dashboard`. All must
exit 0 before a human is asked to look at anything. Also run `git status --porcelain data/streams`
and record that it is empty (the archive must be byte-identical to what the hand derivation reads).

Serve the built output at the same production-shaped URL Round 1 used and **defeat the staged-build
cache trap explicitly**: `curl` the SERVED paths, not the repo files, and record the results —
`http://127.0.0.1:4173/strava-widgets/data/streams/11865310195.json` must return the six-sample
`t`/`d` arrays quoted above, and the served `data/dashboard/index.json` must carry that activity's
row with `"available": true` and `"hr": false`. A stale staged `index.html`/`index.json` has
produced a false result on this project before; pointing at `127.0.0.1` alone is not sufficient.

**Derive R2-1, R2-2 and R2-3's expected values with a throwaway script that does NOT import
`src/analytics/pace-derivation.ts` or anything under `dist/analytics/`** — reimplement the
documented classification rule from the comments, exactly as Round 1's Row 1 derivation did. An
expectation produced by the code under test would make the row vacuous (this project's
checkpoint rows have shipped as vacuous before). Record the derivation method and its intermediate
numbers (`n`, `spanSec`, `coveredSec`, `recordingGapSec`, `pauseSec`, and the sum identity) in the
Round 2 section, per row.

Before writing any row, run a totality check so no row is unsatisfiable for an unrelated reason:
confirm in a scratch vitest run that `computeSplits`, `computePaceDistribution`,
`computeHrZoneTimes` and `breakdownSectionPlan` are all total for `11865310195`'s committed stream
(none throws). If any of them throws, the detail view will not render at all and R2-1/R2-2 are
unpassable — stop and report that instead of handing an unsatisfiable row to a human.

Write a `## Round 2 Checkpoint (R2-1..R2-4)` section into `26-VALIDATION.md`, matching Round 1's
structure, with these four rows. Each row states: where, what to read back, the independent
derivation method and its result, the pre-stated expected value verbatim, an explicit PASS
condition, an explicit FAIL condition, and a both-direction satisfiability note.

- **R2-1 — D-08 / COV-02 / CR-01 — the caption that previously did not render at all, activity
  `11865310195`.** Where: `#/activity/11865310195`, the `Pace Distribution` section. Expected
  caption verbatim: `33% of elapsed time covered · 67% recording gaps · 0% paused`, reconciled
  against the hand sum (span 18, covered 6, recording gap 12, `6 + 12 + 0 = 18`). PASS: the section
  and its caption are present and the three percentages equal the hand-derived 33/67/0 (or
  disagree by no more than one point of rounding). FAIL: no `Pace Distribution` section renders,
  the caption is absent, a named category is missing, or a percentage disagrees by more than one
  rounding point. Both-direction note: this row **fails against pre-fix code** — record that
  `buildBreakdownSection` returned `null` for this activity at HEAD before 26-12, so the row's
  failing observation is demonstrated rather than hypothetical; it passes only if the fix works.

- **R2-2 — the honest note and the absence of bars, same activity.** What to read back: the
  `.text-label` line where the histogram bars would be, and whether any bar row is present.
  Expected verbatim: `No pace buckets — 0:06 of covered time produced no derivable pace.` The
  `0:06` is independently derived (18 second span minus the 12 second recording gap = 6 seconds of
  covered time), not read off the code. PASS: the note reads exactly as pre-stated **and** zero
  histogram bar rows are present. FAIL: the note is absent, its duration is anything other than
  `0:06`, or any bar row renders. Both-direction note: a fix that fell back to the stream span
  rather than the covered seconds would render `0:18` — an observably different, distinguishable
  failure, so the number tests reachable extent against an independent value rather than internal
  agreement. If 26-12's SUMMARY records that the note copy shipped differently, quote the shipped
  string here instead and say so — never pre-state a string the code does not produce.

- **R2-3 — no-regression: the pinned exemplar `4556693525` still renders BOTH caption and bars.**
  Where: `#/activity/4556693525`, the `Pace Distribution` section. Re-run Round 1's hand
  derivation fresh (n=1682, `spanSec = t[1681] - t[0] = 3394`, covered 3363, recording gap 31,
  pause 0). Expected caption verbatim: `99% of elapsed time covered · 1% recording gaps · 0% paused`,
  **and** at least one histogram bar row visible, whose label the human quotes (a
  `m:ss–m:ss/km` bucket label with its minutes/percentage value). PASS: caption matches the hand
  sum and at least one bar row is quoted. FAIL: the caption is absent or disagrees by more than one
  rounding point, **or** no bar rows render. Both-direction note: this is the inversion guard — a
  fix that made the caption unconditional but dropped the bars would pass R2-1 and R2-2 and fail
  here, which is the only row that can catch that specific regression.

- **R2-4 — D-31 preserved now that the section renders where it did not before, activity
  `11865310195`.** Independent input: the SERVED index row's `"hr": false`, confirmed by the curl
  above. Expected: **no** `Heart Rate Zones` heading anywhere on that detail page, no empty panel
  shell, and no "no HR data"-style copy. PASS: the breakdown card contains the pace heading,
  caption and note only. FAIL: a `Heart Rate Zones` heading, an empty zone panel, or any
  placeholder copy appears. Both-direction note: passable (D-31's behaviour at HEAD) and newly
  failable (the card itself did not render for this activity before 26-12, so an empty HR shell
  becomes reachable here for the first time).

Leave the verdict column empty, ready for Task 2 to fill.

Finally, extend `26-VALIDATION.md`'s "Demonstrated-Failing Cases" table with two rows recording
what 26-11 and 26-12 actually watched failing: (9) the documented
`sum(timeSec) === coverage.coveredSec` invariant asserted against the zero-net-advance
`covered` segment flanked by two recording gaps — observed failing `0` against `2` synthetically
and `0` against `6` on activity `11865310195` (Crit 3 · COV-01); and (10) the coverage caption
gated behind `buckets.length` — `buildBreakdownSection([], coverage, null)` returning `null` for
an activity with 33% covered / 67% recording gaps (Crit 3 · COV-02). Quote the verbatim failure
lines from the two SUMMARYs.
  </action>
  <verify>
    <automated>npm run test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build-widgets &amp;&amp; npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - All six pre-session gate commands exit 0 and each exit code is recorded in the Round 2 section; `git status --porcelain data/streams` is empty.
    - `curl -s http://127.0.0.1:4173/strava-widgets/data/streams/11865310195.json` returns `"t":[0,1,2,14,17,18]` — the SERVED file, proving no stale staged copy is in play; the command and its output are recorded.
    - The served `data/dashboard/index.json` row for `11865310195` carries `"available": true` and `"hr": false`; both quoted in the section.
    - `26-VALIDATION.md` contains a `Round 2 Checkpoint` section with exactly four rows, each carrying a pre-stated expected value, an explicit FAIL condition, and a both-direction satisfiability note.
    - R2-1, R2-2 and R2-3's expected values were produced by a script that does not import `pace-derivation.ts` or `dist/analytics/`, and the section states that derivation method with its intermediate numbers and its sum identity.
    - The section records that R2-1 fails against pre-fix code (`buildBreakdownSection` returned `null` at HEAD before 26-12) — so the row is demonstrably failable, not vacuous.
    - The totality check for `11865310195` is recorded as passing (no throw from `computeSplits`, `computePaceDistribution`, `computeHrZoneTimes`, `breakdownSectionPlan`) — so no row is unsatisfiable for an unrelated reason.
    - The "Demonstrated-Failing Cases" table has two new rows (9) and (10) with verbatim failure quotations.
    - `.planning/REQUIREMENTS.md` is NOT modified by this task — `git status --porcelain .planning/REQUIREMENTS.md` is empty.
  </acceptance_criteria>
  <done>The build is green and served from a verified-fresh path, and every Round 2 row carries an independently derived expectation, a stated failing observation, and a both-direction reachability note.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human browser checkpoint — the caption that used to render nothing</name>
  <files>.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VALIDATION.md` — the Round 2 section prepared in Task 1, with its four pre-stated expectations, and the Round 1 verdict table for the recording format to mirror.
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VERIFICATION.md` § "Gaps Summary" — what this session is confirming closed.
  </read_first>
  <action>
Pause execution and hand the prepared Round 2 checkpoint to the developer. Present the served URL,
the hard-reload instruction, the 500-941 px viewport band, and the four rows with their pre-stated
expected values from Task 1.

Do not observe on the developer's behalf and do not infer any verdict from an automated check —
Criterion 3 requires the coverage percentage to be read in the browser at the moment of
observation, and plan 26-12's tests cannot render anything at all (node environment, no DOM). Keep
every row to what a reader can observe on load: no gestures, no drag, no zoom interactions.

Transcribe the developer's per-row verdict and verbatim quotation into `26-VALIDATION.md`'s Round 2
verdict table exactly as given. Apply no code patch during the session — if a row FAILs, record the
failure verbatim with the developer's quotation and stop; a failing row means the gap is not
closed, and Task 3 must not run.
  </action>
  <what-built>
    The `Pace Distribution` section's heading and coverage caption were decoupled from histogram
    bucket presence, so an activity whose coverage is well-defined renders its disclosure even when
    the pace histogram is empty — with a short note naming the covered time that produced no
    derivable pace. Activity `11865310195` (18 second span, 33% covered, 67% recording gap)
    previously rendered no Pace Distribution section at all. The HR half is unchanged: it still
    renders nothing when the activity has no HR stream.
  </what-built>
  <how-to-verify>
    Before starting: **hard reload** the page (Cmd+Shift+R, or disable cache in DevTools). Staged
    builds in this project have served a stale `index.html` and `index.json` before; pointing at
    `127.0.0.1` alone is not enough and has produced a false result here. Keep the viewport within
    the 500-941 px band.

    1. **R2-1** — open `http://127.0.0.1:4173/strava-widgets/#/activity/11865310195` and scroll to
       `Pace Distribution`. First: does the section exist at all? Then quote the caption line under
       the heading verbatim and compare its three percentages against the hand-derived values
       stated in the Round 2 section.
    2. **R2-2** — in that same section, quote the note line where bars would be, verbatim, and say
       whether any histogram bar rows are present.
    3. **R2-4** — still on that page, say whether a `Heart Rate Zones` heading, an empty zone
       panel, or any "no HR data"-style copy appears anywhere.
    4. **R2-3** — hard reload, open `#/activity/4556693525`, scroll to `Pace Distribution`. Quote
       the caption verbatim AND quote one histogram bar's label and value, confirming the bars
       still render.

    For each row, report PASS or FAIL against the pre-stated expectation and include the verbatim
    text you read. If something differs from the expectation in a way the row does not describe,
    report it as its own finding rather than forcing it into a PASS or a FAIL.
  </how-to-verify>
  <resume-signal>Reply with a verdict and a verbatim quotation per row (R2-1, R2-2, R2-3, R2-4), or describe what you saw instead.</resume-signal>
  <verify>
    <human-check>Four Round 2 rows recorded in 26-VALIDATION.md, each with a verdict and a verbatim on-screen quotation</human-check>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VALIDATION.md','utf8'); const i=s.indexOf('Round 2'); if(i&lt;0){console.error('no Round 2 section');process.exit(1)} const m=s.slice(i).match(/PASS|FAIL|BLOCKED|NOT EXERCISABLE/g)||[]; if(m.length&lt;4){console.error('fewer than four recorded Round 2 verdicts: '+m.length);process.exit(1)} console.log('verdicts recorded: '+m.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `26-VALIDATION.md`'s Round 2 verdict table carries one row per checkpoint row, each with a verdict and the developer's verbatim quotation — no row left blank, no verdict inferred from an automated check.
    - R2-1's recorded quotation is the caption text as read on screen and the entry states that it was reconciled against the hand-derived 33/67/0.
    - Every quotation in the table came from the developer's message; the summary states that explicitly.
    - If any row is FAIL, `26-VALIDATION.md` records it verbatim, Task 3 does not run, and the SUMMARY states the phase gap remains open.
    - No source file is modified during this task (`git status --porcelain src` is empty).
  </acceptance_criteria>
  <done>Round 2's four rows carry human-supplied verdicts and verbatim quotations recorded in `26-VALIDATION.md`.</done>
</task>

<task type="auto">
  <name>Task 3: Close COV-01 and COV-02 in REQUIREMENTS.md — gated on every row passing</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the `### Honest coverage (COV)` checklist block (lines ~41-42) and the traceability table rows for `COV-01` and `COV-02` (lines ~132-133). Read the live file; commit `73ab8cbf` already repaired the `PACE-06`/`ERA-03` drift the verification reported, so only these two lines should still be open. If any other Phase 26 requirement is not `Complete`, report it rather than silently ticking it.
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VALIDATION.md` — the Round 2 verdict table filled in by Task 2.
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-11-SUMMARY.md` — COV-01's adjudication evidence (the identity, the archive sweep result).
  </read_first>
  <action>
**Run only if every Round 2 row recorded PASS.** If any row is FAIL or was left unrecorded, do not
edit `REQUIREMENTS.md` at all — state in the SUMMARY that the requirements stay open and why. This
project ticked three requirements before verification during v2.1 and had to revert them; the
lesson is recorded in STATE.md and this gate is that lesson applied.

With every row passing, make exactly four edits:

- Checklist `- [ ] **COV-01**` → `- [x] **COV-01**`, leaving the requirement's own text untouched.
- Checklist `- [ ] **COV-02**` → `- [x] **COV-02**`, leaving the requirement's own text untouched.
- Traceability row `| COV-01 | Phase 26 | Pending (exactness verified; held open pending WR-01 adjudication, see 26-REVIEW.md) |`
  → `| COV-01 | Phase 26 | Complete |`, and append after `Complete` a short parenthetical naming
  the disposition: WR-01 adjudicated REACHABLE, the invariant restated as the itemised identity
  `coveredSec === bucketedSec + unbucketedCoveredSec`, verified across the whole committed archive
  (cite 26-11's measured stream count).
- Traceability row `| COV-02 | Phase 26 | Pending (gaps_found — caption gated behind buckets.length, see 26-VERIFICATION.md) |`
  → `| COV-02 | Phase 26 | Complete |`, with a parenthetical naming the closure evidence: caption
  decoupled from `buckets.length`, regression-tested, and confirmed on screen for activity
  `11865310195` in Round 2 (cite the row id).

Change nothing else in the file — no other requirement's state, no Phase 27+ rows, and no
reordering.
  </action>
  <verify>
    <automated>grep -c "COV-01 | Phase 26 | Pending\|COV-02 | Phase 26 | Pending" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "COV-01 | Phase 26 | Pending\|COV-02 | Phase 26 | Pending" .planning/REQUIREMENTS.md` outputs `0`.
    - `grep -c "^- \[ \] \*\*COV-0" .planning/REQUIREMENTS.md` outputs `0` — both COV checklist boxes are ticked.
    - `grep -c "Phase 26 | Pending" .planning/REQUIREMENTS.md` outputs `0` — no Phase 26 requirement is left Pending.
    - `git diff --stat .planning/REQUIREMENTS.md` shows exactly 4 changed lines, no more.
    - `git diff .planning/REQUIREMENTS.md | grep -c "^[-+].*QUAL-\|^[-+].*PR-0\|^[-+].*CUR-\|^[-+].*ELEV-"` outputs `0` — no later-phase row was touched.
    - Both new traceability parentheticals name their evidence (the archive-wide identity check for COV-01; the Round 2 row id for COV-02).
    - The SUMMARY states which Round 2 rows passed, quoting the R2-1 caption verbatim as the closure evidence for COV-02.
  </acceptance_criteria>
  <done>COV-01 and COV-02 read `Complete` with their disposition stated, and nothing else in `REQUIREMENTS.md` moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo artifacts → served build | a staged build can serve a stale `index.html`/`index.json`; the served bytes, not the repo bytes, are what the human observes |
| human observation → recorded verdict | a transcribed verdict is an evidentiary claim; inferring one from an automated check would falsify the record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-26 | Spoofing | the served build | mitigate | `curl` against the SERVED `data/streams/11865310195.json` and `data/dashboard/index.json` paths, plus a mandatory hard reload before every row |
| T-26-27 | Repudiation | the Round 2 verdict table | mitigate | Every verdict carries a verbatim developer quotation; the summary states no verdict was inferred from an automated check |
| T-26-28 | Tampering | premature requirement closure | mitigate | Task 3 is hard-gated on every row passing, and its diff is bounded to 4 lines by an acceptance criterion |
| T-26-29 | Information disclosure | a vacuous or unsatisfiable checkpoint row | mitigate | Each row carries a both-direction satisfiability note; R2-1's failing direction is demonstrated against pre-fix code, and a totality check confirms the page renders before the human is asked to look |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages |
</threat_model>

<verification>
- Six pre-session gate commands all exit 0; served-path curls recorded.
- `26-VALIDATION.md` carries a Round 2 section with four rows and a filled verdict table.
- `grep -c "Phase 26 | Pending" .planning/REQUIREMENTS.md` outputs `0`.
- `git diff --stat .planning/REQUIREMENTS.md` shows exactly 4 changed lines.
</verification>

<success_criteria>
A human read `33% of elapsed time covered · 67% recording gaps · 0% paused` on activity
`11865310195`'s detail view — the activity that rendered no Pace Distribution section at all when
`26-VERIFICATION.md` was written — and reconciled it against a hand sum taken straight from the
committed stream file. The pinned exemplar still renders both caption and bars. The HR half still
renders nothing. COV-01 and COV-02 are closed, in that order, after the observation rather than
before it.
</success_criteria>

<output>
Create `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-13-SUMMARY.md` when done.
</output>
