---
phase: 27-per-activity-quality-signals
plan: 04
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - src/analytics/dashboard-index.types.ts
  - src/analytics/compute-dashboard-index.ts
  - src/analytics/compute-dashboard-index.test.ts
  - scripts/verify-dashboard-publish.mjs
autonomous: true
requirements: [QUAL-01, QUAL-03]

must_haves:
  decisions: [D-05, D-06, D-12, D-17]
  truths:
    - "Every index row carries the five named quality signals as separately readable fields"
    - "DASHBOARD_INDEX_SCHEMA_VERSION is 1 before and after the addition"
    - "Every activity has a data/stats/pace-quality/{id}.json shard, including stream-less ones"
    - "An activity with no committed stream reports notComputableReason on the index row, never a computed-looking none tier"
    - "The publish verifier fails if the quality field or the shard directory is absent from the published bundle"
  artifacts:
    - path: "src/analytics/dashboard-index.types.ts"
      provides: "the required `quality` field on DashboardIndexRow, schema version unchanged"
      contains: "quality: ActivityQualitySignals"
    - path: "src/analytics/compute-dashboard-index.ts"
      provides: "signal computation in the per-activity loop plus the per-id shard writer"
      contains: "pace-quality"
    - path: "scripts/verify-dashboard-publish.mjs"
      provides: "publish-time spot-check of the new field and a shard sample"
      contains: "pace-quality"
  key_links:
    - from: "src/analytics/compute-dashboard-index.ts"
      to: "src/analytics/pace-quality.ts"
      via: "import computePaceQualitySignals + buildPaceQualityShard"
      pattern: "from './pace-quality.js'"
    - from: "src/analytics/compute-dashboard-index.ts"
      to: "data/stats/pace-quality/{id}.json"
      via: "fileStore.writeJson in a per-id loop"
      pattern: "'pace-quality'"
    - from: "src/analytics/dashboard-index.types.ts"
      to: "src/analytics/pace-quality.ts"
      via: "import type { ActivityQualitySignals }"
      pattern: "ActivityQualitySignals"
---

<objective>
Land the five quality signals on the published dashboard index row additively, write the
per-activity evidence shard, and extend the publish-time verifier so a compute step that
silently stops emitting either one fails the gate rather than shipping.

Purpose: this is Criterion 1 (five separately-disclosed fields, inspectable directly in
`data/dashboard/index.json` and `data/stats/pace-quality/{id}.json`) and the storage half of
Criterion 2 (index additive, shard lazy). Every browser plan in waves 4-5 reads what this plan
writes.

Output: an index row carrying `quality`, a `data/stats/pace-quality/` shard directory with one
file per activity, and a publish gate that discriminates in both directions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-per-activity-quality-signals/27-CONTEXT.md
@.planning/phases/27-per-activity-quality-signals/27-PATTERNS.md
@.planning/phases/27-per-activity-quality-signals/27-02-SUMMARY.md
@src/analytics/dashboard-index.types.ts
@src/analytics/compute-dashboard-index.ts
@src/analytics/compute-best-efforts.ts

<interfaces>
<!-- Produced by plans 27-01/27-02, consumed here unchanged: -->

```typescript
// src/analytics/pace-quality.ts
export interface ActivityQualitySignals {
  decimation: DecimationSignal; gapProfile: GapProfileSignal;
  impossibleSamples: ImpossibleSampleSignal; deviceEra: DeviceEraSignal;
  elapsedVsMoving: ElapsedVsMovingSignal;
  anySevere: boolean; notComputableReason: string | null;
}
export interface PaceQualityShard { activityId: string; signals: ActivityQualitySignals; /* … */ }
export function computePaceQualitySignals(stream: CanonicalStream | null, metadata: ActivityQualityMetadata, options?): ActivityQualitySignals;
export function buildPaceQualityShard(activityId: string, stream: CanonicalStream | null, metadata: ActivityQualityMetadata, options?): PaceQualityShard;
export const NOT_COMPUTABLE_NO_STREAM: string;
```

The additive-field precedent to follow, `src/analytics/dashboard-index.types.ts:74-102` —
BOTH prior additions are REQUIRED on the producer type, never optional (WR-06), with
`ParsedDashboardIndexRow`'s `Partial<>` covering the re-parse side:

```typescript
/** … REQUIRED, deliberately (WR-06). Making the key optional to model "re-parsed JSON may
 *  lack it" also meant compute-dashboard-index could silently stop emitting the field with
 *  no compile error … */
gearName: string | null;
paceDisagreement: PaceDisagreement | null;
```

That file's own header already states: *"Phase 27 asserts index additivity for its own signals
against this same precedent."* `DASHBOARD_INDEX_SCHEMA_VERSION` stays `1`.

The per-id shard-write loop to copy, `src/analytics/compute-best-efforts.ts:308-321`:

```typescript
await fileStore.writeJson(path.join(statsDir, 'best-efforts.json'), doc);
for (const [id, entry] of Object.entries(sortedActivities)) {
  await fileStore.writeJson(path.join(statsDir, 'best-efforts', `${id}.json`), entry);
}
```

The existing per-activity loop to extend, `src/analytics/compute-dashboard-index.ts:208-270` —
`activity` is already in scope with `device_name`, `source_provider`, `elapsed_time`,
`moving_time`, and the conditional PACE-07 stream read shows the try/catch-degrade shape.
NOTE: `StravaActivity` has an `[key: string]: unknown` index signature, so `device_name` and
`source_provider` come through as `unknown` and are narrowed inside `resolveDeviceFamily`.

The `gearName` publish spot-check to mirror, `scripts/verify-dashboard-publish.mjs:269-292`
(presence check + a leak check + `ok()`/`fail()`), and the best-efforts shard sample at
`:637-695` (sample an id from the parent document, fetch that shard, assert its `activityId`
matches).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the required `quality` field to the index contract</name>
  <files>src/analytics/dashboard-index.types.ts</files>
  <read_first>
    - `src/analytics/dashboard-index.types.ts` in full (162 lines) — the header's two
      invariants, the `gearName`/`paceDisagreement` WR-06 doc comments, the
      `ParsedDashboardIndexRow` `Partial<>` split, and `DASHBOARD_INDEX_SCHEMA_VERSION`.
    - `src/analytics/pace-quality.ts` — the `ActivityQualitySignals` tree being imported.
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-CONTEXT.md`
      § D-14 — Phase 26's own additive-field discipline and its explicit note that Phase 27
      must not pre-break the `schemaVersion === 1` assertion.
  </read_first>
  <action>
    Add one REQUIRED field to `DashboardIndexRow`: `quality: ActivityQualitySignals`, imported
    type-only from `./pace-quality.js`. Not optional, following the `gearName` /
    `paceDisagreement` WR-06 reasoning verbatim — an optional key would let
    `compute-dashboard-index.ts` silently stop emitting it with no compile error, which is
    exactly the CR-02 defect Phase 26 shipped once already.

    One nested key rather than five top-level ones, following `paceDisagreement`'s own shape.
    Its doc comment must state that the five named signals ARE separately readable underneath
    it (`quality.decimation`, `quality.gapProfile`, `quality.impossibleSamples`,
    `quality.deviceEra`, `quality.elapsedVsMoving`), which is what Criterion 1's
    "five separate fields" asks for, and that `quality.anySevere` is a derived convenience the
    list filter reads and is NOT a sixth signal.

    Document the deviation from the `PaceDisagreement` precedent explicitly: `PaceDisagreement`
    is redeclared structurally in this file rather than imported from `pace-derivation.ts`,
    but `ActivityQualitySignals` is a six-interface tree and duplicating it here would create
    exactly the drift risk that duplication already carries for a three-field object. Since
    `pace-quality.ts` is pure and client-safe (no `fs`, no `fetch`, no DOM), importing its
    types here costs this contract nothing. State that reason so the deviation is not read as
    an oversight.

    State in the same comment that `DASHBOARD_INDEX_SCHEMA_VERSION` stays `1` because this is
    purely additive, that `scripts/verify-dashboard-publish.mjs` asserts it, and that
    `ParsedDashboardIndexRow`'s `Partial<>` covers the re-parse side with no change needed
    there. Do NOT touch `DASHBOARD_INDEX_SCHEMA_VERSION`.

    Add `qualityAnySevere: number` and `qualityNotComputable: number` to
    `DashboardIndexTotals`, following the existing counter fields' shape, so the aggregate the
    calibration report measures is also visible in the shipped document's own totals.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 — or fails ONLY inside `compute-dashboard-index.ts` for the
      missing required key, which Task 2 supplies. Record which.
    - `grep -n "DASHBOARD_INDEX_SCHEMA_VERSION = 1" src/analytics/dashboard-index.types.ts` still matches.
    - `git diff src/analytics/dashboard-index.types.ts` shows no change to any existing field's
      type or optionality.
    - `quality` is declared without a `?` and without `| undefined`.
    - `DashboardIndexTotals` gains exactly two new number fields.
  </acceptance_criteria>
  <done>The index contract carries a required `quality` field; the schema version is untouched; the deviation from the `PaceDisagreement` duplication precedent is documented with its reason.</done>
</task>

<task type="auto">
  <name>Task 2: Compute the signals in the CI loop and write the per-activity shard</name>
  <files>src/analytics/compute-dashboard-index.ts, src/analytics/compute-dashboard-index.test.ts</files>
  <read_first>
    - `src/analytics/compute-dashboard-index.ts` in full (334 lines) — the options interface
      (`statsDir`, `streamsDir`, `outDir` are all already parameters), the per-activity loop at
      :150-280 with its `try/catch … continue` skip-unreadable discipline, the
      `Omit<DashboardIndexRow, 'gearName'>` two-pass assembly, the totals block, and the
      closing `console.log` summary.
    - `src/analytics/compute-best-efforts.ts:300-325` — the shard-write loop to copy.
    - `src/analytics/compute-dashboard-index.test.ts` — the existing fixture/FileStore setup, so
      new tests follow it rather than inventing a second harness.
    - `src/analytics/pace-quality.ts` — `computePaceQualitySignals`, `buildPaceQualityShard`,
      `NOT_COMPUTABLE_NO_STREAM`.
    - `.planning/phases/27-per-activity-quality-signals/27-CONTEXT.md` § D-06 (the stream-less
      population) and § D-17 (the shard payload).
  </read_first>
  <action>
    In the existing per-activity loop, after the `paceDisagreement` block, build
    `metadata = { deviceName: activity.device_name, sourceProvider: activity.source_provider,
    elapsedTimeSec: activity.elapsed_time, movingTimeSec: activity.moving_time }` (all four read
    off the `activity` object already in scope — no new file read for the two metadata-only
    signals) and read the stream.

    UNLIKE `paceDisagreement`, the stream read here is UNCONDITIONAL when the manifest says a
    stream exists — the three stream-derived signals are computed for every streamed activity,
    not gated behind a pace threshold. Reuse the same try/catch-and-warn shape: a read or parse
    failure yields `stream = null`, which `computePaceQualitySignals` turns into an explicit
    `notComputableReason`, never a zeroed `none`. Where the manifest already says no stream is
    available, pass `null` directly without attempting a read.

    If `paceDisagreement`'s own conditional read already fetched the stream for this activity,
    reuse that value rather than reading the file twice — one read per activity per run.

    Assign `quality` onto the row object. Increment the two new totals counters:
    `qualityAnySevere` when `quality.anySevere`, `qualityNotComputable` when
    `quality.notComputableReason !== null`.

    After the existing `rows.sort(...)` and before or after the index write, add a SECOND loop
    (not a modification of the row-assembly loop — the two-pass shape is
    `compute-best-efforts.ts`'s own and isolates the new I/O) writing
    `path.join(statsDir, 'pace-quality', `${id}.json`)` for every row, using
    `buildPaceQualityShard`. A shard is written for EVERY activity including stream-less ones,
    so the detail view always has an honest artifact to fetch rather than a 404 it must
    interpret. To avoid holding 1,890 shards in memory, build each shard inside the second loop
    from the stream re-read there, or carry the shard alongside the row in `pendingRows` —
    choose one and state which in the summary; do not do both.

    Extend the closing `console.log` block with three lines mirroring the existing ones:
    activities with any severe signal, activities not computable, and the elapsed wall time of
    the quality pass. This pass adds a full stream read over the archive to a step that
    previously read streams only conditionally — the timing line is how a future regression in
    that cost becomes visible.

    Extend `compute-dashboard-index.test.ts` with a `describe` containing the literal substring
    `quality signals`, covering:
      - A fixture activity WITH a stream produces a row whose `quality` carries all five named
        sub-objects and `notComputableReason === null`.
      - A fixture activity with NO stream produces `quality.notComputableReason === NOT_COMPUTABLE_NO_STREAM`,
        all three tiering tiers `'not-computable'`, `anySevere === false`, and a resolved
        `deviceEra.family` — asserted explicitly to NOT be `'none'` on any tier.
      - `doc.schemaVersion === DASHBOARD_INDEX_SCHEMA_VERSION` and that constant still equals 1.
      - `doc.totals.qualityAnySevere` equals the count of rows whose `quality.anySevere` is
        true, recomputed independently in the test from `doc.activities` — the totals cannot
        agree with themselves.
      - A shard file is written for every row, its `activityId` matches the row id, and its
        `signals` deep-equals the row's `quality` — the index scalar and the shard evidence
        cannot drift.
      - The stream-read failure path: a stream file present but containing malformed JSON
        yields `notComputableReason` set and does NOT throw or skip the activity.
  </action>
  <verify>
    <automated>npx vitest run src/analytics/compute-dashboard-index.test.ts -t "quality signals"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analytics/compute-dashboard-index.test.ts` exits 0.
    - `npx vitest run src/analytics/compute-dashboard-index.test.ts -t "quality signals"` exits 0 with at least one passing test.
    - `npx tsc --noEmit` exits 0.
    - `npm run build && npm run compute-dashboard-index` exits 0 against the live archive, and
      afterwards: `ls data/stats/pace-quality/*.json | wc -l` equals the row count printed by
      the run, and `node -e "const d=require('./data/dashboard/index.json'); console.log(d.schemaVersion, d.activities.filter(r=>!r.quality).length)"`
      prints `1 0`.
    - Independently derived cross-check (not the script's own claim): recompute the
      any-severe count from the written `index.json` with a one-liner and confirm it equals
      `totals.qualityAnySevere`, and confirm it equals the composite reported by
      `27-CALIBRATION.md` section 4, or state the delta and its cause (archive drift between
      the two runs is a legitimate cause; a threshold difference is not).
    - `grep -c "readJson" src/analytics/compute-dashboard-index.ts` shows no more stream-read
      call sites than one per activity per run.
    - The run's printed quality-pass wall time is recorded in the summary.
  </acceptance_criteria>
  <done>Every index row carries `quality`, every activity has a shard, the totals are reproducible from the shipped document, and stream-less activities report not-computable rather than a plausible zero.</done>
</task>

<task type="auto">
  <name>Task 3: Publish-time spot-check for the new field and the shard directory</name>
  <files>scripts/verify-dashboard-publish.mjs</files>
  <read_first>
    - `scripts/verify-dashboard-publish.mjs:269-292` — the `gearName` presence + leak check
      block, the `ok()`/`fail()` idiom, and the `schemaVersion !== 1` assertion this must not
      disturb.
    - `scripts/verify-dashboard-publish.mjs:585-700` — the `best-efforts.json` block and the
      per-id shard sample that picks an id from the parent document and fetches
      `/data/stats/best-efforts/{id}.json`. This is the exact shape for the `pace-quality`
      shard sample.
    - `.planning/phases/27-per-activity-quality-signals/27-CONTEXT.md` § D-12's note on
      `device_name` reaching a public artifact.
    - `.planning/STATE.md` § Blockers/Concerns, the 2026-08-11 RESOLVED entry — the lesson that
      this verifier once reported 15/15 green against a broken build because its own checks
      normalized the defect away. Every check added here must be demonstrated failing.
  </read_first>
  <action>
    Add one new block immediately after the existing `gearName` block, inside the same
    `if (Array.isArray(parsed.activities))` guard, asserting:
      - At least one row has a non-`undefined` `quality` object (presence spot-check mirroring
        `hasGearName`). `fail()` naming the field if none does.
      - EVERY row has a `quality` object with all five named sub-keys present. A partial
        rollout — some rows carrying the field, some not — is the exact silent-omission failure
        WR-06's required-field rule exists to prevent, and a presence-on-at-least-one check
        alone would pass it. `fail()` naming the first offending row id.
      - No row's `quality.deviceEra.rawDeviceName` contains `<`, `>` or `"` — a
        defence-in-depth spot-check on the newly-published untrusted string, mirroring the
        `leakedRow` gear-id shape. State in a comment that the REAL XSS guard is
        `textContent`-only rendering at the DOM layer (plans 27-07 / 27-09) and this check is a
        second line, not the first.

    Add a second block after the `best-efforts` shard sample, copying its structure exactly:
    pick a sample id from `parsed.activities`, fetch `/data/stats/pace-quality/{id}.json`,
    assert HTTP 200, assert its `activityId` equals the sampled id, and assert its `signals`
    object carries the same five sub-keys as the index row's `quality`. Use a DIFFERENT sample
    id than the best-efforts block does if one is available, so the two checks do not both pass
    or both fail on one activity's idiosyncrasy.

    Do not alter any existing check, and do not alter the `schemaVersion !== 1` assertion —
    Phase 26's D-14 note explicitly warns Phase 27 not to pre-break it.
  </action>
  <verify>
    <automated>npm run build-widgets && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify-dashboard` exits 0 against a freshly built publish directory, and its
      output contains an `ok(...)` line naming `quality` and one naming `pace-quality`.
    - Demonstrated failing, BOTH new blocks, run and recorded verbatim in the summary:
      (a) delete `quality` from one row of the served `index.json`, re-run, observe a `fail()`
      naming that row id and a non-zero exit; restore.
      (b) delete one `data/stats/pace-quality/{id}.json` shard that the sampler will pick,
      re-run, observe a `fail()` and a non-zero exit; restore.
      A check that cannot fail is not a check.
    - `git diff scripts/verify-dashboard-publish.mjs` shows no modification to any pre-existing
      `ok()`/`fail()` call or to the `schemaVersion` assertion.
    - The `<`/`>`/`"` scan runs over every row, not a sample, and its result is printed.
  </acceptance_criteria>
  <done>The publish gate discriminates in both directions on the new index field and the new shard directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/activities/{id}.json` → published `data/dashboard/index.json` | Untrusted `device_name` reaches a public GitHub Pages artifact for the first time (D-12) |
| `data/streams/{id}.json` → `data/stats/pace-quality/{id}.json` | Committed stream arrays become published per-activity evidence |
| built publish dir → `verify-dashboard-publish.mjs` | The HTTP-layer gate that decides whether a build ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-12 | Information Disclosure | new index/shard fields | mitigate | Field set closed by `ActivityQualitySignals` and `PaceQualityShard`; no athlete id, upload id, external id, gear id or privacy flag is copied. `device_name` is the single new published string and is precedented by `resolveGearLabel` already feeding `gearName`. |
| T-27-13 | Tampering | stored XSS via `device_name` | mitigate | Publish-time scan for `<`, `>`, `"` across every row (Task 3, second line of defence); `textContent`-only rendering at the DOM boundary (plans 27-07 / 27-09, first line). |
| T-27-14 | Tampering | silent field omission | mitigate | `quality` is REQUIRED on the producer type so `tsc` fails on omission; the publish verifier additionally asserts presence on EVERY row, not just one, and is demonstrated failing by deleting a row's field. |
| T-27-15 | Denial of Service | archive sweep in CI | mitigate | Per-activity `try/catch … continue` already in the loop; a malformed stream degrades to `notComputableReason`, asserted by a test. Quality-pass wall time printed so a cost regression is visible. |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npm run test` exits 0.
- `npm run compute-dashboard-index` against the live archive produces one shard per row and
  `schemaVersion: 1`.
- `npm run verify-dashboard` exits 0, and both new checks are demonstrated failing and restored.
- `totals.qualityAnySevere` is independently reproducible from the shipped `index.json`.
</verification>

<success_criteria>
- Five named quality signals are inspectable directly in `data/dashboard/index.json` and
  `data/stats/pace-quality/{id}.json` (Criterion 1).
- `DASHBOARD_INDEX_SCHEMA_VERSION` is unchanged before and after (Criterion 2, storage half).
- Stream-less activities report an explicit not-computable state on both artifacts.
- Both new publish checks are proven to fail when the thing they check is absent.
</success_criteria>

<output>
Create `.planning/phases/27-per-activity-quality-signals/27-04-SUMMARY.md` when done.
Record: the shard-build strategy chosen (second-loop re-read vs carried alongside the row); the
quality-pass wall time; the independently recomputed any-severe count and how it compares to
`27-CALIBRATION.md` section 4; and the verbatim output of both demonstrated-failing publish runs.
</output>
