---
phase: 27-per-activity-quality-signals
plan: 11
type: execute
wave: 7
depends_on: ["27-10"]
gap_closure: true
closes_gaps: ["G-01"]
files_modified:
  - scripts/compute-pace-quality-calibration.mjs
  - scripts/compute-pace-quality-calibration.test.mjs
  - .planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md
autonomous: true
requirements: [QUAL-05]

must_haves:
  decisions: [D-02, D-04]
  truths:
    - "Regenerating 27-CALIBRATION.md reproduces the corrected 1865/25 denominators rather than reverting them to 1866/24"
    - "The stream-file count excludes data/streams/manifest.json, which is an availability index and not a per-activity stream"
    - "A regression test fails against the unfixed glob and passes against the fixed one"
    - "The composite (299) and the activity count (1890) are unchanged by the fix"
  artifacts:
    - path: "scripts/compute-pace-quality-calibration.test.mjs"
      provides: "a regression test pinning the manifest.json exclusion"
      contains: "manifest"
---

<objective>
Close gap G-01 from 27-VALIDATION.md: make the calibration artifact of record genuinely
regenerable at its own stated values.

`scripts/compute-pace-quality-calibration.mjs` counts stream files with
`readdirSync(STREAMS_DIR).filter((f) => f.endsWith('.json'))`, which includes the non-activity
`data/streams/manifest.json` stream-availability index. It therefore emits stream-file count
1866 / stream-less 24. Plan 27-03's continuation corrected those figures to 1865 / 25 in the
PROSE of 27-CALIBRATION.md but did not fix the generator, so re-running the script silently
reverts the documented correction — observed directly during plan 27-10's R7 re-run, which
reverted 42 lines before being restored with `git checkout --`.

This matters beyond tidiness: ROADMAP Criterion 4a's GATE clause requires the rate be measured
"against a live denominator (recomputed at run time, never a hardcoded archive size)". The
denominator IS recomputed at run time — it is simply computed wrong.
</objective>

<context>
Unchanged by this fix, and must stay unchanged — verify, do not re-derive:
- activity count 1890
- composite (anySevere) 299
- per-signal severe cohorts: decimation 154, gapProfile 127, impossibleSamples 31
- the entire Threshold Sensitivity table (six rows, +67/-38/+33/-20/+35/-44)

D-02 forbids retuning any threshold; D-04 locks the decimation rule. This plan touches NO
threshold constant. It changes one glob and the prose the generator emits for the denominators.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exclude the manifest index from the stream-file count, with a regression test</name>
  <files>scripts/compute-pace-quality-calibration.mjs, scripts/compute-pace-quality-calibration.test.mjs</files>
  <action>
    Fix the stream-file glob (around line 425) to exclude `manifest.json`. Prefer excluding the
    known availability-index filename explicitly over a heuristic, and name WHY in a comment —
    manifest.json is written by backfill-streams and the daily intervals.icu sync as an
    availability index, not a per-activity stream.

    Check whether the same naive glob appears anywhere else in this script (or whether
    `idFromFilename` would produce a bogus id for `manifest`), and fix every occurrence rather
    than only the one line the gap named.

    Then update the generator's emitted prose for section 1 so the description it writes matches
    what it actually computes — it currently writes the literal string
    "`readdirSync('data/streams').filter(f => f.endsWith('.json'))` entries", which would become
    a false claim after the fix.

    Add a regression test in `scripts/compute-pace-quality-calibration.test.mjs` that pins the
    exclusion. Demonstrate it FAILING against the unfixed glob before trusting it: temporarily
    restore the old behaviour, watch the test fail, restore the fix, watch it pass, and record
    both runs verbatim in the summary. A test that cannot fail is worth nothing here — this
    whole gap exists because a correction was recorded in prose that no test defended.
  </action>
  <verify>
    <automated>npx vitest run scripts/compute-pace-quality-calibration.test.mjs && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The stream-file count excludes `manifest.json`; every occurrence of the naive glob is fixed
    - The emitted section-1 prose describes what the code actually does
    - A regression test pins the exclusion and was demonstrated failing against the old glob
    - No threshold constant in `src/analytics/pace-quality.ts` is edited (verify: empty diff)
  </acceptance_criteria>
  <done>Regenerating no longer reverts the correction, and a test would catch it if it did.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the report and confirm it reproduces the corrected values</name>
  <files>.planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md</files>
  <action>
    Run `npm run compute-pace-quality-calibration -- --sweep` and confirm the regenerated
    27-CALIBRATION.md now states stream-file count 1865 and stream-less count 25 by computation
    rather than by hand-edit.

    The regenerated file will NOT carry 27-03's hand-written correction note, three-way
    corroboration paragraph, or the section-3 rounding footnote, because the generator does not
    emit them. Those are load-bearing provenance that must survive: re-add them to the
    generator's OUTPUT (so they are emitted on every future run) rather than hand-editing them
    back into the file — hand-editing is precisely what created this gap. If any of that
    content genuinely cannot be generator-emitted, say so explicitly in the summary and explain
    why, rather than quietly dropping it.

    Then diff the regenerated report against its previous committed state and confirm that
    activity count 1890, composite 299, cohorts 154/127/31, and all six Threshold Sensitivity
    rows are byte-identical. Any drift in those is a FAILURE of this plan, not an update.

    Note: `26-RESIDUAL.md` is also rewritten by this pipeline with a timestamp-only diff. That is
    pre-existing behaviour; commit it if it changes, and say so.
  </action>
  <verify>
    <automated>node scripts/compute-pace-quality-recount.mjs && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - Regenerated 27-CALIBRATION.md states 1865 / 25 by computation
    - The correction note, three-way corroboration, and rounding footnote survive regeneration
      (emitted by the generator, not hand-edited back)
    - 1890 / 299 / 154 / 127 / 31 and all six sensitivity rows unchanged
    - Recount still reports 299 and exits 0; verify-dashboard exits 0
  </acceptance_criteria>
  <done>The artifact of record regenerates to its own stated values, and G-01 is closed.</done>
</task>

</tasks>
