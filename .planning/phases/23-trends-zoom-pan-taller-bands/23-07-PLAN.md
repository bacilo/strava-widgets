---
phase: 23-trends-zoom-pan-taller-bands
plan: 07
type: execute
wave: 5
depends_on: ["23-06"]
files_modified:
  - .planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TRN-01, TRN-02, TRN-03, TRN-04]
user_setup: []

must_haves:
  decisions: [D-25, D-01, D-02, D-03, D-06, D-07, D-11, D-13, D-14, D-15, D-17, D-19, D-20, D-21, D-22, D-23]
  truths:
    - "The full automated gate is green: npm test, tsc --noEmit, npm run build-widgets, npm run verify-dashboard."
    - "The built chunk graph is asserted, not assumed: hammerjs and chartjs-plugin-zoom appear in the lazy trends-charts chunk and not in the entry chunk the dashboard's index.html loads."
    - "The expected read-back values are recomputed against the archive as it stands at checkpoint time and written into 23-VALIDATION.md BEFORE the checkpoint runs, per D-25's advance-computation discipline."
    - "The developer verifies against a provably fresh staged build served under /strava-widgets, with the served asset filename quoted and matched against the freshly built one."
    - "D-25: the TRN-04 row is a discriminator, not a presence check - a specific named range is established on two tabs, all five tabs are cycled repeatedly, and the range is read BACK off the canvas aria-label and the axis ticks."
    - "Every checkpoint row is recorded with the exact observation it asked for, or recorded BLOCKED - developer or orchestrator authority does not substitute for the stated observation."
    - "REQUIREMENTS.md is gated strictly on the row map: a requirement is ticked only when every one of its mapped rows PASSED."
  artifacts:
    - path: ".planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md"
      provides: "the populated Per-Task Verification Map, the recomputed expected-value table, and the Round 1 checkpoint record with a verdict per row"
      contains: "Round 1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TRN-01..TRN-04 gated strictly on the row map"
  key_links:
    - from: ".planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the row-to-requirement map decides which requirements may be ticked"
      pattern: "TRN-0"
---

<objective>
Run the full automated gate, prove the lazy chunk boundary held, recompute the expected read-back
values against the live archive, and then hold a BLOCKING human browser checkpoint that
discriminates a working feature from a plausible-looking broken one.

Purpose: `REQUIREMENTS.md`'s Verification Note is explicit that automated tests cannot discharge
TRN-01 through TRN-04, and this project has shipped rendering defects behind a fully green gate
three times. Two of this phase's highest-risk defects - the aria-label and Reset state never
updating after a BUTTON press (research Pitfall 3), and the `→` button revealing earlier instead
of later data (Pitfall 5's sign) - are invisible to every automated check in this repository and
would look like "it works" to a tester who watches the chart move without reading which way.

Read `STATE.md`'s Phase 22 record before running the checkpoint. Rows in this project have
repeatedly been passed on substituted evidence - gathered at a different width, on a different
control, or summarised as "it works" - and that shipped a Critical defect and burned four
gap-closure rounds. Every row below states the exact observation that constitutes proof.

Output: a populated, recorded `23-VALIDATION.md` and a strictly gated `REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-CONTEXT.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-01-SUMMARY.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-05-SUMMARY.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (23-07/T1): Full gate, chunk-graph proof, and recomputed expected values</name>
  <files>.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md</files>
  <read_first>
    - .planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md (the Per-Task Verification Map to populate and the Manual-Only table that becomes the row agenda)
    - src/dashboard/views/trends-zoom-logic.ts (the constant tables the expected values are recomputed from)
    - .planning/phases/23-trends-zoom-pan-taller-bands/23-01-SUMMARY.md (the expected-value table computed at planning time, against the 2026-08-19 archive)
    - src/dashboard/views/trends-charts.ts (the aria-label bases the checkpoint quotes back)
  </read_first>
  <action>
    Run, in order, and paste each command's tail into the plan summary:
    `npm test`, `npx tsc --noEmit`, `npm run build-widgets`, `npm run verify-dashboard`.
    All four must be green before the checkpoint is offered. If any is red, stop and report - do
    not run a checkpoint against a failing gate.

    Prove the lazy chunk boundary rather than assuming it (23-PATTERNS.md flags this as a gap in
    the research's own Wave 0 list). After `npm run build-widgets`:
    - Read the entry script filename out of `dist/widgets/index.html` (the
      `assets/index-*.js` in its module `<script>` tag) and record it.
    - Assert `grep -c "Hammer" "dist/widgets/<entry filename>"` prints `0`.
    - Assert the newest `dist/widgets/assets/trends-charts-*.js` prints at least `1` for the same
      grep. `dist/widgets/assets` accumulates chunks across builds, so identify the newest by
      modification time and record its exact filename in the summary rather than globbing blindly.
    - Note the one caveat in the summary: minifiers preserve legal-comment banners and property
      names, so `Hammer` is a reliable marker; a `0` on the entry chunk together with a non-zero
      on the trends chunk is the proof that `trends.ts`'s static graph never pays for Hammer or
      the zoom plugin.

    Recompute the expected read-back values against the archive AS IT STANDS NOW. The nightly CI
    commits new data, so the archive may have advanced past the 2026-08-19 snapshot planning used
    (last weekly bucket `2026-08-10`, last monthly `2026-08-01`, last yearly `2026-01-01`, last
    training-load day `2026-08-11`). Read the current last entries out of
    `data/stats/weekly-distance.json`, `monthly-stats.json`, `yearly-stats.json` and
    `training-load.json`, apply `computeFullRange` and `computeDefaultWindow` from
    `trends-zoom-logic.ts` with their shipped constants, and produce an "Expected read-back values
    (Round 1)" table in `23-VALIDATION.md` with these columns: tab/state, expected canvas
    `aria-label` verbatim, expected first and last x-axis tick month/year. Include at minimum:
    Volume weekly default, Volume monthly default, Volume yearly default, Volume weekly after one
    `+`, Volume weekly after one `←`, Volume weekly at full zoom-out, Training Load 12mo default.
    Against the 2026-08-19 archive those resolve to: `Weekly distance chart, Aug 2025 to Aug 2026`;
    `Monthly distance chart, Aug 2021 to Aug 2026`; `Yearly distance chart, Jul 2010 to Jul 2026`;
    `Weekly distance chart, Oct 2025 to Jun 2026`; `Weekly distance chart, May 2025 to May 2026`;
    `Weekly distance chart, Aug 2011 to Aug 2026`;
    `Training load chart: CTL, ATL, and TSB over time, Aug 2025 to Aug 2026`. If the recomputation
    disagrees with any of these, the recomputed value wins and the discrepancy is stated in the
    summary with the archive dates that caused it.

    Populate `23-VALIDATION.md`'s Per-Task Verification Map, replacing every `TBD` with the real
    plan and task IDs: the five `trends-zoom-logic.test.ts` rows map to `23-01/T3` (wave 1); the
    two `styles.test.ts` rows map to `23-02/T2` (wave 1); the `npm audit` supply-chain row maps to
    `23-01/T1`. Set each row's Status to green only if its command was actually run and passed in
    this session.

    Finally, stage a build and serve it under a production-shaped path for the checkpoint:
    `npm run build-widgets`, then serve `dist/widgets` mounted at `/strava-widgets` on
    `http://127.0.0.1:8099/strava-widgets/`. Record the served entry-asset filename. This project
    has previously run a checkpoint against a stale staged build; the entry filename recorded here
    is what row R1 checks against.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build-widgets &amp;&amp; npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - All four gate commands green; their tails pasted into the summary.
    - The entry chunk's `grep -c "Hammer"` prints `0` and the newest `trends-charts-*.js` chunk prints at least `1`; both filenames recorded verbatim.
    - `23-VALIDATION.md` contains an "Expected read-back values (Round 1)" table with at least the seven rows listed above, each giving a verbatim expected `aria-label`.
    - `23-VALIDATION.md`'s Per-Task Verification Map has zero remaining `TBD` entries.
    - The staged build is reachable at `http://127.0.0.1:8099/strava-widgets/` and its entry-asset filename is recorded in the summary.
  </acceptance_criteria>
  <done>The gate is green, the lazy chunk boundary is proven by a built-artifact grep, the expected values are recomputed and written down before anyone looks at a browser, and a provably fresh build is served under a production-shaped path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (23-07/T2): BLOCKING human browser checkpoint — 20 rows, Round 1</name>
  <files>.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md (the Phase 22 record on substituted checkpoint evidence — it governs how every row below is scored)
    - .planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md (the Manual-Only table and the expected-value table Task 1 wrote)
    - .planning/REQUIREMENTS.md line 67 (the standing rule that no automated test discharges any TRN requirement)
  </read_first>
  <action>
    Hold the blocking human browser checkpoint described below, then record it. This task performs
    no code change of any kind. Present the 20-row agenda to the developer, collect a verdict and
    the row's own stated evidence for each row, write a `## Round 1` section into
    `23-VALIDATION.md` carrying every verdict verbatim, set that file's `status` and
    `nyquist_compliant` frontmatter from the result, and gate `REQUIREMENTS.md` strictly on the
    row-to-requirement map at the end of the instructions. Any defect found is recorded verbatim as
    a gap and left unpatched, per the house rule since checkpoint 16-09.
  </action>
  <what-built>
    Zoom and pan on the three time-axis Trends tabs (Volume, Cadence &amp; HR, Training Load) via
    modifier-gated wheel, pinch, drag, and a five-button on-screen cluster; a D-06 opening window
    per granularity; taller viewport-relative bands; and within-tab zoom state that survives a tab
    switch and resets on a full remount. Year-over-Year and Gear are structurally excluded.
  </what-built>
  <how-to-verify>
    House rules for this checkpoint, non-negotiable:
    1. Every row below is NON-WAIVABLE. Record PASS only when the row's own stated observation was
       made, at the row's own stated width, on the row's own stated control. Evidence gathered at a
       different width, on a different control, or summarised as "it works" does NOT discharge a
       row - that exact substitution reopened CAL-02 and shipped CR-01 as a Critical in Phase 22.
    2. Developer or orchestrator authority does NOT substitute for the stated observation. If the
       observation was not made, record BLOCKED, not PASS. `workflow.auto_advance` does not apply.
    3. Use the four-verdict vocabulary from Phase 20: PASS / FAIL / BLOCKED / NOT EXERCISABLE.
    4. Do not patch anything found here. Record it verbatim as a gap for a gap-closure round, per
       the house rule since checkpoint 16-09.
    5. All rows run against `http://127.0.0.1:8099/strava-widgets/` in a real browser. No staged
       fixture is expected or permitted — 1,868 activities across 15 years are exactly the data
       this feature exists for (D-25).

    R1 — BUILD FRESHNESS (prerequisite for every other row). In DevTools, quote the
    `assets/index-*.js` filename the served page actually loaded, and confirm it matches the
    filename Task 1 recorded from the fresh build. Hard-reload after any rebuild. If they differ,
    every row below is BLOCKED.

    R2 — TRN-01, wheel zoom changes the rendered range. Volume tab, weekly granularity. Quote the
    first and last x-axis tick labels. Then Cmd/Ctrl + scroll up over the plot area. Quote them
    again. They must differ.

    R3 — TRN-01/D-14, bare wheel still scrolls the page. With the cursor OVER a chart plot area,
    scroll without the modifier. State that the page scrolled AND quote the first/last tick to
    show the chart's range did not change.

    R4 — TRN-01/D-06, the opening picture is not the full archive. On a fresh load of the Volume
    tab at weekly, quote the canvas `aria-label` verbatim and the first and last x-axis tick
    labels. Expected aria-label: the Volume-weekly row of Task 1's expected-value table. The label
    must NOT contain 2011.

    R5 — TRN-01/D-06, the other two granularities. Switch to Monthly, quote the `aria-label`
    verbatim. Switch to Yearly, quote it verbatim. Both must match Task 1's table.

    R6 — TRN-02/D-15, drag-to-pan and the cursor. Drag left across the plot area. Quote the
    first/last tick before and after. State the observed cursor at rest and during the drag
    (expected `grab` then `grabbing`).

    R7 — TRN-02/D-11, all four buttons with NO pointing device at all. Using only the keyboard,
    Tab to each of `←` `→` `−` `+` and activate with Enter or Space. For EACH OF THE FOUR
    individually, quote its `aria-label` and the canvas `aria-label` before and after its
    activation. A single combined "all four work" statement does not discharge this row.

    R8 — TRN-02, the pan direction is not inverted. From the weekly default, press `←` once, then
    `→` once. Quote the canvas `aria-label` after each. Expected: after `←`, the Volume-weekly-
    after-`←` row of Task 1's table; after `→`, back to the Volume-weekly-default row. If `→`
    reveals EARLIER dates, the sign is inverted - record FAIL with both quoted labels.

    R9 — TRN-02/D-11, buttons disable at their clamps. (a) On a fresh weekly load, state whether
    `→` is disabled and whether `←` is enabled (expected: `→` disabled, because the default window
    already ends at the latest data). (b) Press `−` until it stops changing the range; quote the
    resulting canvas `aria-label` (expected: the full-zoom-out row of Task 1's table) and state
    the `disabled` / `aria-disabled` state of `−`, `←` and `→`.

    R10 — TRN-02/D-11, Reset appears only when zoomed and restores the DEFAULT, not full
    zoom-out. (a) Confirm Reset is absent on a fresh weekly load. (b) Press `+` once; confirm
    Reset appears. (c) Press Reset; quote the resulting canvas `aria-label`. It must equal the
    Volume-weekly-DEFAULT row of Task 1's table, NOT the full-zoom-out row. This is the row that
    distinguishes "reset to the designed opening picture" from "reset to everything".

    R11 — TRN-02/D-13, the aria-label updates after BOTH a gesture and a BUTTON. Quote the canvas
    `aria-label` verbatim after a Cmd/Ctrl + scroll zoom, and separately after a `+` button press.
    Both must name the new range. The button case is the one research flagged as silently broken
    if `onZoomComplete` alone were relied on, and no automated check in this repository can see
    it. Quoting only one of the two cases does not discharge this row.

    R12 — TRN-02/D-17, the modifier hint is visible. Quote the hint text as rendered, verbatim,
    and state which band header it appeared in.

    R13 — TRN-03/D-19, bands are taller and viewport-relative. In the console, report
    `getComputedStyle(document.querySelector('.chart-band__canvas-wrap--tall')).height` at two
    DIFFERENT window heights. State both window heights and both computed heights. The values must
    differ from each other and both must sit inside the shipped clamp bounds.

    R14 — TRN-03/D-20, both Cadence &amp; HR bands get the full height. On that tab, report the
    computed height of BOTH bands (report both numbers, not "they look the same") and state
    whether the tab scrolls.

    R15 — TRN-03/D-21, the phone floor holds at real phone widths. At EACH of 390px, 393px, 412px
    and 430px - all four, each named individually - state the emulation method used, report the
    computed band height, and confirm there is no horizontal overflow. Naming a width other than
    the required one does not discharge the row; this is the exact substitution that reopened
    CAL-02 in Phase 22.

    R16 — TRN-04/D-02, the Cadence &amp; HR pair stays in lockstep. Zoom using the cluster while on
    that tab, then quote BOTH canvases' `aria-label`s. They must name the SAME range. Repeat after
    a wheel zoom performed over the HR (lower) band specifically, and quote both again.

    R17 — TRN-04/D-25, the canvas-lifecycle discriminator. (a) Zoom the Volume tab to a specific
    range and WRITE THE ARIA-LABEL DOWN; do the same on Training Load. (b) Cycle all five Trends
    tabs at least three full passes WITHOUT touching the granularity toggle, then return to Volume
    and to Training Load and quote both `aria-label`s again - both must still read the ranges from
    (a). (c) Now toggle granularity weekly to monthly to weekly, and quote the Volume
    `aria-label`: it must read the weekly DEFAULT from Task 1's table, because D-23 resets the
    zoom on a granularity change - neither the range from (a) nor a monthly range. (d) Paste the
    browser console output for the whole sequence; it must contain no errors, and specifically no
    "Canvas is already in use". (e) Paste the output of
    `[...document.querySelectorAll('[role=tabpanel]')].map(p => p.id + ':' + p.querySelectorAll('canvas').length).join(', ')`
    and of `document.querySelectorAll('.chart-zoom-controls').length`. No panel may hold more
    canvases than its design (Volume 1, Year-over-Year 1, Cadence &amp; HR 2, Training Load 1, Gear
    1), and the control-cluster count must be exactly 1 while a zoomable tab is active - a
    stranded cluster or a doubled canvas is the failure this row exists to catch, and an absence
    of console errors alone does not discharge it.

    R18 — TRN-01/D-03(b), decimation at deep zoom. On Training Load, zoom in to roughly a two-week
    span. State whether the series resolves to finer (near-daily) detail or stays visibly
    decimated, and quote the canvas `aria-label` at that zoom so the span is on the record.

    R19 — TRN-04/18-D15, thin-HR-coverage shading still draws correctly at every zoom level. On
    Training Load, zoom into a span you can see shading in, and state whether the shaded region
    still covers the same dates it did when zoomed out. Quote the `aria-label` at both zoom levels.

    R20 — TRN-04/D-22, zoom resets on a full remount. Zoom the Volume tab to a stated range, then
    navigate away to `#/records` and back to `#/trends`. Quote the Volume canvas `aria-label`. It
    must read the weekly DEFAULT from Task 1's table, not the range you left it at.

    RECORDING. Write a "Round 1" section into `23-VALIDATION.md` with one entry per row, each
    carrying: the verdict, the developer's own words, and the quoted values the row required. Then
    gate `REQUIREMENTS.md` strictly on this map:
    - TRN-01 needs R2, R3, R4, R5, R18 all PASS.
    - TRN-02 needs R6, R7, R8, R9, R10, R11, R12 all PASS.
    - TRN-03 needs R13, R14, R15 all PASS.
    - TRN-04 needs R16, R17, R19, R20 all PASS.
    A requirement with any mapped row not PASS stays Pending, and the specific blocking row is
    named in `REQUIREMENTS.md`. Set `23-VALIDATION.md`'s `nyquist_compliant` to `true` only if
    every row PASSED; otherwise `status: partial` and `nyquist_compliant: false`.
  </how-to-verify>
  <verify>
    <human-check>Each of R1 through R20 recorded with its own stated observation quoted verbatim, or recorded BLOCKED. No row marked PASS on a summary, on evidence from a different width or control, or on developer/orchestrator authority.</human-check>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md','utf8');const missing=[];for(let i=1;i<=20;i++){if(!new RegExp('\\\\bR'+i+'\\\\b').test(s))missing.push('R'+i)};if(missing.length){console.error('rows never recorded: '+missing.join(','));process.exit(1)};console.log('all 20 rows recorded')"</automated>
  </verify>
  <acceptance_criteria>
    - `23-VALIDATION.md` contains a Round 1 section with all 20 rows, each carrying a verdict from PASS / FAIL / BLOCKED / NOT EXERCISABLE.
    - Every PASS row quotes the values its instructions demanded: R4/R5/R8/R9/R10/R11/R17/R20 quote canvas `aria-label`s verbatim; R13/R14/R15 quote computed pixel heights with their window height or emulated width; R17 pastes the per-panel canvas counts and the cluster count.
    - R15 names all four widths (390, 393, 412, 430) individually with its own observation at each.
    - R7 gives four individual per-button observations, not one combined statement.
    - R11 quotes both the gesture case and the button case.
    - `REQUIREMENTS.md` ticks a TRN requirement only when every row in its map above PASSED, and names the blocking row for any that stays Pending.
    - `23-VALIDATION.md` frontmatter `nyquist_compliant` is `true` only on a clean sweep.
    - Any defect found is recorded verbatim as a gap and left unpatched.
  </acceptance_criteria>
  <resume-signal>Reply with a verdict per row (R1 through R20). A blanket "approved" is not a valid response to this checkpoint - it cannot be mapped onto the row table, and this phase's requirements are gated per row.</resume-signal>
  <done>All 20 rows have a recorded verdict with their stated evidence, `23-VALIDATION.md` carries the Round 1 record, and `REQUIREMENTS.md` reflects the row map exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built bundle to served page | A stale staged build can make a checkpoint verify code that is not the code under test |
| Lazy chunk boundary | The entry chunk must not carry the plugin or Hammer |
| Checkpoint evidence to requirement state | A requirement is ticked on the strength of what was actually observed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-STALE | Spoofing (of evidence) | staged build served at 127.0.0.1:8099 | mitigate | Row R1 requires the served entry-asset filename to be quoted and matched against the freshly built one, with a hard reload after any rebuild. Every other row is BLOCKED if R1 fails. |
| T-23-EVIDENCE | Repudiation | checkpoint record | mitigate | Every row states its own required observation; the plan's house rules forbid substituted evidence and state plainly that developer or orchestrator authority does not stand in for it. Verdicts use the four-state Phase 20 vocabulary so a dataset or hardware limit is not recorded as a pass. |
| T-23-LAZY | Denial of Service (page weight) | entry chunk | mitigate | Task 1 asserts `grep -c "Hammer"` is `0` on the entry chunk and non-zero on the newest `trends-charts` chunk, against the built artifact rather than the import statement. |
| T-23-SC | Tampering | `chartjs-plugin-zoom` + `hammerjs` shipping in the published bundle | mitigate | Legitimacy established in 23-RESEARCH.md and re-audited at install in plan 23-01; versions pinned in `package-lock.json`; `npm run verify-dashboard` confirms the published bundle's shape. |

No authentication, session, access-control or cryptography surface is added by this phase.
</threat_model>

<verification>
- `npm test`, `npx tsc --noEmit`, `npm run build-widgets`, `npm run verify-dashboard` all green.
- Entry chunk carries no `Hammer`; the newest `trends-charts-*.js` chunk does.
- `23-VALIDATION.md` has zero `TBD` entries in its Per-Task Verification Map and carries the recomputed expected-value table plus a Round 1 record for R1 through R20.
- `REQUIREMENTS.md` reflects the row map exactly.
</verification>

<success_criteria>
- The full automated gate is green and the lazy chunk boundary is proven against built artifacts.
- Expected read-back values were computed and written down BEFORE the browser was opened (D-25).
- All 20 checkpoint rows carry a verdict backed by the exact observation each demanded.
- TRN-01 through TRN-04 are ticked only where every mapped row passed; anything else stays Pending with its blocking row named.
</success_criteria>

<output>
Create `.planning/phases/23-trends-zoom-pan-taller-bands/23-07-SUMMARY.md` when done.
Record in it: the four gate command tails, both chunk filenames with their grep counts, the served
entry-asset filename, the recomputed expected-value table, the per-row verdicts, and the exact
REQUIREMENTS.md gating decision with its justification per requirement.
</output>
