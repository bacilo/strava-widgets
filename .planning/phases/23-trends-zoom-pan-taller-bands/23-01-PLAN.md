---
phase: 23-trends-zoom-pan-taller-bands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/dashboard/views/trends-zoom-logic.ts
  - src/dashboard/views/trends-zoom-logic.test.ts
autonomous: true
requirements: [TRN-01, TRN-02, TRN-04]
user_setup: []

must_haves:
  decisions: [D-06, D-09, D-12, D-13, D-14, D-16, D-22, D-24]
  truths:
    - "D-16: chartjs-plugin-zoom@^2.2.0 and hammerjs@^2.0.8 are declared dependencies and installed."
    - "D-06: a pure function computes each zoomable scale's opening window from the series' own bounds - weekly ~ last 12 months, monthly ~ last 5 years, yearly = everything - and never returns the full archive for weekly."
    - "D-09: a pure function computes limits.x as literal numbers (min, max, minRange) - never the string 'original'."
    - "D-12: a pure function computes the pan pixel delta as 25% of the plot-area pixel width with the sign convention left = positive, right = negative, and exports the 1.5 zoom factor."
    - "D-13: a pure function formats an epoch-ms pair as 'MMM yyyy to MMM yyyy' in UTC and composes it onto an existing aria-label base."
    - "D-14: a pure function maps a platform string to 'meta' on macOS and 'ctrl' elsewhere (Pitfall 2 - the plugin accepts only one key)."
    - "D-22: a pure function returns the saved range when present and the default window when null, so restore-on-rebuild and first-open share one code path."
    - "D-24: the module reads no localStorage, no sessionStorage and no URL query string - zoom state is in-memory only."
    - "Every one of these behaviours is covered by an assertion in trends-zoom-logic.test.ts that runs under environment: 'node' with no DOM."
  artifacts:
    - path: "src/dashboard/views/trends-zoom-logic.ts"
      provides: "D-06/D-09/D-12/D-13/D-14/D-22 pure logic - the only part of this phase automated tests can reach"
      min_lines: 150
      exports: ["ZoomScaleKey", "ZoomRange", "ZOOM_FACTOR", "PAN_FRACTION", "volumeScaleKey", "computeArchiveBounds", "computeFullRange", "computeDefaultWindow", "computeLimits", "loadWindowRange", "panDeltaPx", "formatRangeLabel", "withRangeSuffix", "restoreOrDefault", "rangesEqual", "isAtFullRange", "isAtEarliestEdge", "isAtLatestEdge", "modifierKeyForPlatform", "zoomHintText"]
    - path: "src/dashboard/views/trends-zoom-logic.test.ts"
      provides: "Wave 0 automated coverage for TRN-01/TRN-02/TRN-04's computable parts"
      min_lines: 120
    - path: "package.json"
      provides: "the two new dependencies (D-16)"
      contains: "chartjs-plugin-zoom"
  key_links:
    - from: "src/dashboard/views/trends-zoom-logic.ts"
      to: "src/dashboard/views/trends-training-load-logic.ts"
      via: "LoadWindow type import (D-03 presets are expressed as ranges here)"
      pattern: "trends-training-load-logic.js"
    - from: "src/dashboard/views/trends-zoom-logic.test.ts"
      to: "src/dashboard/views/trends-zoom-logic.ts"
      via: "named imports under the vitest node environment"
      pattern: "trends-zoom-logic.js"
---

<objective>
Install the two new dependencies and create the pure, DOM-free logic module the rest of this
phase is built on.

Purpose: `vitest.config.ts` is `environment: 'node'` - no jsdom, no headless browser, no canvas
polyfill. Chart.js cannot be constructed in a test in this repo. Logic left inline in `trends.ts`
or `trends-charts.ts` is permanently unreachable by automated tests. Extracting D-06's default
window, D-09's limits, D-12's zoom/pan arithmetic, D-13's label formatting, D-14's modifier-key
resolution and D-22's restore-or-default shape into one pure module is the only thing that makes
any part of this phase automatable. This is Wave 0 work, not cleanup.

Output: `package.json` / `package-lock.json` carrying `chartjs-plugin-zoom` + `hammerjs`,
`src/dashboard/views/trends-zoom-logic.ts`, and `src/dashboard/views/trends-zoom-logic.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-CONTEXT.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-RESEARCH.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-PATTERNS.md
@.planning/phases/23-trends-zoom-pan-taller-bands/23-VALIDATION.md
@src/dashboard/views/trends-training-load-logic.ts
@src/dashboard/views/trends-volume-logic.ts
</context>

<interfaces>
Contracts the executor needs. Do not go hunting for these in the codebase.

Existing, consumed by this module:

- `src/dashboard/views/trends-volume-logic.ts` exports
  `type VolumeGranularity = 'weekly' | 'monthly' | 'yearly'` and
  `const VOLUME_GRANULARITIES: readonly VolumeGranularity[]`.
- `src/dashboard/views/trends-training-load-logic.ts` exports
  `type LoadWindow = '3mo' | '12mo' | 'all'`, `const TRAINING_LOAD_WINDOWS`,
  `const DEFAULT_LOAD_WINDOW: LoadWindow = '12mo'`.
- Relative imports in this codebase always carry the `.js` extension (ESM/NodeNext).
- `*-logic.ts` modules are total and never throw on malformed input, and never construct a
  fresh `Date` internally.

New contracts this plan defines - every later plan in Phase 23 imports from here:

- `type ZoomScaleKey = 'volume-weekly' | 'volume-monthly' | 'volume-yearly' | 'cadence-hr' | 'training-load'`
- `interface ZoomRange { min: number; max: number }`
- `const ZOOM_FACTOR = 1.5` and `const PAN_FRACTION = 0.25`
- `function volumeScaleKey(granularity: VolumeGranularity): ZoomScaleKey`
- `function computeArchiveBounds(xs: readonly number[]): ZoomRange | null`
- `function computeFullRange(key: ZoomScaleKey, bounds: ZoomRange): ZoomRange`
- `function computeDefaultWindow(key: ZoomScaleKey, bounds: ZoomRange): ZoomRange`
- `function computeLimits(key: ZoomScaleKey, bounds: ZoomRange): { min: number; max: number; minRange: number }`
- `function loadWindowRange(window: LoadWindow, bounds: ZoomRange): ZoomRange`
- `function panDeltaPx(plotWidthPx: number, direction: 'earlier' | 'later'): number`
- `function formatRangeLabel(minMs: number, maxMs: number): string`
- `function withRangeSuffix(base: string, minMs: number, maxMs: number): string`
- `function restoreOrDefault(saved: ZoomRange | null, fallback: ZoomRange): ZoomRange`
- `function rangesEqual(a: ZoomRange, b: ZoomRange): boolean`
- `function isAtFullRange(current: ZoomRange, full: ZoomRange): boolean`
- `function isAtEarliestEdge(current: ZoomRange, full: ZoomRange): boolean`
- `function isAtLatestEdge(current: ZoomRange, full: ZoomRange): boolean`
- `function modifierKeyForPlatform(platform: string): 'meta' | 'ctrl'`
- `function zoomHintText(modifier: 'meta' | 'ctrl'): string`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1 (23-01/T1): Install chartjs-plugin-zoom and hammerjs</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependency list: chart.js ^4.5.1 present, neither new package is)
    - .planning/phases/23-trends-zoom-pan-taller-bands/23-RESEARCH.md sections "Standard Stack" and "Package Legitimacy Audit"
  </read_first>
  <action>
    Run `npm install chartjs-plugin-zoom@^2.2.0 hammerjs@^2.0.8`, then commit the updated
    `package.json` and `package-lock.json`.

    Both packages are already legitimacy-audited in 23-RESEARCH.md: `chartjs-plugin-zoom` (npm
    since 2016-10-23, 435,218 downloads/week, repo github.com/chartjs/chartjs-plugin-zoom, not
    archived) and `hammerjs` (npm since 2013-02-27, 1,587,784 downloads/week, repo
    github.com/hammerjs/hammer.js, not archived) both returned slopcheck OK. Neither is ASSUMED
    or SUS, so no pre-install human legitimacy checkpoint is required. Do NOT add
    `@types/hammerjs` separately - it arrives transitively as a dependency of
    `chartjs-plugin-zoom`.

    Record two facts in the plan summary. Both are corrections to CONTEXT.md's own stated
    assumptions that research verified against the shipped source:
    (a) Hammer.js is on the critical path for ALL panning, including plain desktop mouse
    drag-to-pan (D-15) - not only touch and pinch as D-16's text says. The decision to take the
    dependency stands; only its stated scope was too narrow. Nothing later in this phase may be
    architected on the assumption that Hammer could be loaded only on touch devices.
    (b) `hammerjs` has had no release since 2022-11-18 and no maintained fork drops it
    (chartjs/chartjs-plugin-zoom#938 is open with no timeline). This is a knowingly-accepted,
    recorded cost per D-16, not a discovery.

    Do NOT import either package anywhere in this plan. Plan 23-04 owns the first import and it
    must land inside the lazy chunk; an import from `trends.ts`, `chart-theme.ts` or any module in
    the main bundle's static graph would break the LAZY-CHUNK BOUNDARY rule stated at
    `trends-charts.ts:1-23`.

    Run `npm audit` after the install and paste its summary line into the plan summary. Treat a
    pre-existing advisory unrelated to these two package names as recorded context, not a
    blocker; a new advisory naming `chartjs-plugin-zoom` or `hammerjs` IS a blocker - stop and
    report rather than proceeding.
  </action>
  <verify>
    <automated>node -e "const d=require('./package.json').dependencies||{};if(!d['chartjs-plugin-zoom']||!d['hammerjs']){process.exit(1)};console.log(d['chartjs-plugin-zoom'],d['hammerjs'])" &amp;&amp; node -e "console.log(require('./node_modules/chartjs-plugin-zoom/package.json').version,require('./node_modules/hammerjs/package.json').version)" &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` contains both `chartjs-plugin-zoom` and `hammerjs`; the first node command above prints two version ranges and exits 0.
    - Installed versions print as `2.2.0` and `2.0.8` (or higher within the same major).
    - `npm test` is green (full suite, unchanged count from before the install).
    - `grep -rn "chartjs-plugin-zoom\|hammerjs" src/ | wc -l` prints `0` - no source file imports either package yet.
    - The plan summary contains both stated corrections (a) and (b) verbatim in substance, and the `npm audit` summary line.
  </acceptance_criteria>
  <done>Both dependencies are declared, installed at the expected versions, no source file imports them yet, and the full test suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (23-01/T2): Create trends-zoom-logic.ts (D-06, D-09, D-12, D-13, D-14, D-22)</name>
  <files>src/dashboard/views/trends-zoom-logic.ts</files>
  <read_first>
    - src/dashboard/views/trends-training-load-logic.ts (the exact `*-logic.ts` conventions to mirror: header comment shape at lines 1-13, the allow-list parse idiom at 162-168, the window/slice pure-transform at 170-196, and the `LoadWindow` type this module imports)
    - src/dashboard/views/trends-volume-logic.ts (the `VolumeGranularity` type and `VOLUME_GRANULARITIES` allow-list this module imports)
    - .planning/phases/23-trends-zoom-pan-taller-bands/23-RESEARCH.md sections "Common Pitfalls" (Pitfalls 1, 2, 5) and "Code Examples"
    - .planning/phases/23-trends-zoom-pan-taller-bands/23-PATTERNS.md section for `trends-zoom-logic.ts`
  </read_first>
  <behavior>
    - `computeArchiveBounds([])` returns `null`; `computeArchiveBounds` ignores non-finite entries and returns `{min, max}` over the rest.
    - `computeDefaultWindow('volume-weekly', {min: Date.parse('2011-08-15T00:00:00Z'), max: Date.parse('2026-08-10T00:00:00Z')})` returns `{min: Date.parse('2025-08-13T12:00:00Z'), max: Date.parse('2026-08-13T12:00:00Z')}`.
    - `computeDefaultWindow('volume-yearly', bounds)` equals `computeFullRange('volume-yearly', bounds)` exactly (yearly opens on everything).
    - `computeDefaultWindow` never returns a window wider than `computeFullRange` for the same key and bounds.
    - `computeLimits` returns three finite numbers and its `min`/`max` equal `computeFullRange`'s.
    - `panDeltaPx(800, 'earlier')` returns `+200`; `panDeltaPx(800, 'later')` returns `-200` (Pitfall 5's sign convention).
    - `formatRangeLabel(Date.parse('2025-08-13T12:00:00Z'), Date.parse('2026-08-13T12:00:00Z'))` returns `'Aug 2025 to Aug 2026'`.
    - `withRangeSuffix('Weekly distance chart', min, max)` returns `'Weekly distance chart, Aug 2025 to Aug 2026'`.
    - `restoreOrDefault(null, fallback)` returns `fallback`; `restoreOrDefault(saved, fallback)` returns `saved`.
    - `rangesEqual(a, b)` is true when both bounds match within 1 ms and false when either differs by more.
    - `modifierKeyForPlatform('MacIntel')` and `('macOS')` return `'meta'`; `('Win32')`, `('Linux x86_64')` and `('')` return `'ctrl'`.
    - `loadWindowRange('all', bounds)` equals `computeFullRange('training-load', bounds)`; `'12mo'` and `'3mo'` return progressively narrower windows sharing the same `max`.
    - No function throws on `NaN`, `Infinity`, empty arrays or a `saved` range whose `min > max`.
  </behavior>
  <action>
    Create `src/dashboard/views/trends-zoom-logic.ts` following the established `*-logic.ts`
    contract exactly: total, DOM-free, never constructs a `Date` for "now", never throws.

    Open with a header comment in the shape of `trends-training-load-logic.ts:1-13`, stating: what
    it is pure logic for (D-06 default window, D-09 limits, D-12 zoom/pan math, D-13 label
    formatting, D-14 modifier-key resolution, D-22 restore-or-default), and an explicit statement
    that it reads no clock, no DOM, no `localStorage` and no URL (D-24) - the anchor point is the
    series' own last data point, never `Date.now()`, which is what makes every function here
    deterministic under vitest.

    Type-only-import `VolumeGranularity` from `./trends-volume-logic.js` and `LoadWindow` from
    `./trends-training-load-logic.js`. Import no charting library and no runtime value from either
    module beyond what is needed.

    Declare these module-private constant tables, keyed by `ZoomScaleKey`, with these exact values:

    | key | PERIOD_MS | DEFAULT_SPAN_MS | MIN_RANGE_MS |
    |---|---|---|---|
    | volume-weekly | 604800000 | 31536000000 | 4838400000 |
    | volume-monthly | 2629800000 | 157788000000 | 15778800000 |
    | volume-yearly | 31557600000 | null (full range) | 94672800000 |
    | cadence-hr | 2629800000 | 157788000000 | 15778800000 |
    | training-load | 86400000 | 31536000000 | 604800000 |

    `PERIOD_MS` is the scale's nominal period (7 d, 30.4375 d, 365.25 d, 30.4375 d, 1 d).
    `DEFAULT_SPAN_MS` is D-06's opening span (12 months weekly, 5 years monthly, everything
    yearly, 5 years cadence/HR, 12 months training load - the last matching 18-D16's shipped
    default so D-03's "12mo" preset and the default window are the same picture).
    `MIN_RANGE_MS` is D-09's zoom-in floor: 8 weeks, 6 months, 3 years, 6 months, 7 days. The
    7-day training-load floor is deliberately below the two-week span the D-03(b) checkpoint row
    zooms to, so that row is comfortably reachable rather than exactly at the clamp.

    Also declare `LOAD_WINDOW_SPAN_MS`: `'3mo'` = 7776000000, `'12mo'` = 31536000000, `'all'` =
    null.

    Implement:
    - `volumeScaleKey(granularity)` mapping `'weekly' | 'monthly' | 'yearly'` to the three
      `volume-*` keys.
    - `computeArchiveBounds(xs)` returning `null` for an empty or all-non-finite input, otherwise
      `{min, max}` over the finite entries only.
    - `computeFullRange(key, bounds)` returning `{min: bounds.min - PERIOD_MS[key] / 2, max:
      bounds.max + PERIOD_MS[key] / 2}`. The half-period pad exists so the first and last bar are
      not clipped in half at the scale edges, and it gives `−`-at-full-zoom-out and Reset one
      well-defined target.
    - `computeDefaultWindow(key, bounds)`: let `full = computeFullRange(key, bounds)`; when
      `DEFAULT_SPAN_MS[key]` is null return `full`; otherwise return
      `{min: Math.max(full.min, full.max - DEFAULT_SPAN_MS[key]), max: full.max}`.
    - `computeLimits(key, bounds)` returning `{min: full.min, max: full.max, minRange:
      MIN_RANGE_MS[key]}` as LITERAL NUMBERS. This function exists specifically so the string
      sentinel `'original'` is never reachable: under D-06 the scale is constructed at the default
      window, so `'original'` would resolve to that window and hard-stop pan and zoom-out there,
      making "zoom out to see everything" impossible (Pitfall 1). Never emit the string
      `'original'` from this module.
    - `loadWindowRange(window, bounds)`: `'all'` returns `computeFullRange('training-load',
      bounds)`; otherwise `{min: Math.max(full.min, full.max - LOAD_WINDOW_SPAN_MS[window]), max:
      full.max}` - D-03's presets are ranges over the always-full series, never a dataset slice.
    - `panDeltaPx(plotWidthPx, direction)` returning `PAN_FRACTION * plotWidthPx` for `'earlier'`
      and the negation for `'later'`, guarding a non-finite or non-positive `plotWidthPx` by
      returning `0`. The sign is Pitfall 5's verified convention: the plugin's `pan()` treats a
      POSITIVE x delta as revealing earlier/smaller-value data. Restate that in a comment on this
      function - it is the single easiest thing in this phase to invert unnoticed.
    - `formatRangeLabel(minMs, maxMs)` returning `"{MMM} {yyyy} to {MMM} {yyyy}"` using UTC month
      and year (`getUTCMonth`, `getUTCFullYear`) and a local `MONTH_ABBR` array identical in
      content to `trends-charts.ts:80`. Return the empty string when either argument is
      non-finite.
    - `withRangeSuffix(base, minMs, maxMs)` returning `base` unchanged when `formatRangeLabel`
      returns empty, otherwise `` `${base}, ${label}` ``. This is D-13's composition point: it
      extends the existing `VOLUME_ARIA_LABELS` mechanism rather than inventing a second one.
    - `restoreOrDefault(saved, fallback)` returning `fallback` when `saved` is null, when either
      bound is non-finite, or when `saved.min >= saved.max`; otherwise `saved`. This is D-22's
      single code path shared by first-open and restore-on-rebuild.
    - `rangesEqual(a, b)` returning true when `Math.abs(a.min - b.min) <= 1` and `Math.abs(a.max - b.max) <= 1`. D-11 uses it to decide whether the Reset control is showing: Reset appears exactly when the live range differs from the D-06 default window.
    - `isAtFullRange(current, full)`, `isAtEarliestEdge(current, full)`, `isAtLatestEdge(current,
      full)` - D-11's button-disable predicates. Compare with a tolerance of one millisecond
      (`current.min <= full.min + 1`, etc.) so floating-point pixel round-tripping through the
      scale cannot leave a button permanently enabled at its clamp.
    - `modifierKeyForPlatform(platform)` returning `'meta'` when `/mac/i` matches the argument and
      `'ctrl'` otherwise. Take the platform string as an argument - the `navigator` read stays in
      the DOM layer (plan 23-04) so this stays testable. `zoom.wheel.modifierKey` accepts exactly
      one key, so D-14's "Cmd/Ctrl" genuinely requires this resolution (Pitfall 2).
    - `zoomHintText(modifier)` returning `'⌘ + scroll to zoom · drag or pinch to pan'`
      for `'meta'` and `'Ctrl + scroll to zoom · drag or pinch to pan'` for `'ctrl'`. This is
      D-17's copy; naming pinch covers the touch case where no modifier key exists.

    Export `ZOOM_FACTOR = 1.5` and `PAN_FRACTION = 0.25` (D-12's factors) as named constants so
    the button handlers in 23-04 use the same numbers the tests assert.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `node -e "const m=require('fs').readFileSync('src/dashboard/views/trends-zoom-logic.ts','utf8');const code=m.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^\s*\/\/.*$/gm,'');if(/original|localStorage|sessionStorage|Date\.now|new Date\(\)|document|window\./.test(code)){console.error('forbidden token in code');process.exit(1)};console.log('clean')"` prints `clean` - proving the module contains no `'original'` sentinel (Pitfall 1), no storage access (D-24), no clock read and no DOM access, with comments stripped first so prose cannot self-invalidate the gate.
    - Every export named in this plan's `<interfaces>` block is present: `node -e "const s=require('fs').readFileSync('src/dashboard/views/trends-zoom-logic.ts','utf8');const want=['ZoomScaleKey','ZoomRange','ZOOM_FACTOR','PAN_FRACTION','volumeScaleKey','computeArchiveBounds','computeFullRange','computeDefaultWindow','computeLimits','loadWindowRange','panDeltaPx','formatRangeLabel','withRangeSuffix','restoreOrDefault','rangesEqual','isAtFullRange','isAtEarliestEdge','isAtLatestEdge','modifierKeyForPlatform','zoomHintText'];const missing=want.filter(w=>!new RegExp('export (function|const|type|interface) '+w+'\\\\b').test(s));if(missing.length){console.error(missing);process.exit(1)};console.log('all exports present')"` prints `all exports present`.
    - The five constant-table rows above appear with exactly the stated numeric values.
    - The test gate is deliberately NOT part of this task's verify: `trends-zoom-logic.test.ts`
      does not exist until Task 3, and `npx vitest run <nonexistent-path>` prints
      `No test files found, exiting with code 1` and exits non-zero - an `&amp;&amp;`-chained test run
      here would block progress to the task that creates the file. Task 3's verify is the real
      gate for this module's behaviour.
  </acceptance_criteria>
  <done>The pure module compiles, exports the full contract, contains no clock/DOM/storage/'original' token, and Task 3's tests pass against it.</done>
</task>

<task type="auto">
  <name>Task 3 (23-01/T3): Create trends-zoom-logic.test.ts</name>
  <files>src/dashboard/views/trends-zoom-logic.test.ts</files>
  <read_first>
    - src/dashboard/views/trends-zoom-logic.ts (the module just written - assert against what it actually exports)
    - src/dashboard/views/trends-volume-logic.test.ts (import style, fixture style, and the "never throws on malformed input" idiom at lines 72-89)
    - src/dashboard/views/trends-training-load-logic.test.ts (describe/it structure for a pure date-window module)
  </read_first>
  <action>
    Create `src/dashboard/views/trends-zoom-logic.test.ts` mirroring
    `trends-volume-logic.test.ts`'s conventions exactly: `import { describe, expect, it } from
    'vitest'`, named imports from `'./trends-zoom-logic.js'` (with the `.js` extension), and
    `describe` blocks grouped by behavioural claim rather than by function name.

    Use these five describe headings so the VALIDATION.md `-t` filters resolve:
    `computeDefaultWindow`, `computeLimits`, `panDeltaPx and zoomFactor`, `formatRangeLabel`,
    `restoreOrDefault`. Add two more: `modifierKeyForPlatform` and `loadWindowRange`.

    Use the real archive bounds as the fixture, so a regression is expressed in dates a human can
    check against the published data:
    - weekly bounds: `Date.parse('2011-08-15T00:00:00.000Z')` to
      `Date.parse('2026-08-10T00:00:00.000Z')` (486 entries in `data/stats/weekly-distance.json`)
    - monthly bounds: `2011-08-01` to `2026-08-01` (124 entries)
    - yearly bounds: `2011-01-01` to `2026-01-01` (14 entries)
    - training-load bounds: `2011-08-16` to `2026-08-11` (5,475 days)

    Assert every claim in this task's sibling `<behavior>` block, by value. Specifically pin these
    exact expected outputs, all computed in advance from the constants in Task 2:
    - weekly default window: `2025-08-13T12:00:00.000Z` to `2026-08-13T12:00:00.000Z`
    - weekly full range: `2011-08-11T12:00:00.000Z` to `2026-08-13T12:00:00.000Z`
    - monthly default window: `2021-08-15T23:15:00.000Z` to `2026-08-16T05:15:00.000Z`
    - yearly default window equals the yearly full range: `2010-07-02T09:00:00.000Z` to
      `2026-07-02T15:00:00.000Z`
    - training-load 12mo default: `2025-08-11T12:00:00.000Z` to `2026-08-11T12:00:00.000Z`
    - `formatRangeLabel` on the weekly default: `'Aug 2025 to Aug 2026'`
    - `withRangeSuffix('Weekly distance chart', ...)`: `'Weekly distance chart, Aug 2025 to Aug 2026'`

    Add one anti-regression assertion that states TRN-01's outcome directly rather than a
    mechanism: the weekly default window's `min` is strictly greater than the weekly archive
    start, and `formatRangeLabel` over the weekly default window does NOT contain `'2011'`. That
    single assertion is what would go red if D-06 were ever quietly reduced to "the capability
    ships but the opening picture is unchanged".

    Add a `never throws on malformed input` describe block in the shape of
    `trends-volume-logic.test.ts:72-89`, covering: empty array, `[NaN, Infinity]`, a `saved` range
    with `min > max`, `panDeltaPx(0, 'earlier')`, `panDeltaPx(NaN, 'later')`, and
    `formatRangeLabel(NaN, NaN)`.

    Do not test `zoomHintText`'s exact glyph beyond asserting the two variants differ and each
    contains `'scroll to zoom'` - the copy is Claude's Discretion and pinning the full string
    would make a wording tweak a test failure for no behavioural reason.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/trends-zoom-logic.test.ts &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/views/trends-zoom-logic.test.ts` is green and reports at least 20 assertions across at least 7 describe blocks.
    - Each of these resolves to at least one test: `npx vitest run src/dashboard/views/trends-zoom-logic.test.ts -t "computeDefaultWindow"`, `-t "computeLimits"`, `-t "panDeltaPx"`, `-t "formatRangeLabel"`, `-t "restoreOrDefault"`.
    - The five exact ISO expected values above appear literally in the test file: `grep -c "2025-08-13T12:00:00.000Z\|2021-08-15T23:15:00.000Z\|2010-07-02T09:00:00.000Z\|2025-08-11T12:00:00.000Z\|2026-08-13T12:00:00.000Z" src/dashboard/views/trends-zoom-logic.test.ts` prints at least `5`.
    - The TRN-01 outcome assertion exists: the file contains an assertion that the weekly default label does not contain `2011`.
    - Mutation check, executed and then reverted, recorded in the summary: temporarily change `DEFAULT_SPAN_MS['volume-weekly']` in `trends-zoom-logic.ts` to `null`; the suite must go RED on the weekly-default and no-2011 assertions. Revert and confirm green again.
    - `npm test` (full suite) is green.
  </acceptance_criteria>
  <done>Every D-06/D-09/D-12/D-13/D-14/D-22 behaviour has a by-value assertion, the mutation check proved the weekly-default assertions are load-bearing, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to build output | Two new third-party packages enter the published client bundle |
| Browser input events to chart state | Synthetic wheel/pointer/click events drive scale bounds (no free-text input newly introduced) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-SC | Tampering | `chartjs-plugin-zoom` + `hammerjs` install (Task 1) | mitigate | Legitimacy already established in 23-RESEARCH.md (registry age, download counts, non-archived repos, empty `scripts.postinstall`, `slopcheck [OK]` for both). Versions are pinned by `package-lock.json`. `npm audit` is run at install; a NEW advisory naming either package blocks the task. `hammerjs`'s unmaintained status (no release since 2022-11-18) is a knowingly-accepted, recorded cost per D-16 - no maintained fork or pointer-events alternative exists. |
| T-23-V5 | Tampering | zoom range as externally-supplied input | accept | D-24 declines persistence and no decision asks for URL-encoded zoom state, so no externally-supplied range crosses a boundary in this phase. Recorded here so a later phase does not add one silently: if zoom range ever becomes URL-shareable, it MUST go through the same allow-list-and-clamp discipline `parseTrendTab` / `parseVolumeGranularity` / `parseLoadWindow` already use, clamped through `computeLimits`' own bounds - never trusted raw. |
| T-23-DOS | Denial of Service | wheel/pan event thrash | accept | Mitigated by the plugin itself (250 ms debounce on the wheel settle path, `pan.threshold`); no application-level control needed. |

No new authentication, session, access-control or cryptography surface: Trends is an already-public, read-only client view over static JSON (23-RESEARCH.md Security Domain, ASVS V2/V3/V4/V6 all Not Applicable, V5 Marginal).
</threat_model>

<verification>
- `npm test` green (full suite).
- `npx tsc --noEmit` clean.
- `npx vitest run src/dashboard/views/trends-zoom-logic.test.ts` green with the five `-t` filters from `23-VALIDATION.md` all resolving to real tests.
- No source file imports `chartjs-plugin-zoom` or `hammerjs` yet.
</verification>

<success_criteria>
- `chartjs-plugin-zoom@^2.2.0` and `hammerjs@^2.0.8` are declared and installed (D-16).
- `trends-zoom-logic.ts` exists, compiles, exports the full documented contract, and contains no clock read, no DOM access, no storage access and no `'original'` sentinel.
- `trends-zoom-logic.test.ts` pins every constant this phase's browser checkpoint will read back, by value, including the weekly default window `2025-08-13T12:00:00.000Z` to `2026-08-13T12:00:00.000Z` and its label `Aug 2025 to Aug 2026`.
- The mutation check proved the D-06 assertions fail when D-06 is removed.
</success_criteria>

<output>
Create `.planning/phases/23-trends-zoom-pan-taller-bands/23-01-SUMMARY.md` when done.
Record in it: the installed versions, the `npm audit` summary line, the two D-16 scope
corrections, the mutation-check result, and the exact expected-value table the browser checkpoint
in plan 23-07 will read back.
</output>
