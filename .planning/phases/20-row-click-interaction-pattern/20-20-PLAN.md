---
phase: 20-row-click-interaction-pattern
plan: 20
type: execute
wave: 15
depends_on: ["20-19"]
files_modified:
  - .planning/phases/20-row-click-interaction-pattern/20-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements: [UX-01, UX-02, UX-03, REC-08]
must_haves:
  decisions: [D-02, D-06, D-10, D-12, D-13, D-14, D-16, D-17]
  truths:
    - "R31 and R32 are re-tested against the shipped D-16 fix, by a human hand for the drag, with what the fix cannot close put to the developer as an explicit disposition question rather than assumed"
    - "The modifier-click behaviour D-16 must not break is re-tested on both Records tables, because a preventDefault() fed the wrong context would cancel it invisibly"
    - "What each cell anchor of one Records row announces after D-17 is observed cell by cell, including the Flags cell announcing its own badge text"
    - "R25's Shift+click-opens-a-new-window half is answered by the developer's own eyes, which is the only observer that can see a second browser window"
    - "R28's disposition question is answered in writing with an explicit yes or no, without needing hardware to perform the gesture"
    - "R29 and R33 are re-asked as separately-labelled sub-questions, so an un-itemized answer is visibly incomplete instead of silently accepted"
    - "The checkpoint runs against a bundle proven to be the one on disk, from 127.0.0.1, so a stale cached asset cannot masquerade as a product defect"
  artifacts:
    - path: ".planning/phases/20-row-click-interaction-pattern/20-VALIDATION.md"
      provides: "A Round 5 ten-row agenda with one named verdict, one required detail and one named observer per row, appended below Rounds 1-4"
      contains: "Round 5"
    - path: ".planning/REQUIREMENTS.md"
      provides: "UX-01/UX-03 status gated on the Round 5 row-to-requirement map, with Rounds 3 and 4's passes counted as carried forward"
      contains: "UX-01"
  key_links:
    - from: "plan 20-19's D-16 anchor link contract"
      to: "rows R34, R35, R36"
      via: "drag-select, double-click and modifier-click on the same Records cells"
      pattern: "R5-VERDICT"
    - from: "plan 20-19's D-17 label removal"
      to: "row R38"
      via: "per-cell screen-reader announcement of one Records row"
      pattern: "R5-VERDICT"
    - from: "the served bundle filename"
      to: "every FAIL verdict in this round"
      via: "a disk-versus-served hash comparison recorded in the preamble"
      pattern: "assets/index-"
---

<objective>
Run the Round 5 human browser checkpoint that discharges Phase 20's success criterion 4, on the full
agenda the operator asked for rather than a fix-scoped one.

Purpose: `20-VERIFICATION.md` has scored 1/4 twice. SC4 is failed on the checkpoint's own recorded
words — `20-VALIDATION.md` Round 4 frontmatter reads `status: blocked`, `nyquist_compliant: false`,
and its Checkpoint Outcome section states "OVERALL ROUND 4 VERDICT: FAIL." Three things keep it
there. GAP 12's two FAIL rows (R31 drag-select, R32 double-click) are answered by plan 20-19's D-16
fix and must be re-tested against it. CR-01's identical-`aria-label` defect is answered by D-17 and
has never been observed at all. And three rows were left unresolved for reasons that are not code
defects: R25's Shift+click new-window half is outside the agent's observable surface, R28's
disposition question was asked and not answered, and R29 and R33 were recorded PASS on an
un-itemized "looks good" that their own Test Instructions did not permit.

Output: a Round 5 section in `20-VALIDATION.md` carrying ten rows, each with its own named verdict,
its own required detail and its own named observer, with Rounds 1, 2, 3 and 4 left exactly as they
stand.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md
@.planning/phases/20-row-click-interaction-pattern/20-VERIFICATION.md
@.planning/phases/20-row-click-interaction-pattern/20-VALIDATION.md
@.planning/phases/20-row-click-interaction-pattern/20-REVIEW.md
@.planning/phases/20-row-click-interaction-pattern/20-19-SUMMARY.md

<house_rules>
Non-negotiable, established across checkpoints 16-09, 17-15, 19-05, 19-12, 19-17, 20-05, 20-08,
20-11 and 20-18:

- **One named verdict per row, and the row's own required detail with it.** A blanket statement
  covering several rows is not evidence. Round 4 caught itself doing a lighter version of this on
  R29 and R33 and recorded the caveat honestly; Round 5 closes it by construction.
- **Rows are presented ONE AT A TIME**, never as a numbered list of ten.
- **Every row names its observer** in its Observation cell, using one of exactly three phrasings:
  "observed by developer", "observed by agent (browser automation against the staged build)", or
  "observed by developer, corroborated by agent".
- **Rows marked "Observer required: developer's own eyes" cannot be answered by agent automation.**
  If the developer declines, the row is recorded BLOCKED with the decline quoted — not settled by a
  computed-style read or a synthetic event dispatch.
- **A theme-sensitive row can only PASS if both the light and the dark theme are named.**
- **A failing row is recorded verbatim and left unpatched.** No suggested fix, no root-cause theory
  for a still-failing row. The next planning round owns diagnosis.
- **Never cite an automated result as evidence for a manual row.** `npm test` cannot observe a drag,
  a tab stop, an announcement, or what a Cmd+click does.
- **Serve under `/strava-widgets`, never at the server root.**
- **Rounds 1, 2, 3 and 4 are a historical record.** Do not edit, reword, re-verdict or delete any of
  their rows, Observation cells, Checkpoint Outcome sections, Evidence Quality notes or Gap-Closure
  Records, including GAP 12. Round 5 is appended below all of them.
- **The four-state verdict vocabulary is kept:** PASS, FAIL, BLOCKED, NOT EXERCISABLE. BLOCKED means
  the observation could not be produced for a dataset or environment reason; NOT EXERCISABLE means
  the gesture itself could not be performed. Neither is a defect verdict.
- **"I could not tell" and "I would rather not" are legitimate answers**, recorded as given.
</house_rules>

<environment_trap>
**The staged-build cache trap, recorded in Round 4's own Evidence Quality section and repeated here
because it cost that round most of a session.**

Chrome's HTTP cache silently served a stale `index.html` (pointing at the superseded bundle
`assets/index-BhoIiIQ_.js`, which lacked D-13) and a stale `data/dashboard/index.json`. Both failure
modes look exactly like product defects: stale JS makes new markup appear missing, stale JSON makes
fixture-dependent UI absent. Round 4 recorded two false failure appearances before proving them to
be caching artifacts.

Two rules follow, both binding on this plan:

1. **Load the app from `http://127.0.0.1:8099/strava-widgets/`, not `localhost`.** A different host
   string is a different cache origin, which is what finally gave Round 4 a clean load. Every URL in
   the agenda, the `what-built` block and the `how-to-verify` block uses `127.0.0.1`.
2. **Confirm the loaded bundle is the one on disk before recording any FAIL verdict.** Task 1
   records both filenames in the Round 5 preamble; Task 2 re-confirms the served page references the
   same one before any row is judged. The Round 4 bundle was `assets/index-CkFhsgc3.js`; plan 20-19
   changed `records.ts`, so the Round 5 bundle filename **must differ** from that one. A served
   bundle whose name matches Round 4's is a stale asset, not a shipped regression.
</environment_trap>

<carried_forward>
Not re-asked this round, because nothing this round changed touches them and they are already
resolved on individually-described evidence:

- **From Round 4:** R26 and R27 (the fixture-induced `No HR` Overview Recent Activities row — one
  Tab stop, no CTA button, both themes, badge text in the accessible name). Both PASS; UX-02 is
  closed on them. Their fixture is **not** re-applied this round and no Round 5 row depends on it.
- **From Round 3, carried forward a second time:** R1, R3, R4, R5, R6, R8, R10, R11, R12, R13, R14,
  R15 and R17, all PASS, all unaffected by D-16 and D-17. See each row's own Round 3 (or Round 2)
  Observation cell.

Re-asked, and why each returns:

- **R31 → R34** and **R32 → R35**: both FAIL in Round 4, both answered by D-16.
- **R23 / R24 → R36**: both PASS in Round 4, re-asked because D-16 adds a `preventDefault()` to the
  same anchors and a wrongly-fed context would cancel exactly this behaviour, invisibly to every
  automated check in the repository.
- **R25 → R37**: BLOCKED in Round 4 on the agent's observation limit, never on a defect.
- **R28 → R39**: NOT EXERCISABLE in Round 4, and its disposition question was asked and left
  unanswered. The question half is answerable without hardware, so this round asks only that half as
  the passing condition.
- **R29 → R40** and **R33 → R41**: PASS in Round 4 on the developer's own-eyes "looks good", with the
  itemized detail their own rows demanded absent from the record — Round 4's Evidence Quality
  section says so in its own words. They return as separately-labelled sub-questions.
- **R22 → R42** and **R30 → R43**: both PASS in Round 4, re-asked as regressions because plan 20-19
  edited `records.ts` (R43's one-stop-per-row invariant lives there) and because R42 is the only
  rendered evidence for a CRITICAL that once shipped behind 1062 green tests.
- **CR-01 → R38**: never observed by any round. D-17 is the fix; R38 is its only rendered evidence.
</carried_forward>

<facts_the_agenda_depends_on>
Verified against source and plan 20-19, not assumed:

- **Records PR table, six columns:** Rank, Time, Pace, Age-Grade, Date, Flags. Every
  content-carrying cell holds an `<a href="#/activity/{id}">`. The Date anchor is the row's only
  keyboard stop and the only one rendered as a visible underlined link; the others carry
  `class="pr-table__cell-link"` and `tabindex="-1"`, and after plan 20-19 also `draggable="false"`
  and no `aria-label` at all. A flag-less row's Flags cell has no anchor.
- **Records PR-progression table, three columns:** Date, Time, Improvement. Time and Improvement
  carry the same cell links.
- **D-16 (plan 20-19)** suppresses the anchor's default navigation when the shared predicate refuses
  — an active text selection (D-12) or a repeat click with `MouseEvent.detail` above 1 (D-14). It
  deliberately does **not** feed the modifier keys or the button into that decision, because
  `preventDefault()` would then cancel the browser's own new-tab and new-window gestures. R36 is the
  rendered evidence that it does not.
- **What D-16 cannot close, stated so row R35 is asked honestly:** `MouseEvent.detail` is 1 on the
  **first** click of a double-click, so at the moment it fires it is indistinguishable from a single
  click and the browser performs the anchor's default navigation. Only a navigation delay could
  suppress it, which `row-navigation.test.ts` forbids by assertion and which would make every single
  click feel broken. A double-click on these cells therefore navigates on the first click, exactly
  as it does on the Date cell, on the Activities table's Activity-cell link, and on every link on
  the web. R35 puts that to the developer as a disposition question.
- **D-17 (plan 20-19)** removed the `aria-label` from all seven cell anchors; only the Date anchor
  keeps one. Each other anchor's accessible name is now its own visible text, and the Flags anchor's
  is its badge text.
- **The status badge texts** a Records Flags cell can carry: `Low confidence`, and
  `Excluded — <reason>` where the reason comes from `data/best-effort-exclusions.json`.
- **`.activity-table__row--navigable { cursor: pointer }`** still comes from `attachRowNavigation`,
  which plan 20-19 did not touch — the row hover and pointer are unchanged.
- **The four tables that must show no pointer and no hover** (D-10): the Riegel race-predictions
  table at `#/records`, the gear/shoe table at `#/trends`, the other `#/trends` table, and the
  best-efforts table on any activity detail view.
- **The Activities zero-match empty state** is reached by typing a search term that matches nothing
  in the `#/list` search box — that is the empty-filter render R42 needs.
</facts_the_agenda_depends_on>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full gate, cache-proof staging, the Flags-badge fixture, and the ten-row Round 5 agenda</name>
  <files>.planning/phases/20-row-click-interaction-pattern/20-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/20-row-click-interaction-pattern/20-VALIDATION.md` in full — the frontmatter,
      the Test Infrastructure section, and every one of Rounds 1, 2, 3 and 4 including Round 4's
      twelve-row table, its Checkpoint Outcome, its Gap-Closure Record with GAP 12 and the
      disposition of GAPs 9/10/11, and its Evidence Quality section with the staged-build cache
      caveat. All of it stays; you are appending.
    - `.planning/phases/20-row-click-interaction-pattern/20-18-PLAN.md` Task 1 Parts 1 and 2 — the
      gate and staging procedure and the redirect-and-capture rule for the build log, which has now
      worked four times.
    - `.planning/phases/20-row-click-interaction-pattern/20-19-SUMMARY.md` — what shipped, and
      specifically its statement of what D-16 closes and what it does not, which row R35 is written
      around.
    - `.planning/phases/20-row-click-interaction-pattern/deferred-items.md` — the five pre-existing
      data-dependent test files and the `verify-dashboard` stale-data failure mode, so a
      known-environment failure is not mistaken for a regression.
    - `src/dashboard/views/records.ts` `buildPrTable`'s Flags cell — the two conditions
      (`row.lowConfidence`, and `row.excluded && row.exclusionReason`) that make a Flags badge exist
      at all, which is what row R38 needs a row for.
    - `data/best-effort-exclusions.json` — its schema (`schemaVersion`, `note`, `exclusions[]` with
      `activityId`, `distances`, `reason`), and the fact that `records.ts` fetches it at runtime from
      `data/best-effort-exclusions.json`, so a staged-copy edit takes effect without re-running the
      pipeline.
  </read_first>
  <action>
**Part 1 — run the full gate from a clean working tree** and record every exit code and count:
`npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets`, `npm run verify-dashboard`.
All four must exit 0 before the checkpoint may open, and `build-widgets` must emit zero
`css-syntax-error` occurrences — that warning is the build-time signature of Phase 19's GAP 1 comment
defect and was misclassified as non-blocking once already.

Record the total test count and compare it against Round 4's recorded 1062/1062 across 49 files: the
count must have grown (plan 20-19 added a describe block with self-tests and two blind-spot proofs)
and no previously-passing file may fail.

Use the redirect-and-capture form: write the build log to a file, test the build's own exit status,
then grep the file separately. Do not rewrite this as a pipeline with `tee` — the developer's shell
is zsh, where `PIPESTATUS` is `pipestatus` and 1-indexed.

If `npm test` or `verify-dashboard` fails on stale or absent generated data rather than on anything
this round changed, run `npm run build` and `node dist/index.js compute-all-stats` first, and record
that you did. A failure confined to the five files named in `deferred-items.md` is an environment
gap, not a regression — say which it was.

**Part 2 — stage the built output under the production-shaped path.** Capture the repository root
into a variable while still inside the repository, create `/tmp/gh-pages`, create the symlink
`/tmp/gh-pages/strava-widgets` pointing at the absolute `dist/widgets` path under that captured root,
confirm `/tmp/gh-pages/strava-widgets/index.html` resolves, then change directory to `/tmp/gh-pages`
and serve port 8099 with `python3 -m http.server`. A bare server root is not acceptable and no
package may be installed to serve it.

**Part 3 — prove the served bundle is the one on disk, before anything else is observed.** Read the
hashed bundle filename out of `dist/widgets/index.html` (the `assets/index-*.js` reference). Then
fetch `http://127.0.0.1:8099/strava-widgets/index.html` and confirm it references the **same**
filename. Assert explicitly that the filename is **not** `assets/index-CkFhsgc3.js` — that was Round
4's bundle, and plan 20-19 changed `records.ts`, so an identical content hash means a stale asset is
being served. Record both filenames verbatim in the Round 5 preamble. See this plan's
`<environment_trap>` block: use `127.0.0.1`, never `localhost`, for every URL in this round.

**Part 4 — make sure a Flags badge exists to announce, and disclose it if it does not exist
organically.** Row R38 asks what the Flags cell announces, which requires a PR-table row whose Flags
cell actually carries a badge.

First determine whether one exists organically: load `http://127.0.0.1:8099/strava-widgets/#/records`
and look for the strings `Low confidence` or `Excluded —` inside a `.pr-table`. If one exists, record
which distance table, which row and which badge string, and apply no fixture.

If none exists, apply the smallest possible disclosed fixture, in exactly the register Round 4 used
for its `No HR` edit:

- Touch **only** `dist/widgets/data/best-effort-exclusions.json` — the staged copy. Both that path's
  parent and `data/` are gitignored for generated output, and the repository copy at
  `data/best-effort-exclusions.json` must not be modified; `git status --porcelain data src scripts`
  must be empty afterwards.
- Add exactly one entry to the `exclusions` array, for an activity that already appears in a PR
  table (Round 4's evidence names `4556693525`, row 1 of the 400m PR table, as a live example), with
  `distances: null` and a `reason` string that says plainly it is a checkpoint fixture. The rendered
  badge is then `Excluded — <reason>`.
- Confirm the badge actually renders before opening the checkpoint, from
  `http://127.0.0.1:8099/strava-widgets/#/records`. If it does not, the fixture failed and R38 is
  recorded BLOCKED for the Flags half — say so rather than proceeding as if it worked.
- Record the edit verbatim in the Round 5 preamble: the file, the activity id, the field, the
  resulting badge string, the statement that it is fixture-induced rather than organic archive data,
  and that the next `npm run build-widgets` overwrites it.

Also state in the preamble that Round 4's `No HR` Overview fixture is **not** re-applied this round
and that no Round 5 row depends on it — R26 and R27 are carried forward, not re-asked.

**Part 5 — append a `## Round 5 - Re-run Verifications (gap closure)` section to
`20-VALIDATION.md`**, below everything already in the file. Nothing above that heading may change
except the frontmatter.

Frontmatter: set `status: pending`, keep `nyquist_compliant: false`, change `round: 4` to `round: 5`,
leave every `round2_*`, `round3_*` and `round4_*` key untouched, and add a `round5_staged` date.

Open the section with a short paragraph stating why Round 5 exists: `20-VERIFICATION.md` scored 1/4
for a second consecutive round; GAP 12's R31/R32 were recorded FAIL and left unpatched; `20-REVIEW.md`
found CR-01, an identical `aria-label` on every cell anchor in a row, which no round had ever
observed; R25's new-window half, R28's disposition question and R29/R33's un-itemized answers were
all left open for reasons that were not code defects. State that decisions D-16 and D-17 were locked
in response and that plan 20-19 implemented them, and state plainly what D-16 does not close (the
first click of a double-click, `MouseEvent.detail` 1) so row R35 is asked honestly. State that
fifteen rows are carried forward and not re-asked (R26 and R27 from Round 4; R1, R3, R4, R5, R6, R8,
R10, R11, R12, R13, R14, R15, R17 from Round 3), and that Rounds 1, 2, 3 and 4 are preserved unedited
above. Include the bundle-freshness record from Part 3 and the fixture disclosure from Part 4.
**Write this paragraph without using the literal tokens `R2-VERDICT`, `R3-VERDICT` or
`R4-VERDICT`.**

Immediately after it, add a **Carried forward (not re-asked)** list naming those fifteen rows with a
one-line reason each, pointing at their own Observation cells. Do not put them in the table.

Then a ten-row table in the same five-column shape Rounds 1-4 use: Behavior, Requirement, Why Manual,
Test Instructions, Observation. Use the verdict token `R5-VERDICT:` so the five rounds can never be
miscounted together. **Every Observation cell contains exactly a pending sentinel and nothing else**;
Task 2 fills them.

Every Test Instructions cell must end with two clauses in this order: a **"Required detail:"**
sentence naming exactly what the answer must contain, and an **"Observer required:"** sentence
reading either `developer's own eyes` or `developer or agent (the Observation cell must name which)`.

Table order and content, exactly:

- **R34. Drag-selecting text inside a Records cell selects the text.** UX-01, UX-03 (D-12, D-16).
  Why manual: whether a drag starting on an anchor becomes a text selection or a link drag is a
  rendered browser fact, and Round 4's FAIL was produced by a synthetic drag whose own record asked
  for a human re-test. Instructions: at `http://127.0.0.1:8099/strava-widgets/#/records`, press the
  mouse down inside a Pace or Age-Grade cell of a PR table, drag across the text, release inside the
  row. The text must stay selected, no link-drag ghost image may appear, and the page must stay on
  `#/records`. Press Cmd+C and paste somewhere to confirm what was captured. Round 4 recorded FAIL: a
  `dragstart` fired on the cell anchor, no text was selected. Required detail: the text you selected
  quoted exactly, whether a drag ghost appeared, what Cmd+C actually captured, and confirmation the
  URL is still `#/records`. Observer required: developer's own eyes (an agent corroboration may be
  recorded alongside using the "observed by developer, corroborated by agent" phrasing).
- **R35. Double-clicking a Records cell value — what happens now, and the disposition.** UX-01, UX-03
  (D-14, D-16). Why manual: the browser fires the first click of a double-click before any word
  selection exists, so only a real double-click shows what the shipped behaviour is. Instructions:
  at `#/records`, double-click a Pace or Time value on a PR-table row and report exactly what
  happened — whether the word ended up selected, and what the URL was afterwards. Then answer the
  disposition question in writing. The question, stated in full: D-16 suppresses the anchor's default
  for a click that ends a drag-selection and for the second click of a double-click
  (`MouseEvent.detail` 2), but it cannot suppress the **first** click, whose `detail` is 1 and which
  is indistinguishable from a single click at the moment it fires; the only mechanism that could is a
  navigation delay, which `row-navigation.test.ts` forbids by assertion and which would make every
  single click on the app feel sluggish. So a double-click on these cells navigates on its first
  click — exactly as it does on the Date cell, on the Activities table's Activity-cell link, and on
  every link on the web. **Do you accept that as the shipped behaviour for these cells (yes), or do
  you want it escalated (no) — escalation meaning a future round revisits D-13's decision to make
  these cells real links at all?** Required detail: the value you double-clicked quoted exactly,
  whether it ended up selected, the URL afterwards, and an explicit yes or no to the disposition
  question. Observer required: developer or agent for the observation half; the disposition answer
  must come from the developer, and the Observation cell must name who supplied each half.
- **R36. A modified click on a non-Date Records cell still opens a background tab, on both tables.**
  UX-01, UX-03 (D-13, D-16). Why manual: whether a modified click opens a tab or is swallowed is a
  rendered browser fact, and this row exists specifically because D-16 added a `preventDefault()` to
  these same anchors — if that guard were ever fed the real modifier keys it would cancel the
  browser's own new-tab gesture, and this row is the only evidence in the project that it does not.
  Instructions, two parts answered separately: (a) at `#/records`, Cmd+click (Ctrl+click on a non-Mac
  keyboard) a **Rank, Time, Pace or Age-Grade** cell of a PR table — not the Date cell; (b) open a
  distance's PR-progression table and Cmd+click its **Time** or **Improvement** cell. In both, a new
  background tab must open on that activity and the tab you clicked in must still show `#/records`.
  Required detail: which cell you clicked in each of (a) and (b), the URL of each new tab (each must
  end `#/activity/<id>`), and confirmation the original tab did not navigate in either case.
  Observer required: developer or agent.
- **R37. Shift+click opens a new window; Alt+click does not navigate in place.** UX-01, UX-03 (D-13,
  D-16). Why manual: Round 4 recorded this BLOCKED because the agent's browser tooling is scoped to
  its own tab group and cannot see whether a second browser window appeared. That is an observation
  limit, not a defect, and only a person looking at their own screen can close it. Instructions: at
  `#/records`, on any non-Date cell of either table, Shift+click one — a separate browser window must
  open on that activity. Then Alt+click one — whatever the browser chooses to do, the current window
  must not navigate to the activity. Required detail: which cell you used for each gesture, whether a
  separate browser window actually appeared for the Shift+click and what it was showing, and what
  the original window was still showing after each of the two clicks. Observer required: developer's
  own eyes.
- **R38. What each cell of one Records row announces, after D-17.** UX-03 (D-17, CR-01). Why manual:
  this repository has no way to compute an accessible name, and no round has ever observed these
  cells announced. Until plan 20-19, every non-Date anchor in a row carried the Date cell's label
  verbatim, so a screen reader announced the same phrase five or six times per row and the Flags
  cell's badge text was discarded entirely. Instructions: with VoiceOver (Cmd+F5) at `#/records`,
  move through the cells of the single PR-table row named in this section's preamble as carrying a
  Flags badge, and report what is announced for each of its six cells: Rank, Time, Pace, Age-Grade,
  Date and Flags. Six answers, one per cell. The Flags cell must announce its own badge text, not the
  date. If you would rather not run VoiceOver, say so — the agent will instead read each anchor's
  computed accessible name off the rendered row and the Observation cell will record that
  substitution explicitly, exactly as R27 did in Round 4. Required detail: six strings quoted
  verbatim, labelled with the cell each belongs to, and a statement that no two of them are
  identical. Observer required: developer or agent (the Observation cell must name which, and must
  say if VoiceOver was declined).
- **R39. Middle-click, and the D-12/D-13 disposition question Round 4 left unanswered.** UX-03 (D-12,
  D-13, D-16). Why manual: `auxclick` behaviour cannot be observed by any test in this repository,
  and the disposition is a decision, not a measurement. Instructions: the gesture is optional this
  round and the question is not. Round 4 recorded the gesture NOT EXERCISABLE (no middle button
  available) **and** left the question unanswered, which is why GAP 11 is still open. Answer both
  numbered questions in writing: **(1)** does D-12's "`auxclick` is deliberately not handled" clause
  still stand — nothing is synthesised and no `auxclick` handler exists in this codebase — yes or no?
  **(2)** is that clause still load-bearing now that every content-carrying Records cell carries a
  real anchor the browser can middle-click natively — yes or no? If you do have a mouse with a middle
  button, also middle-click a non-Date Records cell and report what happened. Required detail:
  whether the gesture was performed at all and its outcome if so, plus an explicit yes or no to each
  of the two numbered questions. This row can be recorded PASS on the two written answers alone, with
  the gesture recorded as not exercisable. Observer required: developer's own eyes.
- **R40. The Records tables still look right — four separate answers.** UX-03 (D-06, D-13, D-16,
  D-17). Why manual: whether seven anchors per row changed the tables' appearance is a perceptual
  question, and this repository's only check is that a CSS rule's text exists. Round 4 recorded this
  PASS on the developer's verbatim "looks good", with none of the four things its own row asked for
  answered. Instructions: at `#/records`, look at a PR table and a PR-progression table and answer
  these four separately, labelled (a) to (d): **(a)** is any Rank, Time, Pace, Age-Grade, Flags or
  Improvement value underlined, or in a different colour from the surrounding text — yes or no;
  **(b)** does the Date cell still read as a link — yes or no; **(c)** does hovering a row still
  light the whole row and show a pointer cursor — yes or no; **(d)** name the two themes you checked,
  individually, and say whether (a), (b) and (c) held in each. Required detail: four separately
  labelled answers (a), (b), (c) and (d), with both theme names spelled out in (d). An answer that
  does not address all four separately is recorded as insufficient evidence. Observer required:
  developer's own eyes.
- **R41. Non-activity tables and link legibility — six separate answers.** UX-03 (D-06, D-10). Why
  manual: `20-VERIFICATION.md` recorded that these claims are inherently about legibility and paint;
  Round 3 settled them by computed-style reads and Round 4 by an un-itemized "looks good". Neither is
  the itemized human judgment the row asks for. Instructions, answered as six labelled parts: for
  each of **(a)** the Riegel race-predictions table at `#/records`, **(b)** the gear/shoe table at
  `#/trends`, **(c)** the other table at `#/trends`, and **(d)** the best-efforts table on any
  activity detail view — does hovering a row show a pointer cursor (yes or no) and does the row light
  up (yes or no)? None of the four should do either. Then **(e)** in your own words, how do the nav
  brand ("Training Dashboard", top left) and the "‹ Newer" / "Older ›" links on an activity detail
  view read — normal text colour and legible, browser-default blue, or invisible? And **(f)** name
  the two themes you checked, individually, and say whether (a) to (e) held in each. Required detail:
  six separately labelled answers, all four tables named by name in (a) to (d) with pointer and hover
  stated for each, your own words in (e), and both theme names in (f). Observer required: developer's
  own eyes.
- **R42. A visit to a detail view still does not steal focus on a later, unrelated list render.**
  UX-01, UX-03. Why manual: module state leaking across two navigations is a rendered-sequence fact
  no text assertion can see; this is the only rendered evidence for a CRITICAL that once shipped
  behind a fully green suite and 21 genuine browser rows. Instructions: open any activity detail view
  from `#/list`; go back to `#/list`; type a search term that matches nothing so the "no activities"
  empty state appears; clear the search so rows return. No row may be highlighted, no row may be
  scrolled to, and keyboard focus must be on the page heading, not on a row. Required detail: the
  name of the activity whose detail view you opened, and what the page did after clearing the filter
  — highlight, scroll or focus, or none of the three. Observer required: developer or agent.
- **R43. Keyboard tab order through the Records tables is still one stop per row.** UX-03 (D-13,
  D-16, D-17). Why manual: tab-stop count is a rendered-interaction fact, and plan 20-19 edited every
  one of those anchors. Instructions: at `#/records`, click the page background and Tab forward into
  a PR table. Each row must take exactly one Tab press — landing on the Date link — not six. Press
  Enter on one and confirm it opens that activity. Repeat in a progression table. Required detail:
  how many Tab presses moved you from one row to the next in each table, and the activity Enter
  opened. Observer required: developer or agent.

Close the section with a note stating: each row needs its own independent verdict, the Required
detail named in its own Test Instructions cell, and its observer named; a blanket statement covering
several rows is not sufficient evidence; any row left individually undescribed — or answered without
its required detail after one re-ask — is recorded as failing for insufficient evidence rather than
manufactured into detail that was never observed; rows R40 and R41 require both the light and the
dark theme to be named; rows R34, R37, R39, R40 and R41 cannot be discharged by agent automation; and
every verdict in this round was taken against the bundle filename recorded in the preamble, loaded
from `127.0.0.1`. Write that note **without using the literal tokens `R5-VERDICT`, `R4-VERDICT`,
`R3-VERDICT` or `R2-VERDICT`**.
  </action>
  <verify>
    <automated>npm test; npx tsc --noEmit -p tsconfig.json && rm -f /tmp/bw20g5.log && npm run build-widgets > /tmp/bw20g5.log 2>&1; BW=$?; cat /tmp/bw20g5.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw20g5.log && npm run verify-dashboard && test -f /tmp/gh-pages/strava-widgets/index.html && readlink /tmp/gh-pages/strava-widgets | grep -q '^/' && node --input-type=module -e "import {readFileSync} from 'node:fs'; const disk=readFileSync('dist/widgets/index.html','utf8'); const m=disk.match(/assets\/index-[A-Za-z0-9_-]+\.js/); if(!m) throw new Error('no hashed bundle reference found in dist/widgets/index.html'); if(m[0]==='assets/index-CkFhsgc3.js') throw new Error('the built bundle is byte-identical to Round 4 - plan 20-19 changed records.ts, so this is a stale build'); const served=readFileSync('/tmp/gh-pages/strava-widgets/index.html','utf8'); if(!served.includes(m[0])) throw new Error('the staged path does not serve the bundle on disk: '+m[0]); console.log('bundle on disk and staged: '+m[0]);" && node --input-type=module -e "import {readFileSync} from 'node:fs'; const p='.planning/phases/20-row-click-interaction-pattern/20-VALIDATION.md'; const s=readFileSync(p,'utf8'); const parts=s.split('## Round 5'); if(parts.length!==2) throw new Error('expected exactly one Round 5 heading'); const before=parts[0]; const v=(before.match(/VERDICT:\s*[A-Z]/g)||[]).length; if(v!==65) throw new Error('Rounds 1-4 altered - expected 65 verdict tokens above Round 5, found '+v); if((s.match(/R2-VERDICT/g)||[]).length!==22) throw new Error('the R2-VERDICT count changed'); if((s.match(/R3-VERDICT/g)||[]).length!==18) throw new Error('the R3-VERDICT count changed'); if((s.match(/R4-VERDICT/g)||[]).length!==12) throw new Error('the R4-VERDICT count changed - Round 5 prose must not use that token and Round 4 must not be edited'); for(const h of ['## Checkpoint Outcome (Round 4)','## Gap-Closure Record (Round 4)','### GAP 12','### Disposition of Round 3','## Evidence Quality (Round 4)']){ if(!before.includes(h)) throw new Error('missing preserved section above Round 5: '+h); } const t=parts[1]; const rows=t.split('\n').filter(l=>l.startsWith('|')&&!/^\|\s*-+/.test(l)&&!/^\|\s*Behavior/.test(l)); if(rows.length!==10) throw new Error('expected 10 Round 5 rows, found '+rows.length); rows.forEach((r,i)=>{ if(r.split('|').length<6) throw new Error('Round 5 row '+(i+1)+' does not have five columns'); }); const ids=['R34','R35','R36','R37','R38','R39','R40','R41','R42','R43']; ids.forEach((id,i)=>{ if(!rows[i].trim().startsWith('| '+id+'.')) throw new Error('Round 5 table row '+(i+1)+' should begin with '+id+'.'); }); if((s.match(/R5-VERDICT/g)||[]).length!==0) throw new Error('Round 5 verdict tokens must be zero before the checkpoint'); const pre=t.split('|')[0]; if(!/Carried forward/.test(pre)) throw new Error('the carried-forward list is missing from the Round 5 preamble'); for(const id of ['R26','R27','R1','R3','R4','R5','R6','R8','R10','R11','R12','R13','R14','R15','R17']){ if(!new RegExp('\\b'+id+'\\b').test(pre)) throw new Error('carried-forward row '+id+' is not named in the Round 5 preamble'); } if(!/assets\/index-/.test(pre)) throw new Error('the bundle-freshness record is missing from the Round 5 preamble'); if(!/127\.0\.0\.1/.test(t)) throw new Error('the Round 5 section must direct the session at 127.0.0.1, not localhost'); if(/localhost:8099/.test(t)) throw new Error('Round 5 must not cite localhost:8099 - see the cache trap'); if(!/detail/i.test(pre)) throw new Error('the preamble must state what D-16 does not close'); const rd=(t.match(/Required detail/g)||[]).length; if(rd!==10) throw new Error('expected one Required detail clause per row, found '+rd); const ob=(t.match(/Observer required/g)||[]).length; if(ob!==10) throw new Error('expected one Observer required clause per row, found '+ob); const fm=s.split('---')[1]||''; if(!/nyquist_compliant: false/.test(fm)) throw new Error('frontmatter verdict is not false'); if(!/round: 5/.test(fm)) throw new Error('frontmatter round is not 5'); if(!/round5_staged/.test(fm)) throw new Error('round5_staged date missing'); if(!/round4_recorded/.test(fm)) throw new Error('round4 frontmatter keys were dropped'); console.log('10 staged Round 5 rows, 0 verdicts, Rounds 1-4 intact, gate green');" && test -z "$(git status --porcelain src scripts data)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets` and `npm run verify-dashboard` all
      exit 0, with counts recorded in the summary. `npm test`'s result is recorded, with the total
      compared against Round 4's 1062/1062 across 49 files; the count must have grown and no
      previously-passing file may fail. Any failure must be confined to the five data-dependent
      files named in `deferred-items.md`, and the summary must say which and why.
    - `npm run build-widgets`'s log contains zero `css-syntax-error` occurrences.
    - `test -f /tmp/gh-pages/strava-widgets/index.html` succeeds and
      `readlink /tmp/gh-pages/strava-widgets` prints an absolute path beginning with the repository
      root.
    - The hashed bundle referenced by `dist/widgets/index.html` is the same one served under
      `/tmp/gh-pages/strava-widgets/index.html`, is **not** `assets/index-CkFhsgc3.js`, and both
      filenames are recorded verbatim in the Round 5 preamble.
    - Either an organic Flags badge is found and its distance table, row and badge string recorded,
      or a one-entry fixture is applied to `dist/widgets/data/best-effort-exclusions.json` only,
      confirmed rendering at `http://127.0.0.1:8099/strava-widgets/#/records`, and disclosed in the
      preamble by file, activity id, field, resulting badge string, and the statement that it is
      fixture-induced and will be overwritten by the next `build-widgets`.
    - The preamble states that Round 4's `No HR` Overview fixture is not re-applied and that no Round
      5 row depends on it.
    - `git status --porcelain src scripts data` is empty — any fixture lives only in the gitignored
      build output.
    - `20-VALIDATION.md` contains exactly one `## Round 5` heading and everything above it is
      unchanged: exactly 65 `VERDICT: <uppercase>` tokens, exactly 22 `R2-VERDICT`, 18 `R3-VERDICT`
      and 12 `R4-VERDICT` occurrences remain, and Round 4's Checkpoint Outcome, Gap-Closure Record,
      GAP 12, the Round 3 gap dispositions and the Evidence Quality section are all still present.
    - The Round 5 table has exactly 10 rows in the five-column shape, each Behavior cell beginning
      with its row id and a period, in the order R34 through R43.
    - All 10 Test Instructions cells contain both a "Required detail:" clause and an "Observer
      required:" clause — 10 occurrences of each literal, no more and no fewer. R34, R37, R39, R40
      and R41 read `developer's own eyes`.
    - Every URL in the Round 5 section uses `127.0.0.1:8099`; the literal `localhost:8099` does not
      appear anywhere in the section.
    - A "Carried forward (not re-asked)" list appears in the preamble naming R26 and R27 plus R1, R3,
      R4, R5, R6, R8, R10, R11, R12, R13, R14, R15 and R17 with a reason each, and none of the
      fifteen appears as a table row.
    - The preamble states what D-16 does not close, in terms of the first click of a double-click, so
      R35's disposition question is not a surprise when it is asked.
    - Every Round 5 Observation cell holds the pending sentinel only; the file contains zero
      `R5-VERDICT` tokens at this point.
    - Frontmatter carries `status: pending`, `nyquist_compliant: false`, `round: 5` and a
      `round5_staged` date, with every `round2_*`, `round3_*` and `round4_*` key untouched.
  </acceptance_criteria>
  <done>The gate is green after plan 20-19, a build proven to be the one on disk is served under /strava-widgets from 127.0.0.1 with a Flags-badge row to announce, and ten rows await verdicts with Rounds 1-4 preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING human browser checkpoint — ten rows, one at a time, each with its own required detail and named observer</name>
  <files>.planning/phases/20-row-click-interaction-pattern/20-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/20-row-click-interaction-pattern/20-VALIDATION.md` — the ten rows staged by
      Task 1 with their Required detail and Observer required clauses, Round 4's Gap-Closure Record
      (GAP 12 and the dispositions of GAPs 9, 10 and 11) which this round is answering, and Round 4's
      Evidence Quality section including the staged-build cache caveat.
    - `.planning/phases/20-row-click-interaction-pattern/20-VERIFICATION.md` — the two `gaps_remaining`
      entries and the SC1/SC3/SC4 gap records, which are what these rows exist to close.
    - `.planning/phases/20-row-click-interaction-pattern/20-19-SUMMARY.md` — what D-16 and D-17
      actually changed, and the residual it records, so row R35's disposition question is put
      accurately and row R36's rationale can be explained if the developer asks.
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` section "Gap-Closure
      Record" — the format for recording a failing row's verbatim wording without a suggested fix.
    - `.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md` decisions D-12, D-13, D-14,
      D-16 and D-17 — so you can answer the developer accurately if they ask why middle-click behaves
      as it does (R39), why only the Date cell is underlined (R40), or why a double-click still
      navigates (R35).
    - `.planning/REQUIREMENTS.md` — the UX-01, UX-02, UX-03 and REC-08 entries and the status table
      this task may update. UX-02 and REC-08 are already Complete; do not un-tick either.
  </read_first>
  <what-built>
Since Round 4, one plan landed. Nothing else changed.

- **Plan 20-19, first half — the Records cells stop fighting you.** Round 4 found two things wrong
  with the links that had just been added to every Records cell. Dragging across a value started
  dragging the *link* instead of selecting the text, so you could not copy a pace off the page any
  more. And a double-click navigated away before the word could select. Both had the same cause: the
  rules that were supposed to prevent this lived on the row, and the browser handles a click on a
  real link itself without ever consulting them. The links are now marked non-draggable, and they
  check the same rulebook the row does before letting the browser navigate.
- **Plan 20-19, second half — every cell says what it actually is.** Every link in a Records row was
  announcing the same phrase — the date cell's text — so a screen reader read "Jan 2, 2021, 400m,
  0:45" six times for one row and never said the rank, the time, the pace or the flag. Now only the
  date cell carries a written-out label; every other cell simply announces its own text, and the
  Flags cell announces its badge again.

**One thing the fix cannot do, said plainly rather than discovered at the checkpoint.** A double-click
still navigates. The first click of a double-click is indistinguishable from a single click at the
moment it happens — the browser only tells us it was a double on the second click — so the only way
to stop it would be to make every single click wait a fraction of a second before doing anything,
which would make the whole app feel sluggish. This is how every link on the web behaves, including
the date cell here and the activity link in the Activities table. Row R35 asks you whether you accept
that or want it escalated, and either answer is fine.

**About the data:** the Flags cell only appears on a row that is flagged. If your archive had none,
one entry was added to the *served copy* of the exclusions file so a `Excluded — …` badge exists for
row R38 to announce. It is a fixture in the built output, not a change to your archive, and it is
recorded as such in the round's preamble.
  </what-built>
  <how-to-verify>
The build is served at **`http://127.0.0.1:8099/strava-widgets/`**. Use that address, not
`localhost` — they are different caches to Chrome, and last round a stale `localhost` cache served an
old copy of the app that looked exactly like two product defects for the first half of the session.
**Confirm the URL bar contains `127.0.0.1:8099/strava-widgets/` before starting.** If it does not,
stop and say so — a root-served build reproduces the class of defect that shipped Phase 16's black
page behind a green gate.

Before recording anything as failing, the agent must confirm the page you are looking at is running
the bundle built from the current source — the filename is recorded in the round's preamble. A stale
asset and a real defect look identical from the screen.

Screens:
- `http://127.0.0.1:8099/strava-widgets/#/records` (Records — rows R34, R35, R36, R37, R38, R39,
  R40, R43)
- `http://127.0.0.1:8099/strava-widgets/#/list` (Activities — row R42)
- `http://127.0.0.1:8099/strava-widgets/#/trends` and any activity detail view (row R41)

**How this round works.** You will be asked **one row at a time**. Each question ends with the
specific thing your answer has to contain and who has to observe it. Ten rows; fifteen from earlier
rounds are already done and are not asked again.

Five things worth knowing before you start:

1. **Five rows need your own eyes and cannot be done by the agent.** R34 (drag to select text — last
   round's drag was synthetic and its own record asked for a human hand), R37 (does a second browser
   *window* open — the agent literally cannot see one), R39 (a written yes/no on middle-click),
   R40 and R41 (does it look right). If you would rather not do one, say so — it is recorded as
   blocked, not talked into a pass.
2. **R40 and R41 are the two rows you answered "looks good" to last round.** That was recorded as
   your verdict and kept, but the round also recorded, honestly, that the specific things those rows
   asked about were never actually stated. They come back this time as four and six separately
   lettered questions. Short answers are fine — "a. no, b. yes, c. yes, d. light and dark, both the
   same" is a complete answer. One word for the whole row is not.
3. **R34 and R35 are the two rows that failed last round**, and R34 is the one to try first: drag
   across a pace value and see whether you get selected text or a ghost of a link following your
   cursor.
4. **R36 is a "did we break what already worked" row.** Cmd+click on a Records cell has opened a
   background tab since last round; the fix in R34/R35 touches the same links, so this confirms it
   still does.
5. **"I could not tell" is a legitimate answer** and is recorded as insufficient evidence rather
   than being talked into a pass.

If anything fails, describe it plainly. It will be recorded verbatim and left unpatched, per the
house rule in force since checkpoint 16-09.
  </how-to-verify>
  <action>
**Present the rows ONE AT A TIME, in this order: R34 first, then R35, R36, R37, R38, R40, R41, R39,
R42, R43.** Do not present the agenda as a numbered list of ten — that is what produced the blanket
answers in Rounds 1 and 2 and the un-itemized ones in Round 4. R34 and R35 lead because they are the
two recorded FAILs; R40 and R41 sit in the middle while attention is still good, because they are the
rows Round 4 could not get itemized; R42 and R43 are regressions and go last.

Before the first row, confirm and state in the session that the page is loaded from
`http://127.0.0.1:8099/strava-widgets/` and that the bundle filename in the served `index.html`
matches the one Task 1 recorded in the preamble. Do not record any FAIL verdict before that
confirmation exists.

Each prompt must restate that row's "Required detail:" and "Observer required:" clauses verbatim from
the staged table, so the developer sees exactly what the answer has to contain and who has to produce
it before answering. For R40 and R41, present the lettered sub-questions as separate lines within the
one prompt, and accept a lettered answer — do not accept a single word for the row.

Record each answer into that row's Observation cell verbatim, prefixed with `R5-VERDICT: PASS`,
`R5-VERDICT: FAIL`, `R5-VERDICT: BLOCKED` or `R5-VERDICT: NOT EXERCISABLE`, followed by the observer
phrase — "observed by developer", "observed by agent (browser automation against the staged build)",
or "observed by developer, corroborated by agent" — and then the observation itself, plus the
theme(s) observed for R40 and R41.

**When an answer is missing its Required detail**, do not spread a nearby answer across it and do not
soften the requirement. Ask again once, naming the specific missing piece (for R40 and R41, name the
specific letters that are missing). If it is still missing, record `R5-VERDICT: FAIL` with the reason
"insufficient evidence - Required detail not supplied", quoting verbatim whatever the developer did
say. If the developer answers several rows in one statement, that statement is recorded against none
of them until each is individually described.

**R34, R37, R39, R40 and R41 may not be recorded PASS on agent observation.** If the developer
declines or cannot perform one, record it `R5-VERDICT: BLOCKED` with the decline quoted. Do not
substitute a computed-style read or a synthetic event dispatch for a perceptual judgment on those
five — that substitution is the specific caveat `20-VERIFICATION.md` raised against Round 3, and the
synthetic drag is what made Round 4's R31 evidence ambiguous.

**R35 needs both halves.** The observation half may be answered by developer or agent; the
disposition half must be the developer's own yes or no on whether a double-click navigating on its
first click is accepted as shipped behaviour. Record both, naming who supplied each. If the developer
answers "no" (escalate), that is recorded as given — do not argue the point and do not patch
anything.

After all ten rows have a verdict:

- Set `status` and `nyquist_compliant` in the frontmatter. `nyquist_compliant: true` and
  `status: passed` only if all ten rows are PASS **and** R40 and R41 each name both themes.
  Otherwise `status: partial` (evidence gap) or `status: blocked` (a shipped defect was found), and
  `nyquist_compliant: false`.
- For any FAIL, add a `## Round 5 Gap-Closure Record` section in the same shape as Round 4's: the
  developer's verbatim words, the requirement each blocks, and no suggested fix and no root-cause
  theory. Continue the GAP numbering from GAP 12 — the next new gap is GAP 13.
- Record the disposition of the two open gaps explicitly in that record, whether or not anything
  failed: **GAP 12** is closed, partially closed or not closed on R34's, R35's and R36's evidence
  together, and the record must state which half of it each row settles; **GAP 11** is closed or
  restated on R39's evidence, and if the gesture was again not performed but both written questions
  were answered, say exactly that rather than restating the gap wholesale.
- Add a `## Evidence Quality (Round 5)` section stating plainly how many rows carry genuine
  individual evidence, how many were observed by the developer versus by the agent, which claims
  remain settled by measurement rather than perception, and whether R38 was answered by VoiceOver or
  by the composed-name substitution — matching the honesty of the Round 4 note rather than improving
  on its tone. State the bundle filename every verdict was taken against.
- Update `.planning/REQUIREMENTS.md` using this Round 5 row-to-requirement map, counting the fifteen
  carried-forward rows as PASS:
  - **UX-01**: R34, R35, R36, R37, R42, plus carried R22\*, R23\*, R24\*, R26\*, R1\*, R3\*, R5\*,
    R11\*, R12\*, R13\*, R14\*
  - **UX-02**: carried R26\*, R4\*, R5\*, R6\* — already Complete; leave it ticked and do not
    re-litigate it
  - **UX-03**: R34, R35, R36, R37, R38, R39, R40, R41, R42, R43, plus carried R27\*, R30\*, R1\*,
    R3\*, R4\*, R8\*, R10\*, R15\*, R17\*
  - **REC-08**: carried R5\*, R6\* — already Complete; leave it ticked
  (rows marked \* are carried forward from an earlier round and counted as PASS)
  Tick a requirement only if every one of its mapped rows passed; a requirement with any failing,
  blocked or insufficiently-evidenced mapped row stays open and its entry names the specific rows
  that still need an individual re-verification. Update both the checklist entry and the status
  table. Do not tick anything on an autonomous plan's say-so; this repository reverted that mistake
  once already in plan 19-15.
- If R38 passes, its REQUIREMENTS.md entry must state whether the Flags badge it announced was
  organic or fixture-induced, and name the activity id if it was a fixture, so a future reader does
  not believe the archive grew a badge.
- Record in the summary the exact `requirements-completed` list, and state plainly if it is empty.
- Do not touch any file under `src/` or `scripts/`. If a row fails, it stays failed.
  </action>
  <verify>
    <human-check>All ten Round 5 rows carry their own named verdict, their own observer and the developer's own words; each PASS row contains the Required detail its Test Instructions cell asked for; R40 and R41 each carry their lettered sub-answers and both theme names; R34, R37, R39, R40 and R41 are recorded as observed by the developer or else BLOCKED; R34 reports what Cmd+C actually captured; R35 carries both the observation and the developer's explicit yes or no on the disposition; R36 reports the new tab's URL for both tables; R38 carries six per-cell announcements with the Flags cell's badge text among them.</human-check>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs'; const p='.planning/phases/20-row-click-interaction-pattern/20-VALIDATION.md'; const s=readFileSync(p,'utf8'); const parts=s.split('## Round 5'); if(parts.length!==2) throw new Error('expected exactly one Round 5 heading'); if((parts[0].match(/VERDICT:\s*[A-Z]/g)||[]).length!==65) throw new Error('Rounds 1-4 altered'); if((s.match(/R2-VERDICT/g)||[]).length!==22) throw new Error('R2-VERDICT count changed'); if((s.match(/R3-VERDICT/g)||[]).length!==18) throw new Error('R3-VERDICT count changed'); if((s.match(/R4-VERDICT/g)||[]).length!==12) throw new Error('R4-VERDICT count changed'); const t=parts[1]; const rows=t.split('\n').filter(l=>l.startsWith('|')&&!/^\|\s*-+/.test(l)&&!/^\|\s*Behavior/.test(l)); if(rows.length!==10) throw new Error('expected 10 Round 5 rows, found '+rows.length); const ids=['R34','R35','R36','R37','R38','R39','R40','R41','R42','R43']; const obs=rows.map(r=>r.split('|')[5]||''); const v=obs.map(o=>(o.match(/R5-VERDICT:\s*(PASS|FAIL|BLOCKED|NOT EXERCISABLE)/)||[])[1]); v.forEach((x,i)=>{ if(!x) throw new Error('row '+ids[i]+' has no R5-VERDICT token in its Observation cell'); }); for(let i=0;i<obs.length;i++){ const body=obs[i].replace(/R5-VERDICT:\s*(PASS|FAIL|BLOCKED|NOT EXERCISABLE)/,'').trim(); if(body.length<60) throw new Error('row '+ids[i]+' observation is too thin ('+body.length+' chars)'); if(!/observed by (developer|agent)/i.test(obs[i])) throw new Error('row '+ids[i]+' does not name its observer'); for(let j=i+1;j<obs.length;j++){ if(obs[i].trim()===obs[j].trim()) throw new Error('rows '+ids[i]+' and '+ids[j]+' share an identical observation'); } } const at=(id)=>ids.indexOf(id); const pass=(id)=>v[at(id)]==='PASS'; const cell=(id)=>obs[at(id)]; const quoted=/[\x22“‘'][^\x22”’']{6,}[\x22”’']/; for(const n of ['R40','R41']){ if(pass(n) && !(/light/i.test(cell(n)) && /dark/i.test(cell(n)))) throw new Error('theme-sensitive row '+n+' is PASS without naming both themes'); } for(const n of ['R34','R37','R39','R40','R41']){ if(pass(n) && !/observed by developer/i.test(cell(n))) throw new Error(n+' is PASS without developer observation - agent automation cannot discharge it'); } if(pass('R34') && !(quoted.test(cell('R34')) && /#\/records/.test(cell('R34')) && /ghost/i.test(cell('R34')))) throw new Error('R34 is PASS without the quoted selection, the drag-ghost answer and the confirmed #/records URL'); if(pass('R35') && !(quoted.test(cell('R35')) && /\b(yes|no)\b/i.test(cell('R35')))) throw new Error('R35 is PASS without the quoted value and an explicit yes/no on the disposition'); if(pass('R36') && (cell('R36').match(/#\/activity\//g)||[]).length<2) throw new Error('R36 is PASS without a new-tab URL for both tables'); if(pass('R37') && !/window/i.test(cell('R37'))) throw new Error('R37 is PASS without reporting the new window'); if(pass('R38')){ for(const c of ['Rank','Time','Pace','Age-Grade','Date','Flags']){ if(!new RegExp(c,'i').test(cell('R38'))) throw new Error('R38 is PASS without naming the '+c+' cell'); } if(!/Low confidence|Excluded/i.test(cell('R38'))) throw new Error('R38 is PASS without the Flags badge text'); } if(pass('R39') && (cell('R39').match(/\b(yes|no)\b/gi)||[]).length<2) throw new Error('R39 is PASS without an explicit yes/no to each of the two questions'); for(const n of ['R40','R41']){ if(pass(n)){ const letters=n==='R40'?['a','b','c','d']:['a','b','c','d','e','f']; for(const L of letters){ if(!new RegExp('(^|[^A-Za-z])\\\\(?'+L+'\\\\)').test(cell(n))) throw new Error(n+' is PASS without a labelled ('+L+') answer'); } } } if(pass('R41')){ for(const tb of ['Riegel','gear','best']){ if(!new RegExp(tb,'i').test(cell('R41'))) throw new Error('R41 is PASS without naming the '+tb+' table'); } } if(pass('R42') && !/(highlight|scroll|focus)/i.test(cell('R42'))) throw new Error('R42 is PASS without stating what the page did after the filter was cleared'); if(pass('R43') && !/\b(one|1)\b/i.test(cell('R43'))) throw new Error('R43 is PASS without stating the tab-press count per row'); const fails=v.filter(x=>x==='FAIL').length; const clean=v.every(x=>x==='PASS'); const fm=s.split('---')[1]||''; const claimed=/nyquist_compliant: true/.test(fm); if(claimed && !clean) throw new Error('nyquist_compliant is true without a clean sweep'); if(claimed && !/status: passed/.test(fm)) throw new Error('nyquist_compliant true requires status: passed'); if(!claimed && clean) throw new Error('all rows pass but nyquist_compliant was not set true'); if(fails>0 && !/## Round 5 Gap-Closure Record/.test(s)) throw new Error(fails+' failing rows but no Round 5 Gap-Closure Record section'); if(!/## Evidence Quality \(Round 5\)/.test(s)) throw new Error('missing Evidence Quality (Round 5) section'); for(const g of ['GAP 12','GAP 11']){ if(!t.includes(g)) throw new Error('Round 5 does not state a disposition for '+g); } if(!/assets\/index-/.test(t)) throw new Error('Round 5 does not record the bundle every verdict was taken against'); console.log('10 Round 5 verdicts recorded, '+fails+' FAIL, observers named, Rounds 1-4 intact');" && test -z "$(git status --porcelain src scripts data)"</automated>
  </verify>
  <acceptance_criteria>
    - `20-VALIDATION.md`'s Round 5 section contains exactly ten `R5-VERDICT:` tokens from the
      four-state vocabulary (PASS / FAIL / BLOCKED / NOT EXERCISABLE), one inside each row's own
      Observation cell.
    - Every Observation cell names its observer with one of the three sanctioned phrases, carries at
      least 60 characters of description beyond the verdict token, and is not identical to any other
      cell — all three enforced mechanically.
    - R34, R37, R39, R40 and R41 are recorded PASS only when the cell says "observed by developer";
      agent observation on those five yields BLOCKED with the decline quoted.
    - Every row recorded PASS satisfies its own Required detail, enforced mechanically: R34 carries a
      quoted selection, an answer on the drag ghost and the confirmed `#/records` URL; R35 carries
      the quoted value and an explicit yes or no; R36 carries two `#/activity/` URLs, one per table;
      R37 reports the new window; R38 names all six cells and carries the Flags badge text; R39
      carries two yes/no answers; R40 carries labelled (a) to (d) and both theme names; R41 carries
      labelled (a) to (f), names the Riegel, gear and best-efforts tables, and both theme names; R42
      states what the page did after the filter was cleared; R43 states the tab-press count per row.
    - A row recorded FAIL, BLOCKED or NOT EXERCISABLE needs only the verdict token, the observer
      phrase and at least 60 characters quoting what was said — the Required-detail checks apply to
      PASS rows only, so an honest non-pass is always recordable.
    - `nyquist_compliant: true` and `status: passed` appear if and only if all ten rows are PASS and
      R40 and R41 each name both themes.
    - Every failing row has a `## Round 5 Gap-Closure Record` entry with verbatim wording, the
      blocked requirement, and no suggested fix or root-cause theory, with new gaps numbered from
      GAP 13.
    - The Round 5 section states an explicit disposition for GAP 12 (naming which of R34, R35 and R36
      settles which half) and for GAP 11, whether or not anything failed.
    - A `## Evidence Quality (Round 5)` section exists, states the developer-versus-agent split
      honestly, says whether R38 used VoiceOver or the composed-name substitution, and names the
      bundle filename every verdict was taken against.
    - Rounds 1, 2, 3 and 4 are byte-identical: 65 `VERDICT: <uppercase>` tokens above the Round 5
      heading, and 22 `R2-VERDICT`, 18 `R3-VERDICT` and 12 `R4-VERDICT` occurrences file-wide.
    - `.planning/REQUIREMENTS.md` ticks only requirements whose every mapped row passed, using the
      Round 5 map with the fifteen carried-forward rows counted as passes, and each still-open
      requirement names the specific rows that failed. UX-02 and REC-08 remain ticked. If R38 passes,
      its entry says whether the badge was organic or fixture-induced.
    - `git status --porcelain src scripts data` is empty — the checkpoint changes no source and no
      archive data, and no defect was patched under checkpoint pressure.
    - The session ran against a URL beginning `http://127.0.0.1:8099/strava-widgets/`, the served
      bundle filename was confirmed against the preamble's record before any FAIL verdict, and the
      rows were presented one at a time in the order stated in this task's action.
  </acceptance_criteria>
  <resume-signal>Answer one row at a time, in the order you are asked, each with the Required detail that row names — for example: "R34: pass — dragged across '5:23/km' in the Pace cell of the 5K table, the text stayed selected with no ghost, Cmd+C pasted 5:23/km, still on #/records". For R40 and R41, answer by letter. A single "approved" or "looks good" covering multiple rows will be handed back with the specific rows and the specific missing details still needed.</resume-signal>
  <done>Phase 20's success criterion 4 is discharged on individually-described, individually-attributed per-row evidence; GAP 12's drag-select half is answered against the shipped fix and its double-click half is answered by the developer's own disposition; CR-01's announcement is observed for the first time; and R25's, R28's, R29's and R33's four rounds of unfinished business are each closed or honestly recorded as still open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build -> production URL shape | Serving at the bare server root reproduces the class of defect that shipped Phase 16's black page behind a green gate; the symlink under `/tmp/gh-pages` is what makes the tested path match GitHub Pages. |
| browser cache -> claimed truth | New as an explicit boundary this round. Round 4 recorded two false failure appearances caused by Chrome serving a stale `index.html` and a stale JSON from the `localhost` origin. A stale asset and a shipped defect are indistinguishable from the screen. |
| automated gate -> claimed truth | A green suite in this repository has been mistaken for evidence about behaviour it cannot see seven times now — Phase 16's black page, Phase 17's two rendering defects, Phase 19's dead CSS token, this phase's CR-01 and CR-02, its unguarded row-click listener, its `notedActivityId` focus leak, and GAP 12, which shipped behind 1062 green tests and twelve genuine browser rows. |
| agent observation -> developer observation | The agent cannot see a second browser window (R37), cannot perform a human hand's drag (R34), and cannot make a perceptual judgment (R40, R41). Round 4 recorded a synthetic drag and two un-itemized "looks good" answers; this round separates the two explicitly and forbids the substitution on the five rows where it matters. |
| fixture data -> archive data | One entry may be added to the served exclusions file so a Flags badge exists to announce. If that edit reached the repository, or went unrecorded, the record would claim an archive property that is not true. |
| new tab / new window opened during R36-R37 | The gestures open additional browser contexts, all pointing at the same local `http://127.0.0.1:8099/strava-widgets/` origin the session already trusts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20G5-C5-01 | Spoofing | a stale cached bundle standing in for the shipped one, producing a false FAIL | mitigate | Task 1 reads the hashed bundle name from `dist/widgets/index.html`, asserts the staged path serves the same one and that it differs from Round 4's `assets/index-CkFhsgc3.js`, and records both in the preamble; Task 2 re-confirms before any FAIL verdict; every URL in the round uses `127.0.0.1`, a different cache origin from `localhost`, and the automated check rejects the string `localhost:8099` anywhere in the section. |
| T-20G5-C5-02 | Spoofing | a root-served build standing in for the production path | mitigate | Task 1's gate asserts `/tmp/gh-pages/strava-widgets/index.html` resolves and the symlink target is absolute; the checkpoint requires confirming `/strava-widgets/` is in the URL bar before any row is judged. |
| T-20G5-C5-03 | Spoofing | fixture data presented as archive data | mitigate | Task 1 asserts the fixture, if any, lives only in `dist/widgets/data/best-effort-exclusions.json` and that `git status --porcelain data src scripts` is empty; the preamble discloses file, activity id, field and resulting badge string; Task 2 requires REQUIREMENTS.md to say whether R38's badge was organic or fixture-induced. |
| T-20G5-C5-04 | Repudiation | a manual row recorded as passed on evidence never given | mitigate | Task 2's automated check requires one four-state verdict token per row, a named observer, at least 60 characters, no two identical cells, both theme names on the two theme-sensitive rows, and a per-row required-detail predicate on every PASS row — including the lettered sub-answers on R40 and R41, which is the specific gap Round 4 recorded against itself. |
| T-20G5-C5-05 | Repudiation | a perceptual claim discharged by a computed-style read or a synthetic event | mitigate | R34, R37, R39, R40 and R41 cannot be recorded PASS unless their Observation cell says "observed by developer"; the automated check enforces it, and the house rules make BLOCKED the honest alternative. |
| T-20G5-C5-06 | Repudiation | R35's residual quietly resolved in the checkpoint's favour | mitigate | The disposition question is stated in full in the row itself and in the preamble, the developer's yes or no is required for a PASS, and an answer of "no" is recorded as given with no argument and no patch. |
| T-20G5-C5-07 | Tampering | patching a defect during the checkpoint | mitigate | `git status --porcelain src scripts data` must be empty, asserted inside both tasks' automated verifies. The house rule since 16-09 is that a failing row is recorded verbatim and left unpatched. |
| T-20G5-C5-08 | Tampering | rewriting Rounds 1-4 to look better | mitigate | Both tasks assert the pre-Round-5 slice still holds exactly 65 `VERDICT:` tokens, that `R2-VERDICT`, `R3-VERDICT` and `R4-VERDICT` still occur exactly 22, 18 and 12 times file-wide, and that Round 4's Checkpoint Outcome, Gap-Closure Record, GAP 12, the Round 3 gap dispositions and the Evidence Quality section are all intact. Round 5 prose is forbidden from using any prior round's verdict token. |
| T-20G5-C5-09 | Elevation of Privilege | R36 and R37 opening new tabs and windows | accept | All gestures target `#/activity/<id>` on the same `http://127.0.0.1:8099` origin already loaded in the session. The anchors are same-origin fragment links; no `window.open` call exists in application code (D-12 still declines to add one), and no download target exists. |
| T-20G5-C5-10 | Elevation of Privilege | VoiceOver enabled system-wide during R38 | accept | VoiceOver is a first-party macOS assistive technology toggled with Cmd+F5 by the developer on their own machine; no software is installed and nothing is granted new access. R38 explicitly permits declining it, with the substitution recorded. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized; no `## Package Legitimacy Audit` is required because no package-manager install task exists. The server is `python3 -m http.server`, already present. Do not install jsdom, happy-dom, puppeteer, playwright or any accessibility-audit tool to automate these rows — no automated substitute exists in this repository and this phase does not accept one. |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run build-widgets` and `npm run verify-dashboard` exit 0 with recorded
  counts; `build-widgets` emits zero `css-syntax-error` occurrences; `npm test`'s result is recorded
  with its total compared against Round 4's 1062/1062 and any failure matched against
  `deferred-items.md`'s five known files.
- The build is served under `http://127.0.0.1:8099/strava-widgets/` from a symlink proven to resolve
  to an absolute path inside the repository, and the served `index.html` references the same hashed
  bundle as `dist/widgets/index.html`, which is not Round 4's `assets/index-CkFhsgc3.js`.
- A PR-table row carrying a Flags badge exists and is named in the preamble — organic, or a
  one-entry staged fixture disclosed by file, id, field and badge string.
- Any fixture exists only in `dist/widgets/`; `git status --porcelain src scripts data` is empty at
  both task boundaries.
- `20-VALIDATION.md` carries ten Round 5 rows and exactly ten `R5-VERDICT` tokens, each with a named
  observer, with Rounds 1, 2, 3 and 4 intact above them.
- Every PASS row satisfies its own mechanically-checked required detail; R34, R37, R39, R40 and R41
  are PASS only on developer observation; R40 and R41 carry lettered sub-answers; no two Observation
  cells are identical.
- The Round 5 record states an explicit disposition for GAP 12 and GAP 11, and names the bundle every
  verdict was taken against.
</verification>

<success_criteria>
Success criterion 4 is discharged on evidence that is individually described, individually attributed
and individually itemized: ten rows asked one at a time, each answered with a detail only performing
it could produce, five of them by the developer's own eyes because their claims are perceptual or
outside the agent's observable surface, and two of them broken into lettered sub-answers because a
whole-row "looks good" is exactly what the previous round recorded as insufficient. GAP 12's
drag-select half is answered against the shipped fix; its double-click half is answered by the
developer's own disposition rather than by the planner's assumption. CR-01's per-cell announcement is
observed for the first time in this phase. R25's new-window half, R28's disposition question and
R29/R33's missing itemization — four rounds of unfinished business — are each closed or honestly
recorded as still open, against a bundle proven to be the one on disk.
</success_criteria>

<output>
Create `.planning/phases/20-row-click-interaction-pattern/20-20-SUMMARY.md` when done. It must state
the exact `requirements-completed` list (or that it is empty), the bundle filename every verdict was
taken against, whether the Flags badge was organic or fixture-induced, and the developer's verbatim
answer to R35's disposition question.
</output>
