---
phase: 20-row-click-interaction-pattern
plan: 10
type: execute
wave: 8
depends_on: ["20-08"]
files_modified:
  - src/dashboard/row-semantics.test.ts
  - src/dashboard/styles.test.ts
autonomous: true
gap_closure: true
requirements: [UX-01, UX-03]
must_haves:
  decisions: [D-01, D-02, D-06, D-09, D-10]
  truths:
    - "The D-01 guard fails when a row builder is given tabindex or role=link in ANY spelling - camelCase property, lowercase attribute, single- or double-quoted - not only the lowercase attribute form"
    - "The D-01 guard still passes the six legitimate heading/h1 tabIndex = -1 focus-target call sites and the two loading role=status live regions"
    - "The guard's blind spot is proven closed in-suite: the same mutation the old guard missed is asserted missed by the old form and caught by the new one"
    - "The four Phase 20 CSS assertions read the cascade winner, so a later override to display: flex, the navigable-row cursor, or either hover formula fails the suite instead of shipping silently"
    - "The first-wins false-green mechanism is proven in-suite against synthetic CSS, not asserted in prose"
  artifacts:
    - path: "src/dashboard/row-semantics.test.ts"
      provides: "A spelling-agnostic D-01 guard over list.ts, records.ts, overview.ts and row-navigation.ts, with self-tests"
      contains: "rowSemanticViolations"
    - path: "src/dashboard/styles.test.ts"
      provides: "Last-wins cascade resolution for the four Phase 20 rule assertions, with a self-test proving the first-wins blind spot"
      contains: "cascadeWinningBodyDeclaring"
  key_links:
    - from: "src/dashboard/row-semantics.test.ts rowSemanticViolations"
      to: "the comment-stripped sources of list.ts, records.ts, overview.ts and row-navigation.ts"
      via: "a spelling-agnostic scan with an explicit heading/h1 allowlist"
      pattern: "rowSemanticViolations\\("
    - from: "src/dashboard/styles.test.ts Phase 20 assertions"
      to: "the last non-at-rule-scoped rule body in styles.css that declares the property"
      via: "cascadeWinningBodyDeclaring / bodyForSelectorListToken instead of first-wins declarationsFor"
      pattern: "cascadeWinningBodyDeclaring\\("
---

<objective>
Close the two WARNING-severity guard-layer defects `20-VERIFICATION.md` and `20-REVIEW.md` both
confirmed: the D-01 `tabindex`/`role="link"` source guards are case-blind, and four Phase 20 CSS
assertions read the first matching rule instead of the cascade winner.

Purpose: neither defect breaks anything that ships today, and both mean a future regression to
something this phase fought for would arrive behind a fully green suite. The `tabindex` guard is
already vacuous - `list.ts` and `records.ts` between them use camelCase `tabIndex` six times, and
the guard only searches the lowercase attribute spelling, so appending `tr.tabIndex = 0;
tr.role = 'link';` to a row builder (the precise regression D-01 was written to block) leaves every
count at zero. The four CSS assertions read through `declarationsFor`, which is first-rule-wins,
in a file whose own 600-line helper audit records first-wins as a proven false-green mechanism
(R3-WR-02) and rewrote `bodyForSelectorListToken` to last-wins for exactly that reason. This
repository has shipped rendering defects behind a green gate four times; a guard that cannot fail
is worse than no guard, because it is cited as evidence.

Output: a spelling-agnostic D-01 guard and four cascade-correct CSS assertions, each with an
in-suite self-test that proves the old form's blind spot rather than asserting it in a comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md
@.planning/phases/20-row-click-interaction-pattern/20-VERIFICATION.md
@.planning/phases/20-row-click-interaction-pattern/20-REVIEW.md
@.planning/phases/20-row-click-interaction-pattern/20-04-SUMMARY.md

<interfaces>
**Every `tabindex` / `role` write in the four files this guard covers, enumerated by grep so the
allowlist below is derived from fact, not guessed:**

  `src/dashboard/views/list.ts`
    line 1233  `loading.setAttribute('role', 'status');`
    line 1277  `heading.tabIndex = -1;`
  `src/dashboard/views/records.ts`
    lines 248, 465, 602, 679  `heading.tabIndex = -1;`
    line 762   `loading.setAttribute('role', 'status');`
    line 797   `h1.tabIndex = -1;`
  `src/dashboard/views/overview.ts`
    line 283   `heading.setAttribute('tabindex', '-1');`
  `src/dashboard/row-navigation.ts`
    (none)

  Every one of those is a legitimate programmatic focus target or a live region. The only
  receivers are `heading`, `h1` and `loading`; the only tabindex value is `-1`; the only role
  value is `status`.

**`row-semantics.test.ts` helpers already in the file, reuse them:**
  `stripComments(source: string): string`  - block comments non-greedy, then `//`-to-end-of-line
                                             where `//` is not preceded by `:`
  `countOccurrences(haystack: string, needle: string): number`  - case-SENSITIVE
                                             `haystack.split(needle).length - 1`
  Pre-stripped module constants already defined: `listStripped`, `overviewStripped`,
  `recordsStripped`, `trendsStripped`, `detailSectionsStripped`, `rowNavigationStripped`.
  Precedent for self-tests inside this file: the `describe('stripComments - self-tests')` block
  at lines 56-73.

**`styles.test.ts` helpers already in the file:**
  `declarationsFor(selector: string): string`      - line 42. FIRST-rule-wins. Regex-escapes the
                                                     selector, anchors on `selector\s*{`, throws
                                                     when not found. No `source` parameter.
  `selectorListDeclares(needle, declaration): boolean` - line 127. ANY-rule-wins; correct for the
                                                     negative assertions that use it, left alone.
  `bodyForSelectorListToken(needle, source = cssNoComments): string` - line 256. LAST-rule-wins,
                                                     skips at-rule-scoped candidates via
                                                     `isAtRuleScoped`, throws via
                                                     `assertNotAtRuleScoped` or a generic message.
                                                     Already accepts an optional `source`
                                                     explicitly so self-tests can exercise it
                                                     against synthetic CSS.
  `RULE_SCANNER()` - a factory returning a fresh rule-head-and-body regex (a shared RegExp's
                                                     `lastIndex` is stateful across callers).
  `isAtRuleScoped(offset, head)` / `assertNotAtRuleScoped(offset, head, needle)`.
  `cssNoComments` - the comment-stripped stylesheet text.

**The stylesheet facts that decide how each of the four assertions migrates** (verified by grep,
not assumed):
  `.activity-row` is declared TWICE at the top level: `styles.css:338` (the `display: flex;
  flex-direction: row; flex-wrap: wrap; background; border; border-radius; padding` block) and
  `styles.css:1530` (a Phase 20 block declaring only `text-decoration: none`). Neither is inside
  an at-rule. So a naive swap to `bodyForSelectorListToken('.activity-row')` returns the SECOND
  body and `toContain('display: flex')` would fail - this is the trap `20-REVIEW.md` WR-03 flags
  in its closing paragraph.
  `.activity-row:hover` (`styles.css:1537`), `.activity-table__row--navigable` (`styles.css:1544`)
  and `.activity-table__row--navigable:hover` (`styles.css:1548`) each appear exactly once, at the
  top level. Those three migrate as a direct swap.
</interfaces>

<constraints>
- **No source file changes.** This plan's entire diff is inside two `.test.ts` files.
  `git status --porcelain src/dashboard/views src/dashboard/styles.css src/dashboard/row-navigation.ts`
  must be empty at plan close. In particular: do NOT consolidate `.activity-row`'s two rules in
  `styles.css` to make the migration easier. The stylesheet is correct as it stands - both rules
  are top-level and both apply - and `styles.css:1527-1529`'s own comment states, deliberately,
  that it does not redeclare `display: flex` so there is only one place to keep in sync. Editing
  it would put a rendered-CSS change into a plan that has no rendered verification.
- **Do not add a DOM or CSSOM dependency.** `vitest.config.ts` is `environment: 'node'` and
  devDependencies contain `vitest` and nothing else test-related. Zero installs are authorized.
  Both defects are text-guard defects and are fixed with text-guard tooling.
- **Do not weaken any existing assertion to make a new one pass.** If a new guard flags a real
  call site, the correct move is to widen the allowlist with the call site named explicitly in a
  comment - not to loosen the pattern until nothing matches.
- **Do not touch `bodyForSelectorListToken`, `selectorListDeclares`, `isAtRuleScoped`,
  `assertNotAtRuleScoped`, `RULE_SCANNER` or `extractNumericDeclaration`.** They carry Phase 19's
  R3-WR-02 / R3-CR-01 / R3-IN-01 fixes and roughly 40 assertions depend on their exact semantics.
  Add alongside them; do not refactor them into a shared implementation in this plan.
- **Plan 20-09 runs in this same wave** and edits `src/dashboard/row-navigation.ts` and
  `src/dashboard/row-navigation.test.ts`. Do not edit either. **Do not run the full suite and do
  not run `row-navigation.test.ts`** - plan 20-09's Task 2 deliberately leaves that file RED, and
  a full-suite run here would report it as a failure of this plan. Verify with the two scoped
  commands in each task's `<verify>` block only.
- **Out of scope, do not opportunistically fix even though you will read past them:** IN-04
  (`stripComments` exported from a test file and its `//`-inside-literal edge case), IN-05
  (hard-coded `.cta` counts for `detail.ts`/`trends.ts`/`detail-map.ts`), IN-06 (the
  `overview.ts` `aria-label` count guard), and WR-05's `badgeIdPrefix` centralisation. Each is a
  real finding and none is in this round's approved scope.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the D-01 guard spelling-agnostic, and prove in-suite that the old form missed the mutation</name>
  <files>src/dashboard/row-semantics.test.ts</files>
  <read_first>
    - `src/dashboard/row-semantics.test.ts` in full (275 lines) - especially the header comment
      (lines 4-21) stating what this file does and does not prove, `stripComments` and its
      self-test block (lines 41-73), `countOccurrences` (line 82), and the
      `describe('D-01 - no fake link semantics on a <tr>')` block at lines 157-172 that you are
      replacing.
    - `.planning/phases/20-row-click-interaction-pattern/20-REVIEW.md` section WR-02 - the executed
      mutation output showing lowercase `tabindex` at 0 while camelCase `tabIndex` is 1 in
      `list.ts` and 5 in `records.ts`, and the proposed fix you are adapting.
    - `src/dashboard/views/list.ts` lines 1230-1280 and `src/dashboard/views/records.ts` lines
      240-260, 460-470, 595-610, 675-685, 755-800 - every legitimate `tabIndex`/`role` write, so
      the allowlist you build is derived from the real call sites rather than from this plan's
      summary of them.
    - `src/dashboard/views/overview.ts` around line 283 - the one `setAttribute('tabindex', '-1')`
      attribute-form call site, which is why `overview.ts` joins the guarded set.
  </read_first>
  <action>
Replace the two `it` blocks inside `describe('D-01 - no fake link semantics on a <tr>')` with a
single spelling-agnostic guard, plus self-tests.

**Add a pure helper `rowSemanticViolations(source: string): string[]`**, defined next to
`countOccurrences` (module scope, not exported - IN-04 already flags an unnecessary export in this
file, do not add a second). It scans a comment-stripped source and returns the matched text of
every `tabindex`/`role` write that is NOT on the allowlist. Scan for these four spellings, all
case-insensitive and all global:

- property assignment: a receiver identifier, optional whitespace, `.`, optional whitespace,
  `tabIndex`, optional whitespace, `=` **not followed by another `=`** (so a `===` comparison can
  never be mistaken for an assignment), then the value up to the next `;`;
- attribute call: a receiver identifier, `.setAttribute(`, a quoted `tabindex` in either quote
  style, `,`, a quoted or bare value, `)`;
- role property assignment: a receiver identifier, `.role`, `=` not followed by `=`, then a quoted
  value;
- role attribute call: a receiver identifier, `.setAttribute(`, a quoted `role`, `,`, a quoted
  value, `)`.

Capture the receiver identifier and the value in each pattern. Apply exactly two allowlist rules,
each with a comment naming the real call sites it exists for:

- a **tabindex** hit is allowed only when the receiver identifier is `heading` or `h1` **and** the
  value is `-1`. Those are the six programmatic focus targets enumerated above
  (`list.ts:1277`, `records.ts:248/465/602/679/797`, `overview.ts:283`). Any other receiver, and
  any value other than `-1`, is a violation - which is precisely the `tr.tabIndex = 0` regression
  D-01 exists to block.
- a **role** hit is allowed only when the value is not `link`, compared case-insensitively. That
  permits the two `loading.setAttribute('role', 'status')` live regions (`list.ts:1233`,
  `records.ts:762`) while catching `tr.role = 'link'` and `tr.setAttribute('role', 'link')` in
  either quote style and either spelling.

Return the full matched substring for each violation, so a failure message names the offending code
rather than just a count.

**Replace the D-01 describe block's body** with one `it` that loops over four named sources -
`['list.ts', listStripped]`, `['records.ts', recordsStripped]`, `['overview.ts', overviewStripped]`
and `['row-navigation.ts', rowNavigationStripped]` - and asserts
`expect(rowSemanticViolations(src), name).toEqual([])` for each. `overview.ts` is newly covered
here and belongs in the set: D-01 governs the div rows that plan 20-02 turned into real anchors,
and `overview.ts` is one of the two files that produce them.

Delete the four superseded literal-count assertions (`countOccurrences(..., 'tabindex')` and the
`role="link"` / `'role', 'link'` counts). Do not keep them "for safety" - `20-REVIEW.md`'s executed
mutation proves they are vacuous, and a vacuous assertion sitting next to a real one is read as
corroboration. Replace them with a comment in the describe block recording that they were removed
because WR-02 proved them vacuous, naming the executed mutation output (lowercase `tabindex`: 0 in
both files while camelCase `tabIndex` is 1 in `list.ts` and 5 in `records.ts`).

**Add a `describe('rowSemanticViolations - self-tests')` block**, following the `stripComments`
self-test precedent already in this file, with one `it` per case:

- `tr.tabIndex = 0;` yields exactly one violation whose text contains `tabIndex`;
- `tr.setAttribute('tabindex', '0');` yields exactly one violation;
- `tr.setAttribute("tabindex", "0");` (double quotes) yields exactly one violation;
- `tr.role = 'link';` yields exactly one violation;
- `tr.setAttribute('role', 'link');` yields exactly one violation;
- `row.tabIndex = -1;` yields exactly one violation - the value is allowed but the receiver is not,
  so a row made programmatically focusable still trips the guard;
- `heading.tabIndex = -1;` yields no violations;
- `h1.tabIndex = -1;` yields no violations;
- `heading.setAttribute('tabindex', '-1');` yields no violations;
- `loading.setAttribute('role', 'status');` yields no violations;
- `if (el.tabIndex === -1) { return; }` yields no violations - the `=(?!=)` anchor holds.

**Add one final `it` that is the blind-spot proof**, and give it a title naming WR-02: assert that
`countOccurrences("tr.tabIndex = 0;", 'tabindex')` is `0` - the old guard's exact miss, asserted
in-suite - and in the same test assert `rowSemanticViolations("tr.tabIndex = 0;")` has length `1`.
Add an assertion message spelling out that the first expectation documents the defect and the second
documents its closure, so nobody later "fixes" the zero as a bug.

Finally, extend the file's header comment (lines 4-21) with a short paragraph recording that the
D-01 guard is spelling-agnostic as of this round, that its allowlist is `heading`/`h1` with value
`-1` for tabindex and any non-`link` value for role, and that widening the allowlist requires
naming the new call site - so the next agent widens it deliberately rather than to make a red test
green.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx vitest run src/dashboard/row-semantics.test.ts && node --input-type=module -e "import {readFileSync} from 'node:fs'; const raw=readFileSync('src/dashboard/row-semantics.test.ts','utf8'); const s=raw.replace(/\/\*[\s\S]*?\*\//g,'').replace(/(?<!:)\/\/.*\$/gm,''); if(!/function rowSemanticViolations/.test(s)) throw new Error('rowSemanticViolations helper not added'); if(/export function rowSemanticViolations/.test(s)) throw new Error('helper must not be exported - IN-04 already flags a stray export in this file'); for(const gone of [\"countOccurrences(listStripped, 'tabindex')\",\"countOccurrences(recordsStripped, 'tabindex')\",\"countOccurrences(rowNavigationStripped, 'tabindex')\"]){ if(s.includes(gone)) throw new Error('superseded vacuous assertion still present: '+gone); } for(const src of ['listStripped','recordsStripped','overviewStripped','rowNavigationStripped']){ if(!new RegExp('rowSemanticViolations[\\\\s\\\\S]{0,400}'+src).test(s) && !new RegExp(src+'[\\\\s\\\\S]{0,400}rowSemanticViolations').test(s)) throw new Error('guard does not cover '+src); } if(!/rowSemanticViolations - self-tests/.test(s)) throw new Error('self-test describe block missing'); if(!/=\\(\\?!=\\)|=\\(\\?\\!=\\)/.test(s) && !/\\(\\?!=\\)/.test(s)) throw new Error('the =(?!=) assignment anchor is missing, so a === comparison can false-positive'); console.log('D-01 guard is spelling-agnostic, vacuous assertions removed, self-tests present');" && test -z "$(git status --porcelain src/dashboard/views src/dashboard/styles.css src/dashboard/row-navigation.ts)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.json` exits 0 and `npx vitest run src/dashboard/row-semantics.test.ts`
      exits 0. Record the test count before and after in the summary; it must increase by at least
      eleven (the self-test cases plus the blind-spot proof).
    - `rowSemanticViolations` exists at module scope in `row-semantics.test.ts` and is NOT exported.
    - The D-01 describe block asserts `rowSemanticViolations(...)` equals `[]` for all four of
      `listStripped`, `recordsStripped`, `overviewStripped` and `rowNavigationStripped`.
    - The three `countOccurrences(..., 'tabindex')` assertions and the four `role="link"` /
      `'role', 'link'` literal-count assertions are gone, with a comment in their place recording
      WR-02's executed mutation output as the reason.
    - The self-test block proves all eleven cases listed in the action, including that
      `row.tabIndex = -1` IS a violation (allowed value, disallowed receiver) and that
      `el.tabIndex === -1` is NOT.
    - One test asserts both halves of the blind-spot proof in the same `it`:
      `countOccurrences("tr.tabIndex = 0;", 'tabindex')` is `0` and
      `rowSemanticViolations("tr.tabIndex = 0;")` has length `1`.
    - The file header records the allowlist and the rule that widening it requires naming the new
      call site.
    - `git status --porcelain src/dashboard/views src/dashboard/styles.css src/dashboard/row-navigation.ts`
      is empty - no source file was touched to make a guard pass.
  </acceptance_criteria>
  <done>Appending `tr.tabIndex = 0; tr.role = 'link';` to a row builder now turns the suite red, in every spelling the codebase actually uses.</done>
</task>

<task type="auto">
  <name>Task 2: Move the four Phase 20 CSS assertions onto the cascade winner, with the first-wins blind spot proven in-suite</name>
  <files>src/dashboard/styles.test.ts</files>
  <read_first>
    - `src/dashboard/styles.test.ts` lines 1-60 - the file header, `cssNoComments`, and
      `declarationsFor` with its "first matching rule" JSDoc.
    - `src/dashboard/styles.test.ts` lines 120-300 - `selectorListDeclares`,
      `splitTopLevelSelectors`, `RULE_SCANNER`, `isAtRuleScoped`, `assertNotAtRuleScoped` and
      `bodyForSelectorListToken` in full. `bodyForSelectorListToken` is the shape your new helper
      copies, including its optional `source` parameter and both of its throw paths.
    - `src/dashboard/styles.test.ts` lines 570-620 - the helper audit's own R3-WR-02 paragraph,
      which records first-wins as a proven false-green mechanism and why `bodyForSelectorListToken`
      and `extractNumericDeclaration` were both converted to last-wins. Your new helper and its
      audit entry must be consistent with that text.
    - `src/dashboard/styles.test.ts` lines 1085-1115 - the four Phase 20 assertions you are
      migrating, in their surrounding describe block.
    - `src/dashboard/styles.css` lines 330-350 and 1515-1552 - both `.activity-row` rules and the
      three single-occurrence Phase 20 rules, so you see for yourself which of the four assertions
      needs multi-body handling and which three are a direct swap.
    - `.planning/phases/20-row-click-interaction-pattern/20-REVIEW.md` section WR-03 - the executed
      mutation showing all four assertions staying green while the rules they guard are dead, and
      its closing note about `.activity-row` being declared twice.
  </read_first>
  <action>
**Step 1 - give `declarationsFor` an optional `source` parameter.** Change its signature to
`declarationsFor(selector: string, source: string = cssNoComments): string` and have it match
against `source` instead of the module-level `cssNoComments`. Every existing call site omits the
argument and keeps reading the real stylesheet unchanged, so this is behaviour-preserving. The
reason for the change is the same one already recorded in `bodyForSelectorListToken`'s JSDoc: the
self-tests below need to exercise the helper against small synthetic CSS strings without editing
`styles.css`. Add that sentence to `declarationsFor`'s JSDoc, and add one sentence marking it as
FIRST-rule-wins and therefore unsuitable for any assertion about a selector that can be declared
more than once - pointing at `bodyForSelectorListToken` and the new helper below as the correct
choices.

**Step 2 - add two helpers**, placed immediately after `bodyForSelectorListToken` so the last-wins
family sits together. Do not modify `bodyForSelectorListToken` itself.

- `bodiesForSelectorListToken(needle: string, source: string = cssNoComments): string[]` - scans
  with a fresh `RULE_SCANNER()`, splits each rule head with `splitTopLevelSelectors`, keeps only
  rules whose selector list contains `needle` exactly, skips at-rule-scoped candidates using
  `isAtRuleScoped` exactly as `bodyForSelectorListToken` does, and returns every surviving body in
  source order. When nothing survives it throws with the same two-path message shape
  `bodyForSelectorListToken` uses: `assertNotAtRuleScoped` when the token was seen only inside an
  at-rule, and the generic "no rule found whose selector list contains" message otherwise. A
  deleted rule must fail loudly, not return an empty array.
- `cascadeWinningBodyDeclaring(needle: string, property: string, source: string = cssNoComments): string` -
  calls `bodiesForSelectorListToken`, then returns the LAST body that declares `property`, anchored
  to a declaration boundary the same way `extractNumericDeclaration` anchors (start of body, or a
  preceding `;` or whitespace) so a property that is a suffix of a longer property can never match.
  Escape `property` with the same `.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')` guard the other helpers
  use (R3-IN-01). Throw, naming both `needle` and `property`, when no body declares it.

Give both a JSDoc explaining why they exist: `bodyForSelectorListToken` returns the last body
whether or not that body declares the property under test, which is right for a
single-declaration selector and wrong for `.activity-row`, declared twice at the top level
(`styles.css:338` carrying `display: flex`, `styles.css:1530` carrying only
`text-decoration: none`). CSS resolves per property, not per rule, so the winner for `display` is
the last body that mentions `display` - that is what `cascadeWinningBodyDeclaring` computes.

**Step 3 - migrate the four Phase 20 assertions**, leaving their `it` titles intact:

- `.activity-row keeps display: flex` -> `cascadeWinningBodyDeclaring('.activity-row', 'display')`
  must contain `display: flex`.
- `D-09: .activity-row:hover mixes from var(--surface) toward var(--text)` ->
  `bodyForSelectorListToken('.activity-row:hover')` must contain
  `color-mix(in srgb, var(--surface) 92%, var(--text))`.
- `D-10: .activity-table__row--navigable declares cursor: pointer` ->
  `bodyForSelectorListToken('.activity-table__row--navigable')` must contain `cursor: pointer`.
- `D-09/D-10: .activity-table__row--navigable:hover mixes with the byte-identical formula` ->
  `bodyForSelectorListToken('.activity-table__row--navigable:hover')` must contain the same
  `color-mix` string.

Leave every other assertion in that describe block exactly as it is - the `selectorListDeclares`
ones are ANY-rule-wins, which is the correct and safe direction for the negative assertions they
carry (`color: var(--accent)` false, `cursor: pointer` false on `.activity-table tbody tr`,
`color-mix` false on its `:hover`).

**Step 4 - add a self-test describe block** proving the migration is not cosmetic, using synthetic
CSS strings passed through the new `source` parameters so `styles.css` is never edited:

- Given a synthetic sheet declaring `.x { display: flex; }` then, later, `.x { display: block; }`:
  `declarationsFor('.x', synthetic)` contains `display: flex` - the FALSE GREEN the old assertion
  would have produced - while `cascadeWinningBodyDeclaring('.x', 'display', synthetic)` contains
  `display: block`. Assert both in the same `it`, with a message naming R3-WR-02 and WR-03.
- Given a synthetic sheet declaring `.x { display: flex; }` then `.x { text-decoration: none; }` -
  the real stylesheet's exact shape: `bodyForSelectorListToken('.x', synthetic)` contains
  `text-decoration: none` and does NOT contain `display`, while
  `cascadeWinningBodyDeclaring('.x', 'display', synthetic)` contains `display: flex`. This is the
  trap `20-REVIEW.md` WR-03's closing paragraph names, asserted rather than described.
- Given a synthetic sheet declaring `.n { cursor: pointer; }` then `.n { cursor: default; }`:
  `declarationsFor('.n', synthetic)` contains `cursor: pointer` while
  `bodyForSelectorListToken('.n', synthetic)` contains `cursor: default` - the navigable-row
  cursor's own version of the same mutation.
- `bodiesForSelectorListToken` on a synthetic sheet with three `.x` rules returns three bodies in
  source order.
- `cascadeWinningBodyDeclaring` throws when no body declares the property, and
  `bodiesForSelectorListToken` throws when the selector is absent entirely - a deleted rule must
  fail loudly.

**Step 5 - extend the helper audit comment block** (the one carrying the R3-WR-02 paragraph) with a
short entry recording that the Phase 20 assertions were migrated off `declarationsFor` in this
round, that `declarationsFor` remains first-wins and is now documented as unsuitable for
possibly-duplicated selectors rather than being converted (converting it would change roughly forty
Phase 16-19 assertions in a plan with no rendered verification), and that
`cascadeWinningBodyDeclaring` exists specifically because `.activity-row` is declared twice at the
top level.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx vitest run src/dashboard/styles.test.ts && node --input-type=module -e "import {readFileSync} from 'node:fs'; const raw=readFileSync('src/dashboard/styles.test.ts','utf8'); const s=raw.replace(/\/\*[\s\S]*?\*\//g,'').replace(/(?<!:)\/\/.*\$/gm,''); if(!/function bodiesForSelectorListToken/.test(s)) throw new Error('bodiesForSelectorListToken not added'); if(!/function cascadeWinningBodyDeclaring/.test(s)) throw new Error('cascadeWinningBodyDeclaring not added'); if(!/function declarationsFor\(selector: string, source/.test(s)) throw new Error('declarationsFor did not gain the optional source parameter'); for(const gone of [\"declarationsFor('.activity-row')\",\"declarationsFor('.activity-row:hover')\",\"declarationsFor('.activity-table__row--navigable')\",\"declarationsFor('.activity-table__row--navigable:hover')\"]){ if(s.includes(gone)) throw new Error('first-wins assertion still present: '+gone); } if(!s.includes(\"cascadeWinningBodyDeclaring('.activity-row', 'display')\")) throw new Error('the display: flex assertion did not migrate to the cascade-winner helper'); for(const need of [\"bodyForSelectorListToken('.activity-row:hover')\",\"bodyForSelectorListToken('.activity-table__row--navigable')\",\"bodyForSelectorListToken('.activity-table__row--navigable:hover')\"]){ if(!s.includes(need)) throw new Error('missing migrated assertion: '+need); } if(!/R3-WR-02/.test(raw)) throw new Error('the helper audit entry naming R3-WR-02 is missing'); console.log('four Phase 20 assertions migrated to last-wins, helpers added, audit updated');" && test -z "$(git status --porcelain src/dashboard/views src/dashboard/styles.css src/dashboard/row-navigation.ts)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.json` exits 0 and `npx vitest run src/dashboard/styles.test.ts`
      exits 0. Record the test count before and after; it must increase by at least five (the
      self-test cases).
    - `styles.test.ts` contains zero occurrences of `declarationsFor('.activity-row')`,
      `declarationsFor('.activity-row:hover')`, `declarationsFor('.activity-table__row--navigable')`
      and `declarationsFor('.activity-table__row--navigable:hover')`.
    - The `display: flex` assertion reads `cascadeWinningBodyDeclaring('.activity-row', 'display')`;
      the other three read `bodyForSelectorListToken` with their own selector token.
    - `declarationsFor` gained an optional `source` parameter defaulting to `cssNoComments`, and its
      JSDoc now marks it FIRST-rule-wins and unsuitable for possibly-duplicated selectors.
    - `bodyForSelectorListToken`, `selectorListDeclares`, `isAtRuleScoped`, `assertNotAtRuleScoped`,
      `RULE_SCANNER` and `extractNumericDeclaration` are byte-identical - `git diff` shows no
      change inside any of them.
    - The self-test block asserts, in-suite, that `declarationsFor` returns the first body while
      `cascadeWinningBodyDeclaring` / `bodyForSelectorListToken` return the cascade winner, for both
      the `display` case and the `cursor` case, and that the `.activity-row`-shaped two-rule case
      (second rule not declaring `display`) resolves to `display: flex`.
    - `bodiesForSelectorListToken` and `cascadeWinningBodyDeclaring` each throw on absence, proven
      by a self-test.
    - The helper audit comment carries a new entry naming this round's migration and the reason
      `declarationsFor` was documented rather than converted.
    - `git status --porcelain src/dashboard/views src/dashboard/styles.css src/dashboard/row-navigation.ts`
      is empty - `styles.css` was NOT edited to simplify the migration.
  </acceptance_criteria>
  <done>Appending a later `.activity-row { display: block }` or `.activity-table__row--navigable { cursor: default }` to the stylesheet now turns the suite red, and the first-wins false green is proven closed by an assertion rather than by a comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> claimed truth | The only boundary this plan sits on. A green suite in this repository has been mistaken for evidence about behaviour it cannot see four times (Phase 16's black page, Phase 17's two rendering defects, Phase 19's dead CSS token, this phase's CR-01/CR-02 behind 960/960 green). Both defects fixed here are guards that cannot fail, which is the same failure mode one layer down. |
| `styles.css` / view sources -> text guard | Both guards read repository source from disk with `readFileSync`. No network, no user input, no execution of the read text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20G-TG-01 | Repudiation | a guard that cannot fail being cited as evidence a decision holds | mitigate | Each repaired guard ships with an in-suite self-test that asserts the OLD form's blind spot and the NEW form catching it - `countOccurrences("tr.tabIndex = 0;", 'tabindex') === 0` next to `rowSemanticViolations(...)` length 1, and `declarationsFor` returning the first body next to the cascade-winner helper returning the last. The proof is executable and permanent, not a prose claim in a summary. |
| T-20G-TG-02 | Tampering | widening the D-01 allowlist later to silence a real violation | mitigate | The allowlist is two narrow rules derived from the six enumerated real call sites (`heading`/`h1` with value `-1`; role value not `link`), each carrying a comment naming the call sites it exists for, and the file header records that widening it requires naming the new call site. `row.tabIndex = -1` is asserted to remain a violation, so the receiver rule cannot be quietly generalised. |
| T-20G-TG-03 | Tampering | editing `styles.css` to make the last-wins migration pass | mitigate | Both tasks assert `git status --porcelain src/dashboard/views src/dashboard/styles.css src/dashboard/row-navigation.ts` is empty. The `.activity-row` two-rule shape is handled by `cascadeWinningBodyDeclaring`, not by consolidating the stylesheet, so no rendered-CSS change enters a plan that has no rendered verification. |
| T-20G-TG-04 | Denial of Service | the new regex scanners hanging on a large source file | accept | All four `rowSemanticViolations` patterns and both new stylesheet scanners are linear, anchored, and free of nested unbounded quantifiers; the largest input is `styles.css` at roughly 1,600 lines. The existing `RULE_SCANNER()` factory is reused rather than a new shared RegExp, so no `lastIndex` state is carried between callers. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized by this plan; no `## Package Legitimacy Audit` is required because no package-manager install task exists. Do not add `jsdom`, `happy-dom`, `postcss`, a CSS parser, or any lint plugin to make either guard easier - both defects are text-guard defects and are fixed with the helpers already in these two files. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- `npx vitest run src/dashboard/row-semantics.test.ts` and
  `npx vitest run src/dashboard/styles.test.ts` each exit 0, with before/after test counts recorded.
- The full suite is deliberately NOT run in this plan: plan 20-09 runs in the same wave and leaves
  `row-navigation.test.ts` RED between its Task 2 and Task 3. The combined green gate is plan
  20-11's Task 1.
- `git status --porcelain src/dashboard/views src/dashboard/styles.css src/dashboard/row-navigation.ts`
  is empty at plan close.
- Nothing here proves anything renders. These are guards over source text, and the file headers of
  both files already say so; this plan does not weaken that framing.
</verification>

<success_criteria>
Both guard-layer defects the fresh review found are closed with executable proof rather than
assertion: the D-01 guard now fails on the exact mutation it was written to block, in every
spelling this codebase uses, and the four Phase 20 stylesheet assertions now read what the browser
would apply instead of the first rule that happens to match.
</success_criteria>

<output>
Create `.planning/phases/20-row-click-interaction-pattern/20-10-SUMMARY.md` when done.
</output>
