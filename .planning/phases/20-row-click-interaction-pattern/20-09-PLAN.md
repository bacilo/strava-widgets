---
phase: 20-row-click-interaction-pattern
plan: 09
type: execute
wave: 8
depends_on: ["20-08"]
files_modified:
  - .planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md
  - src/dashboard/row-navigation.ts
  - src/dashboard/row-navigation.test.ts
autonomous: true
gap_closure: true
requirements: [UX-01, REC-08]
must_haves:
  decisions: [D-01, D-02, D-03, D-10, D-12]
  truths:
    - "A modified click (Cmd/Ctrl/Shift/Alt) on a row cell does not navigate in the current tab - the row defers to the browser's own link contract"
    - "A non-primary mouse button (middle, right) on a row cell does not trigger navigation"
    - "A drag-select that ends inside a row leaves the selection intact and does not navigate away"
    - "A plain primary click on a non-anchor row cell still navigates - the guards only ever reduce the set of navigating clicks, never widen it"
    - "The middle-click/auxclick disposition is a recorded decision (D-12) in 20-CONTEXT.md, not an unexamined gap, and is enforced by a test asserting auxclick is absent from the module"
    - "The link-contract decision is unit-tested with real behavioral assertions, proven RED against the shipped semantics before it is made GREEN"
  artifacts:
    - path: ".planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md"
      provides: "D-12 - the recorded row-click link-contract decision, including the explicit auxclick out-of-scope call"
      contains: "D-12"
    - path: "src/dashboard/row-navigation.ts"
      provides: "shouldNavigateOnRowClick - the pure, node-testable link-contract predicate the click listener delegates to"
      contains: "shouldNavigateOnRowClick"
    - path: "src/dashboard/row-navigation.test.ts"
      provides: "Behavioral coverage of every guard condition plus the source-structure proof that the listener delegates to the predicate"
      contains: "shouldNavigateOnRowClick"
  key_links:
    - from: "src/dashboard/row-navigation.ts attachRowNavigation"
      to: "shouldNavigateOnRowClick"
      via: "the click listener building a RowClickContext from the MouseEvent and the current selection, then delegating the decision"
      pattern: "shouldNavigateOnRowClick\\("
    - from: "shouldNavigateOnRowClick"
      to: "navigateTo(activityDetailPath(activityId))"
      via: "an early return when the predicate is false"
      pattern: "navigateTo\\(activityDetailPath\\("
---

<objective>
Close the BLOCKER `20-VERIFICATION.md` recorded against `row-navigation.ts:58-67`: the row-level
click listener navigates for any click whose target is not inside an anchor, without inspecting
the mouse button, the modifier keys, or an active text selection.

Purpose: plan 20-03 (`670e368`) deleted the "View Activity" anchor column from both Records PR
tables. Confirmed at `records.ts:392-419`: only the Date cell carries an anchor; Rank, Time,
Pace, Age-Grade and Flags are row-click-only, and `.activity-table__row--navigable { cursor:
pointer }` (`styles.css:1544`) advertises all five as link-shaped. So Cmd/Ctrl+click on those
cells loses the user's place instead of opening a background tab, Shift+click and Alt+click do
the same, and a drag-select that ends inside a row discards the selection and navigates away.
The phase goal is propagating `list.ts`'s established pattern - and the pattern being propagated
is a real `<a href>`, which honours every one of those gestures natively. A synthetic row-click
substitute that ignores them is a lesser reinvention, which is exactly what the goal forbids.

Output: a recorded decision (D-12) covering both the guards and the deliberate absence of an
`auxclick` path, a pure predicate carrying the whole link contract, and a node-environment test
proven RED against the shipped semantics before the fix makes it GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md
@.planning/phases/20-row-click-interaction-pattern/20-VERIFICATION.md
@.planning/phases/20-row-click-interaction-pattern/20-REVIEW.md
@.planning/phases/20-row-click-interaction-pattern/20-06-PLAN.md
@.planning/phases/20-row-click-interaction-pattern/20-06-SUMMARY.md

<interfaces>
The module as it ships today (`src/dashboard/row-navigation.ts`), all four exports:

  `NAVIGABLE_ROW_CLASS`   - the string constant `'activity-table__row--navigable'` (D-10's marker)
  `activityDetailPath(activityId: string): string`   - returns `/activity/<id>`
  `activityDetailHref(activityId: string): string`   - returns `#/activity/<id>`
  `attachRowNavigation(rowEl: HTMLElement, activityId: string): void`
      - adds `NAVIGABLE_ROW_CLASS` to `rowEl`
      - registers a `click` listener that returns early when
        `(event.target as HTMLElement).closest('a')` is truthy, and otherwise calls
        `navigateTo(activityDetailPath(activityId))`

`navigateTo` (`src/dashboard/router.ts:174-177`) is the only sanctioned hash writer:
  `navigateTo(path: string, query?: URLSearchParams): void` sets
  `window.location.hash = '#' + path + queryString`.

Call sites of `attachRowNavigation`, all three, unchanged by this plan:
  `src/dashboard/views/list.ts`    - one call, on `buildTableRow`'s `<tr>`
  `src/dashboard/views/records.ts` - two calls, on the PR table `<tr>` (`records.ts:419`) and on
                                     the progression table `<tr>` (`records.ts:532`)

TypeScript note you will rely on: `HTMLElement`'s `addEventListener` overload for the `'click'`
literal resolves the handler parameter to `MouseEvent` via `HTMLElementEventMap`, so
`event.button`, `event.metaKey`, `event.ctrlKey`, `event.shiftKey` and `event.altKey` are all
available without a cast. `event.target` is still `EventTarget | null`, so the existing
`as HTMLElement` cast on that one access stays.
</interfaces>

<constraints>
- **This repository has no DOM in its test environment, and this plan authorizes zero installs.**
  `vitest.config.ts` sets `environment: 'node'`; devDependencies contain `vitest` and nothing
  else test-related. Do not add `jsdom`, `happy-dom`, `@testing-library/*`, `puppeteer` or
  `playwright` - `20-REVIEW.md`'s WR-06 proposes exactly that and it is **declined** here, for
  the same reason `20-06-PLAN.md` declined it: introducing a DOM runtime mid-phase changes what
  every existing source-structure guard in this repo is measuring. The link contract is instead
  made testable by extracting it into a pure predicate.
- Consequently: the listener's own DOM plumbing (`closest('a')`, `window.getSelection()`, the
  `addEventListener` wiring) is **not** automatically provable. It is proven by (a) the
  source-structure assertion this plan adds and (b) the Round 3 human checkpoint rows in plan
  20-11. Say so plainly in the module header and in the summary; do not overstate coverage.
- Do not add a `keydown` handler and do not add Space activation (D-02).
  `row-semantics.test.ts` asserts comment-stripped `row-navigation.ts` contains zero occurrences
  of `keydown` and will fail if you do.
- Do not add `tabindex` or `role` to any element (D-01). The same guard file asserts their
  absence, and plan 20-10 tightens that guard in this same wave.
- Do not change `NAVIGABLE_ROW_CLASS`, `activityDetailPath` or `activityDetailHref`. Their
  existing tests pin exact strings and `row-semantics.test.ts` pins per-file call counts
  (`activityDetailHref(` twice in `list.ts`, once in `overview.ts`, twice in `records.ts`).
- Do not touch `list.ts`, `overview.ts`, `records.ts`, `styles.css` or `router.ts`. This plan's
  entire runtime diff is inside `row-navigation.ts`.
- **Out of scope, do not opportunistically fix even though you will read past them:** WR-01
  (`notedActivityId` surviving three list render paths), WR-04 (`.activity-list--cards` being
  `display: block`), WR-05 (`appendStatusBadges` string-equality dispatch), WR-07 (accessible-name
  divergence between the two list surfaces), and every IN-0x item in `20-REVIEW.md`. If one of
  those appears in your diff, the diff is wrong.
- **Plan 20-10 runs in this same wave** and edits `src/dashboard/row-semantics.test.ts` and
  `src/dashboard/styles.test.ts`. Do not edit either file, and when you run the full suite treat
  a failure inside those two files as "another plan is mid-flight", not as a regression of this
  plan - re-check by running only this plan's own files.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record D-12 - the row-click link contract, and the explicit auxclick call</name>
  <files>.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md</files>
  <read_first>
    - `.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md` in full - specifically the
      `<decisions>` block's "Row semantic and keyboard model" group (D-01 through D-04), the
      `### Claude's Discretion` list at the end of that block, and the `<deferred>` block. D-02 is
      the shape to copy: a decision that records a deliberate ABSENCE so a later agent does not
      "fix" it as an oversight.
    - `.planning/phases/20-row-click-interaction-pattern/20-REVIEW.md` section "Critical Issues"
      (CR-01, Round 2 numbering) - the three concrete failures, the drafted patch, and the closing
      paragraph that explicitly asks for the auxclick disposition to be a recorded decision.
    - `.planning/phases/20-row-click-interaction-pattern/20-VERIFICATION.md` frontmatter `gaps:` -
      the second gap entry's `missing:` list names both halves of this plan's acceptance target.
    - `src/dashboard/views/records.ts` lines 380-425 - see with your own eyes that only `dateTd`
      receives an anchor and that Rank/Time/Pace/Age-Grade/Flags do not.
  </read_first>
  <action>
Add a new decision **D-12** to `20-CONTEXT.md`'s `<decisions>` block, inserted immediately after
the D-02 bullet and before D-03, inside the existing "### Row semantic and keyboard model"
sub-heading. Match the surrounding bullet formatting exactly (a `- **D-NN: ...**` lead sentence
in bold, then indented sub-bullets).

Title it: **D-12: The row-click listener honours the browser's link contract; middle-click
(`auxclick`) is explicitly out of scope.**

The decision body must state, concretely:

1. **What the listener now refuses to do.** It returns without navigating when `event.button` is
   not `0`, when any of `metaKey` / `ctrlKey` / `shiftKey` / `altKey` is set, or when
   `window.getSelection()` returns a selection that is both non-collapsed and non-empty. The
   existing `closest('a')` guard stays first and unchanged.
2. **Why this is load-bearing rather than cosmetic.** Plan 20-03 (`670e368`) removed the
   "View Activity" anchor column from both Records PR tables, so row-click is now the *sole*
   affordance on five of six PR-table cells (Rank, Time, Pace, Age-Grade, Flags - only Date
   carries an anchor, `records.ts:396-419`), while D-10's `.activity-table__row--navigable
   { cursor: pointer }` advertises them as link-shaped. Swallowing a Cmd+click into a same-tab
   hash change, or destroying a drag-selection, is the opposite of "propagating `list.ts`'s
   established pattern" - a real `<a href>`, the pattern being propagated, honours all of these
   natively.
3. **Why `auxclick` is NOT handled, stated as a decision rather than left as a gap.** Browsers
   fire `auxclick`, not `click`, for the middle button, so a middle-click on a row-only cell does
   nothing - which is exactly what a middle-click does on any non-link element, so it is an absent
   affordance, not a hijacked gesture. Synthesising one would mean calling
   `window.open(activityDetailHref(activityId), '_blank')`, which *invents* behaviour instead of
   propagating `list.ts`'s (contradicting D-03 and UX-01), adds a popup-blocker and
   `noopener`-shaped surface for no requirement, and is unverifiable in this repository (no DOM
   and no browser in the test environment - see the Established Patterns note in `<code_context>`).
   The real fix is to give those five cells a real anchor, which needs the activity-name join D-05
   already deferred to Phase 21. Recorded here explicitly so a later agent does not read the
   absence as an oversight - the same reason D-02 exists.
4. **How the decision is enforced.** `row-navigation.test.ts` asserts that comment-stripped
   `row-navigation.ts` contains zero occurrences of `auxclick`, so silently reversing this
   decision turns the suite red.
5. **Where it is observed.** The listener's DOM plumbing is unprovable by any automated test in
   this repository; the rendered behaviour is checked by the Round 3 human checkpoint rows
   covering modifier-click, Shift/Alt-click, middle-click and drag-select on the Records PR table
   (plan 20-11).

Then amend the `### Claude's Discretion` list in the same block. The bullet currently reading
"Whether the `<tr>` click handler additionally guards against text-selection drags and
cmd/ctrl/shift/middle-clicks on the non-anchor part of a row. Default: match what `list.ts`
already ships (the `closest('a')` guard only)." must be replaced with a line recording that this
discretion was **exercised and is now superseded by D-12** after `20-VERIFICATION.md` recorded the
unguarded listener as a BLOCKER, with a one-line note that the stated default (match `list.ts`'s
`closest('a')`-only guard) was the wrong call once plan 20-03 removed the CTA anchor. Do not
delete the bullet - a discretion silently vanishing reads as if it was never considered.

Finally, add one entry to the `<deferred>` block's "## Deferred Ideas" list: an `auxclick` handler,
or a real anchor on the Records PR table's remaining five cells once Phase 21 joins the activity
name into `PrTableRow`, so middle-click works natively there. Reference D-12 and D-05.

Change nothing else in the file. Do not renumber, reword or re-scope D-01 through D-11.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs'; const p='.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md'; const s=readFileSync(p,'utf8'); const need=['D-12','auxclick','event.button','metaKey','getSelection','670e368','records.ts:396-419','window.open']; for(const n of need){ if(!s.includes(n)) throw new Error('20-CONTEXT.md is missing the required token: '+n); } if(!/\*\*D-12:/.test(s)) throw new Error('D-12 is not formatted as a bold decision lead like D-01..D-11'); if(s.includes(\"Default: match what \`list.ts\` already ships\")) throw new Error(\"the superseded Claude's Discretion default is still stated as current\"); if(!/D-12/.test(s.split('## Deferred Ideas')[1]||'')) throw new Error('no Deferred Ideas entry referencing D-12'); for(let i=1;i<=11;i++){ const id='D-'+String(i).padStart(2,'0'); if(!s.includes('**'+id+':')) throw new Error('pre-existing decision '+id+' lost its bold lead - decisions must not be renumbered or reworded'); } console.log('D-12 recorded, discretion superseded, deferred entry present, D-01..D-11 intact');" && test -z "$(git status --porcelain src scripts)"</automated>
  </verify>
  <acceptance_criteria>
    - `20-CONTEXT.md` contains a bold `**D-12:` decision lead inside the `<decisions>` block's
      "Row semantic and keyboard model" group, positioned between D-02 and D-03.
    - D-12's text contains all of: `event.button`, `metaKey`, `getSelection`, `auxclick`,
      `window.open`, `670e368`, `records.ts:396-419`.
    - D-12 states the auxclick disposition as out-of-scope **with** the three named reasons
      (invents rather than propagates, popup/noopener surface for no requirement, unverifiable in
      this repository) and points at D-05/Phase 21 as the real fix.
    - The `### Claude's Discretion` bullet about modifier/middle clicks no longer states
      "Default: match what `list.ts` already ships" as current guidance and instead records the
      discretion as exercised and superseded by D-12.
    - The "## Deferred Ideas" list carries a new entry naming `auxclick` and referencing D-12
      and D-05.
    - Decisions D-01 through D-11 are byte-identical apart from the single amended discretion
      bullet - `git diff` on the file shows no other deletion.
    - `git status --porcelain src scripts` is empty; this task changes no source.
  </acceptance_criteria>
  <done>The middle-click disposition is a decision with stated reasoning that a later agent can find, agree with or overturn - not an unexamined gap.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the click decision into a pure predicate, preserving behavior - the new tests must be RED</name>
  <files>src/dashboard/row-navigation.ts, src/dashboard/row-navigation.test.ts</files>
  <read_first>
    - `src/dashboard/row-navigation.ts` in full (68 lines) - the header comment block, all four
      exports, and the listener body you are refactoring.
    - `src/dashboard/row-navigation.test.ts` in full (53 lines) - match its import style
      (`from './row-navigation.js'`), its `describe`/`it` phrasing, and its `ACTIVITY_ID` constant
      habit. Note its header comment, which currently states that `attachRowNavigation` is not
      covered here; Task 3 updates that wording, not this task.
    - `.planning/phases/20-row-click-interaction-pattern/20-06-PLAN.md` Task 1 - the RED-before-GREEN
      precedent this task follows, including how its verify command asserts a NON-ZERO vitest exit.
    - `.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md` D-12 as written by Task 1 -
      the contract you are about to encode.
  </read_first>
  <action>
This task deliberately leaves the new behavioral tests FAILING. The fix in Task 3 is only credible
if the tests are observed failing against the currently-shipped decision logic first. **Do not
tighten the predicate in this task.**

In `src/dashboard/row-navigation.ts`:

1. Add an exported interface named `RowClickContext` with exactly these seven fields, all
   required: `button: number`, `metaKey: boolean`, `ctrlKey: boolean`, `shiftKey: boolean`,
   `altKey: boolean`, `insideAnchor: boolean`, `hasTextSelection: boolean`. Give it a short JSDoc
   stating it is the DOM-free description of one row click, extracted so the decision is testable
   under `environment: 'node'`.
2. Add an exported function `shouldNavigateOnRowClick(context: RowClickContext): boolean`. In THIS
   task its body encodes the currently-shipped semantics and nothing more: it returns
   `!context.insideAnchor`, ignoring every other field. Add a one-line comment marking the body as
   the pre-fix baseline that Task 3 replaces, so a mid-plan reader is not confused into thinking
   the guards were forgotten.
3. Rewire `attachRowNavigation`'s existing `click` listener to build a `RowClickContext` and
   delegate. Type the handler parameter as `MouseEvent` (the `'click'` literal overload gives you
   this). Populate the context as follows: `button`, `metaKey`, `ctrlKey`, `shiftKey` and `altKey`
   read straight off the event; `insideAnchor` is `Boolean((event.target as HTMLElement).closest('a'))`,
   keeping the existing cast; `hasTextSelection` is computed from a local `const selection =
   window.getSelection();` as `Boolean(selection && !selection.isCollapsed && selection.toString().length > 0)`.
   Then `if (!shouldNavigateOnRowClick(context)) { return; }` followed by the unchanged
   `navigateTo(activityDetailPath(activityId));`.
   Keep the `rowEl.classList.add(NAVIGABLE_ROW_CLASS)` line byte-identical and first.
   Because the predicate ignores every field except `insideAnchor`, this step is behaviour-preserving
   by construction - state that in the summary.
4. Do not write `auxclick` anywhere, in code or in a comment, in this task or the next. Task 3 adds
   a test that asserts its absence in comment-stripped source.

In `src/dashboard/row-navigation.test.ts`:

Import `shouldNavigateOnRowClick` (and the `RowClickContext` type) alongside the existing imports.
Add a module-level factory - name it `plainPrimaryClick` - returning a fresh `RowClickContext`
whose fields are the permissive baseline: `button: 0`, all four modifier flags `false`,
`insideAnchor: false`, `hasTextSelection: false`. Every case below spreads that factory's result
and overrides exactly one field, so each `it` names precisely one condition.

Add a `describe` block titled for the defect (the row-click link contract / D-12 / the BLOCKER
recorded in `20-VERIFICATION.md`) containing these cases, each its own `it`:

- **Baseline, must pass now and after:** a plain primary click on a non-anchor cell returns `true`.
- **Baseline, must pass now and after:** `insideAnchor: true` returns `false` - the row defers to
  its own in-cell anchor and never double-navigates.
- **Will fail now:** `metaKey: true` returns `false`.
- **Will fail now:** `ctrlKey: true` returns `false`.
- **Will fail now:** `shiftKey: true` returns `false`.
- **Will fail now:** `altKey: true` returns `false`.
- **Will fail now:** `button: 1` (middle) returns `false`.
- **Will fail now:** `button: 2` (secondary) returns `false`.
- **Will fail now:** `hasTextSelection: true` returns `false`.
- **Will fail now, and is the anti-over-blocking case:** a context with `button: 0`, no modifiers,
  `insideAnchor: false` and `hasTextSelection: false` returns `true` **while** each of the seven
  single-field variants above returns `false` - assert this as one table-shaped case so a future
  over-tightening (for example refusing every click when any selection object exists, collapsed or
  not) fails loudly.

Write each failing case's assertion message so a reader who sees it fail knows what regressed:
name the gesture, name the five anchor-less Records PR-table cells it applies to, and cite D-12.

Also add a `describe` block asserting the **wiring**, which is the only automated proof available
that the listener actually consults the predicate. Read this module's own source with
`readFileSync(new URL('./row-navigation.ts', import.meta.url), 'utf8')`, strip comments with the
same two-step used in `row-semantics.test.ts` (block comments non-greedy, then `//`-to-end-of-line
where the `//` is not preceded by `:`), and assert on the stripped text that:
- `shouldNavigateOnRowClick(` occurs exactly twice - the definition and its single call site;
- the listener reads `event.button`, `event.metaKey`, `event.ctrlKey`, `event.shiftKey`,
  `event.altKey` and `window.getSelection(`, each at least once;
- `closest('a')` still occurs exactly once.
These wiring assertions PASS in this task, because step 3 already landed the delegation. Only the
behavioral cases are RED.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && node --input-type=module -e "import {readFileSync} from 'node:fs'; const raw=readFileSync('src/dashboard/row-navigation.ts','utf8'); const s=raw.replace(/\/\*[\s\S]*?\*\//g,'').replace(/(?<!:)\/\/.*$/gm,''); if(!/export function shouldNavigateOnRowClick/.test(s)) throw new Error('predicate not exported'); if(!/export interface RowClickContext/.test(s)) throw new Error('RowClickContext not exported'); if((s.match(/shouldNavigateOnRowClick\(/g)||[]).length!==2) throw new Error('expected exactly 2 shouldNavigateOnRowClick occurrences (definition + one call site)'); if(!/window\.getSelection\(/.test(s)) throw new Error('listener does not read window.getSelection()'); if(!/event\.button/.test(s)) throw new Error('listener does not read event.button'); if(/auxclick/i.test(s)) throw new Error('auxclick must not appear - D-12 records it as out of scope'); if(/keydown/.test(s)) throw new Error('D-02 violated: keydown introduced'); if(/tabindex/i.test(s)) throw new Error('D-01 violated: tabindex introduced'); const body=(s.split('export function shouldNavigateOnRowClick')[1]||'').split('export ')[0]; if(/metaKey|ctrlKey|shiftKey|altKey|context\.button|hasTextSelection/.test(body)) throw new Error('Task 2 must NOT tighten the predicate - guard fields found in its body'); console.log('staged for RED: predicate extracted with pre-fix semantics, listener delegating');" && if npx vitest run src/dashboard/row-navigation.test.ts; then echo 'UNEXPECTED GREEN - the link-contract tests pass against the shipped semantics, so they do not cover the defect'; exit 1; else echo 'EXPECTED RED - the link-contract cases fail against the shipped semantics'; fi</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - Comment-stripped `row-navigation.ts` exports `RowClickContext` and `shouldNavigateOnRowClick`,
      and `shouldNavigateOnRowClick(` appears exactly twice (definition plus the listener's single
      call site).
    - The predicate's own body contains none of `metaKey`, `ctrlKey`, `shiftKey`, `altKey`,
      `context.button`, `hasTextSelection` at this point - the guards are deliberately not applied
      yet.
    - Comment-stripped `row-navigation.ts` contains zero occurrences of `auxclick`, `keydown` and
      `tabindex`.
    - `npx vitest run src/dashboard/row-navigation.test.ts` exits NON-ZERO. Record the failure
      output verbatim in the summary: the failing cases must be exactly the seven single-field
      guard cases plus the anti-over-blocking table case - nine assertions across those cases in
      total. The two baseline cases and every wiring assertion pass in this same run. If a baseline
      or wiring case fails, the refactor changed behavior and must be corrected before Task 3.
    - The three pre-existing `describe` blocks (`activityDetailPath`, `activityDetailHref`,
      `NAVIGABLE_ROW_CLASS`) still pass unchanged.
    - `git status --porcelain src` lists only `src/dashboard/row-navigation.ts` and
      `src/dashboard/row-navigation.test.ts`.
  </acceptance_criteria>
  <done>The link-contract defect is pinned by an executing test that fails for the real reason, and the failure output is on the record.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the link contract - the tests turn GREEN, and the module header records D-12</name>
  <files>src/dashboard/row-navigation.ts, src/dashboard/row-navigation.test.ts</files>
  <read_first>
    - `src/dashboard/row-navigation.ts` as left by Task 2 - the predicate you are replacing and the
      listener you are leaving alone.
    - `src/dashboard/row-navigation.test.ts` as left by Task 2 - the eleven cases you are satisfying.
    - `.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md` D-12 as recorded by Task 1 -
      the wording the module header must stay consistent with.
    - `.planning/phases/20-row-click-interaction-pattern/20-REVIEW.md` "Critical Issues" - the
      drafted patch, so you can state where this implementation follows it and where it deviates.
  </read_first>
  <action>
In `src/dashboard/row-navigation.ts`, replace `shouldNavigateOnRowClick`'s pre-fix body with the
full link contract, in this evaluation order, each condition its own early return so the reason a
click was refused stays readable:

1. `context.insideAnchor` is true, return `false` - the row's own in-cell anchor navigates itself;
   this is the pre-existing guard and stays first.
2. `context.button !== 0`, return `false` - a non-primary button is the browser's to own.
3. any of `context.metaKey`, `context.ctrlKey`, `context.shiftKey`, `context.altKey` is true,
   return `false` - new tab, new window and download all belong to the browser.
4. `context.hasTextSelection` is true, return `false` - a drag-select that ends inside the row must
   survive.
5. otherwise return `true`.

Do not touch `attachRowNavigation`, `NAVIGABLE_ROW_CLASS`, `activityDetailPath` or
`activityDetailHref` in this task. The listener already delegates; the whole diff is the predicate
body plus documentation.

Replace the pre-fix marker comment with a JSDoc on `shouldNavigateOnRowClick` recording, in this
order:
- what it decides, in one sentence, and that it is pure so it can be unit-tested under
  `environment: 'node'`;
- the four refusal conditions and the one-line reason for each;
- that the ordering is deliberate: `insideAnchor` first, so an anchor click is refused for the
  double-navigation reason rather than incidentally by a later guard.

Extend the module's existing file header - the block that already records D-01, D-02 and the honest
"assertable surface" paragraph - with a **D-12** paragraph in the same voice, stating:
- the guards, and that they were added after `20-VERIFICATION.md` recorded the unguarded listener
  as a BLOCKER, which became load-bearing when plan 20-03 (`670e368`) removed the "View Activity"
  anchor column and left five of six Records PR-table cells row-click-only;
- that middle-click is deliberately NOT handled and why, pointing at D-12 in
  `.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md` for the full reasoning - written
  in the same "so a later agent does not fix this as an oversight" register the D-02 paragraph uses;
- a stated deviation from `20-REVIEW.md`'s drafted patch: the review inlined all three checks in the
  listener, this implementation extracts them into `shouldNavigateOnRowClick` instead, because an
  inline check inside an `addEventListener` callback is unreachable from any test in a repository
  with no DOM - the same reasoning that drove `20-06-PLAN.md` to use `tagName === 'A'` over
  `instanceof HTMLAnchorElement`. The refusal conditions themselves are the review's, unchanged.

Also update the module header's "assertable surface, honestly" paragraph and the header comment at
the top of `row-navigation.test.ts`: `shouldNavigateOnRowClick` IS now unit-tested and is where the
whole click decision lives; `attachRowNavigation`'s remaining DOM plumbing (`closest`,
`getSelection`, the event wiring) is proven only by the source-structure assertions in
`row-navigation.test.ts` plus the Round 3 human checkpoint (plan 20-11). Do not claim more than
that.

Finally, add one assertion to `row-navigation.test.ts`'s wiring `describe`: comment-stripped
`row-navigation.ts` contains zero occurrences of `auxclick` (case-insensitive), with a message
naming D-12 - this is what makes the recorded decision enforceable rather than advisory.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx vitest run src/dashboard/row-navigation.test.ts src/dashboard/views/list.test.ts src/dashboard/views/overview.test.ts && node --input-type=module -e "import {readFileSync} from 'node:fs'; const raw=readFileSync('src/dashboard/row-navigation.ts','utf8'); const s=raw.replace(/\/\*[\s\S]*?\*\//g,'').replace(/(?<!:)\/\/.*$/gm,''); const body=(s.split('export function shouldNavigateOnRowClick')[1]||'').split('export function attachRowNavigation')[0]; for(const n of ['insideAnchor','button','metaKey','ctrlKey','shiftKey','altKey','hasTextSelection']){ if(!body.includes(n)) throw new Error('predicate body does not consider '+n); } if((s.match(/shouldNavigateOnRowClick\(/g)||[]).length!==2) throw new Error('listener no longer delegates exactly once'); if((s.match(/closest\('a'\)/g)||[]).length!==1) throw new Error('the closest(a) guard was lost or duplicated'); if(/auxclick/i.test(s)) throw new Error('auxclick present - D-12 records it as out of scope'); if(/keydown/.test(s)) throw new Error('D-02 violated: keydown introduced'); if(/tabindex/i.test(s)) throw new Error('D-01 violated: tabindex introduced'); if(!/navigateTo\(activityDetailPath\(/.test(s)) throw new Error('navigation call site lost'); console.log('link contract applied, delegation intact, D-12 enforced');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - `npx vitest run src/dashboard/row-navigation.test.ts` exits 0 - all eleven cases from Task 2
      pass, including the nine assertions that failed there.
    - `src/dashboard/views/list.test.ts` and `src/dashboard/views/overview.test.ts` still pass with
      the same counts as before this plan.
    - The full suite (`npx vitest run`) is executed and its result recorded in the summary. Any
      failure must be either (a) one of the five pre-existing data-dependent files named in
      `deferred-items.md`, or (b) inside `row-semantics.test.ts` / `styles.test.ts`, which plan
      20-10 is editing in this same wave. Re-run this plan's own files to confirm; a failure
      anywhere else is a regression and blocks this plan.
    - The predicate body considers all seven `RowClickContext` fields; the listener delegates
      exactly once; `closest('a')` appears exactly once.
    - Comment-stripped `row-navigation.ts` contains zero occurrences of `auxclick` (case-insensitive),
      `keydown` and `tabindex`, and `row-navigation.test.ts` asserts the `auxclick` zero-count with
      a message naming D-12.
    - The module header carries a D-12 paragraph naming `670e368`, the five anchor-less PR-table
      cells, and the stated deviation from `20-REVIEW.md`'s inline patch with its reason.
    - The module header's "assertable surface" paragraph and `row-navigation.test.ts`'s header
      comment both now say that the decision logic is unit-tested and the DOM plumbing is not,
      naming the Round 3 checkpoint as the plumbing's only rendered proof.
    - `git status --porcelain src` lists only `src/dashboard/row-navigation.ts` and
      `src/dashboard/row-navigation.test.ts`.
  </acceptance_criteria>
  <done>A modified click, a non-primary button and a live text selection all leave the page alone, a plain primary click still navigates, and every one of those is held by a test that was observed failing against the shipped code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dashboard-index.json` / records shards -> `location.hash` | `activityId` is an archive-sourced string (numeric Strava ids and `i`-prefixed intervals.icu ids) that `activityDetailPath` interpolates into a path which `navigateTo` writes to `window.location.hash`. This plan does not change that path; it only narrows which clicks reach it. |
| user gesture -> navigation | The click listener converts a raw `MouseEvent` plus the current text selection into a boolean. All inputs are browser-owned facts about the user's own gesture on the current page. |
| `window.getSelection()` -> guard decision | A newly-read browser API. Only its collapsed-ness and its string length are consumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20G-RN-01 | Tampering | `activityDetailPath(activityId)` interpolating an archive-sourced id into `location.hash` | accept | Pre-existing and unchanged by this plan. The value is only ever concatenated after a literal `#` by `navigateTo` (`router.ts:174-177`), so no scheme change and no cross-origin navigation is reachable - the worst case is a same-origin hash the router does not resolve, which renders the not-found view. The archive is this developer's own intervals.icu export, not third-party input. This plan's guards strictly reduce the number of clicks that reach this sink; they cannot introduce a new destination. |
| T-20G-RN-02 | Elevation of Privilege | the new guards being written so they navigate on a click that previously did not | mitigate | `shouldNavigateOnRowClick` is monotonically more restrictive than the shipped predicate: every added condition can only return `false`. Task 2's two baseline cases (plain primary click returns `true`; `insideAnchor` returns `false`) are asserted RED-phase-green, so a refactor that widened the navigating set would fail before Task 3 ever runs. The anti-over-blocking table case pins the other direction. |
| T-20G-RN-03 | Information Disclosure | reading the user's current text selection on every row click | accept | `window.getSelection()` is same-document and already fully visible to this page's script. Only `isCollapsed` and `toString().length` are consumed - the selected text is never stored, logged, interpolated into a URL, or written to the DOM. No new sink exists. |
| T-20G-RN-04 | Denial of Service | a throw inside the click listener breaking every navigable row on the page | mitigate | The listener performs no parsing and no allocation beyond one object literal; `getSelection()` returning `null` is handled by the `Boolean(selection && ...)` guard, and `event.target` keeps its existing cast with `closest` returning `null` safely. The predicate is total over its input type - no branch can throw - and every field combination it is given is covered by Task 2's cases. |
| T-20G-RN-05 | Repudiation | reversing D-12's auxclick disposition silently in a later phase | mitigate | Task 3 adds a test asserting comment-stripped `row-navigation.ts` contains zero occurrences of `auxclick`, so adding a middle-click path turns the suite red and forces the agent to read D-12 first - the same mechanism D-02's `keydown` zero-count already uses. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized by this plan; no `## Package Legitimacy Audit` is required because no package-manager install task exists. `20-REVIEW.md` WR-06 proposes adding `happy-dom`; this plan explicitly declines it. Do not install `jsdom`, `happy-dom`, `@testing-library/*`, `puppeteer` or `playwright` - the pure-predicate extraction is the sanctioned mechanism and needs no dependency. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- Task 2's run of `npx vitest run src/dashboard/row-navigation.test.ts` exits non-zero with only
  the nine link-contract assertions failing; Task 3's run of the same command exits 0.
- The full suite result is recorded, with any failure attributed to either the five known
  data-dependent files or to plan 20-10's in-flight edits to `row-semantics.test.ts` /
  `styles.test.ts`.
- `20-CONTEXT.md` carries D-12 with the auxclick disposition, its reasoning, and a matching
  Deferred Ideas entry; D-01..D-11 are unchanged.
- `git status --porcelain src` lists only `row-navigation.ts` and `row-navigation.test.ts`.
- Nothing here proves that a real browser honours the guards. Cmd/Ctrl+click, Shift/Alt+click,
  middle-click and drag-select on the five anchor-less Records PR-table cells are rows R18-R21 of
  plan 20-11's Round 3 checkpoint, and that is stated as such in the module header.
</verification>

<success_criteria>
The row-click affordance behaves like the link it advertises itself as: modified clicks and
non-primary buttons are handed back to the browser, a drag-select survives, a plain click still
navigates, the middle-click gap is a decision with stated reasoning rather than an oversight, and
the whole contract lives in one pure function that a node-environment test can actually falsify.
</success_criteria>

<output>
Create `.planning/phases/20-row-click-interaction-pattern/20-09-SUMMARY.md` when done.
</output>
