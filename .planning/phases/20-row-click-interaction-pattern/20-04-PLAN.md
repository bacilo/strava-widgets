---
phase: 20-row-click-interaction-pattern
plan: 04
type: execute
wave: 3
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - src/dashboard/styles.css
  - src/dashboard/styles.test.ts
  - src/dashboard/row-semantics.test.ts
autonomous: true
requirements: [UX-02, UX-03]
must_haves:
  decisions: [D-01, D-02, D-05, D-06, D-09, D-10, D-11]
  truths:
    - "D-06: a bare anchor no longer renders as a browser-default blue underlined link in either theme - styles.css had zero bare a rules before this plan"
    - "D-09: a hovered Overview row and a hovered Records or Activities table row use the byte-identical color-mix formula Phase 19 D-06/D-08 established"
    - "D-10: cursor: pointer and the row hover apply only to rows the helper marked navigable - the Riegel, two Trends and best-efforts tables no longer advertise a click they cannot perform"
    - "D-11: no row-specific focus-ring variant is added; the rows inherit Phase 19 D-09's two-tone ring unchanged and the human checkpoint arbitrates"
    - "The .activity-row rule still declares display: flex, which is what keeps a now-inline <a> laid out as a row"
    - "The .cta rules and the .cta:hover assertion survive - five retry buttons and two back CTAs still use them"
  artifacts:
    - path: "src/dashboard/styles.css"
      provides: "Phase 20 block - bare a rule, row-anchor hover, navigable-row scoping"
      contains: "activity-table__row--navigable"
    - path: "src/dashboard/styles.test.ts"
      provides: "Text assertions pinning the Phase 20 rules, including negative assertions for the de-scoped selectors"
      contains: "Phase 20"
    - path: "src/dashboard/row-semantics.test.ts"
      provides: "Source-text structural guard over the view files - CTA absence, helper call counts, D-01/D-02 enforcement"
      min_lines: 80
  key_links:
    - from: "src/dashboard/styles.css"
      to: "src/dashboard/row-navigation.ts"
      via: "NAVIGABLE_ROW_CLASS literal shared between the helper and the stylesheet"
      pattern: "activity-table__row--navigable"
---

<objective>
Land the stylesheet half of the phase - the shared link treatment Phase 19 deferred (D-06), the
row-anchor hover (D-09), and the re-scoping of `cursor: pointer` and the row hover onto
actually-clickable rows (D-10) - and build the two regression guards that keep this phase's
structural claims from rotting.

Purpose: UX-03 (a row-level affordance that reads as one, in both themes) and UX-02 (the CTA
removal is now structurally pinned, not just done once). Also closes the standing defect where
four non-activity tables show a pointer cursor and light up on hover while doing nothing.

Output: a Phase 20 block in `styles.css`, a matching `describe` block in `styles.test.ts`, and
a new `src/dashboard/row-semantics.test.ts` source-structure guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md
@.planning/phases/20-row-click-interaction-pattern/20-PATTERNS.md
@.planning/phases/20-row-click-interaction-pattern/20-01-SUMMARY.md
@.planning/phases/20-row-click-interaction-pattern/20-02-SUMMARY.md
@.planning/phases/20-row-click-interaction-pattern/20-03-SUMMARY.md

<interfaces>
Test helpers already in `src/dashboard/styles.test.ts` - reuse, do not reimplement:

  `declarationsFor(selector: string): string` (`styles.test.ts:40-48`) - returns the body of the
  first rule whose head is exactly `selector` followed by optional whitespace and `{`. **Throws**
  when no rule matches, so a deleted rule fails loudly.

  `selectorListDeclares(needle: string, declaration: string): boolean` (`styles.test.ts:125-138`)
  - scans every rule, splits its head into top-level selectors, and returns true when some rule
  lists `needle` as an exact selector and its body contains `declaration`. **Returns false**
  rather than throwing, which is what makes it the right tool for a negative assertion.

  `cssNoComments` (`styles.test.ts:29`) - the comment-stripped view of the stylesheet.

From `src/dashboard/row-navigation.ts`:

  `NAVIGABLE_ROW_CLASS === 'activity-table__row--navigable'`

Existing declarations reused verbatim:

  The Phase 19 D-06/D-08 hover formula, currently at `styles.css:530-532`:
  `background: color-mix(in srgb, var(--surface) 92%, var(--text));`
</interfaces>

<binding_facts>
Established during pattern mapping and source verification. Do not re-derive these; do not
contradict them.

- `styles.css` contains **zero** rules for a bare `a`. The only two `text-decoration: none`
  declarations in the whole file are on `.app-nav__link` (`styles.css:256`) and `.cta`
  (`styles.css:374`). Every other anchor in the app - `.app-nav__brand` (which has **no CSS rule
  at all**), `.detail-nav`'s "Newer"/"Older" links, `list.ts`'s Activity-cell anchor, and every
  anchor plans 20-02 and 20-03 just added - renders as a browser-default blue underlined link
  today, including in dark theme against `--bg #1a1a2e`. This is D-06's premise, verified.
- All six `.activity-table pr-table` tables carry the **identical** class string: the two
  Records activity tables (`records.ts:338`, `records.ts:474`) and four that are not activities
  and never will be clickable - Riegel race predictions (`records.ts:608`), two Trends tables
  (`trends.ts:531`, `trends.ts:1022`) and the detail view's best-efforts table
  (`detail-sections.ts:395`). **D-10's scoping therefore cannot key off `pr-table`.** It keys off
  the marker class the helper applies.
- `.activity-row` (`styles.css:335-343`) declares `display: flex; flex-direction: row;
  flex-wrap: wrap`. Anchors are `inline` by default, so this declaration is now load-bearing.
  No competing UA style exists beyond `inline`, so the existing rule fully overrides it - but it
  must be pinned so a later "cleanup" cannot silently drop it.
- `.activity-row__name` declares `color: var(--text)`; `.activity-row__meta` and `.badge` each
  declare `color: var(--text-secondary)`. All three children set their own colour, so a `color`
  declaration on the row anchor cannot fight them.
- `--accent` on `--bg` was measured by this repository at ~3.4:1 in light and ~2.8:1 in dark
  (recorded at `17-UI-SPEC.md:105`). Both are below WCAG AA's 4.5:1 floor for normal-size text.
  **This is why the D-06 link rule must not set `color: var(--accent)`** - it would ship a text
  contrast failure across every anchor in the app.
- `.records-jump__link` is a `<button>`, not an anchor (`records.ts:159`), so the bare `a` rule
  does not reach it.
- After plans 20-02 and 20-03, `className = 'cta'` appears exactly 7 times in
  `src/dashboard/views/`: `detail.ts` x4 (two retry, two back), `trends.ts` x1,
  `detail-map.ts` x1, `records.ts` x1. That matches `20-CONTEXT.md`'s "five retry buttons and
  two back CTAs" exactly.
</binding_facts>

<constraints>
- **Do not delete any `.cta` rule and do not delete the `.cta:hover mixes from var(--accent)`
  assertion at `styles.test.ts:723-727.`** `.cta` survives this phase with seven consumers.
- Theming is attribute-driven only. New rules theme via tokens under the `[data-theme]` blocks;
  `styles.test.ts` asserts the absence of `prefers-color-scheme` in this file and that must stay
  true.
- No new tokens, no new spacing scale, no fifth type role (18-UI-SPEC section 17, carried
  forward through Phase 19). Reuse `--text`, `--text-secondary`, `--surface`, `--accent`.
- **Do not add a row-specific `:focus-visible` variant.** D-11 declines to build one
  speculatively: this repository cannot render-test, so a speculative variant is unverifiable.
  The rows inherit Phase 19 D-09's global two-tone ring (`box-shadow: 0 0 0 2px var(--bg),
  0 0 0 4px var(--accent)`) because Phase 19's D-12 deliberately left `:focus-visible` global
  and unscoped. Plan 20-05's checkpoint arbitrates; if it reads badly, that becomes gap-closure
  work with rendered evidence behind it.
- `styles.css` is linked from `index.html` and never imported from TypeScript (16-D04).
- Zero package installs. Do not add jsdom, happy-dom, puppeteer or playwright.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 20 stylesheet block - link treatment, row hover, navigable-row scoping</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/styles.css` lines 320-390 - `.activity-list`, `.activity-row` and its
      children, `.badge`, and the start of the `.cta` block.
    - `src/dashboard/styles.css` lines 518-545 - the `.activity-table tbody tr` cursor and hover
      rules being re-scoped, and `.activity-table__row--highlight` which stays.
    - `src/dashboard/styles.css` lines 440-470 - the global `:focus-visible` rule the rows
      inherit unchanged.
    - `src/dashboard/styles.css` lines 1276-1300 - the Phase 19 banner-comment block, for the
      section-header format to follow.
    - `src/dashboard/row-navigation.ts` - to copy `NAVIGABLE_ROW_CLASS`'s literal value exactly.
    - `.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md` D-06, D-09, D-10, D-11.
  </read_first>
  <action>
Two edits to `src/dashboard/styles.css`.

**Edit A - de-scope the lying pointer/hover (D-10).** Delete both
`.activity-table tbody tr { cursor: pointer; }` and
`.activity-table tbody tr:hover { background: color-mix(...); }` (currently `styles.css:526-532`)
outright. Leave `.activity-table__row--highlight` immediately below them untouched.

**Edit B - append a new banner-commented `Phase 20 - Row-click interaction pattern` section at
the end of `styles.css`**, following the Phase 17/18/19 precedent (a `/* ===== */` banner with a
stated class contract). It contains exactly four rules:

1. **The D-06 bare link treatment:** a rule whose selector is exactly `a`, declaring
   `color: inherit` and `text-decoration: underline`.
   - `color: inherit` rather than `color: var(--accent)` is a deliberate, evidence-backed call,
     not a shortcut. `--accent` on `--bg` measures ~3.4:1 light and ~2.8:1 dark in this
     repository's own record (`17-UI-SPEC.md:105`), both under the 4.5:1 AA floor for normal
     text, so accent-coloured link text would ship a contrast failure everywhere at once.
     `inherit` also cannot fight `.activity-row__name`/`.activity-row__meta`'s own colours,
     which D-06 names as a constraint. The underline carries the affordance, so the link is not
     distinguished by colour alone.
   - Both existing `text-decoration: none` declarations (`.app-nav__link`, `.cta`) have class
     specificity and continue to win regardless of source order. `.records-jump__link` is a
     button and is unaffected.
   - Record in the block comment that this rule also normalizes two anchors that were rendering
     browser-default blue: `.app-nav__brand` (which has no CSS rule at all) and `.detail-nav`'s
     Newer/Older links. That is an intended in-scope side effect of a *shared* link treatment,
     and plan 20-05's checkpoint has a row confirming it did not make either look wrong.
2. **The whole-row link is not a text link:** a rule whose selector is exactly `.activity-row`,
   declaring `text-decoration: none`. Do not re-declare `display: flex` here - it already lives
   in the `.activity-row` rule at `styles.css:335`, and duplicating it would create two places
   to keep in sync.
3. **D-09 row-anchor hover:** a rule whose selector is exactly `.activity-row:hover`, declaring
   `background: color-mix(in srgb, var(--surface) 92%, var(--text));` - byte-identical to the
   formula deleted in Edit A, not a re-derived equivalent. The pointer cursor comes free from
   the anchor.
4. **D-10 navigable-row scoping:** two rules, `.activity-table__row--navigable` declaring
   `cursor: pointer`, and `.activity-table__row--navigable:hover` declaring the same
   byte-identical `color-mix` background. Each must be its own standalone rule with that exact
   selector as its entire head - `declarationsFor` anchors on `selector` + optional whitespace +
   `{`, so folding either into a combined selector list would make Task 2's assertions
   unresolvable.

Also add a one-line comment above the existing `.activity-row` rule at `styles.css:335` stating
that `display: flex` is load-bearing because `.activity-row` is an `<a>` as of Phase 20 and
anchors are `inline` by default. Do not restructure that rule.

The banner comment must state, in prose:
  - the class contract: `.activity-table__row--navigable` is applied by
    `src/dashboard/row-navigation.ts`'s `attachRowNavigation`, never hand-written in a view;
  - **why the scoping is not `.activity-table pr-table`:** six tables share that identical class
    string and four of them - Riegel race predictions (`records.ts`), two Trends tables, and the
    detail view's best-efforts table - are not activities and never will be clickable. Scoping
    on the marker means a future non-clickable table inherits nothing; an opt-out class on the
    innocent tables was rejected because opt-out is the wrong default (D-10);
  - **D-11, so it is not "fixed" later:** these rows deliberately have no row-specific
    focus-ring variant. They inherit Phase 19 D-09's global two-tone ring because D-12 left
    `:focus-visible` unscoped for exactly this. The `.activity-list` `gap: var(--space-sm)` (8px)
    leaves room for the 4px ring, and no clipping container is in play -
    `.activity-table-wrapper` declares no `overflow`, and the two `overflow-x: auto` containers
    (`.records-jump`, `.splits-scroll`) were already padded by Phase 19's D-10;
  - that `.cta` is untouched and still has seven consumers.

**Comment hygiene, non-negotiable:** this repository lost a full gap-closure round to GAP 1, a
stray `*/` inside comment prose that terminated a comment early and silently discarded a live
token from the parsed stylesheet, invisible to every automated check. `styles.test.ts` now
asserts that the comment-stripped view contains no surviving `*/`. Do not write `*/` inside any
comment body, and do not write comment prose containing a declaration-shaped fragment.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && rm -f /tmp/bw20.log && npm run build-widgets > /tmp/bw20.log 2>&1; BW=$?; cat /tmp/bw20.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw20.log && node --input-type=module -e "import {readFileSync} from 'node:fs'; const css=readFileSync('src/dashboard/styles.css','utf8'); const bare=css.replace(/\/\*[\s\S]*?\*\//g,''); if(bare.includes('*/')) throw new Error('stray */ in a comment - GAP 1 class defect'); for(const sel of ['.activity-table__row--navigable','.activity-table__row--navigable:hover','.activity-row:hover']){ if(!new RegExp(sel.replace(/[.*+?^\${}()|[\]\\]/g,'\\\\\$&')+'\\\\s*\\\\{').test(bare)) throw new Error('missing standalone rule: '+sel); } if(/\.activity-table tbody tr\s*\{/.test(bare)) throw new Error('D-10 violated: .activity-table tbody tr rule survives'); if(/\.activity-table tbody tr:hover\s*\{/.test(bare)) throw new Error('D-10 violated: .activity-table tbody tr:hover rule survives'); const mixes=(bare.match(/color-mix\(in srgb, var\(--surface\) 92%, var\(--text\)\)/g)||[]).length; if(mixes<3) throw new Error('expected the shared hover formula at least 3 times, found '+mixes); if(!/\.cta\s*\{/.test(bare)) throw new Error('.cta rule was deleted - it must survive'); if(/prefers-color-scheme/.test(css)) throw new Error('media-query theming introduced'); console.log('Phase 20 stylesheet block OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/styles.test.ts` exits 0 (existing assertions, including the
      `.cta:hover` one at line 723, still pass).
    - `npm run build-widgets` exits 0 with zero `css-syntax-error` occurrences in its log.
    - The comment-stripped stylesheet contains standalone rules whose heads are exactly `a`,
      `.activity-row`, `.activity-row:hover`, `.activity-table__row--navigable` and
      `.activity-table__row--navigable:hover`.
    - The rule `a` declares `color: inherit` and `text-decoration: underline`, and does not
      declare `color: var(--accent)`.
    - The comment-stripped stylesheet contains zero rules whose head is
      `.activity-table tbody tr` or `.activity-table tbody tr:hover`.
    - `.activity-table__row--highlight` is still present and unchanged.
    - `color-mix(in srgb, var(--surface) 92%, var(--text))` appears at least three times
      (the Phase 19 `button:hover` rule plus the two new hover rules) and every new occurrence
      is byte-identical to the Phase 19 original.
    - The `.cta` and `.cta:hover` rules are present and unchanged
      (`git diff src/dashboard/styles.css` shows no line inside the `.cta` block).
    - `styles.css` contains zero `prefers-color-scheme` occurrences, and the comment-stripped
      view contains zero surviving `*/`.
    - No new custom property is declared in `:root` or either `[data-theme]` block
      (`git diff` shows no `--` token addition).
  </acceptance_criteria>
  <done>Anchors read correctly in both themes, navigable rows advertise themselves and non-navigable ones stop lying, and the shared hover formula is used verbatim in every place that needs it.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 20 stylesheet assertions, mutation-proven</name>
  <files>src/dashboard/styles.test.ts</files>
  <read_first>
    - `src/dashboard/styles.test.ts` lines 1-60 - the file header, `cssNoComments` and
      `declarationsFor`, including the comment explaining why the non-greedy comment regex is
      deliberate.
    - `src/dashboard/styles.test.ts` lines 118-145 - `selectorListDeclares` and
      `splitTopLevelSelectors`.
    - `src/dashboard/styles.test.ts` lines 700-745 - the `.cta:hover` and
      `.activity-table tbody tr:hover` assertions, the shape to copy and the one to replace.
    - `src/dashboard/styles.test.ts` lines 1030-1064 - the anchored-fragment and stray-`*/`
      assertions, for the file's culture of explaining why a naive form was rejected.
    - `src/dashboard/styles.css` - the Phase 20 block written in Task 1.
  </read_first>
  <action>
Add one `describe('styles.css - Phase 20 row-click interaction pattern', ...)` block to
`src/dashboard/styles.test.ts`, placed after the existing Phase 19 blocks. It contains these
assertions and no others:

1. `selectorListDeclares('a', 'color: inherit')` is `true` (D-06).
2. `selectorListDeclares('a', 'text-decoration: underline')` is `true` (D-06).
3. `selectorListDeclares('a', 'color: var(--accent)')` is `false` - the accent-as-link-text
   contrast failure documented at `17-UI-SPEC.md:105` must not creep back in.
4. `declarationsFor('.activity-row')` contains `display: flex` - the declaration that keeps a
   now-inline `<a>` laid out as a row.
5. `selectorListDeclares('.activity-row', 'text-decoration: none')` is `true`.
6. `declarationsFor('.activity-row:hover')` contains
   `color-mix(in srgb, var(--surface) 92%, var(--text))` (D-09).
7. `declarationsFor('.activity-table__row--navigable')` contains `cursor: pointer` (D-10).
8. `declarationsFor('.activity-table__row--navigable:hover')` contains the same byte-identical
   `color-mix(...)` string (D-09/D-10).
9. `selectorListDeclares('.activity-table tbody tr', 'cursor: pointer')` is `false` - the
   negative half of D-10, proving four non-activity tables no longer inherit a pointer.
10. `selectorListDeclares('.activity-table tbody tr:hover', 'color-mix')` is `false`.
11. The marker-class literal in the stylesheet matches `NAVIGABLE_ROW_CLASS` from
    `src/dashboard/row-navigation.ts`: import the constant in this test file and assert the
    comment-stripped CSS contains `'.' + NAVIGABLE_ROW_CLASS`. This is what stops the class name
    drifting between the helper and the stylesheet - the single most likely silent break in the
    whole phase.

**Do not use `declarationsFor('a')` for assertions 1-3.** Its regex is
`new RegExp(escaped + '\\s*\\{')` and is unanchored, so the selector `a` matches the *first*
`a {`-shaped substring in the file - which is `.cta {` at `styles.css:368`, not the bare rule.
It would silently assert against the wrong rule and pass or fail for the wrong reason. Write
that reasoning as a comment above assertions 1-3 in the same explanatory style the rest of this
file uses; this repository has a documented history of assertions that passed identically
against a broken and a fixed stylesheet.

**Mutation-prove every new assertion before you commit.** For each of the eleven, apply one
targeted mutation to `styles.css` (delete the declaration, rename the selector, or change one
token), run `npx vitest run src/dashboard/styles.test.ts`, confirm *that specific* assertion
fails, then revert and confirm green. Record the eleven mutations and their failures in the plan
summary. An assertion that stays green under its own mutation is not a guard, and this is plan
19-15's established discipline in this repository.

Delete the now-obsolete `.activity-table tbody tr:hover` assertion at `styles.test.ts:729-733`
(its selector no longer exists) and note in the summary that it was superseded by assertions 8
and 10, not silently dropped. Do **not** touch the `.cta:hover` assertion at
`styles.test.ts:723-727`.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npx vitest run && node --input-type=module -e "import {readFileSync} from 'node:fs'; const s=readFileSync('src/dashboard/styles.test.ts','utf8'); if(!/Phase 20/.test(s)) throw new Error('Phase 20 describe block missing'); if(/declarationsFor\('a'\)/.test(s)) throw new Error(\"declarationsFor('a') used - it resolves to .cta {\"); if(!/NAVIGABLE_ROW_CLASS/.test(s)) throw new Error('marker class parity assertion missing'); if(!/color-mix\(in srgb, var\(--surface\) 92%, var\(--text\)\)/.test(s)) throw new Error('shared hover formula not asserted'); if(!/\.cta:hover/.test(s)) throw new Error('the .cta:hover assertion was deleted - it must survive'); console.log('styles.test.ts Phase 20 block OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/styles.test.ts` exits 0 and the new `describe` block reports
      exactly eleven assertions.
    - `npx vitest run` (full suite) exits 0.
    - `styles.test.ts` contains zero occurrences of `declarationsFor('a')`.
    - `styles.test.ts` imports `NAVIGABLE_ROW_CLASS` from `./row-navigation.js` and asserts the
      stylesheet contains `.` + that constant.
    - The `.cta:hover` assertion is still present and passing.
    - The old `.activity-table tbody tr:hover` assertion is removed and its removal is explained
      in the summary as superseded, not dropped.
    - The summary lists eleven mutations, one per new assertion, each with the assertion name
      that failed under it and confirmation the stylesheet was reverted and re-run green.
  </acceptance_criteria>
  <done>Every Phase 20 stylesheet claim is pinned by an assertion that has been shown to fail when the claim is false.</done>
</task>

<task type="auto">
  <name>Task 3: Source-structure guard over the view files</name>
  <files>src/dashboard/row-semantics.test.ts</files>
  <read_first>
    - `src/dashboard/styles.test.ts` lines 1-50 - the `readFileSync` + comment-stripping pattern
      this file follows for a different source type, and the non-greedy-regex rationale.
    - `src/dashboard/styles.test.ts` lines 342-370 - the `splitTopLevelSelectors - self-tests`
      describe block: the precedent for self-testing a parsing helper inside the test file that
      uses it.
    - `src/dashboard/views/list.ts`, `src/dashboard/views/overview.ts`,
      `src/dashboard/views/records.ts`, `src/dashboard/row-navigation.ts` - as left by plans
      20-01 through 20-03.
    - `.planning/phases/20-row-click-interaction-pattern/20-CONTEXT.md` D-01, D-02, D-05, D-10.
  </read_first>
  <action>
Create `src/dashboard/row-semantics.test.ts`: a text-assertion regression guard over the
TypeScript **source** of the view files, in the same spirit as `styles.test.ts` is a text guard
over the stylesheet source. Its header comment must state plainly what it is and what it is
not: it proves that the structural decisions of Phase 20 are still present in source; it proves
nothing about rendering, clicking, focus order or announcement, because vitest runs in the
`node` environment and this repository has no jsdom and no headless browser. The only proof of
those is plan 20-05's human browser checkpoint. Say that so nobody later reads a green run of
this file as coverage of row clicking.

**Comment stripping and its self-tests.** Write a small `stripComments(source: string): string`
that removes block comments with the same non-greedy `/\*[\s\S]*?\*\//g` pattern
`styles.test.ts` uses, then removes `//`-to-end-of-line comments **only where the `//` is not
immediately preceded by a `:`**, so a `https://` inside a string literal survives. Add a
`describe('stripComments - self-tests')` block proving: a trailing `// comment` is removed, a
full-line `//` comment is removed, a block comment is removed, and a string containing
`https://example.com` is preserved. Stripping is not cosmetic here - without it, prose in a
comment explaining that the "View Activity" CTA was removed would satisfy the very assertion
that is supposed to prove its absence.

Assertions, grouped into `describe` blocks by decision:

**UX-02 - the CTAs are gone, and `.cta` survives:**
- The comment-stripped source of `list.ts`, `overview.ts` and `records.ts` each contains zero
  occurrences of the quoted literal `'View Activity'`. Assert the quoted form, not the bare
  words, so prose cannot collide with it.
- Across `src/dashboard/views/`, `className = 'cta'` appears exactly 7 times: 4 in `detail.ts`,
  1 in `trends.ts`, 1 in `detail-map.ts`, 1 in `records.ts`, 0 in `list.ts`. Assert the
  per-file counts, not just the total - a total alone would stay green if a retry button were
  deleted and a CTA re-added elsewhere.

**UX-01 / D-03 - one definition of the URL and the click:**
- `list.ts`, `overview.ts` and `records.ts` each import from `../row-navigation.js`.
- `activityDetailHref(` appears exactly 2 times in `list.ts`, 1 in `overview.ts`, 2 in
  `records.ts`.
- The literal `#/activity/` appears zero times in those three files. (`detail.ts` still builds
  its own prev/next hrefs and is deliberately out of this phase's scope - do not assert on it.)

**D-10 - only activity rows are navigable:**
- `attachRowNavigation(` appears exactly 1 time in `list.ts`, exactly 2 times in `records.ts`,
  and exactly 0 times in `overview.ts`, `trends.ts` and `detail-sections.ts`. The three zeros
  are the load-bearing half: they are what keeps the Riegel, Trends and best-efforts tables from
  acquiring the marker class and the pointer cursor that Task 1 just took away from them.

**D-05 - the Records columns are gone:**
- Comment-stripped `records.ts` contains zero occurrences of `label: 'Activity'` and zero
  occurrences of the quoted literal `'Run'`.

**D-01 - no fake link semantics on a `<tr>`:**
- Comment-stripped `list.ts`, `records.ts` and `row-navigation.ts` each contain zero occurrences
  of `tabindex` and zero of `'role', 'link'` / `role="link"`.

**D-02 - Enter-only activation is deliberate:**
- Comment-stripped `row-navigation.ts` contains zero occurrences of `keydown`. Add an
  explanatory comment on this assertion naming D-02, stating that Space is deliberately not
  handled because it is the page-scroll key and a control announced as "link" takes Enter, and
  that this assertion exists specifically so a later agent adding a Space handler as a "fix"
  turns the suite red and has to read the reason first.

**Standing house rules (constraints 8 and 9):**
- No file in `src/dashboard/views/` contains a direct `location.hash` **assignment**. Match
  `location.hash` followed by optional whitespace and `=` **not** followed by another `=`, so
  `trends.ts:406`'s legitimate `!==` comparison does not trip it.
- `list.ts`, `overview.ts` and `records.ts` each contain zero occurrences of `innerHTML`,
  `outerHTML` and `insertAdjacentHTML` (T-16-VW-01 / T-17-VW-01).

Resolve file paths with `new URL('./views/list.ts', import.meta.url)` and friends, the same way
`styles.test.ts` resolves `./styles.css`.

**Mutation-prove the guard.** Pick at least five assertions spanning different decisions - one
CTA count, one `attachRowNavigation` zero-count, the `keydown` one, the `location.hash` one and
one column-removal one - apply a mutation to the relevant source file, confirm that assertion
fails, revert, and confirm green. Record each in the summary. In particular, verify the
`location.hash` assertion still passes with `trends.ts`'s `!==` comparison in place: if that
comparison trips it, your regex is wrong.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/row-semantics.test.ts && npx vitest run && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e "import {readFileSync} from 'node:fs'; const s=readFileSync('src/dashboard/row-semantics.test.ts','utf8'); for(const n of ['stripComments','View Activity','attachRowNavigation','keydown','location.hash','innerHTML','label: ','row-navigation.js']) { if(!s.includes(n)) throw new Error('guard is missing coverage for: '+n); } if(/\bdocument\b|jsdom|happy-dom|puppeteer|playwright/.test(s)) throw new Error('guard reaches for a DOM environment'); console.log('row-semantics guard OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/row-semantics.test.ts` exits 0.
    - `npx vitest run` (full suite) exits 0 and `npx tsc --noEmit -p tsconfig.json` exits 0.
    - The file defines `stripComments` and has a `describe('stripComments - self-tests')` block
      with at least four cases, including one proving `https://example.com` inside a string
      survives stripping.
    - Every assertion group listed in `<action>` is present, with the exact counts named there.
    - The file contains zero references to `document`, `jsdom`, `happy-dom`, `puppeteer` or
      `playwright`.
    - The file's header states in prose that it proves nothing about rendering or clicking and
      names plan 20-05's checkpoint as the only proof of those.
    - The summary records at least five mutations across different decisions, each with the
      assertion that failed and confirmation of revert-and-green, plus explicit confirmation
      that the `location.hash` assertion tolerates `trends.ts:406`'s `!==` comparison.
  </acceptance_criteria>
  <done>Every structural decision this phase made in TypeScript is pinned by an assertion proven to fail when the decision is undone, and the guard is honest in its own header about what it cannot see.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stylesheet source -> parsed stylesheet | A malformed comment can silently discard live declarations from the parsed CSS while the source text still contains them - the exact GAP 1 failure mode this repository shipped once. |
| helper source -> stylesheet | The marker class name is a string literal duplicated across a TypeScript module and a stylesheet; a rename in one place silently breaks the other. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-CSS-01 | Tampering | Phase 20 banner comment in `styles.css` | mitigate | No `*/` may appear inside comment prose, and no declaration-shaped fragment may appear in it. The existing `comment stripping leaves no stray */` assertion (`styles.test.ts`) plus Task 1's `build-widgets` `css-syntax-error` check are the two independent gates; both were the ones GAP 1 evaded and both have since been hardened. |
| T-20-CSS-02 | Spoofing | `.activity-table__row--navigable` literal duplicated in TS and CSS | mitigate | Task 2 assertion 11 imports `NAVIGABLE_ROW_CLASS` from the helper and asserts the stylesheet contains it, so a rename on either side fails the suite rather than silently un-styling every navigable row. |
| T-20-A11Y-03 | Information Disclosure (illegibility) | the D-06 bare `a` colour | mitigate | `color: inherit` is chosen over `color: var(--accent)` on this repository's own measured contrast (~3.4:1 light, ~2.8:1 dark, `17-UI-SPEC.md:105`), and assertion 3 makes the accent variant fail the suite if reintroduced. Whether it actually reads correctly in a browser is checkpoint row 9 of plan 20-05. |
| T-20-FALSEGREEN-04 | Repudiation | assertions that pass against both a broken and a fixed source | mitigate | Every new assertion in Tasks 2 and 3 must be mutation-proven and the mutations recorded. `declarationsFor('a')` is explicitly banned because it resolves to `.cta {`. This is the concrete lesson of Phase 19's GAP 1 and plan 19-15. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized. Do not add jsdom, happy-dom, puppeteer or playwright to make the DOM assertions this repository deliberately cannot make - Task 3's own check fails if the guard names any of them. |
</threat_model>

<verification>
- `npx vitest run` exits 0 with no regression.
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- `npm run build-widgets` exits 0 with zero `css-syntax-error` occurrences (redirect the log to
  a file, test the build's own exit status, then grep the file; do not let a grep in a pipeline
  determine the exit status, and do not use `PIPESTATUS` - the shell is zsh, where it is
  `pipestatus` and 1-indexed).
- `npm run verify-dashboard` exits 0.
- `git status --porcelain src` lists only `src/dashboard/styles.css`,
  `src/dashboard/styles.test.ts` and the new `src/dashboard/row-semantics.test.ts`.
</verification>

<success_criteria>
Anchors across the app render in theme colours instead of browser-default blue; hover and
pointer are on exactly the rows that navigate and nowhere else; the row focus ring is inherited
unchanged so the checkpoint can judge it on rendered evidence; and every structural and stylistic
claim this phase makes has a guard that has been shown to fail when the claim is false.
</success_criteria>

<output>
Create `.planning/phases/20-row-click-interaction-pattern/20-04-SUMMARY.md` when done.
</output>
