---
phase: 18-records-trends-differentiators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - data/private/athlete-private.example.json
  - data/private/README.md
  - src/analytics/athlete-private.ts
  - src/analytics/athlete-private.test.ts
  - scripts/build-widgets.mjs
  - scripts/verify-dashboard-publish.mjs
autonomous: true
requirements: [REC-06, TREND-04]
must_haves:
  decisions: [D-09, D-12, D-14, D-22]
  truths:
    - "The developer's birthDate, sex and restingHr can be supplied locally without ever entering git history or the published site"
    - "A build that would publish those fields fails loudly instead of shipping"
    - "data/best-effort-exclusions.json is reachable in the published site so exclusion reasons can be shown"
  artifacts:
    - path: "src/analytics/athlete-private.ts"
      provides: "Build-time-only parse/load of the private athlete config"
      exports: ["ATHLETE_PRIVATE_SCHEMA_VERSION", "parseAthletePrivateConfig", "loadAthletePrivateConfig"]
    - path: "data/private/athlete-private.example.json"
      provides: "Committed template with placeholder-only values"
    - path: "scripts/build-widgets.mjs"
      provides: "copyDataFiles with data/wma + explicit single-file copies + assertNoPrivateArtifacts guard"
      contains: "assertNoPrivateArtifacts"
  key_links:
    - from: "scripts/build-widgets.mjs"
      to: "dist/widgets/data"
      via: "assertNoPrivateArtifacts scan"
      pattern: "birthDate"
    - from: "scripts/verify-dashboard-publish.mjs"
      to: "/data/private/athlete-private.json"
      via: "expect404"
      pattern: "expect404\\(baseUrl, '/data/private"
---

<objective>
Resolve RESEARCH.md Pitfall 1 (HIGH severity) before any code reads identity fields: create a private, gitignored, never-published home for `birthDate` / `sex` / `restingHr`, and make both the build script and the publish verifier prove it stays out of the public artifact. Also extend `copyDataFiles` for the two new published data locations this phase needs.

Purpose: this repo is a **PUBLIC** GitHub repo (`github.com/bacilo/strava-widgets`, confirmed `visibility: PUBLIC`) publishing to GitHub Pages. Adding `birthDate` to the already-committed `data/config/athlete.json` would leak it twice over — once in git history, once in `dist/widgets/data/config/athlete.json`, both permanent. Every downstream plan in this phase depends on this file layout existing first.
Output: a private-config contract, a build-time leak guard, and a publish-contract 404 assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-records-trends-differentiators/18-CONTEXT.md
@.planning/phases/18-records-trends-differentiators/18-RESEARCH.md
@.planning/phases/18-records-trends-differentiators/18-PATTERNS.md
</context>

<locked_deviation>
**This plan deliberately deviates from CONTEXT.md D-12's literal wording, and from 18-UI-SPEC.md's Copywriting Contract file path. Do not "correct" it back.**

D-12 says: "One extended `data/config/athlete.json` — add `birthDate`, `sex`, and `restingHr`."
RESEARCH.md Pitfall 1 (HIGH, flagged non-discretionary by the phase's security gate) proves that is unshippable here: `copyDataFiles` wholesale-copies every `*.json` under `data/config/`, and the repo itself is public.

**Resolution, locked for the whole phase:**
- `data/config/athlete.json` keeps exactly its current public shape (`schemaVersion`, `note`, `maxHr`, `hrZones`). It gains **no** new fields.
- `birthDate`, `sex`, `restingHr` live in `data/private/athlete-private.json`, which is gitignored and never copied into `dist/widgets/`.
- D-12's actual intent — one hand-maintained file, per-consumer validation, missing fields disable only their own feature — is preserved verbatim, just at a different path.
- **Every user-facing config notice in this phase names `data/private/athlete-private.json`, not `data/config/athlete.json`.** The two strings in 18-UI-SPEC.md's Copywriting Contract become:
  - Age-grading: `Age-grading is off — add birthDate and sex to data/private/athlete-private.json to enable it.`
  - Training load: `Training load is off — add birthDate, sex, and restingHr to data/private/athlete-private.json to enable it.`
  Everything else about those notices (actionable, names file + field, `.config-notice` class) is unchanged from the UI-SPEC.
</locked_deviation>

<tasks>

<task type="auto">
  <name>Task 1: Create the private athlete config contract and its build-time loader</name>
  <files>data/private/README.md, data/private/athlete-private.example.json, .gitignore, src/analytics/athlete-private.ts, src/analytics/athlete-private.test.ts</files>
  <read_first>
    - `.gitignore` — see the existing `data/tokens.json`, `data/stats/`, `data/dashboard/`, `export_data/` entries and their comment style
    - `src/dashboard/views/detail-zones.ts` lines 101-200 — `AthleteConfig`, `AthleteHrZone`, `hasOwn`, and `parseAthleteConfig`: the exact total/never-throws/all-or-nothing validation idiom to mirror
    - `src/analytics/best-effort-exclusions.ts` — the committed-config parse + `schemaVersion` convention
    - `src/storage/file-store.ts` (or wherever `FileStore` is defined — `grep -rn "class FileStore" src`) for `readJson` semantics
    - `data/config/athlete.json` — the sibling public config whose `note` field style the new file copies
  </read_first>
  <action>
Add to `.gitignore`, under a comment explaining that this directory holds hand-maintained identity/health inputs that must never enter a public repo:
`data/private/*.json` on one line, then `!data/private/athlete-private.example.json` on the next so the template stays committed.

Create `data/private/README.md` stating: this directory is read only by build-time compute steps; it is never copied into `dist/widgets/`; copy `athlete-private.example.json` to `athlete-private.json` and fill in real values; the phase and CI pass with the file absent.

Create `data/private/athlete-private.example.json` with `schemaVersion: 1`, a `note` field in the same voice as `data/config/athlete.json`'s, and **placeholder-only** values: `"birthDate": "YYYY-MM-DD"`, `"sex": "male"`, `"restingHr": 0`. It must not contain a real date.

Create `src/analytics/athlete-private.ts` — a **Node/build-time-only** module. It must not import anything from `src/dashboard/`, and no module under `src/dashboard/` may ever import it. Exports:
- `export const ATHLETE_PRIVATE_SCHEMA_VERSION = 1;`
- `export type AthleteSex = 'male' | 'female';`
- `export interface AthletePrivateConfig { schemaVersion: number; birthDate: string; sex: AthleteSex; restingHr: number | null; }`
- `export function parseAthletePrivateConfig(raw: unknown): AthletePrivateConfig | null` — total, never throws, own-property reads only via a local `hasOwn` helper (prototype-pollution discipline, same as `parseAthleteConfig`/`parseGearDocument`). Rejects (returns `null`) unless: `raw` is a non-array object; `schemaVersion` is a finite number; `birthDate` matches `/^\d{4}-\d{2}-\d{2}$/` **and** parses to a real UTC date **and** is not the literal placeholder `"YYYY-MM-DD"`; `sex` is exactly `'male'` or `'female'`. `restingHr` is optional and tolerant: a finite number in `20..120` becomes the value, anything else (including absent, `0`, or the placeholder) becomes `null` — because Edwards TRIMP and age-grading do not need it and must not be blocked by it (D-14).
- `export async function loadAthletePrivateConfig(fileStore: { readJson<T>(p: string): Promise<T> }, filePath = 'data/private/athlete-private.json'): Promise<AthletePrivateConfig | null>` — reads and parses; on **any** failure (ENOENT, malformed JSON, failed validation) logs one `console.warn` naming the path and the consequence, then returns `null`. Never throws. This is the warn-and-degrade pattern from `compute-dashboard-index.ts` lines 90-114.

Create `src/analytics/athlete-private.test.ts` covering: a fully valid config; the shipped example template rejected (placeholder `birthDate`); missing `sex`; `sex: 'M'` rejected; `restingHr` absent → `null`; `restingHr: 0` → `null`; `restingHr: 48` → `48`; `__proto__`-keyed object not reachable; a prose `birthDate` like `"1985"` rejected; `loadAthletePrivateConfig` returning `null` (not throwing) when `readJson` rejects with ENOENT.
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/analytics/athlete-private.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analytics/athlete-private.test.ts` exits 0 with at least 9 passing cases.
    - `git check-ignore -v data/private/athlete-private.json` prints a matching `.gitignore` rule; `git check-ignore data/private/athlete-private.example.json` exits non-zero (the example is NOT ignored).
    - `grep -c 'from .*dashboard' src/analytics/athlete-private.ts` returns `0`.
    - `grep -rn "athlete-private" src/dashboard | wc -l` returns `0`.
    - `node -e "const {parseAthletePrivateConfig}=require('./dist/analytics/athlete-private.js');const t=require('./data/private/athlete-private.example.json');if(parseAthletePrivateConfig(t)!==null)process.exit(1)"` exits 0 (the committed template is rejected, so an unfilled template can never enable a feature with fake values).
    - `grep -vE '^\s*(//|\*|/\*)' data/private/athlete-private.example.json | grep -cE '19[0-9]{2}-|20[0-9]{2}-'` returns `0` (no real-looking date committed).
  </acceptance_criteria>
  <done>The private config has a committed template, a gitignored real path, and a tested build-time-only parser that degrades to `null` rather than throwing or fabricating.</done>
</task>

<task type="auto">
  <name>Task 2: Extend copyDataFiles and add a build-time private-artifact guard</name>
  <files>scripts/build-widgets.mjs</files>
  <read_first>
    - `scripts/build-widgets.mjs` — the whole `copyDataFiles` function (starts at the `const dataDirs = [` array around line 134) plus the `main()`/entry sequence at the bottom of the file that calls it
    - `.planning/phases/18-records-trends-differentiators/18-PATTERNS.md` § `scripts/build-widgets.mjs` — the confirmed wholesale-copy finding
  </read_first>
  <action>
Three changes to `scripts/build-widgets.mjs`, all inside or immediately around `copyDataFiles`:

1. Add one entry to the existing `dataDirs` array: `{ src: 'data/wma', dest: 'dist/widgets/data/wma' }`, with a comment naming it as the committed WMA age-grading factor tables (D-09/D-22). The existing `if (!existsSync(src)) continue;` guard means this is safe to add before `data/wma/` exists.

2. Add an explicit **single-file** copy list alongside the directory list, because `copyDataFiles` only walks subdirectories and `data/best-effort-exclusions.json` sits at `data/` root: a `const dataFiles = [{ src: 'data/best-effort-exclusions.json', dest: 'dist/widgets/data/best-effort-exclusions.json' }];` loop that `mkdirSync`s the parent, skips when `!existsSync(src)`, and copies. Do **not** add a `{ src: 'data', dest: 'dist/widgets/data' }` directory entry — that would publish `data/provenance.json` (282 KB of local export-path metadata) and `data/sync-state.json` for no reason.

3. Add `function assertNoPrivateArtifacts()` and call it at the very end of `copyDataFiles()`. It must `process.exit(1)` with a message naming the offending path when any of these hold:
   - `dist/widgets/data/private` exists;
   - any `.json` file anywhere under `dist/widgets/data/` contains the substring `"birthDate"`, `"restingHr"`, or `"\"sex\""`.
   Walk `dist/widgets/data/` recursively with `readdirSync(dir, { withFileTypes: true })`. On success log one `✓` line stating how many published JSON files were scanned. Add a comment above the `dataDirs` array stating that `data/private/` must never be added to either list and that this function is the enforcement.
  </action>
  <verify>
    <automated>npm run build-widgets</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build-widgets` exits 0 and its output contains a line naming the private-artifact scan and a non-zero count of scanned JSON files.
    - `test -f dist/widgets/data/best-effort-exclusions.json` exits 0.
    - `test -e dist/widgets/data/private` exits non-zero.
    - `grep -rl 'birthDate\|restingHr' dist/widgets/ | wc -l` returns `0`.
    - Negative control (run manually, then revert): `mkdir -p dist/widgets/data/private && echo '{"birthDate":"1985-04-12"}' > dist/widgets/data/private/x.json && npm run build-widgets` exits **non-zero** and names the offending path. Record the observed exit code and message in the plan summary. Clean up with `rm -rf dist/widgets/data/private` afterwards.
    - `grep -c "src: 'data'," scripts/build-widgets.mjs` returns `0` (no whole-`data/` directory entry was introduced).
  </acceptance_criteria>
  <done>`copyDataFiles` carries `data/wma/` and `data/best-effort-exclusions.json`, and the build hard-fails if any identity field reaches the publish directory.</done>
</task>

<task type="auto">
  <name>Task 3: Add the first negative-reachability assertion to the publish verifier</name>
  <files>scripts/verify-dashboard-publish.mjs</files>
  <read_first>
    - `scripts/verify-dashboard-publish.mjs` — `expect200` (~line 168), `expect404` (~line 182), the `main()` assertion sequence (~lines 190-260) including the existing `/data/config/athlete.json` and `/data/config/gear.json` checks
    - `.planning/STATE.md` § Blockers/Concerns — the Phase 16 postmortem: the gate reported green on a broken build because it asserted a local shape production does not have
  </read_first>
  <action>
`expect404` currently hardcodes the phrase `(stream-unavailable activity)` in both its failure and success messages. Generalize it to take a second argument `reason` (a short string) used in both messages, and update its one existing call site to pass `'stream-unavailable activity'` so the current output is unchanged.

Then, in `main()`, immediately after the existing `/data/config/athlete.json` block, add:
- `await expect404(baseUrl, '/data/private/athlete-private.json', 'private athlete config must never be published');`
- `await expect404(baseUrl, '/data/private/', 'private config directory must never be published');`
- A positive check on the newly-published exclusions file: `expect200(baseUrl, '/data/best-effort-exclusions.json')`, parsing the body and failing unless `Array.isArray(parsed.exclusions)`.
- An assertion on the existing public athlete config that it does **not** regress: parse `/data/config/athlete.json`'s body and `fail(...)` if the parsed object has an own `birthDate`, `sex`, or `restingHr` property. This is the check that would have caught a well-meaning future edit re-merging the private fields back into the public file.

Do not add assertions for `data/stats/training-load.json`, `age-grading.json`, `gear-aggregate.json`, or `data/wma/*.json` here — those files do not exist yet; plan 18-11 adds their 200-assertions after the compute steps are wired.
  </action>
  <verify>
    <automated>npm run build-widgets && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify-dashboard` exits 0.
    - Its output contains a line for `GET /data/private/athlete-private.json -> 404` and one for `GET /data/best-effort-exclusions.json -> 200`.
    - Its output still contains the pre-existing stream-unavailable 404 line with the words `stream-unavailable`, proving the `expect404` generalization did not change existing behavior.
    - Negative control (run manually, then revert): temporarily add `"birthDate": "1985-04-12"` to `data/config/athlete.json`, run `npm run build-widgets && npm run verify-dashboard`, and confirm **both** commands fail (build-widgets via `assertNoPrivateArtifacts`, and — after temporarily disabling that guard — verify-dashboard via the new own-property check). Restore `data/config/athlete.json` to its committed state and re-run to green. Record both observed failures in the plan summary.
  </acceptance_criteria>
  <done>The publish contract now asserts an absence, not just presences, and a regression that re-merges identity fields into the public config fails two independent gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → git history | `data/` is committed to a PUBLIC repo; anything not gitignored is permanently public |
| local filesystem → `dist/widgets/` → GitHub Pages | `copyDataFiles` is the only gate deciding what becomes an HTTP-reachable public artifact |
| published JSON → any browser | Everything under `dist/widgets/data/` is anonymously fetchable and archived by third parties |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-PII-01 | Information Disclosure | `data/config/athlete.json` + `copyDataFiles` | mitigate | Identity/health fields live only in gitignored `data/private/athlete-private.json`; `assertNoPrivateArtifacts()` fails the build on any `birthDate`/`restingHr`/`sex` string under `dist/widgets/data/`; `verify-dashboard-publish.mjs` asserts a 404 for the private path and asserts the public config has no such own properties |
| T-18-PII-02 | Information Disclosure | git history | mitigate | `.gitignore` rule `data/private/*.json` with an explicit negation for the placeholder-only example; the committed example is rejected by `parseAthletePrivateConfig`, so it can never masquerade as real config |
| T-18-PII-03 | Information Disclosure | `data/provenance.json`, `data/sync-state.json` | mitigate | Task 2 uses an explicit single-file allow-list for `data/` root rather than a directory wildcard, so these stay unpublished |
| T-18-PROTO-01 | Tampering | `parseAthletePrivateConfig` | mitigate | Own-property reads only via a local `hasOwn` helper, mirroring `parseAthleteConfig`/`parseGearDocument` (T-17-CFG-01 precedent) |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | **This phase installs zero new packages.** RESEARCH.md's only candidate (`chartjs-chart-matrix`) was explicitly rejected in favour of a plain CSS grid. Any executor that finds itself needing an install must stop and run the Package Legitimacy Gate before proceeding — no install task is authorized by this plan |
</threat_model>

<verification>
- `npm test` green (full suite, no regressions).
- `npm run build && npm run build-widgets && npm run verify-dashboard` all exit 0.
- `grep -rl 'birthDate\|restingHr' dist/widgets/` returns nothing.
- Both negative controls (Task 2 and Task 3) observed failing, then reverted to green.
</verification>

<success_criteria>
The developer can create `data/private/athlete-private.json` with real values and be structurally guaranteed — by two independent automated gates plus a `.gitignore` rule — that those values never reach git or the published site.
</success_criteria>

<output>
Create `.planning/phases/18-records-trends-differentiators/18-01-SUMMARY.md` when done. Record the two negative-control observations verbatim (command, exit code, message) — they are the evidence this plan's guards are load-bearing rather than decorative.
</output>
