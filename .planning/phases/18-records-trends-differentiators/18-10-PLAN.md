---
phase: 18-records-trends-differentiators
plan: 10
type: execute
wave: 2
depends_on: ["18-03", "18-05"]
files_modified:
  - src/dashboard/views/trends-cadence-hr-logic.ts
  - src/dashboard/views/trends-cadence-hr-logic.test.ts
  - src/dashboard/views/trends-training-load-logic.ts
  - src/dashboard/views/trends-training-load-logic.test.ts
  - src/dashboard/views/trends-gear-logic.ts
  - src/dashboard/views/trends-gear-logic.test.ts
autonomous: true
requirements: [TREND-03, TREND-04, TREND-05]
must_haves:
  truths:
    - "Monthly cadence and heart-rate means are derivable from the already-fetched index, with a month that has no data appearing as a gap rather than a zero"
    - "The training-load chart can be scoped to a window and switched between models without refetching"
    - "Spans where runs happened but carried no heart rate are identifiable so they can be shaded as absence, not as zero training"
    - "The gear chart shows at most eight named shoes plus a merged Other bar, while the table keeps every shoe"
  artifacts:
    - path: "src/dashboard/views/trends-cadence-hr-logic.ts"
      provides: "Monthly cadence/HR means with explicit nulls for empty months"
      exports: ["buildMonthlyChannelSeries", "MONTHLY_CHANNELS"]
    - path: "src/dashboard/views/trends-training-load-logic.ts"
      provides: "Training-load parse, window scoping, model selection, thin-HR-coverage spans"
      exports: ["parseTrainingLoad", "TRAINING_LOAD_WINDOWS", "parseLoadWindow", "sliceLoadWindow", "selectModelSeries", "findThinCoverageSpans"]
    - path: "src/dashboard/views/trends-gear-logic.ts"
      provides: "Gear aggregate parse, sorting, top-8-plus-Other chart bucketing"
      exports: ["parseGearAggregate", "sortShoes", "buildGearChartBuckets", "GEAR_CHART_MAX_CATEGORIES"]
  key_links:
    - from: "src/dashboard/views/trends-training-load-logic.ts"
      to: "data/stats/training-load.json days[].runs / runsWithHr"
      via: "thin-coverage span detection"
      pattern: "runsWithHr"
---

<objective>
Build the pure logic behind the Trends page's remaining three tabs: monthly cadence/HR means, training-load windowing and thin-HR-coverage detection, and the gear table/chart bucketing (D-14, D-15, D-16, D-18, D-19, D-21, TREND-03/04/05).

Purpose: two of this phase's three hardest honesty requirements land here. A month with no HR-stream runs must be a **gap** in the line, never a zero (§ 10); and a span of days where runs happened but none carried HR must be **identifiable as absence**, so the chart shades it rather than letting a CTL dip read as "no training" (D-15, the user's verbatim constraint).
Output: three tested pure modules, no rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-records-trends-differentiators/18-CONTEXT.md
@.planning/phases/18-records-trends-differentiators/18-UI-SPEC.md
@.planning/phases/18-records-trends-differentiators/18-PATTERNS.md
@.planning/phases/18-records-trends-differentiators/18-03-SUMMARY.md
@.planning/phases/18-records-trends-differentiators/18-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monthly cadence and heart-rate means with real gaps</name>
  <files>src/dashboard/views/trends-cadence-hr-logic.ts, src/dashboard/views/trends-cadence-hr-logic.test.ts</files>
  <read_first>
    - `src/dashboard/views/calendar-logic.ts` lines 71-92 (`activityDayKey`, the Z-suffix rule) and 149-201 (`buildMonthGrid`'s group-by-key reduction)
    - `src/analytics/dashboard-index.types.ts` — `DashboardIndexRow`, especially `avgHr: number | null` and `avgCadenceRpm: number | null` with its comment that cadence is **raw single-leg rpm, deliberately not doubled** (Phase 14 keeps unit conversion in exactly one place)
    - `18-UI-SPEC.md` § 10 — two stacked single-axis charts sharing one pinned y-gutter, and the explicit `spanGaps: false` requirement: a month with no qualifying activities is a gap, never a zero and never an interpolated bridge
    - `src/dashboard/views/list-logic.ts` — the array-reduction-over-index-rows shape
  </read_first>
  <action>
Create `src/dashboard/views/trends-cadence-hr-logic.ts` — pure, DOM-free, no `new Date()`.

Exports:
- `export type MonthlyChannel = 'cadence' | 'hr';`
- `export const MONTHLY_CHANNELS: readonly MonthlyChannel[] = ['cadence', 'hr'];`
- `export interface MonthlyPoint { x: number; monthKey: string; value: number | null; runs: number; contributing: number; }` — `x` is epoch-ms of the month start (a `'linear'` scale value; **never** a `TimeScale`). `value` is `null` for a month with no contributing activity — Chart.js renders a `null` as a gap when `spanGaps: false`, which is exactly the required behaviour. `runs` counts every activity that month; `contributing` counts those with a non-null channel value, so the view can explain a gap.
- `export function buildMonthlyChannelSeries(rows: readonly DashboardIndexRow[], channel: MonthlyChannel): MonthlyPoint[]` — groups by `YYYY-MM` derived from `activityDayKey(row.startDateLocal).slice(0, 7)` (import `activityDayKey`; do not write a second date parser — 18-PATTERNS.md § Shared Patterns makes this a hard rule). The mean is **weighted by `movingTimeSec`**, not a plain row mean, so a 10-minute shakeout does not carry the same weight as a two-hour long run. Rows with a `null` channel value are excluded from the mean but still counted in `runs`.
  **Emit a continuous month spine** from the first to the last month that has any activity, inserting `value: null, runs: 0, contributing: 0` entries for months with no activity at all. A missing month must be a visible gap, not a silently shortened x-axis — the same principle as the training-load daily spine (Pitfall 3).
- `export function channelLabel(channel: MonthlyChannel): string` — `'Average cadence (rpm, single-leg)'` / `'Average heart rate (bpm)'`. The cadence label must state the single-leg unit, matching the detail view's existing `'Cadence (rpm, single-leg)'` stat-card label, because the index value is deliberately not doubled.

Create `src/dashboard/views/trends-cadence-hr-logic.test.ts` covering:
1. Three rows in one month with differing `movingTimeSec` produce a **time-weighted** mean, asserted against a hand-computed value that differs from the plain row mean (a plain-mean implementation fails).
2. A month whose every row has `avgHr === null` yields `value: null` with `runs > 0` and `contributing === 0` — asserted explicitly as `null`, not `0`.
3. A month with no activity at all, sitting between two months that have activity, appears in the output with `value: null` and `runs: 0` — the spine is continuous.
4. The series is ascending by `x` regardless of input row order.
5. Both `Z`-suffixed and non-`Z` `startDateLocal` shapes bucket into the correct month (the archive carries both).
6. A row with an unparseable `startDateLocal` is skipped without throwing and does not create a bogus month.
7. Against the **live** `data/dashboard/index.json`: the cadence series and the HR series both span the same months, at least one month has `value: null` for cadence, and no `value` is `NaN` or `Infinity`.
8. Empty input returns `[]` without throwing.
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/dashboard/views/trends-cadence-hr-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/views/trends-cadence-hr-logic.test.ts` exits 0 with at least 8 passing cases covering every item above.
    - `grep -c "activityDayKey" src/dashboard/views/trends-cadence-hr-logic.ts` returns at least `1`.
    - `grep -cE "document\.|window\.|new Date\(\)|TimeScale" src/dashboard/views/trends-cadence-hr-logic.ts` returns `0`.
    - `grep -c "single-leg" src/dashboard/views/trends-cadence-hr-logic.ts` returns at least `1`.
    - `node -e "const l=require('./dist/dashboard/views/trends-cadence-hr-logic.js');const idx=require('./data/dashboard/index.json');const c=l.buildMonthlyChannelSeries(idx.activities,'cadence');const h=l.buildMonthlyChannelSeries(idx.activities,'hr');if(c.length!==h.length)process.exit(1);if(!c.some(p=>p.value===null))process.exit(1);if(c.some(p=>p.value!==null&&!Number.isFinite(p.value)))process.exit(1);console.log('months:',c.length,'cadence gaps:',c.filter(p=>p.value===null).length,'hr gaps:',h.filter(p=>p.value===null).length)"` exits 0.
  </acceptance_criteria>
  <done>Monthly cadence and HR means are time-weighted, span a continuous month spine, and represent an empty month as an explicit `null` that renders as a line gap.</done>
</task>

<task type="auto">
  <name>Task 2: Training-load windowing, model selection, and thin-HR-coverage spans</name>
  <files>src/dashboard/views/trends-training-load-logic.ts, src/dashboard/views/trends-training-load-logic.test.ts</files>
  <read_first>
    - `src/analytics/training-load.types.ts` (plan 18-03) — `TrainingLoadDocument`, `DailyLoadEntry` (`date`, `runs`, `runsWithHr`, `edwards`, `banister | null`), and the header note that both series live in one document so the model toggle never refetches
    - `.planning/phases/18-records-trends-differentiators/18-07-SUMMARY.md` — the live spine length, date range, and observed count of days with runs but zero HR runs
    - `src/dashboard/views/list-logic.ts` lines 106-140 — the allow-list-or-default parse idiom
    - `18-UI-SPEC.md` § 11 — three series (CTL filled area, ATL line, TSB line), the Edwards/Banister `.segmented` toggle defaulting to Edwards, the default 12-month window with a 3mo/12mo/All control, the flat low-opacity shaded rectangle over no-HR spans, and the caption that says the same thing in words
  </read_first>
  <action>
Create `src/dashboard/views/trends-training-load-logic.ts` — pure, DOM-free, `now` injected.

Exports:
- `export function parseTrainingLoad(raw: unknown): TrainingLoadDocument | null` — total, never throws, own-property reads only. Returns `null` unless `days` is an array whose entries carry a `date` string and an `edwards` object with four finite numbers. Malformed individual days are dropped rather than rejecting the document (tolerant entry-level parse, the `parseGearDocument` discipline).
- `export type TrimpModel = 'edwards' | 'banister';` and `export function parseTrimpModel(raw: string | null, doc: TrainingLoadDocument | null): TrimpModel` — allow-list with an `'edwards'` default, **and** a hard fallback to `'edwards'` when the document reports `models.banister === false`. State in a comment that this is deliberately a parse-time clamp rather than a render-time check, so a stored or URL-supplied `banister` value can never produce an empty chart. Per D-13/§ 11 the Banister option stays **visible but disabled** in the UI — the model exists, it just is not configured.
- `export type LoadWindow = '3mo' | '12mo' | 'all';` `export const TRAINING_LOAD_WINDOWS: readonly LoadWindow[] = ['3mo', '12mo', 'all'];` `export const DEFAULT_LOAD_WINDOW: LoadWindow = '12mo';` `export function parseLoadWindow(raw: string | null): LoadWindow` — allow-list, default `'12mo'`.
- `export function sliceLoadWindow(days: readonly DailyLoadEntry[], window: LoadWindow, now: Date): DailyLoadEntry[]` — returns the trailing 90 days, 365 days, or everything. The **underlying series always covers the full archive** (D-16); only the displayed window is scoped, so this function must never mutate or discard from the source array.
- `export interface LoadPoint { x: number; ctl: number; atl: number; tsb: number; trimp: number; }` and `export function selectModelSeries(days: readonly DailyLoadEntry[], model: TrimpModel): LoadPoint[]` — maps to the chosen model's four values; `x` is epoch-ms of the day (`'linear'` scale, never `TimeScale`). A day whose `banister` is `null` while `model === 'banister'` contributes `null`-free zeros? **No** — it must be excluded from the emitted array entirely rather than emitting zeros, so an unconfigured model produces an empty chart the view can detect, not a flat line at zero that reads as real data.
- `export interface CoverageSpan { startX: number; endX: number; startDate: string; endDate: string; days: number; runsWithoutHr: number; }`
- `export function findThinCoverageSpans(days: readonly DailyLoadEntry[]): CoverageSpan[]` — **D-15's core function.** A thin-coverage span is a maximal run of consecutive days in which every day satisfies `runsWithHr === 0`, **and** the span contains at least one day with `runs > 0`. A pure rest gap (no runs at all) is **not** a coverage span — it is genuine rest and shading it would claim missing data that is not missing. Emit `runsWithoutHr` as the total runs in the span so the view can caption it with real numbers. Document both halves of the rule in a JSDoc block, naming D-15 and the user's verbatim constraint ("I don't want to delete activities but I want to make sure those absences are visible and not 'made up'").
- `export function coverageCaption(spans: readonly CoverageSpan[]): string` — returns the exact § 11 copy `Shaded regions indicate no HR data (not zero training).` when `spans.length > 0`, and `''` when there are none, so the caption and the shading appear and disappear together.

Create `src/dashboard/views/trends-training-load-logic.test.ts` covering:
1. `parseTrainingLoad` against the **live** `data/stats/training-load.json`: non-null, `days.length` matching the summary's recorded spine length. `null`/`{}`/`[]`/`__proto__`-keyed input returns `null` or an empty result without throwing.
2. `parseTrimpModel('banister', docWithBanisterDisabled)` returns `'edwards'`; with Banister enabled it returns `'banister'`; unknown values return `'edwards'`.
3. `parseLoadWindow` allow-list including `null`, `''`, and an unknown string.
4. `sliceLoadWindow` with a fixed `now`: `'3mo'` yields ~90 entries, `'12mo'` ~365, `'all'` the full array; the source array is unmutated (assert its length after the call).
5. `selectModelSeries` with `model: 'banister'` over days whose `banister` is `null` returns `[]` — **not** an array of zeros.
6. **Coverage span — runs but no HR:** five consecutive days with `runs > 0` and `runsWithHr === 0` produce exactly one span of five days with the correct `runsWithoutHr` total.
7. **Coverage span — pure rest is not a span:** five consecutive days with `runs === 0` produce **zero** spans. This is the discriminating case; an implementation that only checks `runsWithHr === 0` shades every rest week in the archive.
8. **Mixed:** a rest gap adjacent to a no-HR run block yields one span whose boundaries include only what the rule allows; two no-HR blocks separated by a day with HR yield two distinct spans.
9. A single isolated no-HR run day yields a one-day span.
10. Against the live document: `findThinCoverageSpans` returns at least one span, every span's `runsWithoutHr` is `> 0`, and the total days across all spans is far smaller than the spine length (a result equal to the whole spine would mean the rule collapsed).
11. `coverageCaption` returns the exact § 11 string for a non-empty span list and `''` for an empty one.
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/dashboard/views/trends-training-load-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/views/trends-training-load-logic.test.ts` exits 0 with at least 11 passing cases covering every item above.
    - Case 7 is present and asserts **zero** spans for a pure rest gap.
    - `grep -c "runsWithHr" src/dashboard/views/trends-training-load-logic.ts` returns at least `1` and `grep -c "runs > 0" src/dashboard/views/trends-training-load-logic.ts` returns at least `1` (both halves of the D-15 rule are in the source).
    - `grep -c "Shaded regions indicate no HR data (not zero training)." src/dashboard/views/trends-training-load-logic.ts` returns `1` (the caption copy lives in exactly one place).
    - `grep -cE "document\.|window\.|new Date\(\)|TimeScale" src/dashboard/views/trends-training-load-logic.ts` returns `0`.
    - `node -e "const l=require('./dist/dashboard/views/trends-training-load-logic.js');const d=l.parseTrainingLoad(require('./data/stats/training-load.json'));const s=l.findThinCoverageSpans(d.days);const tot=s.reduce((a,x)=>a+x.days,0);if(s.length===0)process.exit(1);if(tot>d.days.length*0.5)process.exit(1);console.log('spans:',s.length,'days shaded:',tot,'of',d.days.length)"` exits 0.
  </acceptance_criteria>
  <done>The training-load chart can be windowed and model-switched from one fetched document, and the shaded spans mark only genuine no-HR-run days — never rest.</done>
</task>

<task type="auto">
  <name>Task 3: Gear table sorting and top-8-plus-Other chart bucketing</name>
  <files>src/dashboard/views/trends-gear-logic.ts, src/dashboard/views/trends-gear-logic.test.ts</files>
  <read_first>
    - `src/analytics/gear-aggregate.types.ts` (plan 18-05) — `GearAggregateDocument`, `GearShoeAggregate` (`key`, `label`, `isUnknown`, `runs`, `distanceM`, `movingTimeSec`, `avgPaceSecPerKm`, `avgHr`, `runsWithHr`, `firstDate`, `lastDate`), `GearYearCoverage`
    - `.planning/phases/18-records-trends-differentiators/18-05-SUMMARY.md` — the observed per-year coverage table
    - `src/dashboard/views/list-logic.ts` lines 205-258 — `compareRows`/`sortRows`: the click-to-sort comparator shape to mirror for a tiny dataset
    - `18-UI-SPEC.md` § 12 — the sortable table's seven columns, the always-present Unknown row, the bar chart capped at the top 8 shoes by distance plus one merged neutral "Other ({m} shoes)" bar, and the caption "Top 8 shoes shown by distance; see the table below for all {n}."
  </read_first>
  <action>
Create `src/dashboard/views/trends-gear-logic.ts` — pure, DOM-free.

Exports:
- `export function parseGearAggregate(raw: unknown): GearAggregateDocument | null` — total, never throws, own-property reads only; returns `null` unless `shoes` is an array and `totals` is an object with numeric `runs`. Malformed individual shoe entries are dropped.
- `export type GearSortKey = 'label' | 'distanceM' | 'runs' | 'avgPaceSecPerKm' | 'avgHr' | 'firstDate';` `export const GEAR_SORT_KEYS: readonly GearSortKey[] = [...];` `export function parseGearSort(raw: string | null): GearSortKey` — allow-list, default `'distanceM'`.
- `export function sortShoes(shoes: readonly GearShoeAggregate[], key: GearSortKey, dir: 'asc' | 'desc'): GearShoeAggregate[]` — returns a new array; **the Unknown row is always pinned last regardless of sort key and direction** (D-18: its presence, not its position, is what makes the absence honest, and letting it float to the top of a distance sort would misread as the biggest "shoe"). `null` values (`avgPaceSecPerKm`, `avgHr`, `firstDate` on an empty bucket) always sort after non-null values in both directions, so a missing measurement never masquerades as the best or worst.
- `export const GEAR_CHART_MAX_CATEGORIES = 8;`
- `export interface GearChartBucket { label: string; distanceM: number; isOther: boolean; mergedCount: number; }`
- `export function buildGearChartBuckets(shoes: readonly GearShoeAggregate[]): { buckets: GearChartBucket[]; caption: string }` — takes the **named** shoes (`isUnknown === false`), sorts by `distanceM` descending, keeps the top `GEAR_CHART_MAX_CATEGORIES`, and merges the remainder into a single `{ label: 'Other ({m} shoes)', isOther: true, mergedCount: m }` bucket whose `distanceM` is the sum. Emits no Other bucket when nothing remains. The Unknown bucket is **excluded from the chart** but never from the table — the chart answers "which shoes did the distance", and a bar labelled Unknown would compete as a category; § 12's caption plus the table carry the full picture. `caption` is `Top ${GEAR_CHART_MAX_CATEGORIES} shoes shown by distance; see the table below for all ${n}.` where `n` is the total shoe count including Unknown, or `''` when no merging was needed.
- `export function coverageSentence(totals: GearAggregateDocument['totals'], byYear: readonly GearYearCoverage[]): string` — D-18's "states its real coverage in plain numbers": a single sentence giving the archive-wide percentage and the most recent year's percentage, e.g. `Gear is recorded for 1,160 of 1,868 runs (62.1%); in 2026 it is 19.0%.` Built from the data, never hardcoded.

Create `src/dashboard/views/trends-gear-logic.test.ts` covering:
1. `parseGearAggregate` against the **live** `data/stats/gear-aggregate.json`: non-null, at least 16 shoes plus Unknown. `null`/`[]`/`__proto__`-keyed input returns `null` without throwing.
2. `sortShoes` by `distanceM` descending, then ascending: **Unknown is last in both**. Assert explicitly in both directions.
3. `sortShoes` by `avgHr` with some `null` values: nulls come last in both directions.
4. `sortShoes` by `label` ascending places `Shoe 10` after `Shoe 9` **or** documents the chosen string-vs-natural ordering in a comment and asserts the documented behaviour — do not leave it unspecified.
5. `buildGearChartBuckets` with 12 named shoes yields 8 named buckets plus one Other bucket with `mergedCount === 4`, and the Other bucket's `distanceM` equals the sum of the four merged shoes.
6. With 8 or fewer named shoes: no Other bucket and an empty caption.
7. The Unknown bucket never appears in `buckets`, in any input arrangement — including when Unknown has the largest `distanceM` of all.
8. `coverageSentence` against the live totals produces a sentence containing the real archive-wide percentage and the most recent year's percentage, with no hardcoded number in the source (`grep` for a literal `62` or `19` in the module returns `0`).
9. Empty shoe list: `buildGearChartBuckets` returns empty buckets and an empty caption without throwing; `coverageSentence` handles zero runs without dividing by zero.
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/dashboard/views/trends-gear-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/views/trends-gear-logic.test.ts` exits 0 with at least 9 passing cases covering every item above.
    - `grep -cE "\b62\b|\b19\b|1,160|1868" src/dashboard/views/trends-gear-logic.ts` returns `0` (coverage numbers are derived, never hardcoded).
    - `grep -cE "document\.|window\.|new Date\(\)" src/dashboard/views/trends-gear-logic.ts` returns `0`.
    - `node -e "const l=require('./dist/dashboard/views/trends-gear-logic.js');const d=l.parseGearAggregate(require('./data/stats/gear-aggregate.json'));const {buckets,caption}=l.buildGearChartBuckets(d.shoes);if(buckets.some(b=>b.label==='Unknown'))process.exit(1);if(buckets.filter(b=>!b.isOther).length>8)process.exit(1);const s=l.sortShoes(d.shoes,'distanceM','desc');if(s[s.length-1].isUnknown!==true)process.exit(1);console.log(caption);console.log(l.coverageSentence(d.totals,d.byYear))"` exits 0 and prints both sentences with real numbers.
    - `npm test` exits 0 with no pre-existing test regressed.
  </acceptance_criteria>
  <done>The gear chart is bounded to eight categories plus an honest Other bar, the table keeps every shoe with Unknown pinned last, and the coverage sentence reports the real numbers including the 2026 erosion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL/query values → chart configuration | `?model=` and window values reach series selection and slice bounds |
| fetched public JSON → client parsing | Three published documents are parsed and walked by key in the browser |
| athlete-typed shoe names → downstream DOM | Labels are blank today but will be free text once filled in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-TAB-02 | Tampering | `parseTrimpModel`, `parseLoadWindow`, `parseGearSort` | mitigate | Exact-match allow-lists with safe defaults, asserted against `null`, empty and unknown inputs; `parseTrimpModel` additionally clamps to `edwards` when the document reports Banister unavailable, so a supplied value can never yield an empty chart |
| T-18-PROTO-04 | Tampering | all three `parse*` functions | mitigate | Own-property reads only; `__proto__`-keyed input asserted unreachable |
| T-18-HONEST-03 | Spoofing (fabricated data) | thin-coverage spans, empty months, unconfigured model | mitigate | A pure rest gap is provably **not** shaded (test case 7); an empty month is `null` not `0`; an unconfigured Banister model yields an empty array rather than a flat zero line |
| T-18-HONEST-04 | Repudiation (hidden absence) | Unknown gear bucket | mitigate | Pinned last in every sort and excluded only from the chart, never from the table; the coverage sentence is derived from the data with no hardcoded numbers |
| T-18-XSS-01 | Tampering / XSS | shoe labels | transfer | These modules return strings only; plan 18-15 renders them with `textContent` (T-16-VW-01) |
| T-18-DOS-05 | Denial of Service | `sliceLoadWindow`, `buildGearChartBuckets` | accept | Bounds come from allow-listed enums and a ~5,500-entry document, never from a raw query value |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs authorized. An executor that concludes an install is necessary must stop and run the Package Legitimacy Gate |
</threat_model>

<verification>
- `npm run build` and `npm test` exit 0.
- All three modules were exercised against the **live** published documents, not only fixtures.
- The pure-rest-is-not-a-coverage-span case is present and passing.
</verification>

<success_criteria>
Every remaining Trends transform exists, is DOM-free and tested, and each of the three places this phase could invent data — an empty month, a rest gap, an unconfigured model — provably produces an honest absence instead.
</success_criteria>

<output>
Create `.planning/phases/18-records-trends-differentiators/18-10-SUMMARY.md` when done. Record the live coverage-span count and total shaded days, the live monthly gap counts for cadence and HR, and the generated gear coverage sentence.
</output>
