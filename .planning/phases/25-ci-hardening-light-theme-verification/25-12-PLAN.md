---
phase: 25-ci-hardening-light-theme-verification
plan: 12
type: execute
wave: 9
depends_on: ["25-11"]
files_modified:
  - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [FIX-02, CI-01, CI-02, VER-01]
gap_closure: true
closes_gaps: [GAP-25-01, GAP-25-02]

must_haves:
  decisions: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13]
  truths:
    - "The governing all-rows-PASS rule is applied per requirement under the Round 2 split: FIX-02 ticks only if R6a PASSED, CI-02 only if R6b PASSED, CI-01 only if R6c PASSED, VER-01 only if R7 PASSED while Round 1's R1, R3, R4 and R5 PASSes stand"
    - "Nothing ticks on a mechanical match and nothing ticks on automated evidence alone; a requirement previously reopened stays reopened unless its own mapped row PASSED in Round 2"
    - "Both disclosure obligations are re-confirmed present before sign-off: D-04's amendment disclosure and D-05's dark-OS deviation disclosure, plus the slowed-load disclosure if plan 25-08 selected the throttled mechanism — the absence of any required disclosure is a defect in the round independent of any row's verdict"
    - "STATE.md's milestone-scoped plan counts are verified BY HAND after any completion tooling runs, because phase.complete writes a repo-wide completed_plans over the milestone-scoped total_plans and the phase-based percent hides the bug"
    - "STATE.md's stopped_at and Status block are repaired by hand if state.planned-phase ran, because it rewrites stopped_at and replaces only the first line of Status"
    - "If any mapped row is FAIL, BLOCKED or NOT EXERCISABLE, nothing ticks and the still-open gap states the state a future row must hold simultaneously — the GAP-24-05 / GAP-25-01 shape, not a restatement of what failed"
  artifacts:
    - path: ".planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md"
      provides: "Round 2 outcome summary, the completed Validation Sign-Off checklist, and the frontmatter status/nyquist_compliant reflecting the actual verdicts"
      contains: "Round 2 outcome"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Dated Round 2 disposition paragraph for each of FIX-02, VER-01, CI-01 and CI-02, naming the row that decided it"
      contains: "FIX-02"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 25 plan count of 12 confirmed, the five new plan entries under waves 5-9 ticked according to what executed, and a progress-table row consistent with the disposition"
      contains: "Phase 25"
    - path: ".planning/STATE.md"
      provides: "Current Position and milestone-scoped progress counts reflecting the Round 2 outcome"
      contains: "Phase 25"
  key_links:
    - from: "the Round 2 row verdicts in 25-VALIDATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the all-rows-PASS governing rule applied one row per requirement"
      pattern: "R6a|R6b|R6c|R7"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "the Phase 25 gate entry and the progress-table row"
      pattern: "Phase 25"
---

<objective>
Set the Round 2 disposition for all four requirements under the all-rows-PASS rule, now applied one
row per requirement, and bring `25-VALIDATION.md`, `REQUIREMENTS.md`, `ROADMAP.md` and `STATE.md`
into agreement with the actual verdicts.

Purpose: Round 1 withheld all four requirements because a single BLOCKED row was mapped to three of
them. GAP-25-02's split, drafted in plan 25-09 and run in plan 25-11, exists so that outcome cannot
repeat: each requirement now stands or falls on its own row. This plan applies that rule honestly in
both directions — ticking what earned it and leaving open what did not.

Output: a dated disposition in all four planning documents, with the still-open gaps (if any) stated
in the shape a future round can act on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-10-SUMMARY.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-11-SUMMARY.md

<interfaces>
<!-- Established rules and hazards. Do NOT re-derive; do NOT re-litigate. -->

THE GOVERNING RULE (Phase 24 plans 24-14/24-17 precedent, non-waivable): a requirement ticks ONLY if
every row mapped to it is PASS. Round 2's map is one row per requirement:
  FIX-02 <- R6a       CI-02 <- R6b       CI-01 <- R6c
  VER-01 <- R7, while Round 1's R1, R3, R4 and R5 PASSes stand
Do not tick on a mechanical match. Do not tick because the automated gate is green — that is exactly
what happened when plans 25-01/25-02/25-03 ticked FIX-02, CI-01 and CI-02 before the closing
checkpoint ran, and all three had to be reopened.

THE CURRENT STATE OF THE FOUR REQUIREMENTS in `REQUIREMENTS.md` (lines 53-56): all four are `[ ]`,
each carrying a dated 2026-09-04 withhold paragraph naming the row that withheld it. The status table
at lines 96-99 reads `Withheld (R6 BLOCKED)` for FIX-02, CI-01 and CI-02 and `Withheld (R2 BLOCKED)`
for VER-01. Round 2's disposition amends these rather than replacing their history.

THE ROADMAP ENTRY: `.planning/ROADMAP.md`'s Phase 25 section currently reads `**Plans**: 7 plans`
with waves 1-4 listed and 25-07's checkbox still `[ ]`. Five plans were added by this gap-closure
round (25-08 through 25-12, waves 5-9), so the count becomes 12 and the new entries are listed under
their waves. The milestone-checklist entry and the progress-table row for Phase 25 both need to match
whatever disposition this plan takes.

KNOWN TOOLING HAZARDS, both recorded in this project's memory, both to be checked BY HAND after any
tool call that touches these files:
  - `state.planned-phase` rewrites `stopped_at` and replaces only the FIRST LINE of the `Status`
    block in `STATE.md`. If it runs, repair the rest of the block by hand.
  - `phase.complete` writes a repo-wide `completed_plans` over the milestone-scoped `total_plans` in
    `STATE.md`'s frontmatter, and `percent` is phase-based so it hides the bug. Plan 25-07 verified
    by hand that `total_plans: 98` and `completed_plans: 92` still describe milestone v2.1. With five
    plans added by this round, the milestone-scoped `total_plans` becomes 103, and `completed_plans`
    advances by however many of 25-08 through 25-12 actually executed.

A THIRD HAZARD, also in project memory: GSD plans tick requirements before review and verify run. For
this phase, note that `/gsd-verify-work` has NEVER been run — there is no `25-VERIFICATION.md`. The
authoritative gap record for Phase 25 is `25-VALIDATION.md`. State that plainly in the disposition so
a future reader does not look for a verification artefact that does not exist.

THE DISCLOSURE OBLIGATIONS that must be re-confirmed present at sign-off: D-04's amendment disclosure
and D-05's dark-OS deviation disclosure (both already in the Round 1 section), plus a slowed-load
disclosure in R7 if plan 25-08 selected the throttled mechanism. The absence of a required disclosure
is a defect in the round, independent of any row's verdict — this is the standard plan 25-07's Task 3
applied and it carries forward unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Round 2 outcome, complete the Validation Sign-Off, and set the validation frontmatter</name>
  <files>.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</files>
  <read_first>
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (the whole Round 2 section with its four scored rows; the `## Gaps opened by Round 1` section; the `## Validation Sign-Off` checklist; and the frontmatter block)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-10-SUMMARY.md (R7's verdict and its gap disposition)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-11-SUMMARY.md (R6a/R6b/R6c's verdicts and GAP-25-02's disposition)
  </read_first>
  <action>
Write a `### Round 2 outcome` block at the end of the Round 2 Checkpoint section containing:

1. A one-line summary in the shape Round 1 used: how many rows PASSED, which did not, and what that
   withholds under the one-row-per-requirement map.

2. A verdict table with one line per row: R6a / FIX-02, R6b / CI-02, R6c / CI-01, R7 / VER-01, each
   with its verdict and a one-line decisive-evidence cell quoting the actual value that decided it
   (the run id and conclusion for R6c, the check-count line for R6b, the eight named cases for R6a,
   the frame arithmetic and sampled colour for R7).

3. A statement of what each verdict withholds or releases, per requirement, naming the rule.
   Explicitly note the contrast with Round 1: a single BLOCKED row no longer withholds three
   requirements, because the split was drafted before it was run.

4. The gap disposition, consolidated: for each of GAP-25-01 and GAP-25-02, either a dated CLOSED note
   citing the row and the specific evidence that closed it, or a statement that it remains open with
   which of its numbered clauses is still unmet. If a successor gap was opened by plan 25-10
   (GAP-25-03), restate its clauses here so all open gaps are visible in one place.

5. A disclosure audit paragraph: confirm by name that D-04's amendment disclosure and D-05's dark-OS
   deviation disclosure are present in the file, and that R7 carries a slowed-load disclosure if the
   throttled mechanism was used. Record each as present or absent; an absence is recorded as a defect
   in the round, not silently repaired.

6. A note that `/gsd-verify-work` has never run for Phase 25 and that no `25-VERIFICATION.md` exists,
   so this file is the authoritative record — and, if every row PASSED, a recommendation that
   `/gsd-verify-work 25` run before the v2.1 milestone is considered closed.

Then complete the `## Validation Sign-Off` checklist, ticking only what is genuinely true, and set the
frontmatter:
  - If every Round 2 row PASSED and Round 1's R1/R3/R4/R5 stand: `status: passed` and
    `nyquist_compliant: true`.
  - Otherwise: leave `status: gaps_found` and `nyquist_compliant: false`, and say which row is the
    reason in the Approval paragraph.
Update the `**Approval:**` paragraph at the end of the file with a dated Round 2 statement, keeping
Round 1's withheld paragraph above it as the audit trail rather than overwriting it.

Also update the `## Per-Task Verification Map`'s two outstanding rows: the `25-07-T2` manual VER-01
row and the split `25-02-T3 / 25-06-T2` CI-01 live-run row, replacing their blocked/❌ statuses with
the Round 2 outcome and a pointer to the row that decided each.
  </action>
  <verify>
    <automated>V=.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md; grep -c "Round 2 outcome" $V; grep -c "pending" $V; head -10 $V</automated>
  </verify>
  <acceptance_criteria>
    - `25-VALIDATION.md` contains a `### Round 2 outcome` block with a verdict table naming all four rows, their requirements and their decisive evidence.
    - No Round 2 Verdict cell reads `pending`.
    - Each of GAP-25-01 and GAP-25-02 carries either a dated CLOSED note citing the row and evidence, or a statement of which numbered clause remains unmet; any successor gap opened in plan 25-10 is restated here.
    - The disclosure audit paragraph names D-04's amendment disclosure and D-05's dark-OS disclosure as present, and names the slowed-load disclosure as present or not-applicable.
    - The frontmatter `status` and `nyquist_compliant` values match the verdicts: `passed` / `true` only if every mapped row PASSED, otherwise `gaps_found` / `false`.
    - The Approval paragraph carries a dated Round 2 statement and Round 1's withheld paragraph is retained above it.
    - The Per-Task Verification Map's manual VER-01 row and CI-01 live-run row no longer read blocked or ⬜, and each points at the Round 2 row that decided it.
    - The file states that no `25-VERIFICATION.md` exists and that `/gsd-verify-work` has not run for this phase.
  </acceptance_criteria>
  <done>The validation file records the Round 2 outcome, its gap dispositions and its disclosure audit, with frontmatter that matches the verdicts rather than the hoped-for result.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the disposition to REQUIREMENTS.md, ROADMAP.md and STATE.md, with the known tooling hazards checked by hand</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 53-56 — the four requirements with their Round 1 withhold paragraphs; line 67 — the milestone-wide Verification Note; lines 96-99 — the status table)
    - .planning/ROADMAP.md (the Phase 25 section: the Goal, the five Success Criteria, the `**Plans**: 7 plans` line, the wave checklists, and the Phase 25 row in the progress table; plus the milestone-checklist entry for Phase 25)
    - .planning/STATE.md (the frontmatter `progress` block, the `## Current Position` block, and the format plan 25-07 used for a withheld-gate note)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (Task 1's Round 2 outcome block — the source of truth for every disposition written here)
  </read_first>
  <action>
Apply the all-rows-PASS rule per requirement. Amend, do not overwrite: Round 1's dated paragraphs stay
as the audit trail and Round 2's disposition is appended to each.

REQUIREMENTS.md — for each of FIX-02, VER-01, CI-01 and CI-02:
  - If its mapped row PASSED in Round 2, flip the checkbox to `[x]` and append a dated 2026-09
    paragraph naming the deciding row and quoting the decisive evidence — the dispatched run id and
    conclusion for CI-01, the `N check(s) passed, 0 failure(s).` line and the six named documents for
    CI-02, the eight named regression cases and the five exit codes for FIX-02, the frame arithmetic
    and sampled colour for VER-01. Follow the style of the existing FIX-01 / CUR-01 entries.
  - If its mapped row did not PASS, leave the checkbox `[ ]` and append a dated paragraph stating
    exactly which row withheld it and which clause of that row was unmet. Do not reword the Round 1
    paragraph.
  - Update the status table at lines 96-99 to match, replacing `Withheld (R6 BLOCKED)` and
    `Withheld (R2 BLOCKED)` with the Round 2 outcome per requirement.

ROADMAP.md — Phase 25 section:
  - Confirm the `**Plans**: 12 plans` line and the Wave 5 through Wave 9 entries for 25-08, 25-09,
    25-10, 25-11 and 25-12 are present — the planner wrote them at planning time. Correct the count
    only if a plan was actually added or dropped during execution.
  - Tick 25-07's checkbox `[x]` (it executed in Round 1) and tick each of the five new entries that
    actually executed. Leave unticked any plan that halted rather than completed, and say so in one
    line beside it.
  - If every mapped row PASSED: tick the Phase 25 milestone-checklist entry with a dated "PHASE GATE
    CLOSED" paragraph naming the four rows, and set the progress-table row to `12/12 | Complete |
    2026-09-XX`. Otherwise leave the gate open, set the progress-table row to the actual completed
    count with status `In Progress`, and state in one line which requirement is still open and why.

STATE.md — by hand, not via tooling where avoidable:
  - Update `## Current Position` with a Round 2 note in the same shape as plan 25-07's Round 1 note:
    the verdicts, what ticked and what did not, which gaps closed and which remain, and the next step
    (`/gsd-verify-work 25` if the gate closed and no verification artefact exists yet, or
    `/gsd-plan-phase 25 --gaps` if a gap is still open).
  - Update `stopped_at`, `status` and `last_activity` to describe the Round 2 outcome.
  - Verify the milestone-scoped plan counts BY HAND: `total_plans` should read 103 (98 plus this
    round's five plans) and `completed_plans` should be 92 plus the number of 25-08 through 25-12 that
    actually executed. `completed_phases` increments only if the Phase 25 gate actually closed.
    Quote the verified values in the SUMMARY.
  - If any GSD tooling call rewrote `stopped_at` or replaced only the first line of the `Status`
    block, repair the rest by hand and say so in the SUMMARY. If `phase.complete` ran, re-check that
    `total_plans` and `completed_plans` still describe milestone v2.1 rather than the whole repo; the
    phase-based `percent` will not reveal the error.

Finally, if any requirement remains open, state in one line at the end of the STATE.md note what the
next round's scope is, referencing the still-open gap by number.
  </action>
  <verify>
    <automated>grep -n "FIX-02\|VER-01\|CI-01\|CI-02" .planning/REQUIREMENTS.md | head -8; grep -c "Plans.*: 12 plans" .planning/ROADMAP.md; grep -n "total_plans\|completed_plans\|completed_phases" .planning/STATE.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Each of FIX-02, VER-01, CI-01 and CI-02 in `REQUIREMENTS.md` carries a dated 2026-09 Round 2 paragraph naming the row that decided it, appended below the Round 1 paragraph rather than replacing it.
    - A requirement is `[x]` if and only if its single mapped Round 2 row PASSED (and, for VER-01, Round 1's R1/R3/R4/R5 also stand).
    - The `REQUIREMENTS.md` status table rows for all four requirements match the Round 2 verdicts.
    - `grep -c "Plans.*: 12 plans" .planning/ROADMAP.md` returns 1 for the Phase 25 section, and the five new plan entries appear under wave headings 5 through 9.
    - The ROADMAP progress-table row for Phase 25 shows a real completed count, a status and a date consistent with the disposition — no `TBD`, no `Not started`.
    - `STATE.md`'s `progress.total_plans` reads 103 and `completed_plans` equals 92 plus the number of this round's plans that executed; both values are quoted in the SUMMARY as hand-verified.
    - `STATE.md`'s `## Current Position` carries a Round 2 note naming the verdicts, the gap dispositions and the next command to run.
    - If the gate closed, `completed_phases` incremented; if it did not, `completed_phases` is unchanged.
  </acceptance_criteria>
  <done>All four planning documents agree with the Round 2 verdicts, nothing ticked on a mechanical match, and the two documented STATE.md tooling hazards were checked by hand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Row verdicts -> requirement ticks | The only place a checkpoint outcome becomes a claim about the product. Phase 25 has already ticked three requirements prematurely once and had to reopen them. |
| GSD tooling -> `STATE.md` frontmatter | Two documented tools corrupt milestone-scoped counts or truncate the Status block, and the phase-based percent hides one of them. |
| `25-VALIDATION.md` -> a future reader looking for `25-VERIFICATION.md` | No verification artefact exists for this phase; a reader who assumes one does could mistake its absence for a passing state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-41 | Repudiation (premature tick) | The four requirement dispositions | mitigate | The all-rows-PASS rule applied one row per requirement, with each tick required to quote its row's decisive evidence. Round 1's withhold paragraphs are retained as the audit trail rather than overwritten, so the reopen history stays visible. |
| T-25-42 | Tampering (silent count corruption) | `STATE.md` frontmatter `progress` | mitigate | `total_plans` and `completed_plans` are verified by hand against the milestone-scoped expectation (103, and 92 plus this round's executed plans) after any tooling call, per the documented `phase.complete` hazard; the values are quoted in the SUMMARY so a reviewer can check them. |
| T-25-43 | Tampering (truncated status block) | `STATE.md` `## Current Position` | mitigate | If `state.planned-phase` ran, only the first line of `Status` survives; the task requires repairing the rest by hand and disclosing that it was repaired. |
| T-25-44 | Repudiation (missing disclosure) | The round's disclosure obligations | mitigate | Task 1's disclosure audit records D-04's amendment disclosure, D-05's dark-OS disclosure and any slowed-load disclosure as present or absent, and treats an absence as a defect in the round independent of the verdicts — the standard plan 25-07 applied. |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; this plan edits four Markdown files. `git diff --stat package.json package-lock.json` stays empty, as it has across the whole phase. |

Not applicable, one line each:
- **V2 Authentication / V3 Session Management / V4 Access Control / V5 Input Validation / V6 Cryptography** — this plan edits planning documents only; no runtime code path, credential, parser or crypto surface is touched.
</threat_model>

<verification>
- `25-VALIDATION.md` carries a Round 2 outcome block, a completed sign-off, frontmatter matching the verdicts, and a consolidated gap disposition.
- Every one of FIX-02, VER-01, CI-01 and CI-02 carries a dated Round 2 paragraph, and each checkbox state follows strictly from its own mapped row.
- `ROADMAP.md` reads `**Plans**: 12 plans` for Phase 25 with waves 5-9 listed, and its progress-table row matches the disposition.
- `STATE.md`'s milestone-scoped counts were hand-verified and are quoted in the SUMMARY.
- Any still-open gap is stated in the form a future round can act on, with its numbered clauses.
</verification>

<success_criteria>
- Each of the four requirements stands or falls on its own evidence, which is what GAP-25-02's split was for — a single missing item can no longer withhold three requirements.
- The phase's gate state is honest in both directions: nothing withheld that earned its tick, nothing ticked that did not.
- The four planning documents agree with each other and with `25-VALIDATION.md`, and the next command to run is stated explicitly.
</success_criteria>

<output>
Create `.planning/phases/25-ci-hardening-light-theme-verification/25-12-SUMMARY.md` when done.
It MUST include: `## Round 2 verdicts` (four rows, one requirement each), `## Disposition` (what
ticked, what did not, and the row that decided each), `## Gap dispositions` (GAP-25-01, GAP-25-02
and any successor, closed or open with clauses), `## Disclosure audit` (D-04, D-05 and slowed-load,
each present or absent), and `## STATE.md hand-verification` (the quoted `total_plans`,
`completed_plans` and `completed_phases` values and whether any tooling hazard fired).
`requirements-completed` MUST list ONLY the requirements whose single mapped Round 2 row PASSED.
</output>
