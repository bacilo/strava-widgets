---
phase: 25-ci-hardening-light-theme-verification
plan: 11
type: execute
wave: 8
depends_on: ["25-10"]
files_modified:
  - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md
autonomous: false
requirements: [FIX-02, CI-02, CI-01]
gap_closure: true
closes_gaps: [GAP-25-02]

must_haves:
  decisions: [D-01, D-02, D-03, D-09, D-10, D-11, D-12, D-13]
  truths:
    - "GAP-25-02 clause 4: the dispatch is performed from a normal single-context execution in the main checkout with the developer's explicit authorisation — not from a parallel worktree and not opportunistically during a checkpoint"
    - "The authorisation checkpoint states BOTH production side effects up front: pushing alone matches daily-refresh.yml's own push-trigger paths and starts a production run, and the workflow's Commit and Deploy steps carry no github.ref branch guard so any run deploys to the live Pages site and commits data back to origin"
    - "GAP-25-02 clause 1: origin/master is confirmed to carry compute-all-stats --ci by reading the PUSHED copy of daily-refresh.yml, not the local one — the grep returned 0 on the pushed copy and 1 locally at planning time"
    - "GAP-25-02 clause 2: a dispatched run id and its conclusion are quoted from gh run view; the cited run is the workflow_dispatch one, with any push-triggered run recorded separately as context"
    - "GAP-25-02 clause 3: the collapsed Compute all statistics step's own log carries a per-step line for each of the eight names in COMPUTE_ALL_STATS_STEPS order, enumerated from src/compute-all-stats-steps.ts rather than read off the log"
    - "R6a, R6b and R6c are scored against the conditions plan 25-09 drafted, unedited; each withholds only its own requirement"
    - "R6a's and R6b's evidence is a FRESH run on the exact commit that is pushed, not a citation of the wave-1 numbers, because merging origin/master changes the tree"
    - "Divergence with origin/master is resolved by merge, never rebase (Phase 16 precedent: rebasing would rewrite the phase's commits for auto-generated data)"
    - "Nothing observed during the round is patched (house rule since plan 16-09)"
  artifacts:
    - path: ".planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md"
      provides: "R6a, R6b and R6c scored with quoted evidence; the push and dispatch authorisation recorded verbatim; the collapsed-step log excerpt; and GAP-25-02's disposition"
      contains: "R6c"
  key_links:
    - from: "local master's .github/workflows/daily-refresh.yml"
      to: "origin/master"
      via: "git push, verified afterwards by reading the pushed copy"
      pattern: "compute-all-stats --ci"
    - from: "the dispatched workflow run's collapsed compute step log"
      to: "src/compute-all-stats-steps.ts COMPUTE_ALL_STATS_STEPS"
      via: "the eight step names enumerated from source and matched in the log"
      pattern: "compute-training-load"
---

<objective>
Close GAP-25-02: produce CI-01's live-run evidence, which has never existed for this phase, and score
the three drafted rows R6a, R6b and R6c so that FIX-02 and CI-02 are no longer withheld by a clause
that belongs to CI-01.

Purpose: R6 was BLOCKED in Round 1 because one of its three clauses had no evidence — no
`gh workflow run "Daily Widget Refresh"` dispatch has ever been performed for this phase. Because R6
was the only row mapped to all three requirements, that single absence withheld FIX-02 and CI-02 as
well, whose own evidence is green. Plan 25-09 drafted the split before it was run. This plan runs it.

Output: R6a, R6b and R6c scored in `25-VALIDATION.md` with quoted evidence, plus the authorisation
record and the collapsed-step log excerpt. No requirement is ticked here — disposition is plan
25-12's job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-06-SUMMARY.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-09-SUMMARY.md

<interfaces>
<!-- Established facts, verified 2026-09-04 during planning. Do NOT re-derive by exploration. -->

REPOSITORY STATE at planning time:
  local master HEAD          3003bb8b
  origin/master HEAD         62304636
  local ahead by             51 commits (all of Phase 25 is unpushed)
  pushed workflow check      `git show origin/master:.github/workflows/daily-refresh.yml |
                             grep -c "compute-all-stats --ci"` returned 0
  local workflow             `.github/workflows/daily-refresh.yml:96-97` — step named "Compute all
                             statistics" running `node dist/index.js compute-all-stats --ci`
  gh CLI                     2.86.0, authenticated as `bacilo` with `repo` and `workflow` scopes
  main checkout              /Users/pedf/workspace/strava-widgets (NOT a `.claude/worktrees/` path)

THE TWO PRODUCTION SIDE EFFECTS, both real and both unavoidable:
  1. THE PUSH ITSELF TRIGGERS A RUN. `daily-refresh.yml`'s `on.push` block (lines 17-33) fires for
     `branches: [master]` with `paths` including `src/**`, `scripts/**`, `.github/workflows/**`,
     `package.json`, `package-lock.json`, `tsconfig*.json` and `vite.config*.ts`. Phase 25's 51
     unpushed commits touch several of those, so pushing alone starts a full production run. This is
     a planning-time finding that plan 25-06's blocker note did not surface, and it must be stated to
     the developer BEFORE the push, not discovered afterwards.
  2. NO BRANCH GUARD ON THE SIDE-EFFECTING STEPS. `grep -n "if:\|github.ref"
     .github/workflows/daily-refresh.yml` returns only the two `Warn on *` conditionals. Neither
     `Commit updated data and stats` nor `Deploy widgets to GitHub Pages` is conditioned on
     `github.ref`, so any run — push-triggered or dispatched — deploys to the live Pages site
     (`bacilo.github.io/strava-widgets`) and commits data back to `origin`.

CONCURRENCY: the workflow declares `concurrency: group: widget-refresh, cancel-in-progress: false`,
so a dispatch issued right after the push queues behind the push-triggered run rather than cancelling
it. Expect two runs and expect to wait.

THE EIGHT STEP NAMES, from `src/compute-all-stats-steps.ts`'s exported `COMPUTE_ALL_STATS_STEPS`, in
array order — R6c's independently-derived value:
  compute-stats (mandatory), compute-advanced-stats (mandatory), compute-geo-stats,
  compute-best-efforts, compute-age-grading, compute-dashboard-index, compute-gear-aggregate,
  compute-training-load
`src/index.ts:303-310` prints a line of the form "> NAME" before each step runs; `src/index.ts:313`
prints "All statistics generated successfully!" after the walk; `src/index.ts:322` prints a
"DEGRADED STEPS (N)" summary ONLY when a tolerated step failed (D-03), so its presence is conditional
and is not part of any pass condition.

THE NUMBERS R6a AND R6b RECONCILE AGAINST: the Wave 1 Integration Gate recorded five commands all
exit 0, three consecutive identical `npm test` tallies of 62 files / 1596 tests, and
`verify-dashboard`'s final line `56 check(s) passed, 0 failure(s).` — up from 40/40 at the end of
Phase 24. The 60/1560 pre-planning baseline plus 8 (25-01) + 10 (25-02) + 1 (25-04) + 17 (25-05)
accounts for the delta with no unexplained residue.

MERGE, NEVER REBASE (Phase 16 precedent, and project memory on CI auto-commit push races):
`origin/master` advances with nightly `chore: update activities and stats [skip ci]` auto-commits.
Expect a non-fast-forward. Resolve with `git merge origin/master`; rebasing would rewrite this
phase's commits for auto-generated data.

WORKTREE ENVIRONMENT WARNING from plan 25-06: if this plan is executed inside a
`.claude/worktrees/agent-*` worktree, GAP-25-02 clause 4 is violated by construction and the plan
must STOP rather than proceed. Check with `git rev-parse --show-toplevel` before anything else.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge origin/master locally, run the five-command gate on the exact tree that will be pushed, and record R6a/R6b's raw evidence</name>
  <files>.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</files>
  <read_first>
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (R6a's and R6b's drafted conditions in the Round 2 section; the "Wave 1 Integration Gate" section for the reconciliation format; and the "D-11 RED Observation Log" for the failing-direction citations both rows use)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-06-SUMMARY.md (the worktree data-regeneration precedent: regenerate gitignored `data/stats` and `data/dashboard` with `npm run build` plus `node dist/index.js compute-all-stats` rather than symlinking, and revert the incidental `data/geo/geo-metadata.json` timestamp churn before committing)
    - src/analytics/gear-aggregate-logic.test.ts (the eight FIX-02 regression case names R6a enumerates by name)
    - .github/workflows/daily-refresh.yml (lines 88-125 — the collapsed compute step and the two blocking gate steps, so the local gate mirrors what CI will run)
  </read_first>
  <action>
No pushing and no dispatching in this task. Local work only.

1. STOP IMMEDIATELY if `git rev-parse --show-toplevel` resolves to a path under `.claude/worktrees/`.
   GAP-25-02 clause 4 requires a normal single-context execution in the main checkout
   (`/Users/pedf/workspace/strava-widgets`). Report and halt; do not attempt a workaround.

2. `git fetch origin`, then record the divergence: `git rev-list --count origin/master..master` and
   `git rev-list --count master..origin/master`. Merge with `git merge origin/master` — never
   `git rebase`. If conflicts appear, expect them only in auto-generated data; resolve conservatively
   and record exactly what was resolved and how. Record the resulting merge commit sha: this is the
   commit R6a and R6b are evidence about, and the commit Task 3 pushes.

3. Regenerate the gitignored trees if needed so the gate is honest, following 25-06's precedent:
   `npm run build` then `node dist/index.js compute-all-stats`. Afterwards revert the incidental
   `data/geo/geo-metadata.json` `generatedAt` churn with `git checkout --` so no archive file is
   modified.

4. Run the five gate commands in order, recording each exit code: `npm test`, `npx tsc --noEmit`,
   `npm run build`, `npm run build-widgets`, `npm run verify-dashboard`. Run `npm test` three times
   and record all three tallies, as the Wave 1 Integration Gate did, so a nondeterminism claim can be
   checked rather than assumed.

5. Reconcile the `npm test` tally against 62 files / 1596 tests. If the merge changed it, attribute
   every unit of delta by name; an unexplained residue in either direction is itself a finding and
   must be recorded rather than rounded away.

6. Enumerate R6a's eight FIX-02 regression cases BY NAME from `gear-aggregate-logic.test.ts` and
   confirm each is present and passing — four D-12 shapes (absent `gearName` key, `undefined`, empty
   string, non-string) times two functions (`buildGearAggregate`, `buildGearCoverage`). Quote the
   per-file result line. A green whole suite is not sufficient evidence for this row.

7. Enumerate R6b's six CI-02 documents BY NAME in the `verify-dashboard` output — weekly-distance,
   monthly-stats, yearly-stats, year-over-year, best-efforts.json, and the runtime-derived shard
   (quote the sampled activity id actually used). Record the final `N check(s) passed, M failure(s).`
   line verbatim.

8. Confirm `git status --porcelain data` and `git status --porcelain dist` are both empty after the
   gate.

Write all of this into R6a's and R6b's evidence blocks in the Round 2 section as raw recorded
evidence. Do NOT set either Verdict cell yet — scoring happens in Task 3, after the round's other
evidence exists, so that no row is scored in isolation and then reinterpreted.
  </action>
  <verify>
    <automated>git rev-parse --show-toplevel; npm test 2>&1 | tail -5; npx tsc --noEmit && npm run verify-dashboard 2>&1 | tail -2; git status --porcelain data dist | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --show-toplevel` prints `/Users/pedf/workspace/strava-widgets`; the plan did not run in a worktree.
    - The merge with `origin/master` is recorded with the before/after divergence counts and the resulting commit sha, and `git log --oneline -1 --merges` confirms a merge rather than a rebase.
    - All five gate commands are recorded with exit code 0.
    - Three `npm test` tallies are recorded; any delta from 62 files / 1596 tests is attributed by name with no unexplained residue.
    - R6a's evidence block names all eight FIX-02 regression cases individually and shows each passing.
    - R6b's evidence block names all six CI-02 documents individually, quotes the runtime-sampled shard id, and quotes the final `N check(s) passed, M failure(s).` line with `0 failure(s)`.
    - `git status --porcelain data dist` returns 0 lines.
    - R6a's and R6b's Verdict cells still read `pending`.
  </acceptance_criteria>
  <done>The exact tree that will be pushed has a fresh, fully reconciled five-command gate on record, with R6a's eight cases and R6b's six documents enumerated by name.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Obtain explicit developer authorisation for the push and the dispatch, with both production side effects stated up front</name>
  <files>.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</files>
  <read_first>
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (the "CI-01 live run evidence — blocked" section, GAP-25-02's four clauses, and R6c's drafted text with its production-side-effect note)
    - .github/workflows/daily-refresh.yml (lines 1-45 for the push/schedule/dispatch triggers and the push `paths` list; lines 124-145 for the unguarded Commit and Deploy steps)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-06-SUMMARY.md (why plan 25-06 refused to do this from a worktree)
  </read_first>
  <decision>
    Whether to push Phase 25's 51 unpushed commits to `origin/master` and then dispatch
    `gh workflow run "Daily Widget Refresh"`, knowing both actions have real production side effects.
    This is the action GAP-25-02 clause 4 requires to be separately authorised rather than taken
    opportunistically.
  </decision>
  <context>
    CI-01's live-run evidence does not exist. It cannot be manufactured locally: the requirement is a
    real run on the real GitHub Actions runner, and `workflow_dispatch` is already configured on the
    workflow. Plan 25-06 refused to do it from inside a parallel worktree and recorded that refusal
    rather than faking the evidence. Task 1 has just merged `origin/master` and run a green
    five-command gate on the exact commit that would be pushed.

    TWO SIDE EFFECTS, both real, stated before you decide:

    1. THE PUSH ALONE STARTS A PRODUCTION RUN. `daily-refresh.yml` has an `on.push` trigger for
       `master` filtered by `paths`, and that list includes `src/**`, `scripts/**`,
       `.github/workflows/**`, `package.json`, `package-lock.json`, `tsconfig*.json` and
       `vite.config*.ts`. Phase 25 touches several of them. So pushing is not a quiet operation —
       it starts the same workflow the dispatch would.

    2. NEITHER RUN IS FENCED. The `Commit updated data and stats` and `Deploy widgets to GitHub
       Pages` steps carry no `github.ref` branch guard. Any run of this workflow deploys to the live
       site `https://bacilo.github.io/strava-widgets/` and commits data back to `origin`. There is no
       dry-run mode and no non-production ref that avoids it.

    Because `concurrency: group: widget-refresh` has `cancel-in-progress: false`, the dispatched run
    will queue behind the push-triggered run rather than replacing it. Expect two runs.

    What you get for it: CI-01's only possible evidence — a real run id, a real conclusion, and the
    collapsed "Compute all statistics" step's log showing one invocation carrying all eight step
    names where the workflow previously had eight hand-maintained steps. Without it, R6c stays
    BLOCKED and CI-01 stays open.

    What is also true: production currently serves a build from `origin/master` @ `623046363`, which
    predates all of Phase 25. Pushing brings production up to date with the phase's work. Round 1's
    R5 already disclosed that gap.
  </context>
  <options>
    <option id="authorise-both">
      <name>Authorise the push and the dispatch</name>
      <pros>Produces CI-01's evidence; brings production up to date with the phase; the five-command gate has already passed on exactly this commit; the workflow's own blocking `npm test` and `npm run verify-dashboard` steps run again on the CI runner before anything deploys</pros>
      <cons>Two production runs; live Pages redeploy; data auto-commits to `origin`; if the run fails, production is left mid-flight and the failure is a real finding for this phase</cons>
    </option>
    <option id="authorise-push-only">
      <name>Push only, and cite the push-triggered run instead of a dispatched one</name>
      <pros>One production run instead of two</pros>
      <cons>GAP-25-02 clause 2 asks for a DISPATCHED run id specifically, and the drafted R6c says so; substituting a push-triggered run would be a post-hoc reinterpretation of a drafted row, which is the failure mode Checkpoint Row Discipline rule 3 forbids</cons>
    </option>
    <option id="decline">
      <name>Decline for now</name>
      <pros>No production side effects today</pros>
      <cons>R6c stays BLOCKED, CI-01 stays open, and GAP-25-02 stays open; R6a and R6b can still be scored, so FIX-02 and CI-02 are no longer hostage to CI-01's absence — which was the entire point of the split</cons>
    </option>
  </options>
  <action>
Present the decision to the developer with both side effects stated up front, in the developer's own
reading order: what the push does by itself, what the dispatch adds, and what is gained. Do not
minimise either side effect and do not proceed on an implied yes.

Record the developer's response VERBATIM in R6c's evidence block, together with the timestamp, as
GAP-25-02 clause 4's authorisation evidence. Also record `git rev-parse --show-toplevel` output there
as the not-a-parallel-worktree evidence clause 4 requires.

If the developer declines, or chooses push-only: do not push and do not dispatch beyond what was
authorised. Proceed to Task 3 and score only the rows whose evidence exists — R6a and R6b — leaving
R6c BLOCKED with the developer's decision recorded as the reason. That outcome is not a failure of
this plan; it is the split working as designed.
  </action>
  <verify>
    <automated>V=.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md; grep -c "authoris" $V; git rev-parse --show-toplevel; git log --oneline origin/master..master | wc -l</automated>
    <human-check>The developer read both production side effects — that the push alone triggers a run via the workflow's own paths filter, and that neither the Commit nor the Deploy step is branch-guarded — before choosing, and their choice is recorded verbatim.</human-check>
  </verify>
  <acceptance_criteria>
    - R6c's evidence block contains the developer's authorisation response quoted verbatim with a timestamp.
    - The presented text names both side effects explicitly: that the push alone triggers a run via the `paths` filter, and that neither the Commit nor the Deploy step is branch-guarded.
    - R6c's evidence block records `git rev-parse --show-toplevel` as the main checkout, satisfying GAP-25-02 clause 4's not-a-parallel-worktree requirement.
    - No `git push` and no `gh workflow run` has been executed at the end of this task.
  </acceptance_criteria>
  <done>The developer's decision is recorded verbatim and no production-affecting command has been run yet.</done>
  <resume-signal>Reply with one of: "authorise both", "push only", or "decline" — and any conditions you want attached.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute the authorised actions, capture the live-run evidence, and score R6a, R6b and R6c</name>
  <files>.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</files>
  <read_first>
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (Task 2's recorded authorisation; R6a, R6b and R6c's drafted pass and fail conditions; Task 1's recorded gate evidence; and GAP-25-02's four clauses)
    - src/compute-all-stats-steps.ts (the eight step names and their array order — enumerate from here, not from the log)
    - src/index.ts (lines 289-340 — the "> NAME" announcement format, the success line, and the conditional DEGRADED STEPS summary)
  </read_first>
  <action>
Execute only what Task 2 authorised. If the developer declined, skip straight to the scoring step and
record R6c BLOCKED with the decision as the reason.

1. PUSH. `git push origin master`. If it is rejected non-fast-forward because another nightly
   auto-commit landed in the meantime, `git fetch origin` and `git merge origin/master` again — never
   rebase — then re-run Task 1's five commands if the merge brought in anything other than data
   auto-commits, and push again. Record every attempt and its outcome.

2. VERIFY THE PUSHED COPY (GAP-25-02 clause 1). Run `git fetch origin` then
   `git show origin/master:.github/workflows/daily-refresh.yml | grep -c "compute-all-stats --ci"`
   and record the count. It must be 1; it was 0 before the push. Also record
   `git rev-parse --short origin/master`. This clause is about the PUSHED file, so do not substitute
   a read of the local working copy.

3. RECORD THE PUSH-TRIGGERED RUN as context, not as clause 2's evidence: `gh run list --workflow
   "Daily Widget Refresh" --limit 5` including each run's event, id, status and conclusion. The
   push-triggered run has event `push`.

4. DISPATCH (GAP-25-02 clause 2). `gh workflow run "Daily Widget Refresh"`. Because
   `concurrency: group: widget-refresh` has `cancel-in-progress: false`, the dispatched run queues
   behind the push-triggered one; wait rather than re-dispatching. Identify the dispatched run by
   event `workflow_dispatch`, record its id, then follow it to completion and quote `gh run view
   <id>`'s status and conclusion.

5. EXTRACT THE COLLAPSED STEP'S LOG (GAP-25-02 clause 3). From `gh run view <id> --log`, isolate the
   "Compute all statistics" step's own output and quote it. Then assert, against the list enumerated
   from `src/compute-all-stats-steps.ts` and NOT read off the log, that a per-step line appears for
   each of: compute-stats, compute-advanced-stats, compute-geo-stats, compute-best-efforts,
   compute-age-grading, compute-dashboard-index, compute-gear-aggregate, compute-training-load — in
   that order — plus the "All statistics generated successfully!" line. Record whether a
   "DEGRADED STEPS" summary appeared; if it did, quote it and name every degraded step, but do not
   treat its absence as a failure (D-03 makes it conditional).

6. SCORE THE THREE ROWS against the conditions plan 25-09 drafted, replacing each `pending` Verdict
   cell with PASS / FAIL / BLOCKED / NOT EXERCISABLE:
     R6a maps to FIX-02 only. R6b maps to CI-02 only. R6c maps to CI-01 only.
   A row that is missing a required evidence item is BLOCKED, not PASS. Do not let one row's outcome
   influence another's — that separation is the entire purpose of the split. Do not edit any row's
   drafted conditions; if a row turns out unsatisfiable, HALT it and open a new numbered gap.

7. GAP DISPOSITION. If R6c PASSED, add a dated GAP-25-02 CLOSED note to the `## Gaps opened by
   Round 1` section citing the run id, the conclusion and the eight matched step names, and replace
   the `## CI-01 live run evidence — blocked` section's blocker note with a pointer to the new
   evidence (keeping the original text as the audit trail, in the style this file already uses for
   superseded records). If R6c did not pass, leave GAP-25-02 open and state precisely which of its
   four clauses is still unmet.

8. If the dispatched run FAILS, that is a real finding for this phase, exactly as plan 25-06's own
   instruction said. Record it verbatim, score R6c FAIL, and do NOT patch anything (house rule since
   plan 16-09). A failing nightly is information, not an emergency to fix mid-round.

Do not touch `REQUIREMENTS.md` or `ROADMAP.md` in this plan. Disposition is plan 25-12's job.
  </action>
  <verify>
    <automated>git show origin/master:.github/workflows/daily-refresh.yml | grep -c "compute-all-stats --ci"; gh run list --workflow "Daily Widget Refresh" --limit 5; git diff --stat .planning/REQUIREMENTS.md .planning/ROADMAP.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - If the push was authorised: `git show origin/master:.github/workflows/daily-refresh.yml | grep -c "compute-all-stats --ci"` returns 1, and the pushed `origin/master` sha is recorded.
    - If the dispatch was authorised: a `workflow_dispatch` run id and its conclusion are quoted from `gh run view`, and the push-triggered run is recorded separately as context rather than substituted for it.
    - The collapsed "Compute all statistics" step's log excerpt is quoted, and all eight step names from `COMPUTE_ALL_STATS_STEPS` are shown present in array order, with the enumeration attributed to `src/compute-all-stats-steps.ts`.
    - Whether a `DEGRADED STEPS` summary appeared is recorded either way, and its absence is not treated as a failure.
    - R6a, R6b and R6c each carry exactly one verdict from the four-value vocabulary, each naming exactly one requirement, and no row's verdict cites another row's evidence.
    - Every drafted row condition is unchanged from plan 25-09's commit; `git diff` evidence for that is quoted.
    - `git diff --stat .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns 0 lines: no requirement was ticked here.
    - If R6c passed, `## Gaps opened by Round 1` carries a dated GAP-25-02 CLOSED note citing the run id; if not, it states which clause remains unmet.
  </acceptance_criteria>
  <done>The authorised actions are executed and recorded, CI-01's live-run evidence either exists and is quoted or is explicitly still absent with the reason, and all three rows carry independent verdicts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local `master` -> `origin/master` -> the live Pages site and the `origin` data branch | A push crosses straight into production here: the push trigger starts a run, and neither the Commit nor the Deploy step is branch-guarded. This is the phase's genuine high-consequence action. |
| An agent's judgment -> a production-affecting command | GAP-25-02 clause 4 exists precisely because plan 25-06's executor was in a position to take this action unilaterally and correctly refused. |
| A CI run's log -> the claim "the collapsed step carries what the eight boxes did" | The log is the artefact under test; reading the step names off it and then asserting they are present would be circular. |
| The three split rows -> three separate requirement ticks | If one row's evidence is allowed to bleed into another's verdict, the split provides no isolation and reintroduces exactly R6's defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-36 | Elevation of privilege (unauthorised production action) | `git push origin master` and `gh workflow run` | mitigate | Task 2 is a blocking `checkpoint:decision` that states BOTH side effects before the developer chooses, and Task 3 executes only what was authorised. Task 1 aborts outright if `git rev-parse --show-toplevel` is a `.claude/worktrees/` path, satisfying GAP-25-02 clause 4. |
| T-25-37 | Repudiation (unverified premise) | GAP-25-02 clause 1 | mitigate | The `--ci` check reads the PUSHED copy via `git show origin/master:...`, not the local working copy. It returned 0 before the push and must return 1 after; the local copy already returns 1 and is therefore worthless as evidence. |
| T-25-38 | Tampering (circular log reading) | GAP-25-02 clause 3 | mitigate | The eight step names are enumerated from `src/compute-all-stats-steps.ts` and matched against the log, in that direction. The conditional `DEGRADED STEPS` summary is explicitly excluded from the pass condition so its absence cannot be read as a defect (D-03). |
| T-25-39 | Repudiation (cross-contaminated verdicts) | R6a / R6b / R6c | mitigate | Each row names exactly one requirement and is scored against its own drafted conditions only. The rows were drafted in plan 25-09 before any of this evidence existed, and Task 3 re-confirms with `git diff` that none was edited. |
| T-25-40 | Denial of service (self-inflicted) | The live Pages site | accept | The workflow's own blocking `npm test` and `npm run verify-dashboard` steps run on the CI runner before the deploy step, and Task 1's local five-command gate already passed on exactly the commit being pushed. Residual risk of a mid-flight production state on failure is accepted explicitly by the Task 2 authorisation, and a failing run is recorded as a finding rather than patched. |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | No installs. `25-RESEARCH.md`'s Package Legitimacy Audit records that this phase installs no new external packages, and `git diff --stat package.json package-lock.json` stays empty. The CI runner's own `npm ci` resolves the committed lockfile, unchanged by this phase. |

Not applicable, one line each:
- **V2 Authentication** — `gh` uses an already-established authenticated session; no credential is created, rotated or embedded by this plan.
- **V3 Session Management / V4 Access Control** — no session or access-control surface is touched; the workflow's `permissions` block is unchanged.
- **V5 Input Validation** — no new external input is parsed; the compute chain reads the same committed archive it always has.
- **V6 Cryptography** — none touched.
</threat_model>

<verification>
- The plan ran in the main checkout, not a worktree.
- The divergence with `origin/master` was resolved by merge, and the five-command gate ran green on the exact commit pushed, with the test tally reconciled.
- The developer's authorisation is recorded verbatim, with both production side effects stated before the decision.
- `git show origin/master:.github/workflows/daily-refresh.yml | grep -c "compute-all-stats --ci"` returns 1 after the push.
- A `workflow_dispatch` run id and conclusion are quoted, and the collapsed step's log carries all eight `COMPUTE_ALL_STATS_STEPS` names in order.
- R6a, R6b and R6c each carry an independent verdict, and `REQUIREMENTS.md` and `ROADMAP.md` are untouched.
</verification>

<success_criteria>
- CI-01's live-run evidence exists for the first time in this phase, or its absence is recorded with the developer's own decision as the reason rather than an agent's improvisation.
- FIX-02 and CI-02 are no longer hostage to CI-01's missing clause: R6a and R6b are scored on their own evidence, which is what GAP-25-02's split was for.
- No production-affecting command was run without explicit, informed authorisation naming both side effects.
</success_criteria>

<output>
Create `.planning/phases/25-ci-hardening-light-theme-verification/25-11-SUMMARY.md` when done.
It MUST include: `## Authorisation` (the developer's words verbatim, with timestamp),
`## Push and pushed-copy verification` (the merge, the sha, the grep count before and after),
`## Dispatched run` (id, event, conclusion, and the collapsed step log excerpt with the eight
matched names), `## Row verdicts` (R6a, R6b, R6c — one requirement each), and `## GAP-25-02
disposition` (closed with citations, or which clause remains unmet).
`requirements-completed` MUST be empty — plan 25-12 sets disposition under the all-rows-PASS rule.
</output>
