---
phase: 25-ci-hardening-light-theme-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analytics/dashboard-index.types.ts
  - src/analytics/gear-aggregate-logic.ts
  - src/analytics/gear-aggregate-logic.test.ts
autonomous: true
requirements: [FIX-02]

must_haves:
  decisions: [D-12, D-13]
  truths:
    - "D-13: `gearName` is optional on `DashboardIndexRow`, so `tsc` enumerates every consumer that assumed the key is always present"
    - "D-13: the `tsc --noEmit` triage is bounded — gear-aggregate plus trivially adjacent sites are fixed; any remainder is recorded as a todo in `.planning/todos/pending/` rather than growing the phase"
    - "D-12: a row whose `gearName` key is absent lands in the Unknown bucket instead of reaching `slugify(undefined)` and crashing"
    - "D-12: a row with `gearName: undefined`, `gearName: ''`, or a non-string `gearName` also lands in the Unknown bucket"
    - "D-12: `buildGearCoverage` uses the same widened predicate, so an absent/empty/non-string `gearName` is no longer silently counted as `runsWithGear`"
    - "Every new regression case was observed RED against the unfixed predicate before the fix landed"
  artifacts:
    - path: "src/analytics/dashboard-index.types.ts"
      provides: "`gearName?: string | null` on DashboardIndexRow (D-13)"
      contains: "gearName?"
    - path: "src/analytics/gear-aggregate-logic.ts"
      provides: "Widened Unknown-bucket predicate at both call sites (D-12)"
      contains: "typeof"
    - path: "src/analytics/gear-aggregate-logic.test.ts"
      provides: "Regression cases for absent-key / undefined / empty-string / non-string gearName across buildGearAggregate and buildGearCoverage"
  key_links:
    - from: "src/analytics/gear-aggregate-logic.ts"
      to: "src/analytics/dashboard-index.types.ts"
      via: "DashboardIndexRow import — the widened predicate and the optional field must agree"
      pattern: "DashboardIndexRow"
    - from: "src/analytics/gear-aggregate-logic.test.ts"
      to: "src/analytics/gear-naming.ts"
      via: "UNKNOWN_GEAR_LABEL assertion idiom already used at gear-aggregate-logic.test.ts:61-65"
      pattern: "UNKNOWN_GEAR_LABEL"
---

<objective>
Close FIX-02: make `gear-aggregate-logic.ts` degrade into the Unknown bucket instead of
crashing `slugify(undefined)` when an index row's `gearName` key is absent, fix the identical
silent mis-bucketing in `buildGearCoverage`, and make the row type honest so the compiler
enumerates every other consumer holding the same presence assumption.

Purpose: ROADMAP criterion 1. The rows are parsed from `index.json` at runtime, where the
required-key guarantee does not hold — which is exactly why this defect looked impossible.
Output: a widened predicate at both call sites, an optional `gearName` on the row type, and
four new regression shapes covered by tests that were observed RED first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-RESEARCH.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-PATTERNS.md

<interfaces>
<!-- Current contracts the executor works against. Do not re-derive by exploration. -->

src/analytics/dashboard-index.types.ts:72 (today):
  gearName: string | null;
  // JSDoc above it: "Resolved human gear label from data/config/gear.json, or the
  // deterministic 'Shoe N' ordinal, or null when the activity has no gear. NEVER the
  // raw gear id (17-D32/D33, D-17)."

src/analytics/gear-aggregate-logic.ts:145-148 (the crash site, buildGearAggregate):
  const label = row.gearName;
  const isUnknown = label === null;
  const mapKey = isUnknown ? unknownKey : (label as string);
  // then: newBucket(isUnknown ? UNKNOWN_GEAR_LABEL : (label as string), isUnknown)
  // and later: slugify(bucket.label) -> label.toLowerCase() throws on undefined

src/analytics/gear-aggregate-logic.ts:206-210 (the silent site, buildGearCoverage):
  const hasGear = row.gearName !== null;
  if (hasGear) {
    runsWithGear += 1;
    distinctLabels.add(row.gearName as string);
  }

src/analytics/gear-naming.ts:21:
  export const UNKNOWN_GEAR_LABEL = ... (the label asserted in existing tests)

Known consumers of `.gearName` outside test fixtures (exhaustive grep, RESEARCH Pattern 4):
  - src/analytics/gear-aggregate-logic.ts:146-148  -> fix target
  - src/analytics/gear-aggregate-logic.ts:207      -> fix target
  - src/analytics/compute-dashboard-index.ts:213-257 -> PRODUCER, always assigns the key, no change
  - scripts/verify-dashboard-publish.mjs:239-240   -> already optional-safe (`!== null && !== undefined`, `?? ''`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make gearName optional on DashboardIndexRow and run the bounded tsc triage (D-13)</name>
  <files>src/analytics/dashboard-index.types.ts</files>
  <read_first>
    - src/analytics/dashboard-index.types.ts (lines 55-80 — the DashboardIndexRow interface and the gearName JSDoc being amended)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-RESEARCH.md § "Pattern 4" (the D-13 blast-radius table)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md § D-13 (the bounded-triage instruction)
  </read_first>
  <action>
Change `gearName: string | null;` at `src/analytics/dashboard-index.types.ts:72` to
`gearName?: string | null;`. Extend the existing JSDoc on that field with one sentence
recording WHY it is optional: rows are re-parsed from `data/dashboard/index.json` at runtime,
where the required-key guarantee the producer (`compute-dashboard-index.ts`) provides does not
hold; per D-13 the type is made honest so the compiler enumerates presence assumptions.
Keep the existing "NEVER the raw gear id (17-D32/D33, D-17)" sentence intact.

Then run `npx tsc --noEmit` and enumerate every error it reports.

Bounded-triage rule (D-13, NON-NEGOTIABLE): fix only errors in
`src/analytics/gear-aggregate-logic.ts` (which Task 3 rewrites anyway) plus anything trivially
adjacent — a single-line narrowing or an existing `?? null`. If `tsc` surfaces more than a
handful of sites (more than 5 distinct files), do NOT fix the remainder in this phase: create
`.planning/todos/pending/2026-09-03-gearname-optional-consumer-overflow.md` recording each
remaining file:line and the error text verbatim, and state in the SUMMARY that the overflow was
deferred per D-13 rather than absorbed.

RESEARCH Pattern 4's exhaustive grep predicts ZERO overflow: the only non-test production sites
are the two in `gear-aggregate-logic.ts`, `compute-dashboard-index.ts` (a producer that always
assigns the key), and `verify-dashboard-publish.mjs` (already optional-safe). Record the actual
`tsc` output either way — the escape hatch is an instruction, not an assumption.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tee /tmp/25-01-tsc.txt; grep -c "error TS" /tmp/25-01-tsc.txt || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "gearName" src/analytics/dashboard-index.types.ts` shows `gearName?: string | null;` (the `?` is present).
    - The `tsc --noEmit` output produced by this task is quoted verbatim in the SUMMARY, including the case where it is empty.
    - Every `error TS` line reported by `tsc --noEmit` is accounted for in the SUMMARY as either (a) fixed here, (b) deferred to Task 3's `gear-aggregate-logic.ts` rewrite, or (c) recorded in a new `.planning/todos/pending/` file — no error is left unaccounted.
    - If a todo file was created, `ls .planning/todos/pending/` lists it and its body quotes each deferred `file:line` and error text.
  </acceptance_criteria>
  <done>`gearName` is optional on the row type, `tsc --noEmit` output is recorded, and every reported error has an explicit disposition under D-13's bound.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the four regression shapes and observe them RED against the unfixed predicate (D-12)</name>
  <files>src/analytics/gear-aggregate-logic.test.ts</files>
  <read_first>
    - src/analytics/gear-aggregate-logic.test.ts (whole file — especially the `makeRow` builder at :7-29, the `UNKNOWN_GEAR_LABEL` assertion idiom at :50-66, and the existing `describe('buildGearCoverage', ...)` block at :127-172)
    - src/analytics/gear-aggregate-logic.ts (lines 138-215 — both call sites still unfixed at this point)
    - src/analytics/gear-naming.ts (UNKNOWN_GEAR_LABEL)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-PATTERNS.md § 5 (the fixture-builder analysis: why no existing call can omit the key)
  </read_first>
  <behavior>
    buildGearAggregate — each case asserts the call does not throw AND the row lands in the
    bucket whose `label === UNKNOWN_GEAR_LABEL` and whose `key === 'unknown'`:
    - absent `gearName` key entirely (the shape a parsed `index.json` row can produce)
    - `gearName: undefined` (key present, value undefined)
    - `gearName: ''` (today slugifies to nothing and lands in the `'shoe'` fallback key)
    - `gearName: 123 as unknown as string` (malformed non-string shape)
    buildGearCoverage — for the same four shapes, `totals.runsWithGear` does NOT count the row
    (a single-row input yields `runsWithGear === 0`), and the row's label does not appear in
    the distinct-label count.
  </behavior>
  <action>
Do NOT touch `src/analytics/gear-aggregate-logic.ts` in this task. Only the test file changes.

`makeRow` (`:7-29`) hard-codes `gearName: null` as a default with a
`Partial<DashboardIndexRow> & { id: string }` override type, which is why no existing call can
construct the absent-key shape. With D-13 landed (Task 1), `Partial<DashboardIndexRow>` now
permits `gearName: undefined`, so the spread `{ gearName: null, ...overrides }` overwrites the
default with `undefined` when the key is explicitly present in `overrides`. For the true
absent-key shape, destructure-omit instead: build the row with `makeRow({ id: 'x' })`, then
`const { gearName: _omitted, ...rowWithoutGearName } = row;` and pass `rowWithoutGearName` as
the input. Alternatively add a small named helper `makeRowWithoutGearName(overrides)` in the
same file, next to `makeRow`, that performs the destructure-omit once — pick one and be
consistent; do not use `delete` on a typed object.

Add the eight cases (four shapes × `buildGearAggregate` / `buildGearCoverage`) using the file's
existing `it('...', () => { ... })` phrasing style. Reuse the existing
`unknown?.label === UNKNOWN_GEAR_LABEL` assertion idiom from `:61-65` rather than inventing a
new one. Name each case so the failure output identifies the shape, e.g.
`'absent gearName key lands in the Unknown bucket instead of crashing slugify (FIX-02, D-12)'`.

Then run the file and RECORD THE RED. Per this repo's D-11 precedent (a guard only counts once
observed RED), capture the exact failure output before any fix: expect the absent-key and
`undefined` cases to fail with
`TypeError: Cannot read properties of undefined (reading 'toLowerCase')`, the `''` case to fail
on landing in the `'shoe'` fallback key rather than Unknown, and the coverage cases to fail on
`runsWithGear` being 1 instead of 0. Quote the actual observed failure text per case in the
SUMMARY — do not paraphrase and do not assume the predicted messages.
  </action>
  <verify>
    <automated>npx vitest run src/analytics/gear-aggregate-logic.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analytics/gear-aggregate-logic.test.ts` exits NON-ZERO at the end of this task.
    - At least 8 new `it(...)` cases exist covering the four shapes across both `buildGearAggregate` and `buildGearCoverage`; `grep -c "D-12" src/analytics/gear-aggregate-logic.test.ts` is > 0.
    - The SUMMARY quotes, per failing case, the verbatim assertion/exception text vitest printed — including at least one case whose failure text contains `toLowerCase`.
    - `git diff --name-only` shows `src/analytics/gear-aggregate-logic.ts` is NOT modified by this task.
  </acceptance_criteria>
  <done>Eight new regression cases exist and were observed RED, with per-case failure output recorded verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Widen the Unknown-bucket predicate at both call sites (D-12)</name>
  <files>src/analytics/gear-aggregate-logic.ts</files>
  <read_first>
    - src/analytics/gear-aggregate-logic.ts (lines 30-60 for `slugify`, 138-230 for both call sites)
    - src/analytics/gear-aggregate-logic.test.ts (the cases from Task 2 that must go green)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md § D-12 (the exact predicate)
  </read_first>
  <action>
Site 1 — `buildGearAggregate`, `gear-aggregate-logic.ts:146-148`. Replace
`const isUnknown = label === null;` with the D-12 predicate
`const isUnknown = typeof label !== 'string' || label === '';`. Keep the surrounding
`mapKey`/`newBucket` structure; the two `(label as string)` casts are now guarded by the
predicate and can be simplified to `label` with a narrowing (`isUnknown ? unknownKey : label`)
if TypeScript's control-flow analysis permits — do not introduce a new cast that the predicate
already rules out.

Site 2 — `buildGearCoverage`, `gear-aggregate-logic.ts:207`. Replace
`const hasGear = row.gearName !== null;` with the negation of the same predicate:
`const hasGear = typeof row.gearName === 'string' && row.gearName !== '';`. The
`distinctLabels.add(row.gearName as string)` on the following line is then guarded and the cast
can be dropped. This site does not crash today — it silently mis-buckets an absent-key row as
"has gear" in the coverage totals. RESEARCH open question 2 recommends fixing it in the same
task; do so.

Add a short inline comment at Site 1 naming FIX-02 / D-12 and stating why the predicate is a
type-and-emptiness test rather than `=== null`: the rows are parsed from `index.json` at
runtime, where the required-key guarantee does not hold. Add a one-line cross-reference at
Site 2 pointing at Site 1 so a future reader recognises them as the same predicate applied
twice (same convention `curation-guard.mjs` uses for its two try/catch sites).

Do not change bucket ordering, slug assignment, `finalizeBucket`, or any other behaviour.
  </action>
  <verify>
    <automated>npx vitest run src/analytics/gear-aggregate-logic.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analytics/gear-aggregate-logic.test.ts` exits 0 with every Task 2 case passing.
    - `npx tsc --noEmit` exits 0.
    - `grep -n "typeof" src/analytics/gear-aggregate-logic.ts` returns at least two lines, one inside `buildGearAggregate` and one inside `buildGearCoverage`.
    - `grep -c "label === null" src/analytics/gear-aggregate-logic.ts` returns 0 and `grep -c "gearName !== null" src/analytics/gear-aggregate-logic.ts` returns 0.
    - `npx vitest run src/analytics` exits 0 (no sibling analytics test regressed).
  </acceptance_criteria>
  <done>Both call sites use the D-12 predicate, all Task 2 cases are green, and `tsc --noEmit` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/dashboard/index.json` → `buildGearAggregate` / `buildGearCoverage` | Rows are re-parsed from a generated JSON document at runtime. The producer's required-key guarantee does not survive serialization + re-parse, so the consumer must not assume presence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering (data integrity) | `gear-aggregate-logic.ts` Unknown-bucket predicate | mitigate | ASVS V5 Input Validation: replace the identity check `label === null` with the type-and-emptiness check `typeof label !== 'string' \|\| label === ''` at both call sites (D-12). This is the correct control shape — a type + emptiness guard, not a hand-rolled sanitizer. A malformed or partially-written `index.json` row currently crashes the compute step (`slugify(undefined)`) or silently inflates `runsWithGear`. |
| T-25-02 | Repudiation | `DashboardIndexRow` type contract | mitigate | Make `gearName` optional (D-13) so the compiler — not a runtime crash in CI — enumerates every consumer holding the presence assumption. Bounded per D-13: overflow becomes a todo, never silent absorption. |

Not applicable (one line each, per this phase's minimal security surface):
- **V2 Authentication** — no auth code touched.
- **V3 Session Management** — no sessions touched.
- **V4 Access Control** — no access-control surface; this is a pure in-process transform.
- **V6 Cryptography** — no cryptography touched.

**Supply chain:** no `npm install` in this plan (no new packages anywhere in Phase 25), so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `npx vitest run src/analytics/gear-aggregate-logic.test.ts` — exits 0.
- `npx tsc --noEmit` — exits 0.
- `npx vitest run src/analytics` — exits 0.
- The SUMMARY records: (a) the verbatim `tsc --noEmit` output from Task 1, (b) the verbatim RED failure text per case from Task 2, (c) the D-13 disposition (fixed vs. deferred-to-todo) for every reported error.
</verification>

<success_criteria>
- ROADMAP criterion 1 is satisfied in code: an index row whose `gearName` key is entirely absent degrades into the Unknown bucket instead of crashing `slugify(undefined)`, with a regression test for the missing-key case.
- `buildGearCoverage`'s parallel presence assumption is closed with the same predicate.
- `gearName` is optional on `DashboardIndexRow` and the `tsc` triage stayed inside D-13's bound.
</success_criteria>

<output>
Create `.planning/phases/25-ci-hardening-light-theme-verification/25-01-SUMMARY.md` when done.
It MUST include a section `## RED observations (D-11 precedent)` quoting the verbatim vitest
failure output per new case from Task 2, and a section `## D-13 tsc triage` quoting the verbatim
`tsc --noEmit` output and the disposition of every reported error.
</output>

