---
phase: 25-ci-hardening-light-theme-verification
plan: 10
type: execute
wave: 7
depends_on: ["25-09"]
files_modified:
  - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md
autonomous: false
requirements: [VER-01]
gap_closure: true
closes_gaps: [GAP-25-01]

must_haves:
  decisions: [D-04, D-05, D-07, D-08]
  truths:
    - "R7 is RUN exactly as plan 25-09 drafted it; the row text is not edited during or after the run for any reason, including to obtain a tick"
    - "GAP-25-01 clause 3: the captured frame is tied to the SAME navigation by arithmetic — frame timestamp minus performance.timeOrigin, compared against that navigation's own first-paint startTime — never by wall-clock proximity to the reload"
    - "GAP-25-01 clause 4: the evidence comes from a top-level navigation to https://bacilo.github.io/strava-widgets/, not from the 127.0.0.1 control used to prove the mechanism"
    - "D-05: the OS is in DARK appearance so that white is an unambiguous failure and rgb(26,26,46) an unambiguous pass"
    - "D-04 as amended: localStorage.getItem('dashboard-theme') is quoted at the instant of observation in the null-or-auto class; a quoted 'light' or 'dark' records the row BLOCKED"
    - "D-07: the developer performs the macOS appearance change by their own hand and gives the human judgment on the captured frame; osascript and DevTools rendering overrides are both rejected"
    - "The cache-trap value R7 is checked against is derived BEFORE the run from the deployed origin/gh-pages tree, so the comparison is independent rather than the page agreeing with itself"
    - "A frame that cannot be shown at or before first paint is recorded BLOCKED and a new numbered gap is opened; it is not re-scored against a later frame, and the ordering argument does not substitute for it"
    - "Nothing observed during the round is patched (house rule since plan 16-09)"
  artifacts:
    - path: ".planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md"
      provides: "R7 scored with quoted evidence, the capture arithmetic shown, the D-07 provenance split, and either a closure note for GAP-25-01 or a new numbered gap"
      contains: "R7"
  key_links:
    - from: "scripts/first-paint-capture.mjs"
      to: "https://bacilo.github.io/strava-widgets/"
      via: "a top-level CDP navigation from a fresh headful Chrome profile, with the frame timestamped against that navigation's timeOrigin"
      pattern: "first-paint"
    - from: "the captured document's hashed module script"
      to: "origin/gh-pages"
      via: "asset name derived from the deployed tree before the run, compared afterwards"
      pattern: "assets/index-"
---

<objective>
Run R7, the replacement first-paint row, exactly as plan 25-09 drafted it, and score it.

Purpose: R2 was BLOCKED because every captured frame landed ~243 ms after `first-paint`. Plan 25-08
measured and selected a mechanism that can beat the 612 ms bar and proved it can report white on a
broken bootstrap; plan 25-09 drafted R7 against that mechanism with a bidirectional reachability
proof. This plan is the run — the first time in this phase that ROADMAP criterion 4's first-paint
clause is actually observed rather than inferred.

Output: R7 scored in `25-VALIDATION.md` with quoted evidence, or BLOCKED with a new numbered gap.
No requirement is ticked here — disposition is plan 25-12's job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-08-SUMMARY.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-09-SUMMARY.md

<interfaces>
<!-- Established facts. Do NOT re-derive them, and do NOT redesign R7 in light of them. -->

WHAT R2 PRODUCED, and why R7 exists: `timeOrigin` `1788520780021.8`, `first-paint` at `+612 ms`,
frames saved at `1788520781489` and `1788520781492` — roughly `855`/`858 ms` after navigation start,
~`243 ms` too late. Every other instrumented value was clean (`'auto'` / `true` / `'dark'` /
`rgb(26, 26, 46)`). The failure was capture fidelity, nothing else.

THE DISCRIMINATOR (`src/dashboard/styles.css`): light `--bg: #ffffff` at :18, dark `--bg: #1a1a2e`
at :105 = `rgb(26, 26, 46)`. On dark OS a white first frame is an unambiguous failure and
`rgb(26, 26, 46)` an unambiguous pass. That is D-05, already disclosed in the Round 1 section.

WHY THE STORAGE QUOTE MATTERS (D-04 as amended): `theme.ts` reads the persisted mode BEFORE
consulting `prefers-color-scheme`, so a profile left on an explicit `'light'`/`'dark'` ignores the
OS entirely and looks identical to a passing row. `main.ts:29` writes `'auto'` on every load
(`persist` defaults to `true`, `theme.ts:118`), so `null` is not observable on a working page and
`null`-or-`'auto'` is the PASS-compatible class. The harness's throwaway `--user-data-dir` is the
cleared-storage mechanism; name it in the evidence.

FORBIDDEN (D-07 and the Round 1 provenance record): `osascript` appearance switching, and any CDP
`Emulation.setEmulatedMedia` or DevTools rendering override. The launched Chrome must inherit the
real OS setting, proven by `matchMedia('(prefers-color-scheme: dark)').matches` reading `true` from
inside the captured page.

NOT ACCEPTABLE AS A BASIS FOR PASS (GAP-25-01, by name): the ordering argument — inline head script
complete by `domInteractive` `287.9 ms`, `324 ms` before `first-paint` `612 ms`, plus plan 25-05's
`node:vm` parity pin. It is recorded in R2 already. Restating it does not close the gap.

ENVIRONMENT NOTE from Round 1: the Chrome window must be frontmost during capture. A backgrounded
tab made both live-follow rows initially read as false failures because Chrome defers style
recalculation and media-query dispatch for hidden tabs.

WHAT ROUND 1 ALREADY SETTLED: R1, R3, R4 and R5 all PASSED and are NOT re-run here. VER-01's tick
depends on R7 passing while those four stand.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm the drafted row is intact, derive the cache-trap comparison value before the run, and assemble the exact harness invocation</name>
  <files>.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</files>
  <read_first>
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (the "Round 2 Checkpoint (R6a, R6b, R6c, R7)" section — R7's drafted text, its evidence requirement and its reachability proof; and the "GAP-25-01 capture-mechanism reachability proof" section for the selected mechanism and its flags)
    - scripts/first-paint-capture.mjs (the flag names and the report shape)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-08-SUMMARY.md (the selected mechanism, its measured margin, and the exact throttle parameters if the throttled mechanism won)
  </read_first>
  <action>
Pre-flight only. Do not navigate to production and do not capture anything yet.

1. Confirm R7 exists in the Round 2 section with Verdict `pending` and that its text has not been
   modified since plan 25-09 committed it. Read `ROW_BASELINE_SHA` from `25-09-SUMMARY.md`'s
   `## Row baseline commit` section and run
   `git diff <ROW_BASELINE_SHA> -- .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md`.
   Record the sha and the diff output verbatim. R7's drafted discriminator, evidence requirement and
   both reachability paragraphs must appear nowhere as removed (`-`) or added (`+`) lines. A whole-file
   diff is expected to be non-empty by the time later tasks run — what must be empty is any change
   INSIDE R7's block. Do not substitute `git log`: a log line proves which commit touched the file,
   not that this row's bytes survived it.
   If R7 is missing (because plan 25-08 HALTED), stop: this plan has nothing to run, and that must be
   reported rather than worked around.

2. Derive the cache-trap comparison value BEFORE the run so the later comparison is independent
   rather than circular (Checkpoint Row Discipline rule 2): `git fetch origin gh-pages`, then read
   the deployed asset name with `git ls-tree -r --name-only origin/gh-pages` filtered to
   `assets/index-`, and record both that filename and `git rev-parse --short origin/gh-pages`.
   Round 1's R5 recorded `assets/index-D-Ts7X8C.js` at `origin/gh-pages` `b2b05a4`, deployed
   2026-09-04 09:10:40 +0000, built from source commit `623046363`; record whatever is current now
   and note whether it changed.

3. Assemble the exact harness command line R7 will run, with every flag spelled out: the selected
   mechanism from 25-08, the production URL `https://bacilo.github.io/strava-widgets/`, the output
   directory, and — if the throttled mechanism was selected — the exact `--throttle-ms` value 25-08
   recorded. Write the command line into the R7 block as the row's stated method, so the run cannot
   quietly differ from the draft.

4. Record the current appearance state from `defaults read -g AppleInterfaceStyle` as a pre-flight
   reading. Do not change it here; Task 2's checkpoint handles that.

Append these four items to R7's block under a heading such as "Pre-flight (Task 1)". Leave the
Verdict cell reading `pending`.
  </action>
  <verify>
    <automated>git fetch origin gh-pages >/dev/null 2>&1; git ls-tree -r --name-only origin/gh-pages | grep -c "assets/index-"; grep -c "Pre-flight (Task 1)" .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - R7's block contains a "Pre-flight (Task 1)" subsection quoting the deployed asset filename from `origin/gh-pages`, the `origin/gh-pages` short commit, and whether either changed since R5's recorded `assets/index-D-Ts7X8C.js` at `b2b05a4`.
    - The pre-flight records the exact harness command line, including the mechanism flag and, if applicable, the `--throttle-ms` value 25-08 selected.
    - The pre-flight records the pre-run `defaults read -g AppleInterfaceStyle` output.
    - R7's Verdict cell still reads `pending`, and `git diff <ROW_BASELINE_SHA> -- <the 25-VALIDATION.md path>` — with `ROW_BASELINE_SHA` quoted verbatim from `25-09-SUMMARY.md`'s `## Row baseline commit` — shows no `-` or `+` line inside R7's drafted discriminator, evidence requirement or reachability paragraphs.
    - If R7 does not exist, the task reports that and stops rather than drafting one.
  </acceptance_criteria>
  <done>The row is confirmed intact, the independent cache-trap value is on record before the run, and the exact invocation is written down.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run R7 with the developer — dark OS by hand, capture at or before first paint, human judgment on the frame</name>
  <files>.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</files>
  <read_first>
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (R7's drafted text in full, including its six evidence items and its CAN FAIL / CAN PASS paragraphs, plus Task 1's pre-flight block)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md (D-04 as amended, D-05, D-07, D-08)
  </read_first>
  <what-built>
    Plan 25-08 built `scripts/first-paint-capture.mjs` and measured, on this machine, a capture
    mechanism that produces a raster frame at or before the production page's own `first-paint`
    (612 ms from navigation start) — the bar the browser-extension screenshot round trip could not
    clear at its ~855 ms floor. It also proved the mechanism can report white: a copy of the same
    build with the inline pre-paint bootstrap stripped sampled `rgb(255, 255, 255)` at first paint,
    while the intact copy sampled `rgb(26, 26, 46)`. Plan 25-09 drafted R7 against that mechanism
    before anything was run.
  </what-built>
  <how-to-verify>
    Two things are asked of you, and they are the two D-07 reserves for a human.

    1. **Set macOS Appearance to Dark by your own hand** if `defaults read -g AppleInterfaceStyle`
       does not already print `Dark`. If it is already Dark, say so and the agent discloses that no
       flip was required, exactly as R1 disclosed the mirror-image case. `osascript` is not an
       acceptable substitute, and the agent is forbidden from faking the setting with a DevTools
       rendering override — that would make the whole row meaningless.

       Why Dark and not Light, restated so the choice is visible rather than buried: on a light OS,
       `--bg: #ffffff` (`styles.css:18`) means a white first paint IS the correct final state, so the
       row could not tell a working pre-paint theme from a broken one. On dark OS, white is an
       unambiguous failure and `#1a1a2e` an unambiguous pass. This is D-05, already disclosed in the
       Round 1 write-up as a deliberate deviation from criterion 4's literal wording.

    2. **Judge the captured frame.** The agent will show you the frame it captured at or before first
       paint, together with the arithmetic proving when it was taken relative to that navigation's
       own `first-paint`. Tell it what you see, in your own words. If the frame is white, that is a
       FAIL and it gets recorded as one — not softened, not patched.

    Everything else is the agent's: launching a fresh Chrome with a throwaway profile, navigating to
    `https://bacilo.github.io/strava-widgets/` (D-08), capturing and timestamping the frame, and
    quoting `localStorage.getItem('dashboard-theme')`, `matchMedia('(prefers-color-scheme:
    dark)').matches`, `data-theme`, the paint timings and the hashed asset name.

    Keep the Chrome window frontmost while the capture runs. Round 1 found that a backgrounded tab
    made both live-follow rows initially read as false failures, because Chrome defers style
    recalculation for hidden tabs.

    Verdict vocabulary: PASS / FAIL / BLOCKED / NOT EXERCISABLE. BLOCKED means the evidence obtained
    was not the evidence the row required — it is not a softer PASS. If no frame can be shown to be
    at or before first paint, the honest verdict is BLOCKED again, and the ordering argument (the
    inline script finishing at `domInteractive` 287.9 ms, plus 25-05's parity pin) does not rescue
    it. That was already offered and declined in Round 1.
  </how-to-verify>
  <action>
Run R7 exactly as drafted, using the command line recorded in Task 1's pre-flight block.

Ask the developer to set macOS Appearance to Dark by hand if it is not already Dark, and record
`defaults read -g AppleInterfaceStyle` before and after. Do not use `osascript`. Do not send any CDP
command that forces a colour scheme.

Then run the harness against `https://bacilo.github.io/strava-widgets/` and collect, for the row's
evidence block: the frame file path; the frame timestamp in ms; `performance.timeOrigin`; that
navigation's `first-paint` and `first-contentful-paint` startTimes; the computed arithmetic
`frame_timestamp - timeOrigin` and whether it is at or below the `first-paint` startTime; the
sampled frame colours; `localStorage.getItem('dashboard-theme')`;
`matchMedia('(prefers-color-scheme: dark)').matches`;
`document.documentElement.getAttribute('data-theme')`; the computed `body` background; the navigation
entry's `type`, `responseEnd`, `domInteractive` and `loadEventEnd`; the hashed module-script
filename; and a cache-busted refetch of `index.html` with SHA-256 and byte length for both copies.

Show the developer the captured frame and the arithmetic, and record their judgment VERBATIM. State
the granularity actually given rather than expanding a blanket answer into per-clause detail (plan
19-09 precedent).

Do not score the row in this task's own words if any required evidence item is missing — carry the
gap into Task 3, where it is recorded. Do not edit R7's drafted discriminator, evidence requirement
or reachability proof for any reason. Do not patch anything observed during the round (house rule
since plan 16-09); record it instead.
  </action>
  <verify>
    <automated>defaults read -g AppleInterfaceStyle; grep -c "frame_timestamp\|beats_first_paint" .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</automated>
    <human-check>The developer set Appearance to Dark by their own hand (or it was already Dark and that is disclosed), viewed the captured frame together with the arithmetic tying it to that navigation's first-paint, and gave a judgment recorded verbatim.</human-check>
  </verify>
  <acceptance_criteria>
    - The evidence block quotes `defaults read -g AppleInterfaceStyle` as `Dark`, and states whether a developer-performed flip was required.
    - The evidence block quotes `matchMedia('(prefers-color-scheme: dark)').matches` as `true` from inside the captured page, proving the OS setting reached the launched Chrome.
    - The evidence block quotes `localStorage.getItem('dashboard-theme')` at the instant of observation, and the value is `null` or `'auto'`; a quoted `'light'` or `'dark'` is recorded and the row is BLOCKED.
    - The evidence block shows the arithmetic explicitly: frame timestamp, `performance.timeOrigin`, the difference in ms, and that navigation's own `first-paint` startTime.
    - The captured frame's file path is quoted and the frame is retained for audit.
    - The developer's judgment on the frame is recorded verbatim.
    - No `osascript` was used and no DevTools rendering override was applied; the write-up states this.
    - Nothing was patched during the round.
  </acceptance_criteria>
  <done>The row has been run end to end, every evidence item is either quoted or explicitly recorded as absent, and the developer's judgment is captured verbatim.</done>
  <resume-signal>Tell the agent what you see in the captured frame, in your own words — or "blocked" if the arithmetic does not show the frame at or before first paint.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Score R7, record the D-07 provenance split, and either close GAP-25-01 or open its successor</name>
  <files>.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</files>
  <read_first>
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (Task 2's collected evidence, R7's drafted pass and fail conditions, GAP-25-01's four numbered clauses, and Round 1's "Execution provenance (D-07)" block as the format to mirror)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-09-SUMMARY.md (the drafted row as committed, to confirm nothing drifted)
  </read_first>
  <action>
Score R7 against its own drafted conditions and write the outcome into the Round 2 section. Replace
the `pending` Verdict cell with one of PASS / FAIL / BLOCKED / NOT EXERCISABLE.

Scoring rules, applied literally:
  - PASS only if every one of R7's drafted evidence items is present AND the sampled colour of a
    frame proven at or before that navigation's `first-paint` is `rgb(26, 26, 46)`.
  - FAIL if that frame samples white.
  - BLOCKED if no frame can be shown at or before first paint, or if any required quote is missing,
    or if `dashboard-theme` quoted `'light'`/`'dark'`. BLOCKED is not a softer PASS.

Then write, in R7's block:
  1. An EXECUTION PROVENANCE paragraph in the shape of Round 1's, naming what the developer did by
     their own hand (the appearance change or its disclosed absence, and the frame judgment) and what
     the agent did (profile creation, navigation, capture, every quoted read, the cache-busted
     refetch, the gh-pages comparison). State that `osascript` was not used and no rendering override
     was applied.
  2. The cache-trap comparison against Task 1's pre-derived value: the captured document's hashed
     asset name, the cache-busted refetch's SHA-256 and byte length versus the served document's, and
     whether the asset matches the `origin/gh-pages` name recorded before the run. If production was
     redeployed between Task 1 and the run, say so and record both names rather than quietly using
     the later one.
  3. Any disclosure the drafted row required — the D-05 cross-reference, and the slowed-load
     disclosure if the throttled mechanism was used, with its exact parameters.

If the verdict is PASS: add a short note to the `## Gaps opened by Round 1` section marking GAP-25-01
CLOSED, dated, citing R7 and the specific frame and arithmetic that closed it. Do NOT tick VER-01 and
do not touch `REQUIREMENTS.md` or `ROADMAP.md` — disposition is plan 25-12's job under the
all-rows-PASS rule.

If the verdict is FAIL or BLOCKED: leave GAP-25-01 open, and open a new numbered gap (GAP-25-03,
continuing the phase's numbering) in the same section, following the GAP-24-05 / GAP-25-01 shape —
state the state a future row must hold SIMULTANEOUSLY in order to discriminate, as numbered clauses,
rather than restating what failed. Name explicitly what was tried and did not work so a future round
does not retry it blindly. Do not propose the ordering argument as a route.

In both branches, record whether R7's drafted text was modified at any point after 25-09 committed it
(it must not have been), and quote the `git diff` evidence for that claim.
  </action>
  <verify>
    <automated>V=.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md; grep -c "pending" $V; grep -c "GAP-25-01" $V; git diff --stat .planning/REQUIREMENTS.md .planning/ROADMAP.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - R7's Verdict cell reads exactly one of PASS / FAIL / BLOCKED / NOT EXERCISABLE, and no Round 2 row that this plan did not run has been scored.
    - R7's block contains an EXECUTION PROVENANCE paragraph naming what the developer did by hand and what the agent did, and stating that `osascript` was not used and no rendering override was applied.
    - R7's block contains the cache-trap comparison: the captured hashed asset name, both SHA-256 values with byte lengths, and the `origin/gh-pages` asset name derived in Task 1 BEFORE the run.
    - If PASS: `## Gaps opened by Round 1` carries a dated GAP-25-01 CLOSED note citing R7, the frame path and the arithmetic; and `git diff --stat .planning/REQUIREMENTS.md .planning/ROADMAP.md` is empty because no requirement was ticked here.
    - If FAIL or BLOCKED: GAP-25-01 remains open, a new `### GAP-25-03` block exists stating as numbered clauses the state a future row must hold simultaneously, and it does not propose the ordering argument as a route.
    - The block records that R7's drafted text was unmodified since 25-09's commit, quoting `ROW_BASELINE_SHA` and the `git diff <ROW_BASELINE_SHA> -- <the 25-VALIDATION.md path>` output showing no `-` or `+` line inside R7's block. Re-run at the END of this task, after the verdict is written, so an edit made mid-task is caught.
  </acceptance_criteria>
  <done>R7 carries a scored verdict with quoted evidence and a provenance split, and GAP-25-01 is either explicitly closed by that evidence or explicitly still open with a successor gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macOS appearance setting -> the launched Chrome's `prefers-color-scheme` | If this path is severed (headless launch, `osascript`, a CDP media override) the row produces confident output that means nothing. |
| A captured raster frame -> the claim "this is what the user saw at first paint" | The entire row rests on this attribution. R2 failed here: the frames were real, the attribution was not. |
| GitHub Pages CDN -> the document the capture actually rendered | A stale cached `index.html` has produced false evidence in this repo before (T-25-21). |
| The drafted row -> the row as run | Any silent edit between draft and run destroys the discipline the split exists to honour. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-31 | Spoofing (simulated OS state) | R7's dark-OS precondition | mitigate | `defaults read -g AppleInterfaceStyle` quoted as `Dark` before and after, the change performed by the developer's own hand (D-07), and `matchMedia('(prefers-color-scheme: dark)').matches` quoted as `true` from inside the captured page. No `osascript`, no `Emulation.setEmulatedMedia`. |
| T-25-32 | Repudiation (false attribution of a frame) | R7's captured frame | mitigate | The row requires the arithmetic in the open: frame timestamp, `performance.timeOrigin`, their difference, and that navigation's own `first-paint` startTime. Wall-clock proximity to the reload is explicitly not acceptable (GAP-25-01 clause 3). A frame that cannot be shown at or before first paint is BLOCKED. |
| T-25-33 | Tampering (stale artifact) | The served `index.html` | mitigate | The throwaway `--user-data-dir` gives an empty HTTP cache by construction, and the row compares the captured document's hashed asset name against the `origin/gh-pages` value derived in Task 1 BEFORE the run, plus a byte-identical cache-busted refetch. Independently derived, per Checkpoint Row Discipline rule 2. |
| T-25-34 | Repudiation (row edited to yield a tick) | R7 as drafted versus as run | mitigate | Task 1 confirms R7 is byte-unchanged since 25-09's commit and Task 3 re-confirms it with `git diff` evidence. An unsatisfiable row is HALTED and gapped, never rewritten mid-round (Phase 24 R19/R26, and Round 1's own refusal to split R6). |
| T-25-35 | Information disclosure | CDP debugging port during the run | accept | Loopback-bound ephemeral port on a throwaway profile with no credentials and no site data, torn down with the process. Same posture already accepted for `npm run curate`'s localhost server. |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | No installs. The harness is Node built-ins only and `git diff --stat package.json package-lock.json` must stay empty, as plan 25-08 established. |

Not applicable, one line each:
- **V2 Authentication / V3 Session Management / V4 Access Control** — the dashboard is a public static site with no auth, session or access-control surface.
- **V5 Input Validation** — the `'light' | 'dark' | 'auto'` allow-list (T-16-TH-01) is pinned by plan 25-05 and is not re-litigated by this row.
- **V6 Cryptography** — SHA-256 is used here only as a byte-identity check on a public document, not as a security control.
</threat_model>

<verification>
- R7 carries a scored verdict from the four-value vocabulary, with every drafted evidence item either quoted or explicitly recorded as absent.
- The frame's attribution to first paint is shown as arithmetic against that navigation's own `timeOrigin` and `first-paint`, not asserted.
- The D-07 provenance split is disclosed, and the write-up states that `osascript` and rendering overrides were not used.
- GAP-25-01 is either marked CLOSED with a citation, or left open with a numbered successor gap.
- `REQUIREMENTS.md` and `ROADMAP.md` are untouched by this plan.
</verification>

<success_criteria>
- ROADMAP criterion 4's first-paint clause is observed rather than inferred, on a genuinely dark OS, against the production-served build, with the capture tied to its own navigation by arithmetic.
- Whatever the outcome, it is recorded honestly: a white frame is a FAIL, an unattributable frame is BLOCKED, and neither is softened by the ordering argument that Round 1 already declined.
- No requirement is ticked here, and no drafted row was edited to make one tickable.
</success_criteria>

<output>
Create `.planning/phases/25-ci-hardening-light-theme-verification/25-10-SUMMARY.md` when done.
It MUST include: `## R7 verdict` (the four-value verdict and the one-line reason), `## Capture
arithmetic` (frame timestamp, timeOrigin, difference, first-paint startTime), `## Evidence
provenance` (developer's hand versus agent, per D-07), `## Cache-trap exclusion` (hashed asset,
both SHA-256 values, the pre-derived gh-pages name), `## Disclosures` (D-05 cross-reference, and the
slowed-load disclosure if used), and `## Gap disposition` (GAP-25-01 closed, or still open with its
successor). `requirements-completed` MUST be empty — plan 25-12 sets disposition.
</output>
