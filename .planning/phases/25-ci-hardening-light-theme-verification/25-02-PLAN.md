---
phase: 25-ci-hardening-light-theme-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/compute-all-stats-steps.ts
  - src/compute-all-stats-steps.test.ts
  - src/index.ts
  - .github/workflows/daily-refresh.yml
autonomous: true
requirements: [CI-01]

must_haves:
  decisions: [D-01, D-02, D-03]
  truths:
    - "D-01: the compute-step ordering is declared exactly once, in code, as an exported ordered data structure that both the CLI walker and the workflow's single invocation consume"
    - "D-01: the nightly workflow's eight compute steps are collapsed into one `compute-all-stats` invocation, and no ordering of the chain survives anywhere in YAML"
    - "D-01: the six deleted `::warning::` messages and the gitignored-output explanations survive the collapse — each is carried forward onto the step table, and `::warning::` annotations still reach the Actions run summary"
    - "D-02: `compute-all-stats` is fail-fast by default (a hand-run aborts loudly on any step); the explicit `--ci` flag is what buys warn-and-continue"
    - "D-02: the `--ci` flag flips tolerated-step disposition only — mandatory steps stay fail-fast with or without it"
    - "D-03: `compute-stats` and `compute-advanced-stats` are mandatory; geo, best-efforts, age-grading, dashboard-index, gear-aggregate and training-load are tolerated"
    - "D-03: an end-of-run failure summary names every degraded step, so a nightly that quietly degraded three steps is visible at a glance rather than buried mid-log"
    - "The pre-existing drift (workflow ran dashboard-index before age-grading; the code ran age-grading first) is closed in favour of the code order, and the reason is recorded in the file that now owns it"
  artifacts:
    - path: "src/compute-all-stats-steps.ts"
      provides: "COMPUTE_ALL_STATS_STEPS (the single ordering + disposition declaration) and the pure walker runComputeAllStatsSteps"
      exports: ["COMPUTE_ALL_STATS_STEPS", "runComputeAllStatsSteps", "ComputeStep"]
      min_lines: 80
    - path: "src/compute-all-stats-steps.test.ts"
      provides: "Data-only assertions on the step table plus walker-disposition assertions for the --ci flag"
      contains: "COMPUTE_ALL_STATS_STEPS"
    - path: ".github/workflows/daily-refresh.yml"
      provides: "One collapsed compute step invoking `node dist/index.js compute-all-stats --ci`"
      contains: "compute-all-stats --ci"
  key_links:
    - from: "src/index.ts"
      to: "src/compute-all-stats-steps.ts"
      via: "computeAllStatsCommand imports and walks the exported table instead of inlining the chain"
      pattern: "compute-all-stats-steps"
    - from: ".github/workflows/daily-refresh.yml"
      to: "src/compute-all-stats-steps.ts"
      via: "the single `compute-all-stats --ci` invocation is the workflow's only knowledge of the chain"
      pattern: "compute-all-stats --ci"
    - from: "src/compute-all-stats-steps.test.ts"
      to: "src/compute-all-stats-steps.ts"
      via: "imports ONLY the constant and the walker — never src/index.ts, which calls main() and process.exit at module load"
      pattern: "from './compute-all-stats-steps.js'"
---

<objective>
Close CI-01: collapse the nightly workflow's eight hand-maintained compute steps onto a single
`compute-all-stats --ci` invocation, and make an exported, testable step table in code the sole
declaration of both the chain's order and each step's failure disposition.

Purpose: ROADMAP criterion 2. The drift is not hypothetical — `daily-refresh.yml` runs
`compute-dashboard-index` before `compute-age-grading` while `computeAllStatsCommand` runs
`compute-age-grading` first, and nothing has ever noticed.
Output: `src/compute-all-stats-steps.ts` (table + pure walker), its first-ever unit test, a
rewritten `computeAllStatsCommand`, and a workflow with twelve fewer YAML steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-RESEARCH.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-PATTERNS.md

<interfaces>
<!-- Contracts and blocking facts the executor needs. Do not re-derive by exploration. -->

BLOCKING FACT — why the step table cannot live in `src/index.ts`:
  `src/index.ts:1` is `import 'dotenv/config';` and `src/index.ts:683` is a top-level
  `main().catch(...)` call. Importing `src/index.ts` from a test would execute the whole CLI
  dispatch and hit `process.exit()`. The table therefore lives in a NEW sibling module,
  `src/compute-all-stats-steps.ts`, which the test imports directly.

SAFE to import from the new module at top level:
  `import { config } from './config/strava.config.js'` — every field is a lazy getter
  (`src/config/strava.config.ts`), and `activitiesDir` / `streamsDir` / `streamsManifestPath`
  never throw on missing env. Only `clientId`/`clientSecret`/`refreshToken` throw, and the
  compute steps do not read them.

The eight steps, in the ORDER THE CODE ALREADY DOCUMENTS as load-bearing
(`src/index.ts:329-340`, "Chain ordering below is load-bearing"). THIS ORDER WINS:
  1. compute-stats            MANDATORY   computeAllStats({ activitiesDir: config.activitiesDir, statsDir: 'data/stats' })
  2. compute-advanced-stats   MANDATORY   computeAdvancedStats({ activitiesDir: config.activitiesDir, statsDir: 'data/stats' })
  3. compute-geo-stats        tolerated   computeGeoStats({ activitiesDir: config.activitiesDir, geoDir: 'data/geo' })
  4. compute-best-efforts     tolerated   computeBestEfforts({ activitiesDir, streamsDir, streamsManifestPath, statsDir: 'data/stats' })
  5. compute-age-grading      tolerated   computeAgeGrading({ statsDir: 'data/stats', wmaDir: 'data/wma' })
  6. compute-dashboard-index  tolerated   computeDashboardIndex({ activitiesDir, streamsManifestPath, statsDir: 'data/stats', geoDir: 'data/geo', outDir: 'data/dashboard' })
  7. compute-gear-aggregate   tolerated   computeGearAggregate({ indexPath: 'data/dashboard/index.json', outDir: 'data/stats' })
  8. compute-training-load    tolerated   computeTrainingLoad({ activitiesDir, streamsDir, streamsManifestPath, statsDir: 'data/stats' })

The six `::warning::` messages currently living in `daily-refresh.yml`'s "Warn on X failure"
steps, which must survive the collapse VERBATIM:
  compute-geo-stats        -> "Geocoding failed, widgets will use cached geo data"
  compute-best-efforts     -> "Best-effort computation failed, records data will be stale"
  compute-dashboard-index  -> "Dashboard index computation failed, the dashboard will serve a stale index"
  compute-age-grading      -> "Age-grading computation failed, age-grade data will be stale"
  compute-gear-aggregate   -> "Gear aggregate computation failed, gear data will be stale"
  compute-training-load    -> "Training load computation failed, training load data will be stale"

Existing dynamic-import call sites in `computeAllStatsCommand` (keep this style):
  const { computeGeoStats } = await import('./geo/compute-geo-stats.js');
  const { computeBestEfforts } = await import('./analytics/compute-best-efforts.js');
  const { computeAgeGrading } = await import('./analytics/compute-age-grading.js');
  const { computeDashboardIndex } = await import('./analytics/compute-dashboard-index.js');
  const { computeGearAggregate } = await import('./analytics/compute-gear-aggregate.js');
  const { computeTrainingLoad } = await import('./analytics/compute-training-load.js');
  (`computeAllStats` and `computeAdvancedStats` are STATIC imports in src/index.ts today —
   from './analytics/compute-stats.js' and './analytics/compute-advanced-stats.js')

Test-analog contract (`src/dashboard/view-registry.test.ts`, the exact template per PATTERNS § 1):
  data-only assertions over an exported ordered array; `typeof view.mount === 'function'` is
  asserted but `mount()` is NEVER invoked. The new test must likewise never invoke `run`.

printHelp sites to update (`src/index.ts`):
  :603  console.log('  compute-all-stats      - Compute all statistics (basic, advanced, geo, best efforts, age-grading, dashboard index, gear aggregate, training load)');
  :621  console.log('  npm run compute-all-stats      # Generate all stats');
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the step table and the pure walker (D-01, D-02, D-03)</name>
  <files>src/compute-all-stats-steps.ts</files>
  <read_first>
    - src/index.ts (lines 1-15 for the static imports and `const command = process.argv[2]`; lines 289-392 for the whole `computeAllStatsCommand` body including the "Chain ordering below is load-bearing" comment block at 329-340; lines 680-692 for the top-level `main().catch(...)`)
    - src/config/strava.config.ts (confirm every field is a lazy getter before importing it at module top level)
    - src/dashboard/view-registry.ts (the exported-ordered-config-array analog PATTERNS.md § 1 nominates — imitate its "one flat array, one entry per unit" convention and its header comment style)
    - .github/workflows/daily-refresh.yml (lines 88-170 — the twelve steps being collapsed, their `::warning::` texts, and the inline comments explaining gitignored outputs)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-PATTERNS.md § 1 and § 6
  </read_first>
  <action>
Create `src/compute-all-stats-steps.ts`. It must contain no `process.exit`, no `process.argv`
read, and no top-level side effect other than importing `config`.

Export an interface `ComputeStep` with exactly these fields:
  - `name: string` — the CLI subcommand name, e.g. `'compute-geo-stats'`
  - `mandatory: boolean` — D-03's split
  - `warning: string | null` — the `::warning::` message body for a tolerated step; `null` for
    the two mandatory steps (they abort, so they never warn)
  - `run: () => Promise<void>` — the step's work

Export `const COMPUTE_ALL_STATS_STEPS: readonly ComputeStep[]` with the eight entries in the
order given in `<interfaces>` above, using the exact option objects listed there and the exact
verbatim `::warning::` message bodies listed there. Every `run` uses `await import(...)` inside
the closure — including `computeAllStats` and `computeAdvancedStats`, which are static imports
in `src/index.ts` today — so that importing this module in a test pulls in no compute code.

Carry the load-bearing reasoning forward (D-01's requirement that the collapsed step's own
documentation replaces what the workflow step names used to carry). Move — do not merely copy —
the substance of `src/index.ts:329-340`'s numbered "Chain ordering below is load-bearing" block
into this module's header docblock, expanded to cover all eight steps rather than only 5-8, and
add per-entry inline comments carrying the dependency reason for each step
(age-grading reads best-efforts.json; dashboard-index reads best-efforts.json + gear.json and is
the prerequisite for gear-aggregate; training-load reads the stream manifest and is independent,
which is why it runs last). Also fold in the substance of the YAML comments being deleted: that
`data/stats/` and `data/dashboard/` are gitignored and regenerated every run and are therefore
deliberately absent from the commit step's `file_pattern`, and that `compute-age-grading` reports
`enabled: false` in CI by design because `data/private/athlete-private.json` must not exist there
— that is the expected CI state, not a failure, and must not be "fixed" by committing the private
file.

Record the drift resolution explicitly in that docblock: the workflow ran
`compute-dashboard-index` before `compute-age-grading` while this chain ran age-grading first;
the code order wins because this file's own numbered dependency chain is the declaration D-01
makes authoritative, and neither step depends on the other's output (both only need
`best-efforts.json`), so the change is behaviour-neutral.

Export the walker:
  `export interface DegradedStep { name: string; message: string }`
  `export async function runComputeAllStatsSteps(steps: readonly ComputeStep[], options: { continueOnError: boolean; log?: (line: string) => void }): Promise<DegradedStep[]>`

Walker contract (this IS D-02 and D-03 in executable form):
  - Iterate `steps` in array order, awaiting each `run()`.
  - On a MANDATORY step's rejection: rethrow immediately, regardless of `continueOnError`.
  - On a TOLERATED step's rejection when `continueOnError` is false: rethrow immediately
    (fail-fast is the default so a hand-run aborts loudly — D-02).
  - On a TOLERATED step's rejection when `continueOnError` is true: emit
    `::warning::<step.warning>` via `log` (defaulting to `console.log`), push
    `{ name, message }` where `message` is the caught error's `message`, and continue.
  - Return the accumulated `DegradedStep[]`.
  - `log` exists so the test can capture emitted lines without spying on `console`.

Do not add flag parsing here — `process.argv` belongs to the CLI wrapper in `src/index.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "import('./src/compute-all-stats-steps.ts').catch(()=>{})" 2>/dev/null; grep -c "::warning::" src/compute-all-stats-steps.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -c "process.exit\|process.argv" src/compute-all-stats-steps.ts` returns 0.
    - `grep -o "name: '[a-z-]*'" src/compute-all-stats-steps.ts` lists exactly these eight, in this order: `compute-stats`, `compute-advanced-stats`, `compute-geo-stats`, `compute-best-efforts`, `compute-age-grading`, `compute-dashboard-index`, `compute-gear-aggregate`, `compute-training-load`.
    - All six verbatim `::warning::` message bodies from `<interfaces>` appear in the file: `grep -c "will use cached geo data\|records data will be stale\|serve a stale index\|age-grade data will be stale\|gear data will be stale\|training load data will be stale" src/compute-all-stats-steps.ts` returns 6.
    - `grep -v '^ *[*/]' src/compute-all-stats-steps.ts | grep -c "await import(" ` returns 8 (every `run` uses a dynamic import).
    - The header docblock contains the word `drift` and names both `compute-dashboard-index` and `compute-age-grading`, recording which order wins and why.
    - `grep -c "athlete-private.json" src/compute-all-stats-steps.ts` returns at least 1 (the deleted YAML comment's substance survived).
  </acceptance_criteria>
  <done>`src/compute-all-stats-steps.ts` exports the eight-entry table, the pure walker, and the carried-forward chain-ordering documentation; `tsc` is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Unit-test the step table and the --ci disposition (Wave 0 gap)</name>
  <files>src/compute-all-stats-steps.test.ts</files>
  <read_first>
    - src/compute-all-stats-steps.ts (the module written in Task 1)
    - src/dashboard/view-registry.test.ts (the exact data-only assertion template, quoted in PATTERNS.md § 1 — imitate its docblock convention and its "never invoke the function field" rule)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (the three CI-01 rows this file discharges)
  </read_first>
  <action>
Create `src/compute-all-stats-steps.test.ts`. It imports ONLY from
`./compute-all-stats-steps.js` — never from `./index.js`, which calls `main()` and
`process.exit` at module load. State that constraint in the file's docblock, following
`view-registry.test.ts`'s convention of explaining in a docblock why the test is data-only.

`describe('COMPUTE_ALL_STATS_STEPS', ...)` — data-only, `run` is NEVER invoked:
  - the eight step names equal the documented order exactly, as an ordered array compare
    (no `.sort()` — order is the thing under test):
    `['compute-stats','compute-advanced-stats','compute-geo-stats','compute-best-efforts','compute-age-grading','compute-dashboard-index','compute-gear-aggregate','compute-training-load']`
  - no duplicate step names (`new Set(names).size === names.length`)
  - D-03's split: exactly `compute-stats` and `compute-advanced-stats` have `mandatory === true`;
    the other six have `mandatory === false`
  - every tolerated step has a non-empty string `warning`; both mandatory steps have
    `warning === null`
  - every entry has `typeof step.run === 'function'` (asserted, never called — same as
    `view-registry.test.ts` asserts `typeof view.mount === 'function'`)
  - the six tolerated `warning` strings match the verbatim messages the deleted
    `daily-refresh.yml` "Warn on X failure" steps carried, so the collapse cannot silently drop
    the domain knowledge D-01 named as its accepted cost

`describe('runComputeAllStatsSteps', ...)` — walker disposition, driven by locally-constructed
FAKE step arrays (never `COMPUTE_ALL_STATS_STEPS`, whose `run` would trigger the real pipeline):
  - `continueOnError: false` (the default hand-run posture, D-02): a rejecting TOLERATED step
    causes the returned promise to reject, and steps after it do not run (assert via a
    `ran: string[]` recorder pushed by each fake `run`)
  - `continueOnError: true`: a rejecting TOLERATED step does NOT reject; subsequent steps still
    run; the returned array contains exactly that step's name; and the captured `log` lines
    include `::warning::` followed by that step's `warning` text
  - `continueOnError: true`: a rejecting MANDATORY step STILL rejects and halts the walk — the
    flag must not soften mandatory steps (this is the assertion that pins D-02's boundary)
  - all-green case: returns an empty array and emits no `::warning::` line
  </action>
  <verify>
    <automated>npx vitest run src/compute-all-stats-steps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/compute-all-stats-steps.test.ts` exits 0.
    - `grep -c "from './index.js'\|from './index'" src/compute-all-stats-steps.test.ts` returns 0.
    - The file contains no call to `.run()` on any member of `COMPUTE_ALL_STATS_STEPS`: `grep -n "COMPUTE_ALL_STATS_STEPS" src/compute-all-stats-steps.test.ts` shows only `.map`, `.filter`, `.find`, `.length` or index access — never invocation.
    - A test named for the mandatory-step boundary exists and passes: running the walker with `continueOnError: true` over a fake array whose mandatory step rejects produces a rejected promise.
    - Temporarily reordering two entries in `COMPUTE_ALL_STATS_STEPS` makes this file fail; record that observation (the mutation and the failure text) in the SUMMARY, then restore the file and confirm `git diff --exit-code src/compute-all-stats-steps.ts` is clean.
  </acceptance_criteria>
  <done>The step table and the walker have their first-ever unit coverage, and the order assertion has been observed failing under a deliberate reorder.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite computeAllStatsCommand onto the table and collapse the workflow (D-01, D-02, D-03)</name>
  <files>src/index.ts, .github/workflows/daily-refresh.yml</files>
  <read_first>
    - src/index.ts (lines 289-392 — the body being replaced; lines 595-630 — printHelp's compute-all-stats lines at ~603 and ~621; lines 655-665 — the `case 'compute-all-stats'` dispatch)
    - src/compute-all-stats-steps.ts (Task 1's module — the table and walker signature)
    - .github/workflows/daily-refresh.yml (whole file — the twelve steps at 94-167, the mandatory "Process statistics" step at 91-92, the `paths` trigger, the header comment explaining the push trigger, and the blocking `npm test` / `npm run verify-dashboard` gates)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-PATTERNS.md § 6 (the exact step shapes being deleted and the four things the replacement must preserve)
  </read_first>
  <action>
**src/index.ts.** Replace `computeAllStatsCommand`'s body (lines 289-392) with a thin wrapper:
  - read the flag as `const continueOnError = process.argv.includes('--ci');` — matching the
    file's existing primitive `process.argv` style (there is no flag-parsing library in this
    repo and none is to be added); `--ci` is the chosen spelling for D-02
  - keep the existing opening `console.log('Computing all statistics from synced activities...\n')`
  - call `runComputeAllStatsSteps(COMPUTE_ALL_STATS_STEPS, { continueOnError })`, logging each
    step's name before it runs so the single collapsed Actions step's output carries the
    per-step boundary the eight green/red boxes used to provide (D-01's accepted cost)
  - D-03's end-of-run failure summary: if the returned `DegradedStep[]` is non-empty, print a
    clearly delimited block listing every degraded step name and its error message, and exit
    non-zero-free (`process.exit(0)`, matching today's success path — the whole point of `--ci`
    is that the job continues) — but the summary must be unmissable, printed AFTER the final
    success line, not buried mid-log
  - if the array is empty, keep today's `console.log('\nAll statistics generated successfully!')`
  - keep the existing `catch` shape: log `Compute all stats error: <message>`, keep the
    `error.code === 'ENOENT' && message.includes('activities')` hint about `npm run sync`,
    `process.exit(1)`
  - remove the now-duplicated `computeAllStats` / `computeAdvancedStats` static imports at
    `src/index.ts:7-8` ONLY if no other command in the file still uses them; check first — the
    standalone `compute-stats` / `compute-advanced-stats` commands almost certainly do, in which
    case leave them
  - `printHelp()`: extend the `compute-all-stats` description line (~:603) with
    `[--ci]` and a short clause ("--ci: warn and continue on tolerated steps, print a failure
    summary; default is fail-fast"), and add one example line beside :621 showing
    `node dist/index.js compute-all-stats --ci   # CI mode: tolerate optional steps`

**.github/workflows/daily-refresh.yml.** Delete the "Process statistics" step (91-92) AND all
twelve steps from "Compute geographic statistics" through "Warn on training-load failure"
(94-167), including their inline comments — the comments' substance now lives in
`src/compute-all-stats-steps.ts`'s docblock (Task 1). Replace all of it with exactly one step:

  - name: Compute all statistics
    run: node dist/index.js compute-all-stats --ci

Add a short YAML comment above that step stating that the chain's order and each step's
mandatory/tolerated disposition are declared in `src/compute-all-stats-steps.ts` (CI-01, D-01)
and must not be re-expressed here, and that `--ci` is what buys warn-and-continue (D-02).

DO NOT TOUCH, in the same file: the `paths` trigger list, the header comment explaining why the
push trigger exists, the `workflow_dispatch` trigger, the fetch step and its "Warn on fetch
failure" pair, the `Compile TypeScript` step, `Build widgets`, or the blocking `npm test` /
`npm run verify-dashboard` gate pair and its Phase 16 WR-06 comment.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npx vitest run src/compute-all-stats-steps.test.ts && test "$(grep -c 'Warn on .* failure' .github/workflows/daily-refresh.yml)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0.
    - `npx vitest run src/compute-all-stats-steps.test.ts` still exits 0.
    - `grep -c 'compute-all-stats --ci' .github/workflows/daily-refresh.yml` returns exactly 1.
    - `grep -c 'Warn on .* failure' .github/workflows/daily-refresh.yml` returns exactly 1 (only the surviving "Warn on fetch failure" step, which is out of this phase's scope).
    - `grep -c 'continue-on-error' .github/workflows/daily-refresh.yml` returns exactly 2 (the fetch step and the data-commit step) — the six tolerated compute steps' `continue-on-error` lines are gone.
    - `grep -c 'dist/index.js compute-' .github/workflows/daily-refresh.yml` returns exactly 1 — no individual compute subcommand is named in YAML any more, so no ordering of the chain survives outside `src/compute-all-stats-steps.ts`.
    - These four are still present and byte-unchanged in the workflow: the `paths:` trigger list, the `workflow_dispatch:` trigger, `run: npm test`, and `run: npm run verify-dashboard`. Prove with `git diff .github/workflows/daily-refresh.yml` showing no `-` line touching any of them.
    - `grep -n 'process.argv.includes' src/index.ts` shows the `--ci` read inside `computeAllStatsCommand`.
    - `node dist/index.js compute-all-stats --help` is not a thing; instead `node dist/index.js help` output contains both `compute-all-stats` and `--ci`.
    - Dry disposition proof, recorded in the SUMMARY: run `node dist/index.js compute-all-stats` (no flag) in an environment where a tolerated step fails, or simulate it, and show the run aborted; then show the `--ci` run continuing and printing the end-of-run failure summary. If no natural failure is reachable locally, state that plainly and defer the live proof to plan 25-06's `gh workflow run` evidence rather than fabricating one.
  </acceptance_criteria>
  <done>`computeAllStatsCommand` walks the table, `--ci` selects warn-and-continue, the end-of-run summary names degraded steps, and the workflow names the chain exactly once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → `node dist/index.js compute-all-stats --ci` | The workflow hands control to one command that now owns the entire compute chain's failure policy. A silently-degraded run must remain visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Repudiation | Collapsed compute step's logging | mitigate | D-01's accepted cost is that eight green/red boxes become one, so the command must carry the evidence: per-step name logged before each run, `::warning::<verbatim message>` per degraded tolerated step, and a D-03 end-of-run summary naming every degraded step after the final line. Without this, a nightly that degraded three steps is indistinguishable from a clean one — the same "green gate hides a real failure" class recorded three times in this repo's Verification Note. |
| T-25-04 | Tampering (config drift) | Two hand-maintained orderings of one chain | mitigate | Delete the ordering from YAML entirely (`grep -c 'dist/index.js compute-'` must be 1) so there is no second declaration to drift. Pin the surviving declaration with `src/compute-all-stats-steps.test.ts`'s ordered-array compare. |
| T-25-05 | Elevation-of-privilege-adjacent (failure-policy widening) | `--ci` flag | mitigate | The flag must flip tolerated-step disposition ONLY. A test asserts a rejecting mandatory step still rejects with `continueOnError: true`, so the flag can never silently widen into "ignore everything". |

Not applicable (one line each):
- **V2 Authentication** — no auth code touched; the workflow's `INTERVALS_API_KEY` secret handling is in the untouched fetch step.
- **V3 Session Management** — no sessions.
- **V4 Access Control** — no access-control surface; no new workflow permissions.
- **V5 Input Validation** — the only new input is a boolean CLI flag with no parsed value.
- **V6 Cryptography** — none touched.

**Supply chain:** no `npm install` in this plan; no new package is introduced anywhere in Phase 25, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npm run build` exits 0.
- `npx vitest run src/compute-all-stats-steps.test.ts` exits 0.
- `grep -c 'dist/index.js compute-' .github/workflows/daily-refresh.yml` returns 1.
- `git diff .github/workflows/daily-refresh.yml` shows no deletion touching the `paths` trigger, the push-trigger header comment, `workflow_dispatch`, `npm test`, or `npm run verify-dashboard`.
- The SUMMARY records the deliberate-reorder mutation from Task 2 and its failure text, and the drift-resolution statement (code order wins, and why).
</verification>

<success_criteria>
- ROADMAP criterion 2 is satisfied: the nightly workflow and `compute-all-stats` share a single source of truth for compute-step ordering, and there is no second hand-maintained ordering left anywhere.
- The six `::warning::` messages and the gitignored-output/age-grading-disabled explanations survived the collapse.
- `compute-all-stats` is fail-fast by default; `--ci` is what buys warn-and-continue, and it does not soften mandatory steps.
</success_criteria>

<output>
Create `.planning/phases/25-ci-hardening-light-theme-verification/25-02-SUMMARY.md` when done.
It MUST include: `## Drift resolution` (which order won and why), `## Order-pin mutation proof`
(Task 2's deliberate reorder and the resulting failure text), and `## Collapse audit` (the four
things PATTERNS.md § 6 requires the replacement to preserve, each with the grep or diff line
proving it survived).
</output>
