---
phase: 25-ci-hardening-light-theme-verification
plan: 06
type: execute
wave: 3
depends_on: ["25-01", "25-02", "25-03", "25-04", "25-05"]
files_modified:
  - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md
autonomous: true
requirements: [CI-01, CI-02, FIX-02]

must_haves:
  decisions: [D-01, D-03, D-11]
  truths:
    - "The five-command gate is green on the fully-integrated tree: npm test, npx tsc --noEmit, npm run build, npm run build-widgets, npm run verify-dashboard"
    - "The full-suite tally is compared against the pre-planning baseline of 60 files / 1560 tests, and every delta is accounted for by a named new test file"
    - "D-01/D-03: a REAL nightly run, triggered via `gh workflow run`, shows the single collapsed compute step's log carrying the per-step names and the end-of-run failure summary that the eight green/red boxes used to provide"
    - "D-11: every RED observation recorded in plans 25-03, 25-04 and 25-05's SUMMARYs is transcribed into 25-VALIDATION.md, so the phase's validation contract carries the evidence rather than scattering it"
    - "25-VALIDATION.md's Per-Task Verification Map has every row's Status resolved to green or red on quoted command output, with no row left pending on narration"
  artifacts:
    - path: ".planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md"
      provides: "Filled Per-Task Verification Map statuses, the D-11 RED observation log, and the gh workflow run evidence"
      contains: "D-11 RED Observation Log"
  key_links:
    - from: ".github/workflows/daily-refresh.yml"
      to: "the Actions run log"
      via: "gh run view --log on the workflow_dispatch run, reading the 'Compute all statistics' step's output"
      pattern: "Compute all statistics"
    - from: "25-VALIDATION.md"
      to: "25-03-SUMMARY.md / 25-04-SUMMARY.md / 25-05-SUMMARY.md"
      via: "transcription of each plan's recorded RED/mutation observations"
      pattern: "25-0[345]-SUMMARY"
---

<objective>
Prove the four wave-1 plans integrate, and gather the automated half of ROADMAP criterion 5:
a green `verify-dashboard-publish.mjs` run and a real nightly workflow execution.

Purpose: this repo has shipped defects behind a green gate three times, and Phase 24's Wave-7
retrospective found a cross-plan defect (a test reading the real `dist/widgets` while another
planted fixtures in it) that no single plan could see alone. The gate runs on the merged tree,
not per plan. The `gh workflow run` half is the only evidence that D-01's accepted cost — eight
green/red boxes becoming one — was actually paid for by the command's own logging.
Output: a filled-in `25-VALIDATION.md` carrying the gate results, the transcribed D-11 evidence,
and the workflow-run output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-01-SUMMARY.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-02-SUMMARY.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-03-SUMMARY.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-04-SUMMARY.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-05-SUMMARY.md

<interfaces>
<!-- Baselines and tool facts. Do not re-derive by exploration. -->

Pre-planning baseline, measured green 2026-09-03 immediately before planning:
  npm test -> 60 files passed (60) / 1560 tests passed (1560), duration 6.91s

Expected new test files from wave 1 (each adds to the file count):
  src/compute-all-stats-steps.test.ts        (plan 25-02)
  src/dashboard/theme-bootstrap-parity.test.ts (plan 25-05)
  (25-01 and 25-04 extend EXISTING files, so they add tests but not files)

vitest.config.ts: environment 'node', fileParallelism: false. Never use watch mode — always
`vitest run` / `npm test`.

gh CLI: 2.86.0, confirmed installed and on PATH. `.github/workflows/daily-refresh.yml` already
carries a `workflow_dispatch:` trigger (confirmed present), so `gh workflow run` produces a REAL
on-demand run — `act` is not installed and is not needed.

The workflow's job also runs `npm test` and `npm run verify-dashboard` as BLOCKING gates after
`build-widgets`, and ends with a data-commit step and a Pages deploy. A dispatched run therefore
exercises the whole pipeline, including the collapsed compute step.

CI auto-commit hazard (recorded in project memory and STATE.md): the nightly run commits data to
origin/master via git-auto-commit-action. A run triggered from this plan can land a commit ahead
of local. If a later push is rejected non-fast-forward, MERGE — do not rebase (the established
resolution, chosen to avoid rewriting phase commits for auto-generated data).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the five-command gate on the integrated tree and reconcile the test tally</name>
  <files>.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</files>
  <read_first>
    - .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md (the Per-Task Verification Map, the Sampling Rate section's per-wave command list, and the Validation Sign-Off checklist)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-01-SUMMARY.md, 25-02-SUMMARY.md, 25-03-SUMMARY.md, 25-04-SUMMARY.md, 25-05-SUMMARY.md (the per-plan results being reconciled)
    - .planning/STATE.md § "Wave 7 result (2026-09-02)" (the cross-plan defect precedent: a test reading the real dist/widgets while another planted fixtures in it — the class of defect this gate exists to catch)
  </read_first>
  <action>
Run all five commands on the merged tree, in this order, capturing full output for each:
  1. `npm test`
  2. `npx tsc --noEmit`
  3. `npm run build`
  4. `npm run build-widgets`
  5. `npm run verify-dashboard`

Reconcile `npm test`'s tally against the pre-planning baseline of 60 files / 1560 tests. The
file count must have increased by exactly the number of NEW test files wave 1 created (expected
2: `src/compute-all-stats-steps.test.ts` and `src/dashboard/theme-bootstrap-parity.test.ts` —
confirm against the actual SUMMARYs, since plan 25-05 chose the filename). Attribute the test
count delta to specific plans. An unexplained delta in either direction is a finding, not
rounding: report it rather than absorbing it.

Run `npm test` THREE consecutive times and confirm the same tally each time. `fileParallelism`
is already `false` (commit `e01ea8c`, added precisely because 24-11's whole-tree read raced
24-05's fixture planting in the real `dist/widgets`), but plans 25-03 and 25-04 both plant and
remove fixtures in that same real tree, so re-confirm determinism rather than assuming the
earlier fix covers the new fixtures. If any run differs, HALT and report the flake with both
tallies — do not average or retry until green.

Then update `25-VALIDATION.md`:
  - In the Per-Task Verification Map, set every row's `Status` to ✅ or ❌ based on quoted
    command output. The manual VER-01 row (D-04/D-05/D-07/D-08) stays ⬜ pending — plan 25-07
    owns it. Do NOT mark it green from automated evidence.
  - Add a new section `## D-11 RED Observation Log` transcribing, verbatim, the RED evidence
    from `25-03-SUMMARY.md` (six CI-02 assertions), `25-04-SUMMARY.md` (the WR-19 fixture's
    uncaught EACCES and the planted-directory build-widgets exit codes), `25-05-SUMMARY.md`
    (the three index.html mutations) and `25-01-SUMMARY.md` (the gear-aggregate RED cases).
    Transcribe the quoted output; do not paraphrase into prose.
  - Add a new section `## Wave 1 Integration Gate` with the five commands, their exit codes,
    the three `npm test` tallies, and the tally reconciliation against the 60/1560 baseline.
  - Tick the Validation Sign-Off items that this task's evidence discharges. Leave
    `nyquist_compliant` and the D-05-disclosure item for plan 25-07.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run build && npm run build-widgets && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - All five commands exit 0; each exit code is quoted in `25-VALIDATION.md` § "Wave 1 Integration Gate".
    - Three consecutive `npm test` runs report an identical `Test Files` / `Tests` tally; all three tallies are quoted.
    - The tally reconciliation names the file-count delta and attributes the test-count delta to specific plans; any unexplained delta is recorded as a finding.
    - `npm run verify-dashboard`'s "N check(s) passed" total is quoted and is higher than the 40/40 recorded at the end of Phase 24.
    - Every row in `25-VALIDATION.md`'s Per-Task Verification Map has a non-⬜ Status except the manual VER-01 (D-04/D-05/D-07/D-08) row.
    - `grep -c "D-11 RED Observation Log" .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md` returns 1, and that section contains at least six quoted non-zero exit lines (one per CI-02 assertion).
    - `git status --porcelain data` is empty — the gate touched no archive file.
  </acceptance_criteria>
  <done>The five-command gate is green and deterministic on the integrated tree, and 25-VALIDATION.md carries the results plus the transcribed D-11 evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Trigger a real nightly run and prove the collapsed step's log carries what the eight step names did (D-01, D-03, criterion 5)</name>
  <files>.planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md</files>
  <read_first>
    - .github/workflows/daily-refresh.yml (post-collapse — the single "Compute all statistics" step, the surviving `workflow_dispatch` trigger, and the blocking `npm test` / `npm run verify-dashboard` steps)
    - src/compute-all-stats-steps.ts (the eight step names and the six `::warning::` messages the log must be able to emit)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-02-SUMMARY.md § "Collapse audit"
    - .planning/STATE.md § "Blockers/Concerns" (the 2026-08-11 non-fast-forward precedent: the nightly's own data commit landing ahead of local; merge, never rebase)
  </read_first>
  <action>
Push the wave-1 work to `origin/master` first if it is not already there — `workflow_dispatch`
runs the workflow as it exists on the branch, so an unpushed collapse would dispatch the OLD
eight-step workflow and produce worthless evidence. Confirm the pushed
`.github/workflows/daily-refresh.yml` on origin contains `compute-all-stats --ci` before
dispatching (e.g. `git show origin/master:.github/workflows/daily-refresh.yml | grep -n
"compute-all-stats"`). If the push is rejected non-fast-forward because the nightly's own
auto-commit landed first, resolve with `git merge origin/master` — never rebase — per this
repo's established resolution.

Dispatch and follow the run:
  `gh workflow run "Daily Widget Refresh"`
  `gh run list --workflow "Daily Widget Refresh" --limit 3`   (capture the new run id)
  `gh run watch <run-id>`  or poll `gh run view <run-id>`
  `gh run view <run-id> --log > /tmp/25-06-run.log`

From that log, extract and record in `25-VALIDATION.md` § "CI-01 live run evidence":
  - the run id, its URL, and its conclusion
  - the job's step list, showing ONE compute step named `Compute all statistics` where there
    used to be twelve (the six compute steps plus their six "Warn on X failure" pairs), and
    showing that `Fetch new activities`, `Compile TypeScript`, `Build widgets`, `npm test` and
    `npm run verify-dashboard` all still appear
  - the full stdout of the `Compute all statistics` step, quoted, and an explicit check that all
    EIGHT step names appear in it in the documented order — this is D-01's accepted cost being
    paid: the step names that used to be eight green/red boxes now have to be in the log
  - whether any `::warning::` line was emitted, and if so which step and with which verbatim
    message; and whether the D-03 end-of-run failure summary block appeared
  - if the run was fully clean (no degraded step), state that plainly and record that the
    degraded-path logging is therefore proven only by plan 25-02's unit tests, not by this run.
    Do NOT manufacture a failure to force the summary to appear, and do NOT claim the summary
    was observed when it was not.

If the run FAILS, that is a real finding for this phase: record the failing step and its output
verbatim in `25-VALIDATION.md`, do not retry-until-green, and report it in the SUMMARY as a gap
for `/gsd-plan-phase 25 --gaps` rather than patching under gate pressure (the house rule since
plan 16-09).

Finally, note in the SUMMARY whether the run's data-commit step landed a commit on
origin/master, and whether the local tree needs a merge before any further push.
  </action>
  <verify>
    <automated>gh run list --workflow "Daily Widget Refresh" --limit 1 --json databaseId,conclusion,event | tee /tmp/25-06-run.json</automated>
  </verify>
  <acceptance_criteria>
    - `git show origin/master:.github/workflows/daily-refresh.yml | grep -c "compute-all-stats --ci"` returns 1 BEFORE the dispatch; the command and its output are quoted.
    - A run whose `event` is `workflow_dispatch` appears in `gh run list --workflow "Daily Widget Refresh"`; its id, URL and conclusion are quoted in `25-VALIDATION.md`.
    - The quoted step list contains exactly one step named `Compute all statistics` and zero steps matching `Warn on (geocoding|best-effort|dashboard-index|age-grading|gear-aggregate|training-load) failure`.
    - The quoted step list still contains `Compile TypeScript`, `Build widgets`, a step running `npm test`, and a step running `npm run verify-dashboard`.
    - All eight step names — `compute-stats`, `compute-advanced-stats`, `compute-geo-stats`, `compute-best-efforts`, `compute-age-grading`, `compute-dashboard-index`, `compute-gear-aggregate`, `compute-training-load` — appear in the quoted `Compute all statistics` step output, in that order. Show the grep or the ordered extraction.
    - The `::warning::` / end-of-run-summary observation is recorded as either "observed, with this verbatim text" or "not exercised — the run was clean; degraded-path logging is covered by 25-02's unit tests only". No third option.
    - `grep -c "CI-01 live run evidence" .planning/phases/25-ci-hardening-light-theme-verification/25-VALIDATION.md` returns 1.
  </acceptance_criteria>
  <done>A real dispatched nightly run is recorded with its collapsed compute step's full log, proving the eight step names survived D-01's collapse into one Actions box.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local merged tree → `origin/master` → GitHub Actions runner → GitHub Pages | A dispatched run publishes. Everything it commits and deploys crosses out of the developer's control. |
| the phase's automated gate → the phase's disposition | A green gate that does not actually cover the changed behaviour is how this repo shipped three prior defects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-15 | Repudiation | Cross-plan integration | mitigate | The five-command gate runs on the MERGED tree, not per plan, because Phase 24's Wave-7 found a defect (24-11's whole-tree read racing 24-05's fixture planting in the real `dist/widgets`) that no single plan could see. Plans 25-03 and 25-04 both plant fixtures in that same real tree, so `npm test` is run three consecutive times and the tally must be identical. |
| T-25-16 | Tampering (stale evidence) | `gh workflow run` dispatch | mitigate | `workflow_dispatch` runs the workflow AS IT EXISTS ON THE BRANCH. Dispatching before pushing the collapse would exercise the old eight-step workflow and produce evidence for the wrong code. Gated by an acceptance criterion that greps `origin/master`'s copy for `compute-all-stats --ci` before dispatching. Same class as this repo's documented stale-cached-`index.html` trap. |
| T-25-17 | Repudiation | Reporting a failed or clean run | mitigate | If the dispatched run fails, it is recorded verbatim as a gap and NOT retried-until-green (house rule since plan 16-09). If it is clean, the plan must say the degraded-path logging was not exercised rather than implying it was. A manufactured failure to make the D-03 summary appear is explicitly forbidden. |
| T-25-18 | Tampering (history) | Push races with the nightly's own auto-commit | mitigate | The nightly commits data to `origin/master`. On a non-fast-forward rejection, resolve with `git merge origin/master` — never rebase, which would rewrite phase commits for auto-generated data. This is the resolution already established on 2026-08-11. |

Not applicable (one line each):
- **V2 Authentication** — the workflow's `INTERVALS_API_KEY` secret handling is in the untouched fetch step; no auth code changes in this phase.
- **V3 Session Management** — no sessions.
- **V4 Access Control** — no workflow permission changes; `workflow_dispatch` already existed.
- **V5 Input Validation** — no new parsed input in this plan.
- **V6 Cryptography** — none touched.

**Supply chain:** no `npm install` in this plan; `npm ci` inside the dispatched workflow installs from the unchanged committed lockfile (`git diff package-lock.json` is empty across the whole phase), so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `npm test`, `npx tsc --noEmit`, `npm run build`, `npm run build-widgets`, `npm run verify-dashboard` all exit 0 on the merged tree, with three identical consecutive `npm test` tallies.
- A `workflow_dispatch` run of "Daily Widget Refresh" exists, against an `origin/master` whose workflow contains `compute-all-stats --ci`, and its `Compute all statistics` step log contains all eight step names in order.
- `25-VALIDATION.md` carries § "Wave 1 Integration Gate", § "D-11 RED Observation Log" and § "CI-01 live run evidence", with every Per-Task Verification Map row resolved except the manual VER-01 row.
</verification>

<success_criteria>
- The automated half of ROADMAP criterion 5 is discharged: items 1-3 are confirmed by a green `verify-dashboard-publish.mjs` run and a real nightly workflow execution.
- The evidence for D-11 across plans 25-01/03/04/05 is consolidated in the phase's validation contract rather than scattered across SUMMARYs.
- No requirement is ticked and no ROADMAP gate is closed by this plan — plan 25-07 owns disposition.
</success_criteria>

<output>
Create `.planning/phases/25-ci-hardening-light-theme-verification/25-06-SUMMARY.md` when done.
`requirements-completed` MUST be empty: the automated evidence is necessary but not sufficient,
because ROADMAP criterion 5 makes item 4 a human checkpoint, and this milestone's Verification
Note requires every phase to end with a human browser checkpoint. Plan 25-07 sets disposition.
</output>
