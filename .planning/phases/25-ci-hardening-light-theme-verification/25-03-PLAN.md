---
phase: 25-ci-hardening-light-theme-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-dashboard-publish.mjs
  - dist/widgets  # gitignored build tree; Task 2 mutates and restores it (D-11 RED cycle)
autonomous: true
requirements: [CI-02]

must_haves:
  decisions: [D-09, D-10, D-11]
  truths:
    - "D-09: each of the six documents returns 200, parses as JSON, and satisfies one structural invariant a truncated or empty file would fail — never a bare expect200"
    - "D-09: the four array-shaped documents are rejected when they parse as an empty array, which a bare 200 plus JSON.parse would accept"
    - "D-10: the per-activity best-effort shard sample is derived at runtime from indexDoc.activities, with no pinned activity ids, following the newestRow/newestWithStream convention at verify-dashboard-publish.mjs:203-205"
    - "D-10: the shard check implicitly cross-checks that the index and the shard directory agree about what exists — a sampled id present in the index but absent on disk fails the gate"
    - "D-11: every one of the six new assertions has been observed RED once, with verify-dashboard exiting non-zero and naming that specific document, then restored to green"
    - "The six new assertions are written in the file's existing expect200/expect404/ok/fail helpers — no new HTTP or fetch machinery"
  artifacts:
    - path: "scripts/verify-dashboard-publish.mjs"
      provides: "By-name reachability plus structural-invariant assertions for weekly-distance, monthly-stats, yearly-stats, year-over-year, best-efforts.json and a runtime-derived shard sample"
      contains: "year-over-year.json"
  key_links:
    - from: "scripts/verify-dashboard-publish.mjs"
      to: "dist/widgets/data/stats/best-efforts/{id}.json"
      via: "runtime-derived shard sample GET, ids taken from the already-parsed indexDoc.activities"
      pattern: "best-efforts/\\$\\{"
    - from: "scripts/verify-dashboard-publish.mjs"
      to: "scripts/build-widgets.mjs"
      via: "the { src: 'data/stats', dest: 'dist/widgets/data/stats' } whole-directory copy these assertions stop trusting implicitly"
      pattern: "data/stats"
---

<objective>
Close CI-02: make `verify-dashboard-publish.mjs` assert reachability BY NAME, plus one cheap
structural invariant, for the six published stats documents it currently relies on a
whole-directory copy to carry — then observe every one of those assertions fail.

Purpose: ROADMAP criterion 3. This repo's own Verification Note records three occasions of
shipping a rendering or data defect behind a green automated gate. A publish verifier that
passes on a truncated or empty document is the same "verifier lies" failure class.
Output: six new assertion blocks in the file's existing style, and a recorded RED observation
per assertion (D-11).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-RESEARCH.md
@.planning/phases/25-ci-hardening-light-theme-verification/25-PATTERNS.md

<interfaces>
<!-- Verified on-disk shapes and existing helpers. Do not re-derive by exploration. -->

Existing helpers in scripts/verify-dashboard-publish.mjs (lines 168-189):
  async function expect200(baseUrl, path, { nonEmpty = true } = {})  -> returns body string or null
  async function expect404(baseUrl, path, reason)                    -> void
  function ok(message) / function fail(message)                      -> counters behind
                                                                        "N check(s) passed, M failure(s)"

Existing runtime sample derivation (lines 202-205, inside main(), already parsed):
  const indexDoc = JSON.parse(readFileSync(INDEX_JSON, 'utf8'));   // dist/widgets/data/dashboard/index.json
  const newestRow = indexDoc.activities[0];
  const newestWithStream = indexDoc.activities.find((row) => row.streams?.available === true);
  const newestWithoutStream = indexDoc.activities.find((row) => row.streams?.available === false);

Existing assertion template to clone (the training-load.json block, lines 329-356):
  const body = await expect200(baseUrl, '/data/stats/<doc>.json');
  if (body) {
    const parsed = JSON.parse(body);
    if (<bad shape>) { fail('<path> <what was wrong, with the actual value interpolated>'); }
    else { ok('<path> parses with <the invariant, stated positively>'); }
  }

VERIFIED on-disk shapes (parsed from the real generated files, 2026-09-03 — not assumed):
  weekly-distance.json  ARRAY, 489 entries. item keys:
      weekStartISO, totalKm, runCount, avgPaceMinPerKm, elevationGain, totalMovingTimeMin
  monthly-stats.json    ARRAY, 125 entries. item keys:
      periodStart, periodLabel, totalKm, runCount, avgPaceMinPerKm, elevationGain, totalMovingTimeMin
  yearly-stats.json     ARRAY, 14 entries. same item keys as monthly-stats.
  year-over-year.json   ARRAY, exactly 12 entries (one per calendar month, always pre-filled per
                        compute-advanced-stats.ts:104). item keys: month, monthLabel, years
                        (years is an object keyed by year -> { totalKm, totalRuns, totalHours })
  best-efforts.json     OBJECT. keys: schemaVersion(=1), generatedAt, note, totals, rankings,
                        rejected, activities. activities is an object with 1859 keys;
                        rankings is an object with 7 keys.
  best-efforts/<id>.json OBJECT. keys: activityId (STRING, e.g. "i182358139"), startDate,
                        distanceSource, efforts (non-empty array), excludedFromRecords.
  NONE of the four array documents has a schemaVersion field.

VERIFIED shard/index correspondence (2026-09-03): 1884 index rows, 1859 shard files. The 25
index rows with NO shard are EXACTLY the 25 rows with `streams.available === false` (set
equality confirmed). Therefore the shard sample MUST be drawn only from rows with
`streams.available === true`.

Server mount: ROOT = resolve(process.cwd(), 'dist/widgets'); there is no env override, so the
D-11 cycle mutates the real (gitignored) dist/widgets tree and restores it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the six by-name assertions with per-document structural invariants (D-09, D-10)</name>
  <files>scripts/verify-dashboard-publish.mjs</files>
  <read_first>
    - scripts/verify-dashboard-publish.mjs (whole file — especially the helper trio at 168-189, the index parse and sample derivation at 190-210, the "4b. Phase 18 published artifacts" section at 320-410 which is where the new blocks belong, and the counters/exit at the file's tail)
    - scripts/build-widgets.mjs (line ~141, the `{ src: 'data/stats', dest: 'dist/widgets/data/stats' }` whole-directory copy these assertions stop trusting)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-PATTERNS.md § "Shared Patterns" A and B (the verbatim helper trio and the runtime-sample idiom)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-RESEARCH.md § "Pattern 3" (the per-document invariant table) and § "Pitfall 5"
  </read_first>
  <action>
Add a new commented section to `main()`, immediately after the existing "4b. Phase 18 published
artifacts" block, headed `--- 4c. CI-02: every published stats document, by name ---` with a
short rationale comment: the whole-directory `data/stats` copy has been trusted implicitly to
carry these; a dropped or renamed compute step would 404 silently in production while every
local check stayed green.

Write all six in the existing `expect200` -> `JSON.parse` -> `fail(...)`/`ok(...)` shape. Do NOT
introduce any new HTTP, fetch, or assertion machinery. Every `fail` message must interpolate the
ACTUAL observed value (the existing blocks all do this), and every `fail`/`ok` message must
start with the document's URL path so a failure names the document (this is what D-11's RED
observation will assert on).

The six assertions and their exact invariants (D-09 — a bare `expect200` is explicitly
insufficient; an empty `[]` or `{}` parses fine and must be rejected):

1. `/data/stats/weekly-distance.json`
   - `Array.isArray(parsed)` and `parsed.length > 0`
   - `typeof parsed[0].weekStartISO === 'string' && parsed[0].weekStartISO.length > 0`
   - `Number.isFinite(parsed[0].totalKm)`

2. `/data/stats/monthly-stats.json`
   - `Array.isArray(parsed)` and `parsed.length > 0`
   - `typeof parsed[0].periodLabel === 'string' && parsed[0].periodLabel.length > 0`
   - `Number.isFinite(parsed[0].totalKm)`

3. `/data/stats/yearly-stats.json`
   - `Array.isArray(parsed)` and `parsed.length > 0`
   - `typeof parsed[0].periodStart === 'string' && parsed[0].periodStart.length > 0`
   - `Number.isFinite(parsed[0].totalKm)`

4. `/data/stats/year-over-year.json`
   - `Array.isArray(parsed)` and `parsed.length === 12` EXACTLY — the document is always
     pre-filled with one entry per calendar month, so the fixed length is itself a strong
     truncation detector. Add an inline comment saying so and citing
     `compute-advanced-stats.ts:104`, so a future reader does not "relax" it to `> 0`.
   - `parsed[0].years` is a non-null object

5. `/data/stats/best-efforts.json`
   - `parsed.schemaVersion === 1`
   - `parsed.activities` is a non-null object with `Object.keys(parsed.activities).length > 0`
   - `parsed.rankings` is a non-null object with `Object.keys(parsed.rankings).length > 0`

6. Per-activity shards, sample derived AT RUNTIME (D-10 — no pinned ids ever):
   - Build the candidate pool as
     `const shardCandidates = indexDoc.activities.filter((row) => row.streams?.available === true);`
     This filter is load-bearing, not cosmetic: the 25 index rows without a shard on disk are
     exactly the 25 stream-unavailable rows (verified 2026-09-03). Add an inline comment saying
     so, so the filter is not later removed as redundant.
   - Sample THREE ids spanning the archive: `shardCandidates[0]`,
     `shardCandidates[Math.floor(shardCandidates.length / 2)]`, and
     `shardCandidates[shardCandidates.length - 1]`. De-duplicate so a tiny archive does not
     double-check the same id. Three rather than one because the failure this catches is a
     partial or truncated directory copy, which a newest-only sample would miss.
   - If `shardCandidates.length === 0`, `fail(...)` naming the shard path family — do not
     silently skip. (Contrast with the existing `newestWithoutStream` block, which legitimately
     prints "(skipped: ...)": here an empty pool means the index itself is broken.)
   - For each sampled id: `await expect200(baseUrl, \`/data/stats/best-efforts/${id}.json\`)`,
     then assert `String(parsed.activityId) === String(id)` (the cross-check that the index and
     the shards agree about what exists — D-10's stated implicit benefit) and
     `Array.isArray(parsed.efforts) && parsed.efforts.length > 0`.

Do not change any existing assertion, the server, the mount prefix, or the exit logic.
  </action>
  <verify>
    <automated>npm run build-widgets >/dev/null && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify-dashboard` exits 0.
    - Its "N check(s) passed" total is HIGHER than the pre-change baseline; the SUMMARY quotes both the before and after totals (the last recorded baseline is 40/40 from Phase 24 round 4 — re-measure rather than assuming).
    - Each of these six strings appears at least once in the verify-dashboard stdout: `weekly-distance.json`, `monthly-stats.json`, `yearly-stats.json`, `year-over-year.json`, `best-efforts.json`, `best-efforts/`.
    - `grep -c "expect200" scripts/verify-dashboard-publish.mjs` increased by at least 6; `grep -c "fetch(\|http.request\|new Agent" scripts/verify-dashboard-publish.mjs` is unchanged (no new HTTP machinery).
    - No literal activity id appears in the new code: `grep -nE "best-efforts/[0-9i]" scripts/verify-dashboard-publish.mjs` returns nothing (D-10 — ids are interpolated, never pinned).
    - `grep -c "length === 12" scripts/verify-dashboard-publish.mjs` returns at least 1 (year-over-year's fixed-length invariant).
    - `grep -c "streams?.available === true" scripts/verify-dashboard-publish.mjs` returns at least 2 (the pre-existing `newestWithStream` and the new shard-candidate filter).
  </acceptance_criteria>
  <done>Six new by-name assertions exist in the file's own style, each with an invariant an empty or truncated document would fail, and `verify-dashboard` is green.</done>
</task>

<task type="auto">
  <name>Task 2: Observe all six assertions RED, one document at a time (D-11)</name>
  <files>scripts/verify-dashboard-publish.mjs</files>
  <read_first>
    - scripts/verify-dashboard-publish.mjs (the six blocks written in Task 1, and the ROOT/existsSync preflight at lines 18-45 — note the script FATALs early if `dist/widgets/data/dashboard/index.json` or index.html are missing, so never delete those)
    - .planning/phases/25-ci-hardening-light-theme-verification/25-CONTEXT.md § D-11
    - .planning/phases/24-local-curation-mode/24-VALIDATION.md (skim the Round-3/Round-4 evidence style: quoted exit codes and quoted output lines, not narration)
  </read_first>
  <action>
`dist/widgets` is a gitignored build artifact regenerated by `npm run build-widgets`, so it IS
the scratch tree D-11 calls for. Nothing under `data/` is touched.

Before starting, snapshot the six targets outside the repo so restoration is provable:
`mkdir -p /tmp/25-03-restore && cp dist/widgets/data/stats/{weekly-distance,monthly-stats,yearly-stats,year-over-year,best-efforts}.json /tmp/25-03-restore/`
and copy the three shard files the runtime sample will select (read them from the Task 1 run's
stdout, or recompute the same three indices from
`dist/widgets/data/dashboard/index.json`).

Then run SIX independent cycles, one per assertion. For each cycle:
  1. Break exactly one document, and only that one. Alternate the break shape so both halves of
     D-09's invariant are exercised across the six:
       - `weekly-distance.json`  -> overwrite with `[]` (empty array; a bare expect200 would pass this)
       - `monthly-stats.json`    -> truncate to zero bytes (`: > file`)
       - `yearly-stats.json`     -> overwrite with `[]`
       - `year-over-year.json`   -> overwrite with the first 11 entries only (a plausible
                                    truncation that `length > 0` would have accepted — this is
                                    the cycle that proves the `=== 12` invariant earns its keep)
       - `best-efforts.json`     -> overwrite with `{"schemaVersion":1,"activities":{},"rankings":{}}`
                                    (parses, has the right keys, but is empty)
       - one sampled shard       -> delete the file (proves the 404 path AND the index/shard
                                    correspondence D-10 claims)
  2. Run `npm run verify-dashboard; echo "EXIT=$?"`.
  3. Record the exit code and the verbatim failure line(s).
  4. Restore that document from `/tmp/25-03-restore/` (or `npm run build-widgets` for the
     deleted shard).
  5. Re-run `npm run verify-dashboard; echo "EXIT=$?"` and record that it is back to 0.

HALT and report rather than proceeding if any cycle exits 0, or exits non-zero WITHOUT naming
the document that was broken. A non-zero exit that names a different document means the
assertion under test is not the one firing — that is a blind spot of exactly the kind
GAP-24-02 was, and it must be fixed in Task 1's code before the cycle is re-run.

Finish by confirming the tree is restored: `npm run build-widgets` then `npm run
verify-dashboard` exits 0 with the same check count as the end of Task 1, and
`git status --porcelain` is clean apart from `scripts/verify-dashboard-publish.mjs`.
  </action>
  <verify>
    <automated>npm run build-widgets >/dev/null && npm run verify-dashboard && git status --porcelain | grep -v "verify-dashboard-publish.mjs" | grep -v "^?? .planning" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Six RED observations are recorded in the SUMMARY, one per assertion. Each entry contains: the document broken, the break shape applied, the verbatim `EXIT=<n>` line with n non-zero, and the verbatim `✗`/failure line from verify-dashboard's stdout.
    - Every recorded failure line CONTAINS the path of the document that was broken. This is the criterion — "the assertion exists" is not sufficient.
    - The `year-over-year.json` cycle's failure line references the count (11 vs the expected 12), proving the fixed-length invariant fired rather than a generic non-empty check.
    - The shard cycle's failure line names `best-efforts/<the sampled id>.json`, not a generic stats path.
    - After restoration: `npm run verify-dashboard` exits 0 and reports the same "N check(s) passed" total as the end of Task 1 (quote both).
    - `git status --porcelain dist` is empty (dist/widgets is gitignored) and `git status --porcelain data` is empty (no archive file was touched at any point).
  </acceptance_criteria>
  <done>All six assertions have been observed failing, each naming its own document, and the tree is restored green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/stats/*` (generated) → `dist/widgets/data/stats/*` (published) → browser fetch | A whole-directory copy sits between a compute step and production. Nothing today asserts by name that any given document survived it. |
| publish gate → deploy | `npm run verify-dashboard` is a blocking gate in `daily-refresh.yml`. If it passes on a broken document, the broken document deploys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06 | Tampering (data integrity) | `verify-dashboard-publish.mjs` reachability checks | mitigate | D-09: each of the six documents gets 200 + `JSON.parse` + one structural invariant an empty or truncated file fails (non-empty array, exact 12-entry count, non-empty `activities`/`rankings` objects, non-empty `efforts`). A bare `expect200` passes a zero-byte or `{}` body — this repo's Verification Note records three prior instances of shipping behind a green gate. |
| T-25-07 | Repudiation | The new assertions themselves | mitigate | D-11: each assertion is observed RED once, exiting non-zero and NAMING its own document, before it is trusted. GAP-24-02 existed precisely because a guard's blind spot was never observed failing. An assertion that fires but names the wrong document is treated as a defect, not a pass. |
| T-25-08 | Tampering (sample rot) | Shard sample selection | mitigate | D-10: ids are derived at runtime from `indexDoc.activities`, filtered to `streams.available === true`, never pinned. `String(parsed.activityId) === String(id)` cross-checks that the index and the shard directory agree, so a partial directory copy fails the gate instead of silently 404ing in production. |

Not applicable (one line each):
- **V2 Authentication** — the verifier serves a static tree over loopback with no auth surface.
- **V3 Session Management** — no sessions.
- **V4 Access Control** — the curate server's Origin/Host gating was closed in Phase 24 and is untouched here.
- **V6 Cryptography** — none touched.
- **V1 Architecture (fail-closed)** — relevant to plan 25-04's guard fix, not to this additive assertion work; the verifier already exits non-zero on any `fail()`.

**Supply chain:** no `npm install` in this plan; no new package is introduced anywhere in Phase 25, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `npm run build-widgets` exits 0, then `npm run verify-dashboard` exits 0 with a check count higher than the pre-change baseline.
- Six RED observations recorded, each with a non-zero exit code and a failure line naming its own document.
- `git status --porcelain data` and `git status --porcelain dist` are both empty after restoration.
</verification>

<success_criteria>
- ROADMAP criterion 3 is satisfied: `verify-dashboard-publish.mjs` asserts reachability by name for `weekly-distance`, `monthly-stats`, `yearly-stats`, `year-over-year`, `best-efforts.json` and a runtime-derived sample of per-activity shards, rather than relying on the whole-directory copy to carry them.
- No assertion is a bare 200 — each has an invariant an empty or truncated file fails.
- Every new assertion has been observed RED once, naming its own document.
</success_criteria>

<output>
Create `.planning/phases/25-ci-hardening-light-theme-verification/25-03-SUMMARY.md` when done.
It MUST include a section `## D-11 RED observation log` with one subsection per assertion,
each quoting: the break applied, the verbatim `EXIT=<n>` line, the verbatim failure line, and
the verbatim `EXIT=0` line after restoration. Plan 25-06 transcribes this log into
`25-VALIDATION.md`; do not write to `25-VALIDATION.md` from this plan.
</output>
