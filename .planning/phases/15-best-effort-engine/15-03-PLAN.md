---
phase: 15-best-effort-engine
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - src/index.ts
  - package.json
  - .github/workflows/daily-refresh.yml
autonomous: true
requirements: [REC-01]

must_haves:
  truths:
    - "A developer can run the best-effort computation with a single npm command"
    - "The best-effort step runs as part of the compute-all-stats chain, alongside basic, advanced and geo stats"
    - "The daily CI pipeline recomputes best efforts on every run, and a failure in that step warns instead of failing the workflow (D-04)"
    - "The real archive of 1,842 committed streams produces a best-efforts file whose totals match the manifest and whose fastest times are physically plausible"
    - "The gitignored output file is never added to the workflow's commit file_pattern"
  artifacts:
    - path: "src/index.ts"
      provides: "compute-best-efforts command, chain wiring, switch case and help text"
      contains: "computeBestEffortsCommand"
    - path: "package.json"
      provides: "npm run compute-best-efforts script"
      contains: "compute-best-efforts"
    - path: ".github/workflows/daily-refresh.yml"
      provides: "Non-blocking best-effort computation step in the daily pipeline"
      contains: "compute-best-efforts"
  key_links:
    - from: "src/index.ts"
      to: "src/analytics/compute-best-efforts.ts"
      via: "lazy dynamic import inside the command wrapper and inside computeAllStatsCommand"
      pattern: "await import\\('\\./analytics/compute-best-efforts\\.js'\\)"
    - from: ".github/workflows/daily-refresh.yml"
      to: "dist/index.js compute-best-efforts"
      via: "workflow step with continue-on-error"
      pattern: "compute-best-efforts"
---

<objective>
Wire the best-effort computation into the CLI, the `compute-all-stats` chain, the npm scripts and the daily CI workflow, then run it for real over the 1,842 committed streams and prove the output is physically sane.

Purpose: plans 01 and 02 produce a library that nothing calls. This plan makes it a pipeline step and executes it against real data — success criterion 2 of the phase ("results are written to a durable, gitignored records data file consumable by later phases") is only met once the real file exists and its numbers hold up.
Output: `compute-best-efforts` command and npm script, a non-blocking CI step, and a verified `data/stats/best-efforts.json` over the real archive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-best-effort-engine/15-CONTEXT.md
@.planning/phases/15-best-effort-engine/15-PATTERNS.md
@.planning/phases/15-best-effort-engine/15-02-SUMMARY.md

<interfaces>
Contracts this plan wires up (plan 02 — do not redeclare):
```typescript
export interface ComputeBestEffortsOptions {
  activitiesDir?: string; streamsDir?: string; streamsManifestPath?: string; statsDir?: string;
}
export async function computeBestEfforts(options?: ComputeBestEffortsOptions): Promise<BestEffortsDocument>;
```

Command-wrapper convention to copy (`src/index.ts:166-183`, the lazy-import style used by the newest compute module):
```typescript
async function computeGeoStatsCommand() {
  try {
    const { computeGeoStats } = await import('./geo/compute-geo-stats.js');
    console.log('Computing geographic statistics from synced activities...\n');
    await computeGeoStats({ activitiesDir: config.activitiesDir, geoDir: 'data/geo' });
    console.log('\nGeographic statistics generated successfully!');
    process.exit(0);
  } catch (error: any) {
    console.error('Compute geo stats error:', error.message);
    if (error.code === 'ENOENT' && error.message.includes('activities')) {
      console.error('\nActivities directory not found. Please run: npm run sync');
    }
    process.exit(1);
  }
}
```

Chain convention (`src/index.ts:185-221`) — three steps today, each separated by `console.log('')`:
```typescript
async function computeAllStatsCommand() {
  try {
    console.log('Computing all statistics from synced activities...\n');
    await computeAllStats({ activitiesDir: config.activitiesDir, statsDir: 'data/stats' });
    console.log('');
    await computeAdvancedStats({ activitiesDir: config.activitiesDir, statsDir: 'data/stats' });
    console.log('');
    const { computeGeoStats } = await import('./geo/compute-geo-stats.js');
    await computeGeoStats({ activitiesDir: config.activitiesDir, geoDir: 'data/geo' });
    console.log('\nAll statistics generated successfully!');
    process.exit(0);
  } catch (error: any) { /* ... */ }
}
```

Config getters already available (`src/config/strava.config.ts:57-67`):
```typescript
get activitiesDir(): string { return path.join(this.dataDir, 'activities'); }
get streamsDir(): string { return path.join(this.dataDir, 'streams'); }
get streamsManifestPath(): string { return path.join(this.streamsDir, 'manifest.json'); }
```

Workflow step convention with a non-blocking outcome (`.github/workflows/daily-refresh.yml:64-71`):
```yaml
      - name: Compute geographic statistics
        id: geocode
        continue-on-error: true
        run: node dist/index.js compute-geo-stats

      - name: Warn on geocoding failure
        if: steps.geocode.outcome == 'failure'
        run: echo "::warning::Geocoding failed, widgets will use cached geo data"
```
The commit step's `file_pattern` is `'data/activities/*.json data/sync-state.json data/geo/*.json data/streams/*.json'` and deliberately excludes `data/stats/` because that path is gitignored — adding it would make `git add` fail on an ignored path and skip the deploy. It must stay unchanged.

Measured expectations for the real run (taken from the committed archive this session — use these as the numeric gates):
- `data/streams/manifest.json` totals: 1,867 activities, 1,842 available, 25 unavailable.
- All 1,842 available ids have a matching `data/activities/<id>.json` (zero missing records).
- Activities passing the D-01 pre-filter per target: `400m` 1,841; `1k` 1,837; `5k` 1,775; `10k` 1,492; `half` 118; `marathon` 0.
- Longest activity in the archive is 34.09 km (id `3475727699`), so `marathon` will legitimately yield an empty ranking.
- Activity `5059204779` records 10.80 km against a 20:16 moving time — a known bad record in the archive and a likely source of guard rejections.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the command, the chain step, the npm script and the CI step</name>
  <files>src/index.ts, package.json, .github/workflows/daily-refresh.yml</files>
  <read_first>
    - src/index.ts (the file being modified — read `computeGeoStatsCommand` at lines 166-183, `computeAllStatsCommand` at 185-221, `printHelp` at 417-440, and the `main()` switch at 443-487 before editing)
    - package.json (the scripts block being extended; match the existing `compute-geo-stats` entry's shape)
    - .github/workflows/daily-refresh.yml (the workflow being modified — read the geocode step and the commit step's file_pattern comment before editing)
    - src/config/strava.config.ts (the `activitiesDir` / `streamsDir` / `streamsManifestPath` getters to pass through)
    - src/analytics/compute-best-efforts.ts (the `ComputeBestEffortsOptions` signature being called)
  </read_first>
  <action>
Make four edits, all copying existing conventions rather than inventing new ones.

1. `src/index.ts` — add `async function computeBestEffortsCommand()` immediately after `computeGeoStatsCommand`, copying its structure exactly: lazy `const { computeBestEfforts } = await import('./analytics/compute-best-efforts.js');` inside the `try`, an opening `console.log('Computing best efforts from committed streams...\n')`, the call `await computeBestEfforts({ activitiesDir: config.activitiesDir, streamsDir: config.streamsDir, streamsManifestPath: config.streamsManifestPath, statsDir: 'data/stats' })`, a closing `console.log('\nBest efforts generated successfully!')`, then `process.exit(0)`. The `catch` logs `Compute best efforts error:` plus the message, and adds a targeted hint when `error.code === 'ENOENT'` and the message mentions `streams`: `\nStreams directory not found. Please run: npm run backfill-streams`. Static imports at the top of the file must not be touched — the lazy-import style is the established convention for the newer compute modules.

2. `src/index.ts` — add a fourth step to `computeAllStatsCommand()` after the geo-stats block, separated by a `console.log('')` like the others: lazy-import `computeBestEfforts` and call it with the same four options. Best efforts run last because they depend on committed streams rather than on the other stats outputs.

3. `src/index.ts` — add `case 'compute-best-efforts': await computeBestEffortsCommand(); break;` to the `main()` switch immediately after the `compute-geo-stats` case, and add two `printHelp` lines following the existing column alignment: in the commands list, `  compute-best-efforts   - Compute best efforts (fastest 400m..marathon) from committed streams`; in the examples list, `  npm run compute-best-efforts    # Generate best-effort records`. Also update the existing `compute-all-stats` help line to read `- Compute all statistics (basic, advanced, geo, best efforts)`.

4. `package.json` — add `"compute-best-efforts": "node dist/index.js compute-best-efforts"` to `scripts`, positioned immediately after the existing `compute-geo-stats` entry so the compute family stays grouped.

5. `.github/workflows/daily-refresh.yml` — insert two steps between `Warn on geocoding failure` and `Build widgets`, mirroring the geocode pair exactly: a step named `Compute best efforts` with `id: best-efforts`, `continue-on-error: true`, and `run: node dist/index.js compute-best-efforts`; then a step named `Warn on best-effort failure` with `if: steps.best-efforts.outcome == 'failure'` and `run: echo "::warning::Best-effort computation failed, records data will be stale"`. `continue-on-error` is what satisfies D-04's "the step never fails CI". Add a short comment above the pair noting that the output lands in gitignored `data/stats/` and is therefore deliberately absent from the commit step's `file_pattern`. Do NOT modify `file_pattern`, `timeout-minutes`, or any other step.
  </action>
  <verify>
    <automated>npm run build && node dist/index.js help | grep -q "compute-best-efforts" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `node dist/index.js help` output contains the line `compute-best-efforts`
    - `node -e "const p=require('./package.json'); if(!p.scripts['compute-best-efforts']) process.exit(1)"` exits 0
    - `grep -c "compute-best-efforts" .github/workflows/daily-refresh.yml` outputs at least 2
    - `grep -q "continue-on-error: true" .github/workflows/daily-refresh.yml` exits 0 and the `Compute best efforts` step is one of the steps carrying it
    - `grep -n "file_pattern" .github/workflows/daily-refresh.yml` shows the pattern still equals `'data/activities/*.json data/sync-state.json data/geo/*.json data/streams/*.json'` with no `data/stats` addition
    - `grep -c "await import('./analytics/compute-best-efforts.js')" src/index.ts` outputs 2 (the command wrapper and the chain step)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` (full suite) exits 0 with no regressions
  </acceptance_criteria>
  <done>`npm run compute-best-efforts` runs the engine, `compute-all-stats` includes it as a fourth step, and the daily workflow recomputes it without being able to fail the pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Run the engine over the real archive and validate the output</name>
  <files>data/stats/best-efforts.json (gitignored output — no source file is modified by this task)</files>
  <read_first>
    - src/analytics/compute-best-efforts.ts (to know exactly which totals and fields the document carries)
    - src/analytics/best-effort.types.ts (the document shape being asserted against)
    - data/streams/manifest.json (the totals block the run must reconcile against — read only the `totals` object, not the 1,867-entry activities map)
    - .planning/phases/15-best-effort-engine/15-CONTEXT.md (D-04's rejection-reporting requirement and the never-fails-CI convention)
  </read_first>
  <action>
Build and execute the real run, then validate its output against the archive facts. Do not modify any source file in this task — if a gate fails, record the failure and the diagnosis in the plan summary rather than silently loosening the gate.

Run `npm run build` followed by `time node dist/index.js compute-best-efforts` and capture the full console output including the rejection block.

Then validate `data/stats/best-efforts.json` with a throwaway `node -e` script (the file is gitignored; do not commit it, and do not add a script for it to `package.json`). Check every one of the following against the measured archive facts:

- `schemaVersion` is 1 and `generatedAt` parses as a valid ISO date.
- `totals.activitiesConsidered` is 1842 and `totals.skippedNoStream` is 25, reconciling with the manifest's `with_streams` and `without_streams`.
- `totals.skippedUnreadable` is 0 — every available id has both a stream file and an activity record today, so a non-zero value means a real regression, not expected noise.
- `Object.keys(document.activities).length` is at least 1,835 (1,841 activities clear the 400 m pre-filter; a handful may legitimately produce no effort because their stream span falls short of the target).
- `rankings.marathon` is empty, because the longest activity in the archive is 34.09 km.
- `rankings['400m']`, `rankings['1k']`, `rankings['5k']`, `rankings['10k']` each hold exactly 10 entries; `rankings.half` holds 10 entries drawn from the 118 eligible activities.
- Every ranking array is sorted strictly non-decreasing by `durationSec` with `rank` running 1..N.
- Every effort in the document satisfies `paceSecPerKm > 0`, `durationSec > 0`, and an implied speed below the world-record ceiling for its distance — no exceptions, since the guard should have dropped any that did not.
- `totals.lowConfidenceEfforts` is greater than 0 and every low-confidence effort belongs to an activity whose `distanceSource` is `geo` (38 such activities exist).
- The rank-1 entries for `5k`, `10k` and `half` correspond to plausible recreational times — print them with their activity ids and dates for the summary, and flag anything faster than 2:30 per km as suspicious.
- `totals.effortsRejected` equals `rejected.length`, and each rejection reason names concrete numbers.

Record in the plan summary: wall-clock runtime, the file size of `best-efforts.json`, the totals block, the rank-1 entry per distance, and the full rejection list (or the first 20 rows plus a count if it is long). Activity `5059204779` (10.80 km against a 20:16 moving time) is a known-bad archive record — note explicitly whether it produced rejections and by which guard, since that is the clearest real-world evidence D-04's guard is doing its job.

If wall-clock runtime exceeds 120 seconds, treat it as a Pitfall 2 regression (an end pointer resetting per start index) and report it rather than accepting it — the daily workflow's total budget is 30 minutes shared with fetch, build, geocoding and widget builds.
  </action>
  <verify>
    <automated>npm run build && node dist/index.js compute-best-efforts && node -e "const d=require('./data/stats/best-efforts.json'); const a=[['activitiesConsidered',d.totals.activitiesConsidered,1842],['skippedNoStream',d.totals.skippedNoStream,25],['skippedUnreadable',d.totals.skippedUnreadable,0],['marathonRanks',d.rankings.marathon.length,0]]; for(const [k,got,want] of a){ if(got!==want){console.error('FAIL',k,'got',got,'want',want);process.exit(1);} } if(Object.keys(d.activities).length<1835){console.error('FAIL activities',Object.keys(d.activities).length);process.exit(1);} if(d.totals.effortsRejected!==d.rejected.length){console.error('FAIL rejected count');process.exit(1);} console.log('OK', JSON.stringify(d.totals));"</automated>
  </verify>
  <acceptance_criteria>
    - `node dist/index.js compute-best-efforts` exits 0
    - The verify command prints `OK` followed by the totals object
    - `data/stats/best-efforts.json` exists and parses as JSON
    - `git status --porcelain data/stats` produces no output, confirming the file is gitignored and not staged
    - Wall-clock runtime of the compute step is under 120 seconds
    - `node -e "const d=require('./data/stats/best-efforts.json'); const w={'400m':9.29,'1k':7.58,'1mi':7.23,'5k':6.62,'10k':6.29,'half':6.13,'marathon':5.83}; const M={'400m':400,'1k':1000,'1mi':1609.344,'5k':5000,'10k':10000,'half':21097.5,'marathon':42195}; let bad=0; for(const a of Object.values(d.activities)) for(const e of a.efforts) if(M[e.distance]/e.durationSec > w[e.distance]) bad++; if(bad){console.error('implausible efforts:',bad);process.exit(1);} console.log('all efforts under world-record ceiling');"` exits 0
    - `node -e "const d=require('./data/stats/best-efforts.json'); for(const k of Object.keys(d.rankings)){const r=d.rankings[k]; for(let i=1;i<r.length;i++){ if(r[i].durationSec<r[i-1].durationSec||r[i].rank!==i+1){console.error('bad ranking',k,i);process.exit(1);} }} console.log('rankings ordered');"` exits 0
    - `node -e "const d=require('./data/stats/best-efforts.json'); if(!(d.totals.lowConfidenceEfforts>0)){console.error('no lowConfidence efforts — D-03 flag not propagating');process.exit(1);} console.log('lowConfidence efforts:',d.totals.lowConfidenceEfforts);"` exits 0
    - The plan summary records runtime, file size, totals, rank-1 entry per distance, and the rejection list
  </acceptance_criteria>
  <done>The real archive produces a best-efforts file whose totals reconcile with the manifest, whose rankings are ordered and capped, whose efforts are all under the world-record ceiling, and which computes fast enough to sit inside the daily CI budget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repository working tree | The workflow writes derived data and commits a subset of it; an ignored path in `file_pattern` breaks the commit step and skips the deploy |
| compute step exit code → workflow outcome | A non-zero exit would otherwise fail the daily pipeline and block the widget deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Denial of Service | daily-refresh.yml best-effort step | mitigate | `continue-on-error: true` plus a paired warning step, mirroring the geocode precedent — a best-effort failure degrades records data rather than blocking the widget deploy (D-04) |
| T-15-03 | Denial of Service | real archive run inside the 30-minute CI budget | mitigate | Task 2 gates wall-clock runtime at 120 seconds and treats an overrun as a Pitfall 2 algorithmic regression rather than an acceptable cost |
| T-15-06 | Denial of Service | commit step `file_pattern` | mitigate | `data/stats/` stays out of `file_pattern`; adding a gitignored path makes `git add` fail and silently skips the Pages deploy — this exact failure froze gh-pages once before (Feb-Aug 2026). Asserted by an explicit acceptance criterion. |
| T-15-04 | Tampering | real-run output validation | mitigate | Task 2 asserts every emitted effort is under the world-record ceiling and every ranking is ordered, so a guard regression surfaces against real data rather than only against synthetic tests |
| T-15-SC | Tampering | npm installs | accept | Zero new packages (RESEARCH.md Package Legitimacy Audit: not applicable). `package.json` gains a script entry only — no dependency change, so no slopcheck or legitimacy checkpoint applies. |
</threat_model>

<verification>
- `npm run build` exits 0 and `npx tsc --noEmit` exits 0
- `node dist/index.js help` lists `compute-best-efforts`
- `node dist/index.js compute-best-efforts` exits 0 and writes `data/stats/best-efforts.json`
- `node dist/index.js compute-all-stats` exits 0 and its output includes the best-effort summary block
- `git status --porcelain data/stats` is empty
- `npx vitest run` (full suite) exits 0 with no regressions
</verification>

<success_criteria>
- `npm run compute-best-efforts` and the `compute-all-stats` chain both produce the records file
- The daily workflow computes best efforts on every run and cannot fail the pipeline doing so
- The real run reconciles with the manifest (1,842 considered, 25 skipped, 0 unreadable) in under 120 seconds
- Every emitted effort is under its world-record ceiling and every ranking is ordered and capped at 10
- The gitignored output is never staged and `file_pattern` is unchanged
</success_criteria>

<output>
Create `.planning/phases/15-best-effort-engine/15-03-SUMMARY.md` when done
</output>
