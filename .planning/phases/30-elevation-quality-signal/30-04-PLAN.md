---
phase: 30-elevation-quality-signal
plan: 04
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - scripts/compute-elevation-calibration.mjs
  - scripts/compute-elevation-calibration.test.mjs
  - package.json
  - .planning/phases/30-elevation-quality-signal/30-CALIBRATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ELEV-01, ELEV-02]

must_haves:
  decisions: [D-03, D-04, D-08, D-15, D-16]
  truths:
    - "D-15: an archive-wide dry run over every committed stream reports per-mode flag rates, the loop-gated overlap matrix, the union and its device-family breakdown, each against a named denominator"
    - "D-03: the report derives and justifies the 100 m loop radius from the archive's own bimodal start/end distance distribution, and states that every value in the empty gap is equivalent"
    - "D-04: ROADMAP Criterion 1 and REQUIREMENTS ELEV-01 state the loop-gated drift count, record that 34 was a raw-difference measurement, and the report lists the point-to-point exclusions by id as excluded-by-design"
    - "D-08: the report states in words that carry-forward altitude fill manufactures vertical-rate violations and never masks them, with the 3149636661 trace as evidence"
    - "D-16: data/streams/ is proved byte-unchanged by a sha256 digest taken before and after the sweep, printed into the report, with the script exiting non-zero on a mismatch"
    - "Neither the new script nor the new analytics module has any fs write target under data/"
    - "The report is regenerable: running the script twice produces a byte-identical document"
  artifacts:
    - path: "scripts/compute-elevation-calibration.mjs"
      provides: "the archive sweep, the digest gate, and the markdown renderer; single declared write target"
      contains: "createHash"
    - path: ".planning/phases/30-elevation-quality-signal/30-CALIBRATION.md"
      provides: "the artifact of record for ELEV-02's archive-wide validation"
      contains: "## Loop radius"
    - path: "scripts/compute-elevation-calibration.test.mjs"
      provides: "importing the module runs no sweep and writes nothing; no fs write target under data/"
  key_links:
    - from: "scripts/compute-elevation-calibration.mjs"
      to: "dist/analytics/pace-quality.js detectors"
      via: "direct import of the shipped classifier — this is the one script allowed to"
      pattern: "dist/analytics/pace-quality.js"
    - from: "30-CALIBRATION.md loop-gated drift count"
      to: ".planning/ROADMAP.md Phase 30 Criterion 1 and .planning/REQUIREMENTS.md ELEV-01"
      via: "the D-04 text correction, citing the report as its source"
      pattern: "raw-difference measurement"
---

<objective>
Build the archive-wide dry run ELEV-02 requires, write `30-CALIBRATION.md` as its regenerable
artifact of record, prove `data/streams/` byte-unchanged by digest, and correct the two requirement
documents to the loop-gated count the shipped detector actually flags.

Purpose: ELEV-02 rejects a detector validated only against its exemplar, and D-04 refuses to let a
criterion assert a number the detector does not produce. Those are the same job: measure the whole
archive, publish the measurement, then make the requirement text say what was measured.

Output: two new scripts-side files, one npm script, one calibration report, and two corrected
requirement documents — all landing well before the checkpoint plan drafts a single row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-elevation-quality-signal/30-CONTEXT.md
@.planning/phases/30-elevation-quality-signal/30-RESEARCH.md
@.planning/phases/30-elevation-quality-signal/30-01-SUMMARY.md
@.planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md
@scripts/compute-pace-quality-calibration.mjs

<interfaces>
<!-- Research-measured figures, 2026-09-18. RE-DERIVE every one of them at execution time; the
     archive grows nightly and has drifted by single digits between phases. Use the live value, and
     state the delta wherever it moves. -->

| quantity | measured 2026-09-18 |
|---|---|
| activities / streams carrying `alt` | 1,890 / 1,865 |
| sub-ground (`min alt < -50`) | 11 — worst 4556693525 (−282), 7382336793 (−189), 5493309448 (−125.4) |
| raw drift (`\|alt[end]−alt[start]\| > 60`, no loop gate) | 34 — worst 4745489664 (−197.6), 3149636661 (−172.6), 5493309448 (+164.2) |
| loop-gated drift (within 100 m AND > 60 m) | **21** |
| raw-34 decomposition | 21 loop-gated + 12 point-to-point (626 m … 5,825 m) + 1 no position (4598855187) |
| vertical rate (`\|Δalt\|/Δt > 5`) | 39 — worst 3149636661 (80.4), 5059198439 (19.4), 3925007542 (18.6) |
| loop-gated union | **60** (raw union 71) |
| loop-gated overlaps | sub∩drift 6, sub∩rate 3, drift∩rate 3, all three 1 (5493309448) |
| inclusion-exclusion check | 11 + 21 + 39 − 6 − 3 − 3 + 1 = 60 |
| device breakdown (loop-gated union) | Suunto 9 46, Garmin fēnix 6 Pro 11, no device name 2, Garmin vívoactive 4 1 |
| drift not-computable, archive-wide | **207** of 1,865 alt-carrying streams (150 activities carry `start_latlng: []`) |
| start/end distance distribution | bimodal: 1,048 of 1,658 positioned activities at exactly 0 m, 610 at ≥ 552.44 m, nothing in between |
| carry-forward correlation | 19 of 72 violating pairs preceded by ≥2 byte-identical `alt` samples |

<!-- The 12 point-to-point ids excluded by the loop gate (measured): 3475743040, 3475724298,
     3925007542, 5551478413, 5747186018, 3963359864, 3475724049, 4391829317, 3475715995,
     3475742446, 3475736537, 3744595598. The 1 no-position id: 4598855187. -->

<!-- The 3149636661 trace the D-08 finding rests on: alt holds 191.8/191.8/191.8/191.8/191.8/191.6/
     191.6 across t=69..80, then a single 2-second tick to 30.8 at t=82 — 160.8 m in 2 s = 80.4 m/s. -->

<!-- Script skeleton to mirror: scripts/compute-pace-quality-calibration.mjs — readdirSync sweep over
     data/activities/ and data/streams/, per-file try/catch with warn-and-continue, pure exported
     helpers, markdown renderer, a single declared write target, a guarded `main()` behind a
     self-execution check so importing the module runs nothing. Report shape to mirror:
     27-CALIBRATION.md. Vitest collects `scripts/**/*.test.mjs` only — a `.test.ts` under scripts/
     is never collected. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The sweep script, its digest gate, and its guard tests</name>
  <files>scripts/compute-elevation-calibration.mjs, scripts/compute-elevation-calibration.test.mjs, package.json</files>
  <read_first>
    - `scripts/compute-pace-quality-calibration.mjs:1-120` — the header doc block, the path
      constants, the single declared write target, and the pure-helper convention.
    - The tail of `scripts/compute-pace-quality-calibration.mjs` — `main()` and the self-execution
      guard, plus how it enumerates `data/activities/` and `data/streams/` with per-file try/catch.
    - `scripts/compute-pace-quality-calibration.test.mjs:1-45` — the dynamic-`import()`-inside-
      `beforeAll` plus mtime-before/after assertion that proves importing writes nothing.
    - `scripts/compute-pace-quality-recount.test.mjs:180-200` — the "importing this module triggers
      no read" assertion shape.
    - `package.json:20-30` — the `compute-pace-quality-calibration` / `compute-pr-ceiling-calibration`
      script lines, including which ones prefix `npm run build`.
    - `.planning/phases/30-elevation-quality-signal/30-01-SUMMARY.md` — the exported detector names,
      the `ElevationSignal` shape and the four threshold constants.
  </read_first>
  <action>
    Create `scripts/compute-elevation-calibration.mjs` mirroring `compute-pace-quality-calibration.mjs`:
    the same header doc block style, the same `readdirSync` sweep with per-file try/catch and
    warn-and-continue, the same pure exported helpers, the same guarded `main()` so importing the
    module performs no sweep and writes nothing, and a single declared write target — the constant
    pointing at `.planning/phases/30-elevation-quality-signal/30-CALIBRATION.md` and nothing else.

    It imports the shipped detectors directly from the compiled `dist/analytics/pace-quality.js`
    (`subGroundSignal`, `closureDriftSignal`, `verticalRateSignal`, the elevation assembly, and the
    four threshold constants including `LOOP_RADIUS_M`). This is the one script in this phase allowed
    to import the classifier — it is measuring what the classifier does. Read the thresholds and the
    loop radius FROM the module; do not retype their literals here, or the report can disagree with
    the shipped detector while looking self-consistent.

    Exclude `manifest.json` when enumerating `data/streams/` — Phase 27's own gap G-01 was exactly
    this file being counted as an activity stream, and `27-11-PLAN.md` fixed it there. The stream
    denominator is alt-carrying activity streams, not the raw file count.

    Quantities to compute and carry into the report (every one re-derived live, none copied):
      - denominators: total activities, streams present, streams carrying `alt`, activities with a
        normalizable start/end position;
      - the start/end distance distribution: counts below and above the radius, the minimum non-zero
        distance observed, and the count at exactly 0 m — this is D-03's derivation evidence;
      - per-mode cohorts with counts and the three worst ids and values per mode;
      - the raw (un-loop-gated) drift cohort and its decomposition into loop-gated / point-to-point /
        no-position, with the point-to-point ids listed and their endpoint distances;
      - the loop-gated 3×3 overlap matrix, the all-three set, the union, and an explicit
        inclusion-exclusion arithmetic check that the script asserts rather than merely prints;
      - the device-family breakdown of the union, using the same `(no device name)` explicit category
        the rest of this project uses, never a fabricated device;
      - the archive-wide drift not-computable cohort with its denominator stated;
      - the carry-forward correlation count: violating pairs preceded by at least two byte-identical
        `alt` samples, out of all violating pairs, with the operationalization stated in the report
        so a different one is recognizable as different.
    Every percentage prints its own denominator inline — Phase 27's D-06 rule, and 30-RESEARCH.md's
    assumption A3 flags this as the report's likeliest source of later confusion.

    D-16's digest gate: before the sweep, walk `data/streams/` and roll a per-file sha256 (of the file
    bytes, in sorted filename order) into one aggregate sha256; repeat after the sweep; print both
    into the report; exit non-zero with a named message if they differ. Use Node's built-in
    `crypto.createHash('sha256')` — no new dependency. This is the phase's only net-new machinery and
    has no in-repo analog.

    Add `"compute-elevation-calibration": "npm run build && node scripts/compute-elevation-calibration.mjs"`
    to `package.json` scripts, beside the existing calibration entries. The `npm run build` prefix is
    required because the script imports the compiled classifier, exactly like its Phase 27 sibling.

    Create `scripts/compute-elevation-calibration.test.mjs` (`.mjs`, not `.ts` — vitest never collects
    `scripts/**/*.test.ts`) asserting:
      - importing the module runs no sweep and writes nothing: dynamic `import()` inside `beforeAll`,
        with the report file's mtime compared before and after;
      - the pure helpers are importable and behave on small hand-built inputs: the overlap matrix
        arithmetic, the inclusion-exclusion check, the device-family bucketing including the
        no-device-name category, and the distance-distribution bucketing;
      - the digest helper returns the same digest for the same directory contents and a different one
        after a byte changes, exercised against a temporary directory under the OS temp dir, never
        against `data/`;
      - D-16's source scan: neither `scripts/compute-elevation-calibration.mjs` nor
        `src/analytics/pace-quality.ts` contains any `writeFileSync`/`writeFile`/`appendFile`/`mkdir`
        call whose path argument resolves under `data/` — read both sources as text and assert, the
        T-27-09 pattern extended from a `.planning/`-only check to a `data/` exclusion.
  </action>
  <verify>
    <automated>npx vitest run scripts/compute-elevation-calibration.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createHash" scripts/compute-elevation-calibration.mjs` returns at least 1 and `grep -c "require\|from 'crypto'\|from 'node:crypto'" scripts/compute-elevation-calibration.mjs` shows the built-in import, with no new entry in `package.json` dependencies (`git diff package.json` touches only the scripts block).
    - `grep -c "manifest.json" scripts/compute-elevation-calibration.mjs` returns at least 1 — the manifest exclusion is explicit, not accidental.
    - `grep -c "dist/analytics/pace-quality.js" scripts/compute-elevation-calibration.mjs` returns at least 1, and the four thresholds are imported rather than retyped: `grep -v '^\s*\*' scripts/compute-elevation-calibration.mjs | grep -c "= -50\|= 60;\|= 5;\|= 100;"` returns 0.
    - `grep -c "compute-elevation-calibration" package.json` returns 1.
    - `npx vitest run scripts/compute-elevation-calibration.test.mjs` exits 0, with a passing test naming the `data/` write-target scan and a passing test naming the digest helper.
    - Demonstrated failing, run and recorded verbatim: point the digest helper at a temporary copy of a few stream files, mutate one byte between the two digests, and confirm the script's gate exits non-zero with its named message; then confirm the unmutated run exits 0. The mutation happens in the temp dir, never in `data/`.
    - `git status --porcelain data/` is empty.
  </acceptance_criteria>
  <done>A sweep script exists that measures the archive, proves the streams untouched by digest, writes exactly one file, and does nothing at all when merely imported.</done>
</task>

<task type="auto">
  <name>Task 2: Generate 30-CALIBRATION.md and prove it regenerable</name>
  <files>.planning/phases/30-elevation-quality-signal/30-CALIBRATION.md, scripts/compute-elevation-calibration.mjs</files>
  <read_first>
    - `.planning/phases/27-per-activity-quality-signals/27-CALIBRATION.md` — the section order, table
      style, denominator lines and prose register this report copies.
    - `.planning/phases/30-elevation-quality-signal/30-CONTEXT.md` § D-15 — the named sections this
      report must carry.
    - `.planning/phases/30-elevation-quality-signal/30-RESEARCH.md` § D-03, § D-04, § D-08, and
      § Open Questions 1 — including the recommendation that the loop-gated matrix is the primary
      table and the raw-34 decomposition appears as a labelled sub-table under the correction
      narrative.
  </read_first>
  <action>
    Run `npm run compute-elevation-calibration` and produce
    `.planning/phases/30-elevation-quality-signal/30-CALIBRATION.md` with these sections, all written
    by the script — nothing hand-edited into the document, because a hand-edit to a generated artifact
    silently reverts on the next regeneration, and this project has been bitten by exactly that:

      1. `## Live denominators` — activities, streams, alt-carrying streams, positioned activities,
         each with the date of the run; every later percentage names which of these it divides by.
      2. `## Thresholds in force` — the three requirement thresholds (−50 m on the stream minimum,
         60 m absolute closure delta, 5 m/s per-sample rate) read from the shipped module, each with
         its mechanism stated in one line.
      3. `## Loop radius` — the derived `LOOP_RADIUS_M = 100` with D-03's justification: the measured
         start/end distance distribution is bimodal with a zero-width dead zone (every positioned
         activity sits at exactly 0 m or at ≥ the observed minimum non-zero distance), so every value
         in that empty interval yields an identical partition; 100 m is chosen for headroom against
         future archive growth, not because the boundary is sensitive. Print the distribution counts
         that make this checkable.
      4. `## Per-mode cohorts` — one table per mode with its count, its rate against a named
         denominator, and its three worst ids and values.
      5. `## Overlap matrix (loop-gated)` — the primary 3×3 table, the all-three set, the union, and
         the inclusion-exclusion arithmetic shown.
      6. `## Union by device family` — the breakdown with `(no device name)` as an explicit category.
      7. `## Drift not-computable` — the archive-wide cohort with its denominator stated in the same
         line, and a sentence distinguishing it from the loop-gate exclusions: no position is unknown,
         point-to-point is excluded by design.
      8. `## Loop-gate exclusions` — every point-to-point id with its endpoint distance, plus the
         no-position id, under the heading that names them excluded-by-design.
      9. `## Correction of the raw-difference count (D-04)` — the raw drift cohort, its decomposition
         into loop-gated / point-to-point / no-position, and the statement that the requirement's
         original figure was a raw-difference measurement taken without the loop condition.
     10. `## Mode independence (Criterion 2)` — in prose: which pairs overlap and by how much, that
         the loop-gated overlaps are lower than the raw ones so the gated drift cohort is a cleaner
         independent signal, and that a floor bound alone could not have caught the drift cohort.
     11. `## Carry-forward fill and vertical rate (D-08)` — in words: carry-forward altitude fill
         MANUFACTURES apparent vertical-rate violations and never masks them, because the fill only
         repeats a prior value or preserves an existing jump and has no averaging step that could
         suppress a real spike; the measured correlation count with its operationalization stated;
         and the worst-case trace as evidence. State explicitly that this is a reported finding and
         that the detector stays per-sample per D-08/D-09 — no threshold is changed on account of it.
     12. `## Stream integrity (D-16)` — the before and after digests and the statement that they
         match.

    Then prove regenerability rather than asserting it: run the script twice in a row and confirm the
    two outputs are byte-identical apart from any line that deliberately carries a timestamp; if a
    timestamp line exists, confirm every other byte matches by diffing with that line filtered out.
    A report that changes on a second no-op run is not an artifact of record.

    If any live figure differs from the research table in `<interfaces>`, the live figure wins. Record
    the delta in the summary and say plainly whether it is archive growth (ids added or removed) or a
    behavioral difference (the same ids scoring differently) — only the second is a defect.
  </action>
  <verify>
    <automated>npm run compute-elevation-calibration && npx vitest run scripts/compute-elevation-calibration.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/30-elevation-quality-signal/30-CALIBRATION.md` exists and `grep -c "^## " ` over it returns at least 12.
    - `grep -c "## Loop radius" .planning/phases/30-elevation-quality-signal/30-CALIBRATION.md` returns 1, and that section contains both the distribution counts and the word `equivalent` or an equivalent statement that every radius in the empty interval yields the same partition.
    - `grep -c "manufactures" .planning/phases/30-elevation-quality-signal/30-CALIBRATION.md` returns at least 1 and the D-08 section also contains `never masks` (or an unambiguous equivalent) and the worst-case activity id.
    - The union section's inclusion-exclusion line is present and arithmetically correct as printed.
    - Every percentage in the document is followed by or preceded by its denominator: spot-check by grepping for `%` and confirming each hit's line names a denominator; record the check in the summary.
    - `## Stream integrity` shows two digests and states that they match; `git status --porcelain data/` is empty after the run.
    - Idempotence proven: two consecutive runs produce byte-identical output (ignoring at most one deliberate timestamp line), shown with `diff` output recorded in the summary.
  </acceptance_criteria>
  <done>The archive-wide measurement exists as a regenerable document that states its denominators, derives its radius, discloses the carry-forward finding, and proves the streams untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the two requirement documents to the measured loop-gated counts</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/30-elevation-quality-signal/30-CALIBRATION.md` § Correction of the
      raw-difference count, § Per-mode cohorts, § Union by device family — the live figures this
      correction cites.
    - `.planning/ROADMAP.md` § Phase 30 — Criterion 1's "at least the 34 barometric-closure-drift
      activities" wording and Criterion 3's "reconciles with the measured 71-activity (3.8%) cohort"
      wording, both in full.
    - `.planning/REQUIREMENTS.md:70-76` — ELEV-01's cohort figures and its three mode bullets, and
      ELEV-02, both in full.
    - `.planning/phases/30-elevation-quality-signal/30-CONTEXT.md` § D-04.
    - `.planning/STATE.md` § Carried into the next milestone — the v2.1 process fix that requirements
      are ticked after verification, never before.
  </read_first>
  <action>
    Apply D-04's correction to both documents, citing `30-CALIBRATION.md` as the source in each edit.

    `.planning/ROADMAP.md` § Phase 30 Criterion 1: replace "at least the 34 barometric-closure-drift
    activities (start/end differing >60 m, worst 198 m)" with the measured loop-gated figure from the
    report, and add a parenthetical recording that the original 34 was a raw start/end difference
    measured without the loop condition, that its decomposition is loop-gated + point-to-point +
    no-position as the report enumerates, and that the point-to-point activities are excluded by
    design because a real 60–90 m altitude change is plausible between endpoints kilometres apart.

    `.planning/ROADMAP.md` § Phase 30 Criterion 3: correct the "71-activity (3.8%)" union figure the
    same way and by the same rationale — this is the discretion item CONTEXT.md left open, and it is
    resolved yes, in this task, because leaving one figure raw and the other gated would make the
    correction unauditable. Word the criterion so it requires BOTH: the report reconciles the
    loop-gated union, AND it shows how the raw-definition union decomposes, so the correction is
    visible rather than silent. Correct the device-family breakdown to the loop-gated one, keeping
    all four named families present.

    `.planning/REQUIREMENTS.md` § ELEV-01: correct the measured cohort line and the barometric closure
    drift bullet to the loop-gated figures, add the same raw-measurement note, and add one sentence
    naming the loop test (metadata `start_latlng`/`end_latlng`, radius 100 m, derived in
    `30-CALIBRATION.md`) so a reader sees why the number moved. Leave the sub-ground and vertical-rate
    bullets' figures alone unless the live report moved them, in which case correct them with the same
    citation.

    Do NOT tick the ELEV-01 or ELEV-02 checkboxes and do not change the Requirements Coverage table's
    `Pending` status. This edit changes count wording only. Requirement ticks come after verification
    in this project — three requirements were ticked, reverted and re-ticked during v2.1 for exactly
    this reason.

    Keep every edit surgical: no reflow of untouched paragraphs, no restructuring of either document.
  </action>
  <verify>
    <automated>bash -c 'grep -c "at least 34\|34 barometric-closure-drift" .planning/ROADMAP.md .planning/REQUIREMENTS.md | grep -qv ":[1-9]" && grep -q "raw-difference" .planning/ROADMAP.md && grep -q "raw-difference" .planning/REQUIREMENTS.md && echo CORRECTED'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "34 barometric-closure-drift" .planning/ROADMAP.md` returns 0 and `grep -rc "34 activities whose start and end" .planning/REQUIREMENTS.md` returns 0.
    - Both documents contain the phrase `raw-difference` (or an unambiguous equivalent naming the original measurement as un-loop-gated) within the corrected passages: `grep -c "raw-difference" .planning/ROADMAP.md .planning/REQUIREMENTS.md` returns 1 or more for each file.
    - Both corrected passages cite `30-CALIBRATION.md` by name.
    - ROADMAP Criterion 3 names both the loop-gated union and the raw-union decomposition, and still names all four device families.
    - `grep -n "ELEV-01\|ELEV-02" .planning/REQUIREMENTS.md` shows both checkboxes still unticked (`- [ ]`), and the coverage table still reads `Pending` for both.
    - `git diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows a small, surgical change confined to the Phase 30 / ELEV passages — no other phase's text moved.
  </acceptance_criteria>
  <done>Both requirement documents state the count the shipped detector actually flags, record what the old number measured, and remain unticked pending verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/streams/` + `data/activities/` → the sweep script | The full committed archive is read by a script that must not write to it |
| the sweep script → `30-CALIBRATION.md` | The single declared write target; the artifact later plans and the checkpoint cite |
| the report → ROADMAP / REQUIREMENTS text | A measurement becomes a requirement figure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-14 | Tampering | the sweep mutating `data/streams/` | mitigate | ASVS V6-adjacent: per-file sha256 rolled into one aggregate digest before and after the sweep, printed into the report, non-zero exit on mismatch (D-16); plus a source scan asserting no `fs` write target under `data/` in the script or the analytics module (T-27-09 pattern extended). |
| T-30-15 | Repudiation | a report that cannot be regenerated at its own stated values | mitigate | Two consecutive runs are diffed and must be byte-identical; every figure is computed by the script, none hand-edited into the document. Phase 27's own gap G-01 was a report whose count could not be reproduced. |
| T-30-16 | Tampering | report figures disagreeing with the shipped detector | mitigate | Thresholds and the loop radius are imported from `dist/analytics/pace-quality.js`, never retyped; asserted by grep. A printed union of the raw figure rather than the gated one is an implementation bug, not a wording slip (RESEARCH Pitfall 1). |
| T-30-17 | Denial of Service | a malformed activity or stream file aborting the sweep | mitigate | Per-file try/catch with warn-and-continue, mirroring the Phase 27 sibling; the report states how many files were skipped and why. |
| T-30-18 | Repudiation | a premature requirement tick | mitigate | Task 3 edits count wording only; both checkboxes and the coverage table stay `Pending`, asserted by grep. This is the v2.1 retrospective's own named process fix. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing; the digest uses Node's built-in `crypto`. 30-RESEARCH.md's Package Legitimacy Audit records no packages for this phase. |
</threat_model>

<verification>
- `npm run compute-elevation-calibration` exits 0 and its digest section reports a match.
- `npx vitest run scripts/compute-elevation-calibration.test.mjs` exits 0.
- Two consecutive report generations diff clean (modulo one timestamp line).
- `git status --porcelain data/` is empty.
- Both requirement documents state the loop-gated figures and remain unticked.
</verification>

<success_criteria>
- Every archive-wide figure ELEV-02 asks for is published, each against a named denominator.
- The loop radius is derived from the archive and justified, not asserted.
- The carry-forward finding is disclosed in words without changing a threshold.
- `data/streams/` is proved byte-unchanged by a digest that cannot agree with itself.
- The requirement documents no longer assert a number the detector does not flag.
</success_criteria>

<output>
Create `.planning/phases/30-elevation-quality-signal/30-04-SUMMARY.md` when done.
Record: every live figure against the research table with its delta and whether the delta is archive
growth or behavioral; the two stream digests; the verbatim idempotence diff; the verbatim digest-gate
failure demonstration; and the exact corrected sentences from ROADMAP and REQUIREMENTS, which plan
30-08's checkpoint reads against the report.
</output>
