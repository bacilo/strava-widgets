---
phase: 30-elevation-quality-signal
plan: 03
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/analytics/compute-dashboard-index.ts
  - src/dashboard/data/pace-quality-client.ts
  - src/dashboard/data/pace-quality-client.test.ts
  - scripts/verify-dashboard-publish.mjs
autonomous: true
requirements: [ELEV-01, ELEV-02]

must_haves:
  decisions: [D-01, D-02, D-05, D-06]
  truths:
    - "D-05: elevation rides the existing per-activity compute step, the existing index row and the existing shard — no new compute step, no new file, no second fetch"
    - "D-01: start_latlng and end_latlng reach the detector from the activity file the loop already opens, with zero additional file reads"
    - "Every published index row carries a quality.elevation object, and the publish gate fails if a partial rollout ships"
    - "D-02: the shard client parses elevation tolerantly — a missing or malformed elevation object degrades to not-computable rather than throwing or fabricating a clear reading"
    - "D-06: the published totals for the anySevere composite are unchanged by this phase"
  artifacts:
    - path: "src/analytics/compute-dashboard-index.ts"
      provides: "startLatlng/endLatlng on the qualityMetadata slice"
      contains: "startLatlng: activity.start_latlng"
    - path: "src/dashboard/data/pace-quality-client.ts"
      provides: "parseElevationSignal with its own three-member tier validation"
      contains: "parseElevationSignal"
    - path: "scripts/verify-dashboard-publish.mjs"
      provides: "the published-row gate extended to the sixth quality sub-key"
      contains: "elevation"
  key_links:
    - from: "src/analytics/compute-dashboard-index.ts qualityMetadata"
      to: "activity.start_latlng / activity.end_latlng"
      via: "the already-open StravaActivity in the existing per-activity loop"
      pattern: "endLatlng: activity.end_latlng"
    - from: "data/dashboard/index.json rows"
      to: "quality.elevation"
      via: "the existing whole-object row write, schemaVersion unchanged"
      pattern: "quality.elevation"
---

<objective>
Land elevation on the shipped index row and the shipped shard through the machinery that already
exists, parse it tolerantly on the client, and extend the publish gate so a partial rollout fails
instead of shipping.

Purpose: D-05's whole point is that one place answers "is this activity trustworthy". This plan is
where that stops being an interface claim and becomes a published artifact — it is what plans 30-05,
30-06 and 30-07 read from, and what plan 30-08's checkpoint compares the page against.

Output: a regenerated local index whose every row carries `quality.elevation`, a tolerant client
parse, and a publish check that fails on a missing sixth sub-key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-elevation-quality-signal/30-CONTEXT.md
@.planning/phases/30-elevation-quality-signal/30-PATTERNS.md
@.planning/phases/30-elevation-quality-signal/30-01-SUMMARY.md
@src/analytics/compute-dashboard-index.ts
@src/dashboard/data/pace-quality-client.ts

<interfaces>
<!-- The exact block to extend, compute-dashboard-index.ts:292-306. Two added keys, nothing else
     changes; `activity.start_latlng`/`activity.end_latlng` are already typed `number[] | undefined`
     on StravaActivity (src/types/strava.types.ts:33-34) and the activity file is already open. -->

```typescript
const qualityMetadata: ActivityQualityMetadata = {
  deviceName: activity.device_name,
  sourceProvider: activity.source_provider,
  elapsedTimeSec: activity.elapsed_time,
  movingTimeSec: activity.moving_time,
  // NEW (D-01):
  startLatlng: activity.start_latlng,
  endLatlng: activity.end_latlng,
};
```

<!-- The tolerant-parse fallback shape to copy, pace-quality-client.ts:137-167: every sub-signal is
     `parseX(raw.x) ?? <not-computable literal>`. `parseDecimationSignal` (68-77) is the per-signal
     total-parse template. -->

<!-- The publish gate, scripts/verify-dashboard-publish.mjs:22 — one shared constant drives both the
     index-row check and the shard-sample check:
       const QUALITY_SUB_KEYS = ['decimation', 'gapProfile', 'impossibleSamples', 'deviceEra', 'elapsedVsMoving'];
     Its failure message and its pass message both say "five named sub-keys". -->

<!-- Research-measured baselines to compare the regenerated index against (re-derive; the archive
     grows nightly): 1,890 rows; 1,865 with a computable stream; 25 not-computable; anySevere 299
     (decimation 154 / gapProfile 127 / impossibleSamples 31); elevation union expected near 60,
     sub-ground 11, loop-gated drift 21, vertical rate 39, drift not-computable 207. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass the loop test's position through the existing metadata slice and regenerate</name>
  <files>src/analytics/compute-dashboard-index.ts</files>
  <read_first>
    - `src/analytics/compute-dashboard-index.ts:255-320` — the unconditional `streamForQuality` read,
      the `qualityMetadata` assembly, the two calls into `computePaceQualitySignals` and
      `buildPaceQualityShard`, and the `qualityAnySevereCount`/`qualityNotComputableCount` tallies.
    - `src/types/strava.types.ts:25-45` — `start_latlng` / `end_latlng` on `StravaActivity`.
    - `src/analytics/dashboard-index.types.ts:110-140` — `quality: ActivityQualitySignals` on the row
      and the schema-version comment.
    - `.planning/phases/30-elevation-quality-signal/30-01-SUMMARY.md` — the shipped `ElevationSignal`
      shape and `LOOP_RADIUS_M`.
  </read_first>
  <action>
    Add exactly two keys to the `qualityMetadata` literal: `startLatlng: activity.start_latlng` and
    `endLatlng: activity.end_latlng`. Nothing else in this file changes — no new file read, no new
    try/catch branch, no new import, no change to `DASHBOARD_INDEX_SCHEMA_VERSION` (the field is
    additive, and this project's convention keeps `schemaVersion` at 1 for additive index fields).
    The `streamForQuality` read above already supplies the `alt` array the three detectors need.

    Optionally add an elevation tally beside the existing `qualityAnySevereCount` /
    `qualityNotComputableCount` counters if this file already prints a per-signal summary line; if it
    does, add elevation to that summary using its own counter and do NOT fold it into
    `qualityAnySevereCount` (D-06).

    Then regenerate locally and sanity-check the result against the research baselines:
    `npm run build && npm run compute-dashboard-index`. From the regenerated
    `data/dashboard/index.json`, count with `node -e` (read-only): total rows; rows whose
    `quality.elevation` is present; rows whose `quality.elevation.tier === 'severe'`; rows per mode
    (`subGround.flagged`, `closureDrift.state === 'flagged'`, `verticalRate.flagged`); rows whose
    `closureDrift.state === 'not-computable'`; and `totals.qualityAnySevere`. Compare each against
    the `<interfaces>` baselines. The anySevere total must be exactly what it was before this phase
    — if it moved, stop: elevation has leaked into the composite, which is the D-06 failure mode.
    Small movements in the elevation cohorts themselves are archive drift and are fine; record them.
  </action>
  <verify>
    <automated>npm run build && npm run compute-dashboard-index && node scripts/compute-pace-quality-recount.mjs --expect 299</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "startLatlng: activity.start_latlng" src/analytics/compute-dashboard-index.ts` returns 1 and `grep -c "endLatlng: activity.end_latlng"` returns 1.
    - `git diff src/analytics/compute-dashboard-index.ts` adds no `readJson`, no `readFile` and no new `import` line.
    - `grep -c "DASHBOARD_INDEX_SCHEMA_VERSION" src/analytics/dashboard-index.types.ts` is unchanged and its value is still 1.
    - `node -e` over the regenerated `data/dashboard/index.json` prints: rows with `quality.elevation` present equals total rows; severe-elevation count within a stated few of 60; sub-ground, loop-gated drift and vertical-rate counts within a stated few of 11 / 21 / 39; `closureDrift.state === 'not-computable'` within a stated few of 207. All six numbers recorded in the summary.
    - `node scripts/compute-pace-quality-recount.mjs --expect 299` prints `PASS` and its per-signal line still reads decimation 154 / gapProfile 127 / impossibleSamples 31 (or the live values, with any drift stated and explained as archive growth, never as a threshold change).
    - `git status --porcelain data/streams/` is empty.
  </acceptance_criteria>
  <done>Every regenerated row carries an elevation signal computed from the position the loop already had, and the Phase 27 composite is unmoved.</done>
</task>

<task type="auto">
  <name>Task 2: Tolerant client parse for the sixth signal</name>
  <files>src/dashboard/data/pace-quality-client.ts, src/dashboard/data/pace-quality-client.test.ts</files>
  <read_first>
    - `src/dashboard/data/pace-quality-client.ts:60-90` — `parseDecimationSignal` and the
      `VALID_TIERS` / `parseTier` helpers.
    - `src/dashboard/data/pace-quality-client.ts:125-230` — `parseActivityQualitySignals`, the shard
      parse, and the `?? not-computable` fallback for each sub-signal.
    - `src/dashboard/data/pace-quality-client.test.ts` — the existing malformed-input test
      convention.
    - `.planning/phases/30-elevation-quality-signal/30-01-SUMMARY.md` — the shipped `ElevationSignal`
      shape, including whether drift `deltaM` is signed.
  </read_first>
  <action>
    Add `parseElevationSignal(raw: unknown)` following `parseDecimationSignal`'s total, never-throwing
    shape: an `isPlainObject` guard, then per-field validation, returning `null` on anything it does
    not recognize so the caller's `??` fallback applies.

    Its tier validation uses a NEW sibling set, `VALID_ELEVATION_TIERS`, containing exactly `severe`,
    `none` and `not-computable`. Do not reuse `VALID_TIERS` — that set contains `'minor'`, which D-07
    rejects for elevation, and reusing it would let a `'minor'` value survive the parse into a type
    that has no such member.

    The drift sub-object's `state` validates against exactly `flagged`, `clear`, `not-computable`.
    Numeric fields (`minAltM`, `deltaM`, `startEndDistM`, `worstRateMps`, `violatingSamples`) accept a
    finite number or `null` and coerce nothing — a string, `NaN` or `Infinity` becomes `null`, never
    `0` (T-26-02).

    Wire it into `parseActivityQualitySignals` as the sixth entry, with the fallback literal being
    the whole-signal not-computable value: tier `not-computable`, `subGround.flagged` false with
    `minAltM: null`, `closureDrift.state` `not-computable` with both values null, `verticalRate`
    unflagged with nulls. An index row or shard written before this phase has no `elevation` key at
    all, and must parse to that fallback rather than throwing — this is the same stale-row failure
    mode Phase 26's CR-02 fixed for `paceDisagreement`.

    If the shard carries elevation evidence fields (the capped violating-sample list, the resolved
    loop radius, the measured start/end distance) alongside `signals`, parse them with the same
    tolerance and the same cap discipline the existing evidence fields use.

    Extend `src/dashboard/data/pace-quality-client.test.ts` with cases for: a well-formed elevation
    object round-tripping every field; `elevation` absent entirely (the stale-artifact case) parsing
    to not-computable with no throw; `elevation` present but with `tier: 'minor'` rejected to the
    fallback; each numeric field supplied as a string, `NaN` and `Infinity`, each becoming `null` and
    never `0`; and a malformed `closureDrift.state` rejected to the fallback.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/data/pace-quality-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "parseElevationSignal" src/dashboard/data/pace-quality-client.ts` returns at least 2 (definition plus call site).
    - `grep -c "VALID_ELEVATION_TIERS" src/dashboard/data/pace-quality-client.ts` returns at least 2, and the set literal contains no `'minor'`: `grep -A3 "VALID_ELEVATION_TIERS" src/dashboard/data/pace-quality-client.ts | grep -c "minor"` returns 0.
    - `npx vitest run src/dashboard/data/pace-quality-client.test.ts` exits 0 and its output includes a test naming the absent-`elevation` stale-row case.
    - A test asserts that a `NaN` or string `minAltM` parses to `null`, not `0`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A stale or malformed artifact degrades to an explicit not-computable elevation on the client instead of throwing or fabricating a healthy reading.</done>
</task>

<task type="auto">
  <name>Task 3: Fail the publish gate on a partial elevation rollout</name>
  <files>scripts/verify-dashboard-publish.mjs</files>
  <read_first>
    - `scripts/verify-dashboard-publish.mjs:18-30` — `QUALITY_SUB_KEYS` and its doc comment.
    - `scripts/verify-dashboard-publish.mjs:290-350` — the index-row quality check, including both
      the failure and the pass message text that say "five named sub-keys", and the
      `rawDeviceName` defence-in-depth scan.
    - `scripts/verify-dashboard-publish.mjs:755-800` — the per-activity shard sample check that
      shares the same constant.
  </read_first>
  <action>
    Add `'elevation'` to `QUALITY_SUB_KEYS`, making it six. Update the constant's doc comment and
    BOTH message strings that currently say "five named sub-keys" to say six — a pass message that
    claims to have checked five while checking six is a false receipt, and this project has been
    bitten by stale prose in generated and asserted output before.

    Do not add any elevation-specific tier logic to this script and do not touch its `anySevere`
    handling: the gate's job here is presence of the sixth sub-key on every row and on the sampled
    shard, nothing more.

    Demonstrate the gate catching a partial rollout: take the built publish directory's
    `data/dashboard/index.json`, delete `quality.elevation` from exactly one row in a COPY of that
    file (never in `data/`), point the script at the copy or temporarily stage the copy in
    `dist/widgets/`, run `npm run verify-dashboard`, and confirm it exits non-zero naming that
    activity id. Restore and confirm it exits 0. Record both runs verbatim.
  </action>
  <verify>
    <automated>npm run build-widgets && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'elevation'" scripts/verify-dashboard-publish.mjs` returns at least 1 and `QUALITY_SUB_KEYS` has six members.
    - `grep -ci "five named sub-keys" scripts/verify-dashboard-publish.mjs` returns 0.
    - `npm run verify-dashboard` exits 0 against a freshly built publish directory.
    - The deliberate single-row deletion run is recorded verbatim: non-zero exit, the message naming the offending activity id, and the restored run exiting 0.
    - `git status --porcelain data/` is empty after the demonstration — the mutation happened on a copy, never in `data/`.
  </acceptance_criteria>
  <done>A build that ships elevation on some rows but not others fails the publish gate, demonstrated rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/activities/{id}.json` → `qualityMetadata` | Externally-derived position metadata enters the compute step |
| shipped `index.json` / shard → browser client parse | Possibly stale or malformed published artifacts reach the SPA |
| built publish directory → the publish gate | The last automated point before a partial rollout ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09 | Denial of Service | client parse of a stale row with no `elevation` key | mitigate | `parseElevationSignal` returns `null` into a not-computable fallback; the absent-key case is tested explicitly, mirroring Phase 26's CR-02 fix for `paceDisagreement`. |
| T-30-10 | Tampering | a `'minor'` tier surviving the client parse into a three-member union | mitigate | `VALID_ELEVATION_TIERS` is its own set without `'minor'`; asserted by grep and by a rejection test. |
| T-30-11 | Information Disclosure | partial rollout shipping some rows without the signal | mitigate | `QUALITY_SUB_KEYS` extended to six on both the row check and the shard-sample check; demonstrated catching a single-row deletion. |
| T-30-12 | Tampering | elevation leaking into the published `anySevere` totals | mitigate | Task 1 asserts `compute-pace-quality-recount.mjs --expect 299` still prints PASS against the regenerated index; the elevation tally, if added, uses its own counter. |
| T-30-13 | Tampering | writing to `data/streams/` during regeneration | mitigate | `git status --porcelain data/streams/` asserted empty after `compute-dashboard-index`; the verify demonstration mutates a copy only. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `npm run build && npm run compute-dashboard-index` completes and every row carries `quality.elevation`.
- `node scripts/compute-pace-quality-recount.mjs --expect 299` prints PASS.
- `npx vitest run src/dashboard/data/pace-quality-client.test.ts`, `npm test` and `npx tsc --noEmit` exit 0.
- `npm run verify-dashboard` exits 0, and its deliberate-deletion run exits non-zero.
- `git status --porcelain data/streams/` is empty.
</verification>

<success_criteria>
- Elevation is published on every index row and every shard through the existing compute step, with
  no new fetch, file or schema version.
- Stale and malformed artifacts degrade to an explicit not-computable rather than throwing.
- A partial rollout fails the publish gate, shown failing.
- The Phase 27 composite total is unmoved.
</success_criteria>

<output>
Create `.planning/phases/30-elevation-quality-signal/30-03-SUMMARY.md` when done.
Record: the six counts read off the regenerated index (total rows, elevation present, severe, per
mode, drift not-computable) with their deltas from the research baselines; the recount script's
stdout verbatim; the deliberate-deletion verify run verbatim; and one severe-elevation activity id
plus one healthy activity id plus one drift-not-computable activity id, with their shard-file values,
for plans 30-05, 30-06 and 30-08 to read back against.
</output>
