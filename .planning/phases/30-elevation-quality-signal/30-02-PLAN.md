---
phase: 30-elevation-quality-signal
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/analytics/pace-fixtures.ts
  - src/analytics/pace-fixtures.test.ts
autonomous: true
requirements: [ELEV-02]

must_haves:
  decisions: [D-14]
  truths:
    - "D-14: the archive's own worst case for each mode is pinned as a real fixture whose expected values are re-verified against the live committed stream on every test run"
    - "D-14: 4556693525 is sub-ground only, 4745489664 is drift only, 3149636661 fires both drift and vertical rate — so 3149636661 can never be mistaken for a single-mode isolation fixture"
    - "A pinned expected value that drifts away from the live archive is a loud test failure, not a silently stale table row"
  artifacts:
    - path: "src/analytics/pace-fixtures.ts"
      provides: "PINNED_FIXTURES rows for 4745489664 and 3149636661, plus elevation expected keys on the existing worked-example row"
      contains: "4745489664"
    - path: "src/analytics/pace-fixtures.test.ts"
      provides: "assertExpectedProperties cases for minAltM, driftDeltaM, startEndDistM, worstRateMps"
      contains: "case 'worstRateMps'"
  key_links:
    - from: "src/analytics/pace-fixtures.test.ts assertExpectedProperties"
      to: "subGroundSignal / closureDriftSignal / verticalRateSignal"
      via: "the new switch cases run the shipped detectors against loadPinnedStream + loadPinnedActivity"
      pattern: "closureDriftSignal\\("
---

<objective>
Pin the archive's three real elevation exemplars as fixtures whose expected values are re-verified
against the live committed data on every test run, and extend the total-switch that makes an
unverified expected key impossible.

Purpose: the synthetic fixtures from plan 30-01 prove the mechanism; these prove it on this archive.
ELEV-02 explicitly rejects a detector validated only against its scoping exemplar, and
`assertExpectedProperties` is the machinery that converts "this number was true once" into "this
number is true now".

Output: two new `PINNED_FIXTURES` rows, four new elevation properties on the existing
`worked-example` row, and four new switch cases that run the shipped detectors against real data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-elevation-quality-signal/30-CONTEXT.md
@.planning/phases/30-elevation-quality-signal/30-RESEARCH.md
@.planning/phases/30-elevation-quality-signal/30-01-SUMMARY.md
@src/analytics/pace-fixtures.ts

<interfaces>
<!-- Research-measured values, 2026-09-18, against the live committed archive. RE-DERIVE these
     before writing them: the archive grows nightly and has drifted by single digits between
     phases before. If a value has moved, use the live value and record the delta. -->

| activityId | fixture role | minAltM | driftDeltaM (alt[last]-alt[0]) | startEndDistM | worstRateMps | sampleCount |
|---|---|---|---|---|---|---|
| 4556693525 | existing `worked-example`, sub-ground exemplar | -282.0 | -5.6 | 0 | 3.3 | 1682 |
| 4745489664 | drift exemplar (loop) | +4.2 | -197.6 | 0 | 2.3 | 1712 |
| 3149636661 | vertical-rate exemplar (ALSO drifts) | 19.8 | -172.6 | 0 | 80.4 | 969 |

<!-- The row template to copy verbatim, changing only values (pace-fixtures.ts:413-419): name,
     activityId, deviceFamily, streamSource, why, expected. -->

<!-- The total switch that throws on an unrecognized key (pace-fixtures.test.ts:197-256, the throw
     at ~252). Adding a fixture row with a new `expected` key BEFORE its switch case exists produces
     `unhandled expected key "minAltM"` — a hard failure by design (RESEARCH Pitfall 2). Both edits
     belong in the same task. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the three exemplars' live values, then pin them with their switch cases</name>
  <files>src/analytics/pace-fixtures.ts, src/analytics/pace-fixtures.test.ts</files>
  <read_first>
    - `src/analytics/pace-fixtures.ts:296-330` — the `PinnedFixture` interface and the head of
      `PINNED_FIXTURES`.
    - `src/analytics/pace-fixtures.ts:405-425` — the `worked-example` row (activity 4556693525) in
      full, the exact template for the two new rows.
    - `src/analytics/pace-fixtures.ts:445-482` — `loadPinnedStream`, `loadPinnedActivity`,
      `PACE_FIXTURE_NAMES`.
    - `src/analytics/pace-fixtures.test.ts:190-270` — `assertExpectedProperties` in full, including
      the `default:` throw, and the `it.each(PINNED_FIXTURES)` call site that drives it.
    - `.planning/phases/30-elevation-quality-signal/30-01-SUMMARY.md` — the shipped `ElevationSignal`
      shape and the exported detector names.
    - `.planning/phases/30-elevation-quality-signal/30-RESEARCH.md` § D-14 and § Common Pitfalls 2, 3.
  </read_first>
  <action>
    First re-derive, do not trust the table. Using `node` against the live `data/streams/{id}.json`
    and `data/activities/{id}.json` for 4556693525, 4745489664 and 3149636661, compute the four
    properties the fixtures will pin: minimum finite `alt`, signed `alt[last] - alt[0]`, the
    haversine start/end distance, and the worst `|Δalt|/Δt` over pairs with positive `Δt`. Use the
    shipped detectors from plan 30-01 (import from `dist/` after `npm run build`, or a throwaway
    script under `/tmp` that imports the compiled module) rather than re-implementing the arithmetic
    — a fixture verified by a second implementation verifies the second implementation. Record each
    live value and state whether it matches the research table; use the LIVE value in the fixture row
    and note any delta in the summary.

    Then, in the same commit (RESEARCH Pitfall 2 — the switch throws on an unknown key, so splitting
    these leaves the suite red):

    In `src/analytics/pace-fixtures.ts`:
      - Extend the existing `worked-example` row's `expected` object with `minAltM`, `driftDeltaM`,
        `startEndDistM` and `worstRateMps` at their live values, leaving its existing `spanSec`,
        `elapsedTimeSec`, `distanceM` and `zeroAdvanceFraction` untouched. Extend its `why` text to
        name the sub-ground property, keeping the existing wording intact.
      - Add a row for activity `4745489664` — the loop-gated drift exemplar. Its `expected` carries
        `driftDeltaM`, `startEndDistM` and `minAltM`; its `why` states that it drifts −197.6 m over a
        loop whose endpoints are the same point, and that its minimum altitude and worst vertical
        rate both stay clear of their thresholds, making it the archive's drift-isolated real.
      - Add a row for activity `3149636661` — the vertical-rate exemplar. Its `expected` carries
        `worstRateMps`, `driftDeltaM`, `startEndDistM` and `minAltM`. Its `why` MUST state that this
        activity fires BOTH vertical rate and loop-gated drift, so it is a reported real exemplar and
        never a single-mode isolation fixture (RESEARCH Pitfall 3) — the isolation proofs use plan
        30-01's three synthetics. Carrying `driftDeltaM` in `expected` is deliberate: it makes the
        dual-mode fact machine-checked rather than a comment.
      - Set each new row's `deviceFamily` and `streamSource` from the activity's own
        `data/activities/{id}.json` and the stream's own source field — do not guess them.
      - Add both names to `PACE_FIXTURE_NAMES`.

    In `src/analytics/pace-fixtures.test.ts`, extend `assertExpectedProperties`'s switch with
    `case 'minAltM'`, `case 'driftDeltaM'`, `case 'startEndDistM'` and `case 'worstRateMps'`. Each
    case calls the corresponding plan 30-01 detector against `loadPinnedStream(fixture.name)` — and,
    for `driftDeltaM`/`startEndDistM`, against the `start_latlng`/`end_latlng` read out of
    `loadPinnedActivity(fixture.name)` — and compares to the pinned value with a tolerance stated in
    the code (0.1 m for altitudes and distances, 0.1 m/s for rates) rather than an exact float
    equality. Leave the `default:` throw exactly as it is; it is the mechanism that made this task
    necessary and must keep working.

    Do not widen the tolerance to make a value pass. If a live value has moved beyond the tolerance
    from the research table, that is archive drift: update the pinned value, keep the tolerance, and
    record the movement in the summary.
  </action>
  <verify>
    <automated>npx vitest run src/analytics/pace-fixtures.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "4745489664\|3149636661" src/analytics/pace-fixtures.ts` returns at least 2, and `grep -n "minAltM" src/analytics/pace-fixtures.ts` shows the key present on the `worked-example` row.
    - `grep -c "case 'minAltM'\|case 'driftDeltaM'\|case 'startEndDistM'\|case 'worstRateMps'" src/analytics/pace-fixtures.test.ts` returns 4.
    - `grep -c "unhandled expected key" src/analytics/pace-fixtures.test.ts` returns 1 — the `default:` throw is intact.
    - `npx vitest run src/analytics/pace-fixtures.test.ts` exits 0, and its output names all three activities.
    - Demonstrated failing, run and recorded verbatim: change one pinned `worstRateMps` by 10 m/s, run the test file, observe a named failure quoting both the pinned and the measured value; restore. A pinned value that cannot fail is not verification.
    - The `3149636661` row's `why` text contains both `drift` and a statement that it is not an isolation fixture: `grep -A6 "3149636661" src/analytics/pace-fixtures.ts | grep -ci "isolation"` returns at least 1.
    - The four live re-derived values per activity are recorded in the summary alongside the research table's values, with any delta stated.
    - `npm test` exits 0; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Three real exemplars are pinned with live-verified elevation properties, re-checked against the committed archive on every run, and the dual-mode nature of 3149636661 is machine-checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live `data/` archive → pinned fixture table | A stale expected value silently certifies a detector against a number that is no longer true |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Repudiation | stale pinned expected value | mitigate | `assertExpectedProperties` re-verifies every pinned property against the live committed stream on each run; the `default:` throw makes an unverified key impossible. Demonstrated failing by perturbing a pinned value. |
| T-30-07 | Tampering | 3149636661 misused as a single-mode isolation fixture | mitigate | Its `expected` carries `driftDeltaM` as well as `worstRateMps`, so its dual-mode nature is machine-checked, and its `why` states the restriction in words (RESEARCH Pitfall 3). |
| T-30-08 | Tampering | writing under `data/` while re-deriving | mitigate | Re-derivation runs from a throwaway script outside the project tree or a read-only `node -e`; `git status --porcelain data/` asserted empty afterwards. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `npx vitest run src/analytics/pace-fixtures.test.ts` exits 0.
- `npm test` and `npx tsc --noEmit` exit 0.
- `git status --porcelain data/` is empty.
- The perturbation demonstration is recorded verbatim.
</verification>

<success_criteria>
- The archive's worst sub-ground, worst drift and worst vertical-rate activities are pinned with
  live-verified values.
- No pinned elevation property can go stale without turning the suite red.
- 3149636661's dual-mode nature is asserted, not merely commented.
</success_criteria>

<output>
Create `.planning/phases/30-elevation-quality-signal/30-02-SUMMARY.md` when done.
Record: the twelve live re-derived values (four properties × three activities) against the research
table with deltas; the verbatim output of the perturbation demonstration; and the two new fixture
names, which plan 30-08's checkpoint quotes when reading numbers back off the page.
</output>
