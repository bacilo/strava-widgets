---
phase: 30-elevation-quality-signal
plan: 08
type: execute
wave: 4
depends_on: ["30-02", "30-04", "30-05", "30-06", "30-07"]
files_modified:
  - .planning/phases/30-elevation-quality-signal/30-VALIDATION.md
autonomous: false
requirements: [ELEV-01, ELEV-02]

must_haves:
  decisions: [D-04, D-10, D-11, D-12, D-17]
  truths:
    - "D-17: a human reads the elevation badge and the three detail lines off a rendered page served from a digest that was verified from the fetched bytes, not from the build log"
    - "D-17: every checkpoint row states its expected value from an independently derived source — the committed shard, 30-CALIBRATION.md or the recount script — never from the page agreeing with itself"
    - "D-17: every row carries a written CAN PASS and CAN FAIL line before the checkpoint runs, so a row that cannot fail is struck rather than run"
    - "D-06: an elevation-only-flagged activity is visibly absent from the has-any-severe-signal filter, proving elevation never joined the composite on the shipped surface"
    - "D-12: a healthy activity's Elevation Gain stat card carries no caveat while a flagged activity's does"
    - "A failing row is recorded verbatim as a gap and is not patched under checkpoint pressure"
    - "Requirements are ticked only after every row mapped to them has passed"
  artifacts:
    - path: ".planning/phases/30-elevation-quality-signal/30-VALIDATION.md"
      provides: "the drafted rows, their bidirectional reachability proofs, and the recorded verdicts"
      contains: "Round 1 Checkpoint"
  key_links:
    - from: "30-VALIDATION.md checkpoint rows"
      to: "data/stats/pace-quality/{id}.json, 30-CALIBRATION.md, compute-elevation-recount.mjs stdout"
      via: "an independently derived expected value stated per row before the row is run"
      pattern: "independently"
---

<objective>
Close Phase 30 on rendered evidence: a short human browser round covering the row badge, the three
always-on detail lines, the Elevation Gain caveat, the position-unknown drift wording, and the one
thing only the browser can show — that an elevation-only-flagged activity is absent from the
has-any-severe-signal filter.

Purpose: D-17 keeps this phase's checkpoint small because there is no new interaction, but does not
skip it, because this project has shipped rendering defects behind green gates three times. Every row
compares the page against a number produced somewhere other than the page.

Output: `30-VALIDATION.md` with drafted rows, a reachability audit, and verdicts recorded verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-elevation-quality-signal/30-VALIDATION.md
@.planning/phases/30-elevation-quality-signal/30-CALIBRATION.md
@.planning/phases/30-elevation-quality-signal/30-03-SUMMARY.md
@.planning/phases/30-elevation-quality-signal/30-05-SUMMARY.md
@.planning/phases/30-elevation-quality-signal/30-06-SUMMARY.md
@.planning/phases/30-elevation-quality-signal/30-07-SUMMARY.md
@.planning/ROADMAP.md

<interfaces>
<!-- The independently produced figures a row may compare the browser against. A row comparing the
     browser against another part of the browser is not evidence. -->

| figure | produced by | plan |
|---|---|---|
| per-mode and union elevation cohorts, computed from the streams by the classifier | `npm run compute-elevation-calibration` → `30-CALIBRATION.md` | 30-04 |
| the same cohorts, recounted from the shipped index with no classifier import | `node scripts/compute-elevation-recount.mjs` | 30-07 |
| one activity's own elevation numbers | the committed `data/stats/pace-quality/{id}.json` | 30-03 |
| the Phase 27 composite, unmoved | `node scripts/compute-pace-quality-recount.mjs --expect N` | untouched Phase 27 script |
| expected badge and row strings | recorded verbatim in `30-05-SUMMARY.md` and `30-06-SUMMARY.md` | 30-05, 30-06 |

<!-- Project checkpoint hazards, each of which has cost a round here before:
     - `build-widgets` skips work on mtime and silently no-ops on a locally edited dist/ file —
       verify the SERVED digest, computed from fetched bytes, never the build log.
     - Serving from 127.0.0.1 is not sufficient; a stale index.html / index.json from a staged build
       has been served during a checkpoint. Hard-reload after every fixture edit.
     - The browser viewport clamps to 500..941; do not draft a row needing a width outside that.
     - Gestures need a human; do not draft a row an agent could claim to have performed.
     - A row must assert reachable extent against an INDEPENDENTLY derived value.
     - A row that cannot fail is as broken as one that cannot pass — check both directions.
     - A fixture that contradicts the requirement is investigated before the row is redrafted; a row
       must never be reshaped until it blesses the defect. -->

<!-- 30-VALIDATION.md carries `status: draft`, `nyquist_compliant: false`, `wave_0_complete: false`
     and a Per-Task Verification Map whose rows this phase's tasks have been filling. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serve a digest-verified build, derive every expected value, and draft the rows</name>
  <files>.planning/phases/30-elevation-quality-signal/30-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/30-elevation-quality-signal/30-05-SUMMARY.md` — the exact badge text produced
      for the severe activity, copied from test output.
    - `.planning/phases/30-elevation-quality-signal/30-06-SUMMARY.md` — the three expected `valueText`
      strings per real activity id and their source shard paths.
    - `.planning/phases/30-elevation-quality-signal/30-07-SUMMARY.md` — the recount stdout, the
      reconciliation table, and the Phase 27 gate's output.
    - `.planning/phases/30-elevation-quality-signal/30-CALIBRATION.md` § Per-mode cohorts, § Union by
      device family, § Loop radius, § Correction of the raw-difference count.
    - `.planning/phases/30-elevation-quality-signal/30-VALIDATION.md` — the Manual-Only Verifications
      table and the frontmatter.
    - `.planning/ROADMAP.md` § Phase 30 — the three success criteria AS CORRECTED by plan 30-04, which
      is what these rows are scored against, not the original wording.
    - `.planning/STATE.md` § Accumulated Context — the prior checkpoint precedents: a discriminator
      that went vacuous, a BLOCKED verdict held against pressure to record PASS, and failures logged
      verbatim rather than patched.
  </read_first>
  <action>
    Build and serve, verifying the served bytes before drafting anything:
      1. `npm run build && npm run compute-dashboard-index && npm run build-widgets`.
      2. `shasum -a 256` the emitted JS asset. If `build-widgets` reported a skip for that asset,
         delete the stale `dist/` output and rebuild — this project's build silently no-ops on a
         locally edited `dist/` file, and a clean build log is not evidence.
      3. Serve the publish directory over HTTP, FETCH the asset URL, and `shasum -a 256` the fetched
         bytes. The SERVED digest is the one that matters; record it beside the local one.
      4. `curl` the served `/data/dashboard/index.json` and confirm its row count, its
         `totals.qualityAnySevere` and its count of `quality.elevation` present all match the local
         file. A fresh bundle served beside a stale index has happened here before.
      5. Re-run `npm run compute-elevation-calibration` and confirm `30-CALIBRATION.md` is unchanged
         by the re-run (the idempotence plan 30-04 proved, re-confirmed against the final build), then
         `node scripts/compute-elevation-recount.mjs` and
         `node scripts/compute-pace-quality-recount.mjs --expect <pre-phase composite>`. Record all
         three outputs verbatim.

    Derive and STATE every expected value before drafting, each from a source other than the page:
      - the severe exemplar id (4556693525 unless `30-03-SUMMARY.md` names another) and its badge and
        three line strings, from its committed shard;
      - a healthy activity id whose elevation tier is `'none'` and whose drift state is `'clear'` on a
        loop, with its three expected numbers;
      - a position-unknown activity id from the drift not-computable cohort, with the exact wording
        expected;
      - an ELEVATION-ONLY activity id: `elevation.tier === 'severe'` while `quality.anySevere` is
        false. Select it by reading the shipped index directly and state how many such activities
        exist. If the count is zero, this row is NOT EXERCISABLE and must be struck with that reason
        recorded — do not substitute an activity that is also severe on a tiering signal, because
        that substitution is exactly how a row stops being able to fail.

    Then append a `## Round 1 Checkpoint (R1-R8)` section to `30-VALIDATION.md` drafting the rows
    below. Every row carries, BEFORE any verdict, an explicit `CAN PASS:` line naming the concrete
    state that would make it pass and a `CAN FAIL:` line naming the concrete state that would make it
    fail. A row whose CAN FAIL line cannot be written is vacuous and is struck or redrafted. Every
    verdict reads `pending` until the developer answers.

      - **R1 — served digest.** The developer confirms the asset filename in DevTools' Network panel
        matches the digest computed from the fetched bytes in step 3.
        CAN FAIL: a stale bundle serves a different asset filename.

      - **R2 — the row badge on all three surfaces.** With the severe exemplar visible, quote its
        elevation badge text VERBATIM from each of the three activity-row surfaces. Expected: the
        string recorded in `30-05-SUMMARY.md`, containing the measured value from the committed
        shard, identical on all three.
        CAN FAIL: no badge on one surface, a badge naming a condition with no number, or a number
        disagreeing with the shard.

      - **R3 — the three detail lines on a flagged activity.** Open the severe exemplar's detail view
        and quote all three elevation lines verbatim. Expected: the three strings recorded in
        `30-06-SUMMARY.md`, whose numbers come from `data/stats/pace-quality/{id}.json`.
        CAN FAIL: fewer than three lines, a blank value, `undefined`/`null`/`NaN` on screen, or a
        number disagreeing with the shard file.

      - **R4 — the three detail lines on a HEALTHY activity, and no caveat.** Open the healthy id and
        quote its three elevation lines. Expected: three lines, each with its own number, none blank
        — a healthy activity visibly says so rather than showing nothing. Then confirm its Elevation
        Gain stat card carries NO badge.
        CAN FAIL: a healthy activity showing no elevation lines at all (silence as good news), or a
        caveat on a card the page does not flag.

      - **R5 — the Elevation Gain caveat on the flagged activity.** On the severe exemplar, confirm
        the Elevation Gain stat card carries the caveat and quote it; confirm no other stat card,
        split column or aggregate on that page is caveated.
        CAN FAIL: no caveat on the flagged card, or a caveat appearing on a second surface D-12
        excludes.

      - **R6 — the drift line's position-unknown wording.** Open the position-unknown id and quote its
        closure-drift line. Expected: it states in words that the start/end position is unknown and
        drift was not checked, with no distance and no delta figure invented.
        CAN FAIL: the line shows a number, reads as healthy, reads identically to the not-a-loop
        exclusion, or is absent.

      - **R7 — D-06 on the shipped surface.** Navigate to the has-any-severe-signal filter and confirm
        the elevation-only activity id is ABSENT from the filtered list, then confirm it IS present
        and badged in the unfiltered list. Independently derived: the filtered count is compared
        against the Phase 27 recount's composite from step 5, not against another browser number.
        CAN FAIL: the elevation-only activity appears under the filter, or the filtered count differs
        from the recount's composite by more than the archive drift between the two runs, which must
        be stated as a number.

      - **R8 — the corrected criteria and the report.** A document read, drafted as such rather than
        pretended to be rendered evidence: the developer confirms that ROADMAP Criterion 1 and
        Criterion 3 and REQUIREMENTS ELEV-01 now state the loop-gated figures, that they record the
        original number as a raw-difference measurement, and that `30-CALIBRATION.md` lists the
        point-to-point exclusions by id and derives the loop radius. Compare the report's union
        against the recount's union from step 5.
        CAN FAIL: a document still stating the raw figure, an exclusion list absent, or the two union
        figures disagreeing without a stated cause.

    Add a `Reachability Audit` sub-section repeating all sixteen CAN PASS / CAN FAIL lines together so
    the audit reads in one place. Any row missing either line is struck before the checkpoint runs.
    Check both directions on every row: a row that cannot fail is as broken as one that cannot pass.

    Also fill in `30-VALIDATION.md`'s Per-Task Verification Map — every row's Task ID, Plan and Wave
    columns — from the tasks this phase actually emitted, and note which rows are discharged by
    automated evidence versus by this checkpoint.

    Do not run the browser rows yourself and do not pre-fill any verdict.
  </action>
  <verify>
    <automated>node scripts/compute-elevation-recount.mjs && node scripts/compute-pace-quality-recount.mjs --expect 299 && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Round 1 Checkpoint" .planning/phases/30-elevation-quality-signal/30-VALIDATION.md` returns 1, with eight rows and every verdict reading `pending`.
    - `grep -c "CAN PASS:" .planning/phases/30-elevation-quality-signal/30-VALIDATION.md` and `grep -c "CAN FAIL:"` each return 16 (eight rows plus the audit's repetition).
    - The served digest, computed from FETCHED bytes, is recorded beside the locally computed one, with a statement of whether they match.
    - The verbatim stdout of `compute-elevation-calibration`, `compute-elevation-recount` and `compute-pace-quality-recount --expect N` is recorded in the section.
    - R2, R3, R4, R5, R6, R7 and R8 each state an expected value AND the non-page source it came from (a shard path, `30-CALIBRATION.md` section, or a script's stdout).
    - The elevation-only activity count is stated as a number; if it is zero, R7 is struck as NOT EXERCISABLE with that reason recorded rather than substituted.
    - The Per-Task Verification Map's Task ID, Plan and Wave columns are filled for every row — `grep -c "^| — |" .planning/phases/30-elevation-quality-signal/30-VALIDATION.md` returns 0.
    - `npm run verify-dashboard` exits 0 and `node scripts/compute-pace-quality-recount.mjs --expect <pre-phase composite>` prints PASS.
    - No verdict is pre-filled and no browser row is claimed as performed.
  </acceptance_criteria>
  <done>Eight rows drafted, each proven able to pass and to fail, each stating an expected value from a source other than the page, against a build whose served digest was computed from fetched bytes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>### CHECKPOINT — Task 2: Round 1 browser verification (R1-R8)</name>
  <files>.planning/phases/30-elevation-quality-signal/30-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/30-elevation-quality-signal/30-VALIDATION.md` § Round 1 Checkpoint — the eight
      drafted rows and the Reachability Audit, in full, before presenting anything.
    - `.planning/ROADMAP.md` § Phase 30 — the three success criteria as corrected by plan 30-04; the
      rows are scored against the corrected wording, not the original.
    - `.planning/phases/30-elevation-quality-signal/30-CALIBRATION.md` — the figures R7 and R8 cite.
    - `.planning/STATE.md` § Carried into the next milestone — the v2.1 process fix that requirements
      are ticked after verification, never before.
  </read_first>
  <action>
    Confirm every automated gate is green before presenting anything: `npm test`, `npx tsc --noEmit`,
    `npm run build-widgets` (grep its output for `css-syntax-error`, zero tolerance),
    `npm run verify-dashboard`, `node scripts/compute-elevation-recount.mjs`, and
    `node scripts/compute-pace-quality-recount.mjs --expect <pre-phase composite>`. If any is red,
    stop — do not present a checkpoint against a red gate.

    Then present the eight rows drafted by Task 1 using the blocks below, with each row's stated
    expected value and its source filled in from `30-VALIDATION.md`. Give the developer the serve
    command and the served asset digest recorded in Task 1.

    Do not perform any browser row yourself and do not pre-fill any verdict. When the reply arrives,
    transcribe it verbatim into `30-VALIDATION.md` § Round 1 Checkpoint, apply the requirement→row map
    in the acceptance criteria, and record any FAIL in a `Gap-Closure Record` sub-section without
    patching it.
  </action>
  <what-built>
    Three altitude detectors — sub-ground on the stream minimum below −50 m, loop-gated barometric
    closure drift above 60 m within a 100 m radius derived from the archive's own bimodal start/end
    distance distribution, and per-sample vertical rate above 5 m/s — computed in the existing CI
    step, landed as a sixth signal on the existing index row and the existing evidence shard with no
    new fetch and no new file (plans 30-01, 30-03). One severe-only badge per row on all three
    activity-row surfaces, three always-on lines and an Elevation Gain caveat on the detail view
    (30-05, 30-06). An archive-wide calibration report with the loop radius derived and justified, the
    loop-gated overlap matrix, the union with its device-family breakdown, the point-to-point
    exclusions by id, the carry-forward-fill finding, and a sha256 digest proving `data/streams/`
    byte-unchanged (30-04). An independent recount that never imports the classifier (30-07). Both
    requirement documents corrected from the raw start/end difference count to the loop-gated one
    (30-04). Elevation deliberately does NOT join the has-any-severe-signal composite.

    Everything automatable is already automated and green: the full vitest suite, `tsc --noEmit`,
    `build-widgets` with zero `css-syntax-error`, `verify-dashboard` including a demonstrated-failing
    partial-rollout check, both recount scripts, and the Phase 27 composite gate. What is left needs a
    real browser and a human.
  </what-built>
  <how-to-verify>
    Serve the publish directory and HARD-RELOAD before starting — `127.0.0.1` alone is not sufficient
    here; a stale `index.html` / `index.json` from a staged build has been served during a checkpoint
    on this project before. Hard-reload again after any navigation that changes the dataset. Keep the
    viewport between 500 and 941 px.

    Then work R1 through R8 as drafted in `30-VALIDATION.md` § Round 1 Checkpoint. Each row states
    what to do, what value to expect, and where that expected value came from — a committed shard
    file, `30-CALIBRATION.md`, or a script's stdout. Please compare against the stated value rather
    than against what the page shows elsewhere.

    For each row reply with one of four verdicts, quoting what you actually saw rather than
    summarizing:
      - **PASS** — observed matches the stated expectation. Quote the number or the text.
      - **FAIL** — observed contradicts it. Quote what you saw. Do not describe a fix.
      - **BLOCKED** — you could not gather the evidence the row asks for (a width you cannot reach, a
        browser refused, the page would not load). BLOCKED is not FAIL and is not PASS.
      - **NOT EXERCISABLE** — the row's precondition does not exist in this archive at all.

    A partial answer is BLOCKED, not PASS. If a row's evidence is at a different activity, width or
    theme than the row asks for, that is BLOCKED — the substitution is exactly how a green gate has
    shipped a defect here before.
  </how-to-verify>
  <resume-signal>
    Reply per row: `R1: PASS — "…"`, `R4: FAIL — "…"`, etc. A single blanket approval is accepted and
    will be recorded AS a blanket approval, with a note that no per-row detail was given — it will not
    be expanded into invented per-row observations.
  </resume-signal>
  <verify>
    <human-check>Eight verdicts recorded verbatim in 30-VALIDATION.md § Round 1 Checkpoint, each in the developer's own quoted words.</human-check>
  </verify>
  <acceptance_criteria>
    - Every verdict is transcribed VERBATIM into `30-VALIDATION.md`, not paraphrased. A blanket approval is recorded as a blanket approval and is not expanded per row.
    - Requirement→row mapping, stated before ticking: ELEV-01 ← R2, R3, R4, R5, R6, R7; ELEV-02 ← R7, R8 plus the automated archive-wide evidence in `30-CALIBRATION.md` and `compute-elevation-recount.mjs`, cited by file and command.
    - Requirements are ticked in `REQUIREMENTS.md` ONLY after every row mapped to that requirement has PASSED. A BLOCKED row does not tick. This is the project's standing rule and the v2.1 retrospective's named process fix; three requirements were ticked, reverted and re-ticked during v2.1 for exactly this reason.
    - Any FAIL is recorded verbatim as a gap in a `Gap-Closure Record` sub-section and is NOT patched in this plan. A failing row is logged; the next planning round diagnoses it.
    - If a row's stated expected value turns out to contradict the requirement, the contradiction is investigated and recorded BEFORE any row is redrafted — a redrafted row must never be the thing that blesses a defect.
    - `30-VALIDATION.md` frontmatter is updated: `status` to `passed` only if every row is PASS, otherwise `partial`; `nyquist_compliant` and `wave_0_complete` to `true` only in the all-PASS case.
    - If R7's figures disagree, the delta and its cause are recorded before any verdict is accepted — archive drift between runs is a legitimate cause and must be quantified; elevation appearing in the composite is not, and is a FAIL.
  </acceptance_criteria>
  <done>Eight verdicts recorded verbatim; requirements ticked only where every mapped row passed; any failure logged as a gap rather than patched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built publish directory → local HTTP server → browser | The only place the assembled page is exercised as shipped |
| developer's observation → `30-VALIDATION.md` | Human evidence enters the record and drives requirement ticks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-34 | Tampering | stale served bundle | mitigate | R1 compares the SERVED asset digest, computed from fetched bytes, against the locally computed one; the served `index.json` row count, composite total and elevation-present count are re-checked by `curl`. Hard-reload instructed explicitly. |
| T-30-35 | Repudiation | verdict fabrication | mitigate | Verdicts transcribed verbatim; a blanket approval recorded as one rather than expanded; no browser row may be claimed as performed by an agent. |
| T-30-36 | Tampering | a vacuous or unsatisfiable discriminator | mitigate | Every row carries a written CAN PASS / CAN FAIL pair collected in a Reachability Audit, checked in BOTH directions; a row missing either is struck before the checkpoint runs. |
| T-30-37 | Tampering | a row substituting a convenient activity for the named one | mitigate | R7 states the elevation-only count as a number and is struck as NOT EXERCISABLE if it is zero rather than substituting an also-tiering-severe activity; the instructions classify any substitution as BLOCKED. |
| T-30-38 | Repudiation | a redrafted row blessing a defect | mitigate | A contradiction between a stated expected value and the requirement must be investigated and recorded before any redraft, per the project's own recorded lesson. |
| T-30-39 | Repudiation | a premature requirement tick | mitigate | The requirement→row map is stated before ticking; a BLOCKED row does not tick; ticks happen after verification, never before. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `npm test`, `npx tsc --noEmit`, `npm run build-widgets` (zero `css-syntax-error`) and
  `npm run verify-dashboard` all exit 0 before the checkpoint is presented.
- `node scripts/compute-elevation-recount.mjs` and
  `node scripts/compute-pace-quality-recount.mjs --expect <pre-phase composite>` both exit 0.
- The served digest is computed from fetched bytes and recorded.
- Eight verdicts recorded verbatim with per-row CAN PASS / CAN FAIL lines written beforehand.
</verification>

<success_criteria>
- The badge, the three detail lines, the Elevation Gain caveat and the position-unknown wording are
  read off a rendered page against values derived from committed files.
- Elevation's absence from the has-any-severe-signal composite is demonstrated on the shipped surface,
  not only in the type system.
- The corrected criteria and the calibration report are confirmed as a document read.
- Any failure is logged verbatim as a gap rather than patched in place.
</success_criteria>

<output>
Create `.planning/phases/30-elevation-quality-signal/30-08-SUMMARY.md` when done.
Record: the served digest and whether it matched; the three scripts' stdout verbatim; the eight
verdicts verbatim; the requirement→row map as applied; and any gap, unpatched, with the row that
produced it.
</output>
