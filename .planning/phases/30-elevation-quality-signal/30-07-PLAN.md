---
phase: 30-elevation-quality-signal
plan: 07
type: execute
wave: 3
depends_on: ["30-03", "30-04"]
files_modified:
  - scripts/compute-elevation-recount.mjs
  - scripts/compute-elevation-recount.test.mjs
  - package.json
autonomous: true
requirements: [ELEV-02]

must_haves:
  decisions: [D-06, D-15, D-16]
  truths:
    - "D-15: an independent recount reproduces the elevation cohorts from the shipped index alone, importing nothing from the module that wrote those fields"
    - "D-06: the recount counts elevation without ever touching the anySevere composite, and its own tiering key list excludes elevation"
    - "D-06: Phase 27's composite is byte-stable after this phase — the unmodified compute-pace-quality-recount.mjs still reports its pre-phase numbers"
    - "The recount's elevation figures reconcile with 30-CALIBRATION.md's, with any difference attributable to a stated cause"
    - "Neither new script has an fs write target anywhere under data/"
  artifacts:
    - path: "scripts/compute-elevation-recount.mjs"
      provides: "the shipped-index elevation recount with its own arithmetic and no classifier import"
      contains: "data/dashboard/index.json"
    - path: "scripts/compute-elevation-recount.test.mjs"
      provides: "guard tests: no classifier import, importing runs no read, malformed rows degrade"
  key_links:
    - from: "scripts/compute-elevation-recount.mjs"
      to: "data/dashboard/index.json"
      via: "a direct file read with its own counting arithmetic"
      pattern: "readFileSync"
    - from: "the recount's elevation counts"
      to: "30-CALIBRATION.md's per-mode and union figures"
      via: "two independently produced figures for one cohort, reconciled in the summary"
      pattern: "union"
---

<objective>
Build the verifier that cannot agree with itself: a recount that opens the shipped index off disk and
counts elevation with its own arithmetic, importing nothing from the module that wrote those fields —
and prove in the same pass that Phase 27's composite did not move.

Purpose: Phase 27's D-03 exists because a verifier that imports the classifier verifies the
classifier against itself. ELEV-02's "validated against the whole archive" needs the same
independence. D-06's byte-stability claim needs a number, produced by a script this phase did not
touch.

Output: one new script with its own key lists, one npm script, its guard tests, and a recorded
reconciliation between three figures for one cohort.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-elevation-quality-signal/30-CONTEXT.md
@.planning/phases/30-elevation-quality-signal/30-PATTERNS.md
@.planning/phases/30-elevation-quality-signal/30-03-SUMMARY.md
@.planning/phases/30-elevation-quality-signal/30-04-SUMMARY.md
@scripts/compute-pace-quality-recount.mjs

<interfaces>
<!-- The skeleton to mirror, scripts/compute-pace-quality-recount.mjs: readShippedIndex (returns
     { ok: false, reason } rather than throwing), recountComposite, evaluateReport, guarded main(),
     an `--expect N` flag that turns a number into a PASS/FAIL exit code. -->

<!-- Its key lists, which this phase does NOT modify (lines 43-56):
       VALID_TIERS = ['none', 'minor', 'severe', 'not-computable']
       TIERING_SIGNAL_KEYS = ['decimation', 'gapProfile', 'impossibleSamples']
       REQUIRED_QUALITY_SUBKEYS = ['decimation','gapProfile','impossibleSamples','deviceEra','elapsedVsMoving']
     REQUIRED_QUALITY_SUBKEYS is a presence check, not an exact-key-set check, so a sixth key on the
     row does not break it. Leaving this file untouched keeps it an independent regression gate
     rather than a file this phase edited and then cited as evidence about itself. -->

<!-- D-06 regression baseline, measured 2026-09-18 against the live local index:
       node scripts/compute-pace-quality-recount.mjs --expect 299
       Total rows 1890 / computable 1865 / not-computable 25
       decimation 154, gapProfile 127, impossibleSamples 31 → composite 299 → PASS
     Re-derive at execution time; archive growth moves these by single digits and is not a defect.
     A move caused by elevation joining the composite IS a defect. -->

<!-- Figures to reconcile against, from 30-CALIBRATION.md (plan 30-04): per-mode cohorts, the
     loop-gated union, the drift not-computable cohort, the device-family breakdown. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: An elevation recount that never imports the classifier</name>
  <files>scripts/compute-elevation-recount.mjs, scripts/compute-elevation-recount.test.mjs, package.json</files>
  <read_first>
    - `scripts/compute-pace-quality-recount.mjs` in full, 283 lines — the header doc block explaining
      WHY it must not import the classifier, `readShippedIndex`'s `{ ok: false, reason }` contract,
      the recount arithmetic, `evaluateReport`, the `--expect` flag handling, and the guarded
      `main()`.
    - `scripts/compute-pace-quality-recount.test.mjs` — the full test convention, in particular the
      assertion that importing the module triggers no read of the index.
    - `package.json:26-28` — the `compute-pace-quality-recount` / `compute-pr-ceiling-recount` script
      lines (no `npm run build` prefix, because a recount must not depend on the compiled classifier).
    - `.planning/phases/30-elevation-quality-signal/30-03-SUMMARY.md` — the shape of `quality.elevation`
      as actually published, and the six counts read off the regenerated index.
  </read_first>
  <action>
    Create `scripts/compute-elevation-recount.mjs` structurally near-identical to
    `compute-pace-quality-recount.mjs`, differing only in which fields it counts. Its header doc block
    states, in its own words, the D-03/D-15 rule it exists to honour: this file must never import
    `pace-quality.ts`, `dist/analytics/pace-quality.js` or any module that computes the signals, and
    it must never require `npm run build`, because a verifier that imports the classifier agrees with
    itself by construction.

    It reads `data/dashboard/index.json` only — no stream read, no activity read, no shard read — and
    counts with its own arithmetic:
      - total rows, rows carrying a `quality` object, rows carrying `quality.elevation`;
      - per mode: `subGround.flagged` true, `closureDrift.state === 'flagged'`,
        `verticalRate.flagged` true;
      - the rolled-up severe count (`elevation.tier === 'severe'`), recomputed independently as the
        union of the three per-mode counts with an inclusion-exclusion check printed, and compared
        against the shipped `tier` field — if the two disagree, that is a real defect, so print both
        and exit non-zero;
      - `closureDrift.state === 'not-computable'` and `elevation.tier === 'not-computable'` as
        separate lines with their denominators printed inline, because they count different things
        (D-02 versus the stream-less cohort);
      - the device-family breakdown of the severe set from `quality.deviceEra.family`, with the
        explicit no-device-name category;
      - every percentage printed beside the denominator it used.
    Support an `--expect N` flag against the rolled-up severe count, mirroring the sibling's PASS/FAIL
    exit behaviour, so a later plan and CI can pin the number.

    Define its own key lists. If it needs a valid-tier set, that set is elevation's three members, NOT
    the four-member one. It must NOT define, import or extend any `TIERING_SIGNAL_KEYS`-style array
    that includes `elevation`, and it must not read, recompute or report `anySevere` at all — D-06's
    boundary is that elevation never participates in that composite, and the recount script is the
    second place (after `hasAnySevereSignal`) where a future reader could silently reverse it.

    Do NOT modify `scripts/compute-pace-quality-recount.mjs`. Leaving it untouched is what makes it
    usable as this phase's independent regression gate; a file this phase edited cannot testify about
    what this phase changed.

    Add `"compute-elevation-recount": "node scripts/compute-elevation-recount.mjs"` to `package.json`
    — no `npm run build` prefix, for the reason in the header doc block.

    Create `scripts/compute-elevation-recount.test.mjs` (`.mjs`; `scripts/**/*.test.ts` is never
    collected) asserting:
      - importing the module triggers no read of `data/dashboard/index.json` and runs no recount;
      - the module's own source text contains no import of `pace-quality` in any form — read the file
        as text and assert, so the independence rule is machine-checked rather than remembered;
      - the module's source contains no `anySevere` reference and no tiering-key array containing
        `elevation`;
      - `readShippedIndex`-equivalent returns `{ ok: false, reason }` for a missing file and for
        malformed JSON rather than throwing;
      - the counting helpers handle a row with `quality` absent, with `elevation` absent, and with
        `elevation` present but malformed, counting none of them as severe and never throwing;
      - the inclusion-exclusion check fires on a hand-built set where the shipped `tier` disagrees
        with the three per-mode booleans;
      - no `fs` write target anywhere under `data/` in this script's source (the T-27-09 pattern,
        matching plan 30-04's equivalent assertion).
  </action>
  <verify>
    <automated>node scripts/compute-elevation-recount.mjs && npx vitest run scripts/compute-elevation-recount.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pace-quality" scripts/compute-elevation-recount.mjs` returns 0 (no import of the classifier in any form, compiled or source).
    - `grep -c "anySevere" scripts/compute-elevation-recount.mjs` returns 0.
    - `grep -c "elevation" scripts/compute-pace-quality-recount.mjs` returns 0 and `git diff --name-only | grep -c compute-pace-quality-recount.mjs` returns 0 — the Phase 27 recount is untouched.
    - `grep -c "compute-elevation-recount" package.json` returns 1, and that line contains no `npm run build`.
    - `node scripts/compute-elevation-recount.mjs` exits 0 and prints per-mode counts, the union with its inclusion-exclusion check, both not-computable lines with denominators, and the device-family breakdown.
    - `npx vitest run scripts/compute-elevation-recount.test.mjs` exits 0, including the source-scan tests for the classifier import, `anySevere`, and the `data/` write target.
    - Demonstrated failing, run and recorded verbatim: hand-build an index fixture in a temp directory where one row's `elevation.tier` is `'none'` while `subGround.flagged` is true; point the script at it; observe the disagreement reported and a non-zero exit; then restore.
  </acceptance_criteria>
  <done>A verifier exists that reproduces the elevation cohorts from the published artifact alone, and whose independence from the classifier and from anySevere is machine-checked.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile three figures and prove the Phase 27 composite unmoved</name>
  <files>scripts/compute-elevation-recount.mjs</files>
  <read_first>
    - `.planning/phases/30-elevation-quality-signal/30-CALIBRATION.md` § Per-mode cohorts, § Overlap
      matrix, § Union by device family, § Drift not-computable — the figures produced from the
      streams by the classifier.
    - `.planning/phases/30-elevation-quality-signal/30-04-SUMMARY.md` — the live deltas that report
      recorded against the research table.
    - `.planning/phases/30-elevation-quality-signal/30-03-SUMMARY.md` — the six counts read straight
      off the regenerated index.
    - `scripts/compute-pace-quality-recount.mjs:40-60` — the key lists this phase leaves alone.
  </read_first>
  <action>
    Run all three and reconcile them in one place, because one cohort with three independently
    produced figures is the only evidence here that cannot agree with itself:
      1. `npm run compute-elevation-calibration` — computed from the committed streams by the
         classifier;
      2. `node scripts/compute-elevation-recount.mjs` — recounted from the shipped index with its own
         arithmetic and no classifier;
      3. the raw counts from the regenerated `data/dashboard/index.json` recorded in `30-03-SUMMARY.md`.

    For each of sub-ground, loop-gated drift, vertical rate, the union and the drift not-computable
    cohort, state the three figures side by side. Where they agree, say so. Where they differ, state
    the delta as a number and its cause — a regeneration between the two runs is a legitimate cause
    and must be quantified by re-running both back to back; a threshold or gating difference is NOT
    legitimate and is a defect to fix in this plan before proceeding. Do not paper over a delta with
    a tolerance.

    Then run the untouched Phase 27 gate: `node scripts/compute-pace-quality-recount.mjs --expect N`
    where N is the composite recorded before this phase began (299 at research time — use the value
    from `30-03-SUMMARY.md`'s run). Record its full stdout. It must print PASS, and its per-signal
    breakdown must be the pre-phase breakdown. If the composite moved, stop and diagnose: elevation
    entering `anySevere` is the D-06 failure mode this gate exists to catch, and archive growth is
    the only acceptable alternative explanation — which must then be shown by the row count having
    changed too.

    If the recount script needs a fix to reconcile (for example a denominator it names wrongly), fix
    it here; that is the only reason to touch a file in this task.

    Finally, add the `--expect` invocation for the elevation union to the summary as the number a
    later plan and any future regression check should pin.
  </action>
  <verify>
    <automated>node scripts/compute-elevation-recount.mjs && node scripts/compute-pace-quality-recount.mjs --expect 299</automated>
  </verify>
  <acceptance_criteria>
    - A five-row reconciliation table (sub-ground, drift, vertical rate, union, drift not-computable) with three columns (calibration, recount, index) is recorded in the summary, every cell filled with a number.
    - Every non-zero delta in that table is annotated with a stated cause and a quantification; no delta is annotated only as "close enough".
    - `node scripts/compute-pace-quality-recount.mjs --expect <pre-phase composite>` prints `PASS`, and its per-signal breakdown line is recorded verbatim alongside the pre-phase line from `30-03-SUMMARY.md`.
    - `node scripts/compute-elevation-recount.mjs --expect <measured union>` exits 0 and prints a MATCH line.
    - `git diff --name-only` shows no change to `scripts/compute-pace-quality-recount.mjs`.
    - `git status --porcelain data/` is empty.
  </acceptance_criteria>
  <done>One cohort, three independently produced figures, reconciled with every difference explained — and the Phase 27 composite demonstrated unmoved by a script this phase never edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped `data/dashboard/index.json` → the recount | The published artifact is the only input; a verifier reading anything else loses its independence |
| this phase's code → Phase 27's composite | A silent policy reversal would surface here or nowhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-29 | Repudiation | a verifier that imports the classifier and agrees with itself | mitigate | The recount imports nothing from `pace-quality`; a source-scan test asserts the absence of any such import, and the npm script carries no `npm run build` prefix. |
| T-30-30 | Tampering | elevation silently joining the composite via the recount's key lists | mitigate | The script defines its own key lists, never references `anySevere`, and a source-scan test asserts both. Phase 27's recount is left byte-unchanged so it can testify independently. |
| T-30-31 | Denial of Service | malformed or missing index aborting the recount | mitigate | `{ ok: false, reason }` return contract instead of a throw, mirroring T-27-18; tested for a missing file, malformed JSON, and malformed rows. |
| T-30-32 | Tampering | writing under `data/` from the new script | mitigate | Source-scan test asserting no `fs` write target under `data/` (T-27-09 pattern); `git status --porcelain data/` asserted empty. |
| T-30-33 | Repudiation | a reconciliation delta waved away | mitigate | Every non-zero delta must carry a stated, quantified cause; a threshold or gating difference is treated as a defect, not a tolerance. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `node scripts/compute-elevation-recount.mjs` exits 0 and its counts reconcile with `30-CALIBRATION.md`.
- `node scripts/compute-pace-quality-recount.mjs --expect <pre-phase composite>` prints PASS.
- `npx vitest run scripts/compute-elevation-recount.test.mjs` exits 0.
- `npm test` and `npx tsc --noEmit` exit 0.
- `git diff --name-only` shows `scripts/compute-pace-quality-recount.mjs` unchanged.
</verification>

<success_criteria>
- The elevation cohorts are reproducible from the shipped index alone, by arithmetic that shares no
  code with the classifier.
- Elevation's exclusion from `anySevere` is enforced in the recount script as well as in the type
  system.
- Three independently produced figures for one cohort are reconciled, with every delta explained.
- Phase 27's composite is demonstrated unmoved by an untouched script.
</success_criteria>

<output>
Create `.planning/phases/30-elevation-quality-signal/30-07-SUMMARY.md` when done.
Record: the recount's full stdout verbatim; the five-row three-column reconciliation table with every
delta's cause; the Phase 27 gate's full stdout verbatim beside its pre-phase line; and the
`--expect` number for the elevation union, which plan 30-08 states as an expected value before the
checkpoint runs.
</output>
