---
phase: 30-elevation-quality-signal
plan: 06
type: execute
wave: 3
depends_on: ["30-03"]
files_modified:
  - src/dashboard/views/detail-sections.ts
  - src/dashboard/views/detail-sections.test.ts
autonomous: true
requirements: [ELEV-01]

must_haves:
  decisions: [D-02, D-11]
  truths:
    - "D-11: the detail view's quality section shows three always-on elevation lines — lowest altitude, start/end altitude difference, max vertical rate — on healthy activities as well as flagged ones"
    - "D-11: each line carries its measured number, so silence is never the way a healthy activity reports health"
    - "D-02: the closure-drift line reads its state in words — flagged, clear on a loop with the measured endpoint distance, excluded because the endpoints are far apart, or position unknown so drift was not checked"
    - "A stream-less activity's three elevation lines read not-computable with the reason, never a fabricated zero or a healthy statement"
    - "The section is eight rows; the render loop is unchanged because it iterates the plan generically"
  artifacts:
    - path: "src/dashboard/views/detail-sections.ts"
      provides: "subGroundRow, closureDriftRow, verticalRateRow, their explanations, and three more notAvailableRows entries"
      contains: "closureDriftRow"
    - path: "src/dashboard/views/detail-sections.test.ts"
      provides: "coverage of the three rows across healthy, flagged, not-a-loop, position-unknown and stream-less cases"
      contains: "elevation rows"
  key_links:
    - from: "src/dashboard/views/detail-sections.ts qualitySignalsSectionPlan"
      to: "quality.elevation.subGround / closureDrift / verticalRate"
      via: "three row builders appended to the fixed-order rows array"
      pattern: "verticalRateRow\\("
---

<objective>
Give the detail view three always-on elevation lines whose numbers a human can read back against a
committed file, including the four distinct things the drift check can say.

Purpose: D-11 extends Phase 27's D-08 always-on reasoning to altitude — a healthy activity must
visibly say it is healthy, because silence reads as good news. D-02 is why the drift line needs four
phrasings rather than a boolean: "clear", "not a loop", and "position unknown" are three different
statements about the world, and collapsing them hides which one applies.

Output: three row builders in the existing triad's exact shape, three matching not-available rows,
and a section that grows from five rows to eight without touching the render loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-elevation-quality-signal/30-CONTEXT.md
@.planning/phases/30-elevation-quality-signal/30-PATTERNS.md
@.planning/phases/30-elevation-quality-signal/30-03-SUMMARY.md
@src/dashboard/views/detail-sections.ts

<interfaces>
<!-- The row-builder shape to copy three times (decimationRow, detail-sections.ts:921-955): the
     not-computable branch first, then the healthy/tiered valueText, then shard-derived evidenceText,
     returning { label, valueText, tier, explanation, evidenceText }. -->

```typescript
function decimationRow(signal, notComputableReason, shard): QualitySignalRow {
  const explanation = tieringExplanation('decimation');
  if (notComputableReason !== null) {
    return { label: 'Decimation', valueText: `Not computable — ${notComputableReason}`, tier: 'not-computable', explanation, evidenceText: null };
  }
  let valueText: string;
  if (signal.tier === 'none') valueText = 'No decimation detected';
  else if (signal.zeroAdvanceFraction !== null) valueText = `${Math.round(signal.zeroAdvanceFraction * 100)}% …`;
  else valueText = 'Decimation data unavailable';
  return { label: 'Decimation', valueText, tier: signal.tier, explanation, evidenceText };
}
```

<!-- D-11's three lines, in this order, with their healthy phrasing:
       lowest altitude 12 m
       start/end altitude differ by 4 m (loop, 38 m apart)
       max vertical rate 1.2 m/s
     and the drift line's other states read in words. -->

<!-- tieringExplanation (detail-sections.ts:833) maps ONLY the three tiering signals to their list
     badge explanations via EXPLANATION_PROBE_SPECS. Elevation deliberately does not use it: D-10's
     list badge is ONE rolled-up badge across three modes, while D-11 needs three per-mode
     explanations, so a 1:1 probe lookup does not exist. Follow DEVICE_ERA_EXPLANATION /
     ELAPSED_VS_MOVING_EXPLANATION (detail-sections.ts:~840-850) instead — this section's own
     module-level explanation constants, which is the established pattern for a row with no 1:1
     badge counterpart. -->

<!-- notAvailableRows (detail-sections.ts:1084-1121) is a five-element fallback array shown when the
     quality object itself is null; qualitySignalsSectionPlan (1139-1158) is the fixed-order rows
     array; buildQualitySignalsSection's render loop (1190-1226) iterates plan.rows generically and
     needs zero change. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three always-on elevation rows with the drift line's four phrasings</name>
  <files>src/dashboard/views/detail-sections.ts</files>
  <read_first>
    - `src/dashboard/views/detail-sections.ts:900-1060` — `decimationRow`, `gapProfileRow` and
      `impossibleSamplesRow` in full, including how each draws `evidenceText` from the shard.
    - `src/dashboard/views/detail-sections.ts:760-860` — `EXPLANATION_PROBE_QUALITY` (which plan
      30-01 left with a clean elevation deliberately), the module-load assertion loop over the three
      tiering signals, `tieringExplanation`, and the two standalone explanation constants
      `DEVICE_ERA_EXPLANATION` / `ELAPSED_VS_MOVING_EXPLANATION`.
    - `src/dashboard/views/detail-sections.ts:1069-1160` — `notAvailableRows` and
      `qualitySignalsSectionPlan`.
    - `src/dashboard/views/detail-sections.ts:1185-1230` — `buildQualitySignalsSection`'s render
      loop, to confirm it needs no change.
    - `.planning/phases/30-elevation-quality-signal/30-01-SUMMARY.md` — the shipped `ElevationSignal`
      shape including whether `deltaM` is signed and what elevation evidence rides the shard.
  </read_first>
  <action>
    Add three module-level explanation constants beside `DEVICE_ERA_EXPLANATION`, one per mode, each
    saying why that mode matters: sub-ground — an altitude below plausible ground level means the
    barometric or GPS altitude baseline for this activity is wrong, not that the terrain was; closure
    drift — a loop that returns to its start should return to its start altitude, so a large
    difference is sensor drift over the activity rather than terrain; vertical rate — a per-sample
    climb or descent rate no runner achieves means the altitude channel moved without the athlete
    doing so. Do NOT route these through `tieringExplanation`, and do not extend the module-load
    assertion loop's three-signal list: D-10 ships ONE rolled-up list badge for three modes, so no
    1:1 probe lookup exists, and forcing one would either add a fourth probe spec or produce an empty
    explanation. State this reason in a comment beside the constants.

    Write `subGroundRow`, `closureDriftRow` and `verticalRateRow` in `decimationRow`'s exact shape
    (not-computable branch first, then value, then shard evidence), each taking the matching
    sub-object, the whole-signal not-computable reason, and the shard:

      - `subGroundRow` — label naming lowest altitude. Healthy reads as the measured minimum
        (`lowest altitude 12 m`); flagged reads the same measured minimum with its below-ground
        condition named. A null `minAltM` reads as data unavailable, never `0 m`. Tier: `'severe'`
        when flagged, `'none'` otherwise, `'not-computable'` on the reason branch.

      - `closureDriftRow` — FOUR distinct phrasings, all reading in words (D-02):
          1. flagged: the measured absolute delta plus the measured endpoint distance, e.g. start and
             end altitudes differ by 198 m on a loop whose endpoints are 0 m apart;
          2. clear on a loop: the measured delta and distance, D-11's own example phrasing
             (`start/end altitude differ by 4 m (loop, 38 m apart)`);
          3. excluded because the endpoints are far apart: name the measured distance and say drift
             was not checked because this was not a loop — this is excluded by design, not unknown;
          4. position unknown: say so in words (`start/end position unknown — drift not checked`)
             with no number invented.
        Distinguish cases 3 and 4 by the `startEndDistM` value the detector retained on the excluded
        branch and dropped on the not-computable branch. Never print a delta on cases 3 or 4 and
        never fall back to the raw altitude difference.

      - `verticalRateRow` — label naming the maximum vertical rate. Healthy reads the measured worst
        rate (`max vertical rate 1.2 m/s`); flagged reads the worst rate and the count of violating
        sample pairs. A null `worstRateMps` reads as data unavailable, never `0 m/s`.

    Each row's `evidenceText` comes from the SHARD when present and is `null` when the shard is
    `null` — the offending sample index and its Δt/Δalt for vertical rate, the resolved loop radius
    for drift. Follow exactly how the three existing rows source their evidence.

    Append the three rows to `qualitySignalsSectionPlan`'s returned array in D-11's order (lowest
    altitude, start/end difference, max vertical rate), placed after the three tiering rows and
    before `deviceEraRow`. Add three matching entries to `notAvailableRows()` in the same order with
    the same literal shape the existing five use. Change nothing in `buildQualitySignalsSection` — it
    iterates `plan.rows` generically, and the section simply becomes eight rows.

    The whole-signal not-computable reason drives the sub-ground and vertical-rate rows. The drift
    row's own `state` is authoritative for drift: an activity with a usable stream but no position
    shows healthy sub-ground and vertical-rate lines beside a position-unknown drift line — that
    combination is the entire point of D-02 and must be renderable.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/dashboard/views/detail-sections.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function subGroundRow\|function closureDriftRow\|function verticalRateRow" src/dashboard/views/detail-sections.ts` returns 3.
    - `qualitySignalsSectionPlan` returns eight rows: a test asserts `plan.rows.length === 8`, and `notAvailableRows()` returns eight.
    - `git diff src/dashboard/views/detail-sections.ts` shows zero changes inside `buildQualitySignalsSection`'s render loop, zero changes to `decimationRow`/`gapProfileRow`/`impossibleSamplesRow`, and the module-load assertion loop still lists exactly `decimation`, `gapProfile`, `impossibleSamples`.
    - `grep -c "tieringExplanation('sub\|tieringExplanation('closure\|tieringExplanation('vertical" src/dashboard/views/detail-sections.ts` returns 0 — elevation uses its own explanation constants.
    - `grep -c "innerHTML" src/dashboard/views/detail-sections.ts` returns 0.
    - `npx tsc --noEmit` and `npm test` exit 0.
  </acceptance_criteria>
  <done>The quality section discloses three elevation facts on every activity, and the drift line can say all four of the things the detector can conclude.</done>
</task>

<task type="auto">
  <name>Task 2: Prove every phrasing renderable, including the ones that are easy to never reach</name>
  <files>src/dashboard/views/detail-sections.test.ts</files>
  <read_first>
    - `src/dashboard/views/detail-sections.test.ts:450-520` — `HEALTHY_QUALITY`,
      `NOT_COMPUTABLE_QUALITY` and the `describe` convention for plan-level assertions.
    - `src/dashboard/views/detail-sections.test.ts:560-700` — the existing per-signal row tests,
      including how the shard-present and shard-null cases are paired.
    - `src/dashboard/views/detail-sections.ts` as Task 1 left it — the three new row builders.
    - `.planning/phases/30-elevation-quality-signal/30-03-SUMMARY.md` — the three real activity ids
      (severe, healthy, drift-not-computable) and their committed shard values.
  </read_first>
  <action>
    Add a `describe` whose name contains the literal `elevation rows` covering every branch the three
    builders can take. A branch with no test is a branch the checkpoint may be the first to reach:

      - Healthy activity: all three elevation rows present, each carrying a number, none blank, none
        containing `undefined`, `null` or `NaN`, tier `'none'` on each.
      - Sub-ground flagged: the row names the measured minimum and carries tier `'severe'`; the other
        two elevation rows still read healthy with their own numbers.
      - Drift flagged on a loop: the row names both the absolute delta and the endpoint distance.
      - Drift clear on a loop: D-11's own phrasing shape, with both numbers present.
      - Drift excluded, endpoints far apart: the row says drift was not checked and names the
        measured distance, and contains no delta figure.
      - Drift position unknown: the row says position is unknown in words, contains no distance and
        no delta, and is distinguishable from the excluded case by its text.
      - Vertical rate flagged: the row names the worst rate and the violating-pair count.
      - Whole-signal not-computable (stream-less): the sub-ground and vertical-rate rows read
        `Not computable — {reason}` with tier `'not-computable'`, and NO elevation row reads `0 m`,
        `0 m/s` or any healthy statement — the fabricated-zero case T-26-02 and D-06 forbid.
      - `quality === null`: `notAvailableRows()` supplies eight rows including the three elevation
        ones, so the section still says something rather than vanishing.
      - Shard `null`: every elevation row's `evidenceText` is `null` while its `valueText` is
        unchanged, so a failed shard fetch degrades the evidence line only.

    Assert on the plan's row objects, not on DOM, matching this file's existing convention (there is
    no DOM simulation in this tree, which is why the plan/emitter split exists).

    Then, using the three real activity ids recorded in `30-03-SUMMARY.md`, add one test per id that
    builds a row plan from that activity's ACTUAL committed shard/index values and asserts the three
    rendered `valueText` strings contain the numbers in `data/stats/pace-quality/{id}.json`. Record
    those three expected strings in the summary verbatim: plan 30-08's checkpoint states them as
    expected values before the developer opens the page, so the comparison is against an
    independently derived file rather than against the page agreeing with itself.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/detail-sections.test.ts -t "elevation rows"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/views/detail-sections.test.ts -t "elevation rows"` exits 0 with at least ten passing tests.
    - A test asserts the drift-excluded and drift-position-unknown rows produce DIFFERENT strings, so collapsing the two states into one phrasing fails.
    - A test asserts no elevation row in the not-computable case contains `0 m` or `0 m/s`.
    - Demonstrated failing, run and recorded verbatim: change `closureDriftRow`'s position-unknown branch to return the same string as its excluded branch; run the test file and observe the distinguishing test fail; restore.
    - The three real-activity expected `valueText` strings are recorded verbatim in the summary with their source shard paths.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>Every phrasing the three rows can produce is exercised, the two drift exclusions are proven distinguishable, and three real activities' expected strings are recorded for the checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published index row + shard → rendered detail rows | Published numbers become on-screen statements a human will trust |
| a failed shard fetch → the section | Evidence absence must degrade one line, not the section |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-24 | Repudiation | silence read as good news | mitigate | D-11's rows are always-on and always carry a number; a healthy activity visibly states its lowest altitude, its closure delta and its worst vertical rate. Asserted for the healthy case with no blank, `undefined`, `null` or `NaN` text. |
| T-30-25 | Tampering | the four drift states collapsing into one phrasing | mitigate | Each state has its own text and its own test; a test asserts the excluded and position-unknown strings differ, demonstrated failing by making them equal. |
| T-30-26 | Tampering | a fabricated zero on a stream-less activity | mitigate | The not-computable branch reads the reason; a test asserts no elevation row contains `0 m` or `0 m/s` in that case (T-26-02, Phase 27 D-06). |
| T-30-27 | Denial of Service | shard fetch failure taking down the section | mitigate | `evidenceText` is `null` when the shard is `null` while `valueText` is unchanged; tested. Mirrors Phase 27's T-27-31 disposition. |
| T-30-28 | Tampering | untrusted text reaching the DOM as markup | mitigate | Every string reaches the DOM via `textContent` or `appendAccessibleBadge` through the unchanged render loop; `grep -c innerHTML` asserted 0. Elevation values are numbers, introducing no new free-text field. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. |
</threat_model>

<verification>
- `npx vitest run src/dashboard/views/detail-sections.test.ts` exits 0.
- `npx tsc --noEmit` and `npm test` exit 0.
- `grep -c "innerHTML" src/dashboard/views/detail-sections.ts` returns 0.
- The section plan and the not-available fallback both return eight rows.
</verification>

<success_criteria>
- Three always-on elevation lines on every activity, healthy and flagged alike, each carrying a
  number a human can check against a committed file.
- The drift line says which of four things is true, in words.
- A stream-less activity says not computable rather than showing zeros.
- The render loop and the three existing rows are untouched.
</success_criteria>

<output>
Create `.planning/phases/30-elevation-quality-signal/30-06-SUMMARY.md` when done.
Record: the three expected `valueText` strings for each of the three real activity ids, verbatim,
with the shard path each was derived from; and the verbatim output of the collapsed-drift-state
demonstration.
</output>
