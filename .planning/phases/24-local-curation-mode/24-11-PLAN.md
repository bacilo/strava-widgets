---
phase: 24-local-curation-mode
plan: 11
type: execute
wave: 7
depends_on: ["24-10"]
files_modified:
  - scripts/lib/curation-guard.mjs
  - scripts/lib/curation-guard.test.mjs
autonomous: true
gap_closure: true
closes_gaps: [GAP-24-02]
requirements: [CUR-01]

must_haves:
  decisions: [D-10, D-11]
  truths:
    - "A file under dist/widgets whose extension is NOT explicitly exempted is content-scanned for the __curate marker — the guard fails CLOSED on an extension nobody anticipated, instead of silently skipping it"
    - "A planted .d.ts file, a planted .mjs file and a planted extensionless file, each containing the literal __curate marker, are each flagged by findCurationArtifacts — and each was observed FAILING against the unfixed guard before the fix landed (D-11)"
    - "dist/widgets/data/best-effort-exclusions.json still cannot be flagged, even when its reason text literally contains __curate — the one load-bearing exemption survives, and every remaining exemption carries a written justification (D-10)"
    - "The inverted guard returns zero violations against the real dist/widgets tree as it stands today, including its 22 published .d.ts files, so npm run build-widgets still exits 0"
  artifacts:
    - path: "scripts/lib/curation-guard.mjs"
      provides: "A fail-closed content scanner: a narrow, justified skip-list replaces the extension allowlist"
      contains: "UNSCANNED_EXTENSIONS"
      min_lines: 90
    - path: "scripts/lib/curation-guard.test.mjs"
      provides: "D-11 planted-fixture cases for the three previously-unscanned extension classes, plus the preserved public-JSON exemption case"
      contains: "extensionless"
      min_lines: 140
  key_links:
    - from: "dist/widgets/**/*.d.ts (a published artifact class, 22 files today)"
      to: "findCurationArtifacts content scan"
      via: "skip-list membership test that does NOT contain '.ts'"
      pattern: "UNSCANNED_EXTENSIONS"
    - from: "dist/widgets/data/best-effort-exclusions.json (PUBLIC, must keep returning 200)"
      to: "findCurationArtifacts early-continue"
      via: "the single load-bearing '.json' exemption"
      pattern: "'\\.json'"
---

<objective>
Close GAP-1 from `24-VERIFICATION.md`: make the D-10(a) build-time curation guard fail CLOSED, so
that the word "provably" in ROADMAP criterion 3 and in CUR-01's own text becomes true for the file
classes `dist/widgets` demonstrably contains.

Purpose: `scripts/lib/curation-guard.mjs:37` declares `SCANNED_EXTENSIONS = ['.js', '.html',
'.css', '.map']` and line 90 skips the content scan for everything else. `dist/widgets` publishes
22 `.d.ts` files today. A leaked `scripts/curate-overlay/index.ts` — which itself contains the
literal `const CURATE_PREFIX = '/__curate'` — or its emitted `.d.ts` sibling would return
`violations === []` and print the green `✓ Curation-artifact scan` line at `build-widgets.mjs:222`.
The HTTP layer does not backstop it: `verify-dashboard-publish.mjs:305-309` probes exactly three
hard-coded literal URLs. D-11's planted-fixture suite has only ever planted `.js`, `.html` and
directory-name shapes — the one extension class that IS covered — so the demonstrated blind spot
has never been observed red.

Output: an inverted, justified skip-list in `curation-guard.mjs`; three new planted-fixture cases
(`.d.ts`, `.mjs`, extensionless) recorded RED before the fix and GREEN after; and a recorded
whole-tree run proving the stricter guard does not break today's build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-local-curation-mode/24-CONTEXT.md
@.planning/phases/24-local-curation-mode/24-VERIFICATION.md
@scripts/lib/curation-guard.mjs
@scripts/lib/curation-guard.test.mjs
</context>

<interfaces>
<!-- Current exported surface of scripts/lib/curation-guard.mjs. The executor changes -->
<!-- exactly one of these names and must update every importer named below. -->

  export const CURATE_DIR_NAME = '__curate';
  export const CURATE_MARKER = '__curate';
  export const SCANNED_EXTENSIONS = ['.js', '.html', '.css', '.map'];   // <- the name being replaced
  export function findCurationArtifacts(publishDir): Array<{ path: string, reason: string }>

Importers of this module, both of which must still work after the rename:
  - scripts/build-widgets.mjs      — imports findCurationArtifacts only (wrapper assertNoCurationArtifacts, unchanged by this plan)
  - scripts/lib/curation-guard.test.mjs — line 23 imports CURATE_DIR_NAME, CURATE_MARKER, SCANNED_EXTENSIONS, findCurationArtifacts

Facts established against the real tree on 2026-09-02, before planning (do not re-derive by guessing):
  - `find dist/widgets -type f` extension census: 5588 .json, 58 .js, 44 .map, 22 .ts, 5 .html, 2 .css, 2 .DS_Store. ZERO extensionless files.
  - Simulating the inverted guard with a skip-list of exactly ['.json'] over the real dist/widgets tree yields ZERO violations.
  - Neither `dist/widgets/.DS_Store` nor `dist/widgets/data/.DS_Store` contains the string `curate`.
</interfaces>

<tasks>

<task type="auto" id="T1">
  <name>Task 1: Plant the three unscanned extension classes and observe the guard RED (D-11)</name>
  <files>scripts/lib/curation-guard.test.mjs</files>
  <read_first>
    - `scripts/lib/curation-guard.test.mjs` — the whole file. Lines 36-40 define the `writeFile(relativePath, contents)` helper every fixture uses; lines 60-93 are the four existing planted-fixture cases whose shape the new cases must mirror; lines 95-114 are the two exemption cases that must keep passing unchanged.
    - `scripts/lib/curation-guard.mjs` — lines 53-57 (`scanExtension`, which slices from the LAST dot, so `index.d.ts` yields `.ts`, not `.d.ts`) and lines 89-98 (the skip and the content scan).
    - `.planning/phases/24-local-curation-mode/24-VERIFICATION.md` — gap 2's `missing` list, which names exactly the three fixture classes required here.
  </read_first>
  <action>
    Add three new `it(...)` cases to the existing `describe('findCurationArtifacts', ...)` block in
    `scripts/lib/curation-guard.test.mjs`, placed immediately after the existing
    `planted .curate-dist directory inside the tree` case and BEFORE the two exemption cases. Do
    not modify any existing case in this task. Each new case follows the shape of the existing
    `planted marker in a .js file` case: seed a minimal clean `index.html`, plant one marker-bearing
    file via the `writeFile` helper, call `findCurationArtifacts(tmpDir)`, and assert both
    `violations.length` is greater than 0 and that some violation's `path` ends with the planted
    file's name.

    The three planted paths and their contents, exactly:
    - `shared/curate-overlay.d.ts` — a TypeScript declaration mirroring the real published
      `dist/widgets/shared/widget-base.d.ts` shape, whose body contains the `CURATE_MARKER`
      literal (build the string from the imported `CURATE_MARKER` constant, never a hardcoded
      `'__curate'`, matching how the existing cases interpolate it). Name the case so it states
      the load-bearing fact: `dist/widgets` publishes 22 `.d.ts` files today.
    - `assets/curate-server.mjs` — an ESM module source whose body contains the `CURATE_MARKER`
      literal. Name the case for the leak it represents: a stray copy of `scripts/curate-server.mjs`,
      which carries every route literal in the phase.
    - `assets/overlay` — an EXTENSIONLESS file whose body contains the `CURATE_MARKER` literal.
      Name the case for the fail-open class it represents: `scanExtension` returns `null` for it,
      and line 90's `ext === null` clause skips it outright.

    Then observe RED, which is this task's actual deliverable. Run
    `npx vitest run scripts/lib/curation-guard.test.mjs` and capture its output verbatim into the
    plan SUMMARY: all three new cases MUST fail with `violations.length` equal to 0 (the assertion
    `expected 0 to be greater than 0`), and all seven pre-existing cases MUST still pass. If any of
    the three new cases PASSES against the unfixed guard, STOP and record that as a blocking
    finding — it would mean the reproduction in `24-VERIFICATION.md` gap 2 does not hold and the
    premise of this plan is wrong.

    Do not touch `scripts/lib/curation-guard.mjs` in this task. The whole point of D-11 is that the
    guard is observed failing before it is changed.
  </action>
  <verify>
    <automated>npx vitest run scripts/lib/curation-guard.test.mjs 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/lib/curation-guard.test.mjs` exits NON-ZERO with exactly 3 failing tests and 7 passing tests.
    - Each of the 3 failures reports a received value of 0 for `violations.length`.
    - `grep -c "curate-overlay.d.ts" scripts/lib/curation-guard.test.mjs` returns at least 1.
    - `grep -c "curate-server.mjs'" scripts/lib/curation-guard.test.mjs` returns at least 1.
    - `grep -c "assets/overlay'" scripts/lib/curation-guard.test.mjs` returns at least 1.
    - `git diff --name-only` lists `scripts/lib/curation-guard.test.mjs` and does NOT list `scripts/lib/curation-guard.mjs`.
    - The verbatim RED output is quoted in the plan SUMMARY under a heading naming D-11.
  </acceptance_criteria>
  <done>Three planted-fixture cases for `.d.ts`, `.mjs` and extensionless files exist and are recorded FAILING against the unmodified guard, with the failure output quoted.</done>
</task>

<task type="auto" id="T2">
  <name>Task 2: Invert SCANNED_EXTENSIONS to a justified skip-list so unknown extensions fail closed</name>
  <files>scripts/lib/curation-guard.mjs, scripts/lib/curation-guard.test.mjs</files>
  <read_first>
    - `scripts/lib/curation-guard.mjs` — the whole file, in particular the docblock at lines 20-29 whose deviation 3 explains why the restriction exists (so the guard can never catch the PUBLIC, already-published `dist/widgets/data/best-effort-exclusions.json`), and lines 89-98.
    - `scripts/lib/curation-guard.test.mjs` — line 23 (the import list, which names `SCANNED_EXTENSIONS`) and lines 112-114 (`SCANNED_EXTENSIONS does not include .json`), both of which break on the rename.
    - `scripts/build-widgets.mjs` — the `assertNoCurationArtifacts` wrapper and its import line, to confirm it imports only `findCurationArtifacts` and therefore needs no edit.
    - `.planning/phases/24-local-curation-mode/24-REVIEW.md` § `### CR-02`, including its "Fix" subsection.
  </read_first>
  <action>
    (a) In `scripts/lib/curation-guard.mjs`, replace the exported constant `SCANNED_EXTENSIONS`
    (currently `['.js', '.html', '.css', '.map']`) with an exported constant named
    `UNSCANNED_EXTENSIONS` whose value is exactly `['.json']` — one entry, nothing else.

    Justification for the list being exactly one entry, which the docblock must state:
    `.json` is the ONLY load-bearing exemption. `dist/widgets/data/best-effort-exclusions.json` is
    PUBLIC — copied by `build-widgets.mjs`'s `dataFiles` list, asserted 200-and-parses at
    `verify-dashboard-publish.mjs`, and fetched at runtime by `detail.ts` and `records.ts` — and a
    developer-written reason string in it may legitimately contain the marker text. Every other
    extension in the published tree is scanned. No speculative image or font exemptions are added:
    the tree contains none today (census in this plan's `<interfaces>` block), and an exemption
    with no load-bearing reason is exactly the collateral hole CR-02 was raised about.

    (b) Replace line 90's skip condition. The current form is
    `if (ext === null || !SCANNED_EXTENSIONS.includes(ext)) continue;`. The new form must skip ONLY
    when the extension is non-null AND present in `UNSCANNED_EXTENSIONS`; a `null` extension (an
    extensionless file) must fall THROUGH to the content scan, not skip it. That inversion of the
    `ext === null` branch is the specific fail-open the extensionless fixture in T1 exercises.

    (c) Change the content read from `readFileSync(entryPath, 'utf8')` to
    `readFileSync(entryPath, 'latin1')`. Reason to record in a comment at the read site: the scan
    now reaches arbitrary byte content (`.DS_Store`, and any future binary), and `latin1` decodes
    every byte sequence without throwing or lossily replacing, while the marker is pure ASCII so
    substring matching is unaffected.

    (d) Add a file-name violation for a FILE literally named `.curate-dist`, beside the existing
    file-name check for `__curate` at lines 82-87, with a reason string naming it as the curate
    overlay's esbuild output. This closes the first of CR-02's two named secondary holes. Do NOT
    add a name-match for `curate-overlay.ts` or any other curate-ish filename: the inverted content
    scan now covers those, and name-matching arbitrary substrings would risk false positives on
    legitimate published files — record that reasoning in a comment.

    (e) Rewrite the docblock's deviation 3 (lines 20-29) so it describes the skip-list, not the
    allowlist. It must state, in order: that content scanning now applies to EVERY file unless its
    extension appears in `UNSCANNED_EXTENSIONS`; that an unanticipated extension therefore fails
    CLOSED; that `.json` is the single exemption and why it is load-bearing; that the previous
    allowlist form silently exempted `.ts`/`.d.ts`/`.mjs`/extensionless files while `dist/widgets`
    publishes 22 `.d.ts` files, which is the CR-02 regression this shape prevents; and that the
    file is read as `latin1` so arbitrary bytes cannot throw.

    (f) In `scripts/lib/curation-guard.test.mjs`, update the import on line 23 from
    `SCANNED_EXTENSIONS` to `UNSCANNED_EXTENSIONS`, and replace the `SCANNED_EXTENSIONS does not
    include .json` case with a pair of assertions on the new constant: that
    `UNSCANNED_EXTENSIONS` DOES contain `'.json'`, and that it does NOT contain `'.ts'`, `'.mjs'`,
    `'.js'`, `'.html'`, `'.css'` or `'.map'`. Also update the inline comment at lines 96-99 of the
    `never catches the published exclusions data file` case so it references the skip-list rather
    than the allowlist; the case's assertions themselves must not change — it must still return
    `[]` for a marker-bearing reason string.

    (g) Add one case asserting a FILE named `.curate-dist` (not a directory) is flagged by name,
    covering (d).
  </action>
  <verify>
    <automated>npx vitest run scripts/lib/curation-guard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/lib/curation-guard.test.mjs` exits 0 with zero failing tests, including the three cases that were RED in T1.
    - `grep -c 'SCANNED_EXTENSIONS' scripts/lib/curation-guard.mjs` — after filtering comments with `grep -v '^ \*' | grep -v '^\s*//'` — matches only `UNSCANNED_EXTENSIONS` occurrences; the bare identifier `SCANNED_EXTENSIONS` no longer appears as a standalone token in executable source of either file.
    - `node -e "import('./scripts/lib/curation-guard.mjs').then(m => { console.log(JSON.stringify(m.UNSCANNED_EXTENSIONS)); })"` prints exactly `[".json"]`.
    - `node -e "import('./scripts/lib/curation-guard.mjs').then(m => console.log(m.findCurationArtifacts('dist/widgets').length))"` prints `0`.
    - `grep -c "latin1" scripts/lib/curation-guard.mjs` returns at least 1.
    - `grep -c "fails CLOSED\|fail closed\|fails closed" scripts/lib/curation-guard.mjs` returns at least 1.
    - `npm test` exits 0 (whole suite, no collateral breakage in `build-widgets.mjs`'s importers).
  </acceptance_criteria>
  <done>The guard scans every extension except `.json`, reads as `latin1`, flags a file named `.curate-dist`, carries a rewritten docblock justifying the single exemption, and all fixture cases pass.</done>
</task>

<task type="auto" id="T3">
  <name>Task 3: Prove the stricter guard green against the real publish tree, and surface the 22-.d.ts question</name>
  <files>scripts/lib/curation-guard.test.mjs</files>
  <read_first>
    - `scripts/build-widgets.mjs` — the `assertNoCurationArtifacts` wrapper and its call site at the end of `buildAllWidgets()`, to know the exact `✓ Curation-artifact scan:` and `✗ Curation-artifact guard failed:` strings the build prints.
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` rows R13 and R22 — both prior planted-fixture proofs, so this task's recorded output is comparable to theirs.
    - `scripts/verify-dashboard-publish-guard.test.mjs` — its `afterEach` cleanup discipline for artifacts planted in the REAL `dist/widgets`, which any real-tree planting in this task must match.
  </read_first>
  <action>
    (a) Run `npm run build-widgets` against the untouched real tree and record its exit code and the
    verbatim `✓ Curation-artifact scan: dist/widgets tree scanned, no curation-mode artifacts found.`
    line into the plan SUMMARY. Exit code must be 0. This is the non-regression half: the stricter
    guard must not start failing on the 22 published `.d.ts` files, the 58 `.js`, the 44 `.map` or
    the 2 `.DS_Store` files the tree actually contains.

    (b) Record in the SUMMARY the count of files the guard now content-scans versus before,
    derived by command, not estimated: total files under `dist/widgets`, the count with a `.json`
    extension (skipped), and the difference (scanned). Note explicitly that `.ts` moved from
    skipped to scanned and that 22 files are in that class.

    (c) Add a whole-tree regression case to `scripts/lib/curation-guard.test.mjs`, guarded by
    `describe.skipIf(!existsSync(...))` on `dist/widgets/index.html` exactly as
    `verify-dashboard-publish-guard.test.mjs` guards its subprocess suite, asserting
    `findCurationArtifacts('dist/widgets')` returns `[]`. Reason to state in the case's name: this
    is the guard-rail against a future exemption removal turning a legitimately published artifact
    class into a build-breaking false positive, and it must skip cleanly on a fresh checkout that
    has never run `npm run build-widgets`.

    (d) Record, as a NOTE in the plan SUMMARY under a heading `Operator decision surfaced, not
    taken`, the open question `24-VERIFICATION.md` raises: `dist/widgets` publishing 22 `.d.ts`
    files at all may be unintended, and stopping their publication would be a cleaner root fix than
    scanning them. State both sides — stopping publication removes the leak surface for that class
    entirely, but it changes what the published bundle contains and is a build-configuration change
    outside this gap's scope; whereas the guard change closes the hole for EVERY unanticipated
    extension class, including ones no root fix can anticipate. State that the guard change is
    required regardless and has been made, and that the publication question is left for the
    operator. Do NOT change any build configuration, `tsconfig.json`, or copy list in this plan.
  </action>
  <verify>
    <automated>npm run build-widgets 2>&1 | grep -F "✓ Curation-artifact scan" && npx vitest run scripts/lib/curation-guard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build-widgets` exits 0 and its output contains the literal line `✓ Curation-artifact scan: dist/widgets tree scanned, no curation-mode artifacts found.`
    - `npx vitest run scripts/lib/curation-guard.test.mjs` exits 0 with the new whole-tree case reported as passed (not skipped) on a machine where `dist/widgets/index.html` exists.
    - The SUMMARY contains a scanned-vs-skipped file count derived from a quoted command, and the number 22 appears against the `.ts` class.
    - The SUMMARY contains a heading `Operator decision surfaced, not taken` stating both sides of the publish-`.d.ts` question and confirming no build configuration was changed.
    - `git diff --name-only` lists exactly `scripts/lib/curation-guard.mjs` and `scripts/lib/curation-guard.test.mjs` for this plan's code changes.
  </acceptance_criteria>
  <done>The stricter guard is proven green against the real publish tree by command, pinned by a skipIf-guarded regression case, and the `.d.ts` publication question is recorded for the operator without acting on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → `dist/widgets` publish directory | Anything crossing this boundary is deployed to a PUBLIC GitHub Pages site. Vite/esbuild inputs, copy lists and stray developer files all cross it. |
| `dist/widgets` → public internet | Post-deploy, every byte is world-readable and permanently archivable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-11-01 | Information Disclosure | `scripts/curate-overlay/index.ts` (or its emitted `.d.ts`) reaching `dist/widgets` | mitigate | Invert `SCANNED_EXTENSIONS` to `UNSCANNED_EXTENSIONS = ['.json']` so `.ts`/`.d.ts` are content-scanned for `__curate` and the build hard-fails (T2). |
| T-24-11-02 | Information Disclosure | A stray copy of `scripts/curate-server.mjs` — carrying every route literal, the write endpoint shape and the mirror path — under `dist/widgets` | mitigate | Same inversion covers `.mjs`; planted-fixture case observed RED then GREEN (T1/T2). |
| T-24-11-03 | Information Disclosure | An extensionless file containing the marker (e.g. an esbuild output written without a suffix) | mitigate | The `ext === null` skip branch is removed; extensionless files fall through to the content scan (T2b). |
| T-24-11-04 | Tampering | A future contributor re-widening the exemption list "to make the build pass" | mitigate | Every exemption must carry a written justification in the rewritten docblock; the test asserts `UNSCANNED_EXTENSIONS` contains `.json` and NOT `.ts`/`.mjs`/`.js`/`.html`/`.css`/`.map` (T2f). |
| T-24-11-05 | Denial of Service | The stricter scan false-positiving on a legitimate published artifact and breaking every build | mitigate | Verified before planning: the inverted scan yields zero violations over the real tree today; pinned by a `skipIf`-guarded whole-tree regression case and a real `npm run build-widgets` run (T3). |
| T-24-11-06 | Denial of Service | `readFileSync(..., 'utf8')` on newly-reachable binary content | mitigate | Read as `latin1`; the ASCII marker still substring-matches (T2c). |
| T-24-11-07 | Information Disclosure | A leak at a path none of the three hard-coded `expect404` probes in `verify-dashboard-publish.mjs:305-309` covers | accept | Out of this gap's scope per `24-VERIFICATION.md`'s `missing` list, which routes the fix through the build-time layer. The build-time guard now scans the whole tree by content, which is the general-path coverage the HTTP layer lacks; recorded rather than silently ignored. |
| T-24-11-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — zero new dependencies; `git diff --name-only` must not list `package.json` or `package-lock.json`. |
</threat_model>

<verification>
- `npx vitest run scripts/lib/curation-guard.test.mjs` exits 0.
- `npm test` exits 0.
- `npm run build-widgets` exits 0 and prints the `✓ Curation-artifact scan` line.
- `node -e "import('./scripts/lib/curation-guard.mjs').then(m => console.log(JSON.stringify(m.UNSCANNED_EXTENSIONS)))"` prints `[".json"]`.
- `git diff --name-only` does not list `package.json`, `package-lock.json`, `tsconfig.json`, or `scripts/build-widgets.mjs`.
</verification>

<success_criteria>
- The three previously-unscanned extension classes (`.d.ts`/`.ts`, `.mjs`, extensionless) are each recorded FAILING against the unfixed guard and PASSING after the fix, with both outputs quoted in the SUMMARY (D-11).
- `UNSCANNED_EXTENSIONS` is exactly `['.json']` and the docblock states the load-bearing reason for that single entry (D-10).
- The real `dist/widgets` tree, including its 22 `.d.ts` files, produces zero violations and the build still exits 0.
- The `.d.ts`-publication question is surfaced to the operator in the SUMMARY without any build configuration being changed.
</success_criteria>

<output>
Create `.planning/phases/24-local-curation-mode/24-11-SUMMARY.md` when done. It MUST contain the
verbatim RED output from T1 and the verbatim GREEN output from T2 under a heading naming D-11, the
scanned-vs-skipped file census from T3, and the `Operator decision surfaced, not taken` note.
</output>
