---
phase: 24-local-curation-mode
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/24-local-curation-mode/24-CONTEXT.md
autonomous: true
requirements: [CUR-01]

must_haves:
  decisions: [D-04, D-05, D-06, D-10, OD-1, OD-2, OD-3, OD-4]
  truths:
    - "D-04: CUR-01 in REQUIREMENTS.md no longer requires per-distance exclusion selectability, and carries a dated note pointing at 24-CONTEXT.md D-04"
    - "D-04: ROADMAP.md's Phase 24 goal, milestone-checklist line, success criterion 1 and success criterion 4 all describe whole-activity exclusion, each with a dated amendment marker"
    - "OD-2: 24-CONTEXT.md D-10 carries a dated note recording that the build-time guard is called at the END of buildAllWidgets() after buildDashboard(), amending D-10's literal same-call-site wording"
    - "OD-1: 24-CONTEXT.md D-07 carries a dated note resolving the D-03/D-07 re-render tension to a full location.reload(), with the rejected narrow-DOM-patch alternative recorded"
    - "OD-3/OD-4: 24-CONTEXT.md's Claude's Discretion section records that no UI-SPEC is produced for this phase and that the curate port is fixed at 4173"
    - "After the amendment, every written criterion the phase is judged against is satisfiable by what the phase actually builds"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended CUR-01 with a dated D-04 note"
      contains: "Amended 2026-08-27"
    - path: ".planning/ROADMAP.md"
      provides: "Amended Phase 24 goal, checklist line and criteria 1 and 4"
      contains: "amended 2026-08-27 per D-04"
    - path: ".planning/phases/24-local-curation-mode/24-CONTEXT.md"
      provides: "Dated OD-1..OD-4 orchestrator-decision notes attached to the decisions they amend"
      contains: "OD-2"
  key_links:
    - from: ".planning/REQUIREMENTS.md CUR-01"
      to: ".planning/phases/24-local-curation-mode/24-CONTEXT.md D-04"
      via: "dated amendment note citing the decision id"
      pattern: "D-04"
    - from: ".planning/ROADMAP.md Phase 24 criteria"
      to: ".planning/phases/24-local-curation-mode/24-CONTEXT.md D-04"
      via: "dated amendment marker in criteria 1 and 4"
      pattern: "D-04"
---

<objective>
This phase owns a requirements change. D-04 drops CUR-01's per-distance clause in favour of
whole-activity exclusion, and that decision is worthless until the written criteria the phase
is judged against actually say so. Amend `REQUIREMENTS.md`'s CUR-01, `ROADMAP.md`'s Phase 24
goal, milestone-checklist line and criteria 1 and 4, and record the four orchestrator decisions
(OD-1..OD-4) as dated notes inside `24-CONTEXT.md` beside the decisions they amend.

Purpose: leaving the old text in place would leave Phase 24 permanently unable to satisfy its
own written criteria — every verification pass would read "toggle PR exclusion per distance"
against a UI that deliberately does not offer it. The dated notes are equally load-bearing:
OD-2 in particular is an amendment to a locked decision, and an undocumented amendment is
indistinguishable from a planner quietly ignoring a decision.

Output: three amended planning documents, each edit carrying a date and a decision id.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-local-curation-mode/24-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" id="T1">
  <name>Task 1: Amend CUR-01 in REQUIREMENTS.md (D-04)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` line 51 — the current CUR-01 entry in full, and line 95's
      `| CUR-01 | Phase 24 | Pending |` traceability row
    - `.planning/REQUIREMENTS.md`'s surrounding entries (TRN-02, TRN-03, FIX-01) — note the
      house style for dated amendments and plan citations, and note that requirements are ticked
      only on rendered-checkpoint evidence, never on a mechanical frontmatter match
    - `.planning/phases/24-local-curation-mode/24-CONTEXT.md` D-04 — the developer's verbatim
      statement and the two engine facts that de-fang the original per-distance rationale
  </read_first>
  <action>
    Rewrite CUR-01's body (line 51) so that:

    - The opening clause is unchanged: the user can toggle an activity's exclusion from PR
      calculations from the interface rather than hand-editing `data/best-effort-exclusions.json`,
      via a local curation mode (`npm run curate`) with a localhost-only write path.
    - The sentence beginning "Per-distance exclusion must be selectable, not just whole-run — a
      GPS spike typically corrupts only the short splits while the same run's 5k/10k remain
      honest." is DELETED.
    - The remaining clauses are unchanged: a reason is required and is surfaced in the detail
      view; the write path must be provably absent from the published bundle, verified by an
      assertion in `verify-dashboard-publish.mjs` following the `assertNoPrivateArtifacts`
      precedent.
    - A bolded dated amendment note is appended, of the form
      `**Amended 2026-08-27 (Phase 24 CONTEXT.md D-04):**` followed by: exclusion is
      whole-activity, not per-distance — an excluded activity is withheld from PR calculations
      of every kind but still counts toward aggregates (distance, time, etc). The original
      per-distance clause is dropped because `computeActivityEfforts` emits exactly one effort
      per target distance per activity and `compute-best-efforts.ts:215-219` drops that effort
      from `byDistance` entirely when excluded, so there is never a same-activity runner-up a
      coarser exclusion could wrongly suppress. Also state that per-distance selectability is
      recorded as a Deferred Idea in `24-CONTEXT.md` and remains supported by
      `buildExclusionIndex` on read (D-05), so a future phase could surface it without a schema
      migration.

    Leave CUR-01's checkbox as `- [ ]` — it is ticked only by plan 24-08's rendered checkpoint,
    per this repo's established pattern (Phase 19 UI-02, plan 19-15). Leave the line-95
    traceability row at `Pending` for the same reason. Change no other requirement.
  </action>
  <acceptance_criteria>
    - `grep -F -c "Per-distance exclusion must be selectable" .planning/REQUIREMENTS.md` returns `0`
    - `grep -F -c "Amended 2026-08-27 (Phase 24 CONTEXT.md D-04)" .planning/REQUIREMENTS.md` returns `1`
    - CUR-01's line still contains `npm run curate`, `assertNoPrivateArtifacts` and
      `surfaced in the detail view`
    - CUR-01's checkbox is still `- [ ] **CUR-01**`
    - `grep -F -c "| CUR-01 | Phase 24 | Pending |" .planning/REQUIREMENTS.md` returns `1`
    - `git diff --stat .planning/REQUIREMENTS.md` shows changes on the CUR-01 line only
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -F -c 'Per-distance exclusion must be selectable' .planning/REQUIREMENTS.md)" = "0" &amp;&amp; test "$(grep -F -c 'Amended 2026-08-27 (Phase 24 CONTEXT.md D-04)' .planning/REQUIREMENTS.md)" = "1" &amp;&amp; grep -F -q '- [ ] **CUR-01**' .planning/REQUIREMENTS.md &amp;&amp; echo req-ok</automated>
  </verify>
  <done>CUR-01 describes whole-activity exclusion, carries a dated D-04 note explaining why the per-distance clause was dropped and where it went, and is still unticked pending the rendered checkpoint.</done>
</task>

<task type="auto" id="T2">
  <name>Task 2: Amend ROADMAP.md's Phase 24 goal, checklist line and criteria 1 and 4 (D-04)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` line 74 — the v2.1 milestone checklist line for Phase 24
    - `.planning/ROADMAP.md` lines 423-436 — the Phase 24 block: Goal, Depends on, Requirements,
      the four Success Criteria, `**Plans**:` and `**UI hint**:`
    - `.planning/ROADMAP.md`'s Phase 23 block immediately above — the house style for criteria
      wording and for a criterion that names a human checkpoint
    - `.planning/phases/24-local-curation-mode/24-CONTEXT.md` D-04 and the Domain section's
      point 1 (the exclusions DATA is public and already shipped; only the write path is private)
  </read_first>
  <action>
    Four edits, all in `.planning/ROADMAP.md`.

    (a) Line 74's milestone checklist entry: replace `toggle per-distance PR exclusions` with
    `toggle whole-activity PR exclusion`. Leave the rest of the line and its `- [ ]` checkbox alone.

    (b) The Phase 24 `**Goal**:` line: remove the parenthetical `(or a single distance's)` so the
    goal reads as toggling an activity's exclusion from PR calculations through a localhost-only
    UI, with a required reason surfaced in the detail view and the write path provably absent
    from the published bundle. Append `(**amended 2026-08-27 per D-04**)` at the end of the line.

    (c) Success criterion 1: replace the clause `toggle PR exclusion per distance — not just
    whole-run, since a GPS spike typically corrupts only the short splits while the same run's
    5k/10k remain honest` with wording that says: toggle whole-activity PR exclusion, surfaced as
    an inline control on the activity detail view's "Best Efforts This Run" panel, followed by
    `(**amended 2026-08-27 per D-04** — the original per-distance clause is dropped; see
    24-CONTEXT.md D-04)`.

    (d) Success criterion 4: drop `per-distance` from `toggle a per-distance exclusion end-to-end
    with a reason` so it reads `toggle a whole-activity exclusion end-to-end with a reason`, and
    append the same `(**amended 2026-08-27 per D-04**)` marker. Keep the rest of criterion 4
    verbatim, including the second half about loading the production build served under
    `/strava-widgets` and confirming no curation write endpoint is present or reachable — that
    half is untouched by D-04.

    Do NOT touch criterion 2 or criterion 3 — both are already correct as written. Do NOT touch
    the `**Plans**:` line or any plan list under Phase 24; those are maintained by the planner.
    Do not renumber criteria.
  </action>
  <acceptance_criteria>
    - `grep -F -c "per-distance" .planning/ROADMAP.md` returns `0` within the Phase 24 block
      (verified by `sed -n '/### Phase 24/,/### Phase 25/p' .planning/ROADMAP.md | grep -F -c 'per-distance'` returning `0`)
    - `sed -n '/### Phase 24/,/### Phase 25/p' .planning/ROADMAP.md | grep -F -c 'amended 2026-08-27 per D-04'` returns `3`
      (goal, criterion 1, criterion 4)
    - Line 74 contains `whole-activity PR exclusion` and no longer contains `per-distance`
    - Criterion 3's text is byte-identical to before this task (it already reads correctly)
    - Criterion 4 still contains `no curation write endpoint is present or reachable`
    - The Phase 24 block still has exactly 4 numbered success criteria
  </acceptance_criteria>
  <verify>
    <automated>test "$(sed -n '/### Phase 24/,/### Phase 25/p' .planning/ROADMAP.md | grep -F -c 'per-distance')" = "0" &amp;&amp; test "$(sed -n '/### Phase 24/,/### Phase 25/p' .planning/ROADMAP.md | grep -F -c 'amended 2026-08-27 per D-04')" = "3" &amp;&amp; sed -n '74p' .planning/ROADMAP.md | grep -F -q 'whole-activity PR exclusion' &amp;&amp; echo roadmap-ok</automated>
  </verify>
  <done>Every written statement of Phase 24's scope — checklist line, goal and criteria 1 and 4 — describes whole-activity exclusion and carries a dated pointer back to D-04.</done>
</task>

<task type="auto" id="T3">
  <name>Task 3: Record the four orchestrator decisions as dated notes in 24-CONTEXT.md</name>
  <files>.planning/phases/24-local-curation-mode/24-CONTEXT.md</files>
  <read_first>
    - `.planning/phases/24-local-curation-mode/24-CONTEXT.md` — the whole `<decisions>` block,
      paying particular attention to D-03's rejected-alternatives list, D-07's Save-mirrors
      paragraph, D-10's `(a)`/`(b)` two-layer wording, and the `### Claude's Discretion` list
    - `.planning/phases/24-local-curation-mode/24-RESEARCH.md` § "Assumptions Log" rows A1 and A2
      and § "Open Questions" 1, 2 and 3 — these are the questions OD-1, OD-2 and OD-4 close
    - `.planning/phases/24-local-curation-mode/24-CONTEXT.md`'s existing D-04 sub-bullet style —
      the notes below follow the same indented-bullet form so they read as amendments to the
      decision, not as new decisions
  </read_first>
  <action>
    Add four dated notes, each as an indented sub-bullet under the decision it amends, each
    opening with a bolded `**Orchestrator decision 2026-08-27 (OD-N):**` marker so it is
    visually distinct from the developer's own locked text.

    Under D-10, add OD-2: the build-time guard is called at the END of `buildAllWidgets()`,
    after `await buildDashboard()`, scanning the fully-populated `dist/widgets` tree — not from
    inside `copyDataFiles()` where `assertNoPrivateArtifacts` fires. Record the reason: that call
    site runs BEFORE `buildPages()`/`buildDashboard()`, so a guard there could never see a leaked
    curate bundle or `__curate` marker in JS/HTML, which is the most likely vector and exactly
    the never-red guard D-11 exists to prevent. State that D-10's intent (same guard shape, same
    `process.exit(1)` discipline, defined beside `assertNoPrivateArtifacts` in the file) is
    preserved and only the call site moves. Also record that the guard's content scan is
    restricted to `.js`/`.html`/`.css`/`.map` so it can never catch the public, already-shipped
    `dist/widgets/data/best-effort-exclusions.json`.

    Under D-07, add OD-1: the "re-renders" in D-07 is discharged by a full `location.reload()`
    after the server mirrors the written file into `dist/widgets/data/best-effort-exclusions.json`.
    The existing, unmodified `detail.ts`/`detail-sections.ts` renderer repaints from the fresh
    file — one renderer, no drift seam, which is what reconciles D-07 with D-03's rejection of the
    overlay rebuilding the panel. Record the rejected alternative explicitly: a narrow DOM patch
    of the flags cell, rejected because it would couple the overlay to the badge's internal
    markup — the exact coupling D-03's rejected-MutationObserver note warns against.

    Under `### Claude's Discretion`, add OD-4 against the existing "Port choice" bullet: the
    curate server binds a FIXED port 4173 held in a single named constant, prints
    `http://127.0.0.1:4173/strava-widgets/` on start, and fails fast with a clear message if the
    port is in use rather than hunting for a free one — a curate server on an unpredictable port
    is one the developer bookmarks wrongly. Note that 4173 is free in this repo (no `vite preview`
    script exists in `package.json`).

    Also under `### Claude's Discretion`, add OD-3: no UI-SPEC.md is produced for this phase. The
    overlay is a developer-only local tool that never ships; `src/dashboard/styles.css` lines
    ~1618-1689 already carry Phase 19's bare-element `input`/`textarea`/`button` baseline and its
    comment explicitly anticipates "Phase 24's curation UI", so plain elements inherit the
    treatment with zero new CSS. D-08's ASCII sketch in the `<specifics>` section is the
    interaction contract.

    Do not alter any existing sentence of D-01..D-12. These are additions only.
  </action>
  <acceptance_criteria>
    - `grep -F -c "Orchestrator decision 2026-08-27 (OD-1)" .planning/phases/24-local-curation-mode/24-CONTEXT.md` returns `1`, and likewise for OD-2, OD-3, OD-4
    - The OD-2 note's line index is greater than the index of `**D-10:` and less than the index of `**D-11:`
    - The OD-1 note's line index is greater than the index of `**D-07:` and less than the index of `**D-08:`
    - The OD-3 and OD-4 notes appear inside the `### Claude's Discretion` section
    - The OD-2 note contains the literal `buildAllWidgets()` and `buildDashboard()`
    - The OD-4 note contains the literal `4173` and `127.0.0.1`
    - `git diff .planning/phases/24-local-curation-mode/24-CONTEXT.md` shows additions only —
      zero deleted lines other than whitespace
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "const s=require('fs').readFileSync('.planning/phases/24-local-curation-mode/24-CONTEXT.md','utf8');for(const n of [1,2,3,4]){if(!s.includes('Orchestrator decision 2026-08-27 (OD-'+n+')'))process.exit(1)}const i=k=>s.indexOf(k);if(!(i('(OD-2)')>i('**D-10:')&&i('(OD-2)')<i('**D-11:')))process.exit(1);if(!(i('(OD-1)')>i('**D-07:')&&i('(OD-1)')<i('**D-08:')))process.exit(1);if(!s.includes('4173'))process.exit(1);console.log('context-ok')"</automated>
  </verify>
  <done>OD-1, OD-2, OD-3 and OD-4 are recorded as dated amendments beside the decisions they touch, so a later reader can see exactly which locked wording was amended, when, and why.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| written criteria -> verification agents | Every downstream verifier, checker and human checkpoint reads these files as the definition of done |
| locked developer decisions -> planner/executor amendments | An amendment that is not written down is indistinguishable from an ignored decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-DOC-01 | Repudiation | ROADMAP/REQUIREMENTS criteria | mitigate | Every edit carries the literal date `2026-08-27` and the decision id `D-04`, and Task 1/Task 2 acceptance criteria count those markers exactly |
| T-24-DOC-02 | Repudiation | amended locked decisions D-07 and D-10 | mitigate | OD-1 and OD-2 are written as bolded, dated sub-bullets under the decision they amend, with the rejected alternative recorded, and their placement is asserted by line-index checks |
| T-24-DOC-03 | Tampering | scope creep via an over-broad edit | mitigate | Acceptance criteria pin criteria 2 and 3 as byte-identical, keep CUR-01's checkbox unticked and its traceability row `Pending`, and require an additions-only diff on 24-CONTEXT.md |
| T-24-CUR-01 | Information Disclosure | published bundle | accept | This plan touches only `.planning/` documents; nothing it writes reaches `dist/widgets` |
| T-24-SC | Tampering | npm installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `sed -n '/### Phase 24/,/### Phase 25/p' .planning/ROADMAP.md | grep -F -c 'per-distance'` returns `0`
- `grep -F -c 'Per-distance exclusion must be selectable' .planning/REQUIREMENTS.md` returns `0`
- All four `Orchestrator decision 2026-08-27 (OD-N)` markers are present in `24-CONTEXT.md`
- `git diff --name-only` lists exactly the three `.planning/` files in `files_modified`
- No source file, script or config was touched by this plan
</verification>

<success_criteria>
- CUR-01, the Phase 24 milestone-checklist line, the Phase 24 goal and criteria 1 and 4 all
  describe whole-activity exclusion
- Each amended statement carries the date `2026-08-27` and cites D-04
- OD-1, OD-2, OD-3 and OD-4 are recorded in `24-CONTEXT.md` beside the decisions they amend
- Criterion 2, criterion 3, CUR-01's unticked checkbox and its `Pending` traceability row are
  unchanged
</success_criteria>

<output>
Create `.planning/phases/24-local-curation-mode/24-03-SUMMARY.md` when done.
</output>
