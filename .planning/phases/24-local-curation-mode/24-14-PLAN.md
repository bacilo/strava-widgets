---
phase: 24-local-curation-mode
plan: 14
type: execute
wave: 8
depends_on: ["24-11", "24-12", "24-13"]
files_modified:
  - .planning/phases/24-local-curation-mode/24-VALIDATION.md
  - .planning/phases/24-local-curation-mode/24-VERIFICATION.md
  - .planning/phases/24-local-curation-mode/24-REVIEW.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/2026-08-12-exclusion-tickbox-local-curation-mode.md
  - .planning/todos/completed/2026-08-12-exclusion-tickbox-local-curation-mode.md
autonomous: false
gap_closure: true
closes_gaps: [GAP-24-02, GAP-24-03, GAP-24-04]
requirements: [CUR-01]

must_haves:
  decisions: [D-04, D-05, D-07, D-08, D-09, D-10, D-11, D-12, OD-1, OD-4]
  truths:
    - "Round 3 runs against ONE fresh build whose asset hashes are recorded and confirmed to DIFFER from Round 2's index-UHckEgvm.js — an unchanged hash means plan 24-13's change is not in the bytes being tested"
    - "Every extent claim is judged against a value derived from data/stats/best-efforts.json, weekly-distance.json and monthly-stats.json BEFORE any write — never against what the UI says about itself"
    - "R24 reads the header PR badges AND every Best Efforts flags cell in the SAME paint after Save, and neither claims a PR the live document excludes — the PRExcluded string R15 quoted is absent"
    - "R26 closes the mirror direction against an independently-derived discriminator: at that moment the precomputed document still says excludedFromRecords true, so badges matching the pre-write pinned PR set can only have come from the live document"
    - "R28 observes the curation guard RED against a planted .d.ts, a planted .mjs and a planted extensionless file in the REAL dist/widgets, then GREEN once removed — the three classes D-11's proof had never exercised"
    - "R30 observes the curate server survive GET /% and reject cross-origin and mismatched-Host requests on the static route as well as the write routes"
    - "CUR-01 and the ROADMAP phase gate are re-ticked ONLY if every mapped row is PASS; otherwise both stay open and the next GAP-24-NN is opened verbatim"
    - "The archive data/best-effort-exclusions.json is byte-identical to its pre-checkpoint state at session end, and HEAD equals a developer-recorded BASELINE_HEAD (D-09)"
  artifacts:
    - path: ".planning/phases/24-local-curation-mode/24-VALIDATION.md"
      provides: "An APPENDED Round 3 section with a fresh gate run, pinned expected values, R24-R31 verdicts, a final state check and the disposition"
      contains: "Round 3 Checkpoint"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CUR-01's disposition, earned on this round's rendered and executed evidence"
      contains: "CUR-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 24 plan checklist extended with 24-11..24-14 and the gate note updated"
      contains: "24-14-PLAN.md"
  key_links:
    - from: "data/stats/best-efforts.json (pinned BEFORE any write this round)"
      to: "R24's and R26's header-badge extent assertions"
      via: "PINNED_PR_SET — the exact set of distances the target activity holds wasPRAtTheTime true for"
      pattern: "wasPRAtTheTime"
    - from: "the same precomputed document, still carrying excludedFromRecords true at R26 time"
      to: "R26's discriminating mirror proof"
      via: "no Recompute pressed between R25 and R26"
      pattern: "excludedFromRecords"
    - from: "planted .d.ts / .mjs / extensionless fixtures in the real dist/widgets"
      to: "npm run build-widgets exit 1"
      via: "plan 24-11's UNSCANNED_EXTENSIONS skip-list"
      pattern: "Curation-artifact guard failed"
---

<objective>
Hold a BLOCKING Round 3 checkpoint that observes the three gap-closure fixes from plans 24-11,
24-12 and 24-13 against the real build, the real curate server and the real publish tree — then set
CUR-01's disposition and the Phase 24 gate on that evidence, never before it.

Purpose: `24-VERIFICATION.md` scored 2/5 and reopened the gate. Round 2 recorded "9/9 PASS, clean
sweep" and its own R15 evidence contains the defect it missed — `the 400m flags cell read
PRExcluded — ROUND2-2026-09-01 GPS device unreliable` — quoted but never compared against anything.
That is the failure mode this round is designed against: every row here asserts against a value
derived from a data file before any write, not against the UI agreeing with itself.

`24-VERIFICATION.md` states GAP-1 and GAP-2 are code-level and reproducible by command and do NOT
need a browser round. They are therefore terminal rows here (R28-R31), not gestures. GAP-3's defect
was visible on screen and recorded PASS, so it gets real gestures (R24-R27) — which means a human.

Output: an APPENDED Round 3 section in `24-VALIDATION.md` with eight recorded verdicts, an earned
CUR-01 disposition, dated closure notes on the three review findings, and either a closed origin
todo or the next GAP-24-NN opened verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-local-curation-mode/24-VALIDATION.md
@.planning/phases/24-local-curation-mode/24-VERIFICATION.md
@.planning/phases/24-local-curation-mode/24-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<checkpoint_discipline>
Four house rules this round inherits, all earned the hard way in this repository. Every row below
is written to satisfy them; the executor must not relax any of them mid-round.

1. **A row must assert reachable extent against an INDEPENDENTLY-DERIVED value.** Round 2 produced
   a clean sweep and still missed a Critical rendered in its own evidence, because its rows checked
   only that the UI agreed with itself. Expected values are derived from `data/stats/best-efforts.json`,
   `data/stats/weekly-distance.json`, `data/stats/monthly-stats.json` and
   `data/best-effort-exclusions.json` in Task 1, BEFORE any write, and recorded in the plan output.
2. **Real gestures need a human.** This plan is `autonomous: false` and BLOCKING. R26's native
   `window.confirm()` blocks browser automation outright — it was human-performed in both Round 1
   (R10) and Round 2 (R19) and must be again.
3. **Stale-asset trap.** Staged builds serve stale `index.html`/`index.json` from checkpoints.
   Binding to `127.0.0.1` alone is NOT sufficient. Every row that follows a fixture edit specifies
   a hard reload, and every render verdict must exclude the cache trap BEFORE it is recorded:
   confirm `performance.getEntriesByType('navigation')[0].type === "reload"`, confirm the app
   refetched `best-effort-exclusions.json`, and compare a cache-busted fetch body against the plain
   one.
4. **Quotable evidence.** Every row names the exact string or number to read back, so the record is
   re-auditable. Round 2's WR-05 miss happened because a contradictory string WAS quoted and was
   never compared to anything.
</checkpoint_discipline>

<tasks>

<task type="auto" id="T1">
  <name>Task 1: Fresh gate, recorded build identity, and expected values re-derived from disk BEFORE any write</name>
  <files>.planning/phases/24-local-curation-mode/24-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` § `## Fresh Gate Run (plan 24-10, Round 2)` and § `### Expected values re-derived LIVE from disk (2026-09-01, BEFORE any write this round)` — the five gate commands, the derivation shape, and the explicit note that a literal HEAD hash cannot serve as the D-09 baseline because committing the pin advances HEAD past it.
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` § `### Build identity (this gate run)` — Round 2's recorded hashes, including `index-UHckEgvm.js`, which this round's JS hash must differ from.
    - `.planning/phases/24-local-curation-mode/24-11-SUMMARY.md`, `24-12-SUMMARY.md`, `24-13-SUMMARY.md` — the three fixes this round tests, including 24-13's recorded post-fix bundle hash and 24-11's scanned-vs-skipped census.
    - `.planning/phases/24-local-curation-mode/deferred-items.md` — the known worktree `node_modules`/`data/stats` ENOENT gap, so it is not mistaken for a regression.
  </read_first>
  <action>
    (a) From a clean tree, run the five gate commands in order and record each exit code and its
    notable output into a new `## Fresh Gate Run (plan 24-14, Round 3)` section APPENDED to
    `24-VALIDATION.md`: `npm test`, `npx tsc --noEmit`, `npm run build`, `npm run build-widgets`,
    `npm run verify-dashboard`. All five must exit 0. Do not rewrite or edit any Round 1 or Round 2
    section of that file — every change in this plan is additive.

    (b) Record the build identity the checkpoint will run against: the
    `dist/widgets/assets/index-*.js` and `dist/widgets/assets/index-*.css` filenames. State
    explicitly whether the JS hash differs from Round 2's `index-UHckEgvm.js`. If it does NOT
    differ, STOP and record it as a blocking finding: plan 24-13 changed `detail.ts` and
    `detail-best-efforts-logic.ts`, both in the dashboard entry graph, so an identical JS hash means
    the checkpoint would be testing the pre-fix bytes.

    (c) Re-derive every expected value LIVE from disk in this session, BEFORE any write. Do not copy
    Round 2's literals forward — `data/stats/best-efforts.json` was regenerated by Round 2's two
    Recompute presses. Derive and record, each with the command used:
    - **TARGET_ACTIVITY**: an activityId holding rank 1 in at least one distance's `rankings` entry
      in `data/stats/best-efforts.json`, plus its `startDate`.
    - **PINNED_PR_SET**: the exact set of `TargetDistanceKey` values for which TARGET_ACTIVITY's
      entry has `wasPRAtTheTime === true`, together with the human-readable badge labels those
      map to via `DISTANCE_DISPLAY_NAMES` (e.g. `400m` maps to `PR — 400m`, `5k` maps to `PR — 5K`).
      This set is the discriminator R24 and R26 assert against and it MUST be non-empty; if it is
      empty, pick a different TARGET_ACTIVITY.
    - **PINNED_PRECOMPUTED_EXCLUSION**: the current `excludedFromRecords` value for each of
      TARGET_ACTIVITY's efforts, recorded as a list. State whether it is currently all-false.
    - **PROMOTION_TARGET**: for one distance where TARGET_ACTIVITY holds rank 1, the rank-2
      activityId, its `durationSec` and its `startDate`. State in words that this id is a DIFFERENT
      string from TARGET_ACTIVITY.
    - **PINNED_WEEK** and **PINNED_MONTH**: from `data/stats/weekly-distance.json` and
      `data/stats/monthly-stats.json`, the `totalKm` and `runCount` for the week and month
      containing TARGET_ACTIVITY's `startDate`.
    - **PINNED_EXCLUSIONS_LENGTH**: the current length of the `exclusions` array in
      `data/best-effort-exclusions.json`, and a byte snapshot of that file copied somewhere outside
      the repo for the end-of-round `cmp`.
    - **PINNED_DTS_COUNT**: `find dist/widgets -name "*.ts" | wc -l`, for R29.

    (d) Record the D-09 baseline OPERATIONALLY, not as a literal: ask the developer to run
    `git rev-parse HEAD` at session start and hold the value; this plan must not pin a hash into a
    committed file, because committing the pin advances HEAD past it. Record only the fact that a
    baseline was taken and by whom.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run build && npm run build-widgets && npm run verify-dashboard && ls dist/widgets/assets/index-*.js</automated>
  </verify>
  <acceptance_criteria>
    - All five gate commands recorded with exit code 0 in the new `## Fresh Gate Run (plan 24-14, Round 3)` section of `24-VALIDATION.md`.
    - The section records the `index-*.js` and `index-*.css` filenames and states in words that the JS hash differs from `index-UHckEgvm.js`.
    - Every pinned value listed in (c) appears in the section with the exact command used to derive it.
    - PINNED_PR_SET is non-empty and its badge labels are written out verbatim.
    - PROMOTION_TARGET's activityId is stated in words to be a different string from TARGET_ACTIVITY.
    - `git diff --stat data/best-effort-exclusions.json` produces no output at the end of this task — nothing has been written yet.
    - No Round 1 or Round 2 line in `24-VALIDATION.md` is modified: `git diff .planning/phases/24-local-curation-mode/24-VALIDATION.md` shows additions only.
  </acceptance_criteria>
  <done>A fresh green gate, a recorded build identity confirmed different from Round 2's, and every expected value pinned from disk before any write.</done>
</task>

<task type="checkpoint:human-verify" id="T2" gate="blocking">
  <name>Task 2: BLOCKING Round 3 browser and command checkpoint (R24-R31)</name>
  <files>.planning/phases/24-local-curation-mode/24-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` § `## Round 2 Checkpoint (R15-R23)` — its `### Evidence provenance`, `### Row verdicts` and `### Final state check` structure, which this round's section must mirror, and its R15 entry, whose quoted `PRExcluded — ROUND2-2026-09-01 GPS device unreliable` is the string R24(f) must prove absent.
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` § `## Round 3` fresh-gate section written by Task 1 — every pinned value (TARGET_ACTIVITY, PINNED_PR_SET, PROMOTION_TARGET, PINNED_WEEK, PINNED_MONTH, PINNED_EXCLUSIONS_LENGTH, PINNED_DTS_COUNT) that the rows below assert against.
    - `.planning/phases/24-local-curation-mode/24-11-SUMMARY.md`, `24-12-SUMMARY.md`, `24-13-SUMMARY.md` — what each fix actually changed, so a row failure can be attributed to the right plan.
    - `.planning/phases/24-local-curation-mode/24-CONTEXT.md` — D-07 (Save mirrors instantly, Recompute is a separate press), D-08 (the two-step commit and confirm-before-delete), D-09 (curate never touches git), D-12 and OD-4.
  </read_first>
  <action>
    Run the eight rows R24-R31 exactly as written in `<how-to-verify>` below, in that order, and
    record each one as its own row in an APPENDED `## Round 3 Checkpoint (R24-R31)` section of
    `24-VALIDATION.md`. The section carries three subsections in Round 2's order: an
    `### Evidence provenance` table disclosing per row-class who observed what (agent-driven
    browser, developer hand, or executor shell), a `### Row verdicts` table with one row per
    R24-R31 carrying a verdict and its quoted evidence, and a `### Final state check`.

    R26 is human-only and BLOCKS: a native `window.confirm()` blocks the browser-automation
    extension outright, exactly as Round 1's R10 and Round 2's R19. Do not record it on an agent's
    behalf and do not synthesise its dialog text from source.

    Every render verdict must exclude the stale-asset cache trap BEFORE it is recorded — navigation
    type `reload`, an observed refetch of `best-effort-exclusions.json`, and a cache-busted body
    compared against the plain one. Binding to `127.0.0.1` is not sufficient on its own.

    Record a row that could not be observed as NOT OBSERVED, never as PASS. Do not fix anything
    found during this round: findings are recorded verbatim and left unpatched, per the house rule
    in force since checkpoint 16-09. Every fixture planted into the real `dist/widgets` in R28 must
    be removed before the round ends, including on failure.
  </action>
  <what-built>
    Three gap-closure fixes, all landed and unit-proven, none yet observed against the real build,
    the real curate server or the real publish tree:
    - **24-11** inverted the curation guard's extension allowlist to a one-entry skip-list
      (`UNSCANNED_EXTENSIONS = ['.json']`), so `.ts`/`.d.ts`, `.mjs` and extensionless files under
      `dist/widgets` are now content-scanned for the `__curate` marker and the build fails closed on
      any extension nobody anticipated.
    - **24-12** made `safeResolve` return `null` on a malformed percent-escape instead of throwing,
      wrapped the `createServer` listener body in try/catch via an extracted `respond500`, and put
      D-12's `isTrustedOrigin` gate on the static route it never covered.
    - **24-13** gave `buildPrBadgeLabels` a required `liveExclusions` parameter and suppressed
      `BestEffortPanelRow.isPr` for live-excluded rows, so the header PR badges and the Best Efforts
      flags cells can no longer contradict each other in one paint.
  </what-built>
  <how-to-verify>
Record every row in an APPENDED `## Round 3 Checkpoint (R24-R31)` section of
`24-VALIDATION.md`, using Round 2's `### Evidence provenance` / `### Row verdicts` / `### Final
state check` structure. Disclose per row-class who observed what — agent-driven browser, human
hand, or executor shell — exactly as Rounds 1 and 2 did. A row that could not be observed is
recorded NOT OBSERVED, never PASS.

**R24 — Save flow: badge renders AND nothing claims a PR the live document excludes.**
Start `npm run curate`, navigate to TARGET_ACTIVITY's detail view at
`http://127.0.0.1:4173/strava-widgets/#/activity/<TARGET_ACTIVITY>`, hard reload.
(a) Confirm `document.querySelectorAll('section[data-activity-id="<TARGET_ACTIVITY>"]').length` is
    `1` and the curation controls render below the panel.
(b) BEFORE any write, read back the header badge container's full text and confirm it contains
    exactly the PINNED_PR_SET labels from Task 1 — quote them. This is the pre-write control: it
    proves the badges are being rendered at all, so their later absence is a suppression and not a
    render failure.
(c) Tick the box, type `ROUND3-<today> GPS device unreliable`, press Save.
    **Press NO Recompute at any point in this row.**
(d) Exclude the cache trap BEFORE recording any render verdict:
    `performance.getEntriesByType('navigation')[0].type` reads `"reload"`; the app refetched
    `best-effort-exclusions.json` after that reload; and a cache-busted fetch body is identical to
    the plain one and already contains the typed reason.
(e) Quote the rendered `Excluded — ROUND3-<today> GPS device unreliable` badge verbatim, and state
    on how many distance rows it appears. Em dash present, NOT the reason-less
    `Excluded from records` fallback.
(f) **The WR-05 row proper.** In the SAME paint, with no reload between (e) and (f), read back and
    quote:
    - the header badge container's full text — it must now be EMPTY of every PINNED_PR_SET label;
    - each Best Efforts row's flags-cell `textContent`, quoted individually.
    Assert in words: no flags cell contains both `PR` and `Excluded`; the literal string `PRExcluded`
    appears nowhere on the page (`document.body.textContent.includes('PRExcluded') === false`); and
    the header badges that were present at step (b) are the ones now absent. **Compare (f) against
    (b) explicitly in the recorded evidence** — quoting the two without comparing them is precisely
    the Round 2 failure this row exists to prevent.
(g) On disk: quote the written entry. `git rev-parse HEAD` still equals BASELINE_HEAD (D-09).

**R25 — Recompute: promotion extent, judged against the pre-write pin.**
Press "Recompute records". Record the streamed progress text if a frame can be captured; if not,
record NOT OBSERVED for that sub-check only, per the R8/R17 precedent, without demoting the row.
After the reload, on the Records screen read rank 1 for the PROMOTION_TARGET's distance and quote
its time, date and the `href` it links to (read the `href`, not a label). Assert in words that the
linked activityId equals Task 1's PROMOTION_TARGET and is a DIFFERENT string from TARGET_ACTIVITY,
and that TARGET_ACTIVITY appears nowhere in that distance's table.
Totals sub-check: read the rendered Calendar cell(s) for PINNED_WEEK and the rendered Trends
monthly value for PINNED_MONTH and confirm they still match Task 1's pinned `totalKm` and
`runCount` — exclusion changes PR rankings and nothing else.
This row also sets up R26: after it, `data/stats/best-efforts.json` carries
`excludedFromRecords === true` for TARGET_ACTIVITY. Read that back from disk and quote it — R26's
proof depends on it.

**R26 — Untick mirror direction, against an independently-derived discriminator. HUMAN HAND.**
This row must be performed by the developer: a native `window.confirm()` blocks the browser
automation extension outright, exactly as in Round 1 R10 and Round 2 R19.
Untick the box. Quote the confirm dialog text verbatim. Press Cancel first: confirm the entry is
still on disk and the checkbox returned to ticked. Untick again and press OK.
**Press NO Recompute before recording this row's verdict.**
(a) Exclude the cache trap exactly as in R24(d).
(b) Read back and quote the header badge container's full text and each flags cell's `textContent`.
    The header must now contain exactly the PINNED_PR_SET labels again, and the flags cells must
    show `PR` on exactly the PINNED_PR_SET distances, with the word `Excluded` appearing nowhere
    (`/Excluded/.test(document.body.textContent) === false`).
(c) **The discriminator, recorded explicitly.** At this moment `data/stats/best-efforts.json` still
    carries `excludedFromRecords === true` for TARGET_ACTIVITY, because R25's Recompute ran WITH
    the exclusion active and nothing has recomputed since — quote that from disk. Therefore a
    header badge or an `isPr` gated on the precomputed flag would render NOTHING. Badges matching
    PINNED_PR_SET can only have come from the live document. State that inference in words in the
    recorded evidence; do not leave it implicit.
(d) On disk: no entry with TARGET_ACTIVITY remains; `grep -c '"distances": \[\]'
    data/best-effort-exclusions.json` is `0`; the `exclusions` array length is back to
    PINNED_EXCLUSIONS_LENGTH (D-05). `git rev-parse HEAD` still equals BASELINE_HEAD.

**R27 — Restore.**
Press "Recompute records" again. Confirm `data/stats/best-efforts.json` rank 1 for the
PROMOTION_TARGET's distance is TARGET_ACTIVITY again and `excludedFromRecords` is back to
PINNED_PRECOMPUTED_EXCLUSION's values. Confirm the rendered Records rank 1 shows TARGET_ACTIVITY's
time and date and links to its id. `git status --porcelain data/best-effort-exclusions.json`
produces no output, and `cmp` against Task 1's byte snapshot confirms the archive is byte-identical.

**R28 — GAP-1: the guard observed RED on the three previously-unscanned classes, in the REAL tree.**
Stop curate first. Plant each of the following into the real `dist/widgets`, one at a time, each
containing the literal `__curate` marker, and after each run `npm run build-widgets` and record its
exit code and the verbatim `✗ Curation-artifact guard failed: <path> — file contents contain the
literal "__curate" marker …` line:
  1. `dist/widgets/shared/curate-overlay.d.ts`
  2. `dist/widgets/assets/curate-server.mjs`
  3. `dist/widgets/assets/overlay` (EXTENSIONLESS)
Each run must exit **1**. Then remove all three, confirm removal, re-run `npm run build-widgets`,
and record exit **0** with the verbatim
`✓ Curation-artifact scan: dist/widgets tree scanned, no curation-mode artifacts found.` line.
Finally confirm the clean rebuild reproduces Task 1's recorded asset hashes, so the later rows are
known to have run against the pinned build identity. **Every planted file must be removed even if
the row fails** — a leftover artifact hard-fails the developer's next build.

**R29 — GAP-1 non-regression: the legitimately-public artifacts are untouched.**
`npm run verify-dashboard` exits **0**, and its output shows `/data/best-effort-exclusions.json`
still 200-and-parsing and `/__curate/health`, `/__curate/overlay.js` and the write endpoint still
404. Separately confirm `find dist/widgets -name "*.ts" | wc -l` still equals PINNED_DTS_COUNT and
that the build exited 0 with those files present — the stricter guard did not start failing on the
tree's own published `.d.ts` class.

**R30 — GAP-2: the curate server survives a malformed URL and gates its static route.**
Restart `npm run curate` and record, in order, each command and its observed status code:
  (a) `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:4173/strava-widgets/` → `200` (control)
  (b) `curl -s -o /dev/null -w '%{http_code}' --path-as-is 'http://127.0.0.1:4173/%'` → a 4xx
  (c) repeat (a) → `200` again, and confirm the curate process is still running. **This is the
      liveness proof; before plan 24-12 the process was dead by now.**
  (d) `-H 'Origin: http://evil.example'` on `/strava-widgets/` → `403`
  (e) `-H 'Host: evil.example'` on `/strava-widgets/` → `403`
  (f) a `PUT` to `/__curate/exclusions/<TARGET_ACTIVITY>` with `Origin: http://evil.example` → `403`,
      and the same with `Host: evil.example` → `403` (R23 re-run)
  (g) `git status --porcelain data/best-effort-exclusions.json` → no output.

**R31 — Criterion 4 re-run against the fresh build: production absence.**
Stop curate (confirm port 4173 dead). Serve `dist/widgets` with a plain static Node server under
`/strava-widgets` on a DIFFERENT port, and load it in a real browser.
(a) Enumerate `button,input,textarea` inside the Best Efforts panel → `[]`, while the panel section
    itself is found (a real absence, not a missing panel).
(b) `document.documentElement.outerHTML.includes('__curate') === false`; the served `index.html`
    contains `0` occurrences of `__curate`; the only script src is the Task 1 bundle filename, with
    no overlay tag.
(c) `GET /strava-widgets/` → `200` (control); `/__curate/health`, `/__curate/overlay.js` and
    `/__curate/exclusions/<TARGET_ACTIVITY>` → `404` each.

**Final state check** (mandatory, mirroring Round 2's): `git status --porcelain
data/best-effort-exclusions.json` empty; `cmp` against Task 1's byte snapshot passes; no
`dist/widgets/__curate`, `dist/widgets/shared/curate-overlay.d.ts`, `dist/widgets/assets/curate-server.mjs`
or `dist/widgets/assets/overlay` remains; `git rev-parse HEAD` equals the developer's BASELINE_HEAD;
port 4173 is free.
  </how-to-verify>
  <resume-signal>
Reply with a verdict for each of R24-R31 (PASS / FAIL / NOT OBSERVED / BLOCKED) plus the quoted
evidence each row asks for. For R24 specifically, quote BOTH the header badge text at step (b) and
at step (f) and state the comparison in words. For R26, quote the confirm dialog verbatim and state
the discriminator inference in words. Type "approved" only alongside those verdicts, never instead
of them.
  </resume-signal>
  <verify>
    <automated>test -f .planning/phases/24-local-curation-mode/24-VALIDATION.md && grep -c "Round 3 Checkpoint (R24-R31)" .planning/phases/24-local-curation-mode/24-VALIDATION.md</automated>
    <human-check>All eight rows recorded with individual verdicts and quoted evidence in the appended Round 3 section.</human-check>
  </verify>
  <acceptance_criteria>
    - `24-VALIDATION.md` contains an APPENDED `## Round 3 Checkpoint (R24-R31)` section with an `### Evidence provenance` table, a `### Row verdicts` table carrying eight individually-evidenced rows, and a `### Final state check`.
    - `git diff .planning/phases/24-local-curation-mode/24-VALIDATION.md` shows additions only — no Round 1 or Round 2 line is modified.
    - R24's evidence quotes the header badge text both before the write and after, and states the comparison between them in words.
    - R24's evidence records `document.body.textContent.includes('PRExcluded') === false`.
    - R26's evidence quotes the confirm dialog verbatim and quotes the on-disk `excludedFromRecords` values that make the discriminator valid.
    - R28's evidence records three separate exit-1 runs naming the three planted paths, and one exit-0 run with the `✓ Curation-artifact scan` line.
    - R30's evidence records seven status codes in order: `200`, a 4xx, `200`, `403`, `403`, `403`, `403`.
    - No row is recorded PASS on a blanket approval; a row nobody observed is recorded NOT OBSERVED.
    - `git status --porcelain data/best-effort-exclusions.json` produces no output at the end, and `cmp` against the Task 1 snapshot passes.
    - `find dist/widgets -name "curate-overlay.d.ts" -o -name "curate-server.mjs" -o -name "overlay" -o -name "__curate"` returns nothing.
  </acceptance_criteria>
  <done>Eight rows recorded with individual verdicts and quotable evidence, the archive restored byte-identical, and every planted fixture removed from the real publish tree.</done>
</task>

<task type="auto" id="T3">
  <name>Task 3: Set the disposition — ONLY on the evidence Task 2 produced</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/24-local-curation-mode/24-VERIFICATION.md, .planning/phases/24-local-curation-mode/24-REVIEW.md, .planning/todos/pending/2026-08-12-exclusion-tickbox-local-curation-mode.md, .planning/todos/completed/2026-08-12-exclusion-tickbox-local-curation-mode.md</files>
  <read_first>
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` — the Round 3 section Task 2 just wrote, in full. This task's every claim must trace to a row in it.
    - `.planning/REQUIREMENTS.md` — the CUR-01 entry at line 51 and the traceability-table row at line 95, both of which carry the `REOPENED 2026-09-02` language that this task updates.
    - `.planning/ROADMAP.md` — line 74 (the Phase 24 milestone-checklist entry ending `Next: /gsd-plan-phase 24 --gaps`), and the § `### Phase 24: Local Curation Mode` block's `**Plans**:` count and plan checklist.
    - `.planning/phases/24-local-curation-mode/24-VERIFICATION.md` — the three `gaps:` entries and their `missing:` lists, each of which must be matched to the row that discharges it.
    - `.planning/phases/24-local-curation-mode/24-REVIEW.md` §§ `### CR-01`, `### CR-02`, `### WR-05`.
    - `.planning/phases/24-local-curation-mode/24-10-SUMMARY.md` and the Phase 19 UI-02 / plan 19-15 precedent it cites — a requirement is never ticked on a mechanical match.
  </read_first>
  <action>
    **Gate, evaluated first.** If ANY of R24-R31 is FAIL or BLOCKED, do NOT tick anything. Instead:
    leave CUR-01 Pending, leave the ROADMAP gate open, open the next `GAP-24-NN` in
    `24-VALIDATION.md` quoting the failing row's evidence verbatim, and record in the SUMMARY that
    the disposition was withheld and why. A row recorded NOT OBSERVED for a non-load-bearing
    sub-check (the streaming progress text in R25) does not block, matching the R8/R17 precedent;
    a row recorded NOT OBSERVED for its primary claim DOES block.

    If every row is PASS:

    (a) `.planning/ROADMAP.md`, § `### Phase 24: Local Curation Mode`: the planner already wrote
    `**Plans**: 14 plans in 8 waves` and added the `**Wave 7**` group (24-11, 24-12, 24-13) and the
    `**Wave 8**` group (24-14) as UNCHECKED `- [ ]` entries. Flip each of those four boxes to
    `- [x]`. Confirm the `**Plans**:` line still reads `14 plans in 8 waves` and that all four
    entries are present; add any that is missing in the one-line-objective style of the existing
    entries before ticking.

    (b) `.planning/ROADMAP.md`, the Phase 24 milestone-checklist entry: append a dated
    `**Round 3 gap closure executed <today> (plans 24-11..24-14, waves 7-8) — PHASE GATE CLOSED**`
    paragraph. It must name, per gap, the row that discharged it and the quoted evidence: GAP-1 by
    R28 and R29 (three planted classes exit 1, clean rebuild exit 0, `verify-dashboard` still 0 with
    the public exclusions file 200-and-parsing), GAP-2 by R30 (the `200 / 4xx / 200` liveness triple
    plus four `403`s), GAP-3 by R24 and R26 (header-vs-panel comparison in one paint, `PRExcluded`
    absent, and R26's independently-derived discriminator). Do NOT retract Round 1's or Round 2's
    recorded evidence — state that it stands and is now sufficient in combination with Round 3's.
    Also flip the checklist box for the Phase 24 line from `- [ ]` to `- [x]` and append
    `(completed <today>)` in the style of the Phase 20 and Phase 23 entries.

    (c) `.planning/REQUIREMENTS.md`: flip CUR-01's checkbox at line 51 from `- [ ]` to `- [x]` and
    append a dated `**RESOLVED <today> (plans 24-11..24-14, Round 3 checkpoint, R24-R31 all PASS).**`
    paragraph carrying the same per-gap row citations. Update the traceability-table row at line 95
    from `**Pending** — REOPENED 2026-09-02 …` to `**Complete**`, retaining the reopening history as
    prior-state text exactly as that row already retains Round 2's. Per the Phase 19 UI-02 / plan
    19-15 precedent, the tick must read as earned on evidence, not as a mechanical match.

    (d) `.planning/phases/24-local-curation-mode/24-VERIFICATION.md`: APPEND a
    `## Gap-Closure Record (Round 3, <today>)` section at the END of the document, with one
    subsection per `gaps:` entry naming the plan that closed it, the row that observed it and the
    quoted evidence. Do NOT edit the frontmatter `status:` or `score:` fields and do NOT rewrite any
    existing prose — those are the verifier's record of what was true on 2026-09-02, and a
    re-verification pass owns changing them. State that explicitly in the appended section.

    (e) `.planning/phases/24-local-curation-mode/24-REVIEW.md`: append a one-line dated
    `**CLOSED <today> by plan 24-NN — see 24-VALIDATION.md row RNN.**` note to the end of each of
    the `### CR-01`, `### CR-02` and `### WR-05` sections. Change nothing else in that file; the
    other 24 findings are explicitly out of this round's scope and must be left exactly as they are.

    (f) Move `.planning/todos/pending/2026-08-12-exclusion-tickbox-local-curation-mode.md` to
    `.planning/todos/completed/` with `git mv`, and append a dated closing note recording that its
    approach B shipped, that its step 2 (per-distance checkboxes) was superseded by D-04, and that
    per-distance selectability remains a Deferred Idea in `24-CONTEXT.md`.

    (g) Record in the SUMMARY, as a NOTE, the one item plan 24-11 surfaced and did not act on: that
    `dist/widgets` publishing 22 `.d.ts` files may itself be unintended, and that stopping their
    publication remains an open operator decision independent of the now-closed guard hole.
  </action>
  <verify>
    <automated>grep -c "24-14-PLAN.md" .planning/ROADMAP.md && grep -c "Gap-Closure Record (Round 3" .planning/phases/24-local-curation-mode/24-VERIFICATION.md && test ! -f .planning/todos/pending/2026-08-12-exclusion-tickbox-local-curation-mode.md</automated>
  </verify>
  <acceptance_criteria>
    - If any row FAILED: `grep -c "^- \[x\] \*\*CUR-01" .planning/REQUIREMENTS.md` returns 0, and `24-VALIDATION.md` contains a new `GAP-24-NN` block quoting the failing row.
    - If all rows PASSED: `grep -c "GAP-24-NN\|GAP-24-05" .planning/phases/24-local-curation-mode/24-VALIDATION.md` returns 0 for newly-opened gaps, and CUR-01's line 51 checkbox is `[x]`.
    - `.planning/ROADMAP.md` lists `24-11-PLAN.md`, `24-12-PLAN.md`, `24-13-PLAN.md` and `24-14-PLAN.md` under Wave 7 and Wave 8 headings, all ticked.
    - `.planning/ROADMAP.md`'s Phase 24 checklist entry contains the literal `PHASE GATE CLOSED` in a paragraph dated today, and cites R28, R30 and R24 by row id.
    - `.planning/REQUIREMENTS.md`'s traceability row for CUR-01 reads `**Complete**` and still contains the words `REOPENED 2026-09-02` as retained history.
    - `git diff .planning/phases/24-local-curation-mode/24-VERIFICATION.md` shows additions only, and the frontmatter `status: gaps_found` line is unchanged.
    - `git diff .planning/phases/24-local-curation-mode/24-REVIEW.md` shows exactly three added lines, one each at the end of CR-01, CR-02 and WR-05.
    - `git status --porcelain .planning/todos/` shows the todo renamed from `pending/` to `completed/`.
    - No file outside this plan's `files_modified` list is changed.
  </acceptance_criteria>
  <done>CUR-01 and the Phase 24 gate carry a disposition that traces, per gap, to a specific Round 3 row and its quoted evidence — or both remain open with the next gap recorded verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planted fixtures → the REAL `dist/widgets` publish directory | R28 plants marker-bearing files into the actual deploy artifact. A leftover file hard-fails the developer's next build, and a leftover file that ALSO got committed would ship the marker to a public site. |
| the curate write path → `data/best-effort-exclusions.json` | R24-R27 write to the developer's real archive. It must return byte-identical. |
| the checkpoint record → future readers | A row recorded PASS becomes the evidence a later requirement tick rests on. Round 2 proved a falsely-confident record is itself a security-relevant failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-14-01 | Information Disclosure | A planted `__curate`-bearing fixture left in `dist/widgets` after R28 and later deployed | mitigate | R28 requires removal even on failure; the Final state check and an explicit `find` acceptance criterion assert none of the four planted paths remains; the clean rebuild must reproduce Task 1's hashes. |
| T-24-14-02 | Tampering | R24-R27 leaving the developer's real `data/best-effort-exclusions.json` mutated | mitigate | Task 1 takes a byte snapshot outside the repo; R27 and the Final state check require `git status --porcelain` empty and `cmp` byte-identical. |
| T-24-14-03 | Repudiation | Recording a row PASS on a blanket approval, as Round 2 effectively did for the WR-05 contradiction | mitigate | Every row names the exact string or number to read back; R24(f) requires an explicit comparison against R24(b); the resume-signal refuses a bare "approved". |
| T-24-14-04 | Repudiation | Ticking CUR-01 before the evidence exists — the exact sequencing error that produced the reopening | mitigate | The tick lives in T3, gated on T2's verdicts, and T3's first instruction is the withhold-on-any-FAIL gate. T1 and T2 change no requirement or roadmap state. |
| T-24-14-05 | Spoofing | The stale-asset trap: recording a render verdict against a cached `index.html`/`index.json` from a previous checkpoint | mitigate | Every render row requires navigation-type `reload`, an observed refetch, and a cache-busted-vs-plain body comparison BEFORE the verdict; Task 1 pins and confirms the bundle hash differs from Round 2's. |
| T-24-14-06 | Denial of Service | R30 leaving a curate server bound to 4173, or R31's static server bound to another port, blocking the developer | mitigate | The Final state check requires port 4173 free and every spawned server stopped. |
| T-24-14-07 | Tampering | Editing the verifier's `24-VERIFICATION.md` frontmatter to manufacture a passing status | mitigate | T3(d) forbids touching `status:`/`score:` and requires additions only; asserted by a `git diff` acceptance criterion. |
| T-24-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; `git diff --name-only` must not list `package.json` or `package-lock.json`. |
</threat_model>

<verification>
- `24-VALIDATION.md` contains `## Fresh Gate Run (plan 24-14, Round 3)` and `## Round 3 Checkpoint (R24-R31)`, both appended, with eight individually-evidenced row verdicts.
- `git diff` on `24-VALIDATION.md` and `24-VERIFICATION.md` shows additions only.
- `git status --porcelain data/best-effort-exclusions.json` produces no output; `cmp` against the Task 1 snapshot passes.
- `find dist/widgets \( -name "curate-overlay.d.ts" -o -name "curate-server.mjs" -o -name "overlay" -o -name "__curate" \)` returns nothing.
- `npm test`, `npx tsc --noEmit`, `npm run build`, `npm run build-widgets` and `npm run verify-dashboard` all exit 0 at the end of the round.
- CUR-01's disposition and the ROADMAP gate note each cite a specific Round 3 row id.
</verification>

<success_criteria>
- All three gaps from `24-VERIFICATION.md` are discharged by a named row with quotable evidence: GAP-1 by R28/R29, GAP-2 by R30, GAP-3 by R24/R26.
- R24 compares the header badges before and after the write and records the comparison in words — not two quotes side by side.
- R26 records the independently-derived discriminator (precomputed still `true`, live now empty) that makes the mirror direction provable rather than merely observed.
- The curation guard is observed RED in the REAL publish tree for a `.d.ts`, a `.mjs` and an extensionless file, then GREEN — the three classes D-11's proof had never exercised (D-10, D-11).
- CUR-01 and the Phase 24 gate are set ONLY after those verdicts exist, or are left open with the next gap recorded verbatim.
- The developer's archive and the publish tree are left exactly as they were found.
</success_criteria>

<output>
Create `.planning/phases/24-local-curation-mode/24-14-SUMMARY.md` when done. It MUST carry the eight
row verdicts with their quoted evidence, the Task 1 pinned values, an explicit statement of whether
the disposition was set or withheld and on which rows, and the `.d.ts`-publication NOTE carried
forward from plan 24-11.
</output>
