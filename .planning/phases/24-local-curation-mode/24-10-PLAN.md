---
phase: 24-local-curation-mode
plan: 10
type: execute
wave: 6
depends_on: ["24-09"]
files_modified:
  - .planning/phases/24-local-curation-mode/24-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/2026-08-12-exclusion-tickbox-local-curation-mode.md
  - .planning/todos/completed/2026-08-12-exclusion-tickbox-local-curation-mode.md
autonomous: false
gap_closure: true
closes_gaps: [GAP-24-01]
requirements: [CUR-01]

must_haves:
  decisions: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, OD-1, OD-2, OD-3, OD-4]
  truths:
    - "Round 2 runs against ONE fresh build whose asset hashes are recorded, and whose JS hash is confirmed to DIFFER from Round 1's index-xwaleiOf.js — an unchanged hash means plan 24-09's change is not in the bytes being tested"
    - "Every extent claim is judged against a value derived from data/stats/best-efforts.json, weekly-distance.json and monthly-stats.json BEFORE any write — never against what the UI says about itself"
    - "R15 re-runs Round 1's FAIL in its original sequencing: tick, reason, Save, badge present WITH the reason, BEFORE any Recompute"
    - "R19 closes the mirror direction: after untick the badge disappears BEFORE any Recompute, and the reason-less Excluded from records fallback does not appear"
    - "The documented staged-build cache trap is excluded before any render verdict is recorded — navigation type reload, the refetch observed, and a cache-busted fetch compared"
    - "The rows plan 24-09 could plausibly regress are re-asserted, not assumed: edit-in-place, Recompute extent, untick and restore, and the three production-absence rows"
    - "D-09 holds: HEAD at the end of the session equals a developer-recorded BASELINE_HEAD, not a literal pinned in a committed file"
    - "Evidence provenance is disclosed per row-class; a row that could not be observed is recorded as not observed, never as passed"
    - "CUR-01 is ticked only if every mapped Round 2 row is PASS; otherwise it is held Pending and the next GAP-24-NN is opened verbatim"
  artifacts:
    - path: ".planning/phases/24-local-curation-mode/24-VALIDATION.md"
      provides: "An APPENDED Round 2 section with pinned expected values, R15-R23 verdicts, a final state check and the frontmatter disposition"
      contains: "Round 2 Checkpoint"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CUR-01's disposition, earned on rendered evidence per the Phase 19 UI-02 / plan 19-15 precedent"
      contains: "CUR-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 24 plan checklist extended with 24-09 and 24-10"
      contains: "24-09-PLAN.md"
  key_links:
    - from: "data/stats/best-efforts.json (pre-exclusion, re-derived this round)"
      to: "R17's promoted-next-best assertion"
      via: "independently derived rank-2 activityId and durationSec, pinned before any write"
      pattern: "rankings"
    - from: "data/stats/weekly-distance.json and monthly-stats.json (pre-exclusion)"
      to: "R18's totals-unchanged assertion"
      via: "pinned totalKm and runCount for the excluded activity's week and month"
      pattern: "totalKm"
    - from: "data/best-effort-exclusions.json written by curate Save"
      to: "the rendered Excluded — {reason} badge"
      via: "plan 24-09's live ExclusionIndex read in detail.ts, with no compute-best-efforts rerun in between"
      pattern: "Excluded — "
---

<objective>
Hold a BLOCKING Round 2 browser checkpoint that re-runs Round 1's single FAIL (R5) in its original
sequencing, closes its mirror direction, and re-asserts every row plan 24-09 could plausibly have
regressed — then set CUR-01's disposition on that rendered evidence.

Purpose: GAP-24-01 is claimed fixed in code by plan 24-09, and nothing in this repository can prove
a rendered badge without a real browser — there is no DOM test environment and no headless browser
anywhere in it. Round 1 also taught two sharper lessons this round must honour: three clean rounds
once missed a Critical because every row asserted only that the UI agreed with itself, so the extent
rows here are judged against values derived from the data files before any write; and a literal HEAD
pin written into a committed file is stale on arrival, so the D-09 baseline is recorded
operationally by the developer, never as a literal in this plan.

Output: an APPENDED Round 2 section in `24-VALIDATION.md` with nine recorded verdicts, a CUR-01
disposition that is earned rather than assumed, and either a closed origin todo or the next
GAP-24-NN opened verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-local-curation-mode/24-VALIDATION.md
@.planning/phases/24-local-curation-mode/24-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/todos/pending/2026-08-12-exclusion-tickbox-local-curation-mode.md
</context>

<tasks>

<task type="auto" id="T1">
  <name>Task 1: Fresh gate, recorded asset hashes, and expected values re-derived from disk BEFORE any write</name>
  <files>.planning/phases/24-local-curation-mode/24-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` § "Expected Values (pinned 2026-08-27, pre-exclusion)"
      — Round 1's pinning, including its explicit note that a literal HEAD hash cannot serve as the
      D-09 baseline because committing the pin advances HEAD past it
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` § "Fresh Gate Run (plan 24-08, Task 1)"
      — the five gate commands and the verbatim lines they must produce
    - `.planning/phases/24-local-curation-mode/24-09-SUMMARY.md` — the build identity plan 24-09
      recorded, which this task's fresh build must be compared against
    - `.planning/phases/24-local-curation-mode/deferred-items.md` — the known worktree
      `node_modules`/`data/stats` ENOENT gap, so it is not mistaken for a regression
  </read_first>
  <action>
    (a) From a clean tree, run the five gate commands in order and record each exit code and its
    notable output into a new `## Fresh Gate Run (plan 24-10, Round 2)` section APPENDED to
    `24-VALIDATION.md`: `npm test`, `npx tsc --noEmit`, `npm run build`, `npm run build-widgets`,
    `npm run verify-dashboard`. Do not rewrite or edit any Round 1 section of that file — every
    change in this plan is additive.

    (b) Record the build identity of the build the checkpoint will run against: the
    `dist/widgets/assets/index-*.js` and `dist/widgets/assets/index-*.css` filenames. State
    explicitly whether the JS hash differs from Round 1's `index-xwaleiOf.js`. If it does NOT
    differ, STOP and record that as a blocking finding: plan 24-09 changed `detail.ts` and
    `detail-best-efforts-logic.ts`, both of which are in the dashboard entry graph, so an identical
    JS hash means the checkpoint would be testing the pre-fix bytes.

    (c) Re-derive every expected value LIVE from disk in this session, BEFORE any write. Do not
    copy Round 1's literals forward — `data/stats/best-efforts.json` was regenerated by Round 1's
    two Recompute presses, so its contents are not assumed to match. Derive and record:
    - The exclusion target: an activityId that holds rank 1 in at least one distance's `rankings`
      entry in `data/stats/best-efforts.json`, plus its `startDate` and every distance it appears
      in with its rank and `durationSec`. Round 1 used `4556693525`; verify it still holds rank 1
      in `400m` and use it if so, otherwise pick another and say which and why.
    - The rank-2 promotion target for the one distance where the target holds rank 1: its
      `activityId` and `durationSec`. Assert IN WRITING that this activityId is a DIFFERENT string
      from the exclusion target's — this is the value R17 is judged against.
    - The weekly total from `data/stats/weekly-distance.json` for the entry whose
      `weekStartISO`..`+7d` window covers the target's `startDate`: `weekStartISO`, `totalKm`,
      `runCount`.
    - The monthly total from `data/stats/monthly-stats.json` for the target's month:
      `periodLabel`, `totalKm`, `runCount`.
    - The current `exclusions` array length in `data/best-effort-exclusions.json`, and that
      `git status --porcelain data/best-effort-exclusions.json` is empty.

    (d) Record the D-09 baseline OPERATIONALLY, not as a literal. Write the instruction into the
    section: before ROW R15 the developer runs `git rev-parse HEAD` and writes the value down; that
    recorded value is `BASELINE_HEAD`, and R15, R19 and the final state check pass only if
    `git rev-parse HEAD` still equals it at the time each is checked. State plainly why: a literal
    pin written into this file is advanced past by the commit of this very file — that is exactly
    how Round 1's `cf18820` was stale on arrival.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run build-widgets &amp;&amp; npm run verify-dashboard &amp;&amp; grep -q 'Fresh Gate Run (plan 24-10, Round 2)' .planning/phases/24-local-curation-mode/24-VALIDATION.md &amp;&amp; test -z "$(git status --porcelain data/best-effort-exclusions.json)"</automated>
  </verify>
  <acceptance_criteria>
    - `24-VALIDATION.md` gains a `## Fresh Gate Run (plan 24-10, Round 2)` section with five
      commands, five exit codes and their notable output
    - `npm run build-widgets` exit 0 and its output includes
      `✓ Curation-artifact scan: dist/widgets tree scanned, no curation-mode artifacts found.`
    - `npm run verify-dashboard` exit 0 with `0 failure(s)`, including
      `✓ GET /data/best-effort-exclusions.json -> 200` and the three `/__curate/*` 404 lines
    - The recorded JS asset hash is quoted and is explicitly compared against `index-xwaleiOf.js`
    - Every expected value in (c) is quoted with the file it came from, and the rank-2 target's id
      is stated in words to differ from the exclusion target's id
    - `git diff .planning/phases/24-local-curation-mode/24-VALIDATION.md | grep '^-' | grep -v '^---'`
      shows no deletion of Round 1 content — the edit is purely additive
    - No literal git hash is written as the D-09 baseline; the operational instruction is present
      instead
    - `git status --porcelain data/best-effort-exclusions.json` is empty at the end of this task
  </acceptance_criteria>
  <done>Round 2 has one fresh build with recorded, changed asset hashes, every expected value re-derived from disk before any write, and an operationally defined D-09 baseline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" id="T2">
  <name>Task 2: BLOCKING Round 2 browser checkpoint (R15-R23) — GAP-24-01 both directions, plus the regression-risk rows</name>
  <files>.planning/phases/24-local-curation-mode/24-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` § "Fresh Gate Run (plan 24-10, Round 2)"
      and its expected values, as pinned by Task 1
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` § "Round 1 Checkpoint (R1-R14)" —
      the row format, the evidence-provenance disclosure table, and R5's recorded FAIL
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` § "Gap-Closure Record" and F-1 —
      the mechanism R15 and R19 exist to disprove
    - `.planning/ROADMAP.md` § "Phase 24" criteria 1-4 as amended 2026-08-27 per D-04
    - `.planning/phases/24-local-curation-mode/24-CONTEXT.md` D-08's interaction contract (no
      UI-SPEC exists for this phase, per OD-3)
  </read_first>
  <action>
    Hold execution and present the `<what-built>` and `<how-to-verify>` blocks below to the
    developer verbatim. Do not summarise a row's instructions, do not run a row on the developer's
    behalf without saying so, and do not proceed past this task until a per-row reply arrives.

    Evidence provenance is a non-waivable disclosure, not a formality. Round 1 was agent-driven via
    Claude-in-Chrome at the developer's explicit delegation and said so. Whatever happens this
    round, record per row-class exactly how the evidence was produced: agent-injected clicks and
    keystrokes in real Chrome are a real browser but NOT a human hand; terminal exit codes run by
    the executor are the executor's, not the developer's. ROW R19's `window.confirm()` blocks the
    browser-automation extension outright, so that row needs a human hand — plan for it rather than
    around it, and if no human is available record R19 as BLOCKED, never as PASS. A row that could
    not be observed is recorded as NOT OBSERVED (Round 1's R8 streaming-text sub-check is the
    precedent), never upgraded.

    No row may be passed on a synthesised event, a headless probe, or a `window.confirm` override.
  </action>
  <what-built>
    Plan 24-09 changed the rendered exclusion badge's data source. `detail.ts` now performs one
    fetch of `data/best-effort-exclusions.json` and returns BOTH the reason and a parsed live
    `ExclusionIndex`; `buildBestEffortsPanelRows` derives each row's `excluded` flag from that live
    index when it loaded, and falls back to the precomputed `excludedFromRecords` only when the
    document could not be fetched or parsed. The precomputed field remains the sole authority for
    Records-screen rankings and promoted next-best efforts — nothing is re-ranked in the browser.
    Everything else about curate is unchanged from Round 1: `npm run curate` serves the built
    `dist/widgets` under `/strava-widgets` on `127.0.0.1:4173`, injects `/__curate/overlay.js`, and
    exposes the write endpoints behind an Origin/Host gate.
  </what-built>
  <how-to-verify>
    ENVIRONMENT RULES (non-waivable):
    - Use `http://127.0.0.1:4173/strava-widgets/...`. Never `localhost`, never a root mount.
    - HARD-RELOAD (Cmd-Shift-R / Empty Cache and Hard Reload) after EVERY step that changes a file
      on disk. This repo has a documented staged-build cache trap and `127.0.0.1` alone is not enough.
    - Before ROW R15, confirm in DevTools > Network that the loaded `assets/index-*.js` and
      `assets/index-*.css` filenames match the hashes Task 1 recorded, AND that the JS hash is not
      Round 1's `index-xwaleiOf.js`. If either check fails, stop — the round is invalid.
    - Before ROW R15, run `git rev-parse HEAD` and write the value down. That is BASELINE_HEAD.
    - Record a verdict per row from {PASS, FAIL, BLOCKED, NOT OBSERVED}. A row is PASS only on the
      evidence it asks for, at the values it asks for. Where a row asks for a value, QUOTE the
      value observed, never "matches" or "looks right".
    - Rows R15-R20 must run in order. R15 and R19 are the two GAP-24-01 rows and neither may be
      preceded by a "Recompute records" press.

    ROW R15 — THE GAP ROW, FORWARD DIRECTION: the badge renders at SAVE, before any Recompute.
      Start `npm run curate` and open
      `http://127.0.0.1:4173/strava-widgets/#/activity/{TARGET_ID}` (Task 1's exclusion target).
      (a) Confirm the panel and controls attached:
          `document.querySelectorAll('section[data-activity-id="{TARGET_ID}"]').length` is exactly `1`,
          and the curation controls render below the Best Efforts panel. Quote the count.
      (b) Tick "Exclude this run from PRs", type a distinctive reason
          (e.g. `ROUND2-{date} GPS device unreliable`) and press Save. DO NOT press
          "Recompute records" at any point in this row.
      (c) EXPECT: after the reload, the Best Efforts panel shows the badge
          `Excluded — ROUND2-{date} GPS device unreliable` on its distance rows. Quote the badge
          text exactly as rendered, including the em dash. A badge reading `Excluded from records`
          with no reason is a FAIL, not a PASS.
      (d) EXCLUDE THE CACHE TRAP BEFORE recording any render verdict, pass or fail:
          quote `performance.getEntriesByType('navigation')[0].type` (must be `"reload"`); confirm
          in the Network panel that `best-effort-exclusions.json` was refetched after that reload;
          and run a cache-busted fetch of the same URL with a unique query string, comparing its
          body to the non-busted one. Quote the comparison result and the `exclusions.length`.
      (e) Confirm on disk: `data/best-effort-exclusions.json` contains a new entry with
          `"activityId": "{TARGET_ID}"`, `"distances": null` and exactly the typed reason. Quote it.
      (f) Confirm `git rev-parse HEAD` still equals BASELINE_HEAD. Quote both.
      (g) Confirm no rebuild ran: quote the `assets/index-*.js` filename still loaded in DevTools.

    ROW R16 — already-excluded state and edit-in-place (Round 1 R7 re-run, badge half now unblocked).
      Hard-reload the same activity. EXPECT the checkbox PRE-TICKED and the textarea PRE-FILLED
      with the stored reason. Quote both. Edit the reason to `ROUND2-{date} edited` and press Save,
      still WITHOUT pressing Recompute. EXPECT: (a) the on-disk reason becomes exactly
      `ROUND2-{date} edited`; (b) the `exclusions` array length is Task 1's recorded length PLUS
      ONE, not plus two — quote the number; (c) the rendered badge now reads
      `Excluded — ROUND2-{date} edited`. Quote the badge text.

    ROW R17 — RECOMPUTE PROMOTES A DIFFERENT ACTIVITY (D-07; the extent row, Round 1 R8 re-run).
      Press "Recompute records". EXPECT progress to stream and the page to reload; if the stream
      completes before a frame can be captured, record the streaming sub-check as NOT OBSERVED
      rather than as passed. Then open the Records screen and read the rank 1 row of the distance
      Task 1 identified. EXPECT its displayed time and date to match Task 1's pinned rank-2
      promotion target, and read the row's LINK TARGET from the `href` (not from a label). Quote
      the href, the time and the date, and STATE IN WORDS that the linked activityId differs from
      `{TARGET_ID}`. Also confirm the excluded activity no longer appears in that distance's table,
      and that the badge in the detail panel still reads `Excluded — ROUND2-{date} edited`.

    ROW R18 — TOTALS ARE UNAFFECTED (the second extent row, Round 1 R9 re-run).
      With the exclusion still active, read the rendered weekly total for the week Task 1 pinned
      and the rendered monthly total for the month Task 1 pinned. EXPECT both to equal the pinned
      pre-exclusion values. Quote all four numbers as rendered. Note that the Calendar clips week
      totals to month boundaries, so a week spanning a month end renders as two partial cells that
      must be summed — quote both cells and the sum if so.

    ROW R19 — THE GAP ROW, MIRROR DIRECTION: untick DELETES the entry and the badge clears BEFORE
    any Recompute. THIS ROW NEEDS A HUMAN HAND — a native `window.confirm()` blocks the
    browser-automation extension outright.
      Untick "Exclude this run from PRs". EXPECT a confirm dialog whose text NAMES THE CONSEQUENCE
      (D-08). Quote the dialog text. Press Cancel first: EXPECT the entry still on disk and the
      checkbox returned to ticked — quote both. Untick again and press OK. Then, WITHOUT pressing
      "Recompute records":
      (a) EXPECT no `Excluded` badge of any kind in the Best Efforts panel — neither
          `Excluded — {reason}` nor the reason-less `Excluded from records` fallback Round 1
          observed at R11. Quote what the flags cell shows instead.
      (b) Exclude the cache trap exactly as in R15(d): quote the navigation type, confirm the
          refetch, and compare a cache-busted fetch.
      (c) On disk: no entry with `"activityId": "{TARGET_ID}"` remains; quote
          `grep -c '"distances": \[\]' data/best-effort-exclusions.json` (must be `0`); quote the
          `exclusions` array length (must be back to Task 1's recorded length).
      (d) Quote `git rev-parse HEAD` and confirm it still equals BASELINE_HEAD.

    ROW R20 — the loop is reversible and the archive is restored (Round 1 R11 re-run).
      Press "Recompute records" again. EXPECT `data/stats/best-efforts.json` rank 1 for the pinned
      distance to be `{TARGET_ID}` once more at Task 1's pinned rank-1 `durationSec`, and the
      rendered Records rank 1 row to link to `#/activity/{TARGET_ID}`. Quote the href, time and
      date. Confirm the word `Excluded` appears NOWHERE on the page. Confirm
      `git status --porcelain data/best-effort-exclusions.json` produces no output — the archive is
      byte-identical to its pre-checkpoint state. Quote the empty result.

    ROW R21 — PRODUCTION HAS NO CURATION PATH (criterion 4, Round 1 R12 re-run; re-asserted because
    plan 24-09 changed shipped dashboard render code).
      Stop curate. Serve `dist/widgets` with a plain static server under `/strava-widgets` on a
      DIFFERENT port and load it in a real browser. EXPECT:
      (a) no curation control renders — quote the result of enumerating
          `button,input,textarea` inside the Best Efforts panel (must be empty);
      (b) `document.documentElement.outerHTML.includes('__curate')` is `false`, and the served
          `index.html` contains 0 occurrences of `__curate` — quote both;
      (c) `/strava-widgets/__curate/health`, `/strava-widgets/__curate/overlay.js` and
          `/strava-widgets/__curate/exclusions/{TARGET_ID}` all return 404, with
          `GET /strava-widgets/` returning 200 as a control — quote all four status codes;
      (d) an in-console `PUT` to the write endpoint returns 404 and leaves
          `git status --porcelain data/best-effort-exclusions.json` empty — quote both.

    ROW R22 — the BUILD-TIME guard goes red against a real planted regression (criterion 3, D-11,
    Round 1 R13 re-run).
      Create `dist/widgets/__curate/overlay.js` by hand containing the literal text `__curate`. Run
      `npm run build-widgets`. EXPECT a non-zero exit and a `✗ Curation-artifact guard failed` line
      naming that path. Quote the line and the exit code. Then `rm -rf dist/widgets/__curate`,
      re-run `npm run build-widgets`, and confirm exit 0 with the `✓ Curation-artifact scan:` line.
      Quote both. Confirm the clean rebuild reproduces Task 1's recorded asset hashes, so R21 is
      known to have run against the pinned build identity — quote the hashes.

    ROW R23 — cross-origin writes are rejected (D-12, T-24-CUR-03, Round 1 R14 re-run).
      Restart `npm run curate`. From a terminal, send a `PUT` to
      `http://127.0.0.1:4173/__curate/exclusions/{TARGET_ID}` with header
      `Origin: http://evil.example` and a JSON body carrying a reason. EXPECT `403`. Repeat with
      header `Host: evil.example` and expect `403`. Quote both status lines. Then confirm
      `git status --porcelain data/best-effort-exclusions.json` is still empty and quote it.

    FINAL STATE CHECK before verdicts are recorded: `git status --porcelain` shows no change to
    `data/best-effort-exclusions.json`; `git rev-parse HEAD` still equals BASELINE_HEAD (naming any
    docs-only commit made mid-session and which rows ran against which baseline, as Round 1 did);
    `dist/widgets/__curate` does not exist.
  </how-to-verify>
  <acceptance_criteria>
    - All nine rows R15-R23 have a recorded verdict from {PASS, FAIL, BLOCKED, NOT OBSERVED}
    - Every PASS row quotes the specific value(s) its instructions asked for; a row answered with
      "looks right", "matches" or a blanket approval is recorded BLOCKED, not PASS
    - R15's verdict quotes the rendered badge text INCLUDING the reason, and quotes all three
      cache-trap exclusions (navigation type, the refetch, the cache-busted comparison) — a render
      verdict recorded without the cache exclusion is invalid in either direction
    - R15's verdict states explicitly that no Recompute was pressed before the badge was observed
    - R16's verdict quotes the array length and states it is Task 1's length plus one, not plus two
    - R17's verdict quotes the promoted activity's href and displayed time and states in words that
      the id differs from `{TARGET_ID}`
    - R18's verdict quotes all four totals values
    - R19's verdict quotes the confirm dialog text, states that a human hand performed it (or is
      recorded BLOCKED), quotes the `"distances": []` count as `0`, quotes the restored array
      length, and states explicitly that no Recompute was pressed before the badge absence was
      observed
    - R21's verdict quotes four status codes, R22's quotes both the failing line with its non-zero
      exit and the restored `✓` line, R23's quotes both `403` status lines
    - The evidence-provenance disclosure names, per row-class, how the evidence was produced
    - The final state check is recorded: unchanged working tree, HEAD equal to BASELINE_HEAD, no
      `dist/widgets/__curate`
    - Any defect found is recorded VERBATIM and left UNPATCHED, per this project's house rule since 16-09
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this row set is manual by design; the automated half already ran in Task 1 (npm test, npx tsc --noEmit, npm run build, npm run build-widgets, npm run verify-dashboard) and plan 24-09's unit and source-structure guards were observed red against planted regressions. This repo has vitest with environment: 'node', no DOM library and no headless browser anywhere in it, so a rendered badge, a native confirm dialog and trusted input are provable only here.</automated>
  </verify>
  <done>Nine rows carry a recorded verdict with the quoted values their instructions asked for, both GAP-24-01 directions were observed with the cache trap excluded first, and the final state check confirms an unchanged working tree, an unchanged HEAD and no leftover dist/widgets/__curate.</done>
  <resume-signal>Reply with a verdict per row (R15-R23). For each PASS, include the quoted value the row asked for. For R15 and R19, include the cache-exclusion evidence and confirm no Recompute preceded the observation. A blanket "all good" cannot be recorded as PASS for any row that asks for a quoted value.</resume-signal>
</task>

<task type="auto" id="T3">
  <name>Task 3: Append Round 2 verdicts, set the CUR-01 disposition, update the ROADMAP checklist</name>
  <files>.planning/phases/24-local-curation-mode/24-VALIDATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/todos/pending/2026-08-12-exclusion-tickbox-local-curation-mode.md, .planning/todos/completed/2026-08-12-exclusion-tickbox-local-curation-mode.md</files>
  <read_first>
    - the developer's checkpoint reply in full
    - `.planning/phases/24-local-curation-mode/24-VALIDATION.md` — the Round 1 section's row format,
      its Gap-Closure Record, its Validation Sign-Off checklist and its frontmatter
    - `.planning/REQUIREMENTS.md` CUR-01 (the `### Carried Forward from v2.0` entry) and its
      `| CUR-01 | Phase 24 | Pending ... |` traceability row — both already record, row by row,
      which Round 1 rows discharged which criterion; the Round 2 note must extend that record, not
      replace it
    - `.planning/REQUIREMENTS.md`'s TRN-03 entry — the house style for recording which rows
      discharged a requirement, in which round, at what values
    - `.planning/ROADMAP.md` § "Phase 24" — the `**Plans**: 8 plans in 4 waves` line and the wave
      checklist ending at 24-08
    - `.planning/todos/pending/2026-08-12-exclusion-tickbox-local-curation-mode.md` — the origin todo
  </read_first>
  <action>
    (a) APPEND a `## Round 2 Checkpoint (R15-R23)` section to `24-VALIDATION.md`, below the Round 1
    section and its Gap-Closure Record. Do not rewrite, renumber or edit Round 1. Include the
    recorded BASELINE_HEAD, the in-browser build identity, an evidence-provenance table in Round
    1's shape, the nine row verdicts quoting the developer's own words and values, and a final
    state check table.

    (b) Resolve GAP-24-01 in the Gap-Closure Record: if R15 and R19 are both PASS, append a dated
    line recording GAP-24-01 as CLOSED and naming the two rows and the quoted badge text that
    closed it. If either is FAIL, record GAP-24-01 as STILL OPEN and open the next gap
    (`GAP-24-02`) verbatim from what was observed, with NO suggested fix — the next planning round
    diagnoses it, per this repo's house rule since 16-09.

    (c) Update `24-VALIDATION.md`'s frontmatter: set `nyquist_compliant: true` ONLY if every one of
    R15-R23 is PASS and every Per-Task Verification Map coverage row is green; otherwise leave it
    `false` and give a one-sentence reason naming the failing row. Set `status:` to `complete` or
    `partial` accordingly. Tick the Validation Sign-Off items that are genuinely satisfied and
    leave the rest unticked rather than claimed.

    (d) CUR-01 disposition in `.planning/REQUIREMENTS.md`. Tick `- [x] **CUR-01**` at its
    `### Carried Forward from v2.0` entry AND set its traceability-table row to `Complete` ONLY if
    every one of R15, R16, R17, R18, R19, R20, R21, R22 and R23 is PASS. If ANY is FAIL, BLOCKED or
    NOT OBSERVED, leave CUR-01 unticked and `Pending` and name exactly which row blocked it and
    what was observed. The checkbox and the table row must agree with each other. Follow the
    Phase 19 UI-02 / plan 19-15 precedent explicitly: a requirement is never ticked on a mechanical
    frontmatter match, only on rendered evidence. Either way, append a dated Round 2 note that
    extends the existing Round 1 record — state which rows discharged which criterion at what
    quoted values, and in particular that criterion 2's render half, which Round 1 recorded as
    FAILING at R5, is now discharged (or not) by R15 and R19.

    (e) Update `.planning/ROADMAP.md`'s Phase 24 section. The `**Wave 5**` and `**Wave 6**` groups
    naming `24-09-PLAN.md` and `24-10-PLAN.md` were added at planning time and the `**Plans**:`
    count line already reads `10 plans in 6 waves` — confirm both are present, tick each plan's
    checkbox to match its actual completion, and correct the count line if it does not match the
    number of `-PLAN.md` lines in the checklist. Do not alter the Goal or the four success criteria
    — they were amended 2026-08-27 per D-04 and this round changes neither.

    (f) Origin todo: move
    `.planning/todos/pending/2026-08-12-exclusion-tickbox-local-curation-mode.md` to
    `.planning/todos/completed/` with a dated closing note ONLY if CUR-01 was ticked in step (d).
    The note must record that this phase is that todo; that its approach B (local curation mode) is
    what shipped; that options A (copy/paste JSON) and C (client-side re-ranking) remain rejected,
    C because it would create two authorities for what counts as a PR; and that its step 2
    (per-distance checkboxes) was superseded by D-04 and is recorded as a Deferred Idea in
    `24-CONTEXT.md`. If CUR-01 was NOT ticked, leave the todo in `pending/` and record in the
    SUMMARY why — closing one early is the same error as the v2.0 audit's stale-todo miscount, in
    the other direction.
  </action>
  <verify>
    <automated>test "$(find .planning/todos -name '*exclusion-tickbox*' | wc -l | tr -d ' ')" = "1" &amp;&amp; grep -q 'Round 2 Checkpoint (R15-R23)' .planning/phases/24-local-curation-mode/24-VALIDATION.md &amp;&amp; grep -q '24-09-PLAN.md' .planning/ROADMAP.md &amp;&amp; grep -q '24-10-PLAN.md' .planning/ROADMAP.md &amp;&amp; test -z "$(git status --porcelain src/ scripts/)" &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `24-VALIDATION.md` contains a `## Round 2 Checkpoint (R15-R23)` section with nine rows, each
      carrying one of PASS / FAIL / BLOCKED / NOT OBSERVED and the developer's quoted evidence
    - `git diff .planning/phases/24-local-curation-mode/24-VALIDATION.md | grep '^-' | grep -v '^---'`
      shows no deletion of any Round 1 row — the Round 2 record is purely additive
    - The Gap-Closure Record carries a dated GAP-24-01 resolution line, or a verbatim GAP-24-02
      with no suggested fix
    - `24-VALIDATION.md` frontmatter's `nyquist_compliant` is consistent with the verdicts, and if
      `false`, a one-sentence reason naming the failing row is present
    - CUR-01's checkbox and its traceability-table row agree with each other and with the
      R15-R23 verdicts
    - CUR-01 carries a dated Round 2 note naming which rows discharged which criterion at what
      quoted values, and explicitly addressing criterion 2's render half
    - `.planning/ROADMAP.md`'s Phase 24 `**Plans**:` count matches the number of `-PLAN.md` lines in
      its checklist, and both `24-09-PLAN.md` and `24-10-PLAN.md` appear
    - `find .planning/todos -name '*exclusion-tickbox*'` returns exactly one path, and it is under
      `completed/` if and only if CUR-01 is ticked
    - `git status --porcelain src/ scripts/` is empty — this task modifies no source file
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Round 2 is recorded row by row in the developer's own words and values as an addition to Round 1, GAP-24-01 is either closed on quoted rendered evidence or replaced by a verbatim successor gap, CUR-01's disposition is earned rather than assumed, and the ROADMAP checklist names both new plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated gate → rendered reality | Automated gates in this repo have missed rendering defects three times; GAP-24-01 itself is a defect every automated coverage row passed through. Only a real browser closes that gap |
| UI self-consistency → external truth | A UI can agree with itself and still be wrong; extent must be judged against values derived from the data files before any write |
| browser cache / staged build → a render verdict | A stale `index.html` or a cached `best-effort-exclusions.json` can manufacture either a false PASS or a false FAIL; the cache trap must be excluded before either verdict is recorded |
| checkpoint session → the repo's committed state | The checkpoint writes to the real `data/best-effort-exclusions.json` and must leave it byte-identical, with HEAD unchanged |
| curate origin → the developer's other browser tabs | The write endpoints are reachable from any tab on the machine; the Origin/Host gate is what closes drive-by CSRF and DNS rebinding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-CACHE | Spoofing (of evidence) | R15 and R19 render verdicts | mitigate | Each row requires the navigation type quoted as `"reload"`, the refetch confirmed in the Network panel, and a cache-busted fetch compared, BEFORE any verdict is recorded — in either direction, not only for a FAIL |
| T-24-EXTENT | Repudiation | R17 and R18 extent claims | mitigate | Both are judged against values Task 1 re-derived from `best-efforts.json`, `weekly-distance.json` and `monthly-stats.json` before any write; R17 additionally requires the promoted id read from the `href` and stated in words to differ from the excluded id |
| T-24-BUILD | Tampering | the bytes the round runs against | mitigate | Task 1 records the fresh asset hashes and requires the JS hash to DIFFER from Round 1's `index-xwaleiOf.js`; R22 confirms a clean rebuild reproduces them, so R21 is known to have run against the pinned identity |
| T-24-CUR-01 | Elevation of privilege | curate bundle or `__curate` marker reaching `dist/widgets` | mitigate | R22 plants a real artifact and requires the build-time guard to exit non-zero naming the path, then exit 0 once removed (D-11) |
| T-24-CUR-02 | Elevation of privilege | write endpoints reachable in the publish bundle | mitigate | R21 requires 404 on all three `/__curate/*` paths from a plain static server, a 200 control, `outerHTML.includes('__curate') === false`, and an in-console `PUT` returning 404 with an unchanged working tree |
| T-24-CUR-03 | Spoofing | cross-origin write from another tab | mitigate | R23 requires `403` for a mismatched `Origin` and for a mismatched `Host`, with the file unchanged afterwards (D-12) |
| T-24-GIT | Tampering | the repository's committed state | mitigate | D-09's baseline is recorded operationally by the developer before R15; R15, R19 and the final state check each re-compare `git rev-parse HEAD` against it. No literal hash is written into a file this plan also commits |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no package; it runs existing scripts only, so the Package Legitimacy Gate has nothing to gate |
</threat_model>

<verification>
- Task 1's five gate commands all exit 0, with the curation-artifact scan line and 0
  `verify-dashboard` failures
- The build identity is recorded and its JS hash differs from Round 1's `index-xwaleiOf.js`
- Nine row verdicts are recorded with quoted values
- `git status --porcelain data/best-effort-exclusions.json` is empty at the end of the session
- `git rev-parse HEAD` equals the developer-recorded BASELINE_HEAD (naming any docs-only commit
  made mid-session and which rows ran against which baseline)
- `dist/widgets/__curate` does not exist
- `find .planning/todos -name '*exclusion-tickbox*'` returns exactly one path
</verification>

<success_criteria>
GAP-24-01 is settled on rendered evidence in both directions: the `Excluded — {reason}` badge is
observed at Save before any Recompute, and observed gone after an untick before any Recompute, with
the staged-build cache trap excluded first in each case. The rows plan 24-09 could have regressed
are re-asserted rather than assumed, including the three production-absence rows, because the fix
ships in the dashboard bundle. CUR-01 is ticked only if every mapped row is PASS; otherwise it is
held Pending with the next gap opened verbatim and the origin todo left in `pending/`.
</success_criteria>

<output>
Create `.planning/phases/24-local-curation-mode/24-10-SUMMARY.md` when done, including: the Round 2
build identity and its comparison to Round 1's, the nine verdicts, the GAP-24-01 resolution or its
successor gap, the CUR-01 disposition with the rows that earned it, and the todo's location.
</output>
