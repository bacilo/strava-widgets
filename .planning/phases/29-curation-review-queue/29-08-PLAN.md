---
phase: 29-curation-review-queue
plan: 08
type: execute
wave: 5
depends_on: ["29-01", "29-02", "29-03", "29-04", "29-05", "29-06", "29-07"]
files_modified:
  - .planning/phases/29-curation-review-queue/29-08-SUMMARY.md
autonomous: false
requirements: [CUR-01, CUR-02, CUR-03]
must_haves:
  decisions:
    - "D-16: Criterion 1 is proved by comparing the queue's rendered extent against scripts/compute-pr-ceiling-recount.mjs's independently-derived activity count, never against the page's own claim"
    - "D-01: the population asserted at this checkpoint is every activity with a non-null demotion (all three guards) — the same population the recount counts, never quietly swapped for the ceiling-only cohort"
    - "D-02: already-excluded activities are present and marked, so the listed set equals the flagged set exactly"
    - "D-14: the header states both counts and the Recompute control carries its filtered-population warning"
    - "PD-01: the developer is asked to approve or reject the third fetch of data/dashboard/index.json for activity names"
  truths:
    - "The queue is reachable from the dashboard in one navigation action"
    - "The number of rendered rows equals the independently-derived flagged activity count"
    - "The number of rows marked excluded equals the independently-derived already-excluded share, each showing its stored reason"
    - "The queue's exclude action writes through the existing PUT/DELETE route and a request from an untrusted origin is rejected"
    - "Both publish guards are green on a correct build and were each demonstrated red under a planted leak"
  artifacts:
    - path: ".planning/phases/29-curation-review-queue/29-08-SUMMARY.md"
      provides: "the checkpoint verdict, the measured numbers of the day, and the developer's PD-01 ruling"
  key_links:
    - from: "the queue page's rendered rows"
      to: "scripts/compute-pr-ceiling-recount.mjs"
      via: "human comparison of two separately-derived numbers"
      pattern: "flaggedActivityCount"
---

<objective>
Close Phase 29 on the human browser checkpoint this project's curation UI convention requires: prove
in a real browser, against the real archive, that the queue lists exactly the flagged population,
that its exclude action goes through the existing write path, that an untrusted origin is rejected,
and that both publish guards are green on the build that was actually served.

Purpose: Criteria 1 and 2 have irreducibly manual halves (a rendered extent and a gesture-driven
write). Criterion 3's automated halves land in plans 29-01/29-02; this checkpoint confirms them green
on the correct build.
Output: a recorded verdict plus the developer's ruling on PD-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-curation-review-queue/29-CONTEXT.md
@.planning/phases/29-curation-review-queue/29-VALIDATION.md
@.planning/phases/29-curation-review-queue/29-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — rebuild, run every guard, and capture the day's independent numbers</name>
  <files>.planning/phases/29-curation-review-queue/29-08-SUMMARY.md</files>
  <read_first>
    - .planning/phases/29-curation-review-queue/29-03-SUMMARY.md (the recount's output shape and the four discriminating numbers measured at planning time)
    - scripts/compute-pr-ceiling-recount.mjs (the CLI flags and output lines)
    - scripts/lib/curation-guard.mjs (`findCurationArtifacts`, called directly below)
  </read_first>
  <action>
Run, in order, and capture every exit code and the relevant output lines verbatim into an
`## Evidence` block in the plan SUMMARY. Stop and report if any step exits non-zero — do not hand a
broken build to the checkpoint.

1. `git status --porcelain data/best-effort-exclusions.json` — must be empty before the checkpoint
   starts, so the write test's diff is unambiguous. Record the current entry count.
2. `npm test` — full suite (`fileParallelism: false` makes this the only reliable way to run the
   curation-guard / verify-dashboard-publish-guard pair together).
3. `npm run build` then `npm run build-widgets` then `npm run verify-dashboard`.
4. `node -e "import('./scripts/lib/curation-guard.mjs').then(m => console.log(JSON.stringify(m.findCurationArtifacts('dist/widgets'))))"`
   — must print `[]`.
5. `node scripts/compute-pr-ceiling-recount.mjs` — record `Flagged ACTIVITIES: N`,
   `of which already excluded: M of T`, and `Recomputed demoted total: E`.
6. Derive the two decoy values the checkpoint discriminates against, and record the commands used:
   the ceiling-guard-only activity count (a `node -e` set over `data/stats/best-efforts.json` counting
   activities with at least one `demotion.guard === 'ceiling'`), and `N - M`.
7. `shasum -a 256 dist/widgets/index.html .curate-dist/queue.js` — record both digests. The published
   copy step skips files by mtime, so a stale artifact is a known trap here; these digests are what
   the checkpoint compares the SERVED bytes against, rather than trusting the build log.

Write the four numbers into the SUMMARY as a dated table labelled "measured today, not reused from
planning" — `29-CONTEXT.md`'s figures (47/65/12/28) are a 2026-09-17 snapshot of an archive the
nightly CI sync grows.

Then start the server with `npm run curate` in the background and record the two URLs it prints.
  </action>
  <verify>
    <automated>npm test && npm run build && npm run build-widgets && npm run verify-dashboard && node scripts/compute-pr-ceiling-recount.mjs | grep -E "Flagged ACTIVITIES|already excluded"</automated>
  </verify>
  <acceptance_criteria>
    - Every command in steps 2–5 exits 0 and its exit code is recorded
    - Step 4 prints exactly `[]`
    - The SUMMARY's Evidence block contains N, M, T, E, the ceiling-only count, `N - M`, and both sha256 digests
    - The four discriminating numbers (N, E, ceiling-only, N-M) are mutually distinct; if any two coincide today, say so explicitly and name which checkpoint row loses discrimination as a result
    - `git status --porcelain data/best-effort-exclusions.json` is empty at the end of this task
  </acceptance_criteria>
  <done>The build, the guards and the independent numbers of the day are captured; the server is running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser checkpoint — queue extent, write path, origin rejection, guard status</name>
  <files>.planning/phases/29-curation-review-queue/29-08-SUMMARY.md</files>
  <action>Present the rows below to the developer, hold execution until every row has an explicit verdict and PD-01 is ruled on, then record the verdicts, the measured numbers and the ruling in the plan SUMMARY. Do not tick any requirement or advance the phase on a partial pass — a FAIL row becomes a gap for /gsd-plan-phase 29 --gaps.</action>
  <what-built>
A review queue at `http://127.0.0.1:4173/__curate/queue`, reachable from a "Review queue" link in the
dashboard nav whenever `npm run curate` is running. It lists one row per activity carrying at least
one demoted best effort (all three guards — `ceiling`, `world-record`, `max-speed`), nests that
activity's flagged efforts inside its row, keeps already-excluded activities listed and marked, and
carries the existing whole-activity exclude/edit/remove control inline on every row plus one page
Recompute control. Every write goes through the unchanged `PUT`/`DELETE /__curate/exclusions/:id`
route — its origin gate, its activity-id validator and its atomic write. Both publish guards were
extended to the two new routes and each was demonstrated red under a planted leak.
  </what-built>
  <how-to-verify>
**Setup (do this exactly — stale artifacts are a known trap in this repo):**
- Use `http://127.0.0.1:4173` literally. `localhost:4173` is 403 by design on every route.
- After ANY rebuild, hard-reload the page (Cmd-Shift-R). A normal reload can serve a cached bundle.
- Before starting, confirm the SERVED bytes are the ones just built:
  `curl -s http://127.0.0.1:4173/__curate/queue.js | shasum -a 256` must equal the
  `.curate-dist/queue.js` digest recorded in Task 1's Evidence block. If it does not, stop: you are
  looking at a stale artifact and every row below would be measuring the wrong thing.
- Keep Task 1's Evidence block open. **N** = flagged activity count, **M** = already-excluded share,
  **T** = total exclusions, **E** = demoted effort count, **C** = ceiling-only activity count.

---

**R1 — Reachable in one navigation action (CUR-01, D-07).**
From `http://127.0.0.1:4173/strava-widgets/`, click "Review queue" in the nav.
PASS: one click lands on the queue page.
Fails if: the link is missing (injector broke) or the page does not load.

**R2 — The header count equals the independently-derived count (CUR-01 Criterion 1, D-16, D-01).**
Read the leading number in the queue header. Compare it to **N** from
`node scripts/compute-pr-ceiling-recount.mjs` — a script that derives its count with its own
arithmetic and imports none of the modules that produced the data.
PASS: the header's first number equals **N** exactly.
Fails if it reads **E** (effort-level, not activity-level), **C** (ceiling-only, violating D-01) or
**N − M** (excluded rows hidden, violating D-02). All four values are printed in Task 1's Evidence
block and were confirmed distinct — this row is failable in three separate directions and passable
only by the correct implementation.

**R3 — The page actually renders that many rows (reachable extent, not a claim).**
In DevTools console: `document.querySelectorAll('[data-queue-row]').length`.
PASS: the result equals **N** — the same independently-derived number, not merely the header's claim.
Then scroll to the very bottom and confirm the last row renders completely (date, name link, at least
one effort line, its control).
Fails if: the count is short of **N** (truncated/paginated list) or the final row is a stub.

**R4 — The already-excluded share is listed and marked (D-02, D-15).**
In DevTools console:
`document.querySelectorAll('[data-queue-row][data-excluded="true"]').length`.
PASS: equals **M**, and every such row shows `Excluded — <reason>` with a non-empty reason string.
Fails if: it reads 0 (excluded rows hidden — CUR-04's deferred drainability arriving by the back
door) or the reasons are blank.

**R5 — Ordering (D-05).**
In DevTools console:
`[...document.querySelectorAll('[data-queue-row]')].map(r => r.dataset.excluded + ' ' + r.querySelector('a').textContent.slice(0,12))`
PASS: every `false` row precedes every `true` row, and dates descend within each block.
Fails if: excluded rows interleave with pending ones, or dates are unordered.

**R6 — Row content and prefill (D-12, D-11).**
Pick any pending row. Confirm: a date; an activity name (not just an id); the name links to
`/strava-widgets/#/activity/<id>` and opens that activity's detail page; one line per flagged effort
reading `distance · guard · reason · duration · pace` with the demotion reason quoted verbatim — a
named condition with its measured value (e.g. `implied 6.96 m/s exceeds personal ceiling 5.11 m/s`),
never an adjective like "implausible" or "suspicious".
Tick the row's checkbox. PASS: a textarea appears already filled with that activity's demotion
reason(s), each prefixed with its distance, and it is editable.
Fails if: the name is missing, the link 404s, the prefill is empty, or an effort line invents wording.

**R7 — The exclude action writes through the existing path (CUR-02 Criterion 2).**
On that same row, edit the prefilled reason (append ` — checkpoint test`), press Save.
PASS: the page reloads; that row now reads `Excluded — <your text>`;
`git diff data/best-effort-exclusions.json` shows exactly ONE added entry, for that activity id, with
`"distances": null`.
Then press "Remove exclusion" on that row. PASS: a confirm dialog appears naming the consequence;
accepting reloads the page, the row returns to pending, and
`git diff data/best-effort-exclusions.json` is clean again.
Fails if: no confirm dialog; the file does not change; the entry lacks `"distances": null`; or the
entry count moves by anything other than +1 then back to 0.

**R8 — An untrusted origin is rejected (CUR-02 Criterion 2).** In a terminal:
```
curl -s -o /dev/null -w '%{http_code}\n' -X PUT -H 'Origin: http://evil.example' \
  -H 'Content-Type: application/json' -d '{"reason":"checkpoint probe"}' \
  http://127.0.0.1:4173/__curate/exclusions/<any flagged id>
curl -s -o /dev/null -w '%{http_code}\n' -H 'Origin: http://evil.example' http://127.0.0.1:4173/__curate/queue
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:4173/__curate/queue
```
PASS: `403`, `403`, `200` — and `git diff data/best-effort-exclusions.json` is still clean.
The third call is the control: it proves this row is not unsatisfiable. The first two prove it is not
vacuous.

**R9 — Recompute control (D-13, D-14).**
PASS (required): the control sits at the top of the queue, and its note says plainly that the ceiling
is derived from the already-filtered population, so excluding and recomputing can change which
activities are listed.
Optional (only if you want the full loop — it rewrites `data/stats/**` and takes minutes): press it,
watch the streamed output, let it reload, then re-run `node scripts/compute-pr-ceiling-recount.mjs`
and re-check R2 against the NEW **N**. Say in your answer whether you ran this.

**R10 — Both guards green on the build that was served (CUR-03 Criterion 3).**
Task 1 already recorded `npm test`, `npm run build-widgets`, `npm run verify-dashboard` and
`findCurationArtifacts('dist/widgets') === []` all clean. Confirm the recorded digest for
`dist/widgets/index.html` still matches `shasum -a 256 dist/widgets/index.html` now — i.e. nothing
rebuilt underneath you mid-session.
PASS: digests match and all four results are green. The red halves were demonstrated by plans 29-01
and 29-02's planted-fixture tests, which CI reruns.

---

**R11 — A decision for you (PD-01).**
`data/stats/best-efforts.json` carries no activity `name`, but D-12 requires one on every row. The
implementation therefore makes a THIRD fetch — `/strava-widgets/data/dashboard/index.json`, already
public, already mirrored, already read by the dashboard — and uses it for the name only. D-08 named
two files and said "no new server read route"; this adds no route, and the flagged SET is derived
without it, so a failed third fetch cannot change which rows appear. But D-12's own rejected
alternative treated `index.json` as a real cost once already.
Answer one of:
- **approve** — keep the name join as built;
- **reject** — drop the third fetch; rows will read `Activity <id>` instead of a name (a bounded
  follow-up change, nothing else moves).
  </how-to-verify>
  <resume-signal>
Reply with a verdict per row (R1–R10: PASS or FAIL with what you saw), your PD-01 ruling
(approve / reject), and whether you ran R9's optional recompute. Any FAIL is recorded as a gap for
`/gsd-plan-phase 29 --gaps`; do not tick the phase complete on a partial pass.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a page on another origin → the curate server | Exercised directly by R8's cross-origin probes |
| the developer's gesture → `data/best-effort-exclusions.json` | R7 confirms the write lands through the validated, atomic path and is reversible |
| the built bundle on disk → the bytes the browser executes | Digest comparison, because a stale `dist`/cached bundle would make every other row measure the wrong artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Spoofing | Cross-origin / rebound-Host access to the curate routes | mitigate | R8 probes both a write route and the queue page from an untrusted Origin and requires 403, with a 200 control |
| T-29-03 | Elevation of Privilege | A parallel write surface silently substituted for the existing one | mitigate | R7 asserts the on-disk JSON contract (`"distances": null`, exactly one entry, reversible) that only the existing handler produces |
| T-29-19 | Repudiation | A checkpoint row that cannot fail (agreeing with itself) | mitigate | R2/R3/R4 compare against numbers from an independent script, and Task 1 records the three wrong values each row must reject |
| T-29-20 | Tampering | A stale `dist`/`.curate-dist` artifact making the session certify code nobody ran | mitigate | Served-bytes digest comparison before R1 and again at R10; hard-reload instruction after every rebuild |
| T-29-04 | Information Disclosure | A curate artifact reaching the published bundle | mitigate | R10 re-confirms `findCurationArtifacts` empty and `npm run verify-dashboard` green on the served build |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages (29-RESEARCH.md § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Task 1's Evidence block exists with all four discriminating numbers and both digests
- Every checkpoint row has an explicit PASS/FAIL verdict from the developer
- The PD-01 ruling is recorded
- `git status --porcelain data/best-effort-exclusions.json` is empty at the end of the session
</verification>

<success_criteria>
The developer confirms, in a real browser against the real archive: the queue is one click away, its
rendered extent equals the independently-derived flagged count, the already-excluded share is listed
and marked, the exclude action writes and reverses through the existing path, an untrusted origin is
rejected, and both publish guards are green on exactly the build that was served.
</success_criteria>

<output>
Create `.planning/phases/29-curation-review-queue/29-08-SUMMARY.md` when done
</output>
</content>
</invoke>
