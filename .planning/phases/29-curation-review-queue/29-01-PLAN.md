---
phase: 29-curation-review-queue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/curation-guard.mjs
  - scripts/lib/curation-guard.test.mjs
autonomous: true
requirements: [CUR-03]
must_haves:
  decisions:
    - "D-18: the existing __curate content scan already catches the queue page and bundle — no new marker; prove it with planted fixtures instead of asserting it"
    - "D-19 (build-time half): planted queue-page and queue-bundle fixtures in scripts/lib/curation-guard.test.mjs prove the scan red; a clean tree proves it green"
    - "IN-17: one path must produce exactly one violation in findCurationArtifacts (folded todo, must land before the D-19 fixtures)"
  truths:
    - "A file named .curate-dist whose contents also carry the __curate marker produces exactly one violation, not two (IN-17)"
    - "A queue-page-shaped file planted in a dist/widgets-shaped fixture tree is reported by findCurationArtifacts"
    - "A queue-bundle-shaped file planted in a dist/widgets-shaped fixture tree is reported by findCurationArtifacts"
    - "The same fixture tree without those two files returns exactly []"
  artifacts:
    - path: "scripts/lib/curation-guard.mjs"
      provides: "findCurationArtifacts with the one-path-one-violation invariant"
      contains: "findCurationArtifacts"
    - path: "scripts/lib/curation-guard.test.mjs"
      provides: "IN-17 pin + D-19 planted queue fixtures"
      contains: "queue"
  key_links:
    - from: "scripts/lib/curation-guard.test.mjs"
      to: "scripts/lib/curation-guard.mjs"
      via: "named import of findCurationArtifacts"
      pattern: "findCurationArtifacts"
---

<objective>
Close the build-time half of CUR-03: prove `findCurationArtifacts` reports a leaked review-queue
page and a leaked review-queue bundle, and returns `[]` on the same tree without them — and fix the
pre-existing IN-17 double-violation defect first, so the new fixtures count real leaks rather than
the bug's extra output.

Purpose: Criterion 3 requires the build-time scan demonstrated failing *and* passing on the new
routes' artifacts. D-18 says no new marker is needed; the deliverable is the proof, not a mechanism.
Output: A one-path-one-violation `findCurationArtifacts`, plus four new planted-fixture cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-curation-review-queue/29-CONTEXT.md
@.planning/phases/29-curation-review-queue/29-PATTERNS.md
@.planning/todos/pending/2026-09-02-in17-in18-curation-guard-cosmetics.md

Test entry files must match `vitest.config.ts`'s `include` glob: `['src/**/*.test.ts',
'scripts/**/*.test.mjs']`. `scripts/lib/curation-guard.test.mjs` already matches; do not rename it.
`fileParallelism: false` is deliberate — do not change it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin IN-17 with a test observed RED</name>
  <read_first>
    - scripts/lib/curation-guard.mjs (read the whole of findCurationArtifacts: the directory-name checks, the two file-name checks, the non-regular-file gate, UNSCANNED_EXTENSIONS and the content scan, in that order)
    - scripts/lib/curation-guard.test.mjs (the beforeEach mkdtemp harness, the writeFile helper, and the existing planted-fixture it() blocks to mirror)
    - .planning/todos/pending/2026-09-02-in17-in18-curation-guard-cosmetics.md
  </read_first>
  <action>
Add a `describe('IN-17 — one path produces exactly one violation', ...)` block to
`scripts/lib/curation-guard.test.mjs`, using the file's existing `tmpDir` + `writeFile` harness (never
the real `dist/widgets`).

Three cases:
(a) a FILE literally named `.curate-dist` whose contents contain the `CURATE_MARKER` literal —
assert `violations.filter((v) => v.path.endsWith('.curate-dist')).length` is `1`. Today this is `2`
(the file-name check pushes one, then `scanExtension` returns the whole name `.curate-dist`, which is
absent from `UNSCANNED_EXTENSIONS`, so the content scan pushes a second for the same path).
(b) a FILE literally named `CURATE_DIR_NAME` (`__curate`) whose contents contain the marker — assert
exactly `1` violation for that path, same reason.
(c) a tree-wide invariant over a fixture containing (a), (b), a clean `index.html`, an `assets/x.js`
carrying the marker, and a `__curate/overlay.js` nested file: assert
`new Set(violations.map((v) => v.path)).size === violations.length` — no path is reported twice.

Import `CURATE_DIR_NAME` and `CURATE_MARKER` from `./curation-guard.mjs` (already imported at the top
of the test file) rather than re-typing the literals.

Run `npx vitest run scripts/lib/curation-guard.test.mjs` and record the RED output verbatim in the
plan SUMMARY (expected shape: `expected 2 to be 1`). A guard in this repo is not trusted until it has
been observed failing — do NOT fix the source in this task.
  </action>
  <verify>
    <automated>npx vitest run scripts/lib/curation-guard.test.mjs 2>&1 | grep -E "expected 2 to be 1|IN-17"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/curation-guard.test.mjs` contains the string `IN-17`
    - `npx vitest run scripts/lib/curation-guard.test.mjs` exits non-zero
    - The failure output names at least one of the three new cases and shows a received count of 2 where 1 was expected
    - No file other than `scripts/lib/curation-guard.test.mjs` is modified in this task
  </acceptance_criteria>
  <done>The IN-17 pin exists and has been observed red against the unfixed scanner.</done>
</task>

<task type="auto">
  <name>Task 2: Fix IN-17 — one path, one violation</name>
  <read_first>
    - scripts/lib/curation-guard.mjs (the file-entry branch: the `entry.name === CURATE_DIR_NAME` check, the `entry.name === '.curate-dist'` check, the `!entry.isFile()` gate, and the content scan below them)
  </read_first>
  <action>
In `findCurationArtifacts`'s file-entry branch, make the two name-match checks mutually exclusive and
terminal: after pushing a name-match violation for `CURATE_DIR_NAME` or for `.curate-dist`, `continue`
to the next entry instead of falling through to the `!entry.isFile()` gate and the content scan.
Express this as `if (entry.name === CURATE_DIR_NAME) { push; continue; }` followed by
`if (entry.name === '.curate-dist') { push; continue; }` — do not merge them into one violation with a
combined reason, and do not change either reason string (the existing reason strings are asserted
elsewhere and read by developers at build-failure time).

Preserve every other property of the scanner exactly:
- The directory branch keeps pushing its name-match violation AND keeps descending via `walk()`
  (a nested violation inside a `__curate` directory is still reported at its own distinct path).
- The `!entry.isFile()` gate must still run BEFORE the `UNSCANNED_EXTENSIONS` skip for every entry
  that did not already match a name check (WR-14's case (d) ordering pin).
- `UNSCANNED_EXTENSIONS` gains no new entry; `CURATE_MARKER` is unchanged; nothing becomes exempt.

Update the block comment above the `.curate-dist` file check to record the invariant in one sentence:
a name match is already a complete violation for that path, so the content scan adds no information
and the path must not be reported twice (IN-17).

Re-run the Task 1 pin and confirm GREEN, then run the whole file to confirm none of the pre-existing
cases (clean tree, non-existent directory, planted `__curate` directory, marker-in-`.js`,
marker-in-`index.html`, planted `.curate-dist` directory, marker-in-`.d.ts`, the WR-14/WR-19
unreadable-entry cases) regressed.
  </action>
  <verify>
    <automated>npx vitest run scripts/lib/curation-guard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/lib/curation-guard.test.mjs` exits 0 with every pre-existing case still passing
    - `grep -n "continue" scripts/lib/curation-guard.mjs` shows a `continue` inside both file-name-match branches
    - `grep -c "UNSCANNED_EXTENSIONS" scripts/lib/curation-guard.mjs` is unchanged from before the edit, and the array's members are unchanged
    - `npm run build-widgets` exits 0 (the real tree still certifies clean through the shipped call site)
  </acceptance_criteria>
  <done>One leaked path yields exactly one violation; every prior fixture still discriminates.</done>
</task>

<task type="auto">
  <name>Task 3: D-19 planted queue-page and queue-bundle fixtures</name>
  <read_first>
    - scripts/lib/curation-guard.test.mjs (post-Task-1 state, including the new IN-17 block and the existing clean-tree case)
    - scripts/lib/curation-guard.mjs (post-Task-2 state)
    - .planning/phases/29-curation-review-queue/29-CONTEXT.md (D-18, D-19)
  </read_first>
  <action>
Add `describe('D-19 — the review queue leaks are caught by the existing scan (no new marker)', ...)`
to `scripts/lib/curation-guard.test.mjs`, built on the same `tmpDir`/`writeFile` harness.

Shape every fixture like real build output, so the test proves the scan catches the artifacts this
phase actually produces:
(a) planted queue PAGE: write `queue.html` whose body contains
`<script src="/__curate/queue.js"></script>` and a `<link rel="stylesheet" href="./assets/index-abc.css">`.
Assert `findCurationArtifacts` returns a violation whose `path` ends with `queue.html`, and that
exactly one violation exists for that path.
(b) planted queue BUNDLE: write `assets/queue.js` containing an esbuild-IIFE-shaped body with the
literal `"/__curate/exclusions/"` inside it (this is what the real bundle carries, because it imports
the overlay's transport). Assert a violation whose `path` ends with `queue.js`, exactly one for that
path.
(c) planted bundle under a `__curate` DIRECTORY: write `__curate/queue.js` with the same content.
Assert the directory itself and the file are each reported once, at two distinct paths — pins that
IN-17's `continue` did not suppress a nested finding.
(d) clean control: the same tree as (a)+(b) minus those two files (`index.html`,
`assets/index-abc.js` with no marker, `assets/index-abc.css`, `data/best-effort-exclusions.json`)
returns exactly `[]`. Assert `toEqual([])`, not `length === 0`, so an unexpected violation prints.

Do not add any queue-specific marker string, exemption, or new export to `curation-guard.mjs` —
D-18's whole point is that the existing mechanism already covers this and the deliverable is the
proof.
  </action>
  <verify>
    <automated>npx vitest run scripts/lib/curation-guard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/lib/curation-guard.test.mjs` exits 0 and reports at least 4 more tests than before this task
    - `grep -c "queue.html" scripts/lib/curation-guard.test.mjs` is at least 1 and `grep -c "queue.js" scripts/lib/curation-guard.test.mjs` is at least 2
    - `git diff --stat scripts/lib/curation-guard.mjs` shows no change in this task
    - Deleting the `content.includes(CURATE_MARKER)` branch in a scratch copy of `curation-guard.mjs` makes cases (a) and (b) fail — record this discrimination check in the SUMMARY (revert the scratch edit immediately; never commit it)
  </acceptance_criteria>
  <done>Both queue-shaped leaks are demonstrated red, and the clean tree demonstrated green, by collected tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → `dist/widgets` publish bundle | Anything reaching `dist/widgets` is world-readable on GitHub Pages; the curation write path must be structurally absent |
| build pipeline → CI publish step | A guard that passes for the wrong reason lets a leak ship silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Information Disclosure | `dist/widgets` containing the queue page or bundle | mitigate | `findCurationArtifacts`'s existing `__curate` content scan, proven against planted queue-shaped fixtures (Task 3) |
| T-29-06 | Repudiation | `curation-guard.test.mjs` passing vacuously | mitigate | Every new case is demonstrated red before green (Task 1's IN-17 pin; Task 3's scratch-deletion discrimination check) |
| T-29-07 | Tampering | A widened exemption (`UNSCANNED_EXTENSIONS`, a queue-specific marker) weakening the scan | mitigate | Acceptance criteria forbid any change to `UNSCANNED_EXTENSIONS` or `CURATE_MARKER`; D-18 requires zero new mechanism |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages (29-RESEARCH.md § Package Legitimacy Audit: not applicable); no install task exists in any plan of this phase |
</threat_model>

<verification>
- `npx vitest run scripts/lib/curation-guard.test.mjs` exits 0
- `npm run build-widgets` exits 0 (shipped call site still certifies the real tree clean)
- SUMMARY records the RED output from Task 1 and the discrimination check from Task 3
</verification>

<success_criteria>
One leaked path produces exactly one violation; a planted queue page and a planted queue bundle are
each reported; the same tree without them returns `[]`; all evidence is in collected tests that CI
reruns.
</success_criteria>

<output>
Create `.planning/phases/29-curation-review-queue/29-01-SUMMARY.md` when done
</output>
</content>
</invoke>
