---
phase: 29-curation-review-queue
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-dashboard-publish.mjs
  - scripts/verify-dashboard-publish-guard.test.mjs
  - src/dashboard/curation-seam.test.ts
autonomous: true
requirements: [CUR-03]
must_haves:
  decisions:
    - "D-17: verify-dashboard-publish.mjs gets one explicit literal 404 assertion per new path (/__curate/queue and /__curate/queue.js), never a prefix match"
    - "D-19 (HTTP half): planted-fixture cases in scripts/verify-dashboard-publish-guard.test.mjs prove the new assertions red when the paths are served and green when absent"
    - "IN-18: convert curation-seam.test.ts's WR-17 literal-string pin to the regex-shape pin already used two blocks below it"
  truths:
    - "A file served at /__curate/queue in the published tree makes the shipped verifier exit non-zero naming that path"
    - "A file served at /__curate/queue.js in the published tree makes the shipped verifier exit non-zero naming that path"
    - "A clean build prints five ✓ /__curate/... -> 404 lines in one run"
    - "A reflowed multi-line buildPrBadgeLabels call no longer fails curation-seam.test.ts for formatting alone"
  artifacts:
    - path: "scripts/verify-dashboard-publish.mjs"
      provides: "two new literal expect404 assertions"
      contains: "/__curate/queue"
    - path: "scripts/verify-dashboard-publish-guard.test.mjs"
      provides: "Case E and Case F planted-fixture proofs"
      contains: "queue"
    - path: "src/dashboard/curation-seam.test.ts"
      provides: "regex-shape WR-17 pin"
  key_links:
    - from: "scripts/verify-dashboard-publish-guard.test.mjs"
      to: "scripts/verify-dashboard-publish.mjs"
      via: "execFileSync subprocess run of the real shipped script"
      pattern: "execFileSync"
---

<objective>
Close the HTTP half of CUR-03: the shipped publish verifier asserts an explicit 404 for each new
review-queue route, and both assertions are demonstrated failing when the route content is planted
into `dist/widgets` and passing when it is absent. Also land folded todo IN-18.

Purpose: Criterion 3 requires both guards discriminating in both directions on the new routes.
Output: Two new literal `expect404` lines, two new planted-fixture cases, an extended clean-run case,
and a format-robust WR-17 pin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-curation-review-queue/29-CONTEXT.md
@.planning/phases/29-curation-review-queue/29-PATTERNS.md
@.planning/todos/pending/2026-09-02-in17-in18-curation-guard-cosmetics.md

`scripts/verify-dashboard-publish-guard.test.mjs` plants fixtures inside the REAL `dist/widgets`
tree and is `describe.skipIf(!existsSync(dist/widgets/index.html))`. If it skips, run
`npm run build && npm run build-widgets` first — a skipped guard proves nothing and must not be
reported as green.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Case E/F and extend Case A — observed RED</name>
  <read_first>
    - scripts/verify-dashboard-publish-guard.test.mjs (the whole file: the header comment, `runVerifier`, `cleanupCurateDir`, the `CURATE_DIR` constant, Cases A–D and the post-suite case)
    - scripts/verify-dashboard-publish.mjs (lines around the three existing literal `expect404` calls and the comment forbidding a prefix match, plus the `expect404` helper)
  </read_first>
  <action>
Extend `scripts/verify-dashboard-publish-guard.test.mjs`, mirroring Case B's exact structure
(`mkdirSync(CURATE_DIR, { recursive: true })`, `writeFileSync`, `runVerifier()` inside
`try`/`finally` with `cleanupCurateDir()`, assert `result.status).not.toBe(0)` and
`result.output).toContain(...)`):

- **Case E (planted queue page):** write `resolve(CURATE_DIR, 'queue')` containing
  `'<!doctype html><script src="/__curate/queue.js"></script>'`. Assert the output contains
  `GET /__curate/queue expected 404`.
- **Case F (planted queue bundle):** write `resolve(CURATE_DIR, 'queue.js')` containing
  `'console.log("__curate queue leaked");'`. Assert the output contains
  `GET /__curate/queue.js expected 404`.
- **Case A extension:** add `expect(output).toContain('✓ GET /__curate/queue -> 404')` and
  `expect(output).toContain('✓ GET /__curate/queue.js -> 404')` alongside the three existing ✓ lines,
  in the same clean run, keeping the existing non-regression assertion about
  `/data/best-effort-exclusions.json` untouched.

Beware Case E's path shape: `CURATE_DIR` is `dist/widgets/__curate`, so a file named `queue` there is
served at `/__curate/queue` — this must be a FILE named `queue`, not a directory.

Run `npx vitest run scripts/verify-dashboard-publish-guard.test.mjs` and record the RED output in the
SUMMARY. Expected today: Cases E and F fail because the verifier exits 0 (it does not assert those
paths yet), and the Case A extension fails because those ✓ lines are not printed. Do NOT touch
`verify-dashboard-publish.mjs` in this task.

If the suite reports as skipped, run `npm run build && npm run build-widgets` and re-run; a skip is
not evidence.
  </action>
  <verify>
    <automated>npx vitest run scripts/verify-dashboard-publish-guard.test.mjs 2>&1 | grep -E "Case E|Case F|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/verify-dashboard-publish-guard.test.mjs` exits non-zero and the output names Case E and Case F
    - The run reports the describe block as executed, not skipped (`dist/widgets/index.html` exists)
    - `git diff --stat scripts/verify-dashboard-publish.mjs` shows no change in this task
    - After the run, `test ! -e dist/widgets/__curate` (no planted fixture survives)
  </acceptance_criteria>
  <done>Both new HTTP-layer cases exist and have been observed red against the unextended verifier.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two literal expect404 assertions (D-17)</name>
  <read_first>
    - scripts/verify-dashboard-publish.mjs (the `expect404` helper and the block containing the three existing `/__curate/...` assertions together with the comment that forbids widening them)
  </read_first>
  <action>
Immediately after the existing `await expect404(baseUrl, '/__curate/exclusions/3475726256', ...)`
line, add exactly two lines in the same literal style:

- `await expect404(baseUrl, '/__curate/queue', 'the curate review queue page must never be published');`
- `await expect404(baseUrl, '/__curate/queue.js', 'the curate review queue bundle must never be published');`

Do not collapse the now-five assertions into a loop over an array, do not introduce a prefix or
wildcard match against `/__curate`, and do not modify the surrounding comment's warning — the file's
own docblock argues explicitly against widening, and an explicit list is what makes an unasserted
route visibly missing (D-17).

Extend the existing comment block with one sentence naming Phase 29 and the two new routes, so a
future reader can see why the list grew from three to five.

Re-run the guard test: Cases E and F must now be GREEN, Case A must print five ✓ lines, and Cases
B/C/D must be unaffected.
  </action>
  <verify>
    <automated>npx vitest run scripts/verify-dashboard-publish-guard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/verify-dashboard-publish-guard.test.mjs` exits 0 with Cases A–F all passing
    - `grep -c "expect404(baseUrl, '/__curate" scripts/verify-dashboard-publish.mjs` returns 5
    - `grep -c "startsWith('/__curate'" scripts/verify-dashboard-publish.mjs` returns 0 (no prefix match introduced)
    - `npm run verify-dashboard` exits 0 and its output contains `✓ GET /__curate/queue -> 404`
  </acceptance_criteria>
  <done>The HTTP guard asserts both new routes explicitly and discriminates in both directions.</done>
</task>

<task type="auto">
  <name>Task 3: IN-18 — convert the WR-17 literal pin to a regex-shape pin</name>
  <read_first>
    - src/dashboard/curation-seam.test.ts (the two `toContain` literal-argument pins and, two blocks below, the existing regex-match-and-compare pin whose shape is the target)
  </read_first>
  <action>
Replace the two brittle literal assertions with the regex shape already used later in the same file:

- `expect(detailStripped).toContain('buildPrBadgeLabels(bestEffortsEntry, liveExclusions)')`
  becomes a match against
  `/buildPrBadgeLabels\(\s*bestEffortsEntry\s*,\s*liveExclusions\s*\)/` (whitespace- and
  newline-tolerant, argument identifiers still pinned by name and order).
- `expect(detailStripped).toContain('buildBestEffortsPanelRows(bestEffortsEntry, ageGrading, liveExclusions)')`
  becomes a match against
  `/buildBestEffortsPanelRows\(\s*bestEffortsEntry\s*,\s*ageGrading\s*,\s*liveExclusions\s*\)/`.

Keep the surrounding `countOccurrences(detailStripped, 'buildPrBadgeLabels(')).toBe(1)` assertion
and both `it(...)` titles' intent (update the titles to say "argument-shape" rather than "literal
form"). Keep the existing regex block two describes below unchanged — this task makes the two pins
consistent, it does not remove either.

Prove the new pins still discriminate, without touching `detail.ts`: in the same `it(...)`, run each
new regex against two inline sample strings — a reflowed multi-line call with the same identifiers
(must match) and a call with a wrong second identifier such as
`buildPrBadgeLabels(bestEffortsEntry, staleExclusions)` (must NOT match). This proves the pin is
failable without editing production source.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/curation-seam.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/curation-seam.test.ts` exits 0
    - `grep -c "toContain('buildPrBadgeLabels(bestEffortsEntry, liveExclusions)')" src/dashboard/curation-seam.test.ts` returns 0
    - The file contains both a positive reflowed-sample assertion and a negative wrong-identifier assertion for each converted pin
    - `git diff --stat src/dashboard/views/detail.ts` shows no change (production source untouched)
    - The file's existing `__curate appears zero times in detail.ts/detail-sections.ts` assertions still pass unmodified (D-06)
  </acceptance_criteria>
  <done>The WR-17 pins are format-robust and demonstrated discriminating in both directions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages HTTP surface → the public internet | Any path reachable over HTTP from the published mount is public |
| published view code (`src/dashboard/**`) → curate-only code | `curation-seam.test.ts` pins that no `__curate` string exists in published views |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Information Disclosure | `/__curate/queue`, `/__curate/queue.js` reachable on the published site | mitigate | Two explicit literal `expect404` assertions in `verify-dashboard-publish.mjs` (Task 2) |
| T-29-06 | Repudiation | The new assertions passing vacuously | mitigate | Cases E and F plant the real content and are observed red before Task 2 lands (Task 1) |
| T-29-07 | Tampering | A prefix/wildcard `/__curate` match passing for the wrong reason | mitigate | Acceptance criterion greps for zero prefix matches; the file's own comment forbids widening |
| T-29-08 | Tampering | Curate-shaped code drifting into published views | mitigate | `curation-seam.test.ts`'s zero-occurrence pins keep passing unmodified; IN-18 only changes pin format |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages (29-RESEARCH.md § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx vitest run scripts/verify-dashboard-publish-guard.test.mjs` exits 0, block executed not skipped
- `npx vitest run src/dashboard/curation-seam.test.ts` exits 0
- `npm run verify-dashboard` exits 0 with five `✓ GET /__curate/... -> 404` lines
- `test ! -e dist/widgets/__curate` after the suite
</verification>

<success_criteria>
The shipped verifier names both new routes explicitly; both assertions are proven red under a planted
leak and green on a clean build by collected tests; the WR-17 pin no longer fails for reflowing alone.
</success_criteria>

<output>
Create `.planning/phases/29-curation-review-queue/29-02-SUMMARY.md` when done
</output>
</content>
</invoke>
