---
phase: 17-activity-browser-detail-views
plan: 15
type: execute
wave: 4
depends_on: ["17-09", "17-10", "17-14"]
files_modified:
  - .planning/phases/17-activity-browser-detail-views/17-VALIDATION.md
autonomous: false
requirements: [BROWSE-01, BROWSE-02, BROWSE-03, BROWSE-04, BROWSE-05, BROWSE-06, DETAIL-01, DETAIL-02, DETAIL-03, DETAIL-04, DETAIL-05]

must_haves:
  truths:
    - "Every behavior this repo cannot automate is confirmed in a real browser before the phase gate closes"
    - "The built publish directory serves the list, calendar, and a detail view under the /strava-widgets project path with zero 404s"
    - "The lazily-imported Leaflet and Chart.js chunks and their stylesheet resolve under the project path, not just at the server root"
    - "Every manual row in 17-VALIDATION.md is recorded as confirmed or as a named gap"
  artifacts:
    - path: ".planning/phases/17-activity-browser-detail-views/17-VALIDATION.md"
      provides: "Completed per-task verification map and signed-off manual verification results"
      contains: "nyquist_compliant"
  key_links:
    - from: ".planning/phases/17-activity-browser-detail-views/17-VALIDATION.md"
      to: "the browser checkpoint result"
      via: "Manual-Only Verifications table status column"
      pattern: "Manual-Only Verifications"
---

<objective>
Close the phase with the verification this toolchain genuinely cannot automate: a real browser
walking every DOM interaction, and a project-path-mounted publish check of the built output
including the two new async chunks.

Purpose: this repo has a specific, expensive lesson. Phase 16's own exit gate reported 15/15 green
on a build that rendered a black page in production, because the verifier asserted a URL shape
production does not have. Separately, plan 16-09's human checkpoint caught two real defects
(`isValidActivityId` rejecting `i`-prefixed ids; an invisible theme toggle) that the automated gate
missed entirely. Phase 17 adds the first dynamically-imported browser chunks this codebase has ever
shipped, and Vite's async-CSS `<link>` injection under `base: './'` is 17-RESEARCH.md's Assumption
A1 — documented but never exercised here. None of that is provable without a browser.

Output: a completed `17-VALIDATION.md` and either a clean phase gate or a set of named gaps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-activity-browser-detail-views/17-VALIDATION.md
@.planning/phases/17-activity-browser-detail-views/17-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate plus a project-path publish smoke run</name>
  <files>(no source files — runs the automated gate and prepares the served build)</files>
  <read_first>
    - .planning/phases/17-activity-browser-detail-views/17-VALIDATION.md (the Sampling Rate section and the Manual-Only Verifications table, including its final "Production publish check" row)
    - scripts/verify-dashboard-publish.mjs (how it mounts under `/strava-widgets`, its root-absolute-asset-URL hard failure, and the `expect200` assertions plan 17-06 extended for `data/config/*.json`)
    - .planning/STATE.md (§ Blockers/Concerns — the full black-page postmortem, including why the verifier now mounts under a prefix and why a locally-passing check is not evidence)
    - .github/workflows/daily-refresh.yml (the blocking `npm test` + `npm run verify-dashboard` exit gate added in 16-13, both of which must stay green)
  </read_first>
  <action>
Run and record the complete automated gate, then prepare a locally-served build for the browser
checkpoint in Task 2.

1. `npm test` — the full suite. Record the total test count and the delta versus the 373 tests that
   passed before this phase, so the SUMMARY shows how much new automated coverage Phase 17 added.
2. `npx tsc --noEmit -p tsconfig.json` — clean typecheck.
3. `npm run build-widgets` followed by `npm run verify-dashboard` — both must exit 0. Record the
   verifier's check count and confirm the two new `GET /data/config/*.json -> 200` lines appear.
4. Chunk inventory: list `dist/widgets/dashboard/assets/` (resolve the real path during execution —
   plans 17-11 and 17-14 recorded it) and record, for the SUMMARY, which file is the entry chunk,
   which async chunks exist, their sizes, and whether a `.css` asset was emitted for the map chunk.
   Assert that the entry chunk matches neither `/leaflet/i` nor `/chart\.js|Chart\.register/i`, and
   that at least one async chunk matches each. This is the build-level half of the D-25 proof; the
   network-level half is Task 2.
5. Start a local static server that mounts the built output under the `/strava-widgets` path
   prefix — the SAME prefix `verify-dashboard-publish.mjs` uses — and leave it running for Task 2.
   Serving at the server root would reproduce exactly the mistake that let the Phase 16 black-page
   build report green, so the prefix is mandatory, not cosmetic. Record the exact command and the
   URL (e.g. `http://localhost:<port>/strava-widgets/`) in the checkpoint instructions.

If any of steps 1–4 fails, STOP and report the failure rather than proceeding to the human
checkpoint — a checkpoint run against a failing build wastes the developer's time and produces
misleading results.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json && npm run build-widgets && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0; the total test count and the delta versus 373 are recorded in the SUMMARY.
    - `npx tsc --noEmit` exits 0.
    - `npm run verify-dashboard` exits 0 and its output includes `GET /data/config/gear.json -> 200` and `GET /data/config/athlete.json -> 200`.
    - CLI assertion: the dashboard entry chunk matches neither `/leaflet/i` nor `/chart\.js|Chart\.register/i`, and at least one async chunk matches each — both results recorded with the resolved file names.
    - A static server is running with the built output mounted under `/strava-widgets`, and the exact URL is recorded for Task 2.
  </acceptance_criteria>
  <done>The full automated gate is green, the chunk split is proven at the build level, and a project-path-mounted server is ready for the browser checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-browser verification of every non-automatable behavior</name>
  <files>(no source files — developer-driven browser walkthrough)</files>
  <read_first>
    - .planning/phases/17-activity-browser-detail-views/17-VALIDATION.md (the Manual-Only Verifications table — every row below maps to one of its entries)
    - .planning/phases/17-activity-browser-detail-views/17-UI-SPEC.md (every Surface Contract statement is written as a checkable fact and doubles as this script)
  </read_first>
  <action>
Present the walkthrough below to the developer against the project-path-mounted URL recorded in
Task 1, then capture their verbatim response. Do not perform any code change in this task and do not
substitute your own inspection for the developer's — every item listed is here precisely because it
cannot be proven from source or from a Node-environment test in this repo.
  </action>
  <what-built>
Phase 17 shipped, across plans 17-01 through 17-14:
- A paginated, sortable, filterable, text-searchable activity browser at `#/list` with a desktop
  table above 720px and card rows below it, removable filter chips, a live result count, and full
  hash-query state.
- A real month-grid training log at `#/calendar` with prev/next/jump navigation, distance-tinted
  day cells, and a multi-run day picker.
- A full activity detail page at `#/activity/:id` with an eight-tile stats header including gear
  resolution, a route map, stacked pace/HR/cadence/elevation chart bands with per-band overlay
  shading, a seven-column per-km splits table, and a pace-distribution / HR-zone breakdown.
- Two new committed config files (`data/config/gear.json`, `data/config/athlete.json`) carried
  through the publish pipeline, and the first lazily-imported browser chunks in this codebase.
All automated checks are green and the built output is being served under `/strava-widgets`.
  </what-built>
  <how-to-verify>
Open the URL recorded in Task 1 (it MUST include the `/strava-widgets/` path prefix — a root-mounted
server would reproduce the exact blind spot that let Phase 16's black-page build report green).
Keep the browser devtools Network and Console panels open throughout. Work through every group;
report the group number and what you saw for anything that does not match.

**A. Activity list — sorting and pagination (BROWSE-01, BROWSE-02)**
1. At a window wider than 720px, confirm a real table renders with the seven columns
   `Date | Activity | Distance | Moving Time | Pace | Avg HR | Status`.
2. Click each of the five sortable headers twice. Confirm the order flips on the second click, the
   direction arrow appears only on the active column, and the header's `aria-sort` toggles between
   `ascending`, `descending`, and `none` (inspect the element).
3. Confirm first-click direction matches the spec: Date, Distance, Moving Time, and Avg HR start
   descending; Pace starts ascending.
4. Page through several pages. Confirm 50 rows per page, that "Page {n} of {total}" tracks, and that
   the current-page button is filled with the darker accent and readable white text in BOTH themes.
5. Confirm the Phase 16 "Showing the 100 most recent…" notice is completely gone.
6. Load `#/list?page=99999` directly. Confirm it shows the LAST page with that button highlighted —
   not a blank table.
7. Narrow the window below 720px. Confirm the table is replaced by card rows plus a sort dropdown,
   and that changing the dropdown re-sorts.

**B. Filters, search, and chips (BROWSE-03, BROWSE-04, BROWSE-06)**
8. Type in the search box. Confirm the result count updates about a fifth of a second after you stop
   typing, not on every keystroke.
9. Open the Filters panel. Apply a date range, a distance range, a pace range, and a duration range.
   Confirm each produces exactly one chip, that the counts narrow together (AND semantics), and that
   the Filters button reads "Filters (n active)".
10. Try each preset chip (This year, Last 12 months, 5K, 10K, HM+, Marathon+). Confirm each applies
    immediately with no debounce delay.
11. Remove each chip via its × button. Confirm only that constraint clears. With 2+ chips active,
    confirm "Clear all" appears and clears everything.
12. Copy the URL while filters are active, open it in a new tab, and confirm the identical filtered,
    sorted, paginated view loads. Then use browser Back and Forward and confirm the view follows.
13. Apply a combination with zero matches. Confirm the named empty state appears with its heading,
    body copy, and a working Clear all — NOT a blank table or an empty table header.

**C. Calendar (BROWSE-05)**
14. Navigate at least three months back and forward. Confirm week rows always have seven aligned
    cells, including a month whose 1st is a Sunday, one whose 1st is a Saturday, and February of a
    leap year.
15. Confirm each active day shows its total kilometres as readable text WITHOUT hovering, that rest
    days show a plain dash, and that higher-distance days are visibly more tinted.
16. Confirm the month-total figure and "across {n} runs" caption match the visible cells.
17. Click a single-run day: it should navigate straight to that activity. Click a multi-run day: the
    per-run picker should open showing "{count} runs on {date}" with card rows linking to each.
18. Use the "Jump to month" control and confirm the URL becomes `#/calendar?month=YYYY-MM` and is
    reloadable.
19. Apply filters on the list, then open the calendar. Confirm the calendar is completely unaffected
    by them.

**D. Detail page — the first lazy chunk load (DETAIL-01..05, and the D-25 / Assumption A1 proof)**
20. With the Network panel CLEARED, navigate from the list to any activity with a route. Confirm two
    or more new JS chunks and at least one new CSS file are fetched at that moment, that ALL of them
    return 200 (no 404s, no requests to a root-absolute `/assets/...` path), and that nothing
    Leaflet- or Chart.js-shaped was fetched on the overview, list, or calendar beforehand.
21. Confirm the map renders WITH proper Leaflet styling — visible zoom controls, a layer switcher,
    and correct tile borders. Unstyled or missing controls means Vite's async-CSS injection did not
    work under `base: './'`, which is the one MEDIUM-confidence assumption in this phase.
22. Confirm the stats header shows eight tiles (Distance, Moving Time, Pace, Elevation Gain, Avg HR,
    Max HR, Cadence, Gear) with no gaps in the grid.
23. Open an activity that HAS a `gear_id` and one of the ~708 that do not. Confirm the first shows a
    gear name (or its device name if `gear.json` values are still blank), the second either shows a
    device name or omits the Gear tile entirely with the remaining tiles reflowing — and that a raw
    id like `g16649854` never appears anywhere.

**E. Detail page — charts, splits, breakdowns (DETAIL-03, DETAIL-04, DETAIL-05, BROWSE-06)**
24. Confirm the chart bands render in the order pace, HR, cadence, elevation, that x-axis tick
    labels appear only on the bottom-most band, and that they are vertically aligned.
25. Hover across a band. Confirm a shared vertical crosshair appears in EVERY band at the same x
    position and a marker moves along the route on the map. Confirm the caveat line about the
    position being estimated from distance rather than GPS-matched is visible near the map.
26. On one band, check two "Shade behind" boxes. Confirm both render as shaded areas behind the
    full-contrast primary line, the second fainter than the first, with no extra axis labels. Try a
    third: it should be blocked with the "Up to 2" hint.
27. Set a different overlay pairing on a different band at the same time. Confirm they are
    independent.
28. Reload the page, then open a DIFFERENT activity. Confirm your overlay choices persisted.
29. Toggle the x-axis between Distance and Time. Confirm every band redraws and the active option is
    readable white text on the darker accent fill in both themes.
30. Confirm the splits table shows seven columns, that the final row is labelled like
    "7 (0.4 km, partial)" in italics, and that the per-split distances plainly sum to the activity's
    total. At a narrow width, confirm the table scrolls horizontally INSIDE its own box with the Km
    column staying pinned — and that the PAGE itself never scrolls sideways.
31. Confirm the pace-distribution histogram renders.
32. Open an activity with NO heart-rate data. Confirm: the HR chart band is absent (not empty), the
    Avg HR / Max HR tiles show em dashes, the splits table's Avg HR column shows em dashes, and
    there is NO empty panel shell where the HR-zone breakdown would be. Do the same for an activity
    with no cadence.
33. Open one of the 27 activities whose route cannot be rendered. Confirm the "Route unavailable"
    message appears (distinct from "No route recorded") and that the rest of the page still works.

**F. Cross-cutting**
34. Navigate rapidly back and forth between two different activities several times. Confirm you
    never end up with a stale map, a stale chart, duplicated bands, or a mismatched stats header.
35. Toggle light/dark theme on the detail page. Confirm the route polyline, the chart lines, and the
    calendar tints all recolour, and that no text becomes unreadable.
36. Open a detail view, then press browser Back. Confirm you land on the list at the same page and
    sort you left, and that the row you just viewed is briefly highlighted and scrolled into view.
37. Tab through the list headers, chips, pagination, calendar cells, and overlay checkboxes.
    Confirm every one is reachable and shows a focus ring.
38. Confirm the Console panel shows no uncaught errors across the entire walkthrough.
  </how-to-verify>
  <verify>
    <automated>MISSING — not automatable: no jsdom, no headless browser dependency in this repo; proof is the developer's recorded walkthrough result, captured verbatim in 17-VALIDATION.md by Task 3</automated>
    <human-check>All 38 steps across groups A–F walked in a real browser against a /strava-widgets-mounted build, with the Network and Console panels open, and every group's outcome recorded</human-check>
  </verify>
  <done>The developer has responded with either "approved" or a per-group description of what they observed, and that response is captured verbatim for Task 3.</done>
  <resume-signal>Type "approved" if every group passed, or describe exactly what you saw for each group that did not — verbatim, including any console errors and any 404 URLs. A partial result is a valid and useful outcome; do not approve a group you did not actually check.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record results in 17-VALIDATION.md and close or gap the phase</name>
  <files>.planning/phases/17-activity-browser-detail-views/17-VALIDATION.md</files>
  <read_first>
    - .planning/phases/17-activity-browser-detail-views/17-VALIDATION.md (the frontmatter flags `nyquist_compliant` and `wave_0_complete`, the empty Per-Task Verification Map, the Manual-Only Verifications table, and the Validation Sign-Off checklist)
    - The checkpoint result from Task 2 (verbatim)
    - .planning/STATE.md (§ Blockers/Concerns — the precedent for how plan 16-09's PARTIAL result was recorded: gaps logged verbatim as gap-closure work rather than patched under checkpoint pressure)
  </read_first>
  <action>
Record the outcome honestly in `.planning/phases/17-activity-browser-detail-views/17-VALIDATION.md`.

1. Fill the Per-Task Verification Map with one row per task across plans 17-01 through 17-15,
   carrying each task's requirement id, its threat reference, its automated command, and its final
   status (green / red / flaky). Every row's "File Exists" column should now read yes for the six
   Wave 0 test files.
2. Set `wave_0_complete: true` in the frontmatter once all six Wave 0 test files exist and pass.
3. Add a status column result to each row of the Manual-Only Verifications table, mapped from the
   checkpoint groups above.
4. Complete the Validation Sign-Off checklist. Set `nyquist_compliant: true` ONLY if every box is
   genuinely satisfied; if any is not, leave it false and say why in the file.
5. Record the automated evidence gathered in Task 1: the test count and its delta from 373, the
   verifier check count, and the chunk inventory with the entry-chunk assertions.

If the checkpoint came back with ANY failing group, do NOT patch it here. Follow the 16-09
precedent: record each failure verbatim as a named gap with its requirement id and severity, mark
`status: partial` in the frontmatter, and report that gap-closure planning is needed
(`/gsd-plan-phase 17 --gaps`) rather than fixing under checkpoint pressure. A PARTIAL result that
names real defects is a better outcome than a green mark that hides them — that is precisely the
lesson this project already paid for twice.

If every group passed, set `status: approved` in the frontmatter and state that the phase is ready
for `/gsd-verify-work`.
  </action>
  <verify>
    <automated>node --input-type=module -e "import{readFileSync}from'node:fs';const p='.planning/phases/17-activity-browser-detail-views/17-VALIDATION.md';const s=readFileSync(p,'utf8');if(/_pending planning_/.test(s))throw new Error('Per-Task Verification Map still has the placeholder row');if(!/wave_0_complete: true/.test(s))throw new Error('wave_0_complete not set');if(!/status: (approved|partial)/.test(s))throw new Error('status not resolved to approved or partial');console.log('validation record complete');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `validation record complete` and exits 0.
    - Source assertion: `grep -c '_pending planning_' .planning/phases/17-activity-browser-detail-views/17-VALIDATION.md` returns 0.
    - Source assertion: the Per-Task Verification Map contains at least one row per plan 17-01 through 17-14.
    - Source assertion: every row of the Manual-Only Verifications table carries an explicit result, with no blanks.
    - If any checkpoint group failed: `grep -c 'status: partial' .planning/phases/17-activity-browser-detail-views/17-VALIDATION.md` returns 1 and each gap is recorded with its requirement id and the developer's verbatim observation.
    - If all groups passed: `grep -c 'nyquist_compliant: true' .planning/phases/17-activity-browser-detail-views/17-VALIDATION.md` returns 1.
  </acceptance_criteria>
  <done>The validation record is complete and honest: every task mapped, every manual row resolved, and the phase either signed off or explicitly marked partial with named gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built publish directory → locally-served HTTP under `/strava-widgets` | The deliberate stand-in for the production GitHub Pages project path; serving at the root would invalidate the entire check |
| Dynamically imported chunk URLs → browser module loader | The first async chunks this codebase ships; their resolution under a project-path base is unproven until observed |
| Third-party tile servers → map imagery during the checkpoint | External hosts contacted while the developer walks the detail page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-PUB-03 | Spoofing | A locally-passing check that production does not share | mitigate | The checkpoint server MUST mount under `/strava-widgets`, matching what `verify-dashboard-publish.mjs` already enforces after the Phase 16 postmortem; root-mounted serving is explicitly called out as invalidating the result |
| T-17-PUB-04 | Repudiation | A green phase gate hiding a real defect | mitigate | Task 3 forbids patching a failed group in place and requires recording it verbatim as a named gap with `status: partial`, following the 16-09 precedent that produced the two real defects this project's automated gate had missed |
| T-17-MAP-04 | Denial of Service (feature) | Async-CSS `<link>` injection failing under `base: './'` (Assumption A1) | mitigate | Checkpoint group D step 21 specifically inspects Leaflet control styling and step 20 requires zero 404s and no root-absolute asset paths in the Network panel — the only way to test this claim in this repo |
| T-17-MAP-01 | Information Disclosure | Tile requests during the checkpoint | accept | Same third-party hosts every existing map widget already contacts; route data is already published publicly |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (17-RESEARCH.md § Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm test`, `npx tsc --noEmit`, `npm run build-widgets`, and `npm run verify-dashboard` all exit 0.
- The entry chunk is proven free of Leaflet and Chart.js while async chunks contain them.
- Every Manual-Only Verifications row in 17-VALIDATION.md carries a recorded result.
- 17-VALIDATION.md's frontmatter resolves to `approved` or `partial`, never left as `draft`.
</verification>

<success_criteria>
- The browser walkthrough was performed against a `/strava-widgets`-mounted build with devtools open.
- Every one of the 38 checkpoint steps has a recorded outcome.
- 17-VALIDATION.md's Per-Task Verification Map is fully populated and its sign-off checklist resolved.
- Any failure is recorded verbatim as a named gap with its requirement id, not patched in place.
</success_criteria>

<output>
Create `.planning/phases/17-activity-browser-detail-views/17-15-SUMMARY.md` when done
</output>
