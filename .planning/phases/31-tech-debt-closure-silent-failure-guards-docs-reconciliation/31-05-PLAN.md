---
phase: 31-tech-debt-closure-silent-failure-guards-docs-reconciliation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analytics/best-effort-ceiling.ts
  - src/analytics/best-effort-ceiling.test.ts
  - src/dashboard/views/detail-best-efforts-logic.test.ts
autonomous: true
requirements: [TD-04]

must_haves:
  decisions: [D-10]
  truths:
    - "D-10: the ceiling demotion reason always states the margin, so implied speed and ceiling can never read as equal in a sentence that says one exceeds the other"
    - "D-10: implied speed, ceiling, margin and p90 render at three decimals; the multiplier stays at two"
    - "The house-register regex test still pins the sentence shape, now including the margin clause"
    - "Existing assertions matching /exceeds personal ceiling/ keep passing — no consumer reformats the string"
  artifacts:
    - path: "src/analytics/best-effort-ceiling.ts"
      provides: "the margin-bearing reason template"
      contains: "by ${margin.toFixed(3)} m/s"
  key_links:
    - from: "ceilingDemotion's reason string"
      to: "data/stats/best-efforts.json, the Records note, the detail badge, and 28-DIFF.md"
      via: "every consumer reads the string verbatim; regenerating best-efforts.json rewrites every ceiling row's reason"
      pattern: "exceeds personal ceiling"
---

<objective>
Make the ceiling demotion reason state its margin (TD-04, D-10), closing the post-merge observation
recorded in `28-VALIDATION.md` § PR-04 Sign-off (Round 3): the shipped string reads "implied 4.63 m/s
exceeds personal ceiling 4.63 m/s", a sentence that contradicts itself because 2-dp rounding hides a
real 0.002 m/s margin.

Purpose: this is the one string a human reads when a record changes hands. It must never require the
reader to take "exceeds" on faith.

Output: a three-decimal, margin-bearing template and every assertion that pins its shape updated
together. This plan must land BEFORE the phase's regeneration pass, because
`compute-pr-ceiling-diff.mjs` embeds this string verbatim into `28-DIFF.md`, the artifact the PR-04
re-sign is bound to.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-CONTEXT.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-RESEARCH.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-PATTERNS.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md

<interfaces>
<!-- Established facts. Do not rediscover. -->

| fact | value | source |
|---|---|---|
| the site | `src/analytics/best-effort-ceiling.ts:188-203`, `ceilingDemotion`'s returned `reason` template | live read |
| target format | `implied {3dp} m/s exceeds personal ceiling {3dp} m/s by {3dp} m/s ({2dp} x p90 {3dp} m/s over {n} filtered {distance} efforts)` | D-10 |
| the live worked example | `3475730418@1mi`: `implied 4.630 m/s exceeds personal ceiling 4.628 m/s by 0.002 m/s (1.28 x p90 3.616 m/s over 1851 filtered 1mi efforts)` — derived from `data/best-effort-ceiling.json`'s current `1mi` entry (`ceilingMps 4.6281`, `p90Mps 3.6156908559874186`, `populationN 1851`) | RESEARCH Code Examples |
| assertion sites | `best-effort-ceiling.test.ts` ~line 192 (regex, `\d+\.\d{2}` ×4) and ~line 222 (exact string); `detail-best-efforts-logic.test.ts:332,341` (pass-through fixture strings, cosmetic only) | RESEARCH |
| the exact-string test's new value | its fixture has p90 exactly 3.992 and ceiling 1.28 × 3.992 = 5.10976, so the new string is `implied 8.850 m/s exceeds personal ceiling 5.110 m/s by 3.740 m/s (1.28 x p90 3.992 m/s over 1825 filtered 400m efforts)` | RESEARCH, recomputed |
| NOT affected | `compute-best-efforts.test.ts:987,995` (`/exceeds personal ceiling/` substring, still present); `scripts/curate-queue/derive-flagged.test.mjs:189-193` (synthetic strings that never matched the real format — the word "personal" is absent) | RESEARCH |
| data consequence | regenerating `data/stats/best-efforts.json` rewrites every ceiling row's `reason`; `data/best-effort-ceiling.json` holds only numbers and is NOT affected by this change | RESEARCH Open Question 1 |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the house-register assertions to demand the margin, and watch them fail</name>
  <files>src/analytics/best-effort-ceiling.test.ts</files>
  <read_first>
    - `src/analytics/best-effort-ceiling.test.ts` lines 170-230 — the four tests around the reason
      string: the regex/house-register test, the forbidden-adjective test, the "one ten-thousandth
      above" test, and the exact-string realistic-shape test with its hand-built 1825-element
      population.
    - `src/analytics/best-effort-ceiling.ts` lines 176-203 — the house-register docblock and the
      current template, so the new assertions describe the intended format precisely.
    - `.planning/phases/31-.../31-CONTEXT.md` § D-10 and `.planning/phases/31-.../31-PATTERNS.md`
      § `src/analytics/best-effort-ceiling.ts`.
  </read_first>
  <behavior>
    - The regex test requires three-decimal groups on implied, ceiling, margin and p90, a two-decimal
      multiplier, and an explicit `by <margin> m/s` clause between the ceiling value and the opening
      parenthesis.
    - The exact-string test expects the recomputed string with the margin.
    - A new thin-margin test: for a derivation whose implied speed exceeds the ceiling by less than
      0.005 m/s, the rendered implied and ceiling substrings are NOT equal to each other and the
      margin substring is not `0.000` — the discriminator that the live 4.63-vs-4.63 defect would
      fail. Construct it from the real `1mi` figures (ceiling 4.6281, implied 4.630) so the test
      reproduces the observed case rather than an invented one.
    - The forbidden-adjective test is unchanged and still passes.
  </behavior>
  <action>
    Update the regex assertion so the whole sentence is pinned in the new shape, including the
    `by \d+\.\d{3} m\/s` clause, and keep it anchored at both ends as it is today.

    Update the exact-string assertion to the recomputed value stated in the interfaces table above.
    Recompute it yourself from the fixture's own numbers before writing it — do not paste the value
    on trust — and record the arithmetic in the SUMMARY.

    Add the thin-margin test described in the behavior block, named so it is discoverable
    (include "house register" or "margin" in the name), asserting BOTH that the implied and ceiling
    substrings differ AND that the margin clause is present and non-zero. Assert both directions so
    the test cannot pass vacuously: a 2-dp implementation would render them identical and fail.

    Run the file now and record the RED output verbatim; this is the demonstrated-failing evidence
    for TD-04. Do not touch `best-effort-ceiling.ts` in this task.
  </action>
  <verify>
    <automated>npx vitest run src/analytics/best-effort-ceiling.test.ts 2>&1 | tee /tmp/31-05-red.log; grep -qE "[1-9][0-9]* failed" /tmp/31-05-red.log</automated>
  </verify>
  <acceptance_criteria>
    - The run is RED and the failures are exactly the reason-string tests; the output is recorded verbatim in the SUMMARY as TD-04's demonstrated-failing evidence.
    - The recomputed exact string is derived in the SUMMARY from the fixture's own p90 (3.992), multiplier (1.28) and implied speed (8.85), showing 5.10976 → `5.110` and 8.85 − 5.10976 = 3.74024 → `3.740`.
    - The thin-margin test uses the real `1mi` figures (ceiling 4.6281, implied 4.630) and asserts both the inequality of the two rendered values and a non-zero margin clause.
    - `git status --porcelain` lists only `src/analytics/best-effort-ceiling.test.ts`.
  </acceptance_criteria>
  <done>The assertions demand the margin and fail for exactly that reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the margin at three decimals and bring every consumer assertion with it</name>
  <files>src/analytics/best-effort-ceiling.ts, src/dashboard/views/detail-best-efforts-logic.test.ts</files>
  <read_first>
    - `src/analytics/best-effort-ceiling.ts` lines 150-210 — `ceilingDemotion` and the house-register
      docblock above it, whose worked example must be updated in the same edit so it cannot go stale.
    - `src/dashboard/views/detail-best-efforts-logic.test.ts` lines 315-345 — the `demotionReason`
      fixture strings, pass-through only.
    - `.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md` § PR-04 Sign-off (Round 3) —
      the wording observation this change closes, and the `3475730418@1mi` case it names.
  </read_first>
  <behavior>
    - `ceilingDemotion` returns a reason matching the new regex for every derivation.
    - `margin` is computed as `impliedSpeedMps - derivation.ceilingMps`, always positive, since the
      function already returns `null` unless implied strictly exceeds the ceiling.
    - Consumers that only match `/exceeds personal ceiling/` still pass, unmodified.
  </behavior>
  <action>
    In `ceilingDemotion`, compute `const margin = impliedSpeedMps - derivation.ceilingMps;` after the
    existing guard clauses and change the template to three decimals on implied, ceiling, margin and
    p90, keeping two on the multiplier, inserting `by ${margin.toFixed(3)} m/s` between the ceiling
    value and the opening parenthesis. Nothing else in the function changes.

    Update the house-register docblock directly above it: state the four three-decimal fields and the
    two-decimal multiplier, replace its worked example with the live `3475730418@1mi` case
    (`implied 4.630 m/s exceeds personal ceiling 4.628 m/s by 0.002 m/s (1.28 x p90 3.616 m/s over
    1851 filtered 1mi efforts)`), and name the defect the margin closes — that at 2 dp the sentence
    read "4.63 exceeds 4.63".

    Update the `demotionReason` fixture strings in `detail-best-efforts-logic.test.ts` to the new
    format for consistency. These are pass-through inputs and are not load-bearing; say so in a
    comment so a later reader does not think the badge builder parses them.

    Do not touch `compute-best-efforts.test.ts` or `derive-flagged.test.mjs` — research confirms
    neither is affected. Do not regenerate `data/stats/` in this plan; the phase's regeneration pass
    owns that and must run after this lands.
  </action>
  <verify>
    <automated>npx vitest run src/analytics/best-effort-ceiling.test.ts -t "house register" 2>&1 | tee /tmp/31-05-green.log; grep -qE "Tests +[1-9][0-9]* passed" /tmp/31-05-green.log && ! grep -qE "[1-9][0-9]* failed" /tmp/31-05-green.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analytics/best-effort-ceiling.test.ts -t "house register"` reports at least 1 passed and 0 failed; the count is recorded in `31-VALIDATION.md`.
    - `npx vitest run src/analytics/best-effort-ceiling.test.ts` fully green, including the thin-margin test that was RED in Task 1.
    - `grep -c "toFixed(2)" src/analytics/best-effort-ceiling.ts` returns exactly 1 (the multiplier) and `grep -c "toFixed(3)" src/analytics/best-effort-ceiling.ts` returns exactly 4.
    - The docblock's worked example is the `3475730418@1mi` string, quoted in the SUMMARY.
    - `npm test` exits 0 — in particular `compute-best-efforts.test.ts`'s `/exceeds personal ceiling/` assertions and `derive-flagged.test.mjs` are untouched and still green.
    - `npx tsc --noEmit` exits 0.
    - `git status --porcelain data/` is empty — no regeneration happened in this plan.
  </acceptance_criteria>
  <done>The sentence states its own margin; no consumer needed changing to read it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ceilingDemotion`'s reason string → `data/stats/best-efforts.json` → the Records note, the detail badge, the queue prefill and `28-DIFF.md` | One string crosses into every human-facing explanation of why a record was demoted, and into the artifact a human signs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Repudiation | a demotion reason that reads as self-contradictory ("4.63 exceeds 4.63"), so the human signing the diff cannot verify the claim | mitigate | D-10's explicit margin at three decimals; a thin-margin test built from the real `1mi` figures asserts the two rendered values differ and the margin is non-zero |
| T-31-18 | Tampering | a format change silently invalidating a downstream assertion or consumer | mitigate | Every assertion site was enumerated by research and is updated in the same plan; the two unaffected files are named and verified still green by a full `npm test` |
| T-31-19 | Repudiation | a stale worked example in the house-register docblock outliving the format | mitigate | The docblock's example is replaced in the same edit with the live `3475730418@1mi` case and quoted in the SUMMARY |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
- Task 1's RED output recorded verbatim; Task 2's green run recorded.
- `-t "house register"` match count recorded (expect ≥1, 0 failed).
- `npm test` and `npx tsc --noEmit` exit 0.
- `git status --porcelain data/` empty.
</verification>

<success_criteria>
- Every ceiling demotion reason states implied speed, ceiling, and the margin between them at three decimals.
- The regex test pins the whole new shape; a 2-dp regression would fail it.
- No consumer needed a code change to keep working.
</success_criteria>

<output>
Create `.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-05-SUMMARY.md` when done.
Record: the RED output, the recomputed exact string with its arithmetic, the new docblock example, the
`-t "house register"` match count, and an explicit statement that `data/` was not regenerated here.
TD-04 may be ticked by this plan — the string format is fully landed in code and pinned by tests; the
regenerated artifacts that carry it are TD-05's business.
</output>
