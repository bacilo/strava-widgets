---
phase: 31-tech-debt-closure-silent-failure-guards-docs-reconciliation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/curate-queue/derive-flagged.mjs
  - scripts/curate-queue/derive-flagged.test.mjs
  - scripts/curate-queue/index.ts
autonomous: true
requirements: [TD-03]

must_haves:
  decisions: [D-09]
  truths:
    - "D-09: the curation queue still skips malformed exclusion entries, but now reports how many it skipped"
    - "D-09: the queue page renders a visible line beside the header counts so the count is never quietly short"
    - "D-09: an empty-string reason and a duplicate activityId are counted as malformed, closing the two classes buildExclusionsMap silently tolerated"
    - "deriveFlaggedActivities' return shape is unchanged, so no existing caller or test is disturbed"
    - "derive-flagged.mjs keeps its zero-import, zero-console browser contract"
  artifacts:
    - path: "scripts/curate-queue/derive-flagged.mjs"
      provides: "a sibling export that counts malformed exclusion entries without changing deriveFlaggedActivities"
      contains: "countMalformedExclusions"
    - path: "scripts/curate-queue/index.ts"
      provides: "the rendered malformed-entries line beside the queue summary"
      contains: "data-queue-malformed"
  key_links:
    - from: "scripts/curate-queue/index.ts renderQueue"
      to: "countMalformedExclusions(exclusionsDoc)"
      via: "a call alongside deriveFlaggedActivities/summarizeQueue, rendered as a sibling paragraph"
      pattern: "countMalformedExclusions"
---

<objective>
Close TD-03's third site: the curation review queue keeps skipping malformed exclusion entries, but
now says how many it ignored, on the page, next to the header counts (D-09, 29 WR-02).

Purpose: `buildExclusionsMap` drops bad entries silently today, so the queue's "already excluded"
count can be short by an unknown amount and the page looks internally consistent while being wrong.
The recount's role is to fail closed (31-03); a display surface's role is to degrade visibly.

Output: a sibling counting export, a rendered line, and planted-fixture tests for both.

**Run on the primary checkout.** Task 2's negative-control `npm run curate` smoke run needs the real mirrored archive (`data/stats/`, `data/dashboard/`, `data/activities/`), which a bare worktree does not have (gitignored). Perform the smoke run from the primary checkout, or copy `data/` read-only into the worktree first; record which.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-CONTEXT.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-RESEARCH.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-PATTERNS.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md

<interfaces>
<!-- Established facts. Do not rediscover. -->

| fact | value | source |
|---|---|---|
| function name | `buildExclusionsMap` at `scripts/curate-queue/derive-flagged.mjs:52` — `buildExclusionReasonMap` named in CONTEXT.md does NOT exist anywhere in the repo | RESEARCH correction #2 |
| what it already skips | `null`/non-object entries, non-string `activityId`, `activityId === '__proto__'`, non-string `reason` | live read |
| what it does NOT skip or count | empty-string `reason`; a duplicate `activityId` silently overwrites the map value with no collision count | live read |
| recommended shape | a NEW sibling export (e.g. `countMalformedExclusions(exclusionsDoc)`) rather than changing `deriveFlaggedActivities`'s `QueueRow[]` return — avoids touching every existing test that destructures it | RESEARCH Open Question 2 |
| module contract | MUST stay `.mjs` with JSDoc types (a `.test.ts` sibling would never be collected), and MUST have zero `import` statements and zero `console` calls — it runs unbundled in the browser | `derive-flagged.mjs:7-12` |
| render site | `renderQueue()` in `scripts/curate-queue/index.ts:294-298`: `deriveFlaggedActivities(...)`, `summarizeQueue(rows)`, then a `<p data-queue-summary>` with `${flaggedCount} flagged · ${excludedCount} already excluded` | live read |
| register | named condition plus measured value (Phase 27 D-09) — no adjectives | project convention |
| independence | this implementation and 31-03's recount check are deliberately hand-duplicated; D-08 forbids the recount importing anything, so there is no shared helper to build | RESEARCH Open Question 2 |
| surface scope | the queue is local-only (`npm run curate`), never published; publish guards already keep it out of `dist/widgets` | RESEARCH Architectural Responsibility Map |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Count the malformed entries in derive-flagged.mjs without changing any existing return shape</name>
  <files>scripts/curate-queue/derive-flagged.mjs, scripts/curate-queue/derive-flagged.test.mjs</files>
  <read_first>
    - `scripts/curate-queue/derive-flagged.mjs` — the whole file (196 lines): the module docblock's
      zero-import/zero-console contract, `buildExclusionsMap` (52-69), `deriveFlaggedActivities`
      (104), `summarizeQueue` (190-196).
    - `scripts/curate-queue/derive-flagged.test.mjs` lines 1-70 — the `effort`/`activity` fixture
      helper style and the hand-built-document discipline.
    - `.planning/phases/31-.../31-CONTEXT.md` § D-09 and § Claude's Discretion (wording latitude).
    - `.planning/phases/31-.../31-PATTERNS.md` § `scripts/curate-queue/derive-flagged.mjs`.
  </read_first>
  <behavior>
    - A document whose entries are all well-formed → count 0.
    - A non-string `activityId` → counted once.
    - `activityId === '__proto__'` → counted once.
    - A non-string `reason` → counted once.
    - An empty or whitespace-only `reason` → counted once (the class `buildExclusionsMap` tolerates
      today).
    - Two entries sharing one `activityId` → the collision counted once.
    - A missing/null document, or one whose `exclusions` is not an array → count 0, no throw.
    - `deriveFlaggedActivities` returns exactly what it returns today for every existing fixture.
  </behavior>
  <action>
    Extend `buildExclusionsMap` so it also rejects an empty or whitespace-only `reason` and detects a
    duplicate `activityId`, and so it reports how many entries it dropped — either by returning
    `{ map, skippedCount }` and updating its single internal caller, or by keeping its return type and
    factoring the shared predicate; whichever keeps the diff smallest. Whatever the internal shape,
    the EXPORTED surface change is one new sibling export,
    `countMalformedExclusions(exclusionsDoc)`, returning a number.
    `deriveFlaggedActivities`'s and `summarizeQueue`'s exported signatures and return shapes must not
    change.

    Keep the file's contract: no `import`, no `console`, JSDoc types only. Keyed storage stays a
    `Map`; never index a plain object by an untrusted `activityId`.

    Document in a short docblock on the new export that this validation is deliberately duplicated
    from `scripts/compute-pr-ceiling-recount.mjs`'s own check rather than shared, because that script
    is forbidden from importing anything (Phase 28 D-15 / D-08), and name the four-plus-one classes it
    counts.

    Add tests with names containing "malformed" covering every bullet in the behavior block above,
    each on a hand-built document. Add one test asserting `deriveFlaggedActivities` returns the same
    rows for a document containing malformed entries as for the same document with those entries
    removed — the proof that the count is additive information, not a behaviour change.
  </action>
  <verify>
    <automated>npx vitest run scripts/curate-queue/derive-flagged.test.mjs -t "malformed" 2>&1 | tee /tmp/31-04-t1.log; grep -qE "Tests +[1-9][0-9]* passed" /tmp/31-04-t1.log && ! grep -qE "[1-9][0-9]* failed" /tmp/31-04-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/curate-queue/derive-flagged.test.mjs -t "malformed"` reports at least 6 passed and 0 failed; the count is recorded in `31-VALIDATION.md`.
    - `npx vitest run scripts/curate-queue/derive-flagged.test.mjs` fully green, test count recorded before and after.
    - Contract intact: `grep -cE "^\s*import |console\." scripts/curate-queue/derive-flagged.mjs` returns 0.
    - `grep -c "export function deriveFlaggedActivities" scripts/curate-queue/derive-flagged.mjs` returns 1 and its parameter list is byte-identical to HEAD (`git diff` shows no change to that line).
    - `grep -c "countMalformedExclusions" scripts/curate-queue/derive-flagged.mjs` returns at least 2 (export plus docblock).
    - No test in the file reads `data/best-effort-exclusions.json`: `grep -c "best-effort-exclusions" scripts/curate-queue/derive-flagged.test.mjs` returns 0.
  </acceptance_criteria>
  <done>The queue can say how many entries it ignored, and nothing else about it moved.</done>
</task>

<task type="auto">
  <name>Task 2: Render the ignored-entries line beside the queue header counts</name>
  <files>scripts/curate-queue/index.ts</files>
  <read_first>
    - `scripts/curate-queue/index.ts` lines 280-330 — `renderQueue()`, the
      `<p data-queue-summary>` block, and how `main` is appended to.
    - `scripts/curate-queue/index.ts` line 23 — the existing import list from `./derive-flagged.mjs`.
    - `.planning/phases/29-curation-review-queue/29-CONTEXT.md` § D-14 — the header counts this line
      sits beside, so the new line complements rather than restates them.
    - `.planning/phases/31-.../31-CONTEXT.md` § D-09 and § Claude's Discretion — wording latitude
      within the named-condition-plus-measured-value register.
  </read_first>
  <action>
    Import `countMalformedExclusions` alongside the existing three imports and call it once in
    `renderQueue()` with the same `exclusionsDoc` already passed to `deriveFlaggedActivities`.

    Append a sibling `<p>` immediately after the `data-queue-summary` paragraph carrying its own
    attribute, `data-queue-malformed-note`, whose text states the named condition and the measured
    value — e.g. `N exclusion entries ignored (malformed)`. Render it only when the count is greater
    than zero, so an ordinary session gains no noise; state that choice in a comment so a later
    reader does not mistake its absence for a missing feature.

    Do not restyle, reorder or rewrite the existing summary line, and do not touch any other part of
    `renderQueue()`. The queue is a local-only surface reached through `npm run curate`; no publish
    path or guard changes.

    Verify by building the queue client the way the project already does and confirming the new
    symbol survives into the emitted bundle, then by a local `npm run curate` smoke run in which the
    real, well-formed archive shows NO line (count 0) — record that observation as the negative
    control. A rendered positive case is not required: it is proven by Task 1's unit tests, and
    `31-VALIDATION.md` records the browser spot-check as optional, not required.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build-widgets 2>&1 | tee /tmp/31-04-t2.log; ! grep -q "css-syntax-error" /tmp/31-04-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data-queue-malformed-note" scripts/curate-queue/index.ts` returns 1.
    - `grep -c "countMalformedExclusions" scripts/curate-queue/index.ts` returns at least 2 (import plus call).
    - The existing `data-queue-summary` line is unchanged: `git diff scripts/curate-queue/index.ts` shows no modification to its `textContent` assignment.
    - `npx tsc --noEmit` exits 0 and `npm run build-widgets` exits 0 with zero `css-syntax-error` occurrences.
    - The emitted queue bundle contains the new symbol or its rendered string — state the file inspected and the grep used.
    - A local `npm run curate` run against the real archive renders no malformed-entries line, and that negative control is recorded in the SUMMARY with the exclusions file's entry count.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>The queue page can never be quietly short; when it drops entries, it says how many.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner-editable `data/best-effort-exclusions.json` → `buildExclusionsMap` → the queue page | Hand-maintained JSON parsed in a browser context (ASVS V5) |
| local curate server → browser | Local-only surface, already origin-gated by `curate-server.mjs` (CUR-02, unmodified here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02 | Tampering | malformed or `__proto__`-keyed entries silently dropped, leaving the queue's counts short with no signal | mitigate | `countMalformedExclusions` reports every dropped entry and the page renders the number; keyed storage stays a `Map`, so `__proto__` remains storage-safe and the new check is defence-in-depth plus a report |
| T-31-15 | Tampering | a return-shape change to `deriveFlaggedActivities` silently altering the queue's population | mitigate | The sibling-export approach leaves `deriveFlaggedActivities`'s signature byte-identical, asserted by `git diff`; a test proves identical rows with and without malformed entries present |
| T-31-16 | Information disclosure | the malformed count rendered as an adjective rather than a measured value | mitigate | The house register (Phase 27 D-09) is applied: named condition plus number, no adjective; the exact string is pinned by the unit tests and recorded in the SUMMARY |
| T-31-17 | Tampering | the queue's local-only dev surface leaking into the published build | accept | Unchanged from Phase 24/29: publish guards keep `curate` artifacts out of `dist/widgets`, and `build-widgets` is re-run here with the curation-artifact scan already in the gate |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `npx vitest run scripts/curate-queue/derive-flagged.test.mjs` green; `-t "malformed"` count ≥6 recorded.
- `npx tsc --noEmit`, `npm run build-widgets` (zero `css-syntax-error`) and `npm test` all exit 0.
- A real-archive `npm run curate` run shows no malformed line (negative control recorded).
</verification>

<success_criteria>
- Malformed entries are counted, including the two classes previously tolerated.
- The count reaches the page beside the header counts.
- No existing export's shape or behaviour changed.
</success_criteria>

<output>
Create `.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-04-SUMMARY.md` when done.
Record: the exact rendered string, the `-t "malformed"` match count, the negative-control observation
from the real archive, and the grep proving the bundle carries the new code.

**TD-03 tick rule — read before ticking.** TD-03 spans three code sites across two plans: 31-03
(records-logic, recount) and 31-04 (this plan). Before ticking TD-03 in `REQUIREMENTS.md`, run a full
`npm test` and confirm all three sites' tests are present and green by name: `-t "another guard"`
(≥2), `-t "malformed exclusion"` (≥5), `-t "malformed"` in `derive-flagged.test.mjs` (≥6). Record the
three counts in this SUMMARY as the tick's evidence. If 31-03 has not landed, leave TD-03 unticked and
record why.
</output>
