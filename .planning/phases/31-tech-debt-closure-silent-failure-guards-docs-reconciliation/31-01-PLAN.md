---
phase: 31-tech-debt-closure-silent-failure-guards-docs-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analytics/__fixtures__/best-effort-exclusions.fixture.json
  - src/analytics/compute-best-efforts.test.ts
autonomous: true
requirements: [TD-01]

must_haves:
  decisions: [D-01, D-02, D-03]
  truths:
    - "D-01: no arithmetic assertion in compute-best-efforts.test.ts depends on the contents of the live, owner-editable data/best-effort-exclusions.json — a curation edit cannot turn the nightly deploy gate red for an unexplained numeric mismatch"
    - "D-01: exactly one test still reads the real file, and it asserts only the premise that 4556693525 is excluded all-distance"
    - "D-02: when that premise fails, the message names the entry, the real file, the fixture path and the test, so a human can re-pin it in one minute"
    - "D-03: the fixture is not a silent second source of truth — mutating a COPY of it is demonstrated to break the arithmetic assertions"
  artifacts:
    - path: "src/analytics/__fixtures__/best-effort-exclusions.fixture.json"
      provides: "the committed two-entry exclusions fixture the CR-01 arithmetic runs against"
      contains: "4556693525"
    - path: "src/analytics/compute-best-efforts.test.ts"
      provides: "four CR-01 tests repointed at the fixture, one premise-only live test, one message test, one fixture-copy mutation test"
      contains: "best-effort-exclusions.fixture.json"
  key_links:
    - from: "src/analytics/compute-best-efforts.test.ts"
      to: "src/analytics/__fixtures__/best-effort-exclusions.fixture.json"
      via: "fileURLToPath(new URL(...)) passed as computeBestEfforts({ exclusionsPath })"
      pattern: "best-effort-exclusions\\.fixture\\.json"
    - from: "src/analytics/compute-best-efforts.test.ts premise test"
      to: "data/best-effort-exclusions.json"
      via: "the single remaining live read, premise assertion only"
      pattern: "\\.\\./\\.\\./data/best-effort-exclusions\\.json"
---

<objective>
Decouple the four CR-01 regression tests from the live, owner-editable exclusions file (28 WR-09,
TD-01). All arithmetic moves onto a committed fixture that copies the two real entries verbatim;
exactly one test keeps reading the real file and asserts only its premise, failing loudly with
re-pin instructions.

Purpose: today a developer ticking a box in `npm run curate` can turn the nightly deploy gate red
with a numeric mismatch nobody can explain. After this plan, a curation edit can only ever fail one
named premise assertion whose message says exactly what to do.

Output: one new fixture file and a repointed `compute-best-efforts.test.ts`. No production code
changes — `FileStore.readJson` already resolves an absolute `exclusionsPath` ahead of its `baseDir`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-CONTEXT.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-RESEARCH.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-PATTERNS.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md

<interfaces>
<!-- Facts established by research; the executor does NOT need to rediscover them. -->

| fact | value | source |
|---|---|---|
| the four tests that read the real file | `compute-best-efforts.test.ts` lines 949, 1105, 1138, 1170 (`realExclusionsPath` construction sites) | RESEARCH § TD-01 |
| `3475711469`'s real entry | `distances: null` — ALL-DISTANCE, not 400m-only; the brief and the in-file comment are both wrong | RESEARCH correction #1 |
| `4556693525`'s real entry | `distances: null`, `reason: "bad measurement"` | live file read 2026-09-19 |
| real file top level | `{ "schemaVersion": 1, "note": "...", "exclusions": [...] }` | live file read |
| absolute-path injection | `FileStore.readJson` does `path.resolve(this.baseDir, filePath)`; an absolute path wins — no signature change needed anywhere | `src/storage/file-store.ts:48-49` |
| premise scope | only `4556693525` gets a live premise check; `3475711469` is a negative control and does not | RESEARCH Pitfall 5 |
| `-t` non-vacuity | the Phase 26 audit found a `-t` filter matching 0 tests; every filter below must be proven ≥1 | 31-VALIDATION.md |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit the two-entry fixture and repoint every arithmetic assertion at it</name>
  <files>src/analytics/__fixtures__/best-effort-exclusions.fixture.json, src/analytics/compute-best-efforts.test.ts</files>
  <read_first>
    - `data/best-effort-exclusions.json` — the real file, in full. The two entries are copied from
      here VERBATIM; do not paraphrase, do not invent a `distances` array.
    - `src/analytics/compute-best-efforts.test.ts` lines 860-1200 — `buildPinnedArchive`, the four
      tests at 949 / 1105 / 1138 / 1170, and the idempotence test's shape.
    - `src/storage/file-store.ts` `readJson` — confirms the absolute-path resolution this relies on.
    - `.planning/phases/31-.../31-PATTERNS.md` § `src/analytics/__fixtures__/...` and
      § `src/analytics/compute-best-efforts.test.ts` — the exact excerpts to mirror.
  </read_first>
  <behavior>
    - The four CR-01 tests produce identical results when run against the fixture as they do today
      against the real file (same ceilings, same demotions, same totals).
    - Removing or changing an unrelated entry in `data/best-effort-exclusions.json` no longer changes
      any number any of those four tests assert.
  </behavior>
  <action>
    Create `src/analytics/__fixtures__/best-effort-exclusions.fixture.json` with the same top-level
    shape as the real file: `schemaVersion: 1`, a `note` field stating that this is a verbatim copy
    of two entries from `data/best-effort-exclusions.json` taken 2026-09-19, that it exists so the
    CR-01 arithmetic cannot be moved by a curation edit, and that changing it requires re-running the
    fixture-copy mutation test; and an `exclusions` array holding exactly the two real entries for
    `4556693525` and `3475711469`, each with `distances: null` and `reason: "bad measurement"`, copied
    character-for-character from the real file.

    In `compute-best-efforts.test.ts`, add a `fixtureExclusionsPath` constant alongside the existing
    `realExclusionsPath` idiom, built the same way with
    `fileURLToPath(new URL('./__fixtures__/best-effort-exclusions.fixture.json', import.meta.url))`
    (adjust the relative URL to the test file's own location).

    Repoint the `exclusionsPath` option at lines 971, 1115, 1147 and 1181 from `realExclusionsPath`
    to `fixtureExclusionsPath`. Every `populationN`, ceiling, negative-control speed, demotion and
    totals assertion in those four tests now runs against the fixture.

    Rename the test at line 949 so it no longer contains the phrase "REAL committed exclusion" —
    that phrase is reserved for the premise-only test Task 2 creates, and `-t "REAL committed
    exclusion"` must match exactly one test afterwards. Use wording that names the fixture, e.g.
    "4556693525 with its committed-fixture exclusion entry is ceiling-demoted at 400m and 1k". Leave
    the premise block in place for now; Task 2 extracts it.

    Correct the stale in-file comment at the negative-control site (currently implying `3475711469`
    is "400m only"): it is an all-distance exclusion whose 400m effort simply sits under the ceiling,
    which is what makes it a negative control. Say that.

    Do not touch `computeBestEfforts`, `loadExclusions`, `best-effort-exclusions.ts` or any file
    outside this plan's `files_modified`. Do not edit `data/best-effort-exclusions.json` — not in
    this task, not in any task of this phase.
  </action>
  <verify>
    <automated>npx vitest run src/analytics/compute-best-efforts.test.ts 2>&1 | tee /tmp/31-01-t1.log; grep -qE "Tests +[1-9][0-9]* passed" /tmp/31-01-t1.log && ! grep -qE "[1-9][0-9]* failed" /tmp/31-01-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `src/analytics/__fixtures__/best-effort-exclusions.fixture.json` parses as JSON and its
      `exclusions` array has length exactly 2.
    - `node -e "const f=require('./src/analytics/__fixtures__/best-effort-exclusions.fixture.json');const r=require('./data/best-effort-exclusions.json');const pick=d=>d.exclusions.filter(e=>['4556693525','3475711469'].includes(e.activityId)).sort((a,b)=>a.activityId.localeCompare(b.activityId));if(JSON.stringify(pick(f))!==JSON.stringify(pick(r)))process.exit(1)"` exits 0 — the fixture's two entries are byte-equal to the real file's.
    - `grep -c "fixtureExclusionsPath" src/analytics/compute-best-efforts.test.ts` returns at least 5 (one declaration plus four `exclusionsPath` sites).
    - `grep -n "exclusionsPath: realExclusionsPath" src/analytics/compute-best-efforts.test.ts` returns nothing — no test passes the real path into `computeBestEfforts` any more.
    - `grep -c "400m only" src/analytics/compute-best-efforts.test.ts` returns 0.
    - `git diff --name-only` does NOT list `data/best-effort-exclusions.json`.
    - The whole file's suite is green with the same test count as before the change (record both counts in the SUMMARY).
  </acceptance_criteria>
  <done>The four CR-01 tests compute their numbers from a committed fixture whose two entries are provably identical to the real file's.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract the premise check, make its failure message actionable, and pin the message</name>
  <files>src/analytics/compute-best-efforts.test.ts</files>
  <read_first>
    - `src/analytics/compute-best-efforts.test.ts` lines 944-975 — the existing premise block and its
      current message, which is the thing being replaced.
    - `.planning/phases/31-.../31-CONTEXT.md` § D-02 — the required message content, verbatim intent:
      name the entry, name the file to edit, name the test.
    - `.planning/phases/31-.../31-RESEARCH.md` § Pitfall 5 — only `4556693525` gets a premise check.
  </read_first>
  <behavior>
    - `checkExclusionPremise(doc)` returns `{ ok: true, message: '' }` for a document whose
      `4556693525` entry exists with `distances: null`.
    - It returns `ok: false` with a message naming `4556693525`, `data/best-effort-exclusions.json`,
      `src/analytics/__fixtures__/best-effort-exclusions.fixture.json` and the premise test's own
      name, when the entry is absent.
    - It returns `ok: false` with the same message shape when the entry exists but `distances` is an
      array rather than `null`.
    - The live premise test reads the real file and asserts `ok` is true, using the helper's message
      as the assertion message.
  </behavior>
  <action>
    Add a file-local pure helper `checkExclusionPremise(doc)` to `compute-best-efforts.test.ts` that
    takes a parsed exclusions document, finds the `4556693525` entry, and returns
    `{ ok: boolean; message: string }`. The failure message must name, in one sentence a human can
    act on in a minute: the activity id `4556693525`, that it is no longer excluded all-distance in
    `data/best-effort-exclusions.json`, and that if the change is intentional the fix is to update
    `src/analytics/__fixtures__/best-effort-exclusions.fixture.json` and this premise in
    `src/analytics/compute-best-efforts.test.ts`. No `skipIf`, no environment branch, no CI-only
    path — a red nightly for a reason a human can act on is the intended behaviour (D-02).

    Replace the inline premise block extracted by Task 1's rename with a standalone test whose name
    contains both "REAL committed exclusion" and "premise" and which does nothing but: read the real
    `data/best-effort-exclusions.json` via `fileURLToPath(new URL('../../data/best-effort-exclusions.json', import.meta.url))`,
    parse it, call the helper, and `expect(result.ok, result.message).toBe(true)`. It must NOT call
    `computeBestEfforts` and must assert no arithmetic — that is the whole point of D-01.

    Add a second test, named so `-t "premise"` reaches it, that feeds the helper two doctored
    IN-MEMORY documents (entry absent; entry present with `distances: ['400m']`) and asserts for each
    that `ok` is false and that the message contains all four of: `4556693525`,
    `data/best-effort-exclusions.json`, `best-effort-exclusions.fixture.json`, and
    `compute-best-efforts.test.ts`. This is the demonstrated-failing proof that the premise message
    is actionable rather than merely present.
  </action>
  <verify>
    <automated>npx vitest run src/analytics/compute-best-efforts.test.ts -t "premise" 2>&1 | tee /tmp/31-01-t2.log; grep -qE "Tests +[1-9][0-9]* passed" /tmp/31-01-t2.log && ! grep -qE "[1-9][0-9]* failed" /tmp/31-01-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analytics/compute-best-efforts.test.ts -t "REAL committed exclusion"` reports exactly 1 test passed and 0 failed; the count is recorded in the SUMMARY and in `31-VALIDATION.md`'s row.
    - `npx vitest run src/analytics/compute-best-efforts.test.ts -t "premise"` reports at least 2 tests passed and 0 failed; the exact count is recorded.
    - `grep -c "checkExclusionPremise" src/analytics/compute-best-efforts.test.ts` returns at least 4.
    - The premise-only test body contains no call to `computeBestEfforts` — verify by reading the test and stating so in the SUMMARY.
    - `grep -c "skipIf\|process.env.CI" src/analytics/compute-best-efforts.test.ts` returns 0 for the premise block.
    - The SUMMARY quotes the failure message string verbatim.
  </acceptance_criteria>
  <done>One live premise test, one message test; the premise's failure tells a human exactly what to re-pin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the fixture is not a silent second source of truth</name>
  <files>src/analytics/compute-best-efforts.test.ts</files>
  <read_first>
    - `src/analytics/compute-best-efforts.test.ts` — the idempotence test around line 1138 ("two runs
      ... byte-identical apart from generatedAt") for the tmpDir-copy idiom to mirror.
    - `.planning/phases/31-.../31-CONTEXT.md` § D-03 — mandatory demonstrated-failing for the
      decoupling itself, against a COPY, never the real file and never the committed fixture.
    - `.planning/phases/31-.../31-PATTERNS.md` § `compute-best-efforts.test.ts` — the mutation shape
      suggested (flip `distances: null` to `['400m']` on `4556693525`, expect the 1k demotion to
      disappear).
  </read_first>
  <behavior>
    - Running `computeBestEfforts` with `exclusionsPath` pointed at an unmutated tmpDir copy of the
      fixture reproduces the same demotion state the Task 1 tests assert.
    - Running it against a mutated copy (`4556693525` narrowed to `distances: ['400m']`) changes an
      asserted value — the 1k effort is no longer `excludedFromRecords`, so the arithmetic assertions
      would have caught the drift.
  </behavior>
  <action>
    Add a test whose name contains "fixture copy" that writes the committed fixture's parsed contents
    into the test's own `tmpDir` twice: once unchanged, once with `4556693525`'s `distances` narrowed
    from `null` to `['400m']`. Run `computeBestEfforts` against each copy with the same
    `buildPinnedArchive({ withOneKmBulk: true })` inputs the CR-01 tests use.

    Assert the discriminating pair explicitly, in both directions, so the test cannot pass vacuously:
    against the unchanged copy the 1k effort of `4556693525` has `excludedFromRecords: true`; against
    the mutated copy it has `excludedFromRecords: false`. State in a comment that this is the D-03
    proof — the fixture's contents are load-bearing, so a silent edit to it changes an assertion
    rather than sliding through.

    Never write to `src/analytics/__fixtures__/best-effort-exclusions.fixture.json` and never write to
    `data/best-effort-exclusions.json` from a test; both copies live under the test's `tmpDir` and are
    removed with the existing teardown.
  </action>
  <verify>
    <automated>npx vitest run src/analytics/compute-best-efforts.test.ts -t "fixture copy" 2>&1 | tee /tmp/31-01-t3.log; grep -qE "Tests +[1-9][0-9]* passed" /tmp/31-01-t3.log && ! grep -qE "[1-9][0-9]* failed" /tmp/31-01-t3.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analytics/compute-best-efforts.test.ts -t "fixture copy"` reports at least 1 passed, 0 failed; the count is recorded in `31-VALIDATION.md`.
    - The test asserts BOTH directions (unchanged copy → `excludedFromRecords: true` at 1k; mutated copy → `false`), so it can both pass and fail.
    - `git status --porcelain data/best-effort-exclusions.json src/analytics/__fixtures__/best-effort-exclusions.fixture.json` shows the fixture as added/unchanged and the real file untouched after the run.
    - Full-file suite green: `npx vitest run src/analytics/compute-best-efforts.test.ts` 0 failed.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Mutating a copy of the fixture demonstrably breaks an assertion, so the fixture cannot drift silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner-editable `data/best-effort-exclusions.json` → the test suite → the nightly deploy gate | A hand/UI edit to curation data currently propagates straight into `npm test`'s pass/fail and therefore into whether the site deploys |
| committed test fixture → CR-01 regression arithmetic | A second copy of curation data that could drift from the real file unnoticed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering | `src/analytics/__fixtures__/best-effort-exclusions.fixture.json` becoming a silent second source of truth | mitigate | Task 1 asserts byte-equality of the fixture's two entries against the real file with a `node -e` check; Task 3 proves a mutation to a copy changes an assertion rather than sliding through |
| T-31-08 | Denial of Service | the nightly deploy gate, red for an unexplained numeric mismatch after a curation edit | mitigate | D-01 removes every arithmetic dependency on the live file; the one remaining live read fails with D-02's named, actionable message rather than a numeric diff |
| T-31-09 | Repudiation | a premise assertion that is present but unactionable | mitigate | Task 2's message test pins all four required substrings, so the message cannot degrade to "expected true, got false" |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing; the phase adds zero packages (RESEARCH § Package Legitimacy Audit, not applicable) |
</threat_model>

<verification>
- `npx vitest run src/analytics/compute-best-efforts.test.ts` — 0 failed, test count recorded before and after.
- `npx tsc --noEmit` exits 0.
- `-t` match counts recorded for "REAL committed exclusion" (expect exactly 1), "premise" (expect ≥2), "fixture copy" (expect ≥1).
- `git status --porcelain data/best-effort-exclusions.json` is empty.
</verification>

<success_criteria>
- No arithmetic assertion in the file reads the live exclusions file.
- Exactly one test reads it, asserting only the premise, with an actionable failure message.
- A mutated fixture copy is demonstrated to break an assertion.
</success_criteria>

<output>
Create `.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-01-SUMMARY.md` when done.
Record: the fixture's two entries verbatim, the byte-equality check's output, the premise failure
message verbatim, the three `-t` match counts, and the file's test count before and after.
TD-01 may be ticked by this plan — every site it names is inside this plan's own files.
</output>
