---
phase: 31-tech-debt-closure-silent-failure-guards-docs-reconciliation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/views/records-logic.ts
  - src/dashboard/views/records-logic.test.ts
  - scripts/compute-pr-ceiling-recount.mjs
  - scripts/compute-pr-ceiling-recount.test.mjs
autonomous: true
requirements: [TD-03]

must_haves:
  decisions: [D-07, D-08]
  truths:
    - "D-07: an unrecognized demotion.guard is never folded silently — the Records sentence accounts for it as 'N by another guard' so the on-screen total is always explained"
    - "D-07: records-logic.ts keeps its degrade-rather-than-throw discipline; nothing new can throw on a stale shard"
    - "D-08: a malformed exclusions entry (duplicate activityId, non-string or empty reason, __proto__ key, non-string id) makes compute-pr-ceiling-recount.mjs exit non-zero naming each offending entry"
    - "D-08: the recount still imports nothing from the classifier, the compute step, the best-effort utils or the types (Phase 28 D-15 stands)"
    - "The recount still exits 0 against the real, well-formed data/best-effort-exclusions.json"
  artifacts:
    - path: "src/dashboard/views/records-logic.ts"
      provides: "DemotionCounts.other (required), the counting branch, and the fourth sentence part"
      contains: "another guard"
    - path: "scripts/compute-pr-ceiling-recount.mjs"
      provides: "malformed-exclusion detection surfaced through the existing problems[] channel"
      contains: "malformedExclusions"
  key_links:
    - from: "countDemotedAtDistance's default switch branch"
      to: "describeDemotionCounts's fourth part"
      via: "counts.other, required field on DemotionCounts"
      pattern: "counts\\.other"
    - from: "recountDemotedActivities"
      to: "evaluateReport's problems[] and main()'s process.exitCode = 1"
      via: "a new malformedExclusions: string[] field on the flagged-activities result"
      pattern: "malformedExclusions"
---

<objective>
Close two of TD-03's three silent folds, each in the way its role demands: the Records display
surface degrades honestly (D-07), and the CI-facing recount verifier fails closed (D-08).

Purpose: the Records screen today can show a total larger than the sum of its named parts, with the
remainder invisible; and the recount script — the thing that exists to catch data the pipeline got
wrong — silently skips every class of malformed exclusions entry it was never taught to look at.

Output: an `other` bucket that is a required field (so a future test literal cannot omit it), a
fourth sentence part in fixed trailing position, and four named malformation classes surfaced
through the recount's existing `problems[]` channel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-CONTEXT.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-RESEARCH.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-PATTERNS.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md

<interfaces>
<!-- Established facts. Do not rediscover. -->

| fact | value | source |
|---|---|---|
| `DemotionCounts` | `{ total, ceiling, worldRecord, maxSpeed }` at `records-logic.ts:155-160`; consumed by `countDemotedAtDistance`, `describeDemotionCounts`, `resolvePrTableEmptyState`, `resolvePrTableDemotionNote` | PATTERNS |
| `other` optionality | REQUIRED, not optional — an optional field lets a future literal omit it and reinstates the silent fold | RESEARCH A2 / Anti-Patterns |
| blast radius | `records-logic.test.ts` carries 16 occurrences of `maxSpeed:` (14 single-line literals plus 2 inside multi-line objects) — every `DemotionCounts` literal needs `other: 0`; all existing fixtures have zero unrecognized guards so `0` is correct everywhere | live grep 2026-09-19 |
| sentence order | fixed: ceiling, world-record, max-speed, then `other` appended LAST; zero-count parts stay omitted | PATTERNS / D-07 |
| register | named condition plus measured value (Phase 27 D-09); adjectives are forbidden | `records-logic.ts:227-243` docblock |
| recount gap | `recountDemotedActivities` (`compute-pr-ceiling-recount.mjs:256-293`) only guards `typeof entry.activityId !== 'string'`; no duplicate, reason-shape or `__proto__` check exists | live read |
| recount channel | `evaluateReport` accumulates a flat `problems: string[]`; `main()` prints each and sets `process.exitCode = 1` — extend this, do not invent a third channel | PATTERNS Shared Patterns |
| report field name | `evaluateReport` reads the flagged-activities result as `report.flaggedActivities` (confirm in `buildReport` before wiring) | live read line 583 |
| zero-import rule | `compute-pr-ceiling-recount.mjs` has zero imports naming the ceiling module, the compute step, the best-effort utils or the types, BY ANY SPELLING — D-08 reaffirms Phase 28 D-15 | file docblock lines 1-13 |
| storage safety | exclusion indices are `Map`s, not plain objects, so `__proto__` is already storage-safe; the new check is defence-in-depth and a report, not a new mitigation | RESEARCH Security Domain |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the required `other` bucket and the fourth sentence part (D-07)</name>
  <files>src/dashboard/views/records-logic.ts, src/dashboard/views/records-logic.test.ts</files>
  <read_first>
    - `src/dashboard/views/records-logic.ts` lines 145-250 — the `DemotionCounts` docblock (which
      already describes the unrecognized-guard case in prose), `countDemotedAtDistance`'s switch, and
      `describeDemotionCounts`'s fixed-order parts array.
    - `src/dashboard/views/records-logic.test.ts` lines 480-660 — every `DemotionCounts` object
      literal and the existing `describeDemotionCounts` string assertions.
    - `src/dashboard/views/records.ts` around line 592 — confirms the caller renders whatever the
      sentence builder returns, so no DOM change is needed.
    - `.planning/phases/31-.../31-PATTERNS.md` § `src/dashboard/views/records-logic.ts`.
  </read_first>
  <behavior>
    - An effort whose `demotion.guard` is a value outside the known union increments `other` and
      `total`, and does not increment `ceiling`, `worldRecord` or `maxSpeed`.
    - With one such effort and nothing else, the sentence reads "... (1 by another guard)".
    - With all four buckets non-zero, the breakdown lists ceiling, world-record, max-speed, then
      "another guard" — in that order, `other` last.
    - With `other` zero, the sentence is unchanged from today, character for character.
    - An owner-excluded effort with an unrecognized guard is still skipped entirely (D-10 of Phase 28
      is unaffected).
  </behavior>
  <action>
    Add `other: number` to the `DemotionCounts` interface as a REQUIRED field, initialise it to `0`
    in `countDemotedAtDistance`'s `counts` literal, and change the switch's `default:` branch from a
    no-op comment to `counts.other++`, keeping a comment that says why the branch exists (a closed
    union today, defensive against a shard written by a future guard) and that the count is now
    surfaced rather than folded.

    In `describeDemotionCounts`, append a fourth `parts.push` guarded by `counts.other > 0`, AFTER
    the three named guards, wording it in the house register — a named condition plus its measured
    value, no adjective. Use the singular/plural-neutral form D-07 specifies: `${counts.other} by
    another guard`. Update the function's docblock so the stated fixed order names four parts, and
    update the `DemotionCounts` docblock so its prose matches the new field rather than describing
    the fold.

    Add `other: 0` to every `DemotionCounts` object literal in `records-logic.test.ts` — all 16
    occurrences of `maxSpeed:` in that file, 14 single-line and 2 inside multi-line objects. `0` is
    correct for every existing fixture because none constructs an unrecognized guard. `tsc --noEmit`
    is the gate that proves none was missed; do not make the field optional to shrink this diff.

    Add the negative test D-07 requires, with a name containing "another guard": build an effort
    whose `demotion.guard` is a fabricated string (cast through `as unknown as` the guard type rather
    than widening the production union), assert `countDemotedAtDistance` returns `other: 1` with the
    three named buckets at 0 and `total: 1`, and assert the rendered sentence contains
    "1 by another guard". Add a second case with all four buckets populated that asserts the four
    parts appear in the fixed order — assert the ORDER, by comparing index positions in the string,
    not merely that each substring is present.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/dashboard/views/records-logic.test.ts -t "another guard" 2>&1 | tee /tmp/31-03-t1.log; grep -qE "Tests +[1-9][0-9]* passed" /tmp/31-03-t1.log && ! grep -qE "[1-9][0-9]* failed" /tmp/31-03-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 — proving every `DemotionCounts` literal was updated.
    - `grep -c "other: number" src/dashboard/views/records-logic.ts` returns 1 and `grep -c "other?:" src/dashboard/views/records-logic.ts` returns 0 — the field is required.
    - `grep -c "counts.other++" src/dashboard/views/records-logic.ts` returns 1.
    - `npx vitest run src/dashboard/views/records-logic.test.ts -t "another guard"` reports at least 2 passed, 0 failed; the count is recorded in `31-VALIDATION.md`.
    - The order test compares `indexOf` positions of the four parts, so a reordering would fail it.
    - `npx vitest run src/dashboard/views/records-logic.test.ts` fully green, with the file's test count recorded before and after.
    - No zero-`other` sentence changed: the existing `describeDemotionCounts` string assertions pass unmodified (state in the SUMMARY that none of them was edited, or name and justify any that was).
  </acceptance_criteria>
  <done>An unrecognized guard is counted, named and rendered last; the compiler proves no literal was missed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the recount fail closed on a malformed exclusions entry (D-08)</name>
  <files>scripts/compute-pr-ceiling-recount.mjs, scripts/compute-pr-ceiling-recount.test.mjs</files>
  <read_first>
    - `scripts/compute-pr-ceiling-recount.mjs` lines 1-20 (the zero-import docblock — a binding
      constraint), 246-295 (`recountDemotedActivities`), 476-595 (`evaluateReport`, including how it
      reaches the flagged-activities result), and the `main()` exit wiring near line 796.
    - `scripts/compute-pr-ceiling-recount.test.mjs` around line 198 —
      `describe('recountDemotedActivities', ...)` and the `bestEffortsDoc` / `effort` fixture helpers
      to reuse.
    - `scripts/curate-queue/derive-flagged.mjs:52-69` — read-only, as the reference for what counts as
      malformed. Do NOT import it; D-08's independence means the two implementations are deliberately
      hand-duplicated, matching the `TARGET_METERS_LOCAL` precedent in this same script.
    - `.planning/phases/31-.../31-CONTEXT.md` § D-08 and `.planning/phases/31-.../31-PATTERNS.md`
      § `scripts/compute-pr-ceiling-recount.mjs`.
  </read_first>
  <behavior>
    - Two entries sharing one `activityId` → one problem naming that id as a duplicate.
    - An entry whose `reason` is missing, non-string, or the empty/whitespace-only string → one
      problem naming the entry's id and the defect.
    - An entry whose `activityId` is the literal `"__proto__"` → one problem naming it.
    - An entry whose `activityId` is not a string (number, null, object) → one problem naming its
      array index, since there is no safely stringifiable id.
    - A document with all four shapes at once → four distinct problems, and `evaluateReport` returns
      `pass: false`.
    - The real, well-formed `data/best-effort-exclusions.json` → zero problems from this check and a
      clean exit.
    - `exclusionsTotal` and `excludedWithinFlaggedCount` keep their present meaning for well-formed
      entries, so no downstream count moves on good data.
  </behavior>
  <action>
    In `recountDemotedActivities`, extend the exclusions walk to collect a new
    `malformedExclusions: string[]` return field, one human-readable entry per offending exclusion
    naming both the offender (its `activityId` when safely stringifiable, otherwise its array index)
    and the specific defect. Detect all four classes: duplicate `activityId` (track seen ids in a
    `Set`), non-string or empty/whitespace-only `reason`, an `activityId` equal to `"__proto__"`, and
    a non-string `activityId`. Keep using `Map`/`Set` for any keyed storage — never a plain object
    keyed by an untrusted id. Do not change what `exclusionsTotal` counts for well-formed entries.

    In `evaluateReport`, add a sibling block alongside the existing `unrecognisedGuards` /
    `rankedButDemotedIds` checks that reads the flagged-activities result's `malformedExclusions` and
    pushes one `problems` entry per offender, phrased like its siblings (count, then the joined
    list). Confirm the field name the report object uses (`report.flaggedActivities` per line 583)
    before wiring; do not guess. `main()`'s existing `process.exitCode = 1` wiring then does the rest
    — do not add a second exit path.

    Add no `import` of any kind. Re-verify the zero-import guard after editing.

    In the test file, extend `describe('recountDemotedActivities', ...)` with tests whose names
    contain "malformed exclusion", one per class plus one combined case, each passing a hand-built
    `exclusionsDoc` as the second argument. Add an `evaluateReport` test proving `pass` flips to
    `false` and the problem strings name each offender. Every fixture is hand-built in the test —
    never read `data/best-effort-exclusions.json`, never write to it.

    Finally run the script against the real archive and record its verbatim stdout: it must still
    reach its normal verdict with zero malformed-exclusion problems.
  </action>
  <verify>
    <automated>npx vitest run scripts/compute-pr-ceiling-recount.test.mjs -t "malformed exclusion" 2>&1 | tee /tmp/31-03-t2.log; grep -qE "Tests +[1-9][0-9]* passed" /tmp/31-03-t2.log && ! grep -qE "[1-9][0-9]* failed" /tmp/31-03-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/compute-pr-ceiling-recount.test.mjs -t "malformed exclusion"` reports at least 5 passed and 0 failed (four classes plus the combined case); the count is recorded in `31-VALIDATION.md`.
    - `npx vitest run scripts/compute-pr-ceiling-recount.test.mjs` fully green.
    - Zero-import guard intact: `grep -nE "^\s*(import|const .*= *require|await import)" scripts/compute-pr-ceiling-recount.mjs | grep -vE "node:" | wc -l` returns 0, and the file's own docblock rule is re-read and restated in the SUMMARY.
    - `node scripts/compute-pr-ceiling-recount.mjs` exits 0 against the real, well-formed `data/best-effort-exclusions.json`; its verbatim stdout is recorded, including the flagged-activity and exclusion totals, and they are unchanged from the figures in `28-VALIDATION.md` § PR-04 Sign-off (Round 3) or the difference is explained by archive drift with a number.
    - `git status --porcelain data/best-effort-exclusions.json` is empty.
    - `npm test` and `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>Four malformation classes are named and non-zero-exit; the real file still passes clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner-editable `data/best-effort-exclusions.json` → `compute-pr-ceiling-recount.mjs` | Hand-maintained JSON crossing into the verifier that gates the phase's own numbers (ASVS V5) |
| `data/stats/best-efforts.json` shard → `records-logic.ts` → the Records screen | A shard written by a future or foreign guard value reaching a user-facing total |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02 | Tampering | malformed or prototype-polluting entries in the owner-editable exclusions file reaching the recount unreported | mitigate | D-08's four named malformation classes surfaced through the existing `problems[]` channel; keyed storage stays `Map`/`Set`, never a plain object indexed by an untrusted id; four planted-fixture negative tests plus a combined case |
| T-31-12 | Information disclosure | a Records total larger than the sum of its named parts, with the remainder unexplained on screen | mitigate | D-07's required `other` field, counted in the `default` branch and rendered last in the fixed order; the compiler enforces the field at every literal |
| T-31-13 | Tampering | the recount importing the classifier it checks, making its verdict circular | mitigate | Zero-import discipline reaffirmed (Phase 28 D-15); validation is hand-rolled `typeof`/`Array.isArray`/`__proto__` checks; the guard is re-verified by grep after the edit |
| T-31-14 | Denial of Service | the new fail-closed check turning the real archive red for a well-formed file | mitigate | Task 2 runs the script against the real file and requires exit 0 with its totals recorded; the malformation classes are exactly the four D-08 names, no broader schema check |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/dashboard/views/records-logic.test.ts` and `npx vitest run scripts/compute-pr-ceiling-recount.test.mjs` both green.
- `-t` match counts recorded for "another guard" (expect ≥2) and "malformed exclusion" (expect ≥5).
- `node scripts/compute-pr-ceiling-recount.mjs` exits 0 on the real archive, stdout recorded verbatim.
- `npm test` exits 0.
</verification>

<success_criteria>
- The Records sentence can never understate its own total.
- Four named malformation classes make the recount exit non-zero, each naming its offender.
- The recount still imports nothing it checks, and still passes on real data.
</success_criteria>

<output>
Create `.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-03-SUMMARY.md` when done.
Record: the exact `other`-bucket sentence wording, the four problem-string wordings, both `-t` match
counts, the zero-import re-verification, and the recount's verbatim stdout on the real archive.

**TD-03 tick rule — read before ticking.** TD-03 spans three code sites across two plans: 31-03 (this
plan: records-logic, recount) and 31-04 (the curation queue). Do NOT tick TD-03 in `REQUIREMENTS.md`
from this plan unless 31-04 has already landed AND a full `npm test` is green with all three sites'
tests present. If 31-04 has not landed, leave TD-03 unticked and record that in this SUMMARY with the
reason; 31-04 ticks it.
</output>
