---
phase: 31-tech-debt-closure-silent-failure-guards-docs-reconciliation
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/compute-pr-ceiling-calibration.mjs
  - scripts/compute-pr-ceiling-calibration.test.mjs
autonomous: true
requirements: [TD-05]

must_haves:
  decisions: [D-11]
  truths:
    - "28 WR-07: the largest-drift sentence names whichever distance actually has the greatest absolute drift in this run, computed from the same table it sits under — never a hard-coded '400m'"
    - "28 WR-08: a reader can see why the calibration report's demoted count differs from the pipeline's ceiling-demotion total without leaving the page"
    - "Both fixes are pinned by unit tests on renderCalibrationMarkdown with hand-built reports, so a future edit cannot re-hard-code the sentence"
    - "No ceiling, p90, multiplier or population number moves — this plan changes report prose and columns only"
  artifacts:
    - path: "scripts/compute-pr-ceiling-calibration.mjs"
      provides: "a data-derived drift sentence and a reconciled demoted column"
      contains: "owner-excluded"
  key_links:
    - from: "report.reconciliation per-distance drift values"
      to: "the rendered largest-drift sentence"
      via: "a max-absolute-drift selection computed at render time"
      pattern: "largest drift"
---

<objective>
Fix the two 28-REVIEW findings baked into `compute-pr-ceiling-calibration.mjs`'s renderer: WR-07's
hard-coded "400m shows the largest drift" prose, which is an assertion the data may no longer
support, and WR-08's unlabelled "Demoted" column, which counts only non-owner-excluded efforts and
therefore cannot be reconciled against the pipeline's ceiling-demotion total by a reader of the page.

Purpose: `28-CEILING-CALIBRATION.md` is an artifact of record that this phase regenerates. Correcting
its prose by hand would revert on the next run — the project's own recorded lesson — so the renderer
is what changes.

Output: a renderer whose drift sentence is derived and whose demoted column reconciles, both pinned
by tests against hand-built reports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-CONTEXT.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-RESEARCH.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-PATTERNS.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md

<interfaces>
<!-- Established facts. Do not rediscover. -->

| fact | value | source |
|---|---|---|
| WR-07 site | `scripts/compute-pr-ceiling-calibration.mjs:580`, the string literal beginning `'400m shows the largest drift: ...'`, rendered directly under the `\| Distance \| Live n \| 28-CONTEXT.md n \| Drift \|` table | live read |
| the drift data | `report.reconciliation[key] = { liveN, referenceN, drift }` for each key in `TARGET_ORDER` — the same structure that builds the table above the sentence | live read |
| WR-08 site | `scripts/compute-pr-ceiling-calibration.mjs:619`, the header `\| Distance \| Ceiling (m/s) \| Ceiling (time) \| Eligible \| n \| Demoted \| Demoted of top 10 \|`, fed from `applyCeiling`'s `demotedCount` | live read |
| why the counts differ | `buildFilteredPopulations` (line 86) skips every `effort.excludedFromRecords` effort AND every world-record/max-speed-demoted effort, so `demotedCount` is a NON-EXCLUDED count; the pipeline's ceiling-demotion total (32 at the 2026-09-19 merge, of which 13 are owner-excluded) counts owner-excluded efforts too | live read + `28-VALIDATION.md` Round 3 |
| the pattern to borrow | `scripts/compute-pr-ceiling-diff.mjs:464,471,682` already renders `Of which owner-excluded` / `totals.totalDemotedExcluded` — mirror its presentation rather than inventing one | PATTERNS |
| the test seam | `renderCalibrationMarkdown`, `applyCeiling` and `buildFilteredPopulations` are all exported and already unit-tested in `scripts/compute-pr-ceiling-calibration.test.mjs` with hand-built fixtures; importing the module must not read the archive or write the artifact | test file header |
| discretion | relabel vs extra column is Claude's Discretion (CONTEXT § Claude's Discretion), as long as a reader can see why the two numbers differ without leaving the page | 31-CONTEXT.md |
| scope | prose and columns only; no threshold, multiplier, percentile or ceiling arithmetic may change | ROADMAP § Phase 31 Out of scope (no threshold retune) |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive the largest-drift sentence from the data (WR-07)</name>
  <files>scripts/compute-pr-ceiling-calibration.mjs, scripts/compute-pr-ceiling-calibration.test.mjs</files>
  <read_first>
    - `scripts/compute-pr-ceiling-calibration.mjs` lines 540-600 — the reconciliation table render and
      the hard-coded paragraph that follows it, in full, so the retained mechanism explanation is
      reworded rather than deleted.
    - `scripts/compute-pr-ceiling-calibration.mjs` `buildCalibrationReport` (line 414) — where
      `report.reconciliation` is assembled.
    - `scripts/compute-pr-ceiling-calibration.test.mjs` — the hand-built report fixtures already used
      for `renderCalibrationMarkdown`, to extend rather than invent a new fixture style.
  </read_first>
  <behavior>
    - A report whose largest absolute drift is at `1k` renders a sentence naming `1k`, with its drift
      value, and does not contain the literal string `400m shows the largest drift`.
    - A report whose largest absolute drift is at `400m` names `400m` — the fix must not merely
      rename, it must select.
    - A negative drift of larger magnitude than any positive drift wins the selection (absolute
      value, not signed).
    - A tie between two distances is rendered deterministically (both named, or the first in
      `TARGET_ORDER` named with the tie stated) — the same input always produces the same sentence.
    - A report with every drift at zero renders a sentence stating that no distance drifted, and
      never names a distance as "largest".
  </behavior>
  <action>
    Replace the hard-coded opening clause at line 580 with a value computed at render time from
    `report.reconciliation`: select the distance with the greatest absolute `drift` across
    `TARGET_ORDER`, break ties deterministically, and handle the all-zero case explicitly. State the
    named distance and its drift figure in the house register — named condition plus measured value.

    Keep the paragraph's mechanism explanation (the gitignored `data/stats/` versus the git-tracked,
    already-committed exclusions list) but reword it so it is a hypothesis about whichever distance
    was named, not a claim welded to `400m`. Preserve the reference to activity `4556693525` only
    where it is still accurate; if the named distance is not one the exclusion affects, the sentence
    must say so rather than assert a mechanism the data does not support.

    Extract the selection into a small exported pure helper (e.g. `largestAbsoluteDrift(reconciliation)`
    returning the key and its drift, or `null` when all drifts are zero) so the tests can pin the
    selection independently of the whole rendered document.

    Add tests whose names contain "largest drift", one per bullet of the behavior block, each on a
    hand-built report. At least one must assert the NEGATIVE: that the rendered markdown does not
    contain `400m shows the largest drift` when the largest drift is elsewhere.
  </action>
  <verify>
    <automated>npx vitest run scripts/compute-pr-ceiling-calibration.test.mjs -t "largest drift" 2>&1 | tee /tmp/31-07-t1.log; grep -qE "Tests +[1-9][0-9]* passed" /tmp/31-07-t1.log && ! grep -qE "[1-9][0-9]* failed" /tmp/31-07-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/compute-pr-ceiling-calibration.test.mjs -t "largest drift"` reports at least 4 passed and 0 failed; the count is recorded in `31-VALIDATION.md`.
    - `grep -n "400m shows the largest drift" scripts/compute-pr-ceiling-calibration.mjs` returns nothing.
    - One test asserts the all-zero-drift case renders no "largest" claim, and one asserts a negative drift of larger magnitude wins.
    - `npx vitest run scripts/compute-pr-ceiling-calibration.test.mjs` fully green; the file's test count before and after is recorded.
    - No ceiling/multiplier/percentile arithmetic changed: `git diff scripts/compute-pr-ceiling-calibration.mjs` touches only render-side code and the new helper — state the function names touched in the SUMMARY.
    - Nothing under `.planning/phases/28-*/` was written: `git status --porcelain .planning/phases/28-pr-plausibility-ceiling/` is empty.
  </acceptance_criteria>
  <done>The sentence names whichever distance the data says, or says nothing drifted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the Demoted column reconcile with the pipeline's ceiling total (WR-08)</name>
  <files>scripts/compute-pr-ceiling-calibration.mjs, scripts/compute-pr-ceiling-calibration.test.mjs</files>
  <read_first>
    - `scripts/compute-pr-ceiling-calibration.mjs` lines 86-130 (`buildFilteredPopulations`, which
      drops owner-excluded efforts), 270-300 (`applyCeiling`'s `demotedCount`) and 610-640 (the table
      render) — these three together are why 18 ≠ 32.
    - `scripts/compute-pr-ceiling-diff.mjs` lines 460-475 and 680-685 — the already-solved
      `Of which owner-excluded` presentation to mirror.
    - `.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md` § PR-04 Sign-off (Round 3) — the
      live figures (32 ceiling-only demoted, 13 owner-excluded rows) the reconciliation is checked
      against.
    - `.planning/phases/31-.../31-CONTEXT.md` § Claude's Discretion — relabel vs extra column.
  </read_first>
  <behavior>
    - For a hand-built document containing both non-excluded and owner-excluded efforts above the
      derived ceiling, the rendered table distinguishes the two counts per distance.
    - The rendered page states, in words, that the non-excluded count plus the owner-excluded count is
      the figure the pipeline reports as ceiling demotions, so the two numbers can be reconciled
      without leaving the page.
    - With zero owner-excluded efforts over the ceiling, the reconciliation still renders, showing
      zero, rather than disappearing.
    - The existing non-excluded `demotedCount` values are unchanged for every existing fixture — the
      column is relabelled and joined, never recomputed.
  </behavior>
  <action>
    Add an exported pure helper that counts, per distance, the efforts the existing filtered
    population deliberately drops but which are still above the derived ceiling — that is,
    `excludedFromRecords` efforts whose own implied speed exceeds the distance's `ceilingMps`. Derive
    the speed the same way `buildFilteredPopulations` does (target metres divided by `durationSec`),
    and skip world-record/max-speed-demoted efforts on the same terms, so the two counts partition the
    same universe rather than overlapping.

    Relabel the existing `Demoted` column so its scope is explicit (it counts non-excluded efforts
    only) and add the owner-excluded count as its own column, mirroring
    `compute-pr-ceiling-diff.mjs`'s existing presentation. Below the table, render a reconciliation
    line stating both totals, their sum, and that the sum is the figure the pipeline reports as
    ceiling demotions and that `compute-pr-ceiling-recount.mjs --expect-demoted` checks. Name why the
    filtered population excludes owner-excluded efforts (it is the population the ceiling is DERIVED
    from; the owner's intent must not move the machine's threshold) so the distinction reads as
    deliberate rather than as a bug.

    Add tests whose names contain "owner-excluded": one asserting the per-distance split on a
    hand-built document with both kinds of effort; one asserting the zero case still renders; one
    asserting the rendered sum equals non-excluded plus owner-excluded. Do not read any file under
    `data/` from a test.
  </action>
  <verify>
    <automated>npx vitest run scripts/compute-pr-ceiling-calibration.test.mjs -t "owner-excluded" 2>&1 | tee /tmp/31-07-t2.log; grep -qE "Tests +[1-9][0-9]* passed" /tmp/31-07-t2.log && ! grep -qE "[1-9][0-9]* failed" /tmp/31-07-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/compute-pr-ceiling-calibration.test.mjs -t "owner-excluded"` reports at least 3 passed and 0 failed; the count is recorded in `31-VALIDATION.md`.
    - The rendered header no longer carries a bare `| Demoted |` column: `grep -c "| Demoted |" scripts/compute-pr-ceiling-calibration.mjs` returns 0.
    - The reconciliation sentence names both counts, their sum, and `compute-pr-ceiling-recount.mjs`; it is quoted verbatim in the SUMMARY.
    - Existing `applyCeiling` tests are unmodified and green — the non-excluded counts did not move.
    - `npx vitest run scripts/compute-pr-ceiling-calibration.test.mjs` fully green; `npm test` exits 0.
    - `git status --porcelain .planning/ data/` is empty — this plan regenerates nothing.
  </acceptance_criteria>
  <done>The page shows both counts and says what their sum is, so 18 and 32 reconcile in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator prose → committed artifact of record → a human's judgment at the PR-04 sign-off | A hard-coded claim in generated prose is indistinguishable, to the reader, from a measured one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Repudiation | a generated artifact asserting a finding the data no longer supports (WR-07's hard-coded "400m") | mitigate | The clause is computed from `report.reconciliation` at render time via an exported helper; tests assert selection, tie-breaking, the negative-drift case, the all-zero case, and the ABSENCE of the old literal |
| T-31-23 | Information disclosure | a table whose count silently means something narrower than its label, so a reader reconciles 18 against 32 and concludes the pipeline is wrong | mitigate | WR-08: the column is scoped in its label, the owner-excluded count is rendered beside it, and a reconciliation sentence states the sum and the command that checks it |
| T-31-24 | Tampering | a "reconciliation" that double-counts or overlaps, making the sum coincidentally right | mitigate | The new helper partitions the same universe as `buildFilteredPopulations` (same speed derivation, same world-record/max-speed skip); a test asserts the rendered sum equals the two parts |
| T-31-25 | Tampering | a prose fix that quietly moves a threshold | mitigate | Out-of-scope by the ROADMAP; the acceptance criteria require naming every function touched and keeping every existing `applyCeiling` test unmodified and green |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `npx vitest run scripts/compute-pr-ceiling-calibration.test.mjs` green; `-t "largest drift"` (≥4) and `-t "owner-excluded"` (≥3) counts recorded.
- `grep -n "400m shows the largest drift"` returns nothing.
- `npm test` and `npx tsc --noEmit` exit 0.
- `git status --porcelain .planning/ data/` empty.
</verification>

<success_criteria>
- The drift sentence is selected from the data, with tie and zero cases handled.
- The demoted column's scope is explicit and its complement is on the page with a stated sum.
- No arithmetic that produces a ceiling changed.
</success_criteria>

<output>
Create `.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-07-SUMMARY.md` when done.
Record: the new drift sentence template, the new column headers, the reconciliation sentence verbatim,
both `-t` match counts, and the list of functions touched.

**TD-05 tick rule:** do NOT tick TD-05 here. It ticks only in 31-10, after the PR-04 Round 4 verdict.
</output>
