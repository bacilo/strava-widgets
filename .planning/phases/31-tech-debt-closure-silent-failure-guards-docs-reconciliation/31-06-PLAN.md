---
phase: 31-tech-debt-closure-silent-failure-guards-docs-reconciliation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/stream-files.mjs
  - scripts/compute-pace-residual.mjs
  - scripts/compute-pace-quality-calibration.mjs
  - scripts/compute-pace-residual.test.mjs
autonomous: true
requirements: [TD-05]

must_haves:
  decisions: [D-11]
  truths:
    - "D-11 / 27 G-03: compute-pace-residual.mjs's archive sweep no longer counts data/streams/manifest.json as a stream file, so 26-RESIDUAL.md's denominator is the true stream count"
    - "isStreamFile has exactly one owner: a shared scripts/lib module both generators import, so the two cannot drift apart again"
    - "The manifest exclusion is reachable by a unit test, not only by an archive-wide CLI run"
    - "compute-pace-residual.mjs gains no import edge into compute-pace-quality-calibration.mjs's module-scope machinery"
  artifacts:
    - path: "scripts/lib/stream-files.mjs"
      provides: "the single owner of isStreamFile (and idFromFilename), with no top-level side effects"
      contains: "export function isStreamFile"
    - path: "scripts/compute-pace-residual.mjs"
      provides: "a manifest-excluding archive sweep with a unit-testable seam"
      contains: "isStreamFile"
  key_links:
    - from: "scripts/compute-pace-residual.mjs and scripts/compute-pace-quality-calibration.mjs"
      to: "scripts/lib/stream-files.mjs"
      via: "both import isStreamFile from the shared module; neither redefines it"
      pattern: "from '\\./lib/stream-files\\.mjs'"
---

<objective>
Fix the generator behind 27 G-03: `compute-pace-residual.mjs` counts `data/streams/manifest.json` as
a per-activity stream file, so `26-RESIDUAL.md`'s "Archive size scanned" and every denominator
derived from it are off by one. Lift `isStreamFile` out of `compute-pace-quality-calibration.mjs`
into a shared `scripts/lib/` module so both generators read from one owner, and give the sweep a
seam a unit test can reach.

Purpose: `26-RESIDUAL.md` is the artifact of record for PACE-06's "14 of the 154" figure, which this
phase's docs pass corrects in `REQUIREMENTS.md`. Correcting the prose against a generator that is
still wrong would revert on the next regeneration — the project's own recorded lesson.

Output: a shared helper, a corrected sweep, and a unit test that reaches the exclusion directly.

**Run on the primary checkout.** The CLI archive-count check reads `data/streams/`, which is
gitignored and absent from bare worktrees.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-CONTEXT.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-RESEARCH.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-PATTERNS.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md

<interfaces>
<!-- Established facts. Do not rediscover. -->

| fact | value | source |
|---|---|---|
| the bug | `scripts/compute-pace-residual.mjs:277`, `readdirSync(STREAMS_DIR).filter((f) => f.endsWith('.json'))` — includes `manifest.json` | live read |
| the fix's source | `isStreamFile` and its usual pair `idFromFilename` at `scripts/compute-pace-quality-calibration.mjs:66-83` | live read |
| why a shared module, not a direct import | importing from the calibration script creates a static edge into a file with `execSync`/`REGENERATE_COMMAND` machinery at module scope; `scripts/lib/copy-data-tree.mjs`'s header states the exact precedent for extracting instead | RESEARCH Pitfall 4 |
| the seam gap | `sweepArchive()` (line 273) is NOT exported, so `compute-pace-residual.test.mjs` (which covers only the four exported pure functions) cannot reach the filter today | RESEARCH Validation Architecture |
| the side-effect ordering hazard | `compute-pace-quality-calibration.mjs:646` runs `execSync('npm run compute-pace-residual')` as part of its own run — relevant to the regeneration plan, not to this one; this plan must not regenerate either artifact | RESEARCH Pitfall 3 |
| the ground-truth count | `find data/streams -name '*.json' ! -name manifest.json \| wc -l` | 31-VALIDATION.md |
| vitest collection | `scripts/**/*.test.mjs` only; a `.test.ts` under `scripts/` is never collected | vitest.config.ts |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give isStreamFile one owner in scripts/lib and have both generators import it</name>
  <files>scripts/lib/stream-files.mjs, scripts/compute-pace-quality-calibration.mjs, scripts/compute-pace-residual.mjs</files>
  <read_first>
    - `scripts/compute-pace-quality-calibration.mjs` lines 55-100 — `idFromFilename` and
      `isStreamFile` verbatim, plus every call site of both inside that file.
    - `scripts/lib/copy-data-tree.mjs` lines 1-12 — the extraction-rationale header docblock
      convention the new module's header mirrors, including the no-top-level-side-effects contract.
    - `scripts/compute-pace-residual.mjs` lines 1-60 and 265-300 — its import block and
      `sweepArchive()`.
    - `.planning/phases/31-.../31-PATTERNS.md` § `scripts/lib/stream-files.mjs`.
  </read_first>
  <behavior>
    - `isStreamFile('12345.json')` → true; `isStreamFile('manifest.json')` → false;
      `isStreamFile('notes.txt')` → false.
    - `idFromFilename('12345.json')` → `'12345'`; a name without the extension is returned unchanged.
    - Importing `scripts/lib/stream-files.mjs` runs no archive read, writes no file and spawns no
      subprocess.
  </behavior>
  <action>
    Create `scripts/lib/stream-files.mjs` exporting `isStreamFile` and `idFromFilename`, lifted
    verbatim from `compute-pace-quality-calibration.mjs` including their JSDoc. Its header docblock
    states the extraction rationale in the same terms `copy-data-tree.mjs` uses: extracted so both
    `compute-pace-residual.mjs` and `compute-pace-quality-calibration.mjs` can import it without
    either taking an edge into the other's module-scope `execSync` machinery, and that this module
    has no top-level side effects. Name 27 G-01/G-03 as the history that produced it.

    In `compute-pace-quality-calibration.mjs`, delete the two local definitions and import them from
    the new module. Its behaviour must not change in any way; this is a move, not a rewrite.

    In `compute-pace-residual.mjs`, import `isStreamFile` from the same module and change
    `sweepArchive()`'s filter from `(f) => f.endsWith('.json')` to `isStreamFile`. Add a one-line
    comment naming 27 G-03 so the next reader knows why the filter is not the obvious one.

    Do not regenerate `26-RESIDUAL.md` or `27-CALIBRATION.md` in this plan and do not run either
    generator's `main()` — the regeneration pass owns that and must run after all generator fixes
    land, in dependency order.
  </action>
  <verify>
    <automated>node -e "import('./scripts/lib/stream-files.mjs').then(m=>{if(m.isStreamFile('manifest.json')||!m.isStreamFile('1.json')||m.isStreamFile('a.txt')||m.idFromFilename('1.json')!=='1')process.exit(1);console.log('ok')})" && npm test 2>&1 | tee /tmp/31-06-t1.log; ! grep -qE "[1-9][0-9]* failed" /tmp/31-06-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/stream-files.mjs` exists and exports both functions; importing it prints nothing and creates no file (verify by running the `node -e` check above in a clean tree and confirming `git status --porcelain` is unchanged afterwards).
    - `grep -c "function isStreamFile" scripts/compute-pace-quality-calibration.mjs` returns 0 and `grep -c "stream-files.mjs" scripts/compute-pace-quality-calibration.mjs` returns 1 — exactly one owner.
    - `grep -c "stream-files.mjs" scripts/compute-pace-residual.mjs` returns 1 and `grep -n "endsWith('.json')" scripts/compute-pace-residual.mjs` no longer shows the `sweepArchive` filter line.
    - `npm test` exits 0 with zero failures.
    - `git status --porcelain .planning/phases/26-*/26-RESIDUAL.md .planning/phases/27-*/27-CALIBRATION.md` is empty — nothing was regenerated here.
  </acceptance_criteria>
  <done>One shared `isStreamFile`, imported by both generators; the residual sweep uses it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the manifest exclusion reachable by a unit test and confirm the live count</name>
  <files>scripts/compute-pace-residual.mjs, scripts/compute-pace-residual.test.mjs</files>
  <read_first>
    - `scripts/compute-pace-residual.test.mjs` — the whole file; it covers the four exported pure
      functions and establishes this file's fixture style.
    - `scripts/compute-pace-residual.mjs` lines 265-365 — `sweepArchive()`, `buildReport()`,
      `main()`, and the `Archive size scanned:` line at 201/359.
    - `.planning/phases/31-.../31-RESEARCH.md` § Validation Architecture, TD-05 rows — the seam gap
      and the CLI cross-check this task closes.
  </read_first>
  <behavior>
    - Given a directory containing per-activity stream JSON files, a `manifest.json` and a non-JSON
      file, the seam returns only the per-activity stream filenames.
    - The count the seam reports for the real `data/streams/` equals
      `find data/streams -name '*.json' ! -name manifest.json | wc -l`.
    - The old filter would have returned exactly one more entry — asserted directly, so the fix's
      effect is demonstrated rather than assumed.
  </behavior>
  <action>
    Give `sweepArchive`'s filename selection a testable seam. Prefer the smallest change that lets a
    unit test reach it: export a small pure helper such as `listStreamFilenames(dirPath)` that does
    the `readdirSync` plus `isStreamFile` filter and is called by `sweepArchive`, rather than
    exporting `sweepArchive` itself (which reads and parses every stream and is not unit-testable at
    archive scale). Keep `sweepArchive`'s existing `try`/`catch` warning behaviour for an unreadable
    directory — the helper must degrade the same way, returning an empty list rather than throwing.

    Add tests whose names contain "manifest": plant an `fs.mkdtemp` directory with two per-activity
    JSON files, a `manifest.json` and a `.txt` file; assert the helper returns exactly the two
    per-activity names; and assert in the same test that an unfiltered `.endsWith('.json')` listing of
    the same directory returns three — the explicit before/after that shows the fix is load-bearing.
    Add a case for an unreadable/absent directory returning an empty list without throwing.

    Then run the CLI cross-check on the primary checkout: `node scripts/compute-pace-residual.mjs`,
    capture its `Archive size scanned:` line, and compare it against
    `find data/streams -name '*.json' ! -name manifest.json | wc -l`. They must be equal. Record both
    numbers and the pre-fix number (the same `find` without the `! -name manifest.json` exclusion) in
    the SUMMARY, so the off-by-one G-03 named is quantified.

    The CLI run WILL rewrite `26-RESIDUAL.md`. That file belongs to the regeneration plan, not this
    one: after capturing the numbers, `git checkout -- ` the artifact so this plan's diff stays inside
    its declared `files_modified`, and say so in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run scripts/compute-pace-residual.test.mjs -t "manifest" 2>&1 | tee /tmp/31-06-t2.log; grep -qE "Tests +[1-9][0-9]* passed" /tmp/31-06-t2.log && ! grep -qE "[1-9][0-9]* failed" /tmp/31-06-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/compute-pace-residual.test.mjs -t "manifest"` reports at least 2 passed and 0 failed; the count is recorded in `31-VALIDATION.md`.
    - The test asserts the unfiltered listing returns exactly one more entry than the filtered one, so the fix's effect is demonstrated.
    - `node scripts/compute-pace-residual.mjs`'s `Archive size scanned:` value equals `find data/streams -name '*.json' ! -name manifest.json | wc -l`; both numbers, and the pre-fix number, are recorded verbatim in the SUMMARY.
    - `npx vitest run scripts/compute-pace-residual.test.mjs` fully green.
    - `git status --porcelain` after the task lists only this plan's `files_modified` — `26-RESIDUAL.md` was reverted after the measurement, and that revert is stated in the SUMMARY.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>The manifest exclusion is proven by a unit test and confirmed against the live archive count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/streams/` directory listing → a generator's denominator → a committed artifact of record → a requirement's prose | A miscount at the listing step propagates silently into a figure a human later cites as evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Repudiation | a generated artifact whose denominator is wrong at the source, so any hand-correction to its prose reverts on the next run | mitigate | Fix the generator, not the prose (D-11); prove the exclusion with a unit test at the seam and cross-check the live count against an independently computed `find` |
| T-31-20 | Tampering | two copies of `isStreamFile` drifting apart, reopening G-01/G-03 in one generator only | mitigate | Single owner in `scripts/lib/stream-files.mjs`; greps assert zero local definitions remain in the calibration script |
| T-31-21 | Tampering | a new import edge dragging `execSync`/`REGENERATE_COMMAND` module-scope machinery into the residual generator | mitigate | The shared module has no top-level side effects, asserted by a clean-tree import check; the extraction precedent and rationale are recorded in its header |
| T-31-22 | Repudiation | this plan silently committing a regenerated `26-RESIDUAL.md` out of the regeneration pass's dependency order | mitigate | The CLI measurement's artifact write is reverted with `git checkout --` and the revert is recorded; `git status --porcelain` is asserted clean of it |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `npm test` exits 0 with zero failures.
- `-t "manifest"` match count recorded (expect ≥2).
- `Archive size scanned:` equals the independently computed `find` count; both recorded.
- `git status --porcelain` lists only this plan's `files_modified`.
</verification>

<success_criteria>
- `isStreamFile` has exactly one definition in the repo, in `scripts/lib/`.
- The residual sweep excludes `manifest.json`, proven at unit level and against the live archive.
- No artifact of record was regenerated or committed by this plan.
</success_criteria>

<output>
Create `.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-06-SUMMARY.md` when done.
Record: the three counts (pre-fix listing, post-fix listing, `Archive size scanned:`), the `-t
"manifest"` match count, the shared module's header docblock, and the statement that `26-RESIDUAL.md`
was reverted rather than committed.

**TD-05 tick rule:** do NOT tick TD-05 here. TD-05 spans 31-06, 31-07, 31-08, 31-09 and 31-10 and
ticks only in 31-10, after the PR-04 Round 4 verdict. Leave it unticked and record that.
</output>
