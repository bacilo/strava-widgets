---
phase: 31-tech-debt-closure-silent-failure-guards-docs-reconciliation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/copy-data-tree.mjs
  - scripts/lib/copy-data-tree.test.mjs
autonomous: true
requirements: [TD-02]

must_haves:
  decisions: [D-04, D-05, D-06]
  truths:
    - "D-04: a locally edited dist/widgets/data/*.json file can no longer survive a build-widgets run, whatever its mtime says"
    - "D-04: a genuinely unchanged file is still skipped, so the local rebuild cost does not regress"
    - "D-05: when a same-size, different-content destination is overwritten, the build prints the destination path so the next checkpoint can quote it"
    - "D-06: the old mtime rule is demonstrated to have skipped the doctored file, and the new rule to replace it"
  artifacts:
    - path: "scripts/lib/copy-data-tree.mjs"
      provides: "size-then-digest content comparison replacing the mtime skip, plus the staleness log line"
      contains: "createHash"
    - path: "scripts/lib/copy-data-tree.test.mjs"
      provides: "planted-fixture regression proof in a throwaway mkdtemp tree"
      contains: "mkdtemp"
  key_links:
    - from: "scripts/build-widgets.mjs and scripts/curate-server.mjs"
      to: "copyJsonTree"
      via: "both call sites inherit the new behaviour with no caller change"
      pattern: "copyJsonTree"
---

<objective>
Replace `copyJsonTree`'s mtime skip with a size-then-digest content comparison and log every stale
replacement by destination path (TD-02, 26 deferred, D-04/D-05/D-06).

Purpose: this project's own memory records the trap — `build-widgets` silently no-ops on a locally
edited `dist/` file and reports success, so a checkpoint can be run against bytes nobody built. The
guard being removed saved under two seconds on the primary checkout (measured: 7,580 files / 186 MB,
SHA-1 pass ~1.4 s vs unconditional copy ~1.7 s, size-only stat ~16 ms).

Output: a content-honest `copyJsonTree` and the file's first test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-CONTEXT.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-RESEARCH.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-PATTERNS.md
@.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-VALIDATION.md

<interfaces>
<!-- Established facts. Do not rediscover. -->

| fact | value | source |
|---|---|---|
| call sites | TWO, not one: `scripts/build-widgets.mjs:233` and `scripts/curate-server.mjs:691` — so the log line and the new rule belong INSIDE the helper, never at a caller | RESEARCH § TD-02 |
| module contract | `copy-data-tree.mjs` has NO top-level side effects (imports, function and constant declarations only) so both callers can import it without triggering `build-widgets.mjs`'s self-executing build — adding `import { createHash } from 'node:crypto'` preserves this | file header, Phase 24 |
| digest idiom | `createHash('sha1').update(readFileSync(path)).digest('hex')`, mirroring `scripts/compute-elevation-calibration.mjs:278,286` | PATTERNS § Analog B |
| algorithm choice | SHA-1, because D-04's measured tradeoff is stated for SHA-1; this is a staleness check, not a security boundary, so collision resistance is irrelevant | RESEARCH A1, Security Domain V6 |
| mtime | may never be used as a SKIP condition — a doctored file can be given any mtime, which is exactly the D-06 case | RESEARCH Pattern 2 |
| test pattern | `scripts/lib/curation-guard.test.mjs`'s `fs.mkdtemp` / `afterEach fs.rm` planted-fixture shape | PATTERNS § copy-data-tree.test.mjs |
| vitest collection | `include: ['src/**/*.test.ts', 'scripts/**/*.test.mjs']` — a `.test.ts` under `scripts/` is NEVER collected | vitest.config.ts |

Current logic being replaced (`scripts/lib/copy-data-tree.mjs:37-56`): `if (existsSync(destPath))
{ srcMtime = statSync(srcPath).mtimeMs; destMtime = statSync(destPath).mtimeMs; if (destMtime >=
srcMtime) shouldCopy = false; }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the planted-fixture test and demonstrate the current rule failing</name>
  <files>scripts/lib/copy-data-tree.test.mjs</files>
  <read_first>
    - `scripts/lib/copy-data-tree.mjs` — the whole file (70 lines); the current mtime rule is what
      the first test must be shown skipping.
    - `scripts/lib/curation-guard.test.mjs` lines 1-60 — the mkdtemp setup/teardown and header
      docblock convention to copy exactly.
    - `vitest.config.ts` — confirms `fileParallelism: false` and the `scripts/**/*.test.mjs` include
      pattern.
    - `.planning/phases/31-.../31-PATTERNS.md` § `scripts/lib/copy-data-tree.test.mjs`.
  </read_first>
  <behavior>
    - Planting a destination file that is the SAME SIZE as its source, different in content, and with
      a NEWER mtime, then calling `copyJsonTree(srcDir, destDir)`, leaves the doctored bytes in place
      under the current rule — the test fails RED before Task 2.
    - A destination that is byte-identical to its source is skipped, and `skipped` counts it.
    - A destination that differs in SIZE is copied.
    - A missing destination is copied.
    - Non-`.json` entries are ignored and nested subdirectories still recurse.
  </behavior>
  <action>
    Create `scripts/lib/copy-data-tree.test.mjs` (`.mjs`, never `.test.ts`). Header docblock states
    it is the planted-fixture regression proof for the "staged build browser cache trap" this
    project's memory records, and that it never touches the real `dist/widgets` or `data/` tree.

    `beforeEach`: `fs.mkdtemp(path.join(os.tmpdir(), 'copy-data-tree-'))`, then create `src` and
    `dest` subdirectories inside it. `afterEach`: `fs.rm(tmpDir, { recursive: true, force: true })`.

    Write the doctored-file test with a name containing "stale": write a source JSON file of a known
    byte length; write a destination file of exactly the same byte length but different content
    (choose the doctored content by substituting characters, not by adding them, and assert the two
    sizes are equal with `statSync().size` before proceeding — a test whose premise silently breaks
    is worse than no test); force the destination's mtime newer than the source's with
    `utimesSync`, and assert `statSync(dest).mtimeMs > statSync(src).mtimeMs` so the old rule's skip
    condition is provably satisfied. Then call `copyJsonTree` and assert the destination's bytes now
    equal the source's, and that the returned `copied` count includes it.

    Capture stdout for the log assertion by stubbing `console.log` with `vi.spyOn(console, 'log')`
    inside the test and restoring it afterwards; assert the recorded calls contain a line naming the
    DESTINATION path. (This assertion will also be RED until Task 2.)

    Write the companion tests: a byte-identical destination is skipped and counted in `skipped` with
    a name containing "skip"; a different-size destination is copied; a missing destination is
    copied; a `.txt` sibling is ignored; a nested subdirectory's file is copied and its counts roll
    up into the parent's totals.

    Run the file now and record the RED output verbatim — specifically that the "stale" test fails
    because the doctored bytes survived. This is D-06's demonstrated-failing evidence and must be in
    the SUMMARY. Do not modify `copy-data-tree.mjs` in this task.
  </action>
  <verify>
    <automated>npx vitest run scripts/lib/copy-data-tree.test.mjs 2>&1 | tee /tmp/31-02-red.log; grep -qE "[1-9][0-9]* failed" /tmp/31-02-red.log</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/copy-data-tree.test.mjs` exists and is collected (the run reports a non-zero test count for that file, never "No test files found").
    - The run is RED, and the failing test is the "stale" one; the failure output is recorded verbatim in the SUMMARY as the D-06 demonstrated-failing evidence.
    - The doctored fixture's source and destination sizes are asserted equal inside the test, and the destination's mtime is asserted strictly newer — both before `copyJsonTree` is called.
    - No file under `dist/` or `data/` is created, read or modified: `git status --porcelain` lists only `scripts/lib/copy-data-tree.test.mjs`.
    - The "skip" test passes already (the current rule does skip an identical file), proving the suite is not uniformly red.
  </acceptance_criteria>
  <done>The test file exists, is collected, and fails for exactly the reason TD-02 exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the mtime skip with size-then-digest and log the stale replacement</name>
  <files>scripts/lib/copy-data-tree.mjs</files>
  <read_first>
    - `scripts/lib/copy-data-tree.mjs` — the whole file, including the header docblock whose
      no-top-level-side-effects contract must survive the new import.
    - `scripts/compute-elevation-calibration.mjs` lines 265-291 — the `createHash` idiom to mirror.
    - `scripts/build-widgets.mjs` around line 233 and `scripts/curate-server.mjs` around line 691 —
      read both call sites to confirm neither needs to change.
    - `.planning/phases/31-.../31-CONTEXT.md` § D-04/D-05 — the measured numbers and the exact
      log-line intent.
  </read_first>
  <behavior>
    - Same-size, different-content, newer-mtime destination → replaced, counted in `copied`, and one
      log line naming the destination path is emitted.
    - Byte-identical destination → skipped, counted in `skipped`, no log line.
    - Different-size destination → replaced, no stale log line required (a size difference is
      ordinary staleness, not the silent trap D-05 exists to surface).
    - Missing destination → copied, no log line.
  </behavior>
  <action>
    Add `import { createHash } from 'node:crypto';` and `readFileSync` to the existing `fs` import.
    Replace the mtime block with: if the destination does not exist, copy. Otherwise compare
    `statSync(srcPath).size` against `statSync(destPath).size`; if they differ, copy. If they match,
    compute `createHash('sha1').update(readFileSync(path)).digest('hex')` for both and copy only when
    the digests differ — and in exactly that branch (same size, different digest) emit a single
    `console.log` naming the destination path, worded so the next checkpoint plan can quote it, e.g.
    `replaced stale <destPath>`.

    Do not use mtime as a skip condition anywhere, and do not add a mtime short-circuit that could
    bypass the digest — a doctored file can carry any mtime, which is the whole attack. Keep the
    `copied` / `skipped` counters and their return shape unchanged; they are now honest.

    Rewrite the "Efficiency guard" comment so it states the new rule, cites D-04's measured numbers
    (7,580 files / 186 MB; SHA-1 pass ~1.4 s; unconditional copy ~1.7 s; the replaced guard saved
    under two seconds), and says in one sentence why mtime was abandoned — a locally edited
    `dist/widgets/data/` file could survive a build that reported success. Preserve the header
    docblock's no-top-level-side-effects contract verbatim.

    Neither call site changes. After the unit test is green, run `npm run build-widgets` once on the
    primary checkout and record its `copied`/`skipped` totals and its wall time, so the cost claim is
    measured rather than asserted.
  </action>
  <verify>
    <automated>npx vitest run scripts/lib/copy-data-tree.test.mjs 2>&1 | tee /tmp/31-02-green.log; grep -qE "Tests +[1-9][0-9]* passed" /tmp/31-02-green.log && ! grep -qE "[1-9][0-9]* failed" /tmp/31-02-green.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/lib/copy-data-tree.test.mjs` is fully green, including the "stale" test that was RED in Task 1.
    - `npx vitest run scripts/lib/copy-data-tree.test.mjs -t "skip"` reports at least 1 passed and 0 failed; the count is recorded in `31-VALIDATION.md`.
    - `grep -c "mtimeMs" scripts/lib/copy-data-tree.mjs` returns 0.
    - `grep -c "createHash" scripts/lib/copy-data-tree.mjs` returns at least 1.
    - `grep -n "copyJsonTree" scripts/build-widgets.mjs scripts/curate-server.mjs` shows both call sites unchanged from HEAD (`git diff` for those two files is empty).
    - `npm run build-widgets` exits 0 on the primary checkout; its `copied`/`skipped` totals and wall time are recorded in the SUMMARY, and no `replaced stale` line appears on a clean tree (or, if one does, the path is recorded and explained).
    - `npm test` and `npx tsc --noEmit` both exit 0.
  </acceptance_criteria>
  <done>Content, not mtime, decides the copy; a same-size doctored file is replaced and says so.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → `dist/widgets/data/` → the published site | Anything surviving in the publish directory ships; the copy step is the only thing that refreshes it |
| build log → human reading it | A "success" line is currently not evidence that the bytes were refreshed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | a locally edited or doctored `dist/widgets/data/*.json` surviving `build-widgets` and shipping | mitigate | Replace the mtime skip with size-then-digest in `copyJsonTree`; Task 1 plants a same-size newer-mtime doctored file and proves the old rule kept it, Task 2 proves the new rule replaces it |
| T-31-10 | Repudiation | a silent replacement leaving no record for a later checkpoint to quote | mitigate | D-05's `replaced stale <destPath>` log line, emitted only in the same-size/different-digest branch, asserted by the unit test via a `console.log` spy |
| T-31-11 | Denial of Service | a digest pass making every local rebuild unacceptably slow | accept | Measured on the primary checkout: 7,580 files / 186 MB, SHA-1 ~1.4 s vs unconditional copy ~1.7 s; the size-first short-circuit keeps the common case at stat cost. Task 2 re-measures `build-widgets` wall time and records it |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing; `crypto` is a Node built-in |
</threat_model>

<verification>
- `npx vitest run scripts/lib/copy-data-tree.test.mjs` green; the Task 1 RED output recorded.
- `npm test` and `npx tsc --noEmit` exit 0.
- `npm run build-widgets` exits 0; copied/skipped totals and wall time recorded.
- `git diff scripts/build-widgets.mjs scripts/curate-server.mjs` is empty.
</verification>

<success_criteria>
- A same-size doctored destination cannot survive a copy pass.
- An unchanged file is still skipped and the counts stay honest.
- The replacement is visible in the build output by destination path.
</success_criteria>

<output>
Create `.planning/phases/31-tech-debt-closure-silent-failure-guards-docs-reconciliation/31-02-SUMMARY.md` when done.
Record: the Task 1 RED failure output verbatim, the final green run, the `-t "skip"` match count, the
`build-widgets` copied/skipped totals and wall time, and the exact log-line wording chosen.
TD-02 may be ticked by this plan.
</output>
