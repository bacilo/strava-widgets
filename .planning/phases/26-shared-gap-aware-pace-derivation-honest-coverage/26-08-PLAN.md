---
phase: 26-shared-gap-aware-pace-derivation-honest-coverage
plan: 08
type: execute
wave: 5
depends_on: [26-06, 26-07]
files_modified:
  - src/dashboard/views/list.ts
  - src/dashboard/views/list.test.ts
  - src/dashboard/views/list-logic.test.ts
  - src/dashboard/views/detail.ts
  - src/dashboard/views/detail-sections.ts
  - src/dashboard/views/detail-sections.test.ts
  - src/dashboard/styles.css
  - src/dashboard/styles.test.ts
autonomous: true
requirements: [PACE-07]

must_haves:
  decisions: [D-10, D-11, D-12, D-13]
  truths:
    - "D-10: a flagged activity still displays its metadata-derived pace — 5059204779's Pace stat card still reads 1:53/km — alongside a visible badge naming the disagreement and the stream-derived figure 5:51/km. Nothing is recomputed and nothing is silently substituted."
    - "D-11: the badge appears on both the detail view's Pace stat card and the shared activity row, reaching all four `RowSurface` values through the single `statusBadgeTexts`/`appendStatusBadges` pipeline rather than per-surface duplication."
    - "D-11: the row badge follows `appendLowConfidenceBadge`'s accessible shape — visible text, a `title` for pointer users, and an `aria-describedby`-wired `sr-only` sibling carrying the full explanation."
    - "D-12: flagged activities are not removed from pace sort or filter results — disclosure, not suppression — pinned by a test that fails if a suppression gate is ever added to `sortValueFor` or `matchesFilters`."
    - "D-13: for a flagged activity only, the splits table's `vs. Avg` column diffs against the stream-derived average instead of the disputed metadata average, and the differing baseline is disclosed in a caption note in the same area as the gap legend."
    - "No new colour token, no new colour-scoped CSS class, and no accent usage — the badge reuses the existing neutral `.badge` vocabulary, distinguished by text and never by hue."
  artifacts:
    - path: "src/dashboard/views/list.ts"
      provides: "Pace disputed badge in the single-source row badge pipeline"
      contains: "Pace disputed"
    - path: "src/dashboard/views/detail.ts"
      provides: "Pace stat card badge carrying the stream-derived figure"
      contains: "stream-derived"
  key_links:
    - from: "src/dashboard/views/list.ts"
      to: "DashboardIndexRow.paceDisagreement"
      via: "statusBadgeTexts reads the additive index field"
      pattern: "paceDisagreement"
    - from: "src/dashboard/views/detail-sections.ts"
      to: "buildPaceBarCell"
      via: "the vs. Avg baseline argument rebased for flagged activities"
      pattern: "activityAvgPaceSecPerKm"
---

<objective>
Put PACE-07's disagreement on screen, on every surface where the disputed pace is visible.

Purpose: sorting the Activities list by pace currently ranks 5059204779 as the fastest run in the
archive. A disputed pace must never appear unmarked on the surface where it ranks first. And on the
detail view, every real kilometre of that run currently reads about four minutes slow because the
`vs. Avg` column diffs against the bogus 1:53/km — D-13 rebases that baseline and says so.

Output: the badge on all row surfaces and on the detail Pace stat card, the rebased splits
baseline with its disclosure note, and the tests pinning D-12's no-suppression rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-CONTEXT.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-UI-SPEC.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-PATTERNS.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-06-SUMMARY.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-07-SUMMARY.md
</context>

<interfaces>
From `src/analytics/dashboard-index.types.ts` (shipped by plan 26-07):
`PaceDisagreement { streamPaceSecPerKm: number; metadataPaceSecPerKm: number; ratio: number }` and
the required `paceDisagreement: PaceDisagreement | null` field on `DashboardIndexRow`.

From `src/dashboard/views/list.ts` (read each before editing):
- `export function appendBadge(container: HTMLElement, text: string): void` at line 142 — plain
  neutral badge, `span.className = 'badge'`.
- `export function appendLowConfidenceBadge(container: HTMLElement, idPrefix: string): void` at
  lines 214-233 — the accessible shape to mirror: a local `explanation` string, a description id
  from `lowConfidenceDescriptionId(idPrefix)`, a `.badge` span carrying `textContent`, `title` and
  `aria-describedby`, then an `sr-only` sibling span with that id and the same text.
- `export type RowSurface = 'activity-card' | 'activity-table' | 'overview-prs' | 'overview-activities'`
  and `rowIdPrefix(surface, rowId)` at lines 238-244.
- `export function statusBadgeTexts(row: DashboardIndexRow): string[]` at line 249 — pushes
  no-streams / `'No HR'` / low-confidence / `'Excluded from records'` / `` `${row.prCount} PR` ``.
- `function appendStatusBadges(container, row, idPrefix)` at line 282 — the single dispatch both
  `renderActivityRow` and the desktop `buildTableRow` invoke; it special-cases the low-confidence
  text and otherwise calls `appendBadge`.

From `src/dashboard/views/list-logic.ts` — the two sites D-12 forbids gating:
- `sortValueFor`'s `case 'pace': return row.paceSecPerKm;` at lines 194-195.
- `matchesFilters`'s pace-range block at roughly lines 322-336, which already excludes rows whose
  `paceSecPerKm` is `null` when a pace bound is active.

From `src/dashboard/views/detail.ts`:
- `function buildStatCard(value: string, label: string, titleAttr?: string): HTMLElement` at lines
  145-157 — appends a `.text-display` value div then a `.text-label` label div to a wrapper.
- `statGrid.appendChild(buildStatCard(formatPace(paceSecPerKm), 'Pace'));` at line 617.
- `view.appendChild(buildSplitsSection(splits, paceSecPerKm, gapAnnotations));` at roughly line 677
  as changed by plan 26-06.
- The detail view can read the flagged row via `indexClient.getRow(detail.id)`, the same accessor
  the stream-absent branch already uses for `streams.reason`.

From `src/dashboard/views/detail-sections.ts`:
- `buildPaceBarCell(split, activityAvgPaceSecPerKm)` at lines 74-118 — computes
  `split.paceSecPerKm - activityAvgPaceSecPerKm` and sets an `aria-label` of the form
  `"{sign}{n} sec/km vs. average"`. `activityAvgPaceSecPerKm` is the argument D-13 rebases; it
  arrives from `buildSplitsSection`'s second parameter.
- The conditional footnote pattern at lines 405-444 and the gap legend added by plan 26-06 — the
  D-13 caption note lands in the same area, below the `.splits-scroll` wrapper.

Copy strings, fixed by 26-UI-SPEC.md — use verbatim:
- Detail stat card badge: `Pace disputed — stream-derived {formatted stream pace}`, which for
  5059204779 renders as `Pace disputed — stream-derived 5:51/km`.
- Row badge visible text: `Pace disputed`.
- Row badge title and `sr-only` explanation: `Metadata pace disagrees with the stream-derived pace: {formatted stream pace} measured vs. {formatted metadata pace} shown`.
- D-13 caption note: `Splits above are compared against the stream-derived average ({formatted stream avg}), not the disputed metadata average.`

CSS constraint from 26-UI-SPEC.md: the only stylesheet change permitted is giving the badge inside
a stat tile separation from the label line above it, using the existing `--space-xs` token. No new
token, no new colour, no new colour-scoped class such as `.pace-warning` or `.gap-badge`, and no
`var(--accent)` or `var(--destructive)` usage.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the Pace disputed badge to the single-source row badge pipeline</name>
  <files>src/dashboard/views/list.ts, src/dashboard/views/list.test.ts</files>
  <read_first>
    - `src/dashboard/views/list.ts` — lines 130-300 in full: `appendBadge`, `lowConfidenceDescriptionId`, `appendLowConfidenceBadge`, `RowSurface`, `rowIdPrefix`, `statusBadgeTexts`, `appendStatusBadges`, and both call sites (`renderActivityRow` and the desktop table row builder).
    - `src/dashboard/views/list.test.ts` — how `statusBadgeTexts` is currently exercised and how row fixtures are built, so the new assertions match the file's existing shape.
    - `src/analytics/dashboard-index.types.ts` — the `PaceDisagreement` shape.
  </read_first>
  <action>
In `src/dashboard/views/list.ts`:

Add `export const PACE_DISPUTED_BADGE_TEXT = 'Pace disputed';` beside the existing low-confidence
badge-text constant. Add `export function paceDisputedDescriptionId(idPrefix: string): string`
mirroring `lowConfidenceDescriptionId`'s naming so the two description ids cannot collide on the
same row.

Add `export function appendPaceDisputedBadge(container: HTMLElement, idPrefix: string, disagreement: PaceDisagreement): void`
following `appendLowConfidenceBadge`'s shape exactly: build the explanation string from the copy
contract using the existing `formatPace` helper for both figures, create a `.badge` span with the
visible text, its `title`, and `aria-describedby` pointing at the description id, then append an
`sr-only` sibling span carrying that id and the same explanation text. Do not duplicate
`appendLowConfidenceBadge`'s DOM-building lines by copy-paste into a third place if a shared
private helper is the smaller change — but do not refactor `appendLowConfidenceBadge`'s public
signature, which other surfaces depend on.

Extend `statusBadgeTexts` to push `PACE_DISPUTED_BADGE_TEXT` when `row.paceDisagreement !== null`.
Place it adjacent to the low-confidence push so the badge order stays stable and readable.

Extend `appendStatusBadges`'s dispatch with a branch for `PACE_DISPUTED_BADGE_TEXT` calling
`appendPaceDisputedBadge` with the row's `paceDisagreement`. Because `appendStatusBadges` is the
single call site both `renderActivityRow` and the desktop table-row builder invoke, and `RowSurface`
already enumerates all four surfaces, this one change reaches Activities, the desktop table,
Overview Recent Activities and Overview Recent PRs — D-11's requirement is satisfied without
per-surface duplication. Add a comment saying so, naming the four `RowSurface` values, so a future
reader does not add a second badge path.

In `src/dashboard/views/list.test.ts`, add assertions that `statusBadgeTexts` includes
`'Pace disputed'` for a row whose `paceDisagreement` is a populated object and omits it for a row
whose `paceDisagreement` is `null`, and that `paceDisputedDescriptionId` produces a distinct id
from `lowConfidenceDescriptionId` for the same `idPrefix`. Follow the file's existing stub-based
approach; do not introduce jsdom.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/list.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/views/list.test.ts` exits 0.
    - `grep -c "PACE_DISPUTED_BADGE_TEXT = 'Pace disputed'" src/dashboard/views/list.ts` outputs `1`.
    - `grep -v '^\s*\*' src/dashboard/views/list.ts | grep -c "aria-describedby"` outputs a value of at least 2 — the new badge uses the same accessible wiring as the low-confidence badge.
    - `grep -c "paceDisagreement" src/dashboard/views/list.ts` outputs a value of at least 2 (the text push and the dispatch branch).
    - `grep -c "renderActivityRow\|buildTableRow" src/dashboard/views/list.ts` confirms `appendStatusBadges` remains the single badge entry point — no second badge-append path was added.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The badge reaches all four row surfaces through one pipeline change, with the project's established accessible badge wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Add the detail stat-card badge, rebase the splits baseline, and add the disclosure note</name>
  <files>src/dashboard/views/detail.ts, src/dashboard/views/detail-sections.ts, src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/views/detail.ts` — lines 140-160 (`buildStatCard`) and lines 600-700 (the stat grid, the pace computation at 610-613, the stat card append at 617, the splits call at 677) as changed by plans 26-04 and 26-06.
    - `src/dashboard/views/detail-sections.ts` — `buildPaceBarCell` (lines 74-118), `buildSplitsSection` (line 136 onward), and the gap legend added by plan 26-06, so the D-13 note lands beside it rather than in a new place.
    - `src/dashboard/styles.css` — the `.badge` rule (roughly lines 359-367) and the `.stat-grid > div` rule, to confirm the one-line addition is the minimum change.
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-UI-SPEC.md` § "Color" and § "Spacing Scale" — the no-new-token, no-new-colour constraints and the `--space-xs` note.
  </read_first>
  <action>
**`detail.ts`:** read the flagged row via `indexClient.getRow(detail.id)?.paceDisagreement ?? null`
into a local. Leave the metadata `paceSecPerKm` computation at lines 610-613 completely unchanged
(D-10) — the stat card's `.text-display` value must still render `formatPace(paceSecPerKm)`, which
is `1:53/km` for 5059204779. Capture the element returned by `buildStatCard(formatPace(paceSecPerKm), 'Pace')`
and, when the disagreement is non-null, append a third child: a `.badge` span whose text is the
detail badge copy string, built with `formatPace(disagreement.streamPaceSecPerKm)`. Give it the
same `title` plus `aria-describedby` and `sr-only` sibling treatment the row badge uses, reusing
the helper exported from `list.ts` if its signature fits, rather than writing a fourth badge
builder.

For D-13: when the disagreement is non-null, pass `disagreement.streamPaceSecPerKm` as
`buildSplitsSection`'s `activityAvgPaceSecPerKm` argument instead of the metadata `paceSecPerKm`;
otherwise pass `paceSecPerKm` exactly as today. Also pass the stream-derived average through to
`buildSplitsSection` as a new optional `rebasedAverageDisclosure: string | null` argument, or an
equivalent explicit flag, so the section can render the note without re-deriving anything.

**`detail-sections.ts`:** when the rebase disclosure is present, append a `<p class="text-label">`
carrying the D-13 caption note verbatim, below `.splits-scroll` and adjacent to the gap legend
added by plan 26-06, using the same conditional-append shape as the existing 1k age-grade footnote.
`buildPaceBarCell` itself is not changed — it already diffs against whatever baseline it is handed,
which is precisely why D-13 is a call-site change rather than a logic change.

**`styles.css`:** add exactly one rule giving a `.badge` inside a stat tile `margin-top: var(--space-xs)`,
per UI-SPEC's pinned implementation note. Add nothing else: no new token, no new class, no colour.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stream-derived" src/dashboard/views/detail.ts` outputs a value of at least 1.
    - `grep -c "movingTimeSec / (distanceM / 1000)" src/dashboard/views/detail.ts` outputs `1` — the metadata pace computation is untouched (D-10).
    - `grep -c "formatPace(paceSecPerKm), 'Pace'" src/dashboard/views/detail.ts` outputs `1` — the stat card's big value still shows the metadata pace.
    - `grep -c "not the disputed metadata average" src/dashboard/views/detail-sections.ts` outputs `1`.
    - `grep -c "var(--accent)\|var(--destructive)\|pace-warning\|gap-badge" src/dashboard/styles.css` shows no increase over the pre-plan count for the accent and destructive terms, and outputs `0` for the two class names.
    - `git diff --stat src/dashboard/styles.css` shows a single-rule addition.
    - `npx tsc --noEmit` exits 0 and `npm run build-widgets` exits 0.
  </acceptance_criteria>
  <done>The detail view discloses the disagreement without substituting the metadata value, and the splits comparison is rebased with the differing baseline stated on screen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin D-12's no-suppression rule, the D-13 rebase wiring, and the stylesheet constraint</name>
  <files>src/dashboard/views/list-logic.test.ts, src/dashboard/views/detail-sections.test.ts, src/dashboard/styles.test.ts</files>
  <behavior>
    - D-12, both directions: `sortValueFor(flaggedRow, 'pace')` returns the row's own `paceSecPerKm` unchanged (not `null`, not a substituted stream value), and a flagged row within an active pace range still passes `matchesFilters` — while a row outside the range still fails it, so the test is not vacuous.
    - A flagged row and an identical unflagged row produce the same sort value and the same filter verdict — the flag has zero effect on ranking or inclusion.
    - Source wiring: `list-logic.ts` contains zero references to `paceDisagreement`, proving no suppression gate was added to either site.
    - D-13 wiring: `detail.ts` passes the stream-derived average to `buildSplitsSection` only on the flagged branch, and passes the metadata pace otherwise — asserted by comment-stripped source scan since the DOM builder cannot be invoked in a node environment.
    - The stylesheet gained exactly the one spacing rule and no new colour token or colour-scoped class.
  </behavior>
  <read_first>
    - `src/dashboard/views/list-logic.test.ts` — the existing `sortValueFor` and `matchesFilters` test shapes and row fixtures.
    - `src/dashboard/views/list-logic.ts` — the pace sort case and the pace-range filter block, to confirm neither was touched.
    - `src/dashboard/views/detail-sections.test.ts` as created by plan 26-06 — extend it, do not create a second file.
    - `src/dashboard/styles.test.ts` — the existing rule-assertion helpers so the new stylesheet assertion matches the file's idiom.
    - `src/dashboard/curation-seam.test.ts` — the comment-stripped source-scan idiom for the D-13 wiring assertion.
  </read_first>
  <action>
In `src/dashboard/views/list-logic.test.ts`, add a `describe` block for D-12 asserting the
behaviours above. The critical property is that this test must FAIL if anyone later adds a
`row.paceDisagreement` gate to `sortValueFor` or `matchesFilters` — write the assertion against the
returned values for a flagged row, not against the absence of a string in the source alone, and add
the source scan as a second, corroborating assertion. Include the out-of-range case so the filter
assertion can fail in both directions rather than passing because everything matches.

In `src/dashboard/views/detail-sections.test.ts`, add the D-13 wiring assertions via comment-stripped
source scans of `detail.ts` and `detail-sections.ts`, plus a direct assertion that the caption-note
copy string is present verbatim in `detail-sections.ts`.

In `src/dashboard/styles.test.ts`, add an assertion that a `.badge` inside a stat tile carries
`margin-top: var(--space-xs)`, and an assertion that the stylesheet contains no `.pace-warning` or
`.gap-badge` rule — the anti-pattern UI-SPEC names, checked in both directions by also asserting
the rule the plan did add is found.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/list-logic.test.ts src/dashboard/views/detail-sections.test.ts src/dashboard/styles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/views/list-logic.test.ts src/dashboard/views/detail-sections.test.ts src/dashboard/styles.test.ts` exits 0.
    - `grep -c "paceDisagreement" src/dashboard/views/list-logic.ts` outputs `0` — D-12's rule is the absence of a gate, and the absence is asserted.
    - `npx vitest run src/dashboard/views/list-logic.test.ts -t "pace disputed"` exits 0 and reports at least one passing test.
    - The D-12 test includes an out-of-range row that does NOT match the filter, so the assertion is not vacuous.
    - `npm run test` exits 0.
  </acceptance_criteria>
  <done>Disclosure without suppression is pinned by tests that fail if a suppression gate is ever added, and the stylesheet constraint is enforced in both directions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published `data/dashboard/index.json` → dashboard client | The `paceDisagreement` field arrives from a re-parsed JSON document where no key can be assumed present (`ParsedDashboardIndexRow`) |
| badge text → assistive technology | Meaning must not be carried by colour or glyph alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Denial of Service | `statusBadgeTexts`, `appendStatusBadges`, the detail render | mitigate | The field is read with an explicit `!== null` check and, on the detail side, through `indexClient.getRow(...)?.paceDisagreement ?? null`, so a re-parsed row missing the key renders without a badge instead of throwing |
| T-26-16 | Repudiation | the disputed pace | mitigate | D-10 is enforced by grep: the stat card's big value still renders the metadata pace and the metadata computation is byte-unchanged, so the badge adds disclosure rather than replacing the shipped figure |
| T-26-17 | Tampering | pace ranking and filtering | mitigate | D-12's no-suppression rule is pinned by a behavioural test on `sortValueFor` and `matchesFilters` plus a source scan asserting `list-logic.ts` never references the flag |
| T-26-13 | Information Disclosure | accessibility | mitigate | The badge carries visible text, a `title`, and an `aria-describedby`-wired `sr-only` explanation, mirroring `appendLowConfidenceBadge`; no colour-only or glyph-only signal is introduced |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed; 26-RESEARCH.md records the gate as not applicable. Any new dependency requires re-running the Package Legitimacy Gate protocol |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run test` exits 0.
- `npm run build-widgets` exits 0.
- `npm run verify-dashboard` exits 0.
- `grep -c "paceDisagreement" src/dashboard/views/list-logic.ts` outputs 0.
</verification>

<success_criteria>
- The `Pace disputed` badge renders on all four row surfaces and on the detail Pace stat card, without substituting the metadata value.
- The splits `vs. Avg` column is rebased for flagged activities only, with the differing baseline disclosed on screen.
- Flagged activities remain fully present in pace sort and filter results, pinned by a two-directional test.
- No new colour, token or colour-scoped class was introduced.
</success_criteria>

<output>
Create `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-08-SUMMARY.md` when done.
Record the exact badge strings as they will render for 5059204779, so plan 26-10's human reads back
against a pre-stated expectation rather than judging plausibility.
</output>
