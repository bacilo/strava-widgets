---
phase: 26-shared-gap-aware-pace-derivation-honest-coverage
plan: 11
type: execute
wave: 7
depends_on: [26-01, 26-02, 26-04, 26-10]
files_modified:
  - src/analytics/pace-derivation.ts
  - src/analytics/pace-derivation.test.ts
autonomous: true
gap_closure: true
requirements: [COV-01]

must_haves:
  decisions: [D-07, D-16]
  truths:
    - "WR-01's three-segment shape (recording-gap / zero-net-advance covered / recording-gap) is settled as REACHABLE by a permanent in-suite test, not carried forward as an open ambiguity."
    - "The documented `sum(timeSec) === coverage.coveredSec` invariant is watched failing on that shape before any replacement claim is trusted."
    - "Covered time is itemised so that `coveredSec === bucketedSec + unbucketedCoveredSec` is an exact, checkable identity (D-07's 'covered + Σ named category' clause applied one layer down), asserted with strict `toBe` on both a constructed fixture and real archive activity 11865310195."
    - "No doc comment in `pace-derivation.ts` states an invariant the implementation does not guarantee."
    - "The archive-wide size of the unbucketed-covered residue is measured and reported as a number, never assumed to be zero."
  artifacts:
    - path: "src/analytics/pace-derivation.ts"
      provides: "unbucketedCoveredSec + paceHistogramAccounting — the itemised covered-time identity WR-01 option (b) specifies"
      contains: "export function unbucketedCoveredSec"
    - path: "src/analytics/pace-derivation.test.ts"
      provides: "Permanent WR-01 reachability + itemised-identity regression tests"
      contains: "zero-net-advance"
  key_links:
    - from: "src/analytics/pace-derivation.ts"
      to: "coverage.coveredSec"
      via: "unbucketedCoveredSec computed from the SAME PaceCoverage the samples were derived under (D-16 — no second, independently-computed coverage)"
      pattern: "unbucketedCoveredSec"
---

<objective>
Adjudicate WR-01 to a definite disposition and make covered-time accounting exactly itemisable.

Purpose: `26-REVIEW.md`'s WR-01 says `paceHistogramSamples`'s documented "INVARIANT: the sum of
every returned `timeSec` equals `coverage.coveredSec` EXACTLY" can be silently violated by an
isolated zero-net-advance `covered` segment flanked by two gaps. The verifier tried to construct
that shape and could not (their flat run classified `pause`, not `covered`), so
`26-VERIFICATION.md` carried it forward as unsettled *in both directions* — and COV-01 is held
open in `.planning/REQUIREMENTS.md` waiting on that adjudication. An invariant that is documented
but not guaranteed is exactly the "correct internally, false in the doc comment" failure COV-01
exists to forbid, and the caption the reader sees in plan 26-12 will quote a number derived from
this accounting.

Output: a permanent test that settles the reachability question with an assertion rather than a
comment; an itemised identity (`coveredSec === bucketedSec + unbucketedCoveredSec`) that holds
unconditionally; a corrected doc comment; and a measured archive-wide count of the residue.

Scoping note (deliberate, so it does not read as an omission): the WR-01 shape is constructed
inline in `pace-derivation.test.ts` via `makeStream`, NOT added to `pace-fixtures.ts`. ERA-03 is
already Complete with a green 34/34 suite; adding a name to `PACE_FIXTURE_NAMES` also requires
updating `loadPinnedStream`'s resolver, and the risk of destabilising a delivered requirement is
not worth it for a four-sample synthetic shape that costs one line to construct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-CONTEXT.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-REVIEW.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VERIFICATION.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-INTEGRATION-FIX.md
</context>

<interfaces>
<!-- Measured by the planner against the live tree on 2026-09-09 by calling the real
     compiled functions. These are facts about HEAD, not predictions. The executor must
     re-derive them (the archive grows nightly) but should not need to discover them. -->

**WR-01's shape IS reachable.** Constructed with `makeStream({ t: [0, 12, 14, 26], d: [0, 30, 30, 60] })`:

| Quantity | Value at HEAD |
|----------|---------------|
| `advanceIntervals(t, d)` | `[12, 14]` → p90 quantile → pause threshold `5 × p90` ≫ 2, so the middle segment is NOT a pause |
| `classifyGaps(t, d)` | `{ spanSec: 26, coveredSec: 2, recordingGapSec: 24, pauseSec: 0, gapIntervals: [{startSec:0,endSec:12,kind:'recording-gap'}, {startSec:14,endSec:26,kind:'recording-gap'}] }` |
| middle segment `[12, 14]` | classified **`covered`** — this is the reachability question, and the answer is yes |
| `adaptiveWindowSec(t, d)` | `34.5` |
| `derivePaceSeriesGapAware(...)` | `[null, null, null, null]` — sample index 1's window clamps to exactly `[12, 14]`, `metres = 0`, so `null` |
| `paceHistogramSamples(t, paceSeries, gapIntervals)` | `[]` — `Σ timeSec = 0` |
| **Documented invariant** | `0 === coveredSec (2)` → **FALSE**. WR-01 is a real defect, not a hypothetical. |

**The same shortfall on real archive data.** `data/streams/11865310195.json` carries
`t = [0, 1, 2, 14, 17, 18]`, `d = [0, 0, 0, 0, 0, 0]`:

| Quantity | Value at HEAD |
|----------|---------------|
| `classifyGaps` | `{ spanSec: 18, coveredSec: 6, recordingGapSec: 12, pauseSec: 0, gapIntervals: [{startSec:2,endSec:14,kind:'recording-gap'}] }` |
| `adaptiveWindowSec` | `20` (floor — `advanceIntervals` is empty) |
| `paceSeries` | six `null`s |
| `paceHistogramSamples` | `[]` — `Σ timeSec = 0` against `coveredSec = 6` |

**Existing call sites of `paceHistogramSamples` that must keep working unchanged** (its
signature does not change in this plan):
- `src/dashboard/views/detail-zones.ts` — `computePaceDistribution`
- `scripts/compute-pace-residual.mjs` — imports from `dist/analytics/pace-derivation.js`
- `src/analytics/pace-derivation.test.ts` — the existing
  `describe('paceHistogramSamples — exact coverage invariant …')` block (must stay green;
  its two fixtures genuinely satisfy the identity)

**Test-file helpers already available in `pace-derivation.test.ts`** — use these, do not add new ones:
`makeStream({ t, d })` (imported from `./pace-fixtures.js`), `readStream(activityId)` (local,
reads `data/streams/<id>.json` via `node:fs`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Stage WR-01's shape and watch the documented invariant fail</name>
  <files>src/analytics/pace-derivation.test.ts</files>
  <read_first>
    - `src/analytics/pace-derivation.test.ts` — the whole file, but especially lines 1-60 (the header's demonstrated-failing discipline, the import list, `readStream`) and lines 460-511 (the existing `describe('paceHistogramSamples — exact coverage invariant …')` block whose claim this task tests at its edges).
    - `src/analytics/pace-derivation.ts` lines 151-262 (`computeFlatRunDurations`, `classifyGaps`, the strict-priority rule and the scale-relative pause threshold) and lines 363-511 (`derivePaceSeriesGapAware`'s window clamping, `paceHistogramSamples` and its INVARIANT doc comment).
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-REVIEW.md` § "WR-01" — the traced mechanism and its two proposed fixes (a) and (b); this plan takes (b).
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VERIFICATION.md` — the "WR-01" paragraph in § "Anti-Patterns Found" recording that the verifier could not reach the shape.
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VALIDATION.md` § "Demonstrated-Failing Cases (Nyquist negative staging)" — the table this task adds a ninth row's worth of evidence for.
  </read_first>
  <action>
Add one new `describe` block to `src/analytics/pace-derivation.test.ts`, titled
`'WR-01 — zero-net-advance covered segment flanked by recording gaps'`, placed immediately after
the existing `describe('paceHistogramSamples — exact coverage invariant …')` block so the two read
as a pair.

Write three tests in it.

**Test 1 — reachability, settled with an assertion, not a comment.** Build the shape with
`makeStream({ t: [0, 12, 14, 26], d: [0, 30, 30, 60] })`. Assert with strict `toBe` that
`classifyGaps` returns `spanSec` 26, `coveredSec` 2, `recordingGapSec` 24, `pauseSec` 0, and that
`gapIntervals` has exactly two entries, `{ startSec: 0, endSec: 12, kind: 'recording-gap' }` and
`{ startSec: 14, endSec: 26, kind: 'recording-gap' }`. The `coveredSec === 2` assertion IS the
reachability claim — it says the middle `[12, 14]` segment is classified `covered` and not
`pause`, which is precisely what the verifier could not reach. Add a comment in the test body
naming why the pause branch does not fire here (`advanceIntervals` is `[12, 14]`, so the
scale-relative threshold `5 × p90` is far above this 2-second flat run), so a future reader who
changes `PAUSE_GAP_P90_MULTIPLIER` understands what this test is pinning. This test PASSES against
current code — it is the premise, not the failure.

**Test 2 — the documented invariant, asserted exactly as `paceHistogramSamples`'s doc comment
states it today.** From the same stream, take `derivePaceWithCoverage`, then
`paceHistogramSamples(stream.t, result.paceSeries, result.coverage.gapIntervals)`, sum the
`timeSec` values, and assert `expect(totalWeightedSec).toBe(result.coverage.coveredSec)`. **This
assertion is expected to FAIL** — `0` against `2`. It is the demonstrated-failing negative case
for this plan, mirroring the discipline the file header already records for
`brokenCoverageFromDdSkip` and the absolute-pause-rule case.

**Test 3 — the same shortfall against real committed archive data.** Use `readStream('11865310195')`
(do NOT substitute a synthetic look-alike; the named real input is the point — a convenient
look-alike fixture has inverted a severity call on this project before). Assert `coveredSec` is 6,
`recordingGapSec` is 12, `spanSec` is 18, then assert the same documented invariant
`totalWeightedSec === coverage.coveredSec`. **This is also expected to FAIL** — `0` against `6`.
Guard the archive read the way the file's existing archive-reading tests do, and if the stream file
is absent, fail loudly with the id rather than skipping.

Then run `npx vitest run src/analytics/pace-derivation.test.ts` and **observe the two failures**.
Capture the verbatim vitest failure output for both (the `expected … to be …` lines with their
test titles) into this plan's SUMMARY.md under a "Demonstrated failing" heading — that transcript
is the evidence WR-01 is real, and it is the reason COV-01 can be closed later rather than left
ambiguous.

**Do not run the full phase gate and do not commit at the end of this task.** The tree is
deliberately red here. Task 2 turns it green, and both tasks land in one commit.
  </action>
  <verify>
    <automated>npx vitest run src/analytics/pace-derivation.test.ts 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analytics/pace-derivation.test.ts` reports **exactly 2 failing tests**, both inside the new `WR-01 — zero-net-advance covered segment flanked by recording gaps` describe block. Any other test failing means something unrelated broke and must be resolved before proceeding.
    - The two failure messages are `expected 0 to be 2` (synthetic shape) and `expected 0 to be 6` (activity `11865310195`), or the same numbers in whichever order vitest prints actual/expected. Both verbatim strings are quoted in SUMMARY.md.
    - Test 1 (the `classifyGaps` reachability assertions) PASSES — proving the middle segment really is classified `covered`, so the two failures are the invariant's fault and not a broken fixture.
    - `grep -c "zero-net-advance" src/analytics/pace-derivation.test.ts` outputs a value of at least 1.
    - `grep -c "11865310195" src/analytics/pace-derivation.test.ts` outputs a value of at least 1 — the real named input is used, not only a synthetic stand-in.
    - No file other than `src/analytics/pace-derivation.test.ts` is modified by this task (`git status --porcelain src scripts` lists only that path, plus any pre-existing unrelated modification).
  </acceptance_criteria>
  <done>WR-01's shape is constructed, proven to classify as `covered`, and the documented invariant is observed failing against it twice — once synthetically, once on real committed archive data — with both failures transcribed verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Itemise the unbucketed covered time and correct the invariant's statement</name>
  <files>src/analytics/pace-derivation.ts, src/analytics/pace-derivation.test.ts</files>
  <read_first>
    - `src/analytics/pace-derivation.ts` lines 455-511 — `derivePaceWithCoverage`'s return shape and `paceHistogramSamples`'s full doc comment and body (the text being corrected and the function being wrapped).
    - `src/analytics/pace-derivation.ts` lines 1-60 — the module header's purity constraints (D-15: no `node:fs`, no `document`, no `fetch`) that any new export must respect.
    - `src/analytics/pace-derivation.test.ts` — the block added in Task 1 and the existing exact-coverage block above it.
    - `src/dashboard/views/detail-zones.ts` lines 195-225 — `computePaceDistribution`, the existing three-argument caller whose behaviour must not change.
    - `scripts/compute-pace-residual.mjs` lines 25-40 — the `dist/` import that must keep resolving.
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-CONTEXT.md` § "Coverage accounting and its denominator (COV-01)" — D-06 and D-07 verbatim; the identity added here is D-07's "covered + Σ(excluded by named category)" applied one layer down, inside `covered` itself.
  </read_first>
  <action>
**Add two exports to `src/analytics/pace-derivation.ts`**, placed directly after
`paceHistogramSamples` so they read as that function's accounting companion.

`unbucketedCoveredSec(coverage: PaceCoverage, bucketedTimesSec: readonly number[]): number` —
returns `coverage.coveredSec` minus the sum of `bucketedTimesSec`. **Do not clamp the result at
zero.** A negative return would mean bucketed time exceeded covered time, which is a different and
worse defect than the one being fixed, and clamping would hide it. Document that explicitly in the
doc comment. This is the one definition of the quantity; plan 26-12 imports this exact function
rather than recomputing the subtraction in the dashboard layer (PACE-01's single-derivation rule
applies to this accounting the same way it applies to pace itself).

`paceHistogramAccounting(t: readonly number[], paceSeries: readonly (number | null)[], coverage: PaceCoverage): { samples: Array<{ paceSecPerKm: number; timeSec: number }>; bucketedSec: number; unbucketedCoveredSec: number }` —
calls `paceHistogramSamples(t, paceSeries, coverage.gapIntervals)`, sums the sample `timeSec`
values into `bucketedSec`, and computes `unbucketedCoveredSec` by calling the helper above with
the sample times. Takes the whole `PaceCoverage` (not a bare `gapIntervals` array) precisely so
the itemisation cannot be computed against a different coverage object than the samples were
derived under — the same D-16 structural argument that makes `derivePaceWithCoverage` return both
halves together. Its doc comment states the identity it guarantees:
`coverage.coveredSec === bucketedSec + unbucketedCoveredSec`, unconditionally and by construction.

**Do not change `paceHistogramSamples`'s signature or body.** Its three existing call sites stay
untouched, and `computePaceDistribution` is not modified by this plan.

**Correct `paceHistogramSamples`'s doc comment.** The sentence beginning
`INVARIANT (fixed 2026-09-08, cross-plan integration repair): the sum of every returned timeSec
equals coverage.coveredSec EXACTLY` is false as written — Task 1 demonstrated it failing twice.
Replace that claim with an accurate one: the sum equals `coveredSec` MINUS the covered time whose
own averaging window resolved to a null pace, and the exact, unconditional identity now lives in
`paceHistogramAccounting`. Keep every other paragraph of that comment (the pre-gap-sample
mechanism and the reason `gapIntervals` is a required argument are still correct and still
load-bearing). Name WR-01 and the reproducing shape `t = [0, 12, 14, 26], d = [0, 30, 30, 60]` in
the corrected text so a future reader can re-derive the claim rather than trust it. The word
`EXACTLY` must not survive anywhere in the file — its only occurrence at HEAD is in this false
claim.

**Turn Task 1's two failing assertions into the corrected identity.** In each of the two tests,
replace `expect(totalWeightedSec).toBe(coverage.coveredSec)` with assertions on
`paceHistogramAccounting`:
- synthetic shape: `bucketedSec` is `0`, `unbucketedCoveredSec` is `2`, and
  `coverage.coveredSec` `toBe` `bucketedSec + unbucketedCoveredSec` (strict `toBe`, not
  `toBeCloseTo` — this file's established discipline for the sum identity).
- activity `11865310195`: `bucketedSec` is `0`, `unbucketedCoveredSec` is `6`, and the same
  identity assertion.
Keep in both tests an explicit assertion recording WHY the old claim was wrong — that
`Σ samples.timeSec` does NOT equal `coverage.coveredSec` on this shape
(`expect(bucketedSec).not.toBe(coverage.coveredSec)`). Without it a future edit could restore the
old, false invariant and this suite would not notice.

Add one more test in the same block: `unbucketedCoveredSec` is never negative for either fixture
(`toBeGreaterThanOrEqual(0)`), so the deliberate no-clamp decision is pinned as a checked property
rather than an unverified comment.

Finally, re-read the existing `describe('paceHistogramSamples — exact coverage invariant …')`
block's title and header comment. Its two fixtures genuinely satisfy the identity, so its
assertions stay exactly as they are — but if its prose asserts the invariant holds universally,
narrow that prose to the fixtures it actually covers and point at the new block for the general
case. Do not weaken or delete any assertion in it.

Run the full gate: `npm run test`, then `npx tsc --noEmit`, then
`npx vitest run src/analytics/pace-single-source.test.ts` specifically (the new exports must not
introduce a second `dt/dd` implementation — they contain no pace arithmetic at all, but PACE-01's
audit is the proof, not the intent). Commit Task 1 and Task 2 together once green.
  </action>
  <verify>
    <automated>npm run test &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits 0 with zero failing tests, and `npx tsc --noEmit` exits 0.
    - `grep -v '^\s*\*' src/analytics/pace-derivation.ts | grep -c "export function unbucketedCoveredSec\|export function paceHistogramAccounting"` outputs `2`.
    - `grep -c "EXACTLY" src/analytics/pace-derivation.ts` outputs `0` — the false invariant sentence is gone (it is the file's only occurrence at HEAD; confirm with `git show HEAD:src/analytics/pace-derivation.ts | grep -c EXACTLY` outputting `1` before the edit).
    - `grep -c "WR-01" src/analytics/pace-derivation.ts` outputs a value of at least 1 — the corrected comment names the finding it answers.
    - `node -e "const {classifyGaps,derivePaceSeriesGapAware,adaptiveWindowSec,paceHistogramAccounting}=await import('./dist/analytics/pace-derivation.js'); const fs=await import('node:fs'); const s=JSON.parse(fs.readFileSync('data/streams/11865310195.json','utf8')); const c=classifyGaps(s.t,s.d); const ps=derivePaceSeriesGapAware(s.t,s.d,{windowSec:adaptiveWindowSec(s.t,s.d),gapIntervals:c.gapIntervals}); const a=paceHistogramAccounting(s.t,ps,c); console.log(c.coveredSec===a.bucketedSec+a.unbucketedCoveredSec, c.coveredSec, a.bucketedSec, a.unbucketedCoveredSec)" --input-type=module` prints `true 6 0 6` (run `npm run build` first).
    - `npx vitest run src/analytics/pace-single-source.test.ts` exits 0 — PACE-01's audit still finds one derivation.
    - `git diff --stat src/dashboard scripts` is empty — `paceHistogramSamples`'s callers were not touched.
  </acceptance_criteria>
  <done>`coveredSec === bucketedSec + unbucketedCoveredSec` is an exact, unconditionally-true, test-asserted identity; `paceHistogramSamples`'s doc comment states only what the code guarantees; the suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the archive-wide size of the unbucketed-covered residue</name>
  <files>src/analytics/pace-derivation.test.ts</files>
  <read_first>
    - `scripts/compute-pace-residual.mjs` lines 25-60 and lines 380-395 — the `dist/analytics/pace-derivation.js` import idiom and the `import.meta.url === pathToFileURL(process.argv[1])` main guard this project uses for archive sweeps; the throwaway script written here mirrors that import style.
    - `src/analytics/pace-derivation.ts` — `paceHistogramAccounting` as landed in Task 2.
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-RESIDUAL.md` — the report format PACE-06 established, for the tone of the number this task reports (a measured count, stated plainly, not smoothed).
  </read_first>
  <action>
The phase goal requires residue to be "identified and quantified rather than smoothed into
plausibility." Task 2 made the shortfall itemisable; this task measures how big it actually is
across the committed archive, so the answer is a number rather than an assumption.

Run `npm run build`, then write a **throwaway, uncommitted** node script (put it under the session
scratch directory, not in `scripts/`) that imports `classifyGaps`, `derivePaceSeriesGapAware`,
`adaptiveWindowSec` and `paceHistogramAccounting` from `dist/analytics/pace-derivation.js`, walks
every `data/streams/*.json`, and reports:

1. the total number of streams scanned;
2. the count of activities where `unbucketedCoveredSec > 0`;
3. of those, the count where `bucketedSec > 0` as well — i.e. the partial-shortfall case, where a
   histogram DOES render bars but the caption's covered percentage overstates what those bars sum
   to. This is the number that decides whether the plan-26-12 explanatory note needs to appear
   alongside bars or only when bars are absent;
4. the maximum `unbucketedCoveredSec` observed and the activity id carrying it;
5. the maximum `unbucketedCoveredSec / coveredSec` ratio observed and the activity id carrying it;
6. an explicit assertion, checked in the script, that `coveredSec === bucketedSec +
   unbucketedCoveredSec` holds for **every** activity scanned, with the script printing a loud
   failure naming the offending id if it does not.

Record all six figures verbatim in this plan's SUMMARY.md under a "Archive-wide unbucketed-covered
residue" heading, alongside the scan date and the stream count (the archive grows nightly, so the
count dates the measurement).

Then pin the finding permanently: add one test to the WR-01 describe block asserting the identity
holds for a small named sample of real archive activities read via `readStream` — include
`11865310195` (the pathological case) and `4556693525` (the pinned worked exemplar, where the
identity should hold with `unbucketedCoveredSec === 0` or a small value; use whatever Task 3's
sweep actually measured, and if the measured value is not zero, assert the measured value rather
than adjusting the expectation to a rounder number). Changing an expectation to make this test
pass is a correctness regression, per this file's header discipline.

Do not commit the throwaway script. Do not modify `26-RESIDUAL.md` — it is PACE-06's artifact,
covers a different residue (fast mass under the adaptive window), and already has an unrelated
working-tree modification from the verifier's regeneration run.
  </action>
  <verify>
    <automated>npm run test &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY.md contains all six reported figures, each as a concrete number (or explicit `0`), with the scan date and total stream count.
    - The sweep script's own identity check reports zero violations across every scanned stream; if it reports any, the id is named in SUMMARY.md and treated as a finding rather than absorbed.
    - `grep -c "4556693525" src/analytics/pace-derivation.test.ts` outputs a value of at least 1 and the new sample test asserts the identity for both named real activities.
    - `npm run test` exits 0 and `npx tsc --noEmit` exits 0.
    - `git status --porcelain scripts` shows no new file — the sweep script was throwaway.
    - SUMMARY.md states, in one sentence, whether figure (3) is zero, because plan 26-12 reads that answer to decide whether its explanatory note must also render when bars are present.
  </acceptance_criteria>
  <done>The unbucketed-covered residue is a measured, reported number across the whole committed archive, the exactness identity is verified to hold on every stream in it, and the partial-shortfall count is stated for plan 26-12 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed archive → derivation | `data/streams/*.json` is read as trusted local input; malformed content must degrade, never throw (T-26-01, already mitigated by `validateStreamSeries`) |
| derivation → reader | a stated numeric identity is an assertion of fact to the reader; a false one is the threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-19 | Repudiation | `paceHistogramSamples` doc comment | mitigate | The false `EXACTLY` invariant is removed and replaced by a claim the code guarantees; `grep -c "EXACTLY"` returning 0 is an acceptance criterion |
| T-26-20 | Information disclosure (by omission) | covered time dropped for a null pace | mitigate | `unbucketedCoveredSec` itemises it as a named quantity rather than letting it vanish between `coveredSec` and the bars |
| T-26-21 | Tampering | `unbucketedCoveredSec` clamping | accept-with-control | Deliberately unclamped so a negative (bucketed > covered) surfaces rather than being masked; pinned by a `toBeGreaterThanOrEqual(0)` test on both fixtures |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages — `package.json` is not modified |
</threat_model>

<verification>
- `npm run test` exits 0; `npx tsc --noEmit` exits 0.
- `npx vitest run src/analytics/pace-single-source.test.ts` exits 0 (PACE-01 unbroken).
- `npx vitest run src/analytics/pace-fixtures.test.ts` exits 0 (ERA-03 untouched — this plan adds no fixture name).
- `grep -c "EXACTLY" src/analytics/pace-derivation.ts` outputs `0`.
- SUMMARY.md quotes the two verbatim RED failures from Task 1 and the six sweep figures from Task 3.
</verification>

<success_criteria>
WR-01 is settled: the shape is reachable, proven so by a passing `classifyGaps` assertion; the
old invariant was watched failing against it; the replacement identity `coveredSec === bucketedSec
+ unbucketedCoveredSec` is exact, asserted with `toBe`, and verified to hold on every stream in
the committed archive; and the residue's size is a reported number. COV-01's held-open
adjudication has its answer — the requirement itself is closed in plan 26-13, after the browser
checkpoint.
</success_criteria>

<output>
Create `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-11-SUMMARY.md` when done.
</output>
