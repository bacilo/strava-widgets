---
phase: 26-shared-gap-aware-pace-derivation-honest-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analytics/pace-fixtures.ts
  - src/analytics/pace-fixtures.test.ts
autonomous: true
requirements: [ERA-03]

must_haves:
  decisions: [D-20]
  truths:
    - "D-20: `src/analytics/pace-fixtures.ts` exists and exports the ERA-03 fixture library as named exports, so Phases 27-30 import them rather than rebuilding them."
    - "D-20: the library is stratified across fēnix 6 Pro, Suunto 9, GPX, intervals.icu-only and no-device-name cases, and includes by construction a decimation-aliased stream, a recording gap, a multi-hour pause, an impossible-speed sample and the pinned worked example 4556693525."
    - "D-20: pinned real-archive fixtures are read from `data/streams/` and `data/activities/` via `node:fs`, never from the derived, gitignored `data/stats/`."
    - "D-20: the multi-hour pause fixture is synthetic and says so — 26-RESEARCH.md Pitfall 4 records an exhaustive archive scan finding no real densely-sampled pause longer than 10.4 minutes."
    - "The fixture module is test-layer only: no file under `src/dashboard/` or `src/widgets/` imports it, so its `node:fs` use never reaches the browser bundle."
  artifacts:
    - path: "src/analytics/pace-fixtures.ts"
      provides: "Stratified, named, reusable pace fixture library for Phases 26-30"
      exports: ["PACE_FIXTURE_NAMES", "PINNED_FIXTURES", "loadPinnedStream", "syntheticMultiHourPauseStream", "syntheticRecordingGapStream", "syntheticIntervalSessionStream", "syntheticMultiCategoryCoverageStream"]
      min_lines: 150
    - path: "src/analytics/pace-fixtures.test.ts"
      provides: "Criterion 6's each-fixture-present-by-name assertions"
      min_lines: 60
  key_links:
    - from: "src/analytics/pace-fixtures.ts"
      to: "data/streams/"
      via: "node:fs readFileSync in the pinned-fixture loader"
      pattern: "readFileSync.*data/streams"
    - from: "src/analytics/pace-fixtures.test.ts"
      to: "PACE_FIXTURE_NAMES"
      via: "presence-by-name assertion over the exported name list"
      pattern: "PACE_FIXTURE_NAMES"
---

<objective>
Build the ERA-03 stratified fixture library that Phases 27-30 compose rather than rebuild.

Purpose: Roadmap Criterion 6 requires each named fixture to be verified present *by name* in the
test suite, and 26-RESEARCH.md established that one required case — a multi-hour densely-sampled
pause — does not exist anywhere in the committed archive and must be synthetic. This plan settles
both, with concrete activity IDs already located against the live archive.

Output: `src/analytics/pace-fixtures.ts` and `src/analytics/pace-fixtures.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-CONTEXT.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-RESEARCH.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-PATTERNS.md
</context>

<interfaces>
<!-- Contracts and pinned IDs. The IDs below were located against the live committed archive
     during planning; re-verify each file exists and re-derive the stated property, and treat a
     mismatch as a finding to record, not a value to silently replace. -->

From `src/streams/stream.types.ts`:
`CanonicalStream` = `{ schemaVersion: 1; id: string; source: 'fit'|'gpx'|'intervals'; distanceSource: 'native'|'geo'; sampleCount: number; channels: StreamChannels; t: number[]; d: number[]; hr?: number[]; cadence?: number[]; alt?: number[] }`.
`StreamChannels` = `{ time, distance, hr, cadence, elevation }`, all boolean.

Pinned real-archive candidates located at planning time (activity id → why):

| Fixture name | Activity id | Verified property at planning time |
|---|---|---|
| `fenix-6-pro-fit` | `10041312551` | `device_name` "Garmin fēnix 6 Pro", stream `source: 'fit'`, 939 samples |
| `suunto-9-fit` | `3480808722` | `device_name` "Suunto 9", stream `source: 'fit'`, 1620 samples (same format as fēnix, different family — ERA-01's point) |
| `gpx-source` | `10146303423` | stream `source: 'gpx'`, `device_name` "Strava App", 2545 samples |
| `intervals-icu-only` | `i174284902` | stream `source: 'intervals'`, no `device_name`, 886 samples |
| `no-device-name` | `11183705198` | `device_name` absent, stream `source: 'fit'`, 923 samples |
| `decimation-aliased` | `5059204779` | 96.9% zero-advance samples, span 3788 s, distance 10804 m, max `Δt` 7 s |
| `recording-gap` | `11544429866` | single 127,478 s (35.4 h) recording gap, span 132,212 s |
| `impossible-speed-sample` | `10232917652` | 14.55 m/s between consecutive samples at index 303 (faster than the 100 m world record's 10.44 m/s) |
| `gap-crossing-split` | `10198771331` | 688 s recording gap falling strictly inside km 11 of 11 splits; span 5225 s, distance 11169 m |
| `real-pause` | `3475742397` | the archive's longest real densely-sampled pause, 10.4 min; also Criterion 1's tie-at-zero case |
| `worked-example` | `4556693525` | the pinned PACE-04 exemplar; span 3394 s vs metadata `elapsed_time` 3393, distance 10130 m, 28.7% zero-advance |

`multi-hour-pause` has **no** real candidate and must be synthetic (26-RESEARCH.md Pitfall 4).

From `src/analytics/best-effort-fixtures.test.ts:107-130` — the archive-read pattern to follow:
read `path.join('data/activities', `${activityId}.json`)` and `path.join('data/streams', `${activityId}.json`)` with `readFileSync(..., 'utf-8')` and `JSON.parse`. Never read `data/stats/`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create pace-fixtures.ts with synthetic constructors and pinned real-archive loaders</name>
  <files>src/analytics/pace-fixtures.ts</files>
  <read_first>
    - `src/analytics/best-effort-fixtures.test.ts` — the whole file: its provenance header (lines 1-21), its `FIXTURES` array shape (lines 56-105), and its `node:fs` two-file archive read (lines 107-130). This is the pattern D-20 names by reference.
    - `src/streams/stream.types.ts` — `CanonicalStream` and `StreamChannels` exact field lists.
    - `src/dashboard/views/detail-zones.test.ts` (lines 11-28) — the `makeStream` helper shape to reuse for synthetic constructors.
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-RESEARCH.md` § "Common Pitfalls" Pitfall 4 — why the multi-hour pause is synthetic.
  </read_first>
  <action>
Create `src/analytics/pace-fixtures.ts` with a provenance header in the style of
`best-effort-fixtures.test.ts:1-21`. It must state four things: (a) this is a **test-layer** module
— it uses `node:fs` and must never be imported from `src/dashboard/` or `src/widgets/`, unlike
`pace-derivation.ts` which is pure and client-safe per D-15; (b) synthetic fixtures carry a
hand-derived expected answer because no ground truth exists for real GPS data (ERA-03); (c) pinned
real fixtures read `data/streams/` and `data/activities/` and never the derived, gitignored
`data/stats/`; (d) changing a pinned fixture's stated expected property to make a test pass is a
regression, not a fix.

Export a `makeStream(opts)` helper building a valid `CanonicalStream` from `t`/`d` plus optional
`hr`/`cadence`/`alt`, setting `schemaVersion: 1`, `sampleCount: t.length` and a consistent
`channels` object — model it on `detail-zones.test.ts`'s `makeStream`.

Export these synthetic constructors, each with a doc comment stating its hand-derived expected
answer in seconds and metres:
- `syntheticRecordingGapStream()` — dense 2 s advancing sampling for 200 s, one 300 s jump in `t`
  with no samples across it, dense 2 s advancing sampling for 200 s. Known: exactly 300 s of
  recording gap, span 700 s.
- `syntheticMultiHourPauseStream()` — an otherwise normal short run with dense 2 s sampling
  throughout, in which `d` does not advance for 3 hours (10,800 s) in the middle. Explicitly
  documented as synthetic, citing Pitfall 4's exhaustive negative scan.
- `syntheticMultiCategoryCoverageStream()` — covered + one recording gap + one pause in one stream,
  with each category's exact second count stated in the doc comment; this is the fixture Roadmap
  Criterion 3 calls "a synthetic multi-category fixture".
- `syntheticIntervalSessionStream()` — a constructed interval session (for example 6 repetitions of
  400 m at 3:30/km followed by 200 m at 6:30/km, densely sampled) whose own fast and slow segment
  paces are stated exactly in the doc comment. Roadmap Criterion 5 requires the smoothed output to
  resolve this session's own fast/slow splits to within a stated tolerance; state the intended
  tolerance (±20 sec/km) in the doc comment so plan 26-02's assertion inherits it rather than
  inventing one.
- `syntheticDecimationAliasedStream()` — the alternating full-advance/zero-advance pattern
  `derive-stream.ts`'s index-selection decimation manufactures, with the underlying true pace stated.
- `syntheticImpossibleSpeedStream()` — one sample pair exceeding 10.44 m/s, with the exact
  offending index and speed stated.
- `syntheticStandstillStream()` — a stream whose distance never advances, used to prove the
  derivation returns `null` rather than `0` or `Infinity`.

Export `interface PinnedFixture { name: string; activityId: string; deviceFamily: string; streamSource: 'fit' | 'gpx' | 'intervals'; why: string; expected: Record<string, number | string | null> }` and
`export const PINNED_FIXTURES: readonly PinnedFixture[]` containing one entry per row of the table
in this plan's `<interfaces>` block, with each entry's `expected` carrying the verified property
from that row (sample count, span, distance, max `Δt`, zero-advance fraction, offending index —
whichever the row states).

Export `loadPinnedStream(name: string): CanonicalStream` and
`loadPinnedActivity(name: string): unknown`, both resolving the name through `PINNED_FIXTURES` and
reading via `readFileSync(path.join('data/streams', `${activityId}.json`), 'utf-8')` /
`path.join('data/activities', ...)`. Throw a named, actionable error when the fixture name is
unknown or the file is missing (this module is test-layer, so throwing on a missing fixture is
correct — it is a broken test setup, not untrusted runtime input).

Export `export const PACE_FIXTURE_NAMES: readonly string[]` listing every required name exactly
once: the eleven pinned names from the table plus `multi-hour-pause`, `interval-session`,
`standstill` and `multi-category-coverage`. This constant is what Criterion 6's presence assertion
iterates.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/analytics/pace-fixtures.ts` exists and `npx tsc --noEmit` exits 0.
    - `grep -c "data/stats" src/analytics/pace-fixtures.ts` outputs `0`.
    - `grep -v '^\s*\*' src/analytics/pace-fixtures.ts | grep -c "export const PACE_FIXTURE_NAMES\|export const PINNED_FIXTURES"` outputs `2`.
    - `node -e "const m=require('fs').readFileSync('src/analytics/pace-fixtures.ts','utf8'); for (const id of ['10041312551','3480808722','10146303423','i174284902','11183705198','5059204779','11544429866','10232917652','10198771331','3475742397','4556693525']) if (!m.includes(id)) { console.error('missing '+id); process.exit(1); } console.log('all pinned ids present')"` prints `all pinned ids present`.
    - The `syntheticMultiHourPauseStream` doc comment contains the word `synthetic` and cites the exhaustive-scan finding that no real candidate exists.
    - Each synthetic constructor's doc comment states its hand-derived expected values numerically.
  </acceptance_criteria>
  <done>The fixture library exists as named exports, stratified across five device/source categories, with every synthetic case carrying a stated hand-derived expected answer.</done>
</task>

<task type="auto">
  <name>Task 2: Assert every required fixture is present by name and every pinned archive file resolves</name>
  <files>src/analytics/pace-fixtures.test.ts</files>
  <read_first>
    - `src/analytics/pace-fixtures.ts` (as written in Task 1).
    - `src/analytics/best-effort-fixtures.test.ts` (lines 132-176) — the "coverage guard" `it()` block idiom at the tail of that file, which this test extends from row-counting to naming.
    - `src/dashboard/curation-seam.test.ts` — the `readFileSync` + source-text scanning idiom used for the import-boundary assertion below.
  </read_first>
  <action>
Create `src/analytics/pace-fixtures.test.ts` covering four groups:

1. **Presence by name (Roadmap Criterion 6).** For each entry of `PACE_FIXTURE_NAMES`, assert a
   fixture with that name is resolvable — either present in `PINNED_FIXTURES` or produced by a
   named synthetic constructor. Use `it.each(PACE_FIXTURE_NAMES)` so a deleted fixture fails with
   the missing name in the test title, not as a silent count change. Additionally assert
   `PACE_FIXTURE_NAMES` contains, literally, each of `fenix-6-pro-fit`, `suunto-9-fit`,
   `gpx-source`, `intervals-icu-only`, `no-device-name`, `decimation-aliased`, `recording-gap`,
   `multi-hour-pause`, `impossible-speed-sample` and `worked-example` — the ten categories ERA-03
   and Criterion 6 name explicitly.

2. **Stratification guards.** Assert `PINNED_FIXTURES` spans at least three distinct
   `streamSource` values and at least four distinct `deviceFamily` values, and that
   `no-device-name`'s `deviceFamily` is an explicit own category string (never a fabricated device
   name and never a default fallthrough) — this is the ERA-02 discipline Phase 27 inherits.

3. **Pinned archive files resolve and still match.** For each `PINNED_FIXTURE`, call
   `loadPinnedStream(name)` and assert the returned stream parses, has `t.length === d.length`,
   and satisfies that fixture's stated `expected` properties (span, distance, sample count, max
   `Δt`, zero-advance fraction — whichever the entry declares). A mismatch here means either the
   archive changed or a planning-time measurement was wrong; the test's failure message must name
   the fixture and both values so the executor can record it as a finding.

4. **Import boundary.** Read every `.ts` file under `src/dashboard/` and `src/widgets/` and assert
   none of them imports `pace-fixtures`. This keeps the module's `node:fs` use out of the browser
   bundle and out of `pace-derivation.ts`'s purity seam. Assert in both directions: also assert the
   scanner does find the import when handed a synthetic in-memory file whose text contains it, so
   the guard is not vacuous.
  </action>
  <verify>
    <automated>npx vitest run src/analytics/pace-fixtures.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analytics/pace-fixtures.test.ts` exits 0.
    - The vitest reporter output lists one test per entry of `PACE_FIXTURE_NAMES` (verify by reading the run output; the count matches `PACE_FIXTURE_NAMES.length`).
    - `grep -c "it.each(PACE_FIXTURE_NAMES)" src/analytics/pace-fixtures.test.ts` outputs `1`.
    - The import-boundary test includes both the negative direction (no dashboard/widget file imports the module) and the positive direction (the scanner detects a planted import in a synthetic input), so it can fail both ways.
    - The plan summary records, for each pinned fixture, the property value observed at execution time next to the planning-time value.
  </acceptance_criteria>
  <done>Every fixture ERA-03 and Criterion 6 require is verified present by name, every pinned archive file loads and matches its recorded expectation, and the module is proven not to leak into the browser bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/streams/`, `data/activities/` → `pace-fixtures.ts` | Committed JSON read at test time via `node:fs` |
| `pace-fixtures.ts` → browser bundle | Must NOT cross — a `node:fs` import reaching `src/dashboard/` would break the dashboard build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Denial of Service | dashboard bundle | mitigate | Task 2's import-boundary test asserts no file under `src/dashboard/` or `src/widgets/` imports `pace-fixtures`, in both directions, so the `node:fs` dependency cannot reach the browser build |
| T-26-05 | Tampering | fixture expected values | mitigate | Provenance header states that changing a pinned fixture's expected value to make a test pass is a regression, not a fix — the `best-effort-fixtures.test.ts` discipline |
| T-26-03 | Information Disclosure | archive reads | accept | Only `data/streams/` and `data/activities/` are read, both already committed and public in this repo; `data/stats/` is gitignored and explicitly not read (asserted by grep) |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no package; 26-RESEARCH.md records the Package Legitimacy Gate as not applicable. Adding any dependency requires re-running the gate protocol first |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/analytics/pace-fixtures.test.ts` exits 0.
- `npm run test` exits 0.
- `npm run build-widgets` exits 0 (proves the fixture module did not leak into the bundle).
</verification>

<success_criteria>
- Every fixture named by ERA-03 and Roadmap Criterion 6 is present by name and verified in the suite.
- The multi-hour pause is synthetic and documented as such with the negative-scan reasoning.
- Phases 27-30 can `import` these fixtures without rebuilding them.
</success_criteria>

<output>
Create `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-03-SUMMARY.md` when done.
Record the execution-time value of every pinned fixture's verified property beside its planning-time
value, and flag any drift explicitly rather than silently updating the fixture.
</output>
