---
phase: 26-shared-gap-aware-pace-derivation-honest-coverage
plan: 14
type: execute
wave: 10
depends_on: [26-13]
files_modified:
  - src/dashboard/views/detail-charts-logic.ts
  - src/dashboard/views/detail-charts-logic.test.ts
  - src/analytics/pace-single-source.test.ts
  - src/analytics/trimp.ts
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [PACE-01, PACE-03, PACE-04]

must_haves:
  decisions: [D-01, D-02, D-15, D-16, D-17, D-18]
  truths:
    - "The detail page's Pace & Effort chart band and its Pace Distribution histogram derive pace from the SAME call to `derivePaceWithCoverage`, so for activity 5059204779 the chart plots the 150s-adaptive series (extent 2:27/km → 17:29/km) rather than the fixed-20s series (extent 0:30/km → 4:27/km) that re-enters Criterion 1's named trap."
    - "The equality is pinned by a permanent regression test that compares the chart's own `buildChannelSeries(stream, 'pace', 'time')` output against `derivePaceWithCoverage(stream).paceSeries` index-for-index on the real committed stream for 5059204779, and that test is watched FAILING against the pre-fix code before it is trusted."
    - "`detail-charts-logic.ts` contains no doc comment asserting an invariant the code does not hold: the `:5-9` 'the two surfaces cannot disagree' claim and the `:74-79` 'a floor, not the only value' claim are gone, replaced by statements true of the code as it then stands."
    - "There is exactly ZERO production code outside `pace-derivation.ts` that calls `derivePaceSeriesGapAware(` or passes a `windowSec` of its own — the coverage-less escape hatch D-16 forbids no longer exists at all, rather than merely being called correctly."
    - "The extended single-source audit is demonstrated FAILING against a deliberately reintroduced ES2015-shorthand fixed-window override — both as a permanent in-suite planted case and as a one-off real-tree plant whose failure output is captured verbatim and then reverted."
  artifacts:
    - path: "src/dashboard/views/detail-charts-logic.ts"
      provides: "Chart band reading the shared adaptive derivation, with no coverage-less pace wrapper and no false contract comments"
      contains: "derivePaceWithCoverage"
    - path: "src/dashboard/views/detail-charts-logic.test.ts"
      provides: "The CR-03 chart-vs-histogram equality regression test on activity 5059204779 plus the 4556693525 floor-window control"
      contains: "5059204779"
    - path: "src/analytics/pace-single-source.test.ts"
      provides: "Override audit extended to the object-shorthand form and to any derivePaceSeriesGapAware( call site outside pace-derivation.ts, with a permanent planted-failure demonstration"
      contains: "derivePaceSeriesGapAware("
  key_links:
    - from: "src/dashboard/views/detail-charts-logic.ts (buildChannelSeries)"
      to: "src/analytics/pace-derivation.ts (derivePaceWithCoverage)"
      via: "direct call inside the pace branch, resolving the adaptive window per activity"
      pattern: "derivePaceWithCoverage\\(stream\\)"
    - from: "src/analytics/pace-single-source.test.ts (OVERRIDE_LITERALS)"
      to: "every production file under src/"
      via: "comment-stripped literal scan for the shorthand override and the gap-aware primitive's call site"
      pattern: "derivePaceSeriesGapAware\\("
---

<objective>
Close CR-03: make the detail page's pace chart band read the same adaptively-resolved pace series
as the histogram, coverage caption and split markers beside it — and make the audit that was
supposed to prevent this actually catch the shape that got through.

Purpose: `26-VERIFICATION.md` returned `gaps_found` on a single root cause. `buildChannelSeries`
(`detail-charts-logic.ts:120`) passes the fixed 20s floor as the pace window, so on the phase's own
pinned exemplar `5059204779` the chart band ships exactly the 94.8%-fast-mass reading that Success
Criterion 1 names as "the trap this criterion exists to avoid re-entering", one scroll position
above a histogram that correctly reads ~1.17%. Two doc comments in the same file assert this cannot
happen. The single-source audit's `OVERRIDE_LITERALS` scan misses the ES2015 shorthand the override
is written in, and the audit's own docblock explicitly excuses the file the override lives in.

Output: a chart band that plots the shipped adaptive series, a regression test that would have
caught this, an audit that catches the shorthand shape, and `PACE-01` reopened in `REQUIREMENTS.md`
pending the browser confirmation in plan 26-16.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-CONTEXT.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VERIFICATION.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-REVIEW.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-04-SUMMARY.md
@.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-05-SUMMARY.md
</context>

<interfaces>
<!-- Facts measured by the planner against the live tree on 2026-09-09, using the committed
     dist/ build and the committed data/streams archive. The executor MUST re-derive every
     number independently before pinning it in a test — but should not need to discover the
     shape of the problem. -->

**The call graph as it stands.**

- `detail.ts:717` — `const derived = derivePaceWithCoverage(detail.stream);` — one call, no options,
  so `windowSec` resolves to `adaptiveWindowSec(t, d)`. Feeds `computePaceDistribution` (histogram),
  `coverageCaptionText` (caption) and `splitGapAnnotations` (split markers).
- `detail-charts-logic.ts:120` — `derivePaceSeries(stream.t, stream.d, PACE_SMOOTHING_WINDOW_SEC)`
  where `PACE_SMOOTHING_WINDOW_SEC` (`:80`) is `PACE_WINDOW_FLOOR_SEC` = `20`. This is the defect.
- `detail-charts-logic.ts:92-99` — `derivePaceSeries` itself: re-runs `classifyGaps` independently
  and calls `derivePaceSeriesGapAware(t, d, { windowSec, gapIntervals })`. It returns pace with NO
  coverage attached — the precise escape hatch D-16 exists to make impossible.
- `detail-charts.ts:237`, `:411`, `:451` — the three `buildChannelSeries(stream, channel, xAxisMode)`
  call sites. All pass a `CanonicalStream` already; **no exported signature needs to change**.

**Exclusive-use confirmation (verified by grep across `src/` and `scripts/` on 2026-09-09).**
`derivePaceSeries` is imported by exactly one file: `detail-charts-logic.test.ts`.
`PACE_SMOOTHING_WINDOW_SEC` is used only inside `detail-charts-logic.ts` and that same test file.
`derivePaceSeriesGapAware(` has exactly one production call site outside `pace-derivation.ts`:
`detail-charts-logic.ts:98`. The literal `windowSec` appears outside `pace-derivation.ts` and
`*.test.ts` at exactly four places, all in `detail-charts-logic.ts` (`:85`, `:88` in comments,
`:95`, `:98` in code). Deleting `derivePaceSeries` and `PACE_SMOOTHING_WINDOW_SEC` therefore
removes every one of them — which is what makes the audit extension in Task 3 land clean and
non-vacuous instead of needing a new self-exemption.

**Independently measured numbers (planner probe, `dist/analytics/pace-derivation.js` +
`dist/dashboard/views/detail-zones.js` against `data/streams/*.json`, 2026-09-09).**

| Activity | `adaptiveWindowSec` | Chart extent under the shipped fixed 20s | Chart extent under the adaptive window | Histogram bucket range (already adaptive) |
|---|---|---|---|---|
| `5059204779` | **150** | 0:30/km → 4:27/km | **2:27/km → 17:29/km** | `2:15–2:30/km` … `17:15–17:30/km` (36 buckets) |
| `4598855187` | 247.5 | 0:33/km → 500:00/km | 3:25/km → 412:30/km | `3:15–3:30/km` … `412:30–412:45/km` |
| `4556693525` | **20 (floor)** | 1:35/km → 18:31/km | 1:35/km → 18:31/km — **identical** | `1:30–1:45/km` … `18:30–18:45/km` |

`4556693525` is the control: its adaptive window resolves to the floor, so the fix must be a
byte-for-byte no-op on that activity's chart. `5059204779` is the discriminator: pre-fix, the whole
chart band sits below the histogram's own slowest bucket by more than a factor of three.

**Fast-mass figure — read this before pinning a number.** Three different values for
"5059204779's covered-time mass faster than 3:00/km under the adaptive window" appear in this
phase's artifacts: `1.22%` (`26-VERIFICATION.md`), `1.17%` (`26-REVIEW.md`), and `1.1688%` (the
planner's own probe). They differ only in the DENOMINATOR — bucketed time
(`sum of paceHistogramSamples(...).timeSec` = 3,679s) versus `coverage.coveredSec` (3,788s) versus
a per-sample weighting. **Do not copy a figure from a prose artifact into an assertion.** State the
denominator the test uses, compute the value from the committed stream, and assert against a stated
tolerance. A test that hard-codes `1.22` against a bucketed-time denominator is unsatisfiable in the
passing direction — this project has shipped an unpassable assertion before.

**What must NOT change.**
- `buildChannelSeries`'s exported signature `(stream, channel, xAxis)`.
- `computeSplits` and anything it touches (D-17: the smoothed series never feeds splits or a stat).
- No memoisation, caching or module-level state. `derivePaceWithCoverage` is pure and cheap
  (~2ms on a 1,893-sample stream); the pace band derives it at most three times per render and
  every call receives identical input, so every call returns an identical result. Adding a cache
  would reintroduce a second place where the two surfaces could drift.
- `interpValueAtTime`'s re-export from this module (`detail-charts.ts` and the hover geometry use it).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: RED — pin chart-vs-histogram equality on 5059204779 and watch it fail; reopen PACE-01</name>
  <files>src/dashboard/views/detail-charts-logic.test.ts, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `src/dashboard/views/detail-charts-logic.test.ts` — the whole file. Note `loadWorkedExampleStream()` (lines ~22-40): it reads `data/streams/4556693525.json` via `node:fs` **deliberately**, because `pace-fixtures.ts`'s own import-boundary guard (D-20, `pace-fixtures.test.ts`) asserts NO file under `src/dashboard/` imports it, test files included. Copy that helper's shape; do not import `pace-fixtures.ts`.
    - `src/dashboard/views/detail-charts-logic.ts` lines 73-130 — `PACE_SMOOTHING_WINDOW_SEC`, `derivePaceSeries`, and `buildChannelSeries`'s pace branch, as they stand before the fix.
    - `src/analytics/pace-derivation.ts` lines 337-345 (`adaptiveWindowSec`), 419-467 (`PaceDerivationResult`, `derivePaceWithCoverage`) and 504-534 (`paceHistogramSamples`) — the exact return shape and the Δt-weighting rule the fast-mass metric must use.
    - `src/dashboard/views/detail-zones.ts` lines 94-120 (`computePaceDistribution`) — the histogram consumes `derived.paceSeries` and `derived.coverage.gapIntervals`, which is what "the histogram's series" means in the assertion below.
    - `.planning/REQUIREMENTS.md` — the `PACE-01` checklist line (~18) and its traceability-table row (~125).
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-VERIFICATION.md` — the `gaps:` block, for the exact wording of the four `missing:` items this plan closes.
  </read_first>
  <action>
Add a new `describe` block to `detail-charts-logic.test.ts` titled
`CR-03 — the chart band and the histogram read one derivation (Criterion 1/4/5, PACE-01)`, and add
a `loadPinnedExemplarStream()` helper alongside the existing `loadWorkedExampleStream()` that reads
`data/streams/5059204779.json` through `node:fs` with the same rationale comment.

Write four tests in that block:

1. **Non-vacuity guard, first.** Assert `derivePaceWithCoverage(exemplar).windowSec` is strictly
   greater than `PACE_WINDOW_FLOOR_SEC`, and `toBeCloseTo(150, 3)`. State in a comment that if this
   activity's adaptive window ever collapsed to the floor, every assertion below would pass
   trivially — this guard is what stops the block rotting into a tautology. Assert the same for
   `4556693525` in the opposite direction: its window `toBe(PACE_WINDOW_FLOOR_SEC)`, which is why
   it serves as the control.

2. **The equality itself.** Build the expected points exactly as `buildChannelSeries` does — iterate
   `i` over `stream.t.length`, skip indices where `derived.paceSeries[i] === null`, push
   `{ x: stream.t[i], y: derived.paceSeries[i] }` — from `derivePaceWithCoverage(exemplar)`, and
   assert `buildChannelSeries(exemplar, 'pace', 'time')` `toEqual` that array. This is the
   verification report's `missing:` item 4 in its strongest form: the same array of values, not a
   tolerance on a summary statistic. Add the `'distance'`-axis variant with `x` as
   `stream.d[i] / 1000`.

3. **The fast-mass statistic, with its denominator stated.** Compute the Δt-weighted fraction of
   time below `180` sec/km over `paceHistogramSamples(t, series, gapIntervals)` — denominator =
   the sum of every returned `timeSec`, NOT `coverage.coveredSec`. Compute it twice: once from the
   chart's own `buildChannelSeries` output re-joined to `stream.t`, once from
   `derivePaceWithCoverage(exemplar).paceSeries`. Assert the two agree to within `0.01` percentage
   points, and separately assert the adaptive value is below `2` percent while the fixed-20s value
   (derived in the test via `derivePaceSeriesGapAware` with an explicit `windowSec: PACE_WINDOW_FLOOR_SEC`
   — legal here, this is a `*.test.ts` file the audit exempts) is above `90` percent. Put the
   measured numbers in a comment with the date they were measured and name the denominator, so a
   future reader can tell which of the three published figures this test's number corresponds to.

4. **The control.** Assert `buildChannelSeries(workedExample, 'pace', 'time')` equals the same
   construction from `derivePaceWithCoverage(workedExample).paceSeries`, and note in a comment that
   this row passes both before and after the fix (its window is the floor either way) — it is a
   no-regression control, explicitly NOT a discriminator.

Run the block and **watch it fail**. Capture the verbatim failure output — the `toEqual` diff
showing the chart's first point `y` against the histogram's first point `y` — and record it in the
SUMMARY as the RED transcript, exactly as plans 26-11 and 26-12 recorded theirs. Do not touch
`detail-charts-logic.ts` in this task; the fix is Task 2's job and the failure is this task's
deliverable.

Then reopen `PACE-01` in `.planning/REQUIREMENTS.md` with exactly two edits and nothing else:
- checklist `- [x] **PACE-01**` → `- [ ] **PACE-01**`, requirement text untouched;
- traceability row `| PACE-01 | Phase 26 | Complete |` →
  `| PACE-01 | Phase 26 | Pending (reopened 2026-09-09 — CR-03: the chart band overrode the adaptive window resolution; literal single-source text satisfied, the "two surfaces cannot disagree" contract defeated. See 26-VERIFICATION.md) |`.

Leave every other requirement's state alone. `PACE-04` and `PACE-03` stay `Complete` — the
verification found the histogram and the window formula themselves correct; only the chart's
consumption of them was wrong, and this plan's own claim on those two IDs is a re-confirmation, not
a reopening.
  </action>
  <verify>
    <automated>bash -c 'npx vitest run src/dashboard/views/detail-charts-logic.test.ts -t "CR-03" &amp;&amp; exit 1 || exit 0'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/views/detail-charts-logic.test.ts -t "CR-03"` exits NON-zero at the end of this task, and the SUMMARY quotes the failure output verbatim including the diverging `y` values.
    - The failure message names activity `5059204779` and shows a chart `y` value below `180` against a histogram `y` value above `180` — i.e. it fails for the CR-03 reason, not because the fixture could not be read.
    - `grep -c "loadPinnedExemplarStream" src/dashboard/views/detail-charts-logic.test.ts` is at least `2` (definition plus use) and `grep -c "pace-fixtures" src/dashboard/views/detail-charts-logic.test.ts` outputs `0`.
    - The non-vacuity guard is present and passes even now: `derivePaceWithCoverage(exemplar).windowSec` is `> PACE_WINDOW_FLOOR_SEC` and close to `150`.
    - The fast-mass test names its denominator in a comment and its asserted value was computed in this session, not copied from `26-REVIEW.md` or `26-VERIFICATION.md`. The SUMMARY states the measured value and its denominator.
    - `grep -c "^- \[ \] \*\*PACE-01\*\*" .planning/REQUIREMENTS.md` outputs `1`.
    - `grep -c "PACE-01 | Phase 26 | Pending" .planning/REQUIREMENTS.md` outputs `1`.
    - `git diff --stat .planning/REQUIREMENTS.md` shows exactly 2 changed lines.
    - `git status --porcelain src/dashboard/views/detail-charts-logic.ts` is empty — the production file was not touched in this task.
  </acceptance_criteria>
  <done>A failing, non-vacuous regression test pins the chart series to the histogram series on the pinned exemplar, its RED output is recorded verbatim, and PACE-01 honestly reads Pending.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — the chart band reads derivePaceWithCoverage; delete the coverage-less wrapper and the two false comments</name>
  <files>src/dashboard/views/detail-charts-logic.ts, src/dashboard/views/detail-charts-logic.test.ts, src/analytics/trimp.ts</files>
  <read_first>
    - `src/dashboard/views/detail-charts-logic.ts` — the whole file, especially the header block (`:1-21`), the import list (`:23-33`), `PACE_SMOOTHING_WINDOW_SEC` and its doc (`:73-80`), `derivePaceSeries` and its doc (`:82-99`), and `buildChannelSeries` (`:101-131`).
    - `src/dashboard/views/detail-charts.ts` lines 230-245, 405-460 — the three `buildChannelSeries` call sites, to confirm each already holds a `CanonicalStream` and no signature change is needed.
    - `src/analytics/pace-derivation.ts` lines 419-467 — `derivePaceWithCoverage`'s doc block, whose D-16 contract ("no caller can hold pace without coverage") is the reason the wrapper is being deleted rather than merely re-argumented.
    - `src/dashboard/views/detail-charts-logic.test.ts` — the existing `describe('derivePaceSeries')` block (lines ~84-155). Its five tests must survive this change; they are the only consumers of the function being deleted.
    - `src/analytics/trimp.ts` lines 10-22 — the cross-file citation naming `detail-charts-logic.ts`'s `derivePaceSeries`, which rots the moment that function is deleted. This project has a recorded discipline (plan 19-16, finding R3-IN-05) of stripping rotted foreign citations rather than leaving them.
  </read_first>
  <action>
**Make the chart read the shared result.** In `buildChannelSeries`'s `channel === 'pace'` branch,
replace the `derivePaceSeries(stream.t, stream.d, PACE_SMOOTHING_WINDOW_SEC)` call with a direct
`derivePaceWithCoverage(stream)` call and read `.paceSeries` off the result. The iteration,
null-skipping and `{ x, y }` shaping below it are unchanged. Import `derivePaceWithCoverage` from
`../../analytics/pace-derivation.js`.

**Delete the escape hatch entirely.** Remove the exported `derivePaceSeries` function
(`:82-99`) and the exported `PACE_SMOOTHING_WINDOW_SEC` constant (`:73-80`) from
`detail-charts-logic.ts`. This is the reviewer's preferred fix and it is what makes Task 3's audit
extension land without a new self-exemption: after this change there is no `derivePaceSeriesGapAware(`
call site and no `windowSec` token anywhere in production code outside `pace-derivation.ts`. Drop the
now-unused imports `PACE_WINDOW_FLOOR_SEC`, `classifyGaps` and `derivePaceSeriesGapAware`. Keep the
`interpValueAtTime` re-export — `detail-charts.ts` and the hover geometry still use it.

**Correct both false contract comments**, which is `26-VERIFICATION.md`'s `missing:` item 2:
- The module header's `:5-9` claim currently reads that `derivePaceSeries` and `interpValueAtTime`
  are thin wrappers "so the two surfaces cannot disagree". Rewrite it to state what is then true:
  this module owns no pace arithmetic and no window resolution of its own; the pace band obtains
  its series from `derivePaceWithCoverage(stream)`, the same single D-16 entry point `detail.ts`
  calls for the histogram, coverage caption and split markers, so both surfaces resolve the same
  adaptive window from the same stream. Name CR-03 and the date as the reason the previous wording
  was removed, and state the one fact that makes the claim checkable rather than aspirational:
  `detail-charts-logic.test.ts`'s CR-03 block asserts the two series are equal index-for-index on
  activity `5059204779`, whose adaptive window is 150s, not the 20s floor.
- The `:74-79` "this constant is now a floor, not the only value" comment disappears with the
  constant. Do not relocate that sentence anywhere: `PACE_WINDOW_FLOOR_SEC`'s own doc block in
  `pace-derivation.ts` is the correct and only home for the floor's meaning.

Do not write a replacement comment that asserts anything a reader cannot check from this file plus
the named test. The failure class being closed here — a doc comment claiming an invariant the code
does not hold — is the same class `WR-01` was closed for last round and the class `COV-01` exists
to forbid; a confidently-worded replacement that is merely differently unverifiable is not a fix.

**Keep the deleted function's test coverage.** In `detail-charts-logic.test.ts`, rename the
`describe('derivePaceSeries')` block to name what it now exercises and redirect its five tests to
call `derivePaceSeriesGapAware(t, d, { windowSec, gapIntervals: classifyGaps(t, d).gapIntervals })`
imported from `../../analytics/pace-derivation.js` — legal in a `*.test.ts` file, which the audit
exempts by design. The five behaviours (constant-speed ≈200 s/km at a 20s window; real-Δt weighting
of a 60s burst versus a 1s burst; `null` not `NaN`/`Infinity` on a standstill; never `NaN`/`Infinity`
for any window; and the one-derivation-under-two-names check) must all still run and still pass. The
last of those five compared the wrapper against `derivePaceWithCoverage` — with the wrapper gone,
re-point it to compare `buildChannelSeries`'s pace output against `derivePaceWithCoverage(...).paceSeries`
on `4556693525`, which is the same claim through the surface that still exists.

**Strip the rotted citation.** In `src/analytics/trimp.ts` (~line 18), the phrase
"mirrored from `detail-charts-logic.ts`'s `derivePaceSeries`" names a function that no longer
exists. Repoint it at `pace-derivation.ts`'s `derivePaceSeriesGapAware`, which is where the real-Δt
integration rule the comment is describing actually lives. Change nothing else in that file.

Run the CR-03 block and confirm it now passes, then run the full suite plus `npx tsc --noEmit`.
</action>
  <verify>
    <automated>npx vitest run src/dashboard/views/detail-charts-logic.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/views/detail-charts-logic.test.ts -t "CR-03"` exits `0` — the Task 1 RED test is now GREEN, and the SUMMARY quotes both the RED and the GREEN transcripts.
    - `npm run test` and `npx tsc --noEmit` both exit `0`.
    - `grep -c "derivePaceWithCoverage" src/dashboard/views/detail-charts-logic.ts` is at least `1`.
    - `grep -c "PACE_SMOOTHING_WINDOW_SEC\|export function derivePaceSeries" src/dashboard/views/detail-charts-logic.ts` outputs `0`.
    - `grep -rn "derivePaceSeriesGapAware(\|windowSec" src/ | grep -v "^src/analytics/pace-derivation.ts" | grep -v "\.test\.ts:"` produces no output — zero production references outside the shared module.
    - `grep -v '^ \*' src/dashboard/views/detail-charts-logic.ts | grep -c "cannot disagree\|a floor, not the only value"` outputs `0`, AND the same two phrases are absent from the comment text as well: `grep -c "cannot disagree\|a floor, not the only value" src/dashboard/views/detail-charts-logic.ts` outputs `0`.
    - The rewritten module header names `derivePaceWithCoverage`, names CR-03, and names `detail-charts-logic.test.ts` as the test that checks the claim: `grep -c "derivePaceWithCoverage" src/dashboard/views/detail-charts-logic.ts` and `grep -c "CR-03" src/dashboard/views/detail-charts-logic.ts` are each at least `1`.
    - `grep -c "detail-charts-logic.ts\`'s \`derivePaceSeries\`" src/analytics/trimp.ts` outputs `0`.
    - `buildChannelSeries`'s exported signature is unchanged: `grep -c "stream: CanonicalStream," src/dashboard/views/detail-charts-logic.ts` is at least `1` and `src/dashboard/views/detail-charts.ts` is NOT in this plan's diff (`git status --porcelain src/dashboard/views/detail-charts.ts` is empty).
    - No caching, memoisation or module-level mutable state was added: `git diff src/dashboard/views/detail-charts-logic.ts | grep -c "^+.*\(Map\|cache\|memo\|WeakMap\)"` outputs `0`.
    - All five redirected tests in the former `derivePaceSeries` block still run and pass; the SUMMARY names them.
  </acceptance_criteria>
  <done>The pace chart band plots `derivePaceWithCoverage(stream).paceSeries`, the coverage-less wrapper and the fixed-window constant are gone, both false invariant comments are gone, and the CR-03 test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the single-source audit to the shorthand override and demonstrate it failing</name>
  <files>src/analytics/pace-single-source.test.ts</files>
  <read_first>
    - `src/analytics/pace-single-source.test.ts` — the whole file. Specifically: the header docblock's "KNOWN, DOCUMENTED BLIND SPOT" section, `OVERRIDE_LITERALS` (~:204) with its docblock (~:189-203) that explicitly excuses `detail-charts-logic.ts`'s wrapper, `GAP_MEMBERSHIP_LITERALS` (~:218), `isTypeAnnotationSuffix`, `findConfinedCallSites`, `ALLOWED_FILE_SUFFIX`, the `override containment` test (~:293), and the permanent planted `describe` block (~:332).
    - `src/dashboard/views/detail-charts-logic.ts` **as Task 2 left it** — to confirm the file the audit's docblock excuses no longer contains the shape being excused.
    - `src/analytics/pace-derivation.ts` lines 363-370 — `derivePaceSeriesGapAware`'s own signature and its `const { windowSec, gapIntervals } = options;` destructuring, which is the legitimate in-module occurrence the extended scan must still find (non-vacuity) and must not flag.
    - `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-05-SUMMARY.md` — the one-off real-tree plant-and-revert transcript from plan 26-05, whose format this task's own demonstration must mirror.
  </read_first>
  <action>
**Extend the scan.** Change `OVERRIDE_LITERALS` from
`['clipAtGaps', 'windowSec:', 'pauseRule:']` to
`['clipAtGaps', 'windowSec:', 'windowSec,', 'windowSec }', 'pauseRule:', 'derivePaceSeriesGapAware(']`.

The three new entries are not equivalent and the docblock must say why:
- `'windowSec,'` and `'windowSec }'` catch the ES2015 object-shorthand form
  (`{ windowSec, gapIntervals }` / `{ windowSec }`) that `'windowSec:'` cannot see. They are
  formatting-sensitive heuristics — a call written `{windowSec}` with no spaces would slip past
  `'windowSec }'` — so they are belt-and-braces, not the load-bearing check.
- `'derivePaceSeriesGapAware('` is the load-bearing check: it catches ANY direct call to the
  gap-aware primitive outside `pace-derivation.ts` regardless of how the options object is written,
  which is a structural property rather than a formatting one.

`isTypeAnnotationSuffix`'s exclusion applies only to literals ending in `:`, so `windowSec:` keeps
its TypeScript-type-annotation exemption and the three new literals get no exemption at all. Leave
that function alone.

**Correct the audit's own docblock.** The `OVERRIDE_LITERALS` docblock currently states that a bare
literal scan "would false-positive on `detail-charts-logic.ts`'s `derivePaceSeries` wrapper …
and is not an override of `derivePaceWithCoverage`'s adaptive resolution." That reasoning is what
let CR-03 through — passing a fixed 20s in place of the adaptive resolution IS the override.
Rewrite the docblock to record the correction with its date and the finding id (CR-03), state that
the excused wrapper was deleted in plan 26-14 rather than re-excused, and explain the three new
literals as above. Also update the header block's "KNOWN, DOCUMENTED BLIND SPOT AND ITS TARGETED
CLOSURE" section to name this as the SECOND historically-real blind spot the audit has closed,
alongside the `f32dddd8` gap-membership one — an audit whose header claims completeness it has not
demonstrated is the same false-invariant failure class this phase keeps re-encountering.

**Rename the containment test** so its title matches what it now covers, e.g.
`override containment: clipAtGaps / windowSec (colon, comma and shorthand-close forms) / pauseRule / any derivePaceSeriesGapAware( call site appear only in pace-derivation.ts or *.test.ts files`.
Keep its existing non-vacuity assertion (`matches.length` greater than `0`) and its offender filter
unchanged.

**Add a permanent planted demonstration** — a new `it` inside the existing planted `describe` block
(the one titled "the audit is demonstrated catching a reintroduced second implementation
(planted, permanent)"), or a sibling `describe` if that reads better. It must run
`findConfinedCallSites` against a synthetic `SourceFile[]` containing a file at
`src/dashboard/views/planted-fixed-window.ts` whose source is exactly the CR-03 shape — a named
constant holding `20`, and a call `derivePaceSeriesGapAware(t, d, { windowSec, gapIntervals })` —
and assert the returned matches include that path for BOTH `'windowSec,'` and
`'derivePaceSeriesGapAware('`. Add the negative direction in the same style as the existing planted
pair: the same file rewritten to call `derivePaceWithCoverage(stream)` produces zero matches. This
is the rot-proof half of the proof — it survives even if the real tree is never violated again.

**Then demonstrate it against the real tree, once, and revert.** Write a file at
`src/dashboard/views/planted-fixed-window.ts` containing the shorthand override shape, run
`npx vitest run src/analytics/pace-single-source.test.ts`, and confirm it FAILS naming that path.
Capture the failure output verbatim into the SUMMARY. Delete the file, re-run the audit, confirm it
passes, and confirm `git status --porcelain src` is empty. This is the demonstration
`26-VERIFICATION.md` asks for — an audit that has never been shown to fail on the shape it now
claims to catch is not evidence. Do NOT commit the planted file; if the run is interrupted, deleting
that path is the whole recovery.

Finally re-run the full suite. Confirm the extended `OVERRIDE_LITERALS` produces zero offenders
against the real tree — which it will only because Task 2 deleted `detail-charts-logic.ts`'s
wrapper. If any offender appears, do not add an exemption: report it, because a second real override
site would be a finding, not a false positive.
</action>
  <verify>
    <automated>npx vitest run src/analytics/pace-single-source.test.ts &amp;&amp; npm run test &amp;&amp; test -z "$(git status --porcelain src)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analytics/pace-single-source.test.ts` exits `0` with zero offenders reported, and `npm run test` exits `0`.
    - `grep -c "'derivePaceSeriesGapAware('" src/analytics/pace-single-source.test.ts` is at least `1` and `grep -c "'windowSec,'" src/analytics/pace-single-source.test.ts` is at least `1`.
    - The SUMMARY contains a verbatim transcript of the real-tree plant showing the audit FAILING and naming `src/dashboard/views/planted-fixed-window.ts`, followed by the passing re-run after deletion.
    - `ls src/dashboard/views/planted-fixed-window.ts` returns "No such file"; `git status --porcelain src` is empty at end of task.
    - The permanent planted test asserts the synthetic CR-03-shaped file is flagged for BOTH `windowSec,` and `derivePaceSeriesGapAware(`, and asserts the `derivePaceWithCoverage`-based rewrite of the same file is flagged for neither.
    - The `OVERRIDE_LITERALS` docblock no longer excuses `detail-charts-logic.ts` — `grep -c "would false-positive on" src/analytics/pace-single-source.test.ts` outputs `0` — and names CR-03: `grep -c "CR-03" src/analytics/pace-single-source.test.ts` is at least `1`.
    - The containment test's non-vacuity assertion is intact and passes: the scan still finds legitimate in-module matches inside `pace-derivation.ts`.
    - `isTypeAnnotationSuffix` is unchanged: `git diff src/analytics/pace-single-source.test.ts | grep -c "isTypeAnnotationSuffix(afterColon"` outputs `0`.
  </acceptance_criteria>
  <done>The audit catches the object-shorthand fixed-window override and any out-of-module `derivePaceSeriesGapAware(` call, has been watched failing on that exact shape against the real tree, and its own docblock no longer excuses the file the defect lived in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a local static-dashboard build. The changed code is pure, DOM-free, browser-side
presentation logic that reads already-committed JSON from the same origin and writes nothing.
There is no network surface, no user input, no authentication and no persistence in anything this
plan touches — so an honest register here is short, and the real risks are evidentiary rather than
adversarial.

| Boundary | Description |
|----------|-------------|
| committed stream archive → derived presentation series | the only input crossing into the changed code; already-trusted, repo-committed JSON |
| test assertion → recorded evidence | a green test is an evidentiary claim; a vacuous or unpassable one falsifies the record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-30 | Tampering | `buildChannelSeries`'s window argument | mitigate | The argument is removed entirely — `derivePaceWithCoverage(stream)` resolves the window internally, so there is no call-site value left to tamper with; the extended `OVERRIDE_LITERALS` scan fails the build if one is reintroduced |
| T-26-31 | Repudiation | the CR-03 regression test | mitigate | Watched failing before the fix (Task 1) and passing after (Task 2), both transcripts quoted verbatim in the SUMMARY; a non-vacuity guard asserts the exemplar's window exceeds the floor so the test cannot silently become a tautology |
| T-26-32 | Information disclosure | a false contract comment | mitigate | Both false invariant claims are deleted rather than reworded, and the replacement header names the specific test that checks it — grep-asserted in Task 2's acceptance criteria |
| T-26-33 | Denial of service | repeated `derivePaceWithCoverage` calls per render | accept | Pure function, ~2ms on the largest committed stream, at most three calls per detail render; caching would reintroduce a second place the two surfaces could drift, which is the defect being closed |
| T-26-34 | Elevation of privilege | the planted real-tree probe file | mitigate | Written under `src/dashboard/views/`, deleted in the same task, and `git status --porcelain src` asserted empty as an acceptance criterion; the file is never committed |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages and adds no dependencies |
</threat_model>

<verification>
- `npm run test`, `npx tsc --noEmit`, `npm run build` all exit `0`.
- `npx vitest run src/dashboard/views/detail-charts-logic.test.ts -t "CR-03"` exits `0`.
- `grep -rn "derivePaceSeriesGapAware(\|windowSec" src/ | grep -v "^src/analytics/pace-derivation.ts" | grep -v "\.test\.ts:"` produces no output.
- `grep -c "cannot disagree\|a floor, not the only value" src/dashboard/views/detail-charts-logic.ts` outputs `0`.
- `grep -c "PACE-01 | Phase 26 | Pending" .planning/REQUIREMENTS.md` outputs `1`.
- `git status --porcelain src` is empty.
</verification>

<success_criteria>
For activity `5059204779` — the phase's own pinned exemplar, the one activity carrying the
"Pace disputed" badge, and the activity Success Criterion 1 uses as its worked example — the pace
chart band and the pace histogram now derive from one call to `derivePaceWithCoverage`, resolving
one adaptive window (150s, not the 20s floor), and a permanent test asserts they are the same array
of values index-for-index. The coverage-less pace wrapper D-16 forbids no longer exists in the
codebase. Neither remaining doc comment in `detail-charts-logic.ts` asserts an invariant the code
does not hold. The single-source audit catches the ES2015-shorthand override shape and has been
watched failing on it against the real tree, then reverted clean.
</success_criteria>

<output>
Create `.planning/phases/26-shared-gap-aware-pace-derivation-honest-coverage/26-14-SUMMARY.md` when done.
</output>
