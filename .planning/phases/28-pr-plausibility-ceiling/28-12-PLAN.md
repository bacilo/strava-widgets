---
phase: 28-pr-plausibility-ceiling
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/compute-pr-ceiling-diff.mjs
  - scripts/compute-pr-ceiling-diff.test.mjs
  - scripts/compute-pr-ceiling-calibration.mjs
  - scripts/compute-pr-ceiling-calibration.test.mjs
autonomous: true
gap_closure: true
requirements: [PR-02, PR-04]

must_haves:
  decisions: [D-01, D-02, D-12, D-13, D-14, D-15]
  truths:
    - "The diff generator's OLD reconstruction and the calibration generator's population both decide exclusion per effort (effort.excludedFromRecords), never per activity, matching the pipeline's distance-scoped exclusion (WR-04)"
    - "The calibration population is built exactly as Pass 1 builds it. Efforts demoted by world-record or max-speed are dropped; ceiling-demoted efforts and efforts with no demotion field are kept (WR-03)"
    - "The diff reports, per distance, how many ceiling demotions fall on owner-excluded efforts, and lists every such effort (activity, distance, duration, implied speed, ceiling), so the human sign-off sees exactly what CR-01's fix changed"
    - "The diff's Reconciliation section names the two independent recount figures the ceiling-only total must equal, byGuard.ceiling and the sweep's independent ceiling count, and still carries no sign-off text (D-14)"
    - "The diff script removes its temp directory whether computeBestEfforts succeeds or throws (IN-04), and its stale 'silently ignored' comments are gone (IN-01)"
    - "Neither generator is run against the real archive in this plan; the committed 28-DIFF.md and 28-CEILING-CALIBRATION.md are byte-unchanged at plan end"
  artifacts:
    - path: "scripts/compute-pr-ceiling-diff.mjs"
      provides: "per-effort exclusion in reconstructOldDocument, demotedExcludedCount, ceilingDemotedExcluded listing, try/finally temp cleanup"
      contains: "ceilingDemotedExcluded"
    - path: "scripts/compute-pr-ceiling-calibration.mjs"
      provides: "Pass-1-faithful buildFilteredPopulations"
      contains: "max-speed"
  key_links:
    - from: "scripts/compute-pr-ceiling-diff.mjs renderDiffMarkdown"
      to: "report.ceilingDemotedExcluded"
      via: "a generated section, never hand-written prose"
      pattern: "owner-excluded"
    - from: "scripts/compute-pr-ceiling-calibration.mjs buildFilteredPopulations"
      to: "effort.demotion?.guard"
      via: "absolute-guard filter mirroring compute-best-efforts.ts Pass 1"
      pattern: "world-record"
---

<objective>
Fix the two generators, not their output. The regenerated `28-DIFF.md` and a possible regenerated
`28-CEILING-CALIBRATION.md` (plan 28-14) must then be correct by construction.

Purpose:
- WR-04: a distance-scoped exclusion would make both scripts drop the whole activity. That is
  latent today, since all 12 entries use `distances: null`.
- WR-03: the calibration script still assumes the pre-D-08 document shape. Regenerating it today
  would publish a 400m population of 1852 and a max of 1000 m/s.
- The fresh PR-04 sign-off (plan 28-15) needs the diff to SHOW the 13 owner-excluded efforts that
  CR-01's fix newly ceiling-demotes. Otherwise the ceiling-only total jumps from 18 to 31 with no
  visible explanation.
- IN-01 and IN-04 are in the same file, and cheap.

The project lesson this follows: a hand-edited generated artifact silently reverts on the next run.
Every word the human will read in `28-DIFF.md` is therefore emitted by the generator.

Output: fixed generators and tests. The artifacts are NOT regenerated here; plan 28-14 does that
against the fixed compute step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-pr-plausibility-ceiling/28-REVIEW.md
@.planning/phases/28-pr-plausibility-ceiling/28-07-SUMMARY.md
@.planning/phases/28-pr-plausibility-ceiling/28-DIFF.md
@scripts/compute-pr-ceiling-diff.mjs
@scripts/compute-pr-ceiling-calibration.mjs

<interfaces>
scripts/compute-pr-ceiling-diff.mjs (current):
- imports `computeBestEfforts` (dist), `TARGET_ORDER` (dist types), `markPRs, rankTopN, TOP_N` (dist utils), `CEILING_K, CEILING_MIN_POPULATION` (dist ceiling); imports `mkdtempSync, writeFileSync` from fs
- `reconstructOldDocument(newDoc) -> { rankings, prFlags }` — line ~127 `if (!activity || activity.excludedFromRecords) continue;` (WR-04 target), then per effort: skip `effort.excludedFromRecords`, skip guard `world-record`/`max-speed`, retain null/`ceiling`
- `extractNewState(newDoc) -> { rankings, prFlags }` where `prFlags[`${id}|${distance}`] = { wasPRAtTheTime, durationSec, startDate, demotionGuard }`
- `diffPrState(oldState, newState) -> { perDistance[key]: { rankingRows, flagFlips, flagsBefore, flagsAfter, flagsFlipped, demotedCount, netZeroButMoved }, totals: { totalDemoted, totalFlagFlips, totalRetroactivePromotions, totalRankingRowsMoved } }` — `demotedCount` counts `newInfo.demotionGuard === 'ceiling'` over ALL new efforts (excluded included)
- `buildDiffReport(newDoc) -> { generatedAt, archiveSize, documentDemotedTotal, ceilings, perDistance, totals, ceilingK, ceilingMinPopulation }`
- `renderDiffMarkdown(report)` sections in order: title+provenance, `**Generated:**`, `## How before and after were computed`, `## Summary` (bullets + table `| Distance | Ceiling (m/s) | Demoted | Flags before | Flags after | Flags flipped |`), `## Records that changed hands`, `## PR-at-the-time flag flips`, `## Retroactive promotions`, `## Reconciliation`, `## Inputs`
- `main()` — `mkdtempSync(join(tmpdir(), 'pr-ceiling-diff-'))`, never removed (IN-04); header lines 18-22 and main lines 553-558 say `ceilingStatePath` is "silently ignored" until 28-06 lands (IN-01, stale)
- `safeActivityId`, `safeCell` (T-28-07-B)

scripts/compute-pr-ceiling-calibration.mjs (current):
- imports `TARGET_ORDER, TARGET_METERS` (dist types), `riegelPredict` (dist riegel)
- `buildFilteredPopulations(bestEffortsDoc) -> Map<key, Array<{ activityId, startDate, durationSec, speedMps }>>` — lines ~68-97: skips `activity.excludedFromRecords` (WR-04), skips `effort.excludedFromRecords`; doc comment at ~57-66 says the shipped efforts array "is already post-isPlausible" (WR-03, false since D-08)
- the rest (`describeDistribution`, `deriveCeilingMultiplier`, `applyCeiling`, `compareRiegelGate`, `buildCalibrationReport`, `renderCalibrationMarkdown`, `main`) is unchanged by this plan

Existing tests that encode the OLD behaviour and must be updated on purpose (not weakened):
- scripts/compute-pr-ceiling-calibration.test.mjs ~51-124: fixture `act-activity-excluded` (activity `excludedFromRecords: true`, effort `excludedFromRecords: false`) currently asserted EXCLUDED; under WR-04 the pipeline INCLUDES a non-excluded effort of a distance-scoped-excluded activity, so this becomes an inclusion assertion.
- scripts/compute-pr-ceiling-diff.test.mjs ~73-130: `reconstructOldDocument` membership suite (per-effort exclusion `exclEffort1` stays excluded).

Planner-measured prediction (2026-09-16, shipped document generatedAt 2026-09-10T23:08:32.377Z): a Pass-1-faithful population gives n/p90/max = 400m 1825/3.9920/6.0332, 1k 1843/3.7120/6.1996, 1mi 1842/3.6189/5.6075, 5k 1779/3.3951/4.2398, 10k 1459/3.2997/4.1948, half 104/3.4388/4.0484, marathon 0 — identical to the committed `28-CEILING-CALIBRATION.md` tables. Plan 28-14 relies on this.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Diff generator — per-effort exclusion, the owner-excluded ceiling listing, reconciliation wording, temp cleanup</name>
  <files>scripts/compute-pr-ceiling-diff.mjs, scripts/compute-pr-ceiling-diff.test.mjs</files>
  <read_first>
    - scripts/compute-pr-ceiling-diff.mjs (whole file)
    - scripts/compute-pr-ceiling-diff.test.mjs (whole file — especially the idempotence/render tests and any assertion on the Summary table header or Reconciliation text)
    - .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md (the currently signed-off rendering; section order must be preserved)
    - .planning/phases/28-pr-plausibility-ceiling/28-REVIEW.md WR-04, IN-01, IN-04
  </read_first>
  <behavior>
    - reconstructOldDocument keeps a non-excluded 5k effort of an activity whose `excludedFromRecords` is true while its 400m effort is excluded. This is the distance-scoped case. The 400m effort is dropped and the 5k effort is retained.
    - diffPrState's `perDistance[key].demotedExcludedCount` counts NEW efforts with guard `ceiling` AND `excludedFromRecords` true. `demotedCount` still counts every ceiling-guard effort. `totals.totalDemotedExcluded` is their sum.
    - buildDiffReport exposes `ceilingDemotedExcluded`: one row per NEW effort with guard `ceiling` and `excludedFromRecords` true, `{ activityId, distance, durationSec, impliedSpeedMps, ceilingMps }`, sorted by TARGET_ORDER index then activityId (localeCompare).
    - renderDiffMarkdown:
      - Summary bullet `- Of those, also owner-excluded (no ranking effect): N`.
      - Summary table header `| Distance | Ceiling (m/s) | Demoted (ceiling) | Of which owner-excluded | Flags before | Flags after | Flags flipped |`.
      - A new section `## Ceiling demotions on owner-excluded efforts`, placed immediately after `## Retroactive promotions` and before `## Reconciliation`. It lists one table row per `ceilingDemotedExcluded` entry, with speeds to 4 decimals and duration to 1 decimal. With no entries it prints `None in this run.`
    - The Reconciliation paragraph names `byGuard.ceiling` and the sweep's `independentCeilingCount` from `scripts/compute-pr-ceiling-recount.mjs` as the figures the ceiling-only total must equal. It contains no sign-off, approval or reviewer text.
    - Rendering the same report twice gives identical strings. Two reports built from the same document differ only on the `**Generated:**` line.
    - Rows whose activity id fails `VALID_ACTIVITY_ID` render as `(malformed id)` in the new table too (T-28-07-B).
  </behavior>
  <action>
    Write or adjust the tests first and watch them fail, then implement.

    1. WR-04: in `reconstructOldDocument`, change `if (!activity || activity.excludedFromRecords) continue;`
       to `if (!activity) continue;`. Rely only on `effort.excludedFromRecords`, which the pipeline
       sets per distance. Update the membership doc comment: drop the "drop the whole activity"
       bullet, and add a sentence saying exclusion is per effort because the pipeline's exclusion is
       distance-scoped.
    2. Carry `excludedFromRecords: effort.excludedFromRecords === true` on every `extractNewState`
       prFlags entry.
    3. In `diffPrState`, add `demotedExcludedCount` per distance and `totalDemotedExcluded` to
       totals. Do not change how `demotedCount`, the flags, `rankingRows` or `flagFlips` are
       computed.
    4. In `buildDiffReport`, compute `ceilingDemotedExcluded` from `newDoc.activities` and
       `newDoc.ceilings`. Add `TARGET_METERS` to the existing
       `../dist/analytics/best-effort.types.js` import; this script is allowed to import the
       pipeline vocabulary, and the D-15 prohibition applies only to the recount. Set
       `impliedSpeedMps = TARGET_METERS[distance] / durationSec` and
       `ceilingMps = newDoc.ceilings[distance]?.ceilingMps ?? null`. Sort deterministically by
       TARGET_ORDER index, then activityId.
    5. Update `renderDiffMarkdown`:
       - Add the Summary bullet and the new table column. The two new values go right after the
         existing Demoted value in each row.
       - Add the new section with a generator-emitted explanation paragraph. It must say that these
         efforts were already absent from every ranking and flag chain because the owner excluded
         them. Since the CR-01 fix they also carry a ceiling demotion, because D-10 records the
         owner's intent and the machine's judgment as separate claims, so no ranking row or flag
         flip anywhere above is caused by them. The listing is how a reviewer confirms the
         ceiling-only total's increase is fully explained by owner-excluded efforts.
       - Revise the `## How before and after were computed` paragraph so it says exclusion is
         applied per effort.
       - Revise the `## Reconciliation` paragraph as described in the behavior block, keeping the
         document total sentence. Keep the existing section order everywhere else, and keep every
         existing section's heading text unchanged.
    6. IN-04: in `main()`, wrap everything after `mkdtempSync` in
       `try { ... } finally { rmSync(tempDir, { recursive: true, force: true }); }` (import `rmSync`).
       Keep the existing `computeBestEfforts` error path setting `process.exitCode = 1`.
    7. IN-01: remove the stale "silently ignored" / "plan 28-06, running concurrently" /
       "forward-compatible" wording in the header comment and in `main()`. State plainly that
       `ceilingStatePath` is a throwaway temp path, so the committed ceiling state is never
       rewritten by this script.
    8. Update existing tests that pin the old header or old paragraph text so they pin the new text.
       Add tests for every behavior bullet above, including one for the distance-scoped case
       (WR-04) and one proving the new section renders `None in this run.` when no excluded ceiling
       demotion exists.

    Do NOT run `npm run compute-pr-ceiling-diff` in this plan. `28-DIFF.md` must stay byte-identical
    (sha256 `08e93d5adec6ee3de886ab8b47ac0d4a48e001847e331c18830a812f546f77c6`) until plan 28-14.
    The test file imports from `../dist/`, so run `npm run build` before the tests.

    Commit: `fix(28-12): diff generator applies exclusion per effort and lists owner-excluded ceiling demotions`.
  </action>
  <verify>
    <automated>npm run build && npx vitest run scripts/compute-pr-ceiling-diff.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/compute-pr-ceiling-diff.test.mjs` exits 0
    - `grep -c "activity.excludedFromRecords" scripts/compute-pr-ceiling-diff.mjs` prints 0
    - `grep -c "ceilingDemotedExcluded" scripts/compute-pr-ceiling-diff.mjs` is at least 3; `grep -c "## Ceiling demotions on owner-excluded efforts" scripts/compute-pr-ceiling-diff.mjs` is 1
    - `grep -c "rmSync" scripts/compute-pr-ceiling-diff.mjs` is at least 2 (import plus call), and `grep -c "finally" scripts/compute-pr-ceiling-diff.mjs` is at least 1
    - `grep -ci "silently ignored" scripts/compute-pr-ceiling-diff.mjs` prints 0
    - `grep -ciE "sign-off:|approved by|reviewer:" scripts/compute-pr-ceiling-diff.mjs` prints 0, apart from the existing D-14 provenance sentence stating there is NO sign-off block. Check with `grep -n "sign-off" scripts/compute-pr-ceiling-diff.mjs`, and record the lines in the SUMMARY.
    - `shasum -a 256 .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md` still prints `08e93d5adec6ee3de886ab8b47ac0d4a48e001847e331c18830a812f546f77c6`
  </acceptance_criteria>
  <done>The diff generator gets distance-scoped exclusions right and makes CR-01's effect visible in generated text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Calibration generator — build the population exactly as Pass 1 does (WR-03, WR-04)</name>
  <files>scripts/compute-pr-ceiling-calibration.mjs, scripts/compute-pr-ceiling-calibration.test.mjs</files>
  <read_first>
    - scripts/compute-pr-ceiling-calibration.mjs lines 1-100 (header, `buildFilteredPopulations` and its doc comment)
    - scripts/compute-pr-ceiling-calibration.test.mjs lines 1-130
    - src/analytics/compute-best-efforts.ts Pass 1 block (the membership rule to mirror: not excluded AND `demotion === null` at accumulation time, i.e. no absolute-guard demotion)
    - .planning/phases/28-pr-plausibility-ceiling/28-REVIEW.md WR-03 and WR-04
  </read_first>
  <behavior>
    - An activity with `excludedFromRecords: true` whose effort has `excludedFromRecords: false` is INCLUDED. This is the distance-scoped exclusion, and the pipeline includes it.
    - An effort with `excludedFromRecords: true` is excluded.
    - An effort with `demotion.guard === 'world-record'` is excluded, and so is one with `demotion.guard === 'max-speed'`, even at an absurd implied speed such as 1000 m/s.
    - An effort with `demotion.guard === 'ceiling'` is INCLUDED. The ceiling did not exist when Pass 1 accumulated.
    - An effort with no `demotion` key at all (a pre-D-08 document) or with `demotion: null` is included.
    - Rankings are still never read for membership.
  </behavior>
  <action>
    Test first. Replace the old test `includes only the clean effort — excludes activity-level and effort-level exclusions`
    with `mirrors Pass 1: per-effort exclusion only, absolute-guard demotions dropped, ceiling demotions kept`.
    Extend the fixture with three more activities, one each for world-record-demoted (durationSec
    0.4, so 1000 m/s), max-speed-demoted and ceiling-demoted efforts. Assert the exact included id
    set: `act-activity-excluded`, `act-clean` and `act-ceiling`. Keep the "never reads rankings"
    test, but it must now target an effort that really is excluded, because `act-activity-excluded`
    is now included. Add an effort-excluded activity that is ranked 1 in the fixture's `rankings`,
    and assert it is absent. Run the suite and watch the new test fail against the current code.

    Then implement. In `buildFilteredPopulations`:
    - Change `if (!activity || activity.excludedFromRecords) continue;` to `if (!activity) continue;`.
    - After the effort-level exclusion skip, add a skip for
      `effort.demotion?.guard === 'world-record' || effort.demotion?.guard === 'max-speed'`.
    - Rewrite the doc comment. It must state the membership rule as it is now: per-effort exclusion
      only, absolute-guard demotions dropped, ceiling demotions and missing or null demotions kept,
      which is exactly `compute-best-efforts.ts` Pass 1's `byDistance` population. It must also
      say why: since D-08 the shipped `efforts` array retains absolute-guard demotions, so
      "already post-isPlausible" is no longer true.

    Change nothing else in the calibration script. `CEILING_K` must still come out of
    `deriveCeilingMultiplier` untouched (Phase 27 D-02's anti-quota rule).

    Do NOT run `npm run compute-pr-ceiling-calibration` in this plan; `28-CEILING-CALIBRATION.md`
    must stay byte-identical until plan 28-14. Run `npm run build` first, since the test imports
    from `../dist/`.

    Commit: `fix(28-12): calibration population mirrors Pass 1 (drops absolute-guard demotions, per-effort exclusion)`.
  </action>
  <verify>
    <automated>npm run build && npx vitest run scripts/compute-pr-ceiling-calibration.test.mjs scripts/compute-pr-ceiling-diff.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/compute-pr-ceiling-calibration.test.mjs` exits 0, and the SUMMARY names the test that failed before the implementation
    - `grep -c "activity.excludedFromRecords" scripts/compute-pr-ceiling-calibration.mjs` prints 0
    - `grep -c "already post-" scripts/compute-pr-ceiling-calibration.mjs` prints 0
    - `grep -cE "'world-record'|'max-speed'" scripts/compute-pr-ceiling-calibration.mjs` is at least 2
    - `git status --porcelain .planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md` is empty, and `git log -1 --format=%h -- .planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md` still prints `e1d6aa4e`
  </acceptance_criteria>
  <done>The calibration population matches Pass 1 by construction, so a future regeneration cannot drift from production.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live archive → committed review artifact | Activity ids and strings from the archive land in committed markdown the developer signs off. |
| generator → artifact of record | A hand-edit to generated output reverts silently; only generator changes persist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-12-A | Tampering | new owner-excluded table | mitigate | Ids rendered through `safeActivityId`, numeric cells through `toFixed`, per T-28-07-B; test covers a malformed id. |
| T-28-12-B | Repudiation | sign-off embedded in a generated file | mitigate | D-14: the generator emits no sign-off text; acceptance criterion greps for it. |
| T-28-12-C | Tampering | premature regeneration invalidating the signed diff before the fix lands | mitigate | Both generators' `main()` are forbidden to run in this plan; sha256 and git-status acceptance criteria prove both artifacts unchanged. |
| T-28-12-D | Denial of service | temp dir leak (~1,860 files per run) | mitigate | `try/finally rmSync` (IN-04). |
| T-28-12-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed. |
</threat_model>

<verification>
- `npm run build && npx vitest run scripts/compute-pr-ceiling-diff.test.mjs scripts/compute-pr-ceiling-calibration.test.mjs` exits 0.
- `28-DIFF.md` sha256 is unchanged and `28-CEILING-CALIBRATION.md` is unmodified.
</verification>

<success_criteria>
- WR-03, WR-04, IN-01 (diff script) and IN-04 are closed in the generators.
- The diff can now show a reviewer every owner-excluded effort the CR-01 fix newly ceiling-demotes, as generated text.
</success_criteria>

<output>
Create `.planning/phases/28-pr-plausibility-ceiling/28-12-SUMMARY.md` listing the new report fields, the new section's exact heading and table header, the tests updated on purpose (with the old versus new assertion), and the confirmation that neither artifact was regenerated.
</output>
