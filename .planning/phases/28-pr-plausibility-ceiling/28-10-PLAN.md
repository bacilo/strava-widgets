---
phase: 28-pr-plausibility-ceiling
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/compute-pr-ceiling-recount.mjs
  - scripts/compute-pr-ceiling-recount.test.mjs
autonomous: true
gap_closure: true
requirements: [PR-04, PR-05]

must_haves:
  decisions: [D-04, D-08, D-10, D-15]
  truths:
    - "The recount FAILS when the pinned fixture 4556693525@400m is present but its demotion guard is not 'ceiling', or its durationSec is not 45.2 (WR-05, D-04)"
    - "The recount walks EVERY effort in the shipped document and compares its own arithmetic TARGET/durationSec against doc.ceilings[d].ceilingMps, with distance meters declared locally and no import of any ceiling/compute/utils/types module (D-15)"
    - "The recount FAILS when any effort is over its distance's ceiling with demotion null, and when any effort carries guard 'ceiling' while its implied speed is NOT over the ceiling (both directions)"
    - "The recount derives an independent ceiling count (over-ceiling AND not absolute-guard-demoted) and FAILS when it differs from its own byGuard.ceiling count"
    - "Run against the PRE-FIX shipped document (generatedAt 2026-09-10T23:08:32.377Z) the recount exits 1 and names exactly the 13 efforts 28-VERIFICATION.md lists (11 at 400m, 2 at 1k), reports independent ceiling count 31 against byGuard.ceiling 18, and reports the pinned-fixture problem"
  artifacts:
    - path: "scripts/compute-pr-ceiling-recount.mjs"
      provides: "recountCeilingSweep pure function, --best-efforts/--index path flags, pinned-fixture and sweep problems in evaluateReport"
      contains: "recountCeilingSweep"
    - path: "scripts/compute-pr-ceiling-recount.test.mjs"
      provides: "failing-first tests for the sweep, both directions, and the pinned-fixture verdict"
      contains: "recountCeilingSweep"
  key_links:
    - from: "scripts/compute-pr-ceiling-recount.mjs evaluateReport"
      to: "report.sweep.overCeilingWithoutDemotion"
      via: "a problem string pushed when the list is non-empty"
      pattern: "overCeilingWithoutDemotion"
    - from: "scripts/compute-pr-ceiling-recount.mjs evaluateReport"
      to: "report.demoted.pinnedFixture.guardIsCeiling"
      via: "a problem string pushed when present && !guardIsCeiling"
      pattern: "guardIsCeiling"
---

<objective>
Give the classifier-independent recount the teeth D-15 requires (WR-05, IN-03), and demonstrate those
teeth against the real pre-fix archive before the CR-01 fix lands.

Purpose: 28-VERIFICATION.md gap 1 and gap 3. The recount printed `guardIsCeiling=false` and still
reported PASS, and it never checked any effort's speed against the ceiling. So "18 = 18" was two
artifacts with the same blind spot agreeing with each other. After this plan, the recount FAILS on the
committed pre-fix document. It also produces an independently derived ceiling count (31 on this
snapshot) that plan 28-14 reconciles the regenerated diff against.

Output: an updated recount script and tests. The SUMMARY records the pre-fix FAIL output verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-pr-plausibility-ceiling/28-VERIFICATION.md
@.planning/phases/28-pr-plausibility-ceiling/28-REVIEW.md
@.planning/phases/28-pr-plausibility-ceiling/28-08-SUMMARY.md
@scripts/compute-pr-ceiling-recount.mjs
@scripts/compute-pr-ceiling-recount.test.mjs

<interfaces>
Current exports of scripts/compute-pr-ceiling-recount.mjs (read the file; do not re-derive):
- `readShippedJson(path) -> { ok: true, doc } | { ok: false, reason }`
- `recountDemoted(bestEffortsDoc) -> { ownDemotedTotal, byGuard: { 'world-record', 'max-speed', ceiling, unrecognisedGuards[] }, byDistance, ownRejectedNonErrorRows, rankedButDemotedIds[], demotedWithoutReason[], disagreesWithTotals{...}, pinnedFixture }`
  - `pinnedFixture` is `{ present: false, note }` or `{ present: true, durationSec, guard, durationMatches45_2, guardIsCeiling }`
- `recountImpossibleSampleCohort(indexDoc)`, `computeCohortOverlap(cohortIds, bestEffortsDoc)`
- `evaluateReport(report, expectedDemoted, expectedCohort) -> { pass, problems[] }` where `report = { readErrors, demoted, cohort, overlap }`
- `parseExpectFlags(argv) -> { expectDemoted, expectCohort }`
- Constants: `KNOWN_GUARDS`, `PINNED_ACTIVITY_ID = '4556693525'`, `PINNED_DISTANCE = '400m'`, `PINNED_DURATION_SEC = 45.2`
- Main reads `BEST_EFFORTS_PATH = ../data/stats/best-efforts.json` and `DASHBOARD_INDEX_PATH = ../data/dashboard/index.json`

Shipped document shape the sweep reads (from the live file; no type import allowed):
- `doc.ceilings[distanceKey] = { distance, ceilingMps: number|null, p90Mps: number|null, populationN, multiplier, failOpenReason? }`
- `doc.activities[id].efforts[] = { distance, durationSec, excludedFromRecords, demotion: null | { guard, reason }, wasPRAtTheTime, ... }`
- Distance keys and meters (declare LOCALLY; the D-15 header forbids importing best-effort.types): `400m` 400, `1k` 1000, `1mi` 1609.344, `5k` 5000, `10k` 10000, `half` 21097.5, `marathon` 42195
- The ceiling comparison in production is strict: an effort is over the ceiling when `impliedSpeed > ceilingMps`, and there is no ceiling when `ceilingMps` is null. This is a statement of the contract, NOT an import.

Pre-fix figures measured by the planner on 2026-09-16 against the primary working tree's
`/Users/pedf/workspace/strava-widgets/data/stats/best-efforts.json` (generatedAt `2026-09-10T23:08:32.377Z`):
- byGuard: ceiling 18, world-record 19, max-speed 15; ownDemotedTotal 52
- over-ceiling with `demotion: null`: 13. All 13 have `excludedFromRecords: true`:
  `3475711469@400m, 3475711630@400m, 3475715178@400m, 3475725513@1k, 3475726256@400m, 3475727228@400m, 3475732221@400m, 3475735603@400m, 14122328106@400m, 4556693525@400m, 4556693525@1k, 5059204779@400m, 5588316886@400m`
- the independent ceiling count (over the ceiling and not demoted by world-record/max-speed) is 18 + 13 = 31
- ceiling-guard efforts NOT over the ceiling: 0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — tests for the ceiling sweep, the pinned-fixture verdict and the input-path flags</name>
  <files>scripts/compute-pr-ceiling-recount.test.mjs</files>
  <read_first>
    - scripts/compute-pr-ceiling-recount.test.mjs (whole file: the `cleanDemotedReport()` fixture at ~236-255, the `recountDemoted` fixture shapes, the `parseExpectFlags` suite at ~319)
    - scripts/compute-pr-ceiling-recount.mjs (whole file: the D-15 header forbidding imports, `recountDemoted`, `evaluateReport`, `parseExpectFlags`, `main`)
    - .planning/phases/28-pr-plausibility-ceiling/28-REVIEW.md sections WR-05 and IN-03
  </read_first>
  <behavior>
    - recountCeilingSweep on a doc with one excluded 400m effort (durationSec 45.2, demotion null) and `ceilings['400m'].ceilingMps` 5.1098 returns overCeilingWithoutDemotion `['X@400m']`, and independentCeilingCount is 1 with byGuard-independent arithmetic
    - an effort at exactly ceiling speed (implied === ceilingMps) is NOT over the ceiling (strict `>`)
    - an effort with guard 'ceiling' whose implied speed is <= ceilingMps is listed in ceilingDemotedButNotOverCeiling
    - an over-ceiling effort already demoted by 'world-record' or 'max-speed' is in neither list and is NOT counted in independentCeilingCount
    - a distance whose ceilingMps is null (fail-open marathon) contributes nothing, and is listed in failOpenDistances
    - an effort whose distance key has no local meters entry is listed in unevaluable (never silently skipped); a doc with no `ceilings` object yields `ceilingsMissing: true`
    - evaluateReport adds a problem naming every label when overCeilingWithoutDemotion is non-empty; a problem when ceilingDemotedButNotOverCeiling is non-empty; a problem when independentCeilingCount !== demoted.byGuard.ceiling (naming both numbers); a problem when unevaluable is non-empty or ceilingsMissing is true
    - evaluateReport adds a problem when pinnedFixture.present && !guardIsCeiling, a problem when pinnedFixture.present && !durationMatches45_2, and a problem when pinnedFixture.present === false (a missing pinned fixture makes PR-05's check vacuous)
    - recountDemoted(null) and recountDemoted({}) do not throw (IN-03)
    - parseInputPaths([]) returns the two default paths; parseInputPaths(['--best-efforts','/a.json','--index','/b.json']) returns those; a flag with no value throws naming the flag
  </behavior>
  <action>
    Write the tests before any implementation change. Import the new names `recountCeilingSweep` and
    `parseInputPaths` alongside the existing imports. Build each sweep fixture as a hand-written plain
    object with `ceilings` and `activities` only. Do not import any `dist/analytics` module into the
    test either; the recount's test must stay as classifier-free as the script.

    Update `cleanDemotedReport()` so a clean report is still clean under the new rules. Set
    `pinnedFixture` to `{ present: true, durationSec: 45.2, guard: 'ceiling', durationMatches45_2: true, guardIsCeiling: true }`.
    Change every `evaluateReport` call in the file to pass `report.sweep`. A clean sweep is
    `{ overCeilingWithoutDemotion: [], ceilingDemotedButNotOverCeiling: [], independentCeilingCount: 1, unevaluable: [], failOpenDistances: [], ceilingsMissing: false }`,
    which matches the clean report's `byGuard.ceiling` of 1.

    Add one test named `fails on the CR-01 shape: an owner-excluded over-ceiling effort with demotion null`.
    It builds a two-effort document: `4556693525@400m`, excluded, 45.2s, demotion null; and a
    non-excluded `ok@400m`, 90s, demotion null. Its ceilings are `{ '400m': { ceilingMps: 5.1098, p90Mps: 3.992, populationN: 1825 } }`.
    Assert that `evaluateReport` over the combined report returns `pass: false`, and that one problem
    contains `4556693525@400m` and another contains `guardIsCeiling`.

    Run `npx vitest run scripts/compute-pr-ceiling-recount.test.mjs`. It must FAIL, because
    `recountCeilingSweep` and `parseInputPaths` are not exported yet and `evaluateReport` ignores the
    new checks. Record the failing test names in the SUMMARY. Commit only the test file:
    `test(28-10): add failing tests for recount ceiling sweep and pinned-fixture verdict`.
  </action>
  <verify>
    <automated>npx vitest run scripts/compute-pr-ceiling-recount.test.mjs 2>&1 | grep -E "failed|Failed" (expected: at least one failure at this RED step)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recountCeilingSweep" scripts/compute-pr-ceiling-recount.test.mjs` is at least 6
    - `grep -c "parseInputPaths" scripts/compute-pr-ceiling-recount.test.mjs` is at least 2
    - `grep -n "dist/analytics\|src/analytics" scripts/compute-pr-ceiling-recount.test.mjs` returns nothing
    - `npx vitest run scripts/compute-pr-ceiling-recount.test.mjs` exits non-zero at this commit, and the SUMMARY names the failing tests
    - The RED commit touches only `scripts/compute-pr-ceiling-recount.test.mjs`: `git show --stat HEAD` lists exactly that one file
  </acceptance_criteria>
  <done>Failing tests committed that fix, in advance, what the sweep and verdict must do.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the sweep, the verdict checks and the path flags, then show a FAIL on the real pre-fix archive</name>
  <files>scripts/compute-pr-ceiling-recount.mjs</files>
  <read_first>
    - scripts/compute-pr-ceiling-recount.mjs (whole file)
    - scripts/compute-pr-ceiling-recount.test.mjs (the tests Task 1 committed)
    - .planning/phases/28-pr-plausibility-ceiling/28-VERIFICATION.md "Root Cause" block (the 13 labels)
  </read_first>
  <behavior>
    - All Task 1 tests pass; every pre-existing test in the file still passes
    - Against the pre-fix primary-tree document the CLI exits 1 and names the 13 labels
  </behavior>
  <action>
    Implement against the tests. Keep the D-15 header rule intact: the file keeps zero imports of
    the ceiling module, compute step, best-effort utils or best-effort types, under any spelling.

    1. Add a module constant `TARGET_METERS_LOCAL` mapping the seven keys to their meters (listed in
       the interfaces block). Its comment must say it is declared locally on purpose, per D-15.
    2. Add the exported pure function `recountCeilingSweep(bestEffortsDoc)`, with null-safe input
       handling in the same style as `recountDemoted`. If `doc.ceilings` is missing or not an object,
       return early with `ceilingsMissing: true`. Otherwise iterate activity ids in
       `Object.keys(activities).sort()` order, then each activity's efforts in array order. For each
       effort:
       - Look up `meters = TARGET_METERS_LOCAL[effort.distance]` and
         `ceiling = doc.ceilings[effort.distance]?.ceilingMps`.
       - If `meters` is undefined, or `durationSec` is not a positive finite number, push the label
         `${id}@${distance}` to `unevaluable` and move on.
       - If `ceiling` is null or not finite, skip the effort. The distance is fail-open.
       - Otherwise compute `implied = meters / effort.durationSec`, `over = implied > ceiling` and
         `guard = effort.demotion?.guard ?? null`.
       - If `over && guard === null`, push the label to `overCeilingWithoutDemotion`.
       - If `guard === 'ceiling' && !over`, push the label to `ceilingDemotedButNotOverCeiling`.
       - If `over` and `guard` is neither `'world-record'` nor `'max-speed'`, increment
         `independentCeilingCount`.
       Collect `failOpenDistances` from `Object.keys(doc.ceilings).sort()` where `ceilingMps` is null.
       Return
       `{ overCeilingWithoutDemotion, ceilingDemotedButNotOverCeiling, independentCeilingCount, unevaluable, failOpenDistances, ceilingsMissing: false, perDistanceOverCeilingWithoutDemotion }`.
       The last field maps each distance key to its count of over-ceiling efforts with no demotion.
    3. Extend `evaluateReport(report, expectedDemoted, expectedCohort)` to read `report.sweep`. When
       the sweep is present, add these problems:
       - `${n} over-ceiling effort(s) carry no demotion (CR-01 shape): ${labels.join(', ')}`
       - `${n} ceiling-guard demotion(s) whose implied speed is not over the ceiling: ...`
       - `independent ceiling count (${independentCeilingCount}) disagrees with byGuard.ceiling (${byGuard.ceiling})`,
         only when both `sweep` and `demoted` are present
       - one problem each for a non-empty `unevaluable` and for `ceilingsMissing`
       When `demoted` is present, add these pinned-fixture problems:
       - `pinned fixture 4556693525@400m is absent from the shipped document (PR-05 check is vacuous)`
       - `pinned fixture 4556693525@400m guard is ${JSON.stringify(guard)}, not "ceiling" (guardIsCeiling=false)`
       - `pinned fixture 4556693525@400m durationSec is ${d}, not 45.2 (D-04)`
       When `report.sweep` is absent but `report.demoted` is present, add the problem
       `ceiling sweep was not run`, so the sweep can never be skipped silently.
    4. IN-03: in `recountDemoted`, use `bestEffortsDoc?.rejected` and `bestEffortsDoc?.totals`.
    5. Add the exported `parseInputPaths(argv)`. It accepts `--best-efforts <path>` and
       `--index <path>`, defaults to `BEST_EFFORTS_PATH` and `DASHBOARD_INDEX_PATH`, and throws
       `${flag} requires a path argument` when a flag has no value. Wire it into `main()` next to
       `parseExpectFlags`. `parseExpectFlags` must ignore these new flags; it already only looks up
       its own names. The script stays read-only; the header's "Writes nothing, ever" still holds.
    6. In `main()`, compute `sweep` when the best-efforts read succeeded and add it to `report`.
       Print a `Ceiling sweep (own arithmetic, doc.ceilings vs TARGET/durationSec):` block with:
       - `independentCeilingCount`
       - the count and full label list of `overCeilingWithoutDemotion`
       - the per-distance counts
       - the `ceilingDemotedButNotOverCeiling` count
       - `failOpenDistances`
       - the unevaluable count
       Update the header comment. Replace "Only read targets" with a statement that the default read
       targets can be overridden by the two flags. Add a paragraph saying the sweep is the
       classifier-independent check that catches CR-01's shape: an over-ceiling effort with no
       demotion.
    7. Run `npx vitest run scripts/compute-pr-ceiling-recount.test.mjs`. It must pass.
    8. Demonstrate the FAIL on the real archive. Precondition: `node -e` shows that
       `/Users/pedf/workspace/strava-widgets/data/stats/best-efforts.json` has `generatedAt`
       `2026-09-10T23:08:32.377Z`. If it shows anything else, the pre-fix document has been
       regenerated: STOP and report, and do not continue. When the precondition holds, run
       `node scripts/compute-pr-ceiling-recount.mjs --best-efforts /Users/pedf/workspace/strava-widgets/data/stats/best-efforts.json --index /Users/pedf/workspace/strava-widgets/data/dashboard/index.json`.
       The absolute primary-tree paths make this work from a worktree, where `data/stats/` is
       gitignored and absent. Expect exit code 1 and a FAIL block containing:
       - the over-ceiling problem naming exactly the 13 labels in the interfaces block, with no
         additions and no omissions
       - `independent ceiling count (31) disagrees with byGuard.ceiling (18)`
       - the pinned-fixture `guardIsCeiling=false` problem
       Paste the full stdout and stderr verbatim into the SUMMARY. Do NOT run `compute-all-stats`,
       `compute-best-efforts` or any regeneration in this plan.

    Commit: `feat(28-10): recount sweeps every effort against doc.ceilings and fails on the pinned fixture`.
  </action>
  <verify>
    <automated>npx vitest run scripts/compute-pr-ceiling-recount.test.mjs && node scripts/compute-pr-ceiling-recount.mjs --best-efforts /Users/pedf/workspace/strava-widgets/data/stats/best-efforts.json --index /Users/pedf/workspace/strava-widgets/data/dashboard/index.json; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/compute-pr-ceiling-recount.test.mjs` exits 0
    - `grep -vE '^\s*(\*|//)' scripts/compute-pr-ceiling-recount.mjs | grep -cE "import .*(best-effort-ceiling|compute-best-efforts|best-effort-utils|best-effort\.types)"` prints 0
    - `grep -c "export function recountCeilingSweep" scripts/compute-pr-ceiling-recount.mjs` is 1 and `grep -c "export function parseInputPaths" scripts/compute-pr-ceiling-recount.mjs` is 1
    - `grep -c "guardIsCeiling" scripts/compute-pr-ceiling-recount.mjs` is at least 3 (computed, printed, AND used in `evaluateReport`)
    - `grep -c "bestEffortsDoc?.rejected" scripts/compute-pr-ceiling-recount.mjs` is 1 and `grep -c "bestEffortsDoc?.totals" scripts/compute-pr-ceiling-recount.mjs` is 1
    - The pre-fix CLI run exits 1; its output contains all 13 labels from the interfaces block and no other `@400m` or `@1k` label in the over-ceiling problem line; it contains `independent ceiling count (31) disagrees with byGuard.ceiling (18)`; and it contains `guardIsCeiling=false`
    - `git status --porcelain data/` is empty (nothing under `data/` was written)
    - The SUMMARY quotes the pre-fix CLI output verbatim
  </acceptance_criteria>
  <done>The recount can now fail on the pinned fixture and on CR-01's shape, and it has been shown failing on the real pre-fix archive with the independently expected 13/31/18 figures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped JSON on disk → recount verdict | The recount's value depends entirely on not sharing code or assumptions with the classifier it checks. |
| CLI path flags → file read | New `--best-efforts` / `--index` flags accept operator-supplied paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-10-A | Tampering | a verifier that agrees with itself | mitigate | Distance meters declared locally; the grep acceptance criterion proves zero imports of ceiling/compute/utils/types; the sweep recomputes speed from `durationSec` rather than reading any demotion as truth, and checks both directions (missing demotion AND unsupported demotion). |
| T-28-10-B | Repudiation | a silently skipped check | mitigate | `evaluateReport` raises a problem when the sweep did not run, when `ceilings` is missing, when any effort is unevaluable, and when the pinned fixture is absent — no vacuous PASS path. |
| T-28-10-C | Information disclosure / Tampering | path flags | accept | Read-only `readFileSync` + `JSON.parse` of an operator-chosen local path in a developer CLI; nothing is written; failures return a named reason via `readShippedJson`. |
| T-28-10-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed. |
</threat_model>

<verification>
- `npx vitest run scripts/compute-pr-ceiling-recount.test.mjs` exits 0.
- The pre-fix CLI demonstration exits 1 with the 13 labels, `31` vs `18`, and `guardIsCeiling=false`.
- `git status --porcelain data/` is empty.
</verification>

<success_criteria>
- WR-05 closed: the pinned-fixture check and a check of every effort against the ceiling both decide the verdict.
- IN-03 closed.
- The recount has been shown to fail on the shipped pre-fix document, using counts derived independently of the classifier.
</success_criteria>

<output>
Create `.planning/phases/28-pr-plausibility-ceiling/28-10-SUMMARY.md` with the RED test names, the verbatim pre-fix CLI output, and the independent figures (13 missing, independent count 31, byGuard.ceiling 18).
</output>
