---
phase: 28-pr-plausibility-ceiling
plan: 14
type: execute
wave: 2
depends_on: ["28-10", "28-11", "28-12"]
files_modified:
  - .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md
  - .planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md
  - .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md
  - .planning/phases/28-pr-plausibility-ceiling/deferred-items.md
autonomous: true
gap_closure: true
requirements: [PR-03, PR-04, PR-05]

must_haves:
  decisions: [D-04, D-06, D-07, D-12, D-13, D-15]
  truths:
    - "data/stats/best-efforts.json is regenerated by the fixed compute step against the SAME archive snapshot the pre-fix figures came from (manifest, exclusions and ceiling-state blobs pinned)"
    - "Pre-fix and post-fix documents differ in exactly these places: the demotion field of the 13 named efforts (null → guard 'ceiling'), 13 appended rejected rows, totals.effortsDemoted and totals.effortsRejected (52 → 65), and generatedAt. Rankings, every wasPRAtTheTime flag, ceilings and every other totals field are identical"
    - "The fixed recount PASSES on the regenerated document with byGuard.ceiling 31, independentCeilingCount 31, zero over-ceiling efforts without a demotion, and the pinned fixture guard 'ceiling' at 45.2s. These are the figures independently predicted by plan 28-10's pre-fix run (18 + 13)"
    - "The regenerated 28-DIFF.md states a ceiling-only total of 31, lists exactly the 13 predicted owner-excluded efforts, and reconciles 31 = recount byGuard.ceiling = recount independentCeilingCount. Its ranking, flag-flip and retroactive-promotion sections are byte-identical to the signed-off version (sha256 08e93d5a…77c6)"
    - "28-DIFF.md and 28-CEILING-CALIBRATION.md are each regenerated twice, and the two runs differ only on timestamp lines (D-13 idempotence)"
    - "The regenerated calibration differs from the committed one only on its three timestamp lines, and every published ceiling equals doc.ceilings[d].ceilingMps to 4 decimals"
    - "data/best-effort-ceiling.json is not rewritten by the regeneration: the population did not change"
  artifacts:
    - path: ".planning/phases/28-pr-plausibility-ceiling/28-DIFF.md"
      provides: "regenerated archive-wide diff with the owner-excluded ceiling listing"
      contains: "Ceiling demotions on owner-excluded efforts"
    - path: ".planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md"
      provides: "Round 2 pre-checkpoint evidence: pre/post structural diff, recount stdout, diff/calibration idempotence, hashes"
      contains: "Round 2 Evidence"
  key_links:
    - from: "28-DIFF.md ceiling-only total"
      to: "scripts/compute-pr-ceiling-recount.mjs byGuard.ceiling and independentCeilingCount"
      via: "three numbers recorded side by side in 28-VALIDATION.md, predicted before regeneration"
      pattern: "independentCeilingCount"
---

<objective>
Regenerate the shipped document, the archive-wide diff and the calibration artifact from the fixed
code. Every resulting number is checked against a value predicted BEFORE regeneration by a
different path, and the evidence plan 28-15's checkpoint will cite is recorded.

Purpose: 28-VERIFICATION.md gap 3. "18 = 18" was two artifacts sharing a blind spot. This plan
predicts 31 in advance, by two routes that do not use the fixed classifier:
- the pre-fix byGuard count (18) plus the sweep's 13 missing efforts
- the sweep's own independent ceiling count (31) on the pre-fix document

The plan then requires the regenerated diff and the post-fix recount to land on 31, and requires
everything the fix should NOT touch (rankings, flags, ceilings) to be byte-identical.

MUST run in the primary working tree `/Users/pedf/workspace/strava-widgets`, not a worktree.
`data/stats/`, `data/dashboard/` and the pre-fix snapshot are gitignored and exist only there.

Output: a regenerated `28-DIFF.md` and `28-CEILING-CALIBRATION.md`, a Round 2 Evidence section in
`28-VALIDATION.md`, and deferred items appended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-pr-plausibility-ceiling/28-VERIFICATION.md
@.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md
@.planning/phases/28-pr-plausibility-ceiling/28-DIFF.md
@.planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md
@.planning/phases/28-pr-plausibility-ceiling/28-10-SUMMARY.md
@.planning/phases/28-pr-plausibility-ceiling/28-11-SUMMARY.md
@.planning/phases/28-pr-plausibility-ceiling/28-12-SUMMARY.md
@.planning/phases/28-pr-plausibility-ceiling/deferred-items.md

<interfaces>
Archive snapshot pins, recorded by the planner on 2026-09-16 (local master was 9 commits BEHIND
origin/master at planning time, all CI data commits):
- `git rev-parse HEAD:data/streams/manifest.json` = `b8ac1a5494ae1384f9c5b67ce42752335fcbb532` (1890 activities, 1865 available)
- `git rev-parse HEAD:data/best-effort-exclusions.json` = `8e942ff6711861a85d62c2e4ed71726968dd0cd0` (12 entries, all `distances: null`)
- `git rev-parse HEAD:data/best-effort-ceiling.json` = `f6b4f15d52672a915912b056758cc0a3dc51bc37`
- Do NOT `git pull` / `git merge origin/master` until plan 28-15 has closed. A CI data merge changes the archive and invalidates every predicted number below.

Pre-fix shipped document `data/stats/best-efforts.json` (generatedAt `2026-09-10T23:08:32.377Z`):
- totals: `{"activitiesConsidered":1865,"activitiesWithEfforts":1864,"effortsComputed":8946,"effortsRejected":52,"effortsExcluded":61,"lowConfidenceEfforts":181,"skippedNoStream":25,"skippedUnreadable":0,"effortsDemoted":52}`
- `sha256(JSON.stringify(doc.rankings))` = `cb2a498a416b29eb7c8ca3826841b3e768d66c7ca0993d135d2782a9369ec9fb`
- ceilings (ceilingMps): 400m 5.1098, 1k 4.7513, 1mi 4.6323, 5k 4.3458, 10k 4.2236, half 4.4017, marathon null (population 0)
- byGuard: ceiling 18 (400m 8, 1k 7, 1mi 3), world-record 19, max-speed 15

Predicted post-fix values (same snapshot):
- effortsDemoted 65, effortsRejected 65, 0 unexpected-error rows; all other totals unchanged
- byGuard: ceiling 31 (400m 19, 1k 9, 1mi 3), world-record 19, max-speed 15; recount ownDemotedTotal 65
- recount sweep: overCeilingWithoutDemotion 0, ceilingDemotedButNotOverCeiling 0, independentCeilingCount 31
- pinned 4556693525@400m: durationSec 45.2, guard `ceiling`, reason `implied 8.85 m/s exceeds personal ceiling 5.11 m/s (1.28 x p90 3.99 m/s over 1825 filtered 400m efforts)`
- pinned 4556693525@1k: durationSec 207.4, guard `ceiling`, reason `implied 4.82 m/s exceeds personal ceiling 4.75 m/s (1.28 x p90 3.71 m/s over 1843 filtered 1k efforts)`
- 3475725513@400m keeps guard `world-record` (reason `implied 27.32 m/s exceeds world-record pace 9.30 m/s`); 3475725513@1k becomes `ceiling` (`implied 6.72 m/s exceeds personal ceiling 4.75 m/s (1.28 x p90 3.71 m/s over 1843 filtered 1k efforts)`)

Predicted regenerated 28-DIFF.md (plan 28-12's renderer):
- Summary bullets: ceiling-only total 31; `Of those, also owner-excluded (no ranking effect): 13`; flag flips 14; retroactive promotions 3; ranking rows moved 48; archive size 1865
- Summary table rows (Distance | Ceiling | Demoted (ceiling) | Of which owner-excluded | Flags before | Flags after | Flags flipped):
  400m 5.1098 | 19 | 11 | 11 | 6 | 5; 1k 4.7513 | 9 | 2 | 11 | 9 | 6; 1mi 4.6323 | 3 | 0 | 14 | 13 | 3; 5k 4.3458 | 0 | 0 | 21 | 21 | 0; 10k 4.2236 | 0 | 0 | 16 | 16 | 0; half 4.4017 | 0 | 0 | 6 | 6 | 0; marathon (fail-open text) | 0 | 0 | 0 | 0 | 0
- `## Ceiling demotions on owner-excluded efforts`, exactly these 13 rows in this order (Activity | Distance | Duration (s) | Implied (m/s) | Ceiling (m/s)):
  14122328106 400m 62.1 6.4412 5.1098; 3475711469 400m 57.5 6.9565 5.1098; 3475711630 400m 58.2 6.8729 5.1098; 3475715178 400m 47.6 8.4034 5.1098; 3475726256 400m 44.0 9.0909 5.1098; 3475727228 400m 46.5 8.6022 5.1098; 3475732221 400m 54.6 7.3260 5.1098; 3475735603 400m 55.5 7.2072 5.1098; 4556693525 400m 45.2 8.8496 5.1098; 5059204779 400m 60.3 6.6335 5.1098; 5588316886 400m 65.5 6.1069 5.1098; 3475725513 1k 148.9 6.7159 4.7513; 4556693525 1k 207.4 4.8216 4.7513
- Reconciliation: 31 ceiling-only, document total 65
- Sections `## Records that changed hands`, `## PR-at-the-time flag flips`, `## Retroactive promotions`, `## Inputs`: byte-identical to the signed-off file

Observation to carry to the checkpoint, NOT to investigate (D-04 forbids an open-ended
investigation): the owner-excluded `3475726256@400m` is exactly 44.0s / 9.0909 m/s, the stale
PR-05/ROADMAP figure D-04 recorded as unexplained. The likely explanation is that the requirement
quoted that activity's value against the wrong id. D-04's pinned fixture (4556693525, 45.2s) stays
as locked.

Predicted regenerated 28-CEILING-CALIBRATION.md (plan 28-12's population rule): identical to the
committed file except the `**Generated:**` line and the two `generatedAt:` input lines. The
planner verified that a Pass-1-faithful population reproduces every n/p90/max in the committed
tables.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the snapshot, preserve the pre-fix document, regenerate, and prove the change is exactly the 13 efforts</name>
  <files>.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md</files>
  <read_first>
    - .planning/phases/28-pr-plausibility-ceiling/28-10-SUMMARY.md (pre-fix recount FAIL output)
    - .planning/phases/28-pr-plausibility-ceiling/28-11-SUMMARY.md (representation decision and precedence rule)
    - .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md "Round 1 Checkpoint" and "AMENDED" sections (format to follow)
    - src/analytics/compute-best-efforts.ts (the fixed PASS 3, to know which fields may change)
  </read_first>
  <action>
    1. Preconditions. Stop and report on any failure; do not "fix forward".
       - `git rev-parse --show-toplevel` prints `/Users/pedf/workspace/strava-widgets`, and
         `git rev-parse --git-dir` prints `.git` (so this is not a worktree).
       - The three blob hashes in the interfaces block match `git rev-parse HEAD:<path>`.
       - `git status --porcelain data/` is empty.
       - `data/stats/best-efforts.json` has generatedAt `2026-09-10T23:08:32.377Z`.
       - Plans 28-10, 28-11 and 28-12 have SUMMARY files.
    2. Snapshot the pre-fix document: `SNAP=$(mktemp -d)`, then copy `data/stats/best-efforts.json`
       and `data/dashboard/index.json` into it. Record `$SNAP` and both sha256 values in the SUMMARY.
    3. Pre-fix independent prediction, re-run on the snapshot:
       `node scripts/compute-pr-ceiling-recount.mjs --best-efforts $SNAP/best-efforts.json --index $SNAP/index.json`.
       Expect exit 1, with byGuard.ceiling 18, independentCeilingCount 31 and 13 missing labels.
       The prediction is 18 + 13 = 31. Record it.
    4. Regenerate: `npm run build && npm run compute-all-stats`.
       - Confirm `git status --porcelain data/best-effort-ceiling.json` is empty. The file must not
         be rewritten, because the population did not change.
       - Confirm the console reports `unchanged at every distance` for ceiling movement.
       - If the ceiling state WAS rewritten, or movement is reported, the archive or population
         differs from the snapshot. STOP and report.
    5. Structural pre/post comparison, using a throwaway node script in `$SNAP` that is never
       committed. Load both documents and assert:
       - `doc.rankings` are deep-equal, and the post rankings' sha256 equals
         `cb2a498a416b29eb7c8ca3826841b3e768d66c7ca0993d135d2782a9369ec9fb`.
       - `doc.ceilings` are deep-equal.
       - The activity key sets are equal.
       - For every effort, the JSON with the `demotion` key removed is identical, so
         `wasPRAtTheTime`, `excludedFromRecords`, `durationSec` and every other field are
         unchanged.
       - The set of efforts whose `demotion` differs is exactly the 13 labels in the interfaces
         block, each going from null to guard `ceiling`.
       - Post `rejected` equals pre `rejected` plus 13 rows as a multiset, with those 13 rows'
         `(activityId, distance)` matching the 13 labels.
       - `totals` differ only in `effortsDemoted` and `effortsRejected` (52 → 65).
       - The count of shard files in `data/stats/best-efforts/` is unchanged.
       - Shard `data/stats/best-efforts/4556693525.json`'s 400m and 1k demotions match the predicted
         guard and reason strings EXACTLY, and 3475725513's shard shows 400m `world-record` and 1k
         `ceiling` with the predicted reasons.
       Print a PASS/FAIL line per assertion, and paste the output verbatim into the SUMMARY.
    6. Post-fix recount: `node scripts/compute-pr-ceiling-recount.mjs --expect-demoted 65`. Expect:
       - exit 0 and `PASS`
       - byGuard ceiling 31, world-record 19, max-speed 15
       - independentCeilingCount 31, overCeilingWithoutDemotion 0
       - pinned `guard="ceiling"`, `durationMatches45_2=true`, `guardIsCeiling=true`
       - the cohort count and denominator printed; plan 28-08 recorded 662 of 1,890, so state any
         drift as a number
       Record the full stdout verbatim.
    7. Append a `## Round 2 Evidence (gap closure 28-10..28-14)` section to `28-VALIDATION.md`
       containing:
       - the snapshot pins
       - the pre-fix recount FAIL summary (18 / 31 / 13, with the labels)
       - the pre/post structural comparison output
       - the post-fix recount stdout
       - a three-column table: predicted before regeneration | observed | source, for ceiling-only
         (31), world-record (19), max-speed (15), total demoted (65), independentCeilingCount (31),
         over-ceiling without demotion (0), and pinned guard
       - the D-04 observation about `3475726256` (44.0s / 9.0909 m/s), worded as an observation, not
         a finding
       Do not touch the Round 1 sections, the AMENDED section or the frontmatter.

    Commit: `docs(28-14): regenerate best efforts from the CR-01 fix and record the pre/post evidence`.
    The regenerated `data/stats/` is gitignored and is not committed.
  </action>
  <verify>
    <automated>node scripts/compute-pr-ceiling-recount.mjs --expect-demoted 65 && git diff --quiet -- data/best-effort-ceiling.json && grep -c "Round 2 Evidence" .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Every precondition in step 1 held and is recorded in the SUMMARY; if any failed, the plan stopped and says so
    - The pre/post comparison reports PASS on every assertion, and its verbatim output is in 28-VALIDATION.md
    - `node scripts/compute-pr-ceiling-recount.mjs --expect-demoted 65` exits 0, and its output contains `ceiling:      31`, `independentCeilingCount` with value 31, and `guardIsCeiling=true`
    - `git diff --quiet -- data/best-effort-ceiling.json` exits 0
    - `node -e "const d=require('./data/stats/best-efforts.json');process.exit(d.totals.effortsDemoted===65&&d.totals.effortsExcluded===61?0:1)"` exits 0
    - 28-VALIDATION.md contains `## Round 2 Evidence`, and its predicted-vs-observed table has no row where the two values differ. If any differs, the plan stopped with the discrepancy recorded instead.
  </acceptance_criteria>
  <done>The shipped document is regenerated from the fixed code, and the change is shown to be exactly the 13 predicted efforts and nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate 28-DIFF.md and 28-CEILING-CALIBRATION.md twice each, and reconcile against the independent figures</name>
  <files>.planning/phases/28-pr-plausibility-ceiling/28-DIFF.md, .planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md, .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md</files>
  <read_first>
    - .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md (the signed-off version, sha256 08e93d5a…77c6, BEFORE regeneration)
    - .planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md (committed version, before regeneration)
    - .planning/phases/28-pr-plausibility-ceiling/28-12-SUMMARY.md (the renderer's new heading and table header)
  </read_first>
  <action>
    Diff artifact:
    1. Before regenerating, confirm `shasum -a 256 .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md`
       prints `08e93d5adec6ee3de886ab8b47ac0d4a48e001847e331c18830a812f546f77c6`. Copy the file to
       `$SNAP/28-DIFF.signed.md`.
    2. Run `npm run compute-pr-ceiling-diff`, copy the output to `$SNAP/28-DIFF.run1.md`, then run
       it again. Idempotence: `diff <(grep -v '^\*\*Generated:\*\*' $SNAP/28-DIFF.run1.md) <(grep -v '^\*\*Generated:\*\*' .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md)`
       must print nothing. Confirm that no `pr-ceiling-diff-*` temp directory remains under
       `os.tmpdir()` from these runs (IN-04).
    3. Check the predicted content (interfaces block) line by line: the Summary bullets, all seven
       table rows, the 13 owner-excluded rows in the stated order, and the Reconciliation numbers
       (31 and 65).
    4. Check the unchanged sections. Extract the `## Records that changed hands`,
       `## PR-at-the-time flag flips`, `## Retroactive promotions` and `## Inputs` sections from
       `$SNAP/28-DIFF.signed.md` and from the regenerated file, each running from its heading up to
       the next `## ` heading, and require byte equality for all four.
    5. Reconcile three numbers: the diff's ceiling-only total (31), the recount's
       `byGuard.ceiling` (31) and the recount's `independentCeilingCount` (31). Also check that the
       diff's 13 owner-excluded labels equal, as a set, the 13 labels plan 28-10's pre-fix sweep
       reported. That sweep was produced by a different program from a different document.
    6. Record the regenerated file's sha256. This is the hash plan 28-15's fresh sign-off binds to.

    Calibration artifact:
    7. Run `npm run compute-pr-ceiling-calibration` twice. Idempotence: the files from the two runs
       must differ only on the `**Generated:**` line.
    8. Filter out the `**Generated:**` line and the two `generatedAt:` lines under `## Inputs`. The
       remaining `git diff` of the regenerated file against HEAD must be empty. If any other line
       differs, the WR-03 prediction failed: STOP, run `git checkout -- .planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md`
       to restore the committed file, and record the differing lines as a finding. Never hand-edit
       the file to match.
    9. Cross-check that, for every distance, the calibration's "Resulting coverage and demotions"
       `Ceiling (m/s)` cell equals `doc.ceilings[d].ceilingMps.toFixed(4)` from the regenerated
       `data/stats/best-efforts.json`, and that marathon shows the no-ceiling text. Record the
       seven-row comparison.

    Record everything in `28-VALIDATION.md` under `## Round 2 Evidence`:
    - both idempotence results
    - the unchanged-section equality results
    - the three-way reconciliation (31 / 31 / 31) and the 13-label set equality
    - the new 28-DIFF.md sha256
    - the calibration timestamp-only diff and the ceiling cross-check

    Commit: `docs(28-14): regenerate 28-DIFF.md and calibration from fixed generators (idempotent, reconciled 31/31/31)`.
  </action>
  <verify>
    <automated>test "$(grep -c '^| .* | 400m | .* | 5.1098 |$' .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md)" -eq 11 && grep -q "Ceiling demotions on owner-excluded efforts" .planning/phases/28-pr-plausibility-ceiling/28-DIFF.md && node scripts/compute-pr-ceiling-recount.mjs --expect-demoted 65</automated>
  </verify>
  <acceptance_criteria>
    - Both idempotence diffs print nothing, and the results are recorded
    - The regenerated 28-DIFF.md contains the Summary row `| 400m | 5.1098 | 19 | 11 | 11 | 6 | 5 |` and `| 1k | 4.7513 | 9 | 2 | 11 | 9 | 6 |`, contains `| 4556693525 | 400m | 45.2 | 8.8496 | 5.1098 |` and `| 3475725513 | 1k | 148.9 | 6.7159 | 4.7513 |`, and has exactly 13 rows in the owner-excluded table
    - The four unchanged sections are byte-equal to the signed-off version (recorded as four PASS lines)
    - The three-way reconciliation 31/31/31 and the 13-label set equality are recorded in 28-VALIDATION.md
    - `git diff -U0 HEAD -- .planning/phases/28-pr-plausibility-ceiling/28-CEILING-CALIBRATION.md | grep -E '^[+-][^+-]' | grep -vE 'Generated:|generatedAt:'` prints nothing, or the plan stopped, restored the file and recorded the finding
    - The new 28-DIFF.md sha256 is recorded, and it differs from `08e93d5a…77c6`
    - No `pr-ceiling-diff-*` directory newer than this plan's start remains in the OS temp directory
  </acceptance_criteria>
  <done>The diff and calibration artifacts are regenerated by fixed generators, shown idempotent, and reconciled to numbers predicted before regeneration.</done>
</task>

<task type="auto">
  <name>Task 3: Record the Per-Task map rows for 28-10..28-15 and log what was deliberately not folded in</name>
  <files>.planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md, .planning/phases/28-pr-plausibility-ceiling/deferred-items.md</files>
  <read_first>
    - .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md "Per-Task Verification Map" table (column format)
    - .planning/phases/28-pr-plausibility-ceiling/deferred-items.md (format)
    - .planning/phases/28-pr-plausibility-ceiling/28-REVIEW.md WR-06 and IN-05
  </read_first>
  <action>
    In `28-VALIDATION.md`'s Per-Task Verification Map, append one row per task of plans 28-10
    through 28-15 in the existing column format, each with the exact automated command from that
    task's verify block. Leave the frontmatter unchanged; plan 28-15 owns `status` and
    `nyquist_compliant`.

    Append a `## Gap closure 28-10..28-15 (not folded in)` section to `deferred-items.md` listing
    each of these with its reason:
    - WR-06, second half (making the ceiling-state write opt-in, e.g. CI-only): it changes a D-07
      mechanism, so the developer must decide it. A CI-only gate that silently stops writing would
      be a gate that fails quietly. The comments were corrected in 28-11. Local runs still rewrite
      `data/best-effort-ceiling.json` when the population, p90 or ceiling moves. The workaround is
      to leave that file uncommitted after local runs.
    - IN-05 (`LOW_CONFIDENCE_BADGE_TEXT` pulls `list.ts` into `records-logic.ts`'s import graph):
      a purity refactor of `list.ts`, a file this round does not otherwise touch, with no behaviour
      impact.
    - WR-01's non-empty This-year path is unit-tested only. No 2026 effort is in any all-time top-10
      today, so every This-year table renders its empty state, and a browser row could not tell the
      fixed code from the broken code.
    - R4 (the ceiling-emptied table) is still NOT EXERCISABLE: no distance has zero ranked rows and
      a positive demoted count.
    - A pre-existing case, not from Phase 28: a distance whose every effort is owner-excluded renders
      "No {label} efforts yet", which reads as if no effort was recorded.
    - Merging `origin/master` (9 CI data commits behind at planning time) is held until plan 28-15
      closes, so the snapshot stays pinned. Merge after the checkpoint, and never rebase.

    Commit: `docs(28-14): record gap-closure task map and deferred items`.
  </action>
  <verify>
    <automated>grep -c "28-1[0-5]" .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md && grep -c "Gap closure 28-10..28-15" .planning/phases/28-pr-plausibility-ceiling/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - The Per-Task Verification Map has at least one row for each of 28-10, 28-11, 28-12, 28-13, 28-14 and 28-15
    - deferred-items.md contains `## Gap closure 28-10..28-15 (not folded in)`, and that section contains `WR-06`, `IN-05`, `WR-01`, `R4` and `origin/master`
    - `git diff HEAD~1 -- .planning/phases/28-pr-plausibility-ceiling/28-VALIDATION.md | grep -E '^-[^-]'` prints nothing (additions only)
  </acceptance_criteria>
  <done>The verification map covers the gap round, and every finding not folded in is logged with its reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nightly CI data → local archive | A merge from origin changes the archive under the prediction. |
| generator → signed artifact | A regenerated artifact must be proven to differ from the signed one only where predicted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-14-A | Tampering | archive drift between prediction and regeneration | mitigate | Three blob pins and a no-merge rule; ceiling-state non-rewrite and "unchanged at every distance" as tripwires. |
| T-28-14-B | Repudiation | self-agreeing reconciliation | mitigate | 31 predicted before regeneration from the pre-fix document via two routes (18 + 13, and the sweep's independent count), then matched by the regenerated diff; the 13-label set compared across two different programs and documents. |
| T-28-14-C | Tampering | hand-edited generated artifact | mitigate | Idempotence proven by two runs each; on unexpected calibration diffs the file is restored with `git checkout`, never hand-edited. |
| T-28-14-D | Tampering | fix touching more than intended | mitigate | Full pre/post structural comparison: only 13 demotion fields, 13 rejected rows and two totals may change. |
| T-28-14-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed. |
</threat_model>

<verification>
- The pre/post structural comparison passes on every assertion.
- `node scripts/compute-pr-ceiling-recount.mjs --expect-demoted 65` exits 0.
- Both artifacts are idempotent, and the diff reconciles 31/31/31.
- `git diff --quiet -- data/best-effort-ceiling.json` exits 0.
</verification>

<success_criteria>
- The corrected shipped document, the diff and the calibration artifact exist.
- Every number the checkpoint will cite was predicted before regeneration and matched after it.
</success_criteria>

<output>
Create `.planning/phases/28-pr-plausibility-ceiling/28-14-SUMMARY.md` with the snapshot path and hashes, the pre/post comparison output, both recount outputs, the predicted-vs-observed table, the new 28-DIFF.md sha256, and the calibration verdict (timestamp-only diff, or stopped and restored).
</output>
