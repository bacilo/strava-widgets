---
phase: 28-pr-plausibility-ceiling
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/views/records-logic.ts
  - src/dashboard/views/records-logic.test.ts
  - src/dashboard/views/records.ts
  - src/dashboard/views/records.test.ts
  - src/dashboard/views/detail-best-efforts-logic.ts
  - src/dashboard/views/detail-best-efforts-logic.test.ts
  - src/dashboard/styles.css
  - src/dashboard/styles.test.ts
autonomous: true
gap_closure: true
requirements: [PR-03]

must_haves:
  decisions: [D-03, D-09, D-10]
  truths:
    - "The Records demotion note attributes every demotion to the guard that made it. It counts only efforts the owner did not exclude, reports a per-guard breakdown, and never credits world-record or max-speed demotions to 'the plausibility ceiling' (CR-02)"
    - "On the shipped archive, the 400m note reads 35 efforts (8 personal ceiling, 17 world-record pace guard, 10 activity max-speed guard); 1K reads 11 (7/1/3); 1 Mile reads 5 (3 ceiling, 2 max-speed); 5K, 10K, half and marathon show no note"
    - "The all-time empty-state heading says 'passed the plausibility ceiling' only when the ceiling-guard count is above zero; otherwise it names the plausibility guards generically (CR-02, latent marathon case)"
    - "Under the 'This year' scope no demotion note renders, matching the empty state's existing rule (WR-01)"
    - "A demoted-and-excluded effort renders two separate badges, Demoted then Excluded, each with its own description id, and the demoted badge's explanation no longer uses the word 'excluded' (IN-02, D-09, D-10)"
    - ".badge--demoted text meets 4.5:1 against both --bg and --surface in BOTH themes. It is asserted by a computed contrast test, not only by a 'differs from severe' check (WR-02)"
  artifacts:
    - path: "src/dashboard/views/records-logic.ts"
      provides: "DemotionCounts, per-guard countDemotedAtDistance, guard-accurate resolvePrTableDemotionNote/resolvePrTableEmptyState"
      contains: "DemotionCounts"
    - path: "src/dashboard/styles.css"
      provides: "--demoted-text token declared per theme, used by .badge--demoted"
      contains: "--demoted-text"
  key_links:
    - from: "src/dashboard/views/records.ts renderTables"
      to: "countDemotedAtDistance / resolvePrTableDemotionNote"
      via: "counts object plus currentScope passed through buildPrTableSection"
      pattern: "resolvePrTableDemotionNote\\("
    - from: "src/dashboard/styles.css .badge--demoted"
      to: "var(--demoted-text)"
      via: "per-theme token"
      pattern: "--demoted-text"
---

<objective>
Close CR-02 (misattributed Records copy), and fold in WR-01 (all-time count under the This-year
scope), WR-02 (dark-theme badge contrast) and IN-02 (the word "excluded" in the demoted badge's
explanation). All four sit in files this phase already owns.

Purpose: the Records screen told readers that 36 400m efforts were "demoted by the plausibility
ceiling" when only 8 were. That screen is the one a reviewer consults before signing off
(28-VERIFICATION.md criterion 5). Also, once CR-01 is fixed, the owner-excluded efforts gain
ceiling demotions. Without the exclusion filter below, the note would jump to a number that mixes the
owner's intent with the machine's judgment, which D-10 forbids.

Copy decision, stated once and pinned by tests: a guard-neutral sentence with an exact per-guard
breakdown, counted over efforts the owner did not exclude. This combines the review's two options: it
is guard-neutral, and the ceiling-only count stays visible inside it. Register: named condition plus
measured value (Phase 27 D-09).

Output: updated logic, rendering, styles and tests. No data regeneration here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-pr-plausibility-ceiling/28-REVIEW.md
@.planning/phases/28-pr-plausibility-ceiling/28-04-SUMMARY.md
@src/dashboard/views/records-logic.ts
@src/dashboard/views/detail-best-efforts-logic.ts

<interfaces>
From src/dashboard/views/records-logic.ts (current):
- `export function countDemotedAtDistance(activities: BestEffortsDocument['activities'], distance: TargetDistanceKey): number` (~159) — counts `effort.demotion != null`, any guard, excluded included
- `export function resolvePrTableEmptyState(distance, scope: RecordScope, year: number, demotedCount: number): { heading; body }` (~190) — this-year branch and all-time-zero branch are verbatim-pinned copy that MUST NOT change:
  - this-year: heading `No ${label} efforts in ${year}`, body `The archive has no ${label} effort recorded in ${year}. Switch to All time to see every ranked effort.`
  - all-time zero: heading `No ${label} efforts yet`, body `The archive has no completed ${label} effort. Once one is recorded, its rank will appear here.`
- `export function resolvePrTableDemotionNote(distance, demotedCount: number): string | null` (~222)
- `export type RecordScope = 'all-time' | 'this-year'`
- labels come from `DISTANCE_DISPLAY_NAMES`: `'400m'`, `'1K'`, `'1 Mile'`, `'5K'`, ...

From src/dashboard/views/records.ts (current):
- `buildPrTableEmptyState(distance, scope, year, demotedCount)` (~347) → `resolvePrTableEmptyState(...)`
- `buildPrTableSection(distance, rows, empty, scope, year, demotedCount)` — `resolvePrTableDemotionNote(distance, demotedCount)` at ~591, rendered as `<p class="text-label">`
- `renderTables(currentScope)` (~677): `const demotedCount = countDemotedAtDistance(bestEfforts.activities, distance);`
- records.test.ts source audits: `countOccurrences(buildPrTableSectionBody, 'resolvePrTableDemotionNote(')` === 1; `countOccurrences(renderTablesBody, 'countDemotedAtDistance(')` === 1 — keep both true

From src/dashboard/views/detail-best-efforts-logic.ts:
- `prFlagBadgeSpecs(row, exclusionReason)` emits specs in order pr, demoted, low-confidence, excluded; demoted spec explanation currently `'this effort is excluded from the ranked PR list because a plausibility guard rejected it; it stays visible here with its reason'`

Contrast facts (W3C relative luminance, computed by the planner):
- light theme `--bg #ffffff`, `--surface #f5f5f7`; dark theme `--bg #1a1a2e`, `--surface #242444`
- `#b3390a` (current light `--accent-strong`): 5.99 on #ffffff, 5.50 on #f5f5f7 — passes
- `#c2410c` (current dark `--accent-strong`): 3.29 on #1a1a2e, 2.87 on #242444 — FAILS
- `#fb923c`: 7.54 on #1a1a2e, 6.58 on #242444 — passes
- styles.css declares tokens in `:root` (fallback), `:root[data-theme="light"]` (~87), `:root[data-theme="dark"]` (~103); `--accent-strong` is documented "pagination + segmented control only, never used elsewhere"

Shipped-archive per-guard counts over NON-excluded efforts (planner-measured 2026-09-16; unchanged by CR-01's fix, which only touches excluded efforts):
- 400m: ceiling 8, world-record 17, max-speed 10 → 35
- 1k: ceiling 7, world-record 1, max-speed 3 → 11
- 1mi: ceiling 3, max-speed 2 → 5
- 5k, 10k, half, marathon: 0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard-accurate Records counts and copy (CR-02) and scope-aware note (WR-01)</name>
  <files>src/dashboard/views/records-logic.ts, src/dashboard/views/records-logic.test.ts, src/dashboard/views/records.ts, src/dashboard/views/records.test.ts</files>
  <read_first>
    - src/dashboard/views/records-logic.ts lines 1-40 and 140-240
    - src/dashboard/views/records-logic.test.ts lines 480-600 (the three suites being rewritten, plus their `fixtureActivity`/`fixtureEffort` helpers)
    - src/dashboard/views/records.ts lines 330-370, 560-600, 670-690
    - src/dashboard/views/records.test.ts lines 1-80 (source-text audits)
    - .planning/phases/28-pr-plausibility-ceiling/28-REVIEW.md CR-02 and WR-01
  </read_first>
  <behavior>
    - countDemotedAtDistance returns `{ total, ceiling, worldRecord, maxSpeed }`. It skips efforts with `excludedFromRecords === true`, counts only the matching distance, treats a missing `demotion` key as not demoted (T-28-02-A), and does not throw on it.
    - For a fixture with one non-excluded ceiling demotion, one non-excluded world-record demotion, and one EXCLUDED ceiling demotion at 400m, the result is `{ total: 2, ceiling: 1, worldRecord: 1, maxSpeed: 0 }`.
    - resolvePrTableDemotionNote('400m', 'all-time', { total: 35, ceiling: 8, worldRecord: 17, maxSpeed: 10 }) returns EXACTLY `35 400m efforts were demoted by a plausibility guard (8 by the personal ceiling, 17 by the world-record pace guard, 10 by the activity max-speed guard). Efforts the owner excluded are not counted here. See the activity detail view for each reason.`
    - resolvePrTableDemotionNote('1mi', 'all-time', { total: 5, ceiling: 3, worldRecord: 0, maxSpeed: 2 }) returns EXACTLY `5 1 Mile efforts were demoted by a plausibility guard (3 by the personal ceiling, 2 by the activity max-speed guard). Efforts the owner excluded are not counted here. See the activity detail view for each reason.` Zero-count parts are omitted, and the order is fixed: ceiling, world-record, max-speed.
    - With total 1 and only world-record 1, it returns `1 1K effort was demoted by a plausibility guard (1 by the world-record pace guard). …`, with singular agreement.
    - It returns null when total is 0, and null for scope 'this-year' whatever the counts (WR-01).
    - The resolvePrTableEmptyState this-year branch and all-time-zero branch are byte-identical to today's copy.
    - resolvePrTableEmptyState all-time with ceiling > 0 gives heading `No ${label} efforts passed the plausibility ceiling`. With total > 0 and ceiling === 0 it gives heading `No ${label} efforts passed the plausibility guards`. In both cases the body is the same guard-accurate sentence the note uses.
    - The records.ts audits still hold: `resolvePrTableDemotionNote(` occurs once in buildPrTableSection and `countDemotedAtDistance(` once in renderTables. The note call now receives `scope`.
  </behavior>
  <action>
    Tests first: rewrite the three suites to the behavior above, using exact-string assertions for
    the copy. Run them and watch them fail. Then implement.

    In `records-logic.ts`:
    - Export `interface DemotionCounts { total: number; ceiling: number; worldRecord: number; maxSpeed: number }`.
    - Change `countDemotedAtDistance` to return `DemotionCounts`. It skips `effort.excludedFromRecords === true`
      and switches on `effort.demotion.guard`: `'ceiling'`, `'world-record'`, `'max-speed'`. Any
      other value counts toward `total` only. Rewrite its doc comment to say the count excludes
      owner-excluded efforts because D-10 keeps the owner's intent and the machine's judgment
      apart.
    - Add a private helper, `describeDemotionCounts(label, counts)`, that builds the shared sentence
      so the note and the empty-state body cannot drift apart. Part labels, verbatim:
      `by the personal ceiling`, `by the world-record pace guard`,
      `by the activity max-speed guard`. The trailing sentences, verbatim:
      `Efforts the owner excluded are not counted here. See the activity detail view for each reason.`
    - Change `resolvePrTableDemotionNote(distance, scope, counts)` to return null when
      `scope === 'this-year'` or `counts.total === 0`. Update its doc comment to cite WR-01: the
      note's archive-wide count would otherwise sit under a year-filtered table.
    - Change `resolvePrTableEmptyState(distance, scope, year, counts)`. Its last branch's heading is
      gated on `counts.ceiling > 0`, as described in the behavior block. Keep the two pinned
      branches verbatim.

    In `records.ts`:
    - Thread `counts: DemotionCounts` in place of `demotedCount: number` through
      `buildPrTableEmptyState` and `buildPrTableSection`, and rename the local in `renderTables` to
      `demotionCounts`.
    - Pass `scope` into `resolvePrTableDemotionNote`.
    - Update the doc comments that mention `demotedCount`.
    - Keep exactly one call site of each audited function.
    - Adjust `records.test.ts` only if a signature-level audit needs it; the two occurrence-count
      audits must still pass unchanged.

    Commit: `fix(28-13): Records demotion copy attributes each guard and skips owner-excluded efforts (CR-02, WR-01)`.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/records-logic.test.ts src/dashboard/views/records.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/views/records-logic.test.ts src/dashboard/views/records.test.ts` exits 0, apart from the pre-existing environment-only ENOENT in records-logic.test.ts listed in deferred-items.md, if the worktree lacks `data/stats`. Name it in the SUMMARY if it occurs.
    - `npx tsc --noEmit` exits 0
    - `grep -c "demoted by the plausibility ceiling" src/dashboard/views/records-logic.ts` prints 0
    - `grep -c "by the personal ceiling" src/dashboard/views/records-logic.ts` prints 1 (the single shared helper)
    - `grep -c "export interface DemotionCounts" src/dashboard/views/records-logic.ts` prints 1
    - records-logic.test.ts contains the exact 400m string `35 400m efforts were demoted by a plausibility guard (8 by the personal ceiling, 17 by the world-record pace guard, 10 by the activity max-speed guard). Efforts the owner excluded are not counted here. See the activity detail view for each reason.`
    - `grep -cF 'No ${label} efforts in ${year}' src/dashboard/views/records-logic.ts` prints 1, and `grep -c "Once one is recorded, its rank will appear here." src/dashboard/views/records-logic.ts` prints 1 (the pinned branches are untouched)
  </acceptance_criteria>
  <done>The Records note and empty state attribute each guard correctly, leave out owner-excluded efforts, and stay silent under the This-year scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dark-theme demoted-badge contrast (WR-02) and the demoted badge's wording (IN-02)</name>
  <files>src/dashboard/styles.css, src/dashboard/styles.test.ts, src/dashboard/views/detail-best-efforts-logic.ts, src/dashboard/views/detail-best-efforts-logic.test.ts</files>
  <read_first>
    - src/dashboard/styles.css lines 1-130 (token blocks) and 370-392 (`.badge--demoted` and its comment)
    - src/dashboard/styles.test.ts lines 1-120 (helpers: `declarationsFor`, `cssNoComments`) and 2380-2410 (existing `.badge--demoted` suite)
    - src/dashboard/views/detail-best-efforts-logic.ts lines 200-300 (`prFlagBadgeSpecs`)
    - src/dashboard/views/detail-best-efforts-logic.test.ts (the `prFlagBadgeSpecs` suite, and any assertion on the demoted explanation string)
    - .planning/phases/28-pr-plausibility-ceiling/28-REVIEW.md WR-02 and IN-02
  </read_first>
  <behavior>
    - The styles test extracts `--demoted-text`, `--bg` and `--surface` from the `:root[data-theme="light"]` block and from the `:root[data-theme="dark"]` block. It computes the W3C contrast ratio with its own luminance function in the test and asserts each of the four pairs is >= 4.5. Against the current file it would FAIL, since `--demoted-text` does not exist and dark would give 2.87.
    - `.badge--demoted` declares `color: var(--demoted-text)` and `border-color: var(--demoted-text)`, and no longer references `--accent-strong`.
    - The styles test asserts `--accent-strong` is referenced by no `.badge--demoted` rule, keeping that token's documented contract.
    - prFlagBadgeSpecs for a row that is demoted (reason `implied 8.85 m/s exceeds personal ceiling 5.11 m/s (1.28 x p90 3.99 m/s over 1825 filtered 400m efforts)`) AND excluded (reason `bad measurement`), and not a PR, returns exactly two specs in order:
      - kind `demoted`, visibleText `Demoted — implied 8.85 m/s exceeds personal ceiling 5.11 m/s (1.28 x p90 3.99 m/s over 1825 filtered 400m efforts)`
      - kind `excluded`, visibleText `Excluded — bad measurement`
      Their `descriptionIdSuffix` values differ.
    - The demoted spec's explanation is exactly `this effort is left out of the ranked PR list because a plausibility guard rejected it; it stays visible here with its reason`, and does not contain the substring `excluded`.
  </behavior>
  <action>
    Tests first, then implement.

    In `styles.css`:
    - Declare `--demoted-text` in `:root` and in `:root[data-theme="light"]` with value `#b3390a`,
      and in `:root[data-theme="dark"]` with value `#fb923c`.
    - Give it a comment citing WR-02 and the measured ratios: light 5.99 / 5.50, dark 7.54 / 6.58.
    - Point `.badge--demoted` at `var(--demoted-text)` for both `color` and `border-color`.
    - Rewrite the rule's comment so it no longer claims to reuse `--accent-strong`, and say why a
      dedicated token exists: `--accent-strong` fails AA as dark-theme text, and its own contract
      reserves it for fills.

    In `styles.test.ts`:
    - Add a WR-02 suite with its own small hex-to-luminance helper, local to the test.
    - The suite must read the token values from the real stylesheet (the existing `cssNoComments`
      source). Hard-coding the hex values in the test is not allowed, because the test must fail if
      someone edits the token to a failing colour.

    In `detail-best-efforts-logic.ts`, replace the demoted explanation string with the exact IN-02
    text above. Update any test pinning the old string, and add the two-badge demoted+excluded test.

    Commit: `fix(28-13): dedicated demoted-badge token meets AA in both themes; demoted explanation avoids "excluded" (WR-02, IN-02)`.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts src/dashboard/views/detail-best-efforts-logic.test.ts src/dashboard/views/detail-sections.test.ts && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/styles.test.ts src/dashboard/views/detail-best-efforts-logic.test.ts src/dashboard/views/detail-sections.test.ts` exits 0
    - `grep -c -- "--demoted-text" src/dashboard/styles.css` is at least 5 (three declarations and two uses)
    - `awk '/\.badge--demoted[[:space:]]*\{/,/\}/' src/dashboard/styles.css | grep -c "accent-strong"` prints 0
    - `grep -c "is excluded from the ranked PR list" src/dashboard/views/detail-best-efforts-logic.ts` prints 0, and `grep -c "is left out of the ranked PR list because a plausibility guard rejected it" src/dashboard/views/detail-best-efforts-logic.ts` prints 1
    - The SUMMARY names the WR-02 contrast test and states that it fails when `#c2410c` is substituted for the dark token. Verify this by temporarily editing the value, running the test, and reverting. Record both runs.
    - `npm test` exits 0, apart from the pre-existing environment-only failures deferred-items.md lists, each named in the SUMMARY; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The demoted badge is readable in dark mode, a computed contrast test backs that up, and its screen-reader text no longer overloads the word "excluded".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped best-efforts document → user-facing copy | The Records note states counts about a named mechanism; wrong attribution is user-facing misinformation (Phase 27 D-09 register). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-13-A | Spoofing (misattribution) | Records demotion note | mitigate | Per-guard counts with exact-string tests; the shared helper prevents note/empty-state drift. |
| T-28-13-B | Tampering | stale shard without `demotion` | mitigate | `!= null` / missing-key handling kept and tested (T-28-02-A). |
| T-28-13-C | Repudiation | owner intent merged into machine count | mitigate | Owner-excluded efforts skipped in the count (D-10), tested with an excluded ceiling-demoted fixture. |
| T-28-13-D | Denial of service (accessibility) | dark-theme badge | mitigate | Dedicated token plus a computed contrast test reading the real stylesheet. |
| T-28-13-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed. |
</threat_model>

<verification>
- `npx vitest run src/dashboard/views/records-logic.test.ts src/dashboard/views/records.test.ts src/dashboard/styles.test.ts src/dashboard/views/detail-best-efforts-logic.test.ts` exits 0.
- `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- CR-02, WR-01, WR-02 and IN-02 are closed, each backed by a test that fails against the old behaviour.
- The exact note strings plan 28-15's R6 re-check expects are pinned in tests.
</success_criteria>

<output>
Create `.planning/phases/28-pr-plausibility-ceiling/28-13-SUMMARY.md` recording the copy decision, the exact note and empty-state templates, the token values and ratios, and the contrast test's substitution run (FAIL with #c2410c, PASS restored).
</output>
