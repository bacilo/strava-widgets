---
phase: 14-stream-ingestion-foundation
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/exports/geometry-readers.ts
  - src/exports/geometry-readers.test.ts
  - src/streams/stream-manifest.ts
  - src/streams/stream-manifest.test.ts
autonomous: true
requirements: [STREAM-01, STREAM-03]

must_haves:
  truths:
    - "A FIT original yields per-record heart rate, cadence, distance, altitude and timestamp, not just position"
    - "A GPX original yields per-point time and elevation, not just position"
    - "The two gzipped GPX originals in the archive can be read instead of throwing"
    - "D-04: Availability for every activity lives in one committed central manifest (data/streams/manifest.json) that both ingestion paths write to — channels present plus a reason code when unavailable, no per-activity stub files"
    - "Re-running an ingestion path against unchanged input produces no manifest diff"
  artifacts:
    - path: "src/exports/geometry-readers.ts"
      provides: "Multi-channel raw sample extraction from FIT and GPX originals"
      exports: ["readFit", "readGpx", "readGpxText", "readOriginal", "fitRecordsToSamples"]
    - path: "src/exports/geometry-readers.test.ts"
      provides: "CI-safe coverage using synthetic records and GPX text, no export_data dependency"
    - path: "src/streams/stream-manifest.ts"
      provides: "Central availability manifest read/merge/write"
      exports: ["loadManifest", "upsertAvailable", "upsertUnavailable", "saveManifest", "emptyManifest"]
    - path: "src/streams/stream-manifest.test.ts"
      provides: "Idempotency and reason-code coverage"
  key_links:
    - from: "src/exports/geometry-readers.ts"
      to: "src/streams/stream.types.ts"
      via: "returns RawSample[] on OriginalRecording"
      pattern: "RawSample"
    - from: "src/streams/stream-manifest.ts"
      to: "src/storage/file-store.ts"
      via: "FileStore.writeJson atomic temp-then-rename"
      pattern: "writeJson"
---

<objective>
Extend the existing FIT/GPX readers from position-only to multi-channel, and build the central availability manifest both ingestion paths write to.

Purpose: `readFit`/`readGpx` currently extract only `positionLat`/`positionLong`, and `readOriginal` throws outright on the archive's two `.gpx.gz` files. CONTEXT.md D-04 requires a single committed manifest as the source of truth for availability, so list views get badges without per-activity fetches.
Output: extended `geometry-readers.ts` with a CI-safe test suite, plus `stream-manifest.ts` with idempotent read-merge-write.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-stream-ingestion-foundation/14-CONTEXT.md
@.planning/phases/14-stream-ingestion-foundation/14-RESEARCH.md
@.planning/phases/14-stream-ingestion-foundation/14-PATTERNS.md
@.planning/phases/14-stream-ingestion-foundation/14-01-SUMMARY.md

<interfaces>
From `src/streams/stream.types.ts` (plan 14-01 — implement against these, do not redefine):
- `interface RawSample { tEpochS: number; distanceM?: number; hr?: number; cadenceRawRpm?: number; fractionalCadence?: number; altM?: number; lat?: number; lng?: number }`
- `interface StreamManifest { schemaVersion: 1; generated_at: string; note: string; totals: {...}; activities: Record<string, StreamManifestEntry> }`
- `type StreamUnavailableReason = 'manual' | 'no-original' | 'treadmill' | 'unreadable-original' | 'no-samples'`
- `interface StreamChannels { time: true; distance: boolean; hr: boolean; cadence: boolean; elevation: boolean }`

From `src/storage/file-store.ts` (existing — reuse, do not reimplement):
- `class FileStore { constructor(private baseDir: string) }`
- `async writeJson(filePath: string, data: unknown): Promise<void>` — temp-file-then-rename, creates parent dirs
- `async readJson<T>(filePath: string): Promise<T>` — throws `File not found: <path>` on ENOENT
- `async exists(filePath: string): Promise<boolean>`
- `async listFiles(dirPath: string, extension?: string): Promise<string[]>` — returns `[]` on ENOENT

From `src/exports/geometry-readers.ts` (the file being extended — current state):
- `export interface OriginalRecording { coordinates: [number, number][]; startEpoch?: number }`
- `readFit` loops `messages.recordMesgs ?? []` guarding only `typeof rec.positionLat === 'number'`
- `readGpx` matches `/<trkpt\s+lat="(-?[\d.]+)"\s+lon="(-?[\d.]+)"/g` and one `<time>` for `startEpoch`
- `readOriginal` currently throws `gzipped gpx not implemented` for `.gpx.gz`
- `gunzipSync` from `node:zlib` is already imported at line 1
- `const SEMICIRCLE = 180 / 2 ** 31`

Central-index doc convention to mirror, from `src/exports/consolidate.ts`:
- `{ generated_at, note, sources, archive_total, archive_without_original, activities: Record<id, entry> }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the FIT and GPX readers to multi-channel raw samples</name>
  <files>src/exports/geometry-readers.ts, src/exports/geometry-readers.test.ts</files>
  <read_first>
    - src/exports/geometry-readers.ts (the file being modified — current position-only implementation and the `.gpx.gz` throw at line 81)
    - src/streams/stream.types.ts (the `RawSample` shape being produced)
    - .planning/phases/14-stream-ingestion-foundation/14-RESEARCH.md (Code Examples section supplies the exact verified FIT field names; Pitfall 2 confirms all 38 archive GPX files are extension-free)
    - .planning/phases/14-stream-ingestion-foundation/14-PATTERNS.md (the geometry-readers section: raw fields belong here, normalization belongs in derive-stream)
    - src/exports/csv.test.ts (test style and granularity)
  </read_first>
  <behavior>
    - `fitRecordsToSamples` maps a record carrying `timestamp`, `distance`, `heartRate`, `cadence`, `fractionalCadence`, `enhancedAltitude`, `positionLat`, `positionLong` into a fully-populated `RawSample`
    - `fitRecordsToSamples` prefers `enhancedAltitude` over `altitude` when both are present
    - `fitRecordsToSamples` omits `cadenceRawRpm` entirely for records with no cadence field — the 2017 device-generation case
    - `fitRecordsToSamples` skips records with no usable `timestamp`
    - `fitRecordsToSamples` passes cadence through UNDOUBLED — the `×2` belongs to derive-stream
    - `fitRecordsToSamples` converts semicircle positions using `SEMICIRCLE`
    - `readGpxText` extracts `lat`/`lng` and a `tEpochS` per `<trkpt>` from synthetic GPX text
    - `readGpxText` extracts `<ele>` into `altM` when present and omits `altM` when absent
    - `readGpxText` yields no `distanceM` on any sample — archive GPX carries no distance field
    - `readGpxText` returns an empty samples array for text with no `<trkpt>` elements rather than throwing
    - `readOriginal` no longer throws for a `.gpx.gz` path
  </behavior>
  <action>
Extend `src/exports/geometry-readers.ts` in place. Widen `OriginalRecording` with an added `samples: RawSample[]` field alongside the existing `coordinates` and `startEpoch`, importing `RawSample` from `../streams/stream.types.js`. Existing callers (`consolidate.ts` line 203 uses only `recording.coordinates`) must keep working unchanged — do not rename or remove `coordinates`.

Add `export function fitRecordsToSamples(records: unknown[]): RawSample[]` as a pure, exported function so it is testable without a real FIT file (`export_data/` is gitignored and absent from CI). It extracts, per record, using the field names verified by live decode in RESEARCH.md's Code Examples: `timestamp` (a `Date`; convert to epoch seconds via `Math.floor(getTime()/1000)`; skip the record when absent or not a Date), `distance`, `heartRate`, `cadence` into `cadenceRawRpm`, `fractionalCadence`, `enhancedAltitude` falling back to `altitude` into `altM`, and `positionLat`/`positionLong` multiplied by `SEMICIRCLE` into `lat`/`lng`. Guard every field with the existing `typeof x === 'number'` convention and omit the key when the guard fails — do not substitute defaults. Emit cadence RAW and undoubled; PATTERNS.md is explicit that the `×2` normalization lives only in `derive-stream.ts` so cadence-unit logic exists in exactly one place.

Rewrite `readFit` to build `coordinates` and `samples` from one pass over `messages.recordMesgs ?? []` by calling `fitRecordsToSamples`, keeping the existing `sessionMesgs[0].startTime` handling for `startEpoch` and the existing partial-decode warning behaviour.

Extract the body of `readGpx` into `export function readGpxText(text: string): OriginalRecording` and make `readGpx` a thin wrapper that reads the file and delegates. In `readGpxText`, replace the current two-regex approach with a single per-`<trkpt>` block scan so each point's `lat`, `lon`, `<time>`, and `<ele>` stay associated: match each `<trkpt ...>...</trkpt>` block (and also handle the self-closing `<trkpt ... />` form), then pull `lat`/`lon` from the attributes, `<time>` into `tEpochS`, and `<ele>` into `altM`. Points with no `<time>` fall back to the first point's time plus the point's index in seconds so the series stays usable; if no point has a time at all, still return the coordinates with `samples: []`. Do NOT add regexes for `<hr>`, `<cadence>`, `gpxtpx`, or `extensions` — RESEARCH.md Pitfall 2 grepped all 38 archive activity-GPX files and found zero matches, so those extraction paths would be dead code.

In `readOriginal`, replace the `gzipped gpx not implemented` throw with a gunzip branch: read the file, `gunzipSync` it, `toString('utf-8')`, and pass the result to `readGpxText`. `gunzipSync` is already imported at line 1; no new import is needed.

Create `src/exports/geometry-readers.test.ts` covering the behavior block with synthetic inputs only — plain object literals for `fitRecordsToSamples` and inline GPX strings for `readGpxText`. The suite must not touch `export_data/`, so it runs identically on a CI runner where that directory does not exist.
  </action>
  <verify>
    <automated>npx vitest run src/exports/geometry-readers.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/exports/geometry-readers.test.ts` exits 0 with at least 10 passing tests
    - `npx tsc --noEmit` exits 0
    - `fitRecordsToSamples([{timestamp:new Date(0),cadence:87,fractionalCadence:0.5}])` returns `[{tEpochS:0,cadenceRawRpm:87,fractionalCadence:0.5}]` — cadence undoubled
    - `fitRecordsToSamples([{timestamp:new Date(0),heartRate:150}])[0].cadenceRawRpm === undefined`
    - `readGpxText('<trkpt lat="55.7" lon="12.5"><ele>12.3</ele><time>2020-01-01T10:00:00Z</time></trkpt>')` returns one sample with `lat`, `lng`, `altM: 12.3`, and a numeric `tEpochS`, and `distanceM === undefined`
    - `readOriginal` invoked on a path ending `.gpx.gz` does not throw an error containing `not implemented`
    - `npx vitest run` (full suite) exits 0 — `consolidate.ts`'s existing use of `recording.coordinates` is unbroken
    - Reading a real archive original succeeds: `npm run build && node -e "import('./dist/exports/geometry-readers.js').then(async m=>{const r=await m.readOriginal('export_data/strava/activities/18353469270.fit.gz');console.log(r.samples.length, r.samples[0])})"` prints a sample count above 500 and a first sample carrying `tEpochS`
  </acceptance_criteria>
  <done>FIT and GPX originals both yield `RawSample[]`; the two `.gpx.gz` archive files are readable; the existing geometry consumer is unbroken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the central stream availability manifest</name>
  <files>src/streams/stream-manifest.ts, src/streams/stream-manifest.test.ts</files>
  <read_first>
    - src/streams/stream.types.ts (the `StreamManifest`, `StreamManifestEntry`, `StreamUnavailableReason` contracts)
    - src/storage/file-store.ts (the atomic `writeJson`/`readJson` primitives to reuse rather than reimplement)
    - src/exports/consolidate.ts (lines 248-274: the `doc` shape and summary-counts convention this manifest mirrors)
    - .planning/phases/14-stream-ingestion-foundation/14-CONTEXT.md (D-04 defines the manifest as the single source of truth)
  </read_first>
  <behavior>
    - `loadManifest` returns an empty manifest when the file does not exist, rather than throwing
    - `loadManifest` returns the parsed manifest when the file exists
    - `upsertAvailable` records source, distanceSource, sampleCount and channels for an id
    - `upsertUnavailable` records `available:false` plus a reason code for an id
    - An upsert overwrites an existing entry for the same id rather than appending a duplicate
    - `saveManifest` writes activity ids in sorted order, so the serialized output is byte-stable across runs
    - `saveManifest` recomputes `totals.activities`, `totals.with_streams`, `totals.without_streams`, and `totals.by_reason` from the entries
    - `saveManifest` preserves the previous `generated_at` when nothing about the entries changed, so a no-op re-run produces a byte-identical file
    - `saveManifest` updates `generated_at` when any entry changed
  </behavior>
  <action>
Create `src/streams/stream-manifest.ts` exporting:

- `emptyManifest(): StreamManifest` — `schemaVersion: STREAM_SCHEMA_VERSION`, `generated_at: ''`, a `note` string reading `Stream availability index for data/streams/. Written by both backfill-streams and the daily intervals.icu sync. available:false means no data/streams/<id>.json exists for that activity.`, zeroed `totals`, empty `activities`.
- `async loadManifest(fileStore: FileStore, manifestPath: string): Promise<StreamManifest>` — delegates to `fileStore.readJson`, catching the `File not found` case and returning `emptyManifest()`. A parse failure on an existing file must throw, not silently reset — losing 1,867 availability entries to a swallowed error is worse than a loud failure.
- `upsertAvailable(manifest: StreamManifest, id: string, entry: { source: StreamSource; distanceSource: DistanceSource; sampleCount: number; channels: StreamChannels }): void` — mutates `manifest.activities[id]`.
- `upsertUnavailable(manifest: StreamManifest, id: string, reason: StreamUnavailableReason): void`.
- `async saveManifest(fileStore: FileStore, manifestPath: string, manifest: StreamManifest): Promise<void>`.

`saveManifest` must: rebuild `totals` from `manifest.activities` (`activities` = entry count, `with_streams` = entries with `available: true`, `without_streams` = the remainder, `by_reason` = a count per reason code among unavailable entries); serialize `activities` with keys inserted in `localeCompare` sorted order so re-runs are diff-stable; and set `generated_at` to `new Date().toISOString()` ONLY when the sorted-entries payload differs from what is already on disk — otherwise carry the on-disk `generated_at` forward. Without that guard a no-op re-run still dirties git, which defeats the D-02 inspect-before-commit flow. Persist via `fileStore.writeJson(manifestPath, doc)` — reuse the existing atomic temp-then-rename primitive rather than writing a second one.

Create `src/streams/stream-manifest.test.ts` covering the behavior block. Use a real `FileStore` rooted at a temp directory created with `fs.mkdtempSync(path.join(os.tmpdir(), 'stream-manifest-'))` and removed in an `afterEach`, so the atomic-write path is exercised for real rather than mocked. Assert byte-stability by writing twice with unchanged entries and comparing the two file contents with `toEqual` on the raw strings.
  </action>
  <verify>
    <automated>npx vitest run src/streams/stream-manifest.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/streams/stream-manifest.test.ts` exits 0 with at least 8 passing tests
    - `npx tsc --noEmit` exits 0
    - `loadManifest` against a nonexistent path resolves to an object with `activities` equal to `{}` and does not reject
    - Writing a manifest, then calling `saveManifest` again with identical entries, produces two byte-identical files (asserted by string comparison of the file contents)
    - After `upsertUnavailable(m, '123', 'manual')` and `saveManifest`, the written JSON has `activities["123"].available === false` and `activities["123"].reason === "manual"` and `totals.by_reason.manual === 1`
    - Upserting the same id twice leaves `Object.keys(manifest.activities).length === 1`
    - The written JSON's `activities` keys appear in sorted order (asserted against `Object.keys(parsed.activities)`)
    - `npx vitest run` (full suite) exits 0
  </acceptance_criteria>
  <done>Both ingestion paths have one idempotent, atomically-written availability index to write into.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `export_data/` original file → reader | Untrusted binary/XML of unknown provenance and vintage, 1,841 files |
| existing `manifest.json` on disk → loader | Prior-run output; corruption must not silently destroy 1,867 availability records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Denial of Service | `readGpxText` / `fitRecordsToSamples` | mitigate | Malformed input returns empty/partial samples rather than throwing; a text blob with no `<trkpt>` yields `samples: []`. Per-file try/catch at the call site is added in plan 14-03. |
| T-14-02 | Tampering | `fitRecordsToSamples` | mitigate | Every field guarded with `typeof === 'number'` and omitted on failure; no default substitution. Numeric range checks are applied downstream in `derive-stream.ts` (plan 14-01). |
| T-14-07 | Tampering | `loadManifest` | mitigate | A JSON parse failure on an existing manifest throws rather than returning `emptyManifest()`, so a corrupt file cannot silently wipe availability for the whole archive. |
| T-14-08 | Information Disclosure | reader output path | mitigate | Readers only read under `export_data/`; nothing in this plan writes there. `export_data/` remains gitignored. |
| T-14-SC | Tampering | npm installs | accept | Zero new packages this phase (RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0 across the whole suite
- `readOriginal` succeeds on a real `.fit.gz` and a real `.gpx.gz` from `export_data/strava/activities/`
- `data/streams/manifest.json` is NOT created by this plan — only the helpers that will write it
</verification>

<success_criteria>
FIT and GPX originals both produce `RawSample[]` carrying time, distance (FIT only), heart rate, raw cadence, and altitude driven by actual field presence; the two `.gpx.gz` files no longer throw; and a central manifest module provides idempotent, atomic, sorted, diff-stable read-merge-write with reason codes.
</success_criteria>

<output>
Create `.planning/phases/14-stream-ingestion-foundation/14-02-SUMMARY.md` when done
</output>
