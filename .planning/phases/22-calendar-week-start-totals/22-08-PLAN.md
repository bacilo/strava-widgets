---
phase: 22-calendar-week-start-totals
plan: 08
type: execute
wave: 8
depends_on: ["22-06", "22-07"]
gap_closure: true
files_modified:
  - .planning/phases/22-calendar-week-start-totals/22-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CAL-01, CAL-02, CAL-03]
must_haves:
  decisions: [D-03, D-10, D-16, GC-1, GC-2, GC-3]
  truths:
    - "GC-3: CAL-02 cannot tick without rendered evidence. Round 1 row R11's FAIL was a visual judgement by the developer's own eyes, so no automated CSS assertion can discharge it — Round 2 re-asks the same narrow-viewport question against the FIXED build and CAL-02's tick is gated strictly on that verdict"
    - "GC-3: the R-numbering continues at R12 under a `## Round 2` heading, following the 21-08 precedent named in 22-05-SUMMARY.md:117. The verdict token stays `R22-VERDICT` and the file ends with 17 of them (11 from Round 1, 6 from Round 2 across rows R12..R17)"
    - "GC-3: the Round 1 house-rule-1 waiver (R6/R7/R8/R10 observed by agent browser automation) is NOT carried forward. Every Round 2 row's observer is the developer's own eyes; no agent DOM read, no computed-style dump and no `npm test` result may fill an Observation cell"
    - "GC-2: Gap 2's fix is observed in a real browser, not only unit-tested — a throwing `localStorage` GETTER is installed on the live page and the Calendar route is mounted against it, the one path none of Round 1's eleven rows exercised"
    - "The staged build is proven FRESH before any row is judged: the served bundle filename differs from Round 1's `assets/index-YqJHQsHW.js`, the served CSS carries `.calendar-weekday--total` and the GC-1 compaction inside a 380px block, and the served JS carries the `calendar-weekday--total` class string — the staged-build cache trap that has bitten this project twice"
    - "D-16 discipline holds in Round 2: row R14 READS THE OCTOBER 2025 MONDAY-START WEEK TOTALS BACK against the table already recorded in the Round 1 preamble, so a regression introduced by 22-06/22-07 to the grid math or the total cell cannot hide behind `the totals are there`"
    - "No source file is edited during the checkpoint session; `git status --porcelain src scripts` is empty at the end"
    - "Requirement ticks follow the Round 2 row map exactly: CAL-02 ticks only if R12, R13 and R14 all PASS; CAL-01's tick survives only if R15 PASSes, following the Phase 19 UI-01/UI-02 precedent for reverting a ticked requirement when a gap-closure row fails; CAL-03 is confirm-unregressed by R17 and is not re-gated"
  artifacts:
    - path: ".planning/phases/22-calendar-week-start-totals/22-VALIDATION.md"
      provides: "a `## Round 2` section — freshness preamble, carried-forward house rules with the no-waiver clause, a six-row agenda R12..R17, the recorded outcome and a Gap Closure Record"
      contains: "## Round 2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CAL-01/CAL-02/CAL-03 states matching the Round 2 row map"
      contains: "CAL-02"
  key_links:
    - from: "the re-served build at 127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10"
      to: "row R13"
      via: "the developer narrowing the viewport to ~380px and reading day-cell values back"
      pattern: "R22-VERDICT"
    - from: "a throwing localStorage getter installed in devtools"
      to: "row R15"
      via: "hash-navigating to the Calendar route on the already-booted app"
      pattern: "SecurityError"
    - from: "each answered Round 2 row"
      to: "a REQUIREMENTS.md checkbox"
      via: "the Row-to-requirement map (Round 2)"
      pattern: "R22-VERDICT"
---

<objective>
Run the **blocking Round 2 browser checkpoint** that GC-3 requires, and record the result. This is
the plan that decides whether Phase 22's two gaps are actually closed.

Purpose: `22-VERIFICATION.md` returned `gaps_found` on two truths. Gap 1 (CAL-02, the ~380px
day-cell overflow) was a visual judgement made by the developer's own eyes at Round 1 row R11 — an
automated CSS assertion structurally cannot discharge it (PROJECT.md line 49: automated gates have
missed rendering defects in this project three times). Gap 2 (CR-01) was never exercised by any of
the eleven Round 1 rows: R8 tampered the stored *value*, not the storage handle's *accessibility*.
Plans `22-06` and `22-07` wrote the fixes; only a human looking at the fixed build can close them.

Output: a `## Round 2` section in `22-VALIDATION.md` carrying six answered rows R12..R17 with
verdicts, a Gap Closure Record dispositioning both `22-VERIFICATION.md` gaps, and `REQUIREMENTS.md`
updated strictly on the Round 2 row map.

**No staged fixture is needed or permitted.** The archive supplies everything: October 2025's
Monday-start week totals are already recorded verbatim in the Round 1 preamble and are re-read in
R14 as a regression check. Nothing under `dist/` or `data/` may be edited.

**Explicitly NOT in scope (D-15).** No row is opened against Trends' weekly volume,
`records-logic.ts`'s biggest week, or the streak logic — those read the pipeline's Monday-fixed
`weekStartISO` and are deliberately untouched by this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md
@.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md
@.planning/phases/22-calendar-week-start-totals/22-REVIEW.md
@.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md
@.planning/phases/22-calendar-week-start-totals/22-05-PLAN.md
@.planning/phases/21-overview-rebuild/21-08-PLAN.md
</context>

<house_rules>
Carried forward verbatim from `22-05-PLAN.md` (and from checkpoint 16-09 before it), with three
Round 2 additions. These bind Task 2.

1. **Never cite an automated result as evidence for a manual row.** A green `npm test`, a source
   grep, or an agent's own DOM read is not an answer to a row whose observer is the developer's eyes.
2. **Present rows ONE AT A TIME**, in order, quoting each row's instructions including its
   `Required detail:` and `Observer required:` clauses.
3. **Read values back, do not confirm presence.** A row answered "the totals are there" is not
   answered. Quote the rendered text.
4. **Record the developer's own words.** Do not summarise, do not merge answers, do not fill a cell
   with what the implementation ought to produce.
5. **Do not fix anything during the session.** A failing row is recorded and the session continues to
   the next row; no source file may be edited. `git status --porcelain src scripts` must be empty at
   the end.
6. **R12 gates everything.** If R12 is not PASS, every subsequent row is recorded BLOCKED naming R12.
7. **Do not write a failure's wrong values into a PASS cell.** Task 3's checks treat that as a
   contradiction.
8. **(Round 2) The Round 1 waiver is NOT carried forward.** Round 1 waived house rule 1 for R6, R7,
   R8 and R10 at the developer's explicit request, and `22-VERIFICATION.md` recorded that as a real
   weakening of the evidence behind two ticked requirements. Round 2 grants no waiver in advance.
   Every row's `Observer required:` clause reads *the developer's own eyes*. If the developer asks
   for a waiver mid-session, it is theirs to grant — but it is recorded in that row's Observation
   cell verbatim, and Task 3 writes it into the Checkpoint Outcome section as a stated deviation.
9. **(Round 2) Four-state verdict vocabulary** (the Phase 20 Round 3 precedent):
   `PASS` / `FAIL` / `BLOCKED` / `NOT EXERCISABLE`. `NOT EXERCISABLE` is for a row whose claim could
   not be reached because something outside this round's scope failed first — it is not a soft FAIL
   and it is not a PASS.
10. **(Round 2) Restore the browser.** R16 changes a browser-wide privacy setting. It must be
    reverted before the session ends, and the revert confirmed by reloading the Calendar and seeing
    the week-start preference work again. R15 redefines a property on the live page; a plain reload
    restores it.
</house_rules>

<cache_trap>
`127.0.0.1` alone is NOT sufficient. This project has been bitten twice: Phase 21 Round 1's R13 only
passed after a hard reload cleared a stale cached `streaks.json`, and the staged-build trap recurs
with a stale `index.html` / `index.json` in the observing tab.

Every checkpoint session must: browse `127.0.0.1`, never `localhost`; serve under the
`/strava-widgets` project path, never the server root; and hard-reload (Cmd+Shift+R, or DevTools open
with "Disable cache" ticked, then reload) before judging any row.

**Round 2 adds a third surface: the build itself.** Round 1 was observed against bundle
`assets/index-YqJHQsHW.js`. If Round 2 is judged against that same filename, the fixes are not in the
observed artifact and every verdict is worthless. Task 1 asserts the served bundle filename has
changed AND that the served CSS and JS actually carry this round's changes, and R12 asks the
developer to read the filename off the page and compare it against the preamble.

The `localStorage` surface persists too: R15 and R16 each require a specific storage state, and each
is followed by a reload before it is judged.
</cache_trap>

<round1_carryover>
Facts the Round 2 agenda quotes. All are already recorded in `22-VALIDATION.md`'s Round 1 section and
are restated here so the executor does not have to re-derive them.

**The failing row this round re-asks (R11, FAIL):** the developer's words were *"Total column values
seem fine. Day cell values can slightly overflow once width to narrow (not a big issue for me, can
just be documented if not fixable very quickly)"* and *"behavior similar in light and dark theme"*.
They were explicitly offered a documented-PASS framing and chose FAIL.

**October 2025, Monday-start week totals** (recomputed from the live archive during Round 1 planning;
`data/dashboard/index.json`, 1,868 activities):

| Row | Days (Oct) | Distance | Time | Runs |
|-----|------------|----------|------|------|
| 1 | 1–5 | 59.1 km | 5h 42m | 5 |
| 2 | 6–12 | 80.0 km | 7h 53m | 6 |
| 3 | 13–19 | 80.0 km | 7h 58m | 5 |
| 4 | 20–26 | 80.0 km | 7h 42m | 6 |
| 5 | 27–31 | 58.1 km | 5h 32m | 5 |

**Known rounding artifact — NOT a bug.** The month header renders `357.3 km`; the Monday-start rows
display-sum to `357.2 km`, purely from independent per-row `toFixed(1)`. Unrounded metres reconcile
at 357.349 km under both week starts. Do not open a row about it and do not record it as a defect.

**The Sunday-start values, for the contradiction guard only:** rows 3 and 4 read `56.0 km / 5h 27m /
4 runs` and `104.1 km / 10h 14m / 7 runs` under Sunday. R14 is a Monday-start row; those two values
must not appear in its PASS cell.
</round1_carryover>

<what_this_round_ships>
Quoted into the Round 2 preamble so the developer knows what they are looking at.

**Plan 22-06 (GC-1, IN-05, IN-06):** the `@media (max-width: 380px)` calendar block now also
declares `.calendar-day { min-width: 0 }` (relaxing the seven day columns' 32px floor at that width
only), `.calendar-day__distance { font-size: 14px }` (down from 20px) and
`.calendar-week-total__time, .calendar-week-total__count { font-size: 12px }` (down from 14px), on
top of the padding and `.calendar-week-total__distance` compaction the phase already shipped. The
`Total` header gained `.calendar-weekday--total { text-align: right }`, applied alongside
`.calendar-weekday`. **The `.splits-scroll`-style horizontal-scroll wrapper (DISC-6b) was
deliberately NOT implemented** — GC-1 chose deeper compaction because this grid is full of focusable
day buttons and a horizontal-scroll container with focusable content scroll-jumps on Tab.

**Plan 22-07 (CR-01, WR-01):** `calendar.ts` no longer dereferences `globalThis.localStorage`; the
handle is resolved by `resolveWeekStartStorage()` in `calendar-preferences.ts`, which wraps the
property access in try/catch and returns `null` when the getter throws. `readStoredWeekStart(null)`
returns the Monday default and `writeWeekStart(null, ...)` is a no-op. `buildMonthGrid` is now total
for an off-union `weekStart`.

**Known, out of scope, and expected to surface at R16:** `main.ts:19` runs
`applyThemeMode(readStoredMode(localStorage))` at module scope, outside any try/catch, and
`theme.ts:91` has the same unguarded `options.storage ?? localStorage` shape. This round did not
touch either (GC-2 scoped the fix to the Calendar's own storage path). The inline pre-paint script in
`index.html:36-53` IS wrapped in try/catch, so the theme attribute still applies — but the module
script can still fail to evaluate under a browser-wide storage block, which would leave the page
without nav or view content. R16's disposition rules below tell the executor how to record that
without misattributing it to this round's fix.
</what_this_round_ships>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the gate, serve a PROVABLY FRESH build under /strava-widgets, and stage the six-row Round 2 agenda</name>
  <files>.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` — the whole file: the
      Per-Task Verification Map (lines 63-84), the Round 1 section (from line 186), its preamble,
      both October tables, the eleven answered rows, `## Checkpoint Outcome (Round 1)`,
      `## Known and Accepted` and `## Settled Discretion (phase 22)`. The Round 2 section is
      APPENDED below all of it; nothing above is rewritten except the Per-Task Verification Map's
      Status column.
    - `.planning/phases/22-calendar-week-start-totals/22-05-PLAN.md` lines 169-286 — Task 1's
      precedent: the gate commands, the `/tmp/gh-pages/strava-widgets` symlink staging, the
      `python3 -m http.server 8099 --directory /tmp/gh-pages` serve step, the preamble contents and
      the five-column agenda row shape with one `Required detail:` and one `Observer required:`
      clause per row.
    - `.planning/phases/21-overview-rebuild/21-08-PLAN.md` — the Round-numbering precedent named in
      `22-05-SUMMARY.md:117`.
    - This plan's `<house_rules>`, `<cache_trap>`, `<round1_carryover>` and
      `<what_this_round_ships>` blocks — all four are copied into the Round 2 preamble.
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` — the two gaps as worded,
      which the Round 2 rows are measured against.
  </read_first>
  <action>
    First re-run the full phase gate and fail the task if any part is not clean: `npm test`,
    `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets` (whose output must contain no
    `css-syntax-error`), and `npm run verify-dashboard`.

    Then stage the build exactly as GitHub Pages serves it, under the `/strava-widgets` project path
    and never at the server root: `mkdir -p /tmp/gh-pages` and
    `ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets`; assert
    `/tmp/gh-pages/strava-widgets/index.html` exists and that `readlink` on the symlink returns an
    absolute path. Serve with `python3 -m http.server 8099 --directory /tmp/gh-pages` in the
    background and confirm `http://127.0.0.1:8099/strava-widgets/index.html` and
    `http://127.0.0.1:8099/strava-widgets/data/dashboard/index.json` both respond 200 over HTTP.

    **Then prove the served artifact is this round's build, not Round 1's.** Read the
    `assets/index-*.js` and `assets/index-*.css` filenames out of the served `index.html`; the JS
    filename must NOT be `assets/index-YqJHQsHW.js` (Round 1's bundle). Fetch the CSS over HTTP and
    assert it contains `.calendar-weekday--total`, and that one of its `max-width: 380px` blocks
    contains both `calendar-day__distance` and a `min-width` declaration set to `0`. Fetch the JS
    over HTTP and assert it contains the string `calendar-weekday--total`. Record both filenames —
    the agenda quotes them so a later reader knows which build was observed.

    Do NOT edit anything under `dist/` or `data/`. This checkpoint uses organic archive data.

    Then append a `## Round 2` section to `22-VALIDATION.md` containing, in this order:

    A **preamble** stating: the served URL prefix `http://127.0.0.1:8099/strava-widgets/`; the one
    calendar route this session uses
    (`http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10`); the observed JS and CSS
    filenames plus an explicit sentence that the JS filename differs from Round 1's
    `assets/index-YqJHQsHW.js`; and the four gate commands with their results. Then the whole
    `<cache_trap>` block including its build-freshness paragraph, the whole `<house_rules>` list
    including rules 8, 9 and 10, the whole `<what_this_round_ships>` block, and the whole
    `<round1_carryover>` block including R11's verbatim words, the Monday-start table, the rounding
    artifact and the Sunday-start values named as contradiction-guard material. Then one sentence
    recording the D-15 scope fence.

    The preamble must NOT contain the literal phrases `Required detail` or `Observer required` —
    those belong to agenda rows only, and Task 1's check counts them.

    Then a **six-row agenda table** with columns `Row | Requirement | Instructions | Observation |
    Verdict`, rows labelled `R12.` through `R17.` in that order. Each Instructions cell ends with its
    own `Required detail:` clause naming exactly what must be quoted back, and an
    `Observer required:` clause naming the developer's own eyes. Leave every Observation and Verdict
    cell empty — zero new `R22-VERDICT` tokens may exist in the Round 2 section before the
    checkpoint.

    | Row | Requirement | What it asks |
    |-----|-------------|--------------|
    | R12 | precondition | The URL bar reads `127.0.0.1` and includes `/strava-widgets/`; a hard reload was performed and the method is named; the `assets/index-*.js` filename read from the page matches the preamble AND is not `index-YqJHQsHW.js`. **Required detail:** the URL as typed, the reload method, the bundle filename as read |
    | R13 | CAL-02, D-10, GC-1 | **The R11 re-ask.** Narrow the viewport to roughly 380px (responsive-design mode or a phone emulation preset — name which). At that width, read back the rendered text of at least three day cells that carry a distance, and the rendered text of one week-total cell. Confirm whether any value still overflows its cell, whether the eight-column grid overflows the panel or the viewport, and whether the day columns are still legible. Then state whether the behaviour is the same in the other theme. **Required detail:** the width used, the quoted text of at least three day cells and one total cell, an explicit yes/no on overflow, and the theme comparison. A cell whose value is CLIPPED or TRUNCATED rather than overflowing is also a FAIL — say which it is |
    | R14 | CAL-02, D-16 | Back at normal width on `#/calendar?month=2025-10` under Monday-start, read back all five week-total cells: distance, time and run count. Compare against the Monday-start table in the preamble. This is the regression check on plans 22-06 and 22-07 — the grid math and the total cell must be unchanged by the CSS and storage work. **Required detail:** five triples of distance / time / run count, quoted as rendered |
    | R15 | CAL-01, T-22-WK-02, GC-2 | **Gap 2, isolated.** With the app already loaded, run this in the devtools console: `Object.defineProperty(window, "localStorage", { configurable: true, get() { throw new DOMException("blocked", "SecurityError"); } })`. Then navigate to another view and back to `#/calendar?month=2025-10` (a hash navigation, NOT a reload — a reload would re-run the app bootstrap, which is out of scope for this row). Confirm the Calendar renders its grid and week totals rather than the generic `Something went wrong` panel, and say which week start it defaulted to. Then reload normally to restore the page. **Required detail:** confirmation the property redefinition was run, what the Calendar rendered after the hash navigation, the first weekday heading, and any console error text |
    | R16 | CAL-01 (informational), app-level | **Gap 2, real browser configuration.** Block site data for the origin — Firefox: Settings → Privacy & Security → Cookies and Site Data → Custom → Cookies → **All cookies**; Chrome alternative: `chrome://settings/content/siteData` → *Don't allow sites to save data*. Reload `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10` and describe what the page shows. **Disposition rules, applied by the executor when recording:** (a) the Calendar grid and totals render → **PASS**; (b) the Calendar route renders the generic `Something went wrong` panel → **FAIL**, the CR-01 fix did not hold; (c) the page shows no nav and no view content at all → **NOT EXERCISABLE** for the Calendar-level claim, because the module-scope `applyThemeMode(readStoredMode(localStorage))` at `main.ts:19` is unguarded and pre-existing (out of this round's scope) and fails before any view mounts — record it verbatim as a NEW finding, do not mark it FAIL, and do not attribute it to this round's fix. **Then restore the browser setting and confirm the calendar and its week-start preference work again.** **Required detail:** the browser and exact setting used, what the page rendered, which of (a)/(b)/(c) it matches, and confirmation the setting was restored |
    | R17 | CAL-03, IN-05 | Find a week whose total cell is wide (a long distance plus a `10h 14m`-shaped time — October 2025 under Sunday-start has one). Confirm the `Total` header sits over the values it labels rather than centred away from them. **Required detail:** a description of where the header sits relative to the values beneath it, and the week you used. This row is confirm-unregressed for CAL-03; it does not re-gate it |

    Then a `### Row-to-requirement map (Round 2)` stating: R12 gates all rows. **CAL-02 → R13, R14
    (gating).** **CAL-01 → R15 (gating); R16 is informational and does not gate.** **CAL-03 → R17
    (confirm-unregressed, not gating).**

    Set the file's frontmatter `round: 2` and add a `round2_staged` date. Leave
    `nyquist_compliant: false` and `status: partial` until Task 3. Do not alter `round1_staged` or
    `round1_answered`.

    Finally, update the Per-Task Verification Map: add two rows for this round's automated coverage —
    one for CAL-02 / no threat ref, "the 380px compaction declarations and the `Total` header
    modifier are guarded with at-rule-override-aware assertions", unit,
    `npx vitest run src/dashboard/styles.test.ts`, task `22-06-T3`, wave 6; and one for CAL-01 /
    T-22-WK-02, "a storage stand-in whose PROPERTY GETTER throws resolves to a null handle and the
    read falls back to the Monday default", unit,
    `npx vitest run src/dashboard/views/calendar-preferences.test.ts`, task `22-07-T1`, wave 7.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json && rm -f /tmp/bw22r2.log && npm run build-widgets > /tmp/bw22r2.log 2>&1; BW=$?; tail -3 /tmp/bw22r2.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw22r2.log && npm run verify-dashboard && mkdir -p /tmp/gh-pages && ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets && test -f /tmp/gh-pages/strava-widgets/index.html && readlink /tmp/gh-pages/strava-widgets | grep -q '^/' && curl -sf -o /dev/null http://127.0.0.1:8099/strava-widgets/index.html && curl -sf -o /dev/null http://127.0.0.1:8099/strava-widgets/data/dashboard/index.json && test -z "$(git status --porcelain src scripts data dist)" && node --input-type=module -e 'const base="http://127.0.0.1:8099/strava-widgets/"; const html=await (await fetch(base+"index.html")).text(); const js=(html.match(/assets\/index-[A-Za-z0-9_-]+\.js/)||[])[0]; const cssref=(html.match(/assets\/index-[A-Za-z0-9_-]+\.css/)||[])[0]; if(!js||!cssref) throw new Error("could not read the asset filenames from the served index.html"); if(js==="assets/index-YqJHQsHW.js") throw new Error("the served bundle is Round 1s build - the gap-closure fixes are not in the observed artifact"); const css=await (await fetch(base+cssref)).text(); if(!css.includes("calendar-weekday--total")) throw new Error("served CSS lacks the IN-05 modifier - stale build"); const blocks=[...css.matchAll(/@media\s*\(max-width:\s*380px\)\s*\{/g)].map(m=>{let i=m.index+m[0].length,d=1; while(d>0&&i<css.length){ if(css[i]==="{")d++; else if(css[i]==="}")d--; i++; } return css.slice(m.index,i);}); if(!blocks.some(b=>b.includes("calendar-day__distance")&&/min-width:\s*0/.test(b))) throw new Error("no served 380px block carries the GC-1 compaction - stale CSS"); const bundle=await (await fetch(base+js)).text(); if(!bundle.includes("calendar-weekday--total")) throw new Error("served JS lacks the IN-05 class string - stale bundle"); console.log("served build is fresh: "+js+" "+cssref);' && node --input-type=module -e 'import {readFileSync} from "node:fs"; const p=".planning/phases/22-calendar-week-start-totals/22-VALIDATION.md"; const s=readFileSync(p,"utf8"); if((s.match(/## Round 2/g)||[]).length!==1) throw new Error("expected exactly one Round 2 heading"); const t=s.split("## Round 2")[1]; const ids=["R12","R13","R14","R15","R16","R17"]; const rows=t.split("\n").filter(l=>l.startsWith("|")).filter(r=>ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(r))); if(rows.length!==6) throw new Error("expected 6 agenda rows, found "+rows.length); ids.forEach((id,i)=>{ if(!new RegExp("^\\|\\s*"+id+"\\.").test(rows[i])) throw new Error("agenda row "+(i+1)+" should begin with "+id+"."); if(rows[i].split("|").length<6) throw new Error("row "+id+" does not have five columns"); }); const rd=(t.match(/Required detail/g)||[]).length; if(rd!==6) throw new Error("expected exactly one Required detail clause per row, found "+rd); const ob=(t.match(/Observer required/g)||[]).length; if(ob!==6) throw new Error("expected exactly one Observer required clause per row, found "+ob); if((t.match(/R22-VERDICT/g)||[]).length!==0) throw new Error("no verdict token may exist in Round 2 before the checkpoint"); if((s.match(/R22-VERDICT/g)||[]).length!==11) throw new Error("Round 1 verdict count changed - the Round 1 section must not be rewritten"); const pre=t.split("| Row")[0]; for(const n of ["127.0.0.1:8099/strava-widgets/","2025-10","index-YqJHQsHW.js","59.1 km","80.0 km","7h 58m","7h 42m","58.1 km","357.349","min-width","calendar-weekday--total","splits-scroll","resolveWeekStartStorage","main.ts:19","D-15"]) if(!pre.includes(n)) throw new Error("the Round 2 preamble is missing: "+n); if(!/hard reload/i.test(pre)) throw new Error("the preamble does not carry the hard-reload cache-trap paragraph"); if(!/waiver/i.test(pre)) throw new Error("the preamble does not carry house rule 8 (no carried-forward waiver)"); if(!/NOT EXERCISABLE/.test(pre)) throw new Error("the preamble does not carry the four-state verdict vocabulary"); if(/Required detail/.test(pre)||/Observer required/.test(pre)) throw new Error("the preamble must not use the row-only clause phrases"); if(/localhost:8099/.test(s)) throw new Error("localhost:8099 appears - 127.0.0.1 is required"); if(!/Row-to-requirement map \(Round 2\)/.test(t)) throw new Error("the Round 2 row-to-requirement map is missing"); const fm=s.split("---")[1]||""; if(!/round: 2/.test(fm)) throw new Error("frontmatter round is not 2"); if(!/round2_staged/.test(fm)) throw new Error("round2_staged date missing"); if(!/round1_answered/.test(fm)) throw new Error("round1_answered was dropped from the frontmatter"); if(!/nyquist_compliant: false/.test(fm)) throw new Error("nyquist_compliant must stay false until Task 3"); console.log("6 staged rows, 0 new verdicts, Round 1 intact at 11, fresh build, 127.0.0.1 enforced");'</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets` (zero
      `css-syntax-error` in its output) and `npm run verify-dashboard` all exit 0.
    - `/tmp/gh-pages/strava-widgets/index.html` exists, the symlink resolves to an absolute path, and
      both the served `index.html` and `data/dashboard/index.json` respond 200 over HTTP from
      `127.0.0.1:8099`.
    - The served `assets/index-*.js` filename is NOT `assets/index-YqJHQsHW.js`; the served CSS
      contains `.calendar-weekday--total` and a `max-width: 380px` block containing both
      `calendar-day__distance` and `min-width` set to `0`; the served JS contains
      `calendar-weekday--total`.
    - `git status --porcelain src scripts data dist` is empty.
    - `22-VALIDATION.md` has exactly one `## Round 2` heading with exactly six agenda rows
      `R12.`..`R17.` in order, five columns each, exactly six `Required detail:` and six
      `Observer required:` clauses in the section, and **zero** `R22-VERDICT` tokens inside Round 2.
    - The file still contains exactly 11 `R22-VERDICT` tokens in total (Round 1 untouched).
    - The Round 2 preamble contains every one of: the served URL prefix, `2025-10`,
      `index-YqJHQsHW.js`, all five Monday-start distances/times from the carryover table, `357.349`,
      `min-width`, `calendar-weekday--total`, `splits-scroll`, `resolveWeekStartStorage`,
      `main.ts:19`, `D-15`, a hard-reload paragraph, the no-waiver rule, and `NOT EXERCISABLE`.
    - The preamble contains neither `Required detail` nor `Observer required`.
    - `localhost:8099` appears nowhere in the file.
    - Frontmatter carries `round: 2` and `round2_staged`, retains `round1_answered`, and keeps
      `nyquist_compliant: false`.
  </acceptance_criteria>
  <done>A provably fresh production-shaped build is served from 127.0.0.1 under /strava-widgets against organic archive data, and six unanswered Round 2 rows are staged with the R11 carryover and the fix inventory in the preamble.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] The six-row Round 2 checkpoint — re-ask the narrow-viewport question and observe the blocked-storage path</name>
  <what-built>
    Plan `22-06` deepened the `@media (max-width: 380px)` compaction (day-cell distance 20px→14px,
    week-total time/count 14px→12px, `.calendar-day { min-width: 0 }` relaxing the seven day columns'
    32px floor) and right-aligned the `Total` header with a new `.calendar-weekday--total` modifier.
    The `.splits-scroll`-style horizontal-scroll wrapper was deliberately NOT built — GC-1 chose
    deeper compaction because a horizontal-scroll container full of focusable day buttons
    scroll-jumps on Tab.

    Plan `22-07` removed the unguarded `globalThis.localStorage` dereference from `calendar.ts`. The
    handle now comes from `resolveWeekStartStorage()`, which wraps the property access in try/catch
    and returns `null` when the getter throws; the read then falls back to the Monday default and the
    write becomes a no-op. `buildMonthGrid` was also made total for an off-union `weekStart`.

    Task 1 re-ran the full gate and served that build under `/strava-widgets` on 127.0.0.1 with
    **organic archive data — no fixture of any kind was staged** — and proved the served bundle is
    not Round 1's `assets/index-YqJHQsHW.js`.

    What no automated gate in this repository can see: whether the day-cell values still overflow at
    ~380px (Round 1's R11 FAIL was the developer's own visual judgement), whether a real browser with
    a throwing storage getter still renders the Calendar, and whether the `Total` header now sits
    over the values it labels. `vitest.config.ts` is `environment: 'node'` with no jsdom and there is
    no headless browser in this project.
  </what-built>
  <how-to-verify>
    Open `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10`. **Confirm the URL bar reads
    `127.0.0.1`, not `localhost`, and includes `/strava-widgets/`, before judging any row.**

    **Then hard-reload before judging anything** (Cmd+Shift+R, or DevTools open with "Disable cache"
    ticked, then reload). R12 exists to record that this happened AND that the bundle filename on the
    page is this round's, not Round 1's `assets/index-YqJHQsHW.js`. R12 must be PASS before any other
    row may be judged; if it is not, record every remaining row BLOCKED naming R12 and stop.

    Present the six rows from `22-VALIDATION.md`'s Round 2 table **one at a time**, in R12..R17
    order, following every rule in this plan's `<house_rules>` block — including rule 8: **no waiver
    of house rule 1 is pre-granted this round.** Every row's observer is the developer's own eyes. Do
    not offer to observe a row via browser automation on their behalf; if they ask for it, record the
    request and their words verbatim in that row's Observation cell.

    R13 is the row this entire gap-closure round exists for. It is the same question R11 failed. Do
    not accept "looks better" — the row asks for the quoted rendered text of at least three day cells
    and one total cell at ~380px, an explicit yes/no on overflow, and a statement about clipping
    versus overflowing. If the developer offers a documented-PASS framing, do not encourage it: R11
    was recorded FAIL precisely because they chose accuracy over convenience, and CAL-02's tick
    depends on this verdict being honest.

    R15 asks the developer to paste a property redefinition into the devtools console and then make a
    **hash navigation**, not a reload. Say that explicitly — a reload re-runs the app bootstrap,
    where an unrelated, pre-existing unguarded read lives, and would confound the row.

    R16 changes a browser-wide privacy setting. Read its three disposition rules aloud before they
    start, so they know that a completely blank page is a recognised, pre-existing, out-of-scope
    outcome (`NOT EXERCISABLE`) rather than a failure of this round's fix. **Make sure the setting is
    restored before the session ends** (house rule 10).

    Do not let the developer chase the rounding artifact: the Monday-start rows display-sum to
    357.2 km against the header's 357.3 km, and that is expected per the preamble.
  </how-to-verify>
  <action>
    Present the six Round 2 rows from
    `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` to the developer ONE AT A TIME,
    in R12..R17 order, quoting each row's Instructions cell including its `Required detail:` and
    `Observer required:` clauses in full.

    Before R12, confirm with the developer that the URL bar reads
    `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10`, ask them to perform a hard
    reload and name the method, and ask them to read the `assets/index-*.js` filename off the page
    (devtools Network or Sources panel) and compare it against the preamble — a filename equal to
    `index-YqJHQsHW.js` means Round 1's build is being observed and R12 is FAIL.

    For R13, ask for the width they used and the quoted rendered text of at least three distance-
    carrying day cells and one week-total cell at that width, then the explicit overflow yes/no, the
    clipped-versus-overflowing distinction, and the other-theme comparison. Do not read the DOM
    yourself as a substitute.

    For R14, ask for the five triples as rendered, not as summarised, and compare them against the
    Monday-start table in the preamble. A row whose numbers match neither the Monday-start table nor
    anything else is a FAIL with the seen values quoted.

    For R15, give them the exact console line to paste and state that the next step is a hash
    navigation away and back, not a reload. Ask what the Calendar rendered, which week start it
    defaulted to, and any console error text. `Something went wrong` appearing is a FAIL.

    For R16, apply the three disposition rules literally when recording. If the page is entirely
    blank with no nav, record `NOT EXERCISABLE` and capture their description verbatim as a NEW
    out-of-scope finding naming `main.ts:19` and `theme.ts:91` — do NOT record it as a FAIL of this
    round's fix, and do NOT patch anything. Then confirm the browser setting was restored and that
    the week-start preference works again.

    For R17, ask where the `Total` header sits relative to the values below it and which week they
    used. This row confirms IN-05 and does not re-gate CAL-03.

    Capture each answer in the developer's own words and hold them for Task 3, which writes them into
    the Observation cells. Do not summarise, do not merge answers, do not substitute an agent DOM
    read for any row, and do not edit any source file if a row fails.
  </action>
  <read_first>
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Round 2 — the staged agenda
      and preamble Task 1 wrote, including the R11 carryover words, the Monday-start table, the
      rounding artifact and the fix inventory.
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Round 1 row R11 (line 301)
      — the exact FAIL this round re-asks, in the developer's own words.
    - This plan's `<house_rules>`, `<cache_trap>`, `<round1_carryover>` and
      `<what_this_round_ships>` blocks.
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` — both gaps as worded, so
      the questions asked match the gaps being closed.
    - `.planning/phases/22-calendar-week-start-totals/22-05-SUMMARY.md` lines 91-108 — the two Round 1
      process deviations, especially the house-rule-1 waiver that rule 8 declines to carry forward.
    - `.planning/PROJECT.md` line 49 — the house rule this whole task exists to honour.
  </read_first>
  <verify>
    <human-check>All six rows have an answer from the developer, each with the row's own required detail and the developer named as observer; the URL bar was confirmed to read 127.0.0.1 and /strava-widgets/ and the bundle filename was confirmed not to be Round 1's before R12 was judged; no row after R12 was judged unless R12 was PASS; R16's browser setting was restored and the restoration confirmed; no source file was edited during the session.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer confirmed the URL bar reads
      `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10` and named the hard-reload
      method before R12 was judged.
    - R12's answer quotes the `assets/index-*.js` filename as read from the page, and it is not
      `index-YqJHQsHW.js`.
    - R13's answer quotes the viewport width used, the rendered text of at least three day cells and
      one week-total cell, an explicit yes/no on overflow, a clipped-versus-overflowing statement,
      and the other-theme comparison.
    - R14's answer quotes five distance / time / run-count triples as rendered.
    - R15's answer confirms the property redefinition was run, states it was followed by a hash
      navigation (not a reload), and reports what the Calendar rendered plus any console error text.
    - R16's answer names the browser and exact setting, states which of the three disposition rules
      it matches, and confirms the setting was restored afterwards.
    - R17's answer describes the `Total` header's position relative to the values and names the week
      used.
    - Every row after R12 was judged only because R12 was PASS; otherwise those rows read BLOCKED
      naming R12.
    - No row outside R12..R17 was asked; in particular no row was opened against Trends, biggest week
      or streaks (D-15).
    - No source file was edited during the session: `git status --porcelain src scripts` is empty.
  </acceptance_criteria>
  <done>Six developer answers are in hand, each attributable to the developer's own eyes, with the narrow-viewport rendering and the blocked-storage behaviour quoted verbatim.</done>
  <resume-signal>Per-row verdicts recorded. Type "done" when all six rows have an answer, or name the rows you are declining.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the Round 2 outcome, disposition both verification gaps, and re-gate the Calendar requirements</name>
  <files>.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Round 2 as answered in
      Task 2, its `Row-to-requirement map (Round 2)`, and the Per-Task Verification Map whose
      manual-only Status cells this task updates.
    - `.planning/phases/22-calendar-week-start-totals/22-05-PLAN.md` Task 3 — the precedent for the
      recording sections and for gating `REQUIREMENTS.md` strictly on the row map rather than on an
      overall impression.
    - `.planning/REQUIREMENTS.md` lines 33-35 (the CAL-01/CAL-02/CAL-03 entries with their Round 1
      closure notes) and lines 88-90 (the status table rows).
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` — both gap entries, whose
      dispositions this task records.
    - `.planning/STATE.md` § Decisions — the Phase 19 precedent (UI-01/UI-02 reverted from complete
      to blocked when a gap-closure row failed), which is the rule this task applies to CAL-01 if
      R15 does not pass.
  </read_first>
  <action>
    Write the six verdicts into the Round 2 Observation cells in the developer's own words, each with
    one `R22-VERDICT: PASS|FAIL|BLOCKED|NOT EXERCISABLE` token, the row's required detail, and its
    observer attribution. Do not summarise and do not merge answers.

    None of the prose sections below may contain the literal token `R22-VERDICT` — it belongs to
    Verdict cells only, and Task 3's check counts exactly six of it in the Round 2 section and
    exactly seventeen in the file.

    Then add three sections below the Round 2 table:

    - `## Checkpoint Outcome (Round 2)` — the count of PASS / FAIL / BLOCKED / NOT EXERCISABLE across
      R12..R17, and one sentence per non-PASS row naming what was not established. Record any
      mid-session waiver of house rule 1 that the developer granted, verbatim, as a stated deviation.
      No fix, no root-cause theory, no remediation plan.
    - `## Gap Closure Record (Round 2)` — one entry per gap in `22-VERIFICATION.md`, each stating
      CLOSED or STILL OPEN and the row that decided it. Gap 1 (CAL-02, the ~380px overflow) is
      decided by R13. Gap 2 (CR-01, the unguarded storage getter) is decided by R15, with R16's
      observation recorded alongside as informational. If R16 landed on disposition (c), record the
      new out-of-scope finding here in full — the unguarded module-scope
      `applyThemeMode(readStoredMode(localStorage))` at `main.ts:19` and the same shape at
      `theme.ts:91` — stated as pre-existing, not introduced by this round, not patched per house
      rule 5, and left for the user to disposition.
    - `## Known and Accepted (Round 2)` — restate that the rounding artifact (Monday-start rows
      display-summing to 357.2 km against the header's 357.3 km; 357.349 km unrounded under both week
      starts) and the D-15 scope fence remain knowingly shipped, and add that the DISC-6b
      `.splits-scroll` horizontal-scroll wrapper remains documented and unimplemented by deliberate
      choice (GC-1), not by omission.

    Update the Per-Task Verification Map's manual-only Status cells to reflect the Round 2 verdicts
    of their mapped rows.

    Then update `.planning/REQUIREMENTS.md`, strictly on the Round 2 row map and nothing else:

    - **CAL-02** is ticked `[x]` and its status-table row set to `Complete` if and only if R12, R13
      and R14 are ALL PASS. Append a `**Closed 2026-08-18 (plan 22-08, Round 2)**` note naming those
      rows and quoting what was read back at ~380px. If any of them did not pass, CAL-02 stays `[ ]`
      / `Pending` with a one-line note naming the failing row and pointing at Round 2.
    - **CAL-01** keeps its Round 1 tick if and only if R15 is PASS; append a note recording that
      Gap 2 / CR-01's blocked-storage path was observed closed at R15, and R16's disposition
      alongside. **If R15 is not PASS, revert CAL-01 to `[ ]` and its status-table row to `Pending`**,
      with a note naming R15 and citing the Phase 19 UI-01/UI-02 precedent for reverting a ticked
      requirement when a gap-closure row fails.
    - **CAL-03** is not re-gated. Leave its tick and status row exactly as they are. If R17 did not
      pass, record it in `## Checkpoint Outcome (Round 2)` as an open finding and say plainly in the
      CAL-03 line's note that R17 is confirm-unregressed and does not change the requirement's state.

    Finally set the file's frontmatter: `round2_answered` with today's date either way;
    `status: complete` and `nyquist_compliant: true` only if every one of R12..R17 is PASS; otherwise
    `status: partial` and `nyquist_compliant: false`. Do not alter the Round 1 section, its verdicts,
    or the Round 1 frontmatter dates.

    Do not touch `ROADMAP.md` — its Phase 22 checkbox belongs to verification and `/gsd-progress`.
    `22-VERIFICATION.md` is not edited either; re-verification is `/gsd-verify-work`'s job.
  </action>
  <verify>
    <automated>node --input-type=module -e 'import {readFileSync} from "node:fs"; const p=".planning/phases/22-calendar-week-start-totals/22-VALIDATION.md"; const s=readFileSync(p,"utf8"); const t=s.split("## Round 2")[1]; const ids=["R12","R13","R14","R15","R16","R17"]; const rows=t.split("\n").filter(l=>l.startsWith("|")).filter(r=>ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(r))); if(rows.length!==6) throw new Error("expected 6 Round 2 rows, found "+rows.length); if((t.match(/R22-VERDICT/g)||[]).length!==6) throw new Error("expected exactly 6 Round 2 verdicts"); if((s.match(/R22-VERDICT/g)||[]).length!==17) throw new Error("expected 17 verdict tokens in the file (11 Round 1 + 6 Round 2)"); for(const r of rows){ if(!/R22-VERDICT:\s*(PASS|FAIL|BLOCKED|NOT EXERCISABLE)/.test(r)) throw new Error("a row has no well-formed verdict: "+r.slice(0,40)); const cells=r.split("|"); if(cells.length<6) throw new Error("a row lost a column"); if((cells[4]||"").trim().length<20) throw new Error("an Observation cell is empty or near-empty: "+r.slice(0,40)); } const verdict=id=>{const r=rows.find(x=>new RegExp("^\\|\\s*"+id+"\\.").test(x)); return (r.match(/R22-VERDICT:\s*(PASS|FAIL|BLOCKED|NOT EXERCISABLE)/)||[])[1];}; const r14=rows.find(x=>/^\|\s*R14\./.test(x)); if(verdict("R14")==="PASS" && /(56\.0|104\.1|5h 27m|10h 14m)/.test(r14)) throw new Error("R14 PASS cell quotes Sunday-start values - contradiction guard"); const r13=rows.find(x=>/^\|\s*R13\./.test(x)); if(verdict("R13")==="PASS" && !/px/.test(r13)) throw new Error("R13 PASS cell does not state the viewport width it was judged at"); if(verdict("R13")==="PASS" && !/km/.test(r13)) throw new Error("R13 PASS cell quotes no rendered day-cell value"); for(const h of ["## Checkpoint Outcome (Round 2)","## Gap Closure Record (Round 2)","## Known and Accepted (Round 2)"]) if(!s.includes(h)) throw new Error("missing section: "+h); const gcr=s.split("## Gap Closure Record (Round 2)")[1].split("\n## ")[0]; for(const n of ["R13","R15","CR-01"]) if(!gcr.includes(n)) throw new Error("the Gap Closure Record does not name "+n); if(verdict("R16")==="NOT EXERCISABLE" && !/main\.ts:19/.test(gcr)) throw new Error("R16 was NOT EXERCISABLE but the out-of-scope bootstrap finding is not recorded"); const ka=s.split("## Known and Accepted (Round 2)")[1]; if(!/357\.349/.test(ka)||!/D-15/.test(ka)||!/splits-scroll/.test(ka)) throw new Error("Known and Accepted (Round 2) is missing the rounding artifact, the D-15 fence or the unimplemented DISC-6b fallback"); const fm=s.split("---")[1]||""; if(!/round2_answered/.test(fm)) throw new Error("round2_answered date missing"); if(!/round1_answered/.test(fm)) throw new Error("round1_answered was dropped"); const all=ids.every(id=>verdict(id)==="PASS"); if(all!==/nyquist_compliant: true/.test(fm)) throw new Error("nyquist_compliant does not match the all-PASS state"); const req=readFileSync(".planning/REQUIREMENTS.md","utf8"); const line=id=>req.split("\n").find(l=>l.includes("**"+id+"**")); const status=id=>req.split("\n").find(l=>l.startsWith("| "+id+" ")); const cal2ok=["R12","R13","R14"].every(id=>verdict(id)==="PASS"); if(line("CAL-02").startsWith("- [x]")!==cal2ok) throw new Error("CAL-02 tick state does not match its Round 2 row map"); if(cal2ok!==/Complete/.test(status("CAL-02"))) throw new Error("CAL-02 status row does not match its Round 2 row map"); const cal1ok=verdict("R15")==="PASS"; if(line("CAL-01").startsWith("- [x]")!==cal1ok) throw new Error("CAL-01 tick state does not match R15 (Phase 19 revert precedent applies)"); if(cal1ok!==/Complete/.test(status("CAL-01"))) throw new Error("CAL-01 status row does not match R15"); if(!line("CAL-03").startsWith("- [x]")||!/Complete/.test(status("CAL-03"))) throw new Error("CAL-03 must be left as it was - R17 does not re-gate it"); if(!/22-08/.test(req)) throw new Error("REQUIREMENTS.md carries no Round 2 attribution"); console.log("6 Round 2 verdicts recorded, three sections present, requirement states match the Round 2 row map");' && test -z "$(git status --porcelain src scripts)" && npm test && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `22-VALIDATION.md`'s Round 2 section has exactly six `R22-VERDICT` tokens, one per row, each a
      well-formed `PASS` / `FAIL` / `BLOCKED` / `NOT EXERCISABLE`, with no Observation cell shorter
      than 20 characters; the file carries 17 verdict tokens in total (11 Round 1 + 6 Round 2).
    - Contradiction guards hold: an R14 PASS cell quotes no Sunday-start value (`56.0`, `104.1`,
      `5h 27m`, `10h 14m`); an R13 PASS cell states a viewport width and quotes at least one rendered
      `km` value.
    - The three Round 2 sections exist. The Gap Closure Record names R13, R15 and `CR-01`; if R16 was
      `NOT EXERCISABLE`, it also records the `main.ts:19` out-of-scope finding.
    - `## Known and Accepted (Round 2)` records the 357.349 km rounding artifact, the D-15 fence and
      the deliberately-unimplemented `splits-scroll` fallback.
    - `REQUIREMENTS.md`: CAL-02 is ticked and `Complete` if and only if R12, R13 and R14 all PASS;
      CAL-01 is ticked and `Complete` if and only if R15 is PASS; CAL-03 is left ticked and
      `Complete` regardless of R17. The file names plan `22-08`.
    - Frontmatter carries `round2_answered`, retains `round1_answered`, and sets
      `nyquist_compliant: true` if and only if all six Round 2 rows passed.
    - `ROADMAP.md` and `22-VERIFICATION.md` are not modified by this plan.
    - `git status --porcelain src scripts` is empty; `npm test` and
      `npx tsc --noEmit -p tsconfig.json` both exit 0.
  </acceptance_criteria>
  <done>Round 2 is recorded verbatim, both verification gaps carry an explicit disposition tied to a named row, and each Calendar requirement's state follows its row map rather than an overall impression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer devtools → `localStorage` | R15 crosses this deliberately, redefining the property with a throwing getter to exercise the CR-01 path in a real browser |
| browser privacy configuration → the served app | R16 crosses this by blocking site data browser-wide, the realistic form of the same threat |
| local HTTP server → browser | A loopback-only static server on 127.0.0.1 serving the built bundle and already-public published JSON |
| the observed artifact → the recorded verdict | A stale bundle would make every verdict describe code that is not under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-WK-02 | Denial of service | the Calendar view under blocked storage | mitigate (observed) | R15 installs a throwing `localStorage` getter on the live page and mounts the Calendar against it; R16 repeats it via a real browser privacy setting. This is the only place in the phase where CR-01's fix is observed rather than unit-simulated, and it is the exact path none of Round 1's eleven rows exercised |
| T-22-VER-03 | Tampering | the observed build | mitigate | Task 1 asserts `git status --porcelain src scripts data dist` is empty, and additionally proves freshness three ways: the served JS filename differs from Round 1's `index-YqJHQsHW.js`, the served CSS carries `.calendar-weekday--total` plus the 380px compaction, and the served JS carries the same class string. R12 makes the developer read the filename off the page — the staged-build cache trap that has bitten this project twice |
| T-22-VER-01 | Repudiation | the checkpoint record itself | mitigate | Every row carries the developer as named observer; house rule 8 refuses to pre-grant Round 1's waiver, and any waiver granted mid-session is recorded verbatim as a deviation. Task 3's contradiction guards reject an R14 PASS cell quoting Sunday-start values and an R13 PASS cell with no width or rendered value |
| T-22-VER-04 | Repudiation | misattributed failure | mitigate | R16's three disposition rules are written into the agenda in advance, so a pre-existing out-of-scope bootstrap failure (`main.ts:19`) is recorded as `NOT EXERCISABLE` with a new finding rather than silently charged against this round's fix — or, worse, silently patched |
| T-22-SRV-01 | Information disclosure | the local static server | accept | `python3 -m http.server` serves `/tmp/gh-pages` on loopback only; its contents are the published dashboard bundle plus already-public activity JSON. Transient checkpoint fixture, not a deployed artifact |
| T-22-GC-BR-01 | Denial of service | the developer's browser configuration | mitigate | R16 changes a browser-wide privacy setting; house rule 10 requires it to be restored before the session ends and the restoration confirmed by exercising the week-start preference again |
| T-22-SC | Tampering | npm/pip/cargo installs | accept (not applicable) | Zero new packages. `22-RESEARCH.md` § Package Legitimacy Audit records "Not applicable"; no install task exists in any Phase 22 plan |
</threat_model>

<verification>
- Task 1's staging check proves six rows are staged with zero new verdicts, Round 1 intact at eleven,
  no fixture, `127.0.0.1` enforced, and — the addition this round — that the served artifact actually
  contains the 22-06 and 22-07 changes and is not Round 1's bundle.
- Task 2's human-check proves every row has a developer answer with its required detail, observed by
  the developer's own eyes with no pre-granted waiver.
- Task 3's recording check proves six verdicts, non-empty observations, both contradiction guards,
  the three sections, the gap dispositions, and that each requirement's state matches the Round 2 row
  map exactly.

This is the ONLY plan in the gap-closure round that discharges a visual or interaction claim. Plans
`22-06` and `22-07` each state explicitly that they do not.
</verification>

<success_criteria>
- Six rows asked one at a time, answered by the developer's own eyes, recorded verbatim with
  four-state verdicts under a `## Round 2` heading continuing at R12.
- The narrow-viewport question that failed as R11 was re-asked against a provably fresh build, and
  answered with quoted rendered values rather than an impression.
- The blocked-storage path was observed in a real browser, isolated at R15 and configuration-level at
  R16, with a pre-written rule preventing a pre-existing bootstrap failure from being misattributed.
- Both `22-VERIFICATION.md` gaps carry an explicit CLOSED / STILL OPEN disposition tied to a row.
- CAL-02 is ticked if and only if its row map passed; CAL-01's tick survives if and only if R15
  passed; CAL-03 is untouched.
- No source file was edited and no fixture was staged during the checkpoint.
</success_criteria>

<output>
Create `.planning/phases/22-calendar-week-start-totals/22-08-SUMMARY.md` when done.
</output>
