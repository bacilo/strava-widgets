---
phase: 22-calendar-week-start-totals
plan: 10
type: execute
wave: 9
depends_on: ["22-08"]
gap_closure: true
round: 3
files_modified:
  - src/dashboard/storage.ts
  - src/dashboard/storage.test.ts
  - src/dashboard/views/calendar-preferences.ts
  - src/dashboard/views/calendar-preferences.test.ts
  - src/dashboard/views/calendar.ts
  - src/dashboard/views/calendar.test.ts
autonomous: true
requirements: [CAL-01]
must_haves:
  decisions: [D-05, D-06, D-07, GC-5]
  truths:
    - "GC-5 / BL-03: a single shared `resolveStorage(override?: WebStorage): WebStorage | null` exists in a NEW module `src/dashboard/storage.ts`, and it is the ONLY place in `src/dashboard/` that dereferences a storage global. It wraps the `globalThis.localStorage` PROPERTY GETTER in try/catch and returns `null` on throw or absence"
    - "GC-5 / D-06 fence: `storage.ts` resolves a HANDLE and nothing else. It declares no storage key, calls no `getItem`/`setItem`, parses no value, writes no console output, and has no key registry — the deferred 'shared view-preference facility' idea in 22-CONTEXT.md is NOT built. `WEEK_START_STORAGE_KEY`, `parseWeekStart`, `readStoredWeekStart` and `writeWeekStart` stay in `calendar-preferences.ts`, which keeps owning the week-start persistence contract"
    - "`resolveWeekStartStorage` is RECONCILED with the shared helper rather than left as a parallel implementation: it delegates to `resolveStorage` in one line and contains no second try/catch and no second `globalThis.localStorage` dereference"
    - "WR-01 is closed: `resolveStorage`'s and `resolveWeekStartStorage`'s LIVE-AND-WORKING branch (a readable `globalThis.localStorage` present) is covered by a test that also reads a value back through it. Mutating either function to `return null;` now turns the suite red — before this plan it stayed green while silently disabling week-start persistence for every real user"
    - "WR-02 is closed: `calendar.test.ts` carries a source-text regression guard proving `calendar.ts` (comments stripped) never dereferences `globalThis.localStorage`, never dereferences a bare `localStorage`, and does resolve through `resolveWeekStartStorage(`. Re-adding the access the gap-closure round removed now fails a test instead of passing every test in the repo"
    - "BL-03 documentation truth: `calendar-preferences.ts`'s header and `calendar.ts`'s rationale comment describe the app's ACTUAL behaviour. The false claim that an unguarded read here 'would take the whole view down through main.ts's generic error panel' is removed — under blocked site data an unguarded module-scope read kills the whole dashboard module graph and the page renders BLANK, with no nav, no view and no reachable error panel"
    - "22-07-PLAN.md's own must-have — 'T-22-WK-02 is TRUE as documented' — becomes satisfiable: the comments now name the app-wide guard (`storage.ts`) as what closes the blocked-site-data threat, rather than claiming a calendar-scoped guard closes an app-level threat"
    - "WR-04 counters are undisturbed: `calendar.ts` still contains exactly two `tabindex` occurrences and exactly two `.focus()` occurrences, and `setWeekStart`'s body still contains none of `focus`, `mount(`, `navigateTo`, `await`"
    - "T-22-WK-01 keeps exactly one trust path: `parseWeekStart`'s allow-list is still the ONLY way a stored value reaches the grid math. Neither `resolveStorage` nor `resolveWeekStartStorage` normalises, repairs, writes back, parses JSON, or emits console output (D-07)"
    - "This plan discharges NO browser claim. Whether a real blocked-site-data browser now boots the app is decided ONLY by plan 22-12's Round 3 row R22, which may not be declined and may not be waived"
  artifacts:
    - path: "src/dashboard/storage.ts"
      provides: "the shared `WebStorage` interface and `resolveStorage` handle resolver — the single app-wide storage-global dereference site"
      exports: ["WebStorage", "resolveStorage"]
      contains: "export function resolveStorage"
    - path: "src/dashboard/storage.test.ts"
      provides: "override identity, getter-throwing, absent-global AND live-and-working coverage, plus source guards on storage.ts's own narrowness"
      contains: "Reflect.deleteProperty"
    - path: "src/dashboard/views/calendar-preferences.ts"
      provides: "a delegating `resolveWeekStartStorage`, the unchanged week-start contract, and a header that describes the app-wide guard truthfully"
      contains: "resolveStorage"
    - path: "src/dashboard/views/calendar.test.ts"
      provides: "the WR-02 source-text regression guard against re-introducing a storage-global dereference in calendar.ts"
      contains: "globalThis"
  key_links:
    - from: "globalThis.localStorage (property access)"
      to: "a try/catch"
      via: "resolveStorage in src/dashboard/storage.ts"
      pattern: "export function resolveStorage"
    - from: "resolveWeekStartStorage"
      to: "resolveStorage"
      via: "one-line delegation, no parallel implementation"
      pattern: "return resolveStorage\\(override\\)"
    - from: "calendar.ts"
      to: "a storage global"
      via: "nothing — asserted absent by the WR-02 source guard"
      pattern: "resolveWeekStartStorage\\("
---

<objective>
Open the app-wide half of **Gap 2** of `22-VERIFICATION.md` — code-review finding **BL-03
(Critical)** — by introducing the shared `resolveStorage()` helper the user has locked in, wiring the
Calendar's own resolver to it, closing the two named test-quality gaps the verifier tied to this gap
(**WR-01**, **WR-02**), and making the two overclaiming source comments tell the truth.

Purpose: `resolveWeekStartStorage()` is locally sound and Round 2 row R15 confirmed it in a real
browser. But `calendar-preferences.ts`'s header (lines 19-31) and `calendar.ts`'s rationale comment
(lines 429-432) both claim it closes "a browser configuration where site data is blocked". It does
not. `main.ts:19` runs `applyThemeMode(readStoredMode(localStorage))` at MODULE SCOPE with no
try/catch; in that exact configuration the getter throws during module evaluation, the entire
dashboard module graph fails to evaluate, and the page renders **blank** — not the "generic error
panel" `calendar.ts`'s comment names as the fallback. The comment is wrong about the failure mode
regardless of which fix is chosen.

The user's locked decision is to fix it **app-wide** with a shared helper. This plan builds and
proves the helper and reconciles the Calendar with it; plan `22-11` wires the remaining consumers.

Output: `src/dashboard/storage.ts` + `src/dashboard/storage.test.ts`, a one-line delegating
`resolveWeekStartStorage`, the WR-01 live-branch test, the WR-02 source guard, and two corrected
comments.

**Explicitly NOT in scope.** This plan does NOT edit `main.ts`, `nav.ts`, `theme.ts`,
`detail-charts.ts` or `detail-charts-logic.ts` — plan `22-11` owns those and depends on the module
this plan creates. It does NOT build a shared view-preference facility, a key registry, or a generic
`readPreference`/`writePreference` pair: 22-CONTEXT.md lists that under **Deferred Ideas** and D-06
scopes the week-start contract to `calendar-preferences.ts`. **WR-04**, **WR-05**, **WR-08**–**WR-11**
are out of scope; this plan only avoids tripping WR-04's counters. `styles.css` and `styles.test.ts`
are plan `22-09`'s files and are not touched here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md
@.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md
@.planning/phases/22-calendar-week-start-totals/22-REVIEW.md
@.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md
@.planning/phases/22-calendar-week-start-totals/22-PATTERNS.md
@.planning/phases/22-calendar-week-start-totals/22-07-PLAN.md
</context>

<interfaces>
<!-- Current contracts extracted from source. No exploration needed to know what exists. -->

`src/dashboard/views/calendar-preferences.ts` (108 lines, as shipped by plan 22-07):

- `export const WEEK_START_STORAGE_KEY = 'dashboard-calendar-week-start'` (35)
- `export interface WeekStartStorage { getItem(key: string): string | null; setItem(key: string, value: string): void }` (37-40)
- `export function resolveWeekStartStorage(override?: WeekStartStorage): WeekStartStorage | null` (57-64)
  — `if (override) return override;` then `try { return globalThis.localStorage ?? null; } catch { return null; }`
- `export function parseWeekStart(raw: unknown): WeekStart` (72-75)
- `export function readStoredWeekStart(storage: WeekStartStorage | null): WeekStart` (84-91) — `if (!storage) return 'monday';`
- `export function writeWeekStart(storage: WeekStartStorage | null, value: WeekStart): void` (100-108) — `if (!storage) return;`
- header comment 1-31, carrying the T-22-WK-01 note (10-18) and the T-22-WK-02/CR-01 note (20-30)
  whose closure claim BL-03 falsifies

`src/dashboard/views/calendar.ts`:

- line 33: imports `readStoredWeekStart, writeWeekStart, type WeekStartStorage` (and
  `resolveWeekStartStorage`) from `./calendar-preferences.js`
- lines 424-432: the four-plus-line rationale comment, whose last clause ("doing that unguarded here
  would take the whole view down through `main.ts`'s generic error panel") is the false statement
- line 433: `const storage = resolveWeekStartStorage(deps.storage);`
- line 435: `let weekStart = readStoredWeekStart(storage);`
- the file contains exactly 2 `tabindex` occurrences and exactly 2 `.focus()` occurrences

`src/dashboard/views/calendar-preferences.test.ts` (191 lines): `fakeStorage(initial, { throwOnGet,
throwOnSet })` at 12-35 is the stand-in style to follow. The
`describe('resolveWeekStartStorage — CR-01 ...')` block at 133-191 holds five cases: override
identity; override with a throwing getter; throwing getter → `null`; global absent → `null`;
resolve-then-read under a throwing getter. **There is no case where `globalThis.localStorage` is
present and readable** — that is WR-01.

`src/dashboard/views/calendar.test.ts` (336 lines): `describe('calendar.ts — Phase 22
source-structure guards')` at 238-336 reads `calendarSource` via `readFileSync(new
URL('./calendar.ts', import.meta.url), 'utf8')` and asserts `.focus()` count 2, `tabindex` count 2,
segmented markup, and control ordering. It carries **no** guard against a storage-global
dereference — that is WR-02.

In-repo precedent for a comment-stripped source guard: `src/dashboard/styles.test.ts`'s
`cssNoComments` (line 31) and `src/dashboard/row-semantics.test.ts`.

The other unguarded sites (plan `22-11`'s scope, listed here so this plan's comments can name them
accurately): `main.ts:19` `applyThemeMode(readStoredMode(localStorage))` at module scope;
`nav.ts:186` `updateThemeToggle(readStoredMode(localStorage))` inside `createNav`, which `main.ts:22`
calls at module scope; `nav.ts:206` inside the theme-toggle click handler; `theme.ts:93` and
`theme.ts:130` `options.storage ?? localStorage`; `detail-charts.ts:218`
`options.storage ?? globalThis.localStorage`. `src/dashboard/index.html`'s inline pre-paint script
(lines 36-53) already wraps its `localStorage.getItem` in try/catch and is NOT a crash site.
</interfaces>

<settled_decisions>
| # | Choice | Decision | Rationale |
|---|--------|----------|-----------|
| GC-5a | Where the shared helper lives | A new `src/dashboard/storage.ts`, exporting `WebStorage` and `resolveStorage` | Verbatim `22-REVIEW.md` BL-03's sketch. `src/dashboard/` root (beside `theme.ts`, `nav.ts`, `router.ts`) because its consumers span the bootstrap, the nav chrome, the theme engine and two views — putting it under `views/` would make the bootstrap import upward out of a view directory |
| GC-5b | Helper shape | `resolveStorage(override?: WebStorage): WebStorage | null` — return `override` when supplied, else `globalThis.localStorage ?? null` inside try/catch, else `null` from the catch | Verbatim BL-03's sketch and identical in behaviour to the `resolveWeekStartStorage` Round 2 shipped and R15 observed working. Keeping the behaviour byte-identical means R15's PASS is not invalidated by this refactor |
| GC-5c | `WeekStartStorage` vs `WebStorage` | `export type WeekStartStorage = WebStorage;` — the interface is replaced by an alias, the exported NAME survives | `calendar.ts` imports `type WeekStartStorage` and `calendar-preferences.test.ts` imports it too; the alias keeps both compiling with zero churn while removing the parallel structural declaration. All three shapes (`WeekStartStorage`, `ThemeStorage`, `Pick<Storage,'getItem'|'setItem'>`) are already structurally identical |
| GC-5d | Is this the deferred "shared view-preference facility"? | **No, and the plan asserts it isn't.** `storage.ts` resolves a HANDLE only — no key, no `getItem`/`setItem` call, no parse, no registry | 22-CONTEXT.md defers a shared preference facility and D-06 keeps the week-start contract calendar-scoped. A handle resolver is the thing every one of the six unguarded sites already duplicates inline; generalising it is deduplication, not a new facility. Task 1's automated check enforces the narrowness so a future edit cannot quietly grow it into the deferred idea |
| GC-5e | Does `resolveWeekStartStorage` survive? | **Yes, as a one-line delegation.** It is not deleted and not left as a parallel implementation | Deleting it would churn `calendar.ts`, `calendar-preferences.test.ts` and plan 22-07's recorded `key_links` pattern for no gain; leaving it as a second try/catch is exactly the "parallel implementation" the user's decision rules out. Delegation satisfies both |
| GC-5f | WR-01's fix | A live-and-working case for BOTH `resolveStorage` and `resolveWeekStartStorage`, each reading a real value back through the resolved handle | `22-REVIEW.md` WR-01's own suggested fix. Asserting only `toBe(fake)` would pass on a resolver that returns the handle but is never actually used, so the case also drives `readStoredWeekStart(resolveWeekStartStorage())` to a non-default value |
| GC-5g | WR-02's fix | A comment-stripped source guard in `calendar.test.ts`, exactly as `22-REVIEW.md` WR-02 writes it | This repo already uses source-text guards for this class of invariant (`row-semantics.test.ts`, the `NAVIGABLE_ROW_CLASS` parity check). Comment-stripping is mandatory — `calendar.ts`'s own comments discuss `localStorage` at length, so a raw-source guard would be red on arrival |
| GC-5h | What the corrected comments must say | That the page renders BLANK under blocked site data if the bootstrap is unguarded — NOT that a generic error panel catches it | `main.ts`'s try/catch (53-78) wraps `view.mount(...)` inside `onMatch`, which is only reached after module evaluation succeeds. A module-scope throw at line 19 never reaches it. This is confirmed by direct source read in `22-VERIFICATION.md` and independently in `22-REVIEW.md` BL-03 |
</settled_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared storage.ts handle resolver and prove BOTH its failing and its working branches</name>
  <files>src/dashboard/storage.ts, src/dashboard/storage.test.ts</files>
  <read_first>
    - `src/dashboard/views/calendar-preferences.ts` — the whole file (108 lines). Lines 42-64 are the
      implementation being generalised; its JSDoc is the tone and the level of detail the new
      module's header must match.
    - `src/dashboard/views/calendar-preferences.test.ts` — the whole file (191 lines), especially
      `fakeStorage` (12-35) and the `resolveWeekStartStorage` describe (133-191) with its
      `Object.defineProperty(globalThis, 'localStorage', { configurable: true, get() { throw ... } })`
      stand-in and its `afterEach` `Reflect.deleteProperty` teardown. The new test file mirrors this
      shape rather than inventing a second style.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § BL-03 — the finding and its
      `src/dashboard/storage.ts` sketch, which this task implements.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § WR-01 — why the
      live-and-working branch matters ("mutating it to `return null` leaves the whole suite green …
      while silently disabling week-start persistence for every real user") and its suggested case.
    - `.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md` § Deferred Ideas — the
      "shared view-preference facility" entry, and D-06, which together define the fence this module
      must stay behind.
    - `src/dashboard/theme.ts` lines 1-20 and 85-110 — the module-header style used elsewhere in
      `src/dashboard/`, and the `options.storage ?? localStorage` shape plan `22-11` will replace.
      **Read for context only; do not edit `theme.ts` in this plan.**
    - `vitest.config.ts` — `environment: 'node'`, `include: ['src/**/*.test.ts']`, no jsdom. This is
      why `globalThis.localStorage` is absent by default and why the stand-ins are installed with
      `Object.defineProperty` rather than assigned.
  </read_first>
  <behavior>
    Write `src/dashboard/storage.test.ts` FIRST, watch it fail against a missing module, then
    implement `src/dashboard/storage.ts`:

    - `resolveStorage(fake)` returns the exact `fake` object (identity).
    - `resolveStorage(fake)` still returns `fake`, without throwing, while a `globalThis.localStorage`
      whose getter throws is installed — the override path never touches the global.
    - With `globalThis.localStorage` defined as a property whose GETTER throws,
      `resolveStorage()` does not throw and returns `null`.
    - With no `localStorage` on `globalThis` (the repo's default `environment: 'node'` state),
      `resolveStorage()` returns `null`.
    - **The WR-01 case:** with a WORKING `globalThis.localStorage` installed (a `fakeStorage`-shaped
      object holding one key), `resolveStorage()` returns that exact object AND a value written into
      it is readable back through the resolved handle. Mutating `resolveStorage`'s body to
      `return null;` must fail this case.
    - A full resolve cycle under the throwing getter writes nothing to the console: spy on
      `console.warn` and `console.error` and assert zero calls.
    - Source guards on the module's own narrowness (GC-5d): the file's text contains no
      `.getItem(` call, no `.setItem(` call, no `STORAGE_KEY` identifier, no `JSON.parse`, and no
      `console.` — it resolves a handle and nothing else.

    Restore `globalThis` in an `afterEach` with `Reflect.deleteProperty(globalThis, 'localStorage')`
    so neither stand-in can leak into any other test file in the suite.
  </behavior>
  <action>
    Create `src/dashboard/storage.ts` exporting exactly two things:

    - `export interface WebStorage { getItem(key: string): string | null; setItem(key: string, value: string): void }`
    - `export function resolveStorage(override?: WebStorage): WebStorage | null` — returns `override`
      immediately when one is supplied; otherwise reads `globalThis.localStorage` **inside a `try`
      block**, returning that handle or `null` when it is absent, and returning `null` from the
      `catch`.

    Give the module a header JSDoc that states, naming `BL-03` literally:

    1. this module owns the ONLY dereference of a storage global in `src/dashboard/` — every other
       module resolves its handle through `resolveStorage`;
    2. the throwing operation under blocked site data (Firefox "Block cookies and site data", Chrome
       "Don't allow sites to save data", a storage-partitioned iframe) is the PROPERTY GETTER, which
       throws before any `getItem`/`setItem` could run — so a try/catch around `getItem` is not
       sufficient on its own;
    3. why this is app-wide rather than calendar-scoped: `main.ts:19`, `nav.ts:186`, `nav.ts:206`,
       `theme.ts:93`, `theme.ts:130` and `detail-charts.ts:218` all dereferenced the global
       unguarded, and `main.ts:19`/`nav.ts:186` run at module scope, so under that configuration the
       whole dashboard module graph failed to evaluate and the page rendered BLANK — before any
       router, any view and any error panel existed;
    4. the D-06 fence, named literally: this module resolves a HANDLE. It declares no storage key,
       calls no `getItem`/`setItem`, parses no value and holds no key registry. Per-key contracts
       (`calendar-preferences.ts` for the week start, `theme.ts` for the theme mode,
       `detail-charts-logic.ts` for the overlay config) stay where they are; the shared
       view-preference facility 22-CONTEXT.md defers is deliberately NOT built here.

    Then write `src/dashboard/storage.test.ts` covering the `<behavior>` list, following
    `calendar-preferences.test.ts`'s stand-in style. Read the module's own source with
    `readFileSync(new URL('./storage.ts', import.meta.url), 'utf8')` for the narrowness guards, and
    strip comments before asserting absence (the header names `getItem`/`setItem` in prose).
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/storage.test.ts && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e 'import {readFileSync} from "node:fs"; const s=readFileSync("src/dashboard/storage.ts","utf8"); if(!/export function resolveStorage\(/.test(s)) throw new Error("resolveStorage is not exported"); if(!/export interface WebStorage/.test(s)) throw new Error("WebStorage is not exported"); const fn=s.slice(s.indexOf("export function resolveStorage(")); const body=fn.slice(0,fn.indexOf("\n}")); if(!/try\s*\{/.test(body)) throw new Error("resolveStorage has no try block"); if(!/catch/.test(body)) throw new Error("resolveStorage has no catch"); if(!/globalThis\.localStorage/.test(body)) throw new Error("resolveStorage does not read globalThis.localStorage"); if(body.indexOf("globalThis.localStorage")<body.indexOf("try")) throw new Error("the globalThis.localStorage access is not inside the try block"); if(!s.includes("BL-03")) throw new Error("the module header does not cite BL-03"); if(!s.includes("D-06")) throw new Error("the module header does not record the D-06 fence"); const live=s.replace(/\/\*[\s\S]*?\*\//g,"").replace(/\/\/.*$/gm,""); for(const banned of [".getItem(",".setItem(","STORAGE_KEY","JSON.parse","console."]) if(live.includes(banned)) throw new Error("storage.ts must stay a handle resolver - it must not contain "+banned); const t=readFileSync("src/dashboard/storage.test.ts","utf8"); if(!/defineProperty\([^)]*globalThis/.test(t)) throw new Error("no globalThis stand-in installed in storage.test.ts"); if(!/get\s*\(\s*\)\s*\{[\s\S]{0,160}throw/.test(t)) throw new Error("no getter-throwing stand-in"); if(!/Reflect\.deleteProperty/.test(t)) throw new Error("the stand-ins are not torn down - they will leak into other test files"); if(!/present and readable|live and working|live global/i.test(t)) throw new Error("WR-01: no live-and-working branch case"); console.log("storage.ts is a narrow, guarded handle resolver; both branches covered including WR-01s live one");'</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/storage.ts` exports `WebStorage` and `resolveStorage`, and `resolveStorage`'s
      body reads `globalThis.localStorage` **inside** a `try` and returns `null` from its `catch`.
    - The module header cites `BL-03` and records the `D-06` fence.
    - `storage.ts`, comments stripped, contains none of `.getItem(`, `.setItem(`, `STORAGE_KEY`,
      `JSON.parse`, `console.`.
    - `src/dashboard/storage.test.ts` installs `globalThis` stand-ins via `Object.defineProperty`
      (one whose getter throws, one that works), tears them down with `Reflect.deleteProperty` in an
      `afterEach`, and includes a case covering the live-and-working branch whose title names it.
    - Replacing `resolveStorage`'s body with `return null;` fails at least one case in
      `storage.test.ts` (WR-01's whole point) — confirm by reasoning against the written assertions,
      not by shipping the mutation.
    - `npx vitest run src/dashboard/storage.test.ts` exits 0 and `npx tsc --noEmit -p tsconfig.json`
      exits 0.
  </acceptance_criteria>
  <done>One guarded, narrow, app-wide storage-handle resolver exists, and both the branch that saves the page under blocked site data and the branch that serves every real user are covered by tests that fail if either is broken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile resolveWeekStartStorage with the shared helper and close WR-01 on the calendar path</name>
  <files>src/dashboard/views/calendar-preferences.ts, src/dashboard/views/calendar-preferences.test.ts</files>
  <read_first>
    - `src/dashboard/views/calendar-preferences.ts` — the whole file, especially the
      `WeekStartStorage` interface (37-40) being replaced by an alias and `resolveWeekStartStorage`
      (42-64) being reduced to a delegation. Read the JSDoc before rewriting it.
    - `src/dashboard/storage.ts` as created by Task 1 — the `WebStorage` shape and `resolveStorage`'s
      exact signature and semantics.
    - `src/dashboard/views/calendar-preferences.test.ts` — the whole file. The five existing
      `resolveWeekStartStorage` cases (133-191) must all still pass unchanged after the delegation;
      the new case is appended to that same describe.
    - `src/dashboard/views/calendar.ts` lines 33 and 415-440 — the import and the single call site,
      so the widened/aliased types are known to compile. **`calendar.ts` is edited in Task 3, not
      here.**
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § WR-01 — the exact suggested
      case, including its `readStoredWeekStart(resolveWeekStartStorage())` read-back.
    - `.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md` D-05, D-06 and D-07 — the
      persistence contract that must survive this refactor untouched: injectable storage, calendar-
      scoped module, validating allow-list parse with a silent Monday fallback and no repair.
  </read_first>
  <behavior>
    Add to the existing `resolveWeekStartStorage` describe in
    `src/dashboard/views/calendar-preferences.test.ts`, watch the new case fail if the resolver is
    stubbed, then implement:

    - **WR-01:** with a working `globalThis.localStorage` stand-in holding
      `{ [WEEK_START_STORAGE_KEY]: 'sunday' }`, `resolveWeekStartStorage()` returns that exact object
      AND `readStoredWeekStart(resolveWeekStartStorage())` returns `'sunday'` — not the `'monday'`
      default. Mutating `resolveWeekStartStorage` to `return null;` must fail this case.
    - `resolveWeekStartStorage` and `resolveStorage` agree: with the same working stand-in installed,
      `resolveWeekStartStorage()` and `resolveStorage()` return the same object; with the throwing
      getter installed, both return `null`.
    - Every pre-existing case in the file still passes unchanged — in particular the
      `throwOnGet`/`throwOnSet` cases, which exercise the method-throwing path the resolver does not
      replace, and the D-07 silence and no-repair cases.
  </behavior>
  <action>
    In `src/dashboard/views/calendar-preferences.ts`:

    - Import `resolveStorage` and `type WebStorage` from `../storage.js`.
    - Replace the `WeekStartStorage` interface declaration with
      `export type WeekStartStorage = WebStorage;`, keeping the exported name so `calendar.ts` and
      the test file compile unchanged. Give the alias a one-line comment recording that the shape is
      now declared once, in `storage.ts`, rather than restated per module.
    - Reduce `resolveWeekStartStorage`'s body to a single delegation —
      `return resolveStorage(override);`. After this task the file must contain **zero** occurrences
      of `globalThis.localStorage` and **zero** `try` blocks inside `resolveWeekStartStorage`; the
      guard now lives in exactly one place (GC-5e).
    - Rewrite `resolveWeekStartStorage`'s JSDoc to say what is now true: it exists as the Calendar's
      named entry point into the shared resolver (D-06 keeps the week-start contract in this module),
      it performs no guarding of its own, and `storage.ts`'s `resolveStorage` is where the property
      getter is wrapped. Cite `BL-03` alongside the existing `CR-01`.
    - Leave `WEEK_START_STORAGE_KEY`, `parseWeekStart`, `readStoredWeekStart` and `writeWeekStart`
      byte-identical in behaviour: same allow-list, same silent Monday fallback, same falsy-handle
      early returns, same try/catch around `getItem`/`setItem`, no console output (D-07).

    Then append the `<behavior>` cases to the existing
    `describe('resolveWeekStartStorage — CR-01 ...')` block in
    `src/dashboard/views/calendar-preferences.test.ts`, importing `resolveStorage` from
    `../storage.js` for the agreement case and reusing the file's own `fakeStorage` helper for the
    working stand-in. Keep the existing `afterEach` `Reflect.deleteProperty` teardown covering the
    new cases. Also drop the unused `beforeEach` import on line 1 (WR-11's one-line half that lands
    for free in a file this task is already editing).
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/calendar-preferences.test.ts src/dashboard/storage.test.ts && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e 'import {readFileSync} from "node:fs"; const p=readFileSync("src/dashboard/views/calendar-preferences.ts","utf8"); if(p.includes("globalThis.localStorage")) throw new Error("calendar-preferences.ts still dereferences a storage global - it must delegate to resolveStorage"); if(!/from "\.\.\/storage\.js"|from .\.\.\/storage\.js./.test(p)) throw new Error("calendar-preferences.ts does not import from ../storage.js"); if(!/export type WeekStartStorage = WebStorage/.test(p)) throw new Error("WeekStartStorage was not reduced to an alias of WebStorage"); const fn=p.slice(p.indexOf("export function resolveWeekStartStorage")); const body=fn.slice(0,fn.indexOf("\n}")); if(/try\s*\{/.test(body)) throw new Error("resolveWeekStartStorage still carries a parallel try/catch"); if(!/return resolveStorage\(override\)/.test(body)) throw new Error("resolveWeekStartStorage does not delegate to resolveStorage(override)"); for(const t of ["CR-01","BL-03","T-22-WK-01","T-22-WK-02","D-06"]) if(!p.includes(t)) throw new Error("calendar-preferences.ts lost the marker: "+t); if(/console\./.test(p)) throw new Error("D-07 forbids console output on these paths"); for(const keep of ["WEEK_START_STORAGE_KEY","parseWeekStart","readStoredWeekStart","writeWeekStart","if (!storage)"]) if(!p.includes(keep)) throw new Error("the week-start contract lost: "+keep); const t2=readFileSync("src/dashboard/views/calendar-preferences.test.ts","utf8"); if(/import \{[^}]*beforeEach/.test(t2)) throw new Error("the unused beforeEach import was not dropped"); if(!/resolveStorage/.test(t2)) throw new Error("no agreement case between resolveWeekStartStorage and resolveStorage"); if(!/present and readable|live and working|live global/i.test(t2)) throw new Error("WR-01: no live-and-working case on the calendar path"); if(!/readStoredWeekStart\(resolveWeekStartStorage\(\)\)/.test(t2)) throw new Error("WR-01: the live case does not read a value back through the resolved handle"); console.log("resolveWeekStartStorage delegates, no parallel guard, WR-01 closed on the calendar path");'</automated>
  </verify>
  <acceptance_criteria>
    - `calendar-preferences.ts` imports from `../storage.js`, declares
      `export type WeekStartStorage = WebStorage;`, and contains **zero** occurrences of
      `globalThis.localStorage`.
    - `resolveWeekStartStorage`'s body is `return resolveStorage(override);` with no `try` block of
      its own.
    - The file still contains `WEEK_START_STORAGE_KEY`, `parseWeekStart`, `readStoredWeekStart`,
      `writeWeekStart`, the `if (!storage)` early returns, and the markers `CR-01`, `BL-03`,
      `T-22-WK-01`, `T-22-WK-02`, `D-06`; it contains zero `console.` calls.
    - `calendar-preferences.test.ts` no longer imports `beforeEach`, contains a case asserting
      `readStoredWeekStart(resolveWeekStartStorage())` returns `'sunday'` against a working
      `globalThis` stand-in, and contains a case asserting `resolveWeekStartStorage()` and
      `resolveStorage()` agree.
    - All five pre-existing `resolveWeekStartStorage` cases still pass unchanged.
    - `npx vitest run src/dashboard/views/calendar-preferences.test.ts src/dashboard/storage.test.ts`
      exits 0 and `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>There is exactly one storage guard in the codebase, the Calendar reaches it through its own named entry point, and the branch that serves every real user is finally under test.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the two overclaiming comments (BL-03) and add the WR-02 regression guard</name>
  <files>src/dashboard/views/calendar.ts, src/dashboard/views/calendar.test.ts, src/dashboard/views/calendar-preferences.ts</files>
  <read_first>
    - `src/dashboard/views/calendar-preferences.ts` header lines 1-31 as left by Task 2 — the
      T-22-WK-01 note (10-18) and the T-22-WK-02/CR-01 note (20-30) whose closure claim is the
      thing being corrected.
    - `src/dashboard/views/calendar.ts` lines 415-440 — the rationale comment at 424-432 and the
      call site at 433. The clause to remove is "doing that unguarded here would take the whole view
      down through `main.ts`'s generic error panel for the sake of an optional cosmetic preference".
    - `src/dashboard/main.ts` — the whole file (88 lines). Line 19 is the module-scope read; lines
      53-78 are the try/catch whose catch renders the "Something went wrong" panel, and it wraps
      `view.mount(...)` INSIDE `onMatch`, which is only reachable after module evaluation succeeds.
      This is the source-level proof that the comment being corrected is false.
    - `src/dashboard/nav.ts` lines 1-11 and 180-210 — the header claim that this file "never touches
      localStorage directly" and the two places (`:186`, `:206`) where it does. `nav.ts` is plan
      `22-11`'s file; read it only so this plan's comments name the crash sites correctly.
    - `src/dashboard/views/calendar.test.ts` lines 238-336 — the Phase 22 source-structure guard
      describe. Note the whole-file counters at 280-284 (`.focus()` must be exactly 2) and 311-314
      (`tabindex` must be exactly 2): WR-04 is out of scope, so nothing written into `calendar.ts`
      may move either count.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § BL-03 and § WR-02 — the
      finding text and WR-02's suggested guard, including its comment-stripping preamble.
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` Gap 2 — the two artifact
      `issue:` entries naming exactly which comment says what, and the `missing:` item requiring the
      comments to state plainly what is and is not covered.
  </read_first>
  <action>
    **`calendar-preferences.ts` header (BL-03).** Rewrite the T-22-WK-02 note so it describes the
    shipped app. It must state, citing `BL-03` literally alongside the existing `CR-01`:

    - the read and the write are wrapped against a throwing `getItem`/`setItem` (Safari private mode,
      quota exceeded);
    - separately, the `localStorage` PROPERTY GETTER throws under blocked site data before either is
      called, and that access is now guarded in exactly one place app-wide — `src/dashboard/storage.ts`'s
      `resolveStorage` — which this module's `resolveWeekStartStorage` delegates to;
    - that as shipped in Rounds 1 and 2 this note claimed a calendar-scoped guard closed an
      app-level threat, and that claim was FALSE: `main.ts:19` and `nav.ts:186` ran the same
      unguarded read at module scope, so the whole dashboard module graph failed to evaluate and the
      page rendered BLANK before the Calendar's own working guard was ever reached;
    - that the closure claim is only true once plan `22-11` has wired `main.ts`, `nav.ts`, `theme.ts`
      and `detail-charts.ts` to the same resolver — name that dependency rather than asserting
      closure this module cannot deliver alone.

    Leave the T-22-WK-01 note's substance as Round 2 left it (allow-list is the only path to the grid
    math; `weekStartOffset` is total defence in depth); adjust only wording made stale by the
    resolver moving modules.

    **`calendar.ts` rationale comment (BL-03).** Rewrite lines 424-432. Keep the existing point about
    lazy resolution keeping the module importable in the Node test environment. **Delete the generic-
    error-panel clause entirely** and replace it with the truth: under blocked site data the property
    access throws, and an unguarded read reached at module scope kills the entire dashboard module
    graph — the page renders blank with no nav and no view content, and `main.ts`'s error panel is
    NOT reachable because that try/catch wraps `view.mount(...)` inside `onMatch`, which never runs.
    State that this is why Round 3 moved the guard into `src/dashboard/storage.ts` and applied it at
    every bootstrap-reachable site, and that this call site delegates to the same helper via
    `resolveWeekStartStorage`. Cite `BL-03`.

    **Hard constraint:** do not write the literal strings `tabindex` or `.focus()` anywhere in
    `calendar.ts`, in code or in a comment. `calendar.test.ts:280-284` and `:311-314` count
    whole-file occurrences and require exactly 2 of each; WR-04 (which would relax those counters) is
    out of scope, so tripping them is a self-inflicted red. Change no executable line in
    `calendar.ts` — line 433's `resolveWeekStartStorage(deps.storage)` stays exactly as it is.

    **`calendar.test.ts` (WR-02).** Add one `it(` case to the Phase 22 source-structure guard
    describe, titled so it names `WR-02`. It must build a comment-stripped view of `calendarSource`
    (strip `/* ... */` then `// ...` to end of line, the same two-step `22-REVIEW.md` WR-02 writes)
    and assert three things about that view: it does not match `/globalThis\.(localStorage|sessionStorage)/`;
    it does not match a bare `localStorage`/`sessionStorage` identifier (a pattern anchored so a
    property access like `.localStorage` or a longer identifier cannot match); and it does contain
    `resolveWeekStartStorage(`. Add a comment recording that this is the single regression the
    gap-closure round exists to prevent, that re-adding `const storage = globalThis.localStorage;`
    used to pass every test in the repository, and that comment-stripping is mandatory because
    `calendar.ts`'s own comments discuss `localStorage` at length.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/calendar.test.ts src/dashboard/views/calendar-preferences.test.ts src/dashboard/storage.test.ts && npm test && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e 'import {readFileSync} from "node:fs"; const c=readFileSync("src/dashboard/views/calendar.ts","utf8"); if(/generic error panel/.test(c)) throw new Error("calendar.ts still claims a reachable generic error panel - BL-03 says it is not reachable"); if(!c.includes("BL-03")) throw new Error("calendar.ts rationale comment does not cite BL-03"); if(!/blank/i.test(c)) throw new Error("calendar.ts rationale comment does not state the actual failure mode (a blank page)"); if(c.includes("globalThis.localStorage")) throw new Error("calendar.ts dereferences a storage global"); if(!c.includes("resolveWeekStartStorage(deps.storage)")) throw new Error("calendar.ts call site changed"); if((c.match(/tabindex/g)||[]).length!==2) throw new Error("calendar.ts tabindex count moved off 2 (WR-04 guard)"); if((c.match(/\.focus\(\)/g)||[]).length!==2) throw new Error("calendar.ts .focus() count moved off 2 (WR-04 guard)"); const p=readFileSync("src/dashboard/views/calendar-preferences.ts","utf8"); if(!p.includes("BL-03")) throw new Error("calendar-preferences.ts header does not cite BL-03"); if(!/main\.ts/.test(p)) throw new Error("the corrected header does not name the bootstrap crash site"); if(!/storage\.js|storage\.ts/.test(p)) throw new Error("the corrected header does not point at the shared resolver"); const t=readFileSync("src/dashboard/views/calendar.test.ts","utf8"); if(!t.includes("WR-02")) throw new Error("the WR-02 guard case is missing or unnamed"); if(!/replace\(\/\\\/\\\*/.test(t)&&!/\/\\\*\[\\s\\S\]\*\?\\\*\\\//.test(t)) throw new Error("the WR-02 guard does not strip block comments before scanning"); if(!/globalThis\\\.\(localStorage\|sessionStorage\)/.test(t)) throw new Error("the WR-02 guard does not assert the globalThis dereference is absent"); if(!/resolveWeekStartStorage\(/.test(t)) throw new Error("the WR-02 guard does not assert the resolver is used"); console.log("BL-03 comments corrected, WR-02 regression guard in place, WR-04 counters intact");'</automated>
  </verify>
  <acceptance_criteria>
    - `calendar.ts` no longer contains the string `generic error panel`; its rationale comment cites
      `BL-03` and states the real failure mode (a blank page, no reachable error panel).
    - `calendar.ts` contains `resolveWeekStartStorage(deps.storage)`, contains `globalThis.localStorage`
      zero times, and still contains exactly 2 occurrences of `tabindex` and exactly 2 of `.focus()`.
    - `calendar-preferences.ts`'s header cites `BL-03`, names `main.ts` as the bootstrap crash site,
      points at the shared resolver module, and no longer asserts that a calendar-scoped guard closes
      the app-level blocked-site-data threat on its own.
    - `calendar.test.ts` carries a case naming `WR-02` that strips comments from `calendarSource`
      before asserting no `globalThis.(localStorage|sessionStorage)` dereference, no bare
      `localStorage`/`sessionStorage` identifier, and the presence of `resolveWeekStartStorage(`.
    - `npm test` exits 0 with no test regressed and `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>The two comments that a maintainer would read as "blocked site data is handled" now say what actually happens and where the guard actually is, and the one regression this round exists to prevent is caught by a test instead of by review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser storage policy → module evaluation | The `localStorage` property getter is configuration-controlled: with site data blocked it throws before any method call. This boundary is crossed at module scope by the bootstrap, which is what makes BL-03 Critical rather than cosmetic |
| `localStorage` value → grid math | `localStorage` is user- and extension-writable (T-16-TH-01's precedent). `parseWeekStart`'s allow-list remains the only sanctioning step |
| source comment → future maintainer | A comment asserting a closed threat is a security control in its own right; a false one causes the next reader to skip a real hole. `22-VERIFICATION.md` scores this as a failed truth, not a style nit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-WK-02 | Denial of service | the `globalThis.localStorage` property access, app-wide | mitigate | `resolveStorage` wraps the property access in try/catch in exactly ONE place and returns `null`; every per-key reader already tolerates a null handle or is widened to (plan `22-11`). Locked by getter-throwing tests in `storage.test.ts` and `calendar-preferences.test.ts`, and observed in a real browser by plan `22-12` row R22 |
| T-22-GC3-ST-01 | Tampering | the week-start persistence contract | mitigate | WR-01's live-and-working case means a resolver silently returning `null` — which would disable persistence for every real user while leaving the whole suite green — now fails. WR-02's source guard means re-introducing the unguarded dereference in `calendar.ts` fails |
| T-22-WK-01 | Tampering | the persisted week-start value | mitigate | `parseWeekStart`'s exact allow-list stays the ONLY path from storage to grid math. `resolveStorage` and `resolveWeekStartStorage` return a handle, never a value, and perform no parsing, normalisation, repair or write-back (D-07) — enforced by Task 1's narrowness check |
| T-22-GC3-ST-02 | Elevation of privilege | the shared module's surface | mitigate | GC-5d fences `storage.ts` to handle resolution: no key constant, no `getItem`/`setItem`, no `JSON.parse`, no console. A future edit that grows it into the deferred shared-preference facility fails Task 1's automated check, so the D-06 boundary is enforced rather than trusted |
| T-22-GC3-ST-03 | Elevation of privilege | test-injected `globalThis` mutation | mitigate | Both stand-ins are installed with `configurable: true` and removed in `afterEach` via `Reflect.deleteProperty`, so neither can leak into the other 51 test files (`npm test` running green is the check) |
| T-22-GC3-ST-04 | Repudiation | silent storage failure | accept | The fallback stays deliberately silent (no console output) per D-07 and `theme.ts`'s precedent. A user with blocked storage gets a working app on defaults rather than a diagnostic; the trade is recorded here rather than left implicit |
| T-22-SC | Tampering | npm/pip/cargo installs | accept (not applicable) | Zero new packages. `22-RESEARCH.md` § Package Legitimacy Audit records "Not applicable"; no install task exists in any Phase 22 plan |
</threat_model>

<verification>
- Task 1 proves the shared resolver exists, guards the property getter inside a `try`, stays a
  handle resolver (no key, no `getItem`/`setItem`, no parse, no console), and has BOTH branches
  covered — including the live one WR-01 named as untested.
- Task 2 proves `calendar-preferences.ts` no longer carries a parallel implementation, delegates in
  one line, keeps the whole week-start contract intact, and closes WR-01 on the calendar path with a
  value read back through the resolved handle.
- Task 3 proves both overclaiming comments are corrected in the specific terms
  `22-VERIFICATION.md` names, that `calendar.ts`'s executable lines and WR-04 counters are
  untouched, and that WR-02's regression guard exists and strips comments before scanning.

**This plan discharges no browser claim.** Whether a real blocked-site-data browser now boots the app
is settled only by plan `22-12`'s Round 3 row **R22**, which is mandatory, may not be declined, and
may not be observed by an agent (PROJECT.md line 49).
</verification>

<success_criteria>
- Exactly one storage-global dereference exists in `src/dashboard/` and it sits inside a `try`.
- `resolveWeekStartStorage` delegates to it in one line; no parallel implementation remains.
- `storage.ts` is a handle resolver and provably not the deferred shared-preference facility.
- WR-01 and WR-02 are both closed by tests that fail on the mutation they exist to catch.
- `calendar-preferences.ts`'s header and `calendar.ts`'s rationale comment describe the app's actual
  behaviour under blocked site data, and name the app-wide guard as what closes it.
- `npm test` and `npx tsc --noEmit -p tsconfig.json` both exit 0.
- **No requirement is ticked by this plan.**
</success_criteria>

<output>
Create `.planning/phases/22-calendar-week-start-totals/22-10-SUMMARY.md` when done.
</output>
