---
phase: 22-calendar-week-start-totals
plan: 16
type: execute
wave: 14
depends_on: ["22-13", "22-14", "22-15"]
gap_closure: true
round: 4
files_modified:
  - .planning/phases/22-calendar-week-start-totals/22-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CAL-01, CAL-02, CAL-03]
user_setup: []
must_haves:
  decisions: [D-03, D-05, D-10, D-16, GC-5, GC-6, GC-7, GC-8, GC-10]
  truths:
    - "GC-6/GC-10: `22-VALIDATION.md` stays APPEND-ONLY. The Round 4 section, its house rule 14, its five-row agenda R24..R28 and its row-to-requirement map were staged by the planner and are ALREADY IN THE FILE — this plan inserts the build-freshness preamble beneath the `## Round 4` heading and fills the Observation and Verdict cells. The Round 1 (R1-R11), Round 2 (R12-R17) and Round 3 (R18-R23) sections, their verdicts and their frontmatter dates are not rewritten. The file ends with exactly 28 `R22-VERDICT` tokens (11 + 6 + 6 + 5)."
    - "GC-10 / NO WAIVER, HARD FAIL: house rule 1 CANNOT be waived for R25, R26 or R27. Three rounds have now been scored down for thin or agent-substituted evidence on precisely the rows that decided a gap — R19 PASSED at one width and Gap 1 reopened; R22 PASSED without clicking anything and a Critical shipped. If an agent observes R25, R26 or R27, that row is recorded **BLOCKED**, never PASS, and the executor's checkpoint prompt must refuse the observation rather than perform it."
    - "GC-10 / R27 IS MANDATORY AND MAY NOT BE DECLINED. It is the only row that exercises the control CR-01 broke, under the only configuration that breaks it. If declined, the row is BLOCKED, Gap 2 is recorded STILL OPEN, and it may not be closed on `nav-theme.test.ts`'s unit evidence or on source reading."
    - "R25 states a PIXEL WIDTH STRICTLY ABOVE 380px and confirms BOTH `matchMedia('(max-width: 640px)').matches === true` AND `matchMedia('(max-width: 380px)').matches === false`. The second confirmation is what makes the row new evidence rather than a repeat of R19's 375px observation — the two bands were governed by disjoint CSS rule sets until plan 22-15."
    - "R27 records THREE `aria-label` readings in order and an explicit statement of whether the page colours changed on each click. `Theme: dark` must be reached at least once. A row answered 'the toggle works' is not answered — R22 was answered at that level of detail and missed this exact defect."
    - "The staged build is proven FRESH before any row is judged: the served JS filename differs from ALL THREE previously observed builds (`index-YqJHQsHW.js`, `index-Dlom2BM3.js`, `index-Bsnjp2E6.js`), the served CSS filename differs from both previously observed stylesheets (`index-aaEmW9us.css`, `index-C-Jvo-sR.css`), and the served CSS carries a `@media (max-width: 640px)` block containing `.calendar-week-total` and `minmax(0, max-content)` — a POSITIVE Round 4 discriminator, not merely a changed hash."
    - "D-16 discipline holds: R28(ii) reads the five October 2025 Monday-start week-total triples back against the table recorded in the Round 1 preamble, so a regression from the widened CSS breakpoint or the nav/theme refactor cannot hide behind 'the totals are there'."
    - "R28(i) closes the evidence gap `22-VERIFICATION.md` recorded against R23: the week-start persistence half was answered only by a stale bare 'confirm' given before R22 was run. This round asks it properly, after the browser setting is restored."
    - "No source file is edited during the checkpoint session; `git status --porcelain src scripts` is empty at the end."
    - "Requirement ticks follow the Round 4 row map exactly and nothing else. CAL-02's current 'Complete' tick is REVERTED to Pending at Task 1 because `22-VERIFICATION.md` found it OVERSTATED, and re-ticks only if R24, R25, R26 and R28(ii) are ALL PASS. CAL-01's tick survives only if R27 and R28 are both PASS. CAL-03 is not re-gated this round and its state is left exactly as it is."
  artifacts:
    - path: ".planning/phases/22-calendar-week-start-totals/22-VALIDATION.md"
      provides: "a Round 4 build-freshness preamble, five answered rows R24..R28 with verdicts, a Checkpoint Outcome and a Gap Closure Record dispositioning all three 22-VERIFICATION.md gaps"
      contains: "## Round 4"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CAL-01/CAL-02/CAL-03 states matching the Round 4 row map, with CAL-02's overstated Round 3 tick corrected"
      contains: "CAL-02"
  key_links:
    - from: "the re-served build at 127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10"
      to: "rows R25 and R26"
      via: "the developer narrowing to a stated width above 380px and to ~600px and reading values back"
      pattern: "R22-VERDICT"
    - from: "a real browser-level blocked-site-data setting plus three clicks on the theme toggle"
      to: "row R27"
      via: "the aria-label read back after each click"
      pattern: "R22-VERDICT"
    - from: "each answered Round 4 row"
      to: "a REQUIREMENTS.md checkbox"
      via: "the Row-to-requirement map (Round 4)"
      pattern: "R22-VERDICT"
---

<objective>
Run the **blocking Round 4 browser checkpoint** and record the result. This is the plan that decides
whether Phase 22's three remaining items are actually closed, after three rounds in which each round
closed the literal statement of a gap while leaving the defect class alive one step away.

Purpose: `22-VERIFICATION.md` returns `gaps_found` at 5/8. Gap 1 (CAL-02/SC3) was REOPENED because
Round 3's fix was gated at `@media (max-width: 380px)` while the defect-causing rules stayed
unconditional above it, and R19 — the non-waivable row that closed it — observed exactly one width,
375px. Gap 2 is a NEW Critical that Round 3's own fix made reachable: the header theme toggle is
permanently stuck on light under blocked site data, and R22 — the other non-waivable row — proved the
page rendered without ever clicking anything on it. Plans `22-13`, `22-14` and `22-15` wrote the
fixes; only a developer looking at this build can close them.

Output: a completed `## Round 4` section in `22-VALIDATION.md` — freshness preamble, five answered
rows R24..R28 with verdicts, a Checkpoint Outcome and a Gap Closure Record dispositioning all three
`22-VERIFICATION.md` items — and `REQUIREMENTS.md` updated strictly on the Round 4 row map.

**The Round 4 agenda already exists in the file.** The planner staged `## Round 4`, its
`### What Round 4 ships` block, `### House rules (Round 4)` (rule 14), the five-row
`### Agenda (Round 4)` table with empty Observation/Verdict cells, and
`### Row-to-requirement map (Round 4)`. Task 1 does NOT create them — it inserts the build-freshness
preamble immediately below the `## Round 4` heading and asserts everything else is present and
unanswered.

**No staged fixture is needed or permitted.** The archive supplies everything: October 2025's
Monday-start week totals are recorded verbatim in the Round 1 preamble and re-read at R28(ii). Nothing
under `dist/` or `data/` may be edited.

**Explicitly NOT in scope (D-15).** No row is opened against Trends' weekly volume,
`records-logic.ts`'s biggest week, or the streak logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md
@.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md
@.planning/phases/22-calendar-week-start-totals/22-REVIEW.md
@.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md
@.planning/phases/22-calendar-week-start-totals/22-12-PLAN.md
@.planning/phases/22-calendar-week-start-totals/deferred-items.md
</context>

<cache_trap>
`127.0.0.1` alone is NOT sufficient. This project has been bitten repeatedly: Phase 21 Round 1's R13
only passed after a hard reload cleared a stale cached `streaks.json`, and the staged-build trap
recurs with a stale `index.html` / `index.json` in the observing tab.

Every checkpoint session must: browse `127.0.0.1`, never `localhost`; serve under the
`/strava-widgets` project path, never the server root; and hard-reload (Cmd+Shift+R, Cmd+Option+R in
Safari, or DevTools open with "Disable cache" ticked, then reload) before judging any row.

**Round 4's build-freshness bar is the strictest of the phase, because THREE builds have now been
observed.** Round 1 was judged against `assets/index-YqJHQsHW.js`. Round 2 against
`assets/index-Dlom2BM3.js` + `assets/index-aaEmW9us.css`. Round 3 against `assets/index-Bsnjp2E6.js` +
`assets/index-C-Jvo-sR.css`. If Round 4 is judged against any of them, the Round 4 fixes are not in the
observed artifact and every verdict is worthless.

Round 4 additionally has a POSITIVE served-text discriminator the earlier rounds lacked for the CSS
side: the served stylesheet must carry a `@media (max-width: 640px)` block that contains both
`.calendar-week-total` and `minmax(0, max-content)`. A build that does not carry it is pre-`22-15`.
Honestly noted: the JS-side changes (`nav-theme.ts`'s controller, the null-honouring resolver) cannot
be discriminated in a minified bundle by string match, so their freshness rests on the changed filename
plus the behavioural rows R27 and R28.

The `localStorage` surface persists too: R27 requires a browser-wide storage state and R28 requires it
restored, and each is judged only after a reload.
</cache_trap>

<round_carryover>
Facts the Round 4 agenda quotes. All are already recorded in `22-VALIDATION.md`'s earlier sections and
are restated here so the executor does not re-derive them.

**The claim R25 re-asks, for the fourth time, in a band no row has ever entered.**
Round 1 R11 (FAIL), developer's words: *"Total column values seem fine. Day cell values can slightly
overflow once width to narrow"*, *"behavior similar in light and dark theme"*.
Round 2 R13 (FAIL), the re-ask against the fixed build: *"Days still overflow. Total colum remains wide
(wider than any other column) and all text fits. But other columns (day columns) become narrow and
distance text overflows."* No width was stated — the agent's resize attempt never engaged the media
query.
Round 3 R19 (PASS), at a stated **375px** via Chrome DevTools device emulation with
`matchMedia('(max-width: 380px)').matches === true`: *"Day cell texts doesn't overflow. It gets stacked
until there is space so one row could have only the first number of a distance for instance."* and
*"dark and light behave same"*. **That PASS is valid for the sub-380px half of the band and nothing
else.** R25 exists because 381-640px was governed by disjoint CSS rules until plan `22-15`, and no row
in any round has ever been judged there.

**The control R27 exercises, which R22 rendered but never touched.** Round 3 R22 (PASS,
non-waivable, three stages), developer's words: *"Safari,blocked cookies. Nav rendered and grid.
Started at monday as default. no errors. a)."* The page rendered — and the theme toggle on it was
broken the whole time.

**The half R28(i) re-asks.** Round 3 R23 (PASS, thin/waived), developer's words after the restore:
*"1. Yeah theme sticks after allowing cookines."* The week-start half — select Sunday, hard-reload,
confirm `Sunday` filled with `Sun` first — was answered only by an earlier, stale bare *"R23: confirm"*
given BEFORE R22 was run. `22-VERIFICATION.md` records this as a real evidence-quality gap.

**October 2025, Monday-start week totals** (from the live archive, `data/dashboard/index.json`,
1,868 activities):

| Wk | Days (Oct) | Distance | Time | Runs |
|-----|------------|----------|------|------|
| 1 | 1-5 | 59.1 km | 5h 42m | 5 |
| 2 | 6-12 | 80.0 km | 7h 53m | 6 |
| 3 | 13-19 | 80.0 km | 7h 58m | 5 |
| 4 | 20-26 | 80.0 km | 7h 42m | 6 |
| 5 | 27-31 | 58.1 km | 5h 32m | 5 |

**Known rounding artifact — NOT a bug.** The month header renders `357.3 km`; the Monday-start rows
display-sum to `357.2 km`, purely from independent per-row `toFixed(1)`. Unrounded metres reconcile at
357.349 km under both week starts. Do not open a row about it.

**Known and accepted, not a defect.** `deferred-items.md` records that at very narrow widths a distance
value may wrap after its first digit (`overflow-wrap: anywhere` has no preference for breaking between
tokens). R19's own criterion treats wrapping as intended behaviour, and R25/R26 carry the same clause.

**The Sunday-start values, for the contradiction guard only:** October 2025 rows 3 and 4 read
`56.0 km / 5h 27m / 4 runs` and `104.1 km / 10h 14m / 7 runs` under Sunday. R28(ii) is a Monday-start
reading; those values must not appear in its PASS cell.

**Bundles already observed:** Round 1 `assets/index-YqJHQsHW.js`; Round 2 `assets/index-Dlom2BM3.js` +
`assets/index-aaEmW9us.css`; Round 3 `assets/index-Bsnjp2E6.js` + `assets/index-C-Jvo-sR.css`.

**The three aria-label strings R27 reads back**, from `nav.ts`'s `THEME_MODE_LABEL`:
`Theme: light`, `Theme: dark`, `Theme: auto`.
</round_carryover>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the gate, serve a PROVABLY FRESH Round 4 build under /strava-widgets, insert the Round 4 freshness preamble, and revert CAL-02's overstated tick</name>
  <files>.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` — the whole file. The
      Per-Task Verification Map, the Round 1 section with its two October tables, the Round 2 and
      Round 3 sections with their preambles and house rules, and — critically — the `## Round 4`
      section the PLANNER already appended: `### What Round 4 ships`, `### House rules (Round 4)`,
      `### Agenda (Round 4)` with five rows R24..R28 and empty Observation/Verdict cells, and
      `### Row-to-requirement map (Round 4)`. **You are not creating any of those.** Nothing above
      `## Round 4` is rewritten except the Per-Task Verification Map's Status column.
    - `.planning/phases/22-calendar-week-start-totals/22-12-PLAN.md` Task 1 — the staging precedent
      this task follows: the gate commands, the `/tmp/gh-pages/strava-widgets` symlink, the
      `python3 -m http.server 8099 --directory /tmp/gh-pages` serve step, the served-artifact
      freshness assertions, and the preamble contents.
    - This plan's `<cache_trap>` and `<round_carryover>` blocks — both are copied verbatim into the
      Round 4 preamble.
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` — the three gaps as worded
      and its two `human_verification` frontmatter items, which the Round 4 rows are measured against.
    - `.planning/phases/22-calendar-week-start-totals/22-13-SUMMARY.md`, `22-14-SUMMARY.md` and
      `22-15-SUMMARY.md` — what actually shipped, so the preamble's fix inventory is quoted accurately
      rather than from this plan's expectations. If any of them records a deviation, the preamble must
      say so.
    - `.planning/phases/22-calendar-week-start-totals/deferred-items.md` — the two documented
      environmental gate failures (`data/stats/` and `data/dashboard/index.json` absent in a fresh
      worktree) so a failure of that exact shape is recorded rather than chased.
    - `.planning/REQUIREMENTS.md` lines 33-35 and 88-90 — CAL-01/CAL-02/CAL-03's checkbox entries and
      the phase status table rows, both of which this task edits for CAL-02 only.
  </read_first>
  <action>
    First re-run the phase gate and fail the task if any part is not clean: `npx tsc --noEmit -p
    tsconfig.json`, `npx vitest run src/dashboard`, and `npm run build-widgets` (whose captured output
    must contain no `css-syntax-error`). Then run `npm test` and `npm run verify-dashboard` and record
    both results in the preamble: if either fails ONLY in the shape `deferred-items.md` documents
    (the five `data/stats/`-dependent suites, or a missing generated `data/dashboard/index.json`),
    record that verbatim as a pre-existing environmental failure and continue; any OTHER failure fails
    the task.

    Then stage the build exactly as GitHub Pages serves it, under the `/strava-widgets` project path
    and never at the server root: `mkdir -p /tmp/gh-pages` and
    `ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets`; assert
    `/tmp/gh-pages/strava-widgets/index.html` exists and that `readlink` on the symlink returns an
    absolute path. Serve with `python3 -m http.server 8099 --directory /tmp/gh-pages` in the
    background and confirm `http://127.0.0.1:8099/strava-widgets/index.html` and
    `http://127.0.0.1:8099/strava-widgets/data/dashboard/index.json` both respond 200 over HTTP.

    **Then prove the served artifact is THIS round's build.** Read the `assets/index-*.js` and
    `assets/index-*.css` filenames out of the served `index.html`. The JS filename must be none of
    `assets/index-YqJHQsHW.js`, `assets/index-Dlom2BM3.js`, `assets/index-Bsnjp2E6.js`; the CSS
    filename must be neither `assets/index-aaEmW9us.css` nor `assets/index-C-Jvo-sR.css`. Fetch the
    CSS over HTTP and assert POSITIVELY that one of its `max-width: 640px` blocks contains BOTH
    `.calendar-week-total` and `minmax(0, max-content)`, and that NO `max-width: 380px` block in the
    served CSS contains `.calendar-`. Record both filenames.

    Do NOT edit anything under `dist/` or `data/`.

    Then insert a **preamble** into `22-VALIDATION.md` immediately after the `## Round 4` heading and
    before the existing `### What Round 4 ships` heading, containing in this order: the served URL
    prefix `http://127.0.0.1:8099/strava-widgets/`; the calendar route this session uses
    (`http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10`, and the same route under
    Sunday-start for R28(i)); the observed JS and CSS filenames with an explicit sentence that the JS
    filename differs from ALL THREE prior rounds' and the CSS from both prior stylesheets; the
    positive `max-width: 640px` served-CSS assertion result; and every gate command with its result,
    including any deferred-items-shaped failure recorded verbatim. Then the whole `<cache_trap>` block
    and the whole `<round_carryover>` block from this plan, including R11's, R13's, R19's, R22's and
    R23's verbatim words, the Monday-start table, the rounding artifact, the accepted mid-number
    wrapping item, the Sunday-start contradiction-guard values, all five prior bundle filenames, and
    the three `Theme: …` aria-label strings. Then one sentence recording the D-15 scope fence.

    The preamble must NOT contain the literal phrases `Required detail` or `Observer required` — those
    belong to agenda rows only, and this task's check counts them.

    Do not add, remove, reorder or reword any of the five agenda rows; do not touch
    `### House rules (Round 4)` or `### Row-to-requirement map (Round 4)`; and leave every Observation
    and Verdict cell empty — zero `R22-VERDICT` tokens may exist inside the Round 4 section before the
    checkpoint.

    Set the file's frontmatter `round4_staged` to today's date. Leave `round: 4`,
    `nyquist_compliant: false` and `status: partial` as the planner set them, and do not alter
    `round1_*`, `round2_*`, `round3_*` or `round4_planned`.

    Then update the Per-Task Verification Map with three rows for this round's automated coverage:
    CAL-02 / no threat ref — "the calendar compaction breakpoint is parsed from its own prelude and
    asserted `>= 530px`, so the fix's COVERAGE BAND is enforced, not merely its existence" — unit,
    `npx vitest run src/dashboard/styles.test.ts`, task `22-15-T2`, wave 12; CAL-01 / T-22-R4-04 —
    "three consecutive toggles advance light->dark->auto with no storage handle available, and a
    source guard proves `nav.ts` performs no per-click storage read" — unit,
    `npx vitest run src/dashboard/nav-theme.test.ts`, task `22-14-T1`, wave 13; CAL-01 / T-22-R4-03 —
    "an explicit `storage: null` is honoured, proven against an installed sentinel global that records
    every write, with a control case proving the fallthrough branch still writes" — unit,
    `npx vitest run src/dashboard/theme.test.ts src/dashboard/storage.test.ts`, task `22-13-T2`,
    wave 12.

    Finally, in `.planning/REQUIREMENTS.md`, **revert CAL-02's tick**: change its checkbox from `[x]`
    to `[ ]` and its phase-status-table entry from `Complete (Round 3, ...)` to
    `Pending (Round 3 tick REVERTED 2026-08-19 — 22-VERIFICATION.md found it OVERSTATED: R19's single
    375px observation does not support the requirement's own "every viewport" language, and the
    identical overflow-causing rules were confirmed unconditional at 381px+; re-decided by Round 4 rows
    R24/R25/R26/R28(ii), plan 22-16)`. Append that same sentence to CAL-02's checkbox paragraph
    without deleting its existing Round 1/2/3 history. Do NOT touch CAL-01's or CAL-03's entries in
    this task — they are decided at Task 3.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx vitest run src/dashboard && rm -f /tmp/bw22r4.log && npm run build-widgets > /tmp/bw22r4.log 2>&1; BW=$?; tail -3 /tmp/bw22r4.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw22r4.log && mkdir -p /tmp/gh-pages && ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets && test -f /tmp/gh-pages/strava-widgets/index.html && readlink /tmp/gh-pages/strava-widgets | grep -q '^/' && curl -sf -o /dev/null http://127.0.0.1:8099/strava-widgets/index.html && curl -sf -o /dev/null http://127.0.0.1:8099/strava-widgets/data/dashboard/index.json && test -z "$(git status --porcelain src scripts data dist)" && node --input-type=module -e '
const base="http://127.0.0.1:8099/strava-widgets/";
const html=await (await fetch(base+"index.html")).text();
const js=(html.match(/assets\/index-[A-Za-z0-9_-]+\.js/)||[])[0];
const cssref=(html.match(/assets\/index-[A-Za-z0-9_-]+\.css/)||[])[0];
if(!js||!cssref) throw new Error("could not read the asset filenames from the served index.html");
for(const stale of ["assets/index-YqJHQsHW.js","assets/index-Dlom2BM3.js","assets/index-Bsnjp2E6.js"]) if(js===stale) throw new Error("the served bundle is a PREVIOUS rounds build ("+stale+") - the Round 4 fixes are not in the observed artifact");
for(const stale of ["assets/index-aaEmW9us.css","assets/index-C-Jvo-sR.css"]) if(cssref===stale) throw new Error("the served stylesheet is a previous rounds ("+stale+") - the 22-15 breakpoint move is not in the observed artifact");
const css=await (await fetch(base+cssref)).text();
const walk=(w)=>{const out=[];const re=new RegExp("@media\\s*\\(max-width:\\s*"+w+"px\\)\\s*\\{","g");let m;while((m=re.exec(css))){let i=m.index+m[0].length,d=1;while(d>0&&i<css.length){if(css[i]==="{")d++;else if(css[i]==="}")d--;i++;}out.push(css.slice(m.index,i));}return out;};
const b640=walk(640).filter(b=>b.includes(".calendar-week-total"));
if(b640.length!==1) throw new Error("expected exactly one served 640px block mentioning .calendar-week-total, found "+b640.length+" - stale CSS");
if(!(b640[0].includes("minmax(0,max-content)")||b640[0].includes("minmax(0, max-content)"))) throw new Error("the served 640px calendar block does not carry the BL-01 track fix - stale CSS");
if(walk(380).some(b=>b.includes(".calendar-"))) throw new Error("a served 380px block still governs a .calendar- selector - the 22-15 move is not in the observed artifact");
console.log("served build is fresh for Round 4: "+js+" "+cssref);
' && node -e '
const fs=require("fs");
const p=".planning/phases/22-calendar-week-start-totals/22-VALIDATION.md";
const s=fs.readFileSync(p,"utf8");
const i=s.indexOf("\n## Round 4\n"); if(i<0) throw new Error("the Round 4 section is missing");
const t=s.slice(i);
if((s.match(/\n## Round 4\n/g)||[]).length!==1) throw new Error("expected exactly one Round 4 heading");
const ids=["R24","R25","R26","R27","R28"];
const rows=t.split("\n").filter(l=>ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(l)));
if(rows.length!==5) throw new Error("expected 5 agenda rows, found "+rows.length);
ids.forEach((id,n)=>{ if(!new RegExp("^\\|\\s*"+id+"\\.").test(rows[n])) throw new Error("agenda row "+(n+1)+" should begin with "+id+"."); if(rows[n].split("|").length<6) throw new Error("row "+id+" does not have five columns"); });
if((t.match(/Required detail/g)||[]).length!==5) throw new Error("expected exactly one Required detail clause per row");
if((t.match(/Observer required/g)||[]).length!==5) throw new Error("expected exactly one Observer required clause per row");
for(const id of ["R25","R26","R27"]){ const r=rows.find(x=>new RegExp("^\\|\\s*"+id+"\\.").test(x)); if(!/NON-WAIVABLE/i.test(r)) throw new Error(id+" lost its NON-WAIVABLE observer clause"); }
const r27=rows.find(x=>/^\|\s*R27\./.test(x));
if(/NOT EXERCISABLE for this row|has no .NOT EXERCISABLE/.test(r27)===false && /There is no `NOT EXERCISABLE` disposition/.test(r27)===false) throw new Error("R27 must state it has no NOT EXERCISABLE disposition");
if(!/may not be declined/i.test(r27)) throw new Error("R27 does not state that it may not be declined");
const r25=rows.find(x=>/^\|\s*R25\./.test(x));
if(!/max-width: 640px/.test(r25)||!/max-width: 380px/.test(r25)) throw new Error("R25 must require BOTH matchMedia confirmations");
if((t.match(/R22-VERDICT/g)||[]).length!==0) throw new Error("no verdict token may exist in Round 4 before the checkpoint");
if((s.match(/R22-VERDICT/g)||[]).length!==23) throw new Error("Rounds 1-3 verdict count changed - those sections must not be rewritten");
const pre=t.slice(0, t.indexOf("### What Round 4 ships"));
for(const n of ["127.0.0.1:8099/strava-widgets/","2025-10","index-YqJHQsHW.js","index-Dlom2BM3.js","index-Bsnjp2E6.js","index-aaEmW9us.css","index-C-Jvo-sR.css","59.1 km","80.0 km","7h 58m","7h 42m","58.1 km","357.349","max-width: 640px","minmax(0, max-content)","Theme: dark","D-15","hard reload","NON-WAIVABLE"]) if(!pre.includes(n)) throw new Error("the Round 4 preamble is missing: "+n);
if(/Required detail/.test(pre)||/Observer required/.test(pre)) throw new Error("the preamble must not use the row-only clause phrases");
if(/localhost:8099/.test(s)) throw new Error("localhost:8099 appears - 127.0.0.1 is required");
if(!/Row-to-requirement map \(Round 4\)/.test(t)) throw new Error("the Round 4 row-to-requirement map is missing");
const fm=s.split("---")[1]||"";
for(const k of ["round: 4","round4_staged","nyquist_compliant: false","status: partial","round1_answered","round2_answered","round3_answered","round4_planned"]) if(!fm.includes(k)) throw new Error("frontmatter is missing or changed: "+k);
const req=fs.readFileSync(".planning/REQUIREMENTS.md","utf8");
if(!/- \[ \] \*\*CAL-02\*\*/.test(req)) throw new Error("CAL-02s overstated tick was not reverted to an unticked checkbox");
if(/\| CAL-02 \| Phase 22 \| Complete/.test(req)) throw new Error("CAL-02s phase-status row still reads Complete");
if(!/REVERTED/.test(req)) throw new Error("CAL-02s reversion is not recorded with a reason");
if(!/- \[x\] \*\*CAL-01\*\*/.test(req)||!/- \[x\] \*\*CAL-03\*\*/.test(req)) throw new Error("CAL-01/CAL-03 must not be altered in Task 1");
console.log("5 staged rows R24..R28, 0 new verdicts, Rounds 1-3 intact at 23, fresh Round 4 build, CAL-02 reverted");
'</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.json` exits 0, `npx vitest run src/dashboard` is green, and
      `npm run build-widgets` exits 0 with zero `css-syntax-error` in its output.
    - `npm test` and `npm run verify-dashboard` results are recorded in the preamble; any failure is
      either absent or is exactly the shape `deferred-items.md` documents and is quoted as such.
    - `/tmp/gh-pages/strava-widgets/index.html` exists, the symlink resolves to an absolute path, and
      both the served `index.html` and `data/dashboard/index.json` respond 200 from `127.0.0.1:8099`.
    - The served JS filename is none of `index-YqJHQsHW.js`, `index-Dlom2BM3.js`, `index-Bsnjp2E6.js`;
      the served CSS filename is neither `index-aaEmW9us.css` nor `index-C-Jvo-sR.css`.
    - The served CSS has exactly one `max-width: 640px` block mentioning `.calendar-week-total`, that
      block contains `minmax(0, max-content)`, and no served `max-width: 380px` block mentions
      `.calendar-`.
    - `git status --porcelain src scripts data dist` is empty.
    - `22-VALIDATION.md` has exactly one `## Round 4` heading, five agenda rows `R24.`..`R28.` in
      order with five columns each, exactly five `Required detail:` and five `Observer required:`
      clauses, and **zero** `R22-VERDICT` tokens inside Round 4. The file still holds exactly 23
      `R22-VERDICT` tokens overall.
    - R25, R26 and R27 all still carry `NON-WAIVABLE`; R27 still states it may not be declined and has
      no `NOT EXERCISABLE` disposition; R25 still requires both the `max-width: 640px` and
      `max-width: 380px` `matchMedia` confirmations.
    - The Round 4 preamble sits between the `## Round 4` heading and `### What Round 4 ships`, and
      contains every one of: the served URL prefix, `2025-10`, all five prior bundle filenames, all
      five Monday-start distances/times, `357.349`, `max-width: 640px`, `minmax(0, max-content)`,
      `Theme: dark`, `D-15`, a hard-reload paragraph, and the non-waivable clause. It contains neither
      `Required detail` nor `Observer required`.
    - `localhost:8099` appears nowhere in the file.
    - Frontmatter carries `round: 4`, `round4_planned`, a new `round4_staged`,
      `nyquist_compliant: false`, `status: partial`, and retains `round1_answered`,
      `round2_answered` and `round3_answered`.
    - `.planning/REQUIREMENTS.md` shows CAL-02 as `- [ ]` with a recorded `REVERTED` reason and a
      phase-status row that no longer reads `Complete`; CAL-01 and CAL-03 are unchanged.
  </acceptance_criteria>
  <done>A provably fresh Round 4 production-shaped build is served from 127.0.0.1 under /strava-widgets against organic archive data, the five staged rows carry a freshness preamble quoting all four prior FAIL/thin observations, and CAL-02's overstated tick is corrected before any row is judged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] The five-row Round 4 checkpoint — the phone-width band no round has entered, and the control R22 never clicked</name>
  <what-built>
    Plan `22-15` closed `22-VERIFICATION.md`'s reopened Gap 1. Round 3's overflow fix was real but was
    gated at `@media (max-width: 380px)`; above that breakpoint the original defect-causing rules were
    still unconditional and governed every width up to at least 1000px — a band containing 390px
    (iPhone 12-15), 393px (iPhone 15/16 Pro) and 412px (Pixel), which no checkpoint row in any of the
    three prior rounds ever observed. R19, the row that closed Gap 1, observed exactly 375px. The
    entire compaction block's breakpoint is now raised to `max-width: 640px`, with every declaration
    inside it byte-identical. A new test parses the breakpoint out of the block's own prelude and fails
    if it is ever narrowed below 530px.

    Plans `22-13` and `22-14` closed Gap 2, a Critical that Round 3's own fix made reachable. The
    header theme toggle held no in-memory state — it re-derived the mode from storage on every click,
    so under blocked site data `readStoredMode(null)` returned `'auto'` every time and
    `cycleThemeMode('auto')` returned `'light'` every time. The toggle was stranded on light and dark
    was unreachable. Before Round 3 the same configuration crashed the page blank so the toggle could
    never be clicked; Round 3 made the page render and the broken control clickable, and R22 checked
    only that things rendered. The mode now lives in an in-session variable owned by a new
    `nav-theme.ts` controller — the same `let weekStart` shape `calendar.ts` already uses, which is
    exactly why the week-start toggle never had this bug — and `watchSystemTheme`'s auto-only guard
    consults that in-memory mode instead of re-reading storage. Separately, an explicit
    `storage: null` is now honoured rather than silently upgraded to the real `localStorage`, and the
    three Round 3 tests that relied on that accident were replaced with sentinel-backed cases that fail
    if the override is ignored.

    Task 1 re-ran the gate and served that build under `/strava-widgets` on 127.0.0.1 with **organic
    archive data — no fixture of any kind was staged** — and proved the served bundle is none of the
    three previously observed builds, and that the served CSS positively carries a
    `@media (max-width: 640px)` calendar block with no `.calendar-` rules left at 380px.

    What no automated gate in this repository can see: what actually renders at a real phone width
    above 380px, what the widened compaction looks like at a small-tablet width, and whether a real
    browser with site data blocked now lets the theme toggle reach dark. `vitest.config.ts` is
    `environment: 'node'`, there is no jsdom and there is no headless browser in this project.
  </what-built>
  <how-to-verify>
    Open `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10`. **Confirm the URL bar reads
    `127.0.0.1`, not `localhost`, and includes `/strava-widgets/`, before judging any row.**

    **Then hard-reload before judging anything** (Cmd+Shift+R, Cmd+Option+R in Safari, or DevTools
    open with "Disable cache" ticked, then reload). R24 exists to record that this happened AND that
    the bundle filename on the page is this round's — none of `index-YqJHQsHW.js`,
    `index-Dlom2BM3.js`, `index-Bsnjp2E6.js`. R24 must be PASS before any other row may be judged; if
    it is not, record every remaining row BLOCKED naming R24 and stop.

    Present the five rows from `22-VALIDATION.md`'s Round 4 table **one at a time**, in R24..R28
    order, following house rules 1-13 in the Round 3 section and house rule 14 in the Round 4 section.

    **The non-negotiable rules for this session (house rules 10, 11, 12 and 14):**

    - **R25, R26 and R27 cannot be waived.** Do not offer to observe any of them on the developer's
      behalf. Do not resize a window, open device emulation, evaluate a `matchMedia` expression,
      change a browser privacy setting, take a screenshot, or reason from source in order to fill any
      of those three Observation cells. If the developer offers a waiver or asks you to do the row,
      **decline**, record their words verbatim, and record that row **BLOCKED**. Three rounds of this
      phase have been scored down for exactly this: R19 PASSED as a non-waivable row and Gap 1 reopened
      because it only ever saw one width; R22 PASSED as a non-waivable row and a Critical shipped
      because it only ever saw a page render.
    - **R27 may not be declined.** It is the only row that exercises the control CR-01 broke, under the
      only configuration that breaks it. Say plainly, before asking it, that the browser setting is
      reverted at R28, and that declining leaves Gap 2 open for a second round. If it is still
      declined, record BLOCKED and say so in the outcome — do not substitute `nav-theme.test.ts`'s unit
      evidence for it.

    **R25 is the row this gap-closure round exists for.** Do not accept "looks better". The row asks
    for a STATED width of 390, 393 or 412px reached by device emulation (a native macOS window resize
    cannot get there and is what defeated R13 and R19's first attempt), and for BOTH
    `matchMedia('(max-width: 640px)').matches === true` AND
    `matchMedia('(max-width: 380px)').matches === false` — the second is what proves this is new
    evidence and not a repeat of R19's 375px reading. Then the quoted rendered text of at least three
    distance-carrying day cells and one total cell, an explicit yes/no on overflow, a
    clipped-versus-overflowing statement, and the both-themes comparison. Tell them explicitly that a
    value WRAPPING onto extra lines inside its cell is intended behaviour and is NOT a failure — only
    overflowing the cell, being clipped, or being truncated is. If they offer a documented-PASS
    framing, do not encourage it: R11 and R13 were both recorded FAIL precisely because the developer
    chose accuracy over convenience.

    **R26 is the new risk this round's own fix creates.** The compaction that used to apply only below
    380px now applies at every width up to 640px, so a ~600px viewport renders with the single-column
    day stack and the 14px/12px type for the first time in this phase's history. Ask for a stated
    width, the `matchMedia('(max-width: 640px)')` result, values read back, and an explicit
    legible/not-legible judgement.

    **R27 is the row R22 should have been.** Read the disposition rules aloud before they start:
    (a) the `aria-label` CHANGES on each of the three clicks and `Theme: dark` is reached at least
    once, with the colours visibly changing → PASS; (b) it reads `Theme: light` after every click and
    dark is never reached → FAIL, meaning the fix is not in the observed build; (c) blank page or
    `Something went wrong` → FAIL and a BL-03 regression. Tell them where the button is — the sun/moon
    button at the right-hand end of the nav bar — and how to read the label (devtools Elements panel,
    or hover). **Make sure the setting is restored at R28** (house rule 9).

    Do not let the developer chase the rounding artifact (Monday-start rows display-sum to 357.2 km
    against the header's 357.3 km, expected per the preamble) or the accepted mid-number wrapping item
    recorded in `deferred-items.md`.
  </how-to-verify>
  <action>
    Present the five Round 4 rows from
    `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` to the developer ONE AT A TIME,
    in R24..R28 order, quoting each row's Instructions cell including its `Required detail:` and
    `Observer required:` clauses in full.

    Before R24, confirm with the developer that the URL bar reads
    `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10`, ask them to perform a hard reload
    and name the method, and ask them to read BOTH the `assets/index-*.js` and `assets/index-*.css`
    filenames off the page (devtools Network or Sources panel) and compare them against the preamble.
    A JS filename equal to `index-YqJHQsHW.js`, `index-Dlom2BM3.js` or `index-Bsnjp2E6.js`, or a CSS
    filename equal to `index-aaEmW9us.css` or `index-C-Jvo-sR.css`, means a previous round's build is
    being observed and R24 is FAIL.

    For R25, ask for the emulation method and the stated width (390, 393 or 412) first, then BOTH
    `matchMedia` results, and do not proceed to judge until `(max-width: 640px)` reads `true` and
    `(max-width: 380px)` reads `false`. If the developer cannot reach a width in that band, record the
    attempt per house rule 4 and ask them to switch to Chrome DevTools device emulation (Cmd+Shift+M)
    with a named device preset. Then ask for the quoted rendered text of at least three
    distance-carrying day cells and one week-total cell, the explicit overflow yes/no, the
    clipped-versus-overflowing distinction, and the other-theme comparison. **Do not evaluate the
    `matchMedia` expressions yourself, do not read the DOM as a substitute, and do not accept a waiver**
    (house rules 10, 11, 14) — if one is offered, decline, record their words, and record BLOCKED.

    For R26, ask for the stated ~600px width and method, the `matchMedia('(max-width: 640px)')`
    result, at least two day cells and one total cell quoted, and an explicit legible-or-broken
    judgement plus an overflow yes/no. Same waiver rules as R25.

    For R27, give them the exact settings path for their browser (Safari: Settings → Privacy → Block
    all cookies — the same one R22 used; Firefox: Settings → Privacy & Security → Cookies and Site
    Data → Custom → Cookies → All cookies) and state that the next step is a FULL RELOAD, then THREE
    clicks on the theme toggle with the `aria-label` read after each one. Ask for the three label
    values in order, whether the colours visibly changed on each click, and which of (a)/(b)/(c) it
    matches. Apply the disposition rules literally. **Do not accept a waiver and do not accept a
    decline** — if it is declined anyway, record BLOCKED with their words verbatim, and do not patch
    anything (house rule 5).

    For R28, confirm the R27 setting was restored, then walk the three sub-checks in order: (i) the
    Sunday-selection-survives-a-reload check including the first weekday heading — this is the half
    R23 left thin and it must be answered with detail this time, not with a bare "confirm"; (ii) the
    five Monday-start week-total triples at normal desktop width, read back and compared against the
    preamble table; (iii) the theme-toggle-survives-a-reload check.

    Capture each answer in the developer's own words and hold them for Task 3, which writes them into
    the Observation cells. Do not summarise, do not merge answers, do not substitute an agent
    observation for any row, and do not edit any source file if a row fails.
  </action>
  <read_first>
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Round 4 — the preamble Task 1
      wrote and the five staged rows, including all four prior observations quoted verbatim.
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Round 4
      `### House rules (Round 4)` — rule 14 — and § Round 3 `<house_rules>` — rules 1 through 13,
      which bind this round unchanged.
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Round 1 row R11, § Round 2
      row R13, § Round 3 rows R19, R22 and R23 — the exact judgements this round re-asks or extends,
      in the developer's own words.
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Checkpoint Outcome (Round 3)
      — the three recorded thin/waived rows, which house rule 14 exists to stop repeating.
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` — all three gaps as worded
      and the two `human_verification` frontmatter items, so the questions asked match the gaps.
    - `.planning/phases/22-calendar-week-start-totals/deferred-items.md` — the accepted mid-number
      wrapping item, so a developer report of a distance breaking after its first digit is recorded as
      known-and-accepted rather than treated as a new FAIL.
    - `.planning/PROJECT.md` line 49 — the house rule this whole task exists to honour.
  </read_first>
  <verify>
    <human-check>All five rows have an answer carrying that row's own required detail; the URL bar was confirmed to read 127.0.0.1 and /strava-widgets/ and BOTH asset filenames were confirmed to be none of the three prior rounds' before R24 was judged; no row after R24 was judged unless R24 was PASS; R25, R26 and R27 were answered by the developer's own eyes with no waiver granted or exercised, or are recorded BLOCKED; R25's answer states a width strictly above 380px and BOTH matchMedia results; R26's answer states a width near 600px and its matchMedia result; R27 was actually run against a real browser-level blocked-site-data setting with three clicks and three aria-label readings, or is recorded BLOCKED; the setting was restored and the restoration confirmed at R28; R28(i) was answered with the filled option and the first weekday heading, not a bare confirm; no source file was edited during the session.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer confirmed the URL bar reads
      `http://127.0.0.1:8099/strava-widgets/#/calendar?month=2025-10` and named the hard-reload method
      before R24 was judged.
    - R24's answer quotes BOTH the `assets/index-*.js` and `assets/index-*.css` filenames as read from
      the page, and neither matches any of the five previously observed artifacts.
    - R25's answer quotes the emulation method, a viewport width of 390, 393 or 412px, BOTH
      `matchMedia` results (`(max-width: 640px)` true AND `(max-width: 380px)` false), the rendered
      text of at least three day cells and one week-total cell, an explicit yes/no on overflow, a
      clipped-versus-overflowing statement, and the both-themes comparison — **observed by the
      developer**. If an agent observed it or a waiver was exercised, the row reads BLOCKED and quotes
      the request verbatim.
    - R26's answer quotes a stated width near 600px, the `matchMedia('(max-width: 640px)')` result, at
      least two day cells and one total cell, an explicit legible/not-legible judgement and an
      overflow yes/no — **observed by the developer**, or BLOCKED.
    - R27's answer names the browser and the exact setting, gives THREE `aria-label` values in the
      order read, states whether the page colours visibly changed on each click, and states which of
      (a)/(b)/(c) it matches — **observed by the developer**. If it was declined or waived, the row
      reads BLOCKED with the developer's words verbatim, and no `NOT EXERCISABLE` verdict is used.
    - R28's answer confirms the R27 browser setting was restored, states which option was filled and
      the first weekday heading after the Sunday reload, quotes five distance / time / run-count
      triples as rendered, and states the theme mode before and after its reload.
    - Every row after R24 was judged only because R24 was PASS; otherwise those rows read BLOCKED
      naming R24.
    - `git status --porcelain src scripts` is empty at the end of the session — no source file was
      edited, no row was "fixed" mid-session (house rule 5).
  </acceptance_criteria>
  <done>Five developer-observed answers exist, with the three decisive rows answered by the developer's own eyes and no waiver exercised on any of them — or recorded BLOCKED rather than fabricated.</done>
</task>

<task type="auto">
  <name>Task 3: Record the five verdicts, write the Checkpoint Outcome and Gap Closure Record, and set the requirement ticks strictly on the Round 4 row map</name>
  <files>.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - The developer's answers captured verbatim during Task 2 — every Observation cell is filled from
      those words, not from what the implementation ought to produce (house rule 4).
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` § Round 4 in full, and
      § Checkpoint Outcome (Round 3) / § Gap Closure Record (Round 3) as the format precedent for the
      two sections this task appends.
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` — the three items to
      disposition: Gap 1 (CAL-02/SC3 coverage band), Gap 2 (CR-01 stuck theme toggle) and the
      Warning-severity WR-01 (vacuous null-override tests), plus truth #8.
    - `.planning/REQUIREMENTS.md` — CAL-01/CAL-02/CAL-03's checkbox paragraphs and the phase status
      table, and the Phase 19 UI-01/UI-02 precedent for reverting a ticked requirement when a
      gap-closure row fails.
  </read_first>
  <action>
    Fill each of the five Observation cells with the developer's own words, quoted, plus the executor's
    literal application of that row's stated disposition rules. Do not summarise, do not merge two
    rows' answers, and do not write a failure's values into a PASS cell. Where a row took multiple
    attempts, record every attempt in order (house rule 4) as Round 3's R19 and R22 cells do. Where a
    waiver was requested on R25, R26 or R27, record the request verbatim and record that row BLOCKED.

    Write each row's Verdict cell as one of `R22-VERDICT: PASS`, `R22-VERDICT: FAIL` or
    `R22-VERDICT: BLOCKED` — the same token the file's other 23 rows use, so the count reaches exactly
    28. R27 has no `NOT EXERCISABLE` disposition.

    Then append `## Checkpoint Outcome (Round 4)` stating the tally across R24-R28, every stated
    deviation with the developer's words verbatim, and — separately and explicitly — whether R25, R26
    and R27 were each fully observed by the developer with no waiver requested or granted. If any of
    the three was waived, declined or agent-observed, say so plainly and record it BLOCKED; do not
    soften it.

    Then append `## Gap Closure Record (Round 4)` dispositioning all three `22-VERIFICATION.md` items
    by name:
    - **Gap 1 (CAL-02/SC3, the 381-640px coverage band, reopened at Round 3).** CLOSED only if R25 and
      R26 are both PASS. State the width the developer actually observed and both `matchMedia` results,
      so a later verifier can see the band was entered rather than inferred. If either row FAILED,
      record Gap 1 STILL OPEN and name the DISC-6b `.splits-scroll` wrapper as the remaining documented
      fallback — do not propose, plan or implement it here.
    - **Gap 2 (CR-01, the theme toggle stuck on light under blocked storage).** CLOSED only if R27 is
      PASS with disposition (a). Quote the three `aria-label` values the developer read back. If R27 is
      FAIL or BLOCKED, record Gap 2 STILL OPEN and state plainly that the control's behaviour under
      blocked storage remains unobserved.
    - **WR-01 / truth #8 (the vacuous null-override tests).** Disposition on the automated evidence
      alone — it is a test-quality gap, not a rendering claim — citing plan `22-13`'s sentinel-backed
      cases and its mutation gate (reverting the resolver to the truthiness form turns the suite red).
      Say explicitly that this item required no checkpoint row and was not closed by one.

    Then append `## Known and Accepted (Round 4)` carrying forward, unchanged: the 357.349 km rounding
    artifact; the D-15 scope fence; the DISC-6b `.splits-scroll` fallback as documented and deliberately
    unimplemented; `deferred-items.md`'s mid-number wrapping polish item; `22-REVIEW.md`'s WR-02,
    WR-03, WR-04, WR-05 and WR-06 and its six Info findings as open code-review items the user may
    disposition, none of which any Round 4 row opened.

    Update the frontmatter: set `round4_answered` to today's date. Set `status: complete` and
    `nyquist_compliant: true` ONLY if all five rows are PASS; otherwise leave both as Task 1 left them
    (`partial` / `false`) and say why in the Checkpoint Outcome. Do not alter any earlier round's
    dates. Update the Per-Task Verification Map's Status column for the Round 4 rows added at Task 1.

    Then set `.planning/REQUIREMENTS.md` strictly on the Round 4 row map and nothing else:
    - **CAL-02** re-ticks to `[x]` / `Complete (Round 4, plan 22-16, <date>)` ONLY if R24, R25, R26 and
      R28's five-triple reading are ALL PASS. Its paragraph must state the width the developer
      observed and both `matchMedia` results — the specific evidence whose absence made the Round 3
      tick overstated. Otherwise it stays `[ ]` / Pending with the failing row named.
    - **CAL-01**'s existing tick survives only if R27 and R28 are both PASS. If either FAILED or is
      BLOCKED, revert it to `[ ]` / Pending with the reason recorded, following the Phase 19
      UI-01/UI-02 precedent.
    - **CAL-03** is not re-gated this round; leave its entry exactly as it is.
    Do not pre-tick anything and do not tick a requirement on a row that was BLOCKED.
  </action>
  <verify>
    <automated>node -e '
const fs=require("fs");
const p=".planning/phases/22-calendar-week-start-totals/22-VALIDATION.md";
const s=fs.readFileSync(p,"utf8");
const t=s.slice(s.indexOf("\n## Round 4\n"));
const ids=["R24","R25","R26","R27","R28"];
const rows=t.split("\n").filter(l=>ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(l)));
if(rows.length!==5) throw new Error("expected 5 Round 4 rows, found "+rows.length);
const total=(s.match(/R22-VERDICT/g)||[]).length;
if(total!==28) throw new Error("expected exactly 28 R22-VERDICT tokens (11+6+6+5), found "+total);
const verdicts={};
for(const r of rows){ const id=r.match(/^\|\s*(R2[4-8])\./)[1]; const m=r.match(/R22-VERDICT:\s*(PASS|FAIL|BLOCKED|NOT EXERCISABLE)/); if(!m) throw new Error(id+" has no verdict token"); verdicts[id]=m[1]; const cells=r.split("|"); if(cells[4].trim().length<120) throw new Error(id+"s Observation cell is too thin to be an answer ("+cells[4].trim().length+" chars) - house rule 3 requires values read back, not a confirmation"); }
if(verdicts.R27==="NOT EXERCISABLE") throw new Error("R27 has no NOT EXERCISABLE disposition");
if(verdicts.R24!=="PASS"&&Object.values(verdicts).filter(v=>v==="BLOCKED").length<4) throw new Error("R24 is not PASS but the remaining rows are not all BLOCKED (house rule 6)");
const r25=rows.find(x=>/^\|\s*R25\./.test(x));
if(verdicts.R25==="PASS"&&!/(390|393|412)/.test(r25)) throw new Error("R25 PASSed without a stated width in the 381-530px band");
if(verdicts.R25==="PASS"&&!/false/i.test(r25)) throw new Error("R25 PASSed without the (max-width: 380px) === false confirmation that makes it new evidence");
const r27=rows.find(x=>/^\|\s*R27\./.test(x));
if(verdicts.R27==="PASS"&&!/Theme: dark/.test(r27)) throw new Error("R27 PASSed without a Theme: dark aria-label reading - disposition (a) requires dark to be reached");
for(const h of ["## Checkpoint Outcome (Round 4)","## Gap Closure Record (Round 4)","## Known and Accepted (Round 4)"]) if(!t.includes(h)) throw new Error("missing section: "+h);
const gcr=t.slice(t.indexOf("## Gap Closure Record (Round 4)"));
for(const n of ["Gap 1","Gap 2","WR-01"]) if(!gcr.includes(n)) throw new Error("the Gap Closure Record does not disposition "+n);
const fm=s.split("---")[1]||"";
if(!/round4_answered/.test(fm)) throw new Error("round4_answered date missing");
for(const k of ["round1_answered","round2_answered","round3_answered"]) if(!fm.includes(k)) throw new Error(k+" was dropped");
const allPass=ids.every(i=>verdicts[i]==="PASS");
if(allPass&&!/status: complete/.test(fm)) throw new Error("all five rows PASS but status is not complete");
if(!allPass&&/status: complete/.test(fm)) throw new Error("status: complete with a non-PASS row present");
if(!allPass&&/nyquist_compliant: true/.test(fm)) throw new Error("nyquist_compliant: true with a non-PASS row present");
const req=fs.readFileSync(".planning/REQUIREMENTS.md","utf8");
const cal02Ticked=/- \[x\] \*\*CAL-02\*\*/.test(req);
const cal02Should=verdicts.R24==="PASS"&&verdicts.R25==="PASS"&&verdicts.R26==="PASS"&&verdicts.R28==="PASS";
if(cal02Ticked!==cal02Should) throw new Error("CAL-02s tick does not match the Round 4 row map (R24/R25/R26/R28 all PASS = "+cal02Should+")");
const cal01Ticked=/- \[x\] \*\*CAL-01\*\*/.test(req);
const cal01Should=verdicts.R27==="PASS"&&verdicts.R28==="PASS";
if(cal01Ticked!==cal01Should) throw new Error("CAL-01s tick does not match the Round 4 row map (R27/R28 both PASS = "+cal01Should+")");
if(!/- \[x\] \*\*CAL-03\*\*/.test(req)) throw new Error("CAL-03 was altered - it is not re-gated this round");
if(cal02Ticked&&!/(390|393|412)/.test(req.split("**CAL-02**")[1].split("\n")[0])) throw new Error("CAL-02 re-ticked without recording the observed width - the exact omission that made the Round 3 tick overstated");
console.log("Round 4 recorded: "+ids.map(i=>i+"="+verdicts[i]).join(" ")+"; 28 verdict tokens; requirement ticks match the row map");
' && test -z "$(git status --porcelain src scripts data dist)" && echo "no source file was edited during the checkpoint"  </automated>
  </verify>
  <acceptance_criteria>
    - Every one of the five Observation cells is filled with the developer's own quoted words plus the
      executor's literal application of that row's disposition rules; none is shorter than 120
      characters, and none contains a value the developer did not actually report.
    - Every one of the five Verdict cells carries exactly one `R22-VERDICT: PASS|FAIL|BLOCKED` token,
      and `22-VALIDATION.md` now holds exactly **28** `R22-VERDICT` tokens (11 + 6 + 6 + 5). R27
      carries no `NOT EXERCISABLE` verdict.
    - If R24 is not PASS, every remaining row reads BLOCKED naming R24 (house rule 6).
    - If R25 is PASS, its cell states a width of 390, 393 or 412px and records
      `matchMedia('(max-width: 380px)').matches` reading `false` — the confirmation that makes it new
      evidence rather than a restatement of R19's 375px reading.
    - If R27 is PASS, its cell quotes a `Theme: dark` aria-label reading; disposition (a) is not
      satisfiable without dark being reached.
    - `## Checkpoint Outcome (Round 4)`, `## Gap Closure Record (Round 4)` and
      `## Known and Accepted (Round 4)` all exist, and the Gap Closure Record dispositions `Gap 1`,
      `Gap 2` and `WR-01` by name.
    - The Checkpoint Outcome states, separately and explicitly, whether R25, R26 and R27 were each
      fully observed by the developer with no waiver requested or granted.
    - Frontmatter carries `round4_answered` and retains `round1_answered`, `round2_answered` and
      `round3_answered`. `status: complete` and `nyquist_compliant: true` appear if and only if all
      five rows are PASS.
    - `.planning/REQUIREMENTS.md` matches the Round 4 row map exactly: CAL-02 is ticked iff R24, R25,
      R26 and R28 are all PASS; CAL-01 is ticked iff R27 and R28 are both PASS; CAL-03 is unchanged.
      A re-ticked CAL-02 paragraph names the observed width (390/393/412).
    - `git status --porcelain src scripts data dist` is empty — no source file, fixture or build
      artifact was edited by the checkpoint (house rule 5).
  </acceptance_criteria>
  <done>Round 4's five verdicts, outcome, gap dispositions and requirement ticks are recorded from the developer's own words, with every tick traceable to a named PASS row and every open item named as open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser cache / served artifact → the observation | a stale bundle makes every verdict describe code that is not the code under review |
| developer report → recorded evidence | the checkpoint's entire value is that this crossing is not forged, softened or substituted |
| browser-wide privacy setting → the machine's later state | R27 changes a setting outside this repository's control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-R4-09 | Spoofing | a stale cached bundle observed as if it were this round's build — three builds have already been observed in this phase | mitigate | Task 1 asserts the served JS filename differs from all three prior rounds' and the CSS from both prior stylesheets, and asserts POSITIVELY that a served `max-width: 640px` block carries `.calendar-week-total` + `minmax(0, max-content)` while no served `380px` block mentions `.calendar-`; R24 makes the developer read both filenames off the page after a hard reload |
| T-22-R4-10 | Repudiation | a thin, waived or agent-substituted answer recorded as decisive evidence — the failure mode that let Gap 1 reopen and a Critical ship | mitigate | house rule 14 makes R25, R26 and R27 non-waivable and R27 undeclinable; Task 3's verify rejects an Observation cell under 120 characters, rejects an R25 PASS without a stated 390/393/412px width and a `false` 380px `matchMedia` reading, and rejects an R27 PASS without a `Theme: dark` reading |
| T-22-R4-11 | Denial of Service | the browser-wide "Block all cookies" setting left enabled after the session, degrading every site the developer visits | mitigate | house rule 9; R28 begins by restoring it and recording the confirmation, and the row cannot PASS without that confirmation |
| T-22-R4-18 | Tampering | an earlier round's verdicts being rewritten to make this round look cleaner | mitigate | `22-VALIDATION.md` is append-only; Task 1's verify asserts the pre-checkpoint token count is exactly 23 and Task 3's asserts exactly 28, so no earlier verdict can be edited or deleted without failing |
| T-22-R4-19 | Elevation of Privilege | a requirement ticked on a BLOCKED or FAILED row, letting the phase gate close on unearned evidence | mitigate | Task 3's verify computes the expected tick state from the parsed verdict tokens and fails if `REQUIREMENTS.md` disagrees, in either direction |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs nothing and edits no source — `files_modified` is two planning documents, and both Task 1's and Task 3's verify commands assert `git status --porcelain src scripts data dist` is empty. `22-RESEARCH.md`'s Package Legitimacy Gate is not triggered; no `[ASSUMED]`/`[SUS]` package exists in this plan |

No `high`-severity threat is left undispositioned.
</threat_model>

<verification>
- Task 1's gate is clean and the served build is proven to be this round's, by both a negative
  filename discrimination against all five prior artifacts and a positive served-CSS assertion.
- `22-VALIDATION.md` holds exactly 23 `R22-VERDICT` tokens before the checkpoint and exactly 28 after.
- All five Observation cells carry the developer's own words; R25, R26 and R27 are either
  developer-observed with no waiver or recorded BLOCKED.
- `.planning/REQUIREMENTS.md`'s three requirement states are each derivable from a named Round 4
  verdict, and Task 3's verify fails if any of them is not.
- `git status --porcelain src scripts data dist` is empty at both Task 1 and Task 3.
</verification>

<success_criteria>
- The 381-640px band is observed by the developer at a stated real phone width, with both
  `matchMedia` results recorded, for the first time in this phase.
- The theme toggle is clicked three times under real blocked site data and its `aria-label` readings
  are recorded, for the first time in this phase.
- R28(i) supplies the week-start persistence detail R23 left as a stale bare "confirm".
- All three `22-VERIFICATION.md` items are dispositioned by name, open ones plainly recorded as open.
- No requirement is ticked on a row that did not PASS.
</success_criteria>

<output>
Create `.planning/phases/22-calendar-week-start-totals/22-16-SUMMARY.md` when done.
</output>
