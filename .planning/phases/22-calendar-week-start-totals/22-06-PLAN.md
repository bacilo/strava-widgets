---
phase: 22-calendar-week-start-totals
plan: 06
type: execute
wave: 6
depends_on: ["22-05"]
gap_closure: true
files_modified:
  - src/dashboard/styles.css
  - src/dashboard/styles.test.ts
  - src/dashboard/views/calendar.ts
autonomous: true
requirements: [CAL-02, CAL-03]
must_haves:
  decisions: [D-10, GC-1, GC-2]
  truths:
    - "GC-1: the ~380px day-cell overflow that made Round 1 row R11 FAIL is addressed by DEEPER TYPE COMPACTION, not by the DISC-6b horizontal-scroll wrapper — `.calendar-day` gains `min-width: 0`, `.calendar-day__distance` drops 20px to 14px, and `.calendar-week-total__time`/`__count` drop 14px to 12px, on top of the padding and `.calendar-week-total__distance` compaction the phase already shipped"
    - "GC-1: the `.splits-scroll`-style scroll wrapper remains DOCUMENTED AND UNIMPLEMENTED — the DISC-6b comment is updated, never deleted, and `.calendar-grid` gains no `overflow` declaration at any breakpoint"
    - "D-10: the eight-column contract (`grid-template-columns: repeat(7, 1fr) auto`) is unchanged at every breakpoint — the fix compacts type inside eight columns rather than changing the track list at 380px"
    - "GC-2/IN-05: the `Total` header carries a `.calendar-weekday--total { text-align: right }` modifier applied ALONGSIDE `.calendar-weekday`, so the label sits over the `justify-items: end` values it names instead of centred above them"
    - "GC-2/IN-06: the calendar 380px block's comment no longer claims it joined `the existing breakpoint` — it records that this file carries three disjoint `@media (max-width: 380px)` blocks and why consolidation was declined"
    - "WR-02 caveat honoured: every NEW assertion about a value the 380px block overrides asserts the override exists (`expect(() => assertNoAtRuleOverride(...)).toThrow(/redeclares/)`) rather than asserting the base value in isolation. The pre-existing false-green Phase 22 describe is NOT touched — WR-02/WR-03 stay open this round"
    - "WR-03 ordering is preserved: the unguarded padding rule stays FIRST inside the 380px block, because `styles.test.ts`'s `RULE_SCANNER` swallows an at-rule block's first nested rule into the prelude pseudo-body, making it invisible to both halves of the guard machinery"
    - "WR-04 counters are undisturbed: `calendar.ts` still contains exactly two `tabindex` occurrences and exactly two `.focus()` occurrences, so `calendar.test.ts:280-284` and `:311-314` stay green (WR-04 itself is out of scope this round)"
  artifacts:
    - path: "src/dashboard/styles.css"
      provides: "the deepened 380px compaction block, the `.calendar-weekday--total` modifier, and the corrected DISC-6b/IN-06 comment"
      contains: ".calendar-weekday--total"
    - path: "src/dashboard/styles.test.ts"
      provides: "a NEW gap-closure describe whose every override claim is override-aware, appended after the untouched Phase 22 block"
      contains: "Phase 22 gap closure"
    - path: "src/dashboard/views/calendar.ts"
      provides: "the Total header cell carrying both the base class and the IN-05 modifier"
      contains: "calendar-weekday calendar-weekday--total"
  key_links:
    - from: ".calendar-day__distance (20px base)"
      to: "the 380px @media block"
      via: "a font-size override asserted by an assertNoAtRuleOverride-throws pairing"
      pattern: "font-size: 14px"
    - from: "calendar.ts's Total header cell"
      to: ".calendar-weekday--total"
      via: "className carrying base + modifier"
      pattern: "calendar-weekday calendar-weekday--total"
---

<objective>
Close **Gap 1** of `22-VERIFICATION.md` — the ~380px day-cell overflow the developer recorded as
FAIL on Round 1 row R11 — by extending the existing `@media (max-width: 380px)` compaction, per
locked decision **GC-1**. Fold in the two cheap same-file code-review fixes GC-2 puts in scope:
**IN-05** (the `Total` header centred over a right-aligned column) and **IN-06** (a third disjoint
380px block whose comment claims it joined an existing one).

Purpose: `REQUIREMENTS.md` keeps **CAL-02** `Pending` solely because of R11. This plan produces the
CSS the Round 2 checkpoint (plan `22-08`) observes. It does **not** tick CAL-02 — no automated CSS
assertion can discharge a visual overflow judgement (PROJECT.md line 49; the R11 FAIL was the
developer's own eyes).

Output: a deeper 380px compaction, a `.calendar-weekday--total` modifier, a corrected DISC-6b/IN-06
comment, and a new, override-aware CSS guard block.

**Explicitly NOT in scope.** The DISC-6b `.splits-scroll`-style horizontal-scroll wrapper is NOT
implemented (GC-1: this grid is full of focusable day buttons and a horizontal-scroll container with
focusable content scroll-jumps on Tab). Its comment is updated, never deleted. **WR-02** and
**WR-03** (the pre-existing false-green Phase 22 CSS assertions and the unguardable
first-nested-rule) are NOT fixed — the existing `describe('styles.css — Phase 22 calendar week
totals')` block is left byte-identical and an automated check in Task 3 enforces that. **WR-04**
(exact-count source guards in `calendar.test.ts`) is not fixed either; this plan only avoids
tripping it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md
@.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md
@.planning/phases/22-calendar-week-start-totals/22-REVIEW.md
@.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md
@.planning/phases/22-calendar-week-start-totals/22-02-PLAN.md
</context>

<interfaces>
<!-- Helpers already defined in src/dashboard/styles.test.ts. Use these; do not add new helpers. -->

Module-scoped in `src/dashboard/styles.test.ts`:

- `const css: string` — the raw stylesheet, comments included (line 14)
- `const cssNoComments: string` — comments stripped (line 31)
- `bodyForSelectorListToken(needle: string, source?: string): string` (line 309) — last TOP-LEVEL
  body whose selector list contains `needle` as an exact token. **Skips every at-rule-scoped rule
  by construction**, so it cannot see a `@media` override.
- `bodiesForSelectorListToken(needle, source?): string[]` (line 364) — same, all bodies.
- `cascadeWinningBodyDeclaring(needle, property, source?): string` (line 428) — last top-level body
  declaring `property`. Same at-rule blind spot.
- `assertNoAtRuleOverride(needle, property, source?): void` (line 470) — the companion for that
  blind spot. **Throws** with a message matching `/redeclares "<property>"/` when an at-rule-scoped
  rule redeclares `property` for `needle`.
- `RULE_SCANNER(): RegExp` (line 118) — `/([^{}]+)\{([^}]*)\}/g`. Its body class `[^}]*` permits an
  unmatched `{`, which is exactly why an at-rule block's FIRST nested rule is swallowed into the
  prelude's pseudo-body and is invisible to every helper above (WR-03).

Current calendar CSS state (`src/dashboard/styles.css`):

- `.calendar-grid` (741-745): `grid-template-columns: repeat(7, 1fr) auto`
- `.calendar-weekday` (747-753): `font-size: 14px`, `color: var(--text-secondary)`, `text-align: center`
- `.calendar-day` (755-769): `min-width: 32px`, `min-height: 32px`, `padding: var(--space-sm)`
- `.calendar-day__distance` (790-796): `font-size: 20px`, `font-weight: 600`, `line-height: 1.2`
- `.calendar-week-total` (816-824): `justify-items: end`, `white-space: nowrap`
- `.calendar-week-total__distance` (826-830): `font-size: 20px`
- `.calendar-week-total__time` (832-837) and `.calendar-week-total__count` (839-844): `font-size: 14px`
- the DISC-6b comment (846-851) and the `@media (max-width: 380px)` block (852-863), which today
  declares only `.calendar-day, .calendar-week-total { padding: var(--space-xs) }` and
  `.calendar-week-total__distance { font-size: 14px; font-weight: 600; line-height: 1.5 }`
- the file's other two `@media (max-width: 380px)` blocks: `.chart-band__canvas-wrap` (945) and
  `.pr-evolution-card__canvas-wrap` (1294)
</interfaces>

<settled_decisions>
The planner settled these; an executor implements them and does not reopen them.

| # | Choice | Decision | Rationale |
|---|--------|----------|-----------|
| GC-1a | Fix mechanism for the R11 overflow | Deeper type compaction inside the existing 380px block | Locked user decision GC-1. The scroll wrapper scroll-jumps on Tab in a grid of focusable day buttons — the cost `22-02-PLAN.md`'s DISC-6b entry already weighed when it ranked the wrapper second |
| GC-1b | `.calendar-day`'s narrow-viewport floor | `min-width: 0` at 380px only; the 32px base floor is untouched | IN-05's diagnosis: `repeat(7, 1fr) auto` with a 32px floor on seven columns plus a `nowrap` `auto` track can exceed a 380px viewport. `1fr` alone cannot shrink below the content/`min-width` floor |
| GC-1c | Which type shrinks | `.calendar-day__distance` 20px→14px; `.calendar-week-total__time`/`__count` 14px→12px; `.calendar-week-total__distance` stays at the 14px the phase already shipped | GC-1's approved target shape, reconciled with what the block already declares. The two widest contributors (a 20px day distance, a `nowrap` three-line total) are the ones the current block leaves untouched |
| GC-1d | The DISC-6b fallback | Left unimplemented; its comment updated to record that compaction was extended and the wrapper remains available but unbuilt | GC-1 forbids deleting the DISC-6b comment |
| IN-06a | Consolidate the three 380px blocks, or reword? | **Reword.** Consolidation is declined | GC-2 permits consolidation only if it does not disturb source-order cascade. Merging would relocate the `.chart-band__canvas-wrap` and `.pr-evolution-card__canvas-wrap` rules out of their component sections — two selectors outside this phase's scope — and change their source-order cascade position, exactly the WR-03 hazard GC-2 names |
| GC-1e | Rule ORDER inside the 380px block | The `.calendar-day, .calendar-week-total { padding }` rule stays in position 1; every new rule goes after it | WR-03: position 1 is swallowed by `RULE_SCANNER` and is unguardable. Keeping the one rule nobody asserts in that slot lets every new declaration be guarded. This is self-enforcing — if the order breaks, Task 3's `toThrow` pairings stop throwing and go red |
| IN-05a | Modifier name and placement | `.calendar-weekday--total { text-align: right }`, declared immediately after `.calendar-weekday`, applied ALONGSIDE the base class | 22-REVIEW.md IN-05's own suggested fix. `justify-items: end` on `.calendar-week-total` means `right`, not `center` |
</settled_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Deepen the 380px compaction and correct its DISC-6b / IN-06 comment</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/styles.css` lines 707-880 — the entire Calendar block as it stands: the base
      `.calendar-day` (`min-width: 32px`), `.calendar-day__distance` (20px), `.calendar-week-total`
      (`justify-items: end`, `white-space: nowrap`), `__distance`/`__time`/`__count`, the DISC-6b
      comment at 846-851 and the 380px block at 852-863. Read the CURRENT declarations before
      editing — GC-1's target shape is a target state to reconcile with, not a paste.
    - `src/dashboard/styles.css` lines 940-960 and 1288-1300 — the file's other two
      `@media (max-width: 380px)` blocks (`.chart-band__canvas-wrap`,
      `.pr-evolution-card__canvas-wrap`), which IN-06's reworded comment names and which this task
      does NOT move.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § IN-05 and § IN-06 — the
      finding text verbatim, including IN-05's "the compaction leaves the widest contributors
      untouched" diagnosis and its `min-width: 0` observation.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § WR-03 — why the first nested
      rule of an at-rule block is structurally unguardable, and its "keep the guarded rule out of
      position 1" fix option.
    - `.planning/phases/22-calendar-week-start-totals/22-02-PLAN.md` lines 113-124 — the
      `<settled_discretion>` table, especially DISC-6 and DISC-6b, whose wording this task updates
      rather than replaces.
    - `.planning/phases/22-calendar-week-start-totals/22-VALIDATION.md` line 301 — Round 1 row R11's
      recorded FAIL, in the developer's own words, which is what this compaction answers.
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` (skim the Gap-Closure
      Record only) — this project shipped a CSS comment with a stray `*/` that silently killed a
      custom property and passed every automated gate. Any comment written here is verified by a
      clean `npm run build-widgets` with zero `css-syntax-error`.
  </read_first>
  <action>
    Edit ONLY the calendar `@media (max-width: 380px)` block at `src/dashboard/styles.css:846-863`
    and its preceding comment. Do not move, merge, or reorder the other two 380px blocks.

    **Rule content.** Keep the existing first nested rule exactly as it is —
    `.calendar-day, .calendar-week-total { padding: var(--space-xs); }` must remain the FIRST rule
    inside the block (GC-1e / WR-03). Keep the existing
    `.calendar-week-total__distance { font-size: 14px; font-weight: 600; line-height: 1.5; }` rule.
    Add, after the padding rule:

    - a rule whose selector is exactly `.calendar-day` declaring `min-width: 0;` and nothing else
      (this relaxes the 32px base floor at this breakpoint only — the base rule at line 757 is not
      edited);
    - a rule whose selector is exactly `.calendar-day__distance` declaring `font-size: 14px;`
      (the base 20px stays at line 792);
    - a rule whose selector list is exactly `.calendar-week-total__time, .calendar-week-total__count`
      declaring `font-size: 12px;` (their base 14px stays at lines 833 and 840).

    Declare nothing else. In particular add no `overflow`, `overflow-x`, `grid-template-columns`,
    `display` or `white-space` declaration anywhere in this block — the eight-column contract (D-10)
    and the `nowrap` total are unchanged at 380px, and the scroll-wrapper fallback stays unbuilt
    (GC-1d).

    **Comment content.** Replace the DISC-6b comment above the block with one that keeps its
    original point and adds this round's record. The comment must retain `DISC-6b` as a literal
    token — keep it as the comment's opening heading, exactly as the shipped comment already has
    it. Task 3's automated verify locates this comment by searching for that token, so dropping it
    fails the check even if every other requirement below is met. The comment must state, in prose:

    1. that this is padding + type compaction, NOT a `.splits-scroll`-style horizontal-scroll
       wrapper, and that the wrapper remains the DOCUMENTED FALLBACK and is still UNIMPLEMENTED —
       the literal token `splits-scroll` must survive in the comment;
    2. that Round 1 checkpoint row R11 recorded FAIL (day-cell values slightly overflowed at ~380px)
       and that the chosen answer was to deepen this compaction per gap-closure decision `GC-1`,
       naming `GC-1` literally, because unlike `.splits-scroll` this grid is full of focusable day
       buttons and a horizontal-scroll container with focusable content scroll-jumps on Tab;
    3. `IN-06`, named literally: that this is a THIRD independent `@media (max-width: 380px)` block
       in this file — the others govern `.chart-band__canvas-wrap` and
       `.pr-evolution-card__canvas-wrap` — and that it does not join either; and that consolidation
       was declined because it would relocate rules for two selectors outside this phase's scope and
       change their source-order cascade;
    4. `WR-03`, named literally: that `styles.test.ts`'s `RULE_SCANNER` swallows an at-rule block's
       first nested rule into the prelude's pseudo-body, so the padding rule is kept in position 1
       deliberately and every rule a test guards sits below it.

    Do not write the character sequence `*/` inside the comment body other than as its terminator,
    and do not nest a `/*` inside it.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && node --input-type=module -e 'import {readFileSync} from "node:fs"; const css=readFileSync("src/dashboard/styles.css","utf8"); const s=css.replace(/\/\*[\s\S]*?\*\//g,""); const starts=[...s.matchAll(/@media \(max-width: 380px\)\s*\{/g)]; if(starts.length!==3) throw new Error("expected exactly 3 comment-stripped @media (max-width: 380px) blocks, found "+starts.length); const blocks=starts.map(m=>{let i=m.index+m[0].length,d=1; while(d>0&&i<s.length){ if(s[i]==="{")d++; else if(s[i]==="}")d--; i++; } return s.slice(m.index+m[0].length,i-1);}); const cal=blocks.filter(b=>b.includes(".calendar-")); if(cal.length!==1) throw new Error("expected exactly one calendar 380px block, found "+cal.length); const b=cal[0]; const firstOpen=b.indexOf("{"); const firstBody=b.slice(firstOpen+1,b.indexOf("}")); if(!/padding/.test(firstBody)) throw new Error("WR-03 ordering broken: the first nested rule must remain the padding rule; found head: "+b.slice(0,firstOpen).trim()); const rules=[...b.matchAll(/([^{}]+)\{([^}]*)\}/g)].map(m=>({sels:m[1].split(",").map(x=>x.trim()),body:m[2]})); const decl=(sel,d)=>rules.some(r=>r.sels.includes(sel)&&r.body.includes(d)); for(const pair of [[".calendar-day","min-width: 0"],[".calendar-day__distance","font-size: 14px"],[".calendar-week-total__distance","font-size: 14px"],[".calendar-week-total__time","font-size: 12px"],[".calendar-week-total__count","font-size: 12px"]]) if(!decl(pair[0],pair[1])) throw new Error(pair[0]+" does not declare "+pair[1]+" inside the 380px block"); const bodies=rules.map(r=>r.body).join(";"); for(const banned of ["overflow","grid-template-columns","white-space"]) if(bodies.includes(banned)) throw new Error("the 380px block must not declare "+banned); const commentStart=css.indexOf("DISC-6b"); if(commentStart===-1) throw new Error("the DISC-6b comment was deleted"); const commentEnd=css.indexOf("*/",commentStart); const comment=css.slice(commentStart,commentEnd); for(const t of ["splits-scroll","GC-1","IN-06","WR-03","R11"]) if(!comment.includes(t)) throw new Error("the DISC-6b comment is missing the token: "+t); console.log("380px block: 5 compaction declarations present, padding rule first, 3 disjoint blocks, comment tokens intact");' && rm -f /tmp/bw22gc6.log && npm run build-widgets > /tmp/bw22gc6.log 2>&1; BW=$?; tail -5 /tmp/bw22gc6.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw22gc6.log && echo "build clean, no css-syntax-error"</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/styles.css` contains exactly three `@media (max-width: 380px)` blocks after
      comment stripping, and exactly one of them mentions a `.calendar-` selector.
    - Inside that calendar block: the first nested rule declares `padding`; a rule with the exact
      selector `.calendar-day` declares `min-width: 0`; `.calendar-day__distance` declares
      `font-size: 14px`; `.calendar-week-total__distance` still declares `font-size: 14px`; and the
      selector list `.calendar-week-total__time, .calendar-week-total__count` declares
      `font-size: 12px`.
    - No declaration in that block contains `overflow`, `grid-template-columns` or `white-space`.
    - The base rules are unedited: `.calendar-day` still declares `min-width: 32px`,
      `.calendar-day__distance` still declares `font-size: 20px`, and `.calendar-week-total__time`
      and `__count` still declare `font-size: 14px` at the top level.
    - The comment above the block contains all five literal tokens `splits-scroll`, `GC-1`, `IN-06`,
      `WR-03` and `R11`.
    - `npx vitest run src/dashboard/styles.test.ts` exits 0 (the pre-existing Phase 22 assertions
      still pass — this task adds an override for `.calendar-day__distance`, which no existing
      assertion covers).
    - `npm run build-widgets` exits 0 and its captured output contains zero occurrences of
      `css-syntax-error`.
  </acceptance_criteria>
  <done>The 380px block compacts the two widest contributors and drops the day columns' width floor, the scroll wrapper is still unbuilt and still documented, and the block's comment states what is actually true about the file's three breakpoint blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Right-align the Total header (IN-05) with a modifier applied alongside .calendar-weekday</name>
  <files>src/dashboard/styles.css, src/dashboard/views/calendar.ts</files>
  <read_first>
    - `src/dashboard/styles.css` lines 747-753 (`.calendar-weekday`, `text-align: center`) and
      816-824 (`.calendar-week-total`, `justify-items: end`) — the two rules whose disagreement is
      IN-05.
    - `src/dashboard/views/calendar.ts` lines 302-345 — `renderGrid`, specifically the weekday loop
      (323-328) and the `Total` header cell built at 330-333, which is the single line this task
      edits.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § IN-05 — the finding and its
      exact suggested fix (`.calendar-weekday--total { text-align: right; }`, applied alongside
      `.calendar-weekday`).
    - `src/dashboard/views/calendar.test.ts` lines 238-320 — the Phase 22 source-structure guards.
      Note lines 280-284 and 311-314: they count WHOLE-FILE occurrences of `.focus()` and
      `tabindex` and require exactly 2 of each. WR-04 is out of scope this round, so this task must
      not change either count.
  </read_first>
  <action>
    In `src/dashboard/styles.css`, immediately after the `.calendar-weekday` rule, add a
    `.calendar-weekday--total` rule declaring `text-align: right;` and nothing else. Precede it with
    a short comment naming `IN-05` literally and stating that the `Total` header sits above
    `.calendar-week-total`'s `justify-items: end` column, so the shared `.calendar-weekday`
    `text-align: center` leaves the label off-centre from the values it names in any `auto` track
    wider than the word `Total`; and that the modifier is applied ALONGSIDE `.calendar-weekday`,
    never instead of it.

    In `src/dashboard/views/calendar.ts`, change the `Total` header cell's className assignment at
    line 331 from `'calendar-weekday'` to the exact literal
    `'calendar-weekday calendar-weekday--total'`. Change nothing else about that cell — its
    `textContent` stays `'Total'`, it stays a `div`, and it stays non-focusable (D-11).

    **Hard constraint on any comment or code you write in `calendar.ts`:** do not write the literal
    strings `tabindex` or `.focus()`. `calendar.test.ts:280-284` and `:311-314` count whole-file
    occurrences of both and assert exactly 2 of each; WR-04 (which would relax those counters) is
    explicitly out of scope this round, so tripping them is a self-inflicted red.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/calendar.test.ts src/dashboard/styles.test.ts && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e 'import {readFileSync} from "node:fs"; const css=readFileSync("src/dashboard/styles.css","utf8"); const s=css.replace(/\/\*[\s\S]*?\*\//g,""); const rules=[...s.matchAll(/([^{}]+)\{([^}]*)\}/g)].map(m=>({sels:m[1].split(",").map(x=>x.trim()),body:m[2]})); const r=rules.filter(x=>x.sels.includes(".calendar-weekday--total")); if(r.length!==1) throw new Error("expected exactly one .calendar-weekday--total rule, found "+r.length); if(!r[0].body.includes("text-align: right")) throw new Error(".calendar-weekday--total does not declare text-align: right"); const base=rules.filter(x=>x.sels.includes(".calendar-weekday")); if(!base.some(x=>x.body.includes("text-align: center"))) throw new Error(".calendar-weekday lost its text-align: center base - the modifier must override, not replace"); const cal=readFileSync("src/dashboard/views/calendar.ts","utf8"); if(!cal.includes("calendar-weekday calendar-weekday--total")) throw new Error("calendar.ts does not apply the modifier alongside the base class"); if((cal.match(/tabindex/g)||[]).length!==2) throw new Error("calendar.ts tabindex occurrence count moved off 2 - calendar.test.ts:311-314 will fail (WR-04 is out of scope)"); if((cal.match(/\.focus\(\)/g)||[]).length!==2) throw new Error("calendar.ts .focus() occurrence count moved off 2 - calendar.test.ts:280-284 will fail"); if(!/IN-05/.test(css)) throw new Error("the modifier is not attributed to IN-05 in a comment"); console.log("IN-05 modifier shipped, applied alongside the base class, WR-04 counters still at 2");'</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/styles.css` declares exactly one `.calendar-weekday--total` rule, and its body
      declares `text-align: right`.
    - `.calendar-weekday` still declares `text-align: center` at the top level (the modifier
      overrides the base rather than replacing it).
    - A comment in `styles.css` attributes the modifier to `IN-05`.
    - `src/dashboard/views/calendar.ts` contains the exact string
      `calendar-weekday calendar-weekday--total`.
    - `calendar.ts` still contains exactly 2 occurrences of `tabindex` and exactly 2 of `.focus()`.
    - `npx vitest run src/dashboard/views/calendar.test.ts src/dashboard/styles.test.ts` exits 0 and
      `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>The Total header carries a right-aligning modifier over the right-aligned column it labels, and the phase's existing source guards are undisturbed.</done>
</task>

<task type="auto">
  <name>Task 3: Add an override-aware CSS guard block, leaving the pre-existing false-green Phase 22 block untouched</name>
  <files>src/dashboard/styles.test.ts</files>
  <read_first>
    - `src/dashboard/styles.test.ts` lines 100-230 — `RULE_SCANNER`, `computeAtRuleRanges`,
      `isAtRuleScoped`. This is why an at-rule block's first nested rule is invisible and why
      position 1 of the 380px block is deliberately unguarded (WR-03).
    - `src/dashboard/styles.test.ts` lines 280-500 — `bodyForSelectorListToken`,
      `bodiesForSelectorListToken`, `cascadeWinningBodyDeclaring` and `assertNoAtRuleOverride`,
      including each JSDoc's explicit statement that the first three skip at-rule-scoped rules by
      construction and must be PAIRED with the fourth.
    - `src/dashboard/styles.test.ts` lines 1751-1817 — the existing
      `describe('styles.css — Phase 22 calendar week totals')` block, 15 `it(` cases. This task
      appends AFTER it and does not edit a single character of it.
    - `src/dashboard/styles.test.ts` line ~1748 — Phase 21's `assertNoAtRuleOverride` pairing, the
      in-repo precedent for the shape this task copies.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § WR-02 — the false-green
      finding, including its "where a deliberate override exists, assert the override instead of
      pretending it isn't there" instruction and the `expect(() => assertNoAtRuleOverride(...))
      .toThrow(/redeclares "font-size"/)` shape.
    - `src/dashboard/styles.css` as left by Tasks 1 and 2.
  </read_first>
  <action>
    Append a new `describe` to the END of `src/dashboard/styles.test.ts`, titled exactly
    `styles.css — Phase 22 gap closure (22-06): the 380px compaction and the Total header modifier`.
    Do not modify the existing `styles.css — Phase 22 calendar week totals` describe — WR-02 and
    WR-03 are out of scope this round and an automated check below asserts that block still holds
    exactly 15 `it(` cases.

    Write exactly these eight cases, using only the module's existing helpers:

    1. `GC-1: .calendar-day keeps min-width: 32px at the default breakpoint and is relaxed at 380px`
       — `cascadeWinningBodyDeclaring('.calendar-day', 'min-width')` contains `min-width: 32px`, AND
       `expect(() => assertNoAtRuleOverride('.calendar-day', 'min-width')).toThrow(/redeclares "min-width"/)`.
    2. `GC-1: .calendar-day__distance is 20px at the default breakpoint and overridden at 380px` —
       `bodyForSelectorListToken('.calendar-day__distance')` contains `font-size: 20px`, AND the
       same `toThrow(/redeclares "font-size"/)` pairing.
    3. `GC-1: .calendar-week-total__time is 14px at the default breakpoint and overridden at 380px`
       — base body contains `font-size: 14px`, AND the `toThrow(/redeclares "font-size"/)` pairing.
    4. The same case for `.calendar-week-total__count`.
    5. `WR-02 caveat: .calendar-week-total__distance's 380px override is asserted, not assumed away`
       — only the `expect(() => assertNoAtRuleOverride('.calendar-week-total__distance',
       'font-size')).toThrow(/redeclares "font-size"/)` half. Add a comment recording that the
       neighbouring pre-existing case asserting the 20px base in isolation is WR-02's documented
       false green and is deliberately left in place this round.
    6. `IN-05: .calendar-weekday--total declares text-align: right and is not overridden at any
       breakpoint` — `bodyForSelectorListToken('.calendar-weekday--total')` contains
       `text-align: right`; `assertNoAtRuleOverride('.calendar-weekday--total', 'text-align')` is
       called bare (it must NOT throw); and `bodyForSelectorListToken('.calendar-weekday')` contains
       `text-align: center`, proving the modifier overrides a real base value.
    7. `D-10/GC-1: the eight-column contract survives the compaction and the scroll wrapper stays
       unimplemented` — `bodyForSelectorListToken('.calendar-grid')` contains
       `grid-template-columns: repeat(7, 1fr) auto`; `assertNoAtRuleOverride('.calendar-grid',
       'grid-template-columns')` called bare; and that same body does not match `/overflow/`.
    8. `IN-06: this stylesheet carries exactly three disjoint @media (max-width: 380px) blocks` —
       count matches of `/@media \(max-width: 380px\)/g` in **`cssNoComments`** (never the raw `css`,
       whose comments now name the breakpoint in prose — this is IN-09's rationale applied to a
       structural check) and expect length 3. Add a comment stating this pairs with the reworded
       IN-06 comment in `styles.css`: if a future edit consolidates the blocks, this goes red and
       the comment must be corrected with it.

    Every positive assertion about a property that the 380px block overrides MUST carry its
    `toThrow(/redeclares/)` companion in the same `it(` — that is the WR-02 caveat this round is
    bound by. Do not add a bare positive assertion for `.calendar-day__distance`'s font-size,
    `.calendar-day`'s min-width, or either total sub-line's font-size without it.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npm test && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e 'import {readFileSync} from "node:fs"; const t=readFileSync("src/dashboard/styles.test.ts","utf8"); const OLD="styles.css — Phase 22 calendar week totals"; const NEW="styles.css — Phase 22 gap closure (22-06)"; const oi=t.indexOf(OLD); const ni=t.indexOf(NEW); if(oi===-1) throw new Error("the pre-existing Phase 22 describe is missing"); if(ni===-1) throw new Error("the new gap-closure describe is missing or misnamed"); if(ni<oi) throw new Error("the new describe must be appended AFTER the existing Phase 22 block"); const oldBlock=t.slice(oi,ni); if((oldBlock.match(/\bit\(/g)||[]).length!==15) throw new Error("the pre-existing Phase 22 describe no longer holds 15 it() cases - WR-02/WR-03 are out of scope this round"); const nb=t.slice(ni); if((nb.match(/\bit\(/g)||[]).length!==8) throw new Error("expected exactly 8 cases in the new block, found "+(nb.match(/\bit\(/g)||[]).length); if((nb.match(/assertNoAtRuleOverride/g)||[]).length<7) throw new Error("expected at least 7 assertNoAtRuleOverride calls in the new block"); if((nb.match(/toThrow\(\/redeclares/g)||[]).length<5) throw new Error("expected at least 5 override-asserting toThrow(/redeclares/) pairings"); for(const tok of [".calendar-weekday--total",".calendar-day__distance",".calendar-week-total__count","cssNoComments"]) if(!nb.includes(tok)) throw new Error("the new block does not reference "+tok); console.log("new gap-closure CSS block: 8 cases, 7+ override companions, 5+ throw pairings; pre-existing Phase 22 block untouched at 15 cases");'</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/styles.test.ts` contains a describe titled
      `styles.css — Phase 22 gap closure (22-06): the 380px compaction and the Total header modifier`,
      appended after the existing Phase 22 describe, holding exactly 8 `it(` cases.
    - The pre-existing `styles.css — Phase 22 calendar week totals` describe still holds exactly 15
      `it(` cases (unmodified).
    - The new block calls `assertNoAtRuleOverride` at least 7 times, at least 5 of them wrapped in
      `expect(() => ...).toThrow(/redeclares .../)`.
    - The new block's three-block count assertion reads `cssNoComments`, not `css`.
    - `npx vitest run src/dashboard/styles.test.ts` exits 0, `npm test` exits 0 (baseline 1203
      passing plus the 8 new cases; no test regresses), and
      `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>Every declaration this plan added to the 380px block is guarded by an assertion that acknowledges the override rather than asserting a base value the browser does not render at that width.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published stylesheet → browser rendering | The only untrusted-ish input crossing here is viewport width; the stylesheet itself is build-time content |
| stylesheet source → the test suite's regex substrate | `styles.test.ts` parses CSS with regexes that have documented blind spots (WR-03); a guard can silently claim more than it proves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-GC-CSS-01 | Information disclosure | `.calendar-day` / `.calendar-week-total` at ≤380px | mitigate | The fix shrinks type and relaxes the width floor; it adds no `overflow: hidden`, no `text-overflow` and no truncation, so a compacted value is never silently clipped into a WRONG displayed number. Task 1's automated check rejects any `overflow` declaration in the block |
| T-22-GC-CSS-02 | Tampering | the CSS guard suite | mitigate | Every new assertion about an overridden property pairs with `assertNoAtRuleOverride` (Task 3), so a future regression to the 380px block fails loudly instead of leaving a false green — the exact WR-02 defect class this round is forbidden from repeating |
| T-22-GC-CSS-03 | Denial of service | the parsed stylesheet | mitigate | Task 1 rewrites a large CSS comment. Phase 19 GAP 1 proved a malformed comment can silently discard live declarations while every text-assertion gate stays green, so Task 1 gates on `npm run build-widgets` exiting 0 with zero `css-syntax-error` — a real CSS parser, not a regex |
| T-22-GC-CSS-04 | Elevation of privilege | grid focus order | accept (avoided by design) | GC-1 rejects the horizontal-scroll wrapper precisely because a scroll container full of focusable day buttons scroll-jumps on Tab. No focusable element, no `tabindex` and no scroll container is introduced by this plan |
| T-22-SC | Tampering | npm/pip/cargo installs | accept (not applicable) | Zero new packages. `22-RESEARCH.md` § Package Legitimacy Audit records "Not applicable"; no install task exists in any Phase 22 plan |
</threat_model>

<verification>
- Task 1 proves the compaction's five declarations exist in the one calendar 380px block, that the
  padding rule still occupies the unguardable position 1, that exactly three disjoint blocks exist,
  that the DISC-6b comment kept `splits-scroll` and gained `GC-1`/`IN-06`/`WR-03`/`R11`, and that a
  real CSS parser accepts the file.
- Task 2 proves the IN-05 modifier exists, declares `text-align: right`, overrides a still-present
  `text-align: center` base, is applied alongside the base class in `calendar.ts`, and did not move
  either WR-04 counter.
- Task 3 proves the new guards are override-aware and that the pre-existing false-green block was
  not touched.

**This plan discharges no visual claim.** Whether the ~380px overflow is actually gone is settled
only by plan `22-08`'s Round 2 row R13, observed by the developer's own eyes in a real browser
(PROJECT.md line 49; the R11 FAIL that opened this gap was itself a visual judgement).
</verification>

<success_criteria>
- The calendar 380px block compacts `.calendar-day__distance` (20px→14px) and
  `.calendar-week-total__time`/`__count` (14px→12px) and relaxes `.calendar-day` to `min-width: 0`,
  on top of the padding and `__distance` compaction already shipped.
- The `.splits-scroll` fallback remains documented and unimplemented; `.calendar-grid` gains no
  `overflow` and keeps `repeat(7, 1fr) auto` at every breakpoint.
- The `Total` header is right-aligned via a modifier applied alongside `.calendar-weekday`.
- The block's comment states the truth about the file's three 380px blocks.
- Eight new override-aware CSS assertions pass; the pre-existing Phase 22 CSS block is byte-stable.
- `npm test` and `npx tsc --noEmit -p tsconfig.json` both exit 0; `npm run build-widgets` exits 0
  with zero `css-syntax-error`.
- CAL-02 is NOT ticked by this plan.
</success_criteria>

<output>
Create `.planning/phases/22-calendar-week-start-totals/22-06-SUMMARY.md` when done.
</output>
