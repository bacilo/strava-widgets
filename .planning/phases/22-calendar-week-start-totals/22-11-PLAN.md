---
phase: 22-calendar-week-start-totals
plan: 11
type: execute
wave: 10
depends_on: ["22-10"]
gap_closure: true
round: 3
files_modified:
  - src/dashboard/theme.ts
  - src/dashboard/theme.test.ts
  - src/dashboard/main.ts
  - src/dashboard/nav.ts
  - src/dashboard/views/detail-charts.ts
  - src/dashboard/views/detail-charts-logic.ts
  - src/dashboard/views/detail-charts-logic.test.ts
  - src/dashboard/storage.test.ts
autonomous: true
requirements: [CAL-01]
must_haves:
  decisions: [D-05, D-06, GC-5]
  truths:
    - "GC-5 / BL-03 closes end to end: every bootstrap-reachable storage-global dereference resolves through `storage.ts`'s `resolveStorage` — `main.ts:19`, `nav.ts:186`, `nav.ts:206`, `theme.ts:93`, `theme.ts:130` and `detail-charts.ts:218`. Under blocked site data the app boots, the nav renders and every view mounts on its defaults instead of the page going blank"
    - "GC-5 discovery, recorded rather than silently absorbed: `nav.ts:186` and `nav.ts:206` carry the identical unguarded shape and were NOT named in the user's locked list (which named main.ts:19, theme.ts:93, theme.ts:130, detail-charts.ts:218). `nav.ts:186` runs inside `createNav`, which `main.ts:22` calls at MODULE SCOPE — so fixing main.ts:19 alone would still blank the page. Including them is required to deliver the decision's own stated outcome, not a widening of it"
    - "Every per-key reader tolerates a null handle explicitly, before its own try/catch: `readStoredMode(null)` returns `'auto'`, `applyThemeMode` still sets `data-theme` and simply skips the persist step, `watchSystemTheme` still registers its listener, and `readStoredOverlayConfig(null)` returns `DEFAULT_OVERLAY_CONFIG` while `writeStoredOverlayConfig(null, ...)` is a no-op — the same explicit-fallback shape `readStoredWeekStart(null)` already uses (GC-2b)"
    - "A repo-wide source guard in `storage.test.ts` proves the invariant instead of trusting it: across every non-test `.ts` file under `src/dashboard/`, comments stripped, `storage.ts` is the ONLY file matching `globalThis.localStorage` or a bare `localStorage`/`sessionStorage` identifier, and it matches exactly once. Re-introducing an unguarded dereference anywhere in the dashboard now fails a test"
    - "`nav.ts`'s header claim that the file `never touches localStorage directly` becomes TRUE for the first time — it was false at lines 186 and 206 as shipped, and the comment is updated to name where the handle now comes from"
    - "`theme.ts`'s ownership claim survives unweakened: it still owns the entire theme contract and `THEME_STORAGE_KEY`; only the HANDLE resolution moved to `storage.ts`. No theme key, parse or value handling moves out of `theme.ts`"
    - "Behaviour is unchanged for every user whose storage works: `resolveStorage()` returns the live `globalThis.localStorage` exactly as `?? localStorage` did, so theme persistence, the theme toggle cycle, the overlay config and the week-start preference all behave identically — proven by the existing suites staying green plus plan 22-12's Round 3 row R23"
    - "The full phase gate is green on the combined 22-09 + 22-10 + 22-11 tree before the Round 3 checkpoint stages a build from it: `npm test`, `npx tsc --noEmit`, `npm run build-widgets` with zero `css-syntax-error`, and `npm run verify-dashboard`"
    - "This plan discharges NO browser claim. Whether a real blocked-site-data browser now boots is decided ONLY by plan 22-12's Round 3 row R22, which may not be declined and may not be waived"
  artifacts:
    - path: "src/dashboard/main.ts"
      provides: "a bootstrap whose module-scope theme read cannot throw"
      contains: "resolveStorage()"
    - path: "src/dashboard/nav.ts"
      provides: "guarded theme reads at both the initial toggle render and the click handler, and a header claim that is finally true"
      contains: "resolveStorage()"
    - path: "src/dashboard/theme.ts"
      provides: "null-tolerant `readStoredMode`, and `applyThemeMode`/`watchSystemTheme` resolving their handle through the shared helper"
      contains: "resolveStorage"
    - path: "src/dashboard/views/detail-charts-logic.ts"
      provides: "null-tolerant overlay-config read and write"
      contains: "if (!storage)"
    - path: "src/dashboard/storage.test.ts"
      provides: "the repo-wide single-dereference-site guard across src/dashboard/"
      contains: "readdirSync"
  key_links:
    - from: "main.ts's module-scope theme read"
      to: "the guarded resolver"
      via: "readStoredMode(resolveStorage())"
      pattern: "readStoredMode\\(resolveStorage\\(\\)\\)"
    - from: "nav.ts's two theme reads"
      to: "the guarded resolver"
      via: "readStoredMode(resolveStorage())"
      pattern: "resolveStorage\\(\\)"
    - from: "every non-test module under src/dashboard/"
      to: "exactly one storage-global dereference site"
      via: "the repo-wide source guard in storage.test.ts"
      pattern: "globalThis\\.localStorage"
---

<objective>
Close **Gap 2** of `22-VERIFICATION.md` at the app level: wire every remaining unguarded
storage-global dereference to the shared `resolveStorage()` plan `22-10` created, so the
blocked-site-data threat `calendar-preferences.ts`'s header and `calendar.ts`'s rationale comment
have been claiming to close is actually closed end to end.

Purpose: the user's locked decision is to fix **BL-03** app-wide. Plan `22-10` built and proved the
helper; without this plan it guards only the Calendar's own read, and `main.ts:19` still throws
during module evaluation under blocked site data — meaning the whole dashboard module graph fails,
the page renders blank, and the corrected comments plan `22-10` wrote would still be describing an
outcome the code does not deliver.

**Discovery this plan is obliged to record, not absorb silently.** The locked decision named four
sites: `main.ts:19`, `theme.ts:93`, `theme.ts:130`, `detail-charts.ts:218`. A grep of
`src/dashboard/` finds **two more of the identical shape**: `nav.ts:186`
(`updateThemeToggle(readStoredMode(localStorage))`, inside `createNav`, which `main.ts:22` calls at
MODULE SCOPE) and `nav.ts:206` (inside the theme-toggle click handler). Fixing `main.ts:19` and
leaving `nav.ts:186` would move the module-scope throw down two lines and still blank the page —
so the decision's own stated expected outcome ("app boots, nav renders, Calendar grid and week
totals render") is unreachable without them. They are in scope.

Output: six guarded call sites, four null-tolerant readers, a repo-wide source guard proving there
is exactly one dereference site left, and a green full phase gate on the combined Round 3 tree.

**Explicitly NOT in scope.** `src/dashboard/index.html`'s inline pre-paint script (lines 36-53)
already wraps its `localStorage.getItem` in try/catch and is NOT edited — it is why the theme
ATTRIBUTE still applies under blocked storage even today, while the module script dies. No storage
key, parse function or preference contract moves out of its owning module (D-06 / the deferred
shared-preference facility). `styles.css`, `styles.test.ts`, `calendar.ts`, `calendar-preferences.ts`
and `storage.ts` are earlier plans' files and are not edited here. **WR-04**, **WR-05**,
**WR-08**–**WR-11** stay out of scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-calendar-week-start-totals/22-CONTEXT.md
@.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md
@.planning/phases/22-calendar-week-start-totals/22-REVIEW.md
@.planning/phases/22-calendar-week-start-totals/22-10-PLAN.md
</context>

<interfaces>
<!-- Every site this plan edits, extracted from source. No exploration needed. -->

Created by plan `22-10` — `src/dashboard/storage.ts`:

- `export interface WebStorage { getItem(key: string): string | null; setItem(key: string, value: string): void }`
- `export function resolveStorage(override?: WebStorage): WebStorage | null`

`src/dashboard/theme.ts` (143 lines):

- `export interface ThemeStorage { getItem(key: string): string | null; setItem(key: string, value: string): void }` (54-57)
- `export function readStoredMode(storage: ThemeStorage): ThemeMode` (63-69) — try/catch around
  `storage.getItem` only; returns `'auto'` on throw
- `export interface ApplyThemeOptions { prefersDark?: boolean; doc?: Document; storage?: ThemeStorage; persist?: boolean }` (71-76)
- `export function applyThemeMode(mode, options = {}): Theme` (91-111) — line 93
  `const storage = options.storage ?? localStorage;` ← unguarded; line 99 sets `data-theme`;
  lines 101-108 persist inside a try/catch
- `export function watchSystemTheme(callback, options = {}): () => void` (124-143) — line 130
  `const storage = options.storage ?? localStorage;` ← unguarded
- header lines 1-17, including "nothing else in `src/dashboard/` may read or write `localStorage`
  for theming" and the T-16-TH-01 note

`src/dashboard/main.ts` (88 lines):

- line 10: `import { applyThemeMode, readStoredMode } from './theme.js';`
- line 19: `applyThemeMode(readStoredMode(localStorage));` ← module scope, unguarded, the BL-03
  crash site
- line 22: `const nav = createNav(document.getElementById('app-nav-root')!);` ← also module scope,
  and `createNav` reaches `nav.ts:186`
- lines 53-78: the try/catch around `view.mount(...)` INSIDE `onMatch` whose catch renders the
  generic "Something went wrong" panel — unreachable from a module-scope throw

`src/dashboard/nav.ts`:

- header line 10: "Theming always goes through theme.ts; this file never touches localStorage
  directly." ← false as shipped
- line 186: `updateThemeToggle(readStoredMode(localStorage));` ← unguarded, reached from
  `main.ts:22` at module scope
- line 206: `const current = readStoredMode(localStorage);` inside `handleThemeToggleClick`
  ← unguarded
- there is no `nav.test.ts` in this repo

`src/dashboard/views/detail-charts.ts` (567 lines):

- line 181: `storage?: Pick<Storage, 'getItem' | 'setItem'>;` on the mount options
- line 218: `const storage = options.storage ?? globalThis.localStorage;` ← unguarded
- line 219: `let overlayConfig: OverlayConfig = readStoredOverlayConfig(storage);`
- line 377: `writeStoredOverlayConfig(storage, overlayConfig);`

`src/dashboard/views/detail-charts-logic.ts`:

- line 301: `export function readStoredOverlayConfig(storage: Pick<Storage, 'getItem'>): OverlayConfig`
  — try/catch, returns `DEFAULT_OVERLAY_CONFIG` on throw or absent key
- line 315: `export function writeStoredOverlayConfig(storage: Pick<Storage, 'setItem'>, config: OverlayConfig): void`
  — try/catch, swallows failures

The precedent this plan copies, already shipped in `src/dashboard/views/calendar-preferences.ts`:
`readStoredWeekStart(storage: WeekStartStorage | null)` opens with `if (!storage) return 'monday';`
BEFORE its try/catch, so the fallback is intentional rather than an artefact of a `TypeError` being
swallowed by a catch written for storage-subsystem failures (GC-2b).
</interfaces>

<settled_decisions>
| # | Choice | Decision | Rationale |
|---|--------|----------|-----------|
| GC-5i | `nav.ts:186` / `nav.ts:206` | **In scope**, and the deviation from the locked four-site list is recorded in the summary | `nav.ts:186` is reached from `main.ts:22`, a module-scope statement. Leaving it unguarded means the page still goes blank under blocked site data, i.e. the locked decision's own stated expected outcome for row R22 is unreachable. This is an addition required BY the decision, never a reduction of it |
| GC-5j | How readers tolerate a null handle | An explicit `if (!storage)` early return BEFORE the existing try/catch, in each reader | Mirrors `readStoredWeekStart`/`writeWeekStart` exactly (GC-2b). Relying on the existing catch to swallow a `TypeError` would work by accident and would silently absorb real bugs of a different class |
| GC-5k | Widen the public signatures, or guard at each call site? | **Widen** — `readStoredMode`, `readStoredOverlayConfig`, `writeStoredOverlayConfig` and `ApplyThemeOptions.storage` all accept `| null` | Guarding at call sites would put the same three-line dance in six places, which is the duplication BL-03 exists to remove. Widening is backwards compatible: every existing test passes a real fake and keeps compiling |
| GC-5l | Type used in the widened signatures | `WebStorage` from `storage.ts`, with `export type ThemeStorage = WebStorage;` keeping the exported name | Same reconciliation `22-10` applied to `WeekStartStorage`. `theme.test.ts` and `detail-charts.ts` import these names; the alias keeps them compiling. All shapes were already structurally identical, `Pick<Storage,'getItem'|'setItem'>` included |
| GC-5m | `applyThemeMode` under a null handle | Still resolves `prefersDark`, still sets `data-theme`, simply skips the persist step | The DOM attribute is the behaviour that matters — the module's own existing comment says exactly that about a throwing `setItem`. A user with blocked storage gets a correctly themed, working page on defaults |
| GC-5n | How the app-wide invariant is enforced | A repo-wide, comment-stripped source scan in `storage.test.ts` over every non-test `.ts` under `src/dashboard/` | This is the guard class the repo already uses (`row-semantics.test.ts`, `styles.test.ts`'s `cssNoComments`, and `22-10`'s WR-02 guard). A per-file guard would leave the next new module free to reintroduce the defect |
| GC-5o | `index.html`'s inline pre-paint script | **Not touched** | It already wraps `localStorage.getItem` in try/catch. It is the reason the theme attribute survives blocked storage today while the module script dies, and re-touching it would add an unmeasured variable to R22 |
</settled_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make theme.ts null-tolerant and resolve its handle through the shared helper</name>
  <files>src/dashboard/theme.ts, src/dashboard/theme.test.ts</files>
  <read_first>
    - `src/dashboard/theme.ts` — the whole file (143 lines): the header's ownership claim and
      T-16-TH-01 note (1-17), `ThemeStorage` (54-57), `readStoredMode` (63-69),
      `ApplyThemeOptions` (71-76), `applyThemeMode` including line 93's
      `options.storage ?? localStorage` and the lazy-default rationale in its JSDoc (78-111), and
      `watchSystemTheme` including line 130 (119-143).
    - `src/dashboard/storage.ts` as created by plan `22-10` — `WebStorage` and `resolveStorage`'s
      exact signature and semantics.
    - `src/dashboard/views/calendar-preferences.ts` `readStoredWeekStart` / `writeWeekStart` — the
      shipped `if (!storage) return ...;`-before-try/catch shape this task copies (GC-5j).
    - `src/dashboard/theme.test.ts` — the whole file (246 lines): its `fakeStorage` helper and the
      `readStoredMode` (141-157), `applyThemeMode` (159-193) and `watchSystemTheme` (195+) describes,
      so the new cases match the file's existing style and none of the existing ones regress.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § BL-03 — the finding, its
      `resolveStorage` sketch, and its note that `readStoredMode`/`applyThemeMode`/`watchSystemTheme`
      already accept an injected storage so "the change is small and the theme module already
      tolerates a throwing `getItem`/`setItem`".
  </read_first>
  <behavior>
    Add these cases to `src/dashboard/theme.test.ts` FIRST, watch them fail to type-check or fail at
    runtime, then implement:

    - `readStoredMode(null)` returns `'auto'` and does not throw.
    - `applyThemeMode('dark', { prefersDark: false, doc, storage: null })` returns `'dark'` and still
      sets `data-theme="dark"` on `doc.documentElement` — the DOM update is not conditional on a
      usable storage handle.
    - `applyThemeMode('light', { prefersDark: true, doc, storage: null })` does not throw with
      `persist` left at its default `true`.
    - `watchSystemTheme(callback, { mediaQuery, storage: null })` registers its listener and, when
      the media query fires, does NOT invoke the callback — because `readStoredMode(null)` is
      `'auto'`… **assert the actual resulting behaviour rather than guessing**: `readStoredMode(null)`
      returns `'auto'`, so the auto-only guard passes and the callback IS invoked. Write the case to
      assert the callback fires exactly once, and add a comment stating that a blocked-storage user
      therefore keeps following the system theme, which is the correct default.
    - Every pre-existing case in `theme.test.ts` still passes unchanged, including the
      `throwOnGet`/`throwOnSet` cases.
  </behavior>
  <action>
    In `src/dashboard/theme.ts`:

    - Import `resolveStorage` and `type WebStorage` from `./storage.js`.
    - Replace the `ThemeStorage` interface declaration with `export type ThemeStorage = WebStorage;`,
      keeping the exported name (`theme.test.ts` and `nav.ts` reference it). Add a one-line comment
      recording that the shape is declared once, in `storage.ts`.
    - Widen `readStoredMode` to `(storage: ThemeStorage | null): ThemeMode` and open its body with an
      explicit `if (!storage) return 'auto';` placed BEFORE the existing try/catch. Leave the
      try/catch itself untouched — it still covers a throwing `getItem`.
    - Widen `ApplyThemeOptions.storage` to `ThemeStorage | null`.
    - Replace line 93 with `const storage = resolveStorage(options.storage ?? undefined);` and change
      the persist branch's condition so it also requires a non-null handle (the existing try/catch
      stays around the `setItem` call). The `data-theme` write at line 99 stays unconditional.
    - Replace line 130 the same way in `watchSystemTheme`; the listener registration and the
      auto-only guard are otherwise unchanged, since `readStoredMode` now tolerates `null`.
    - Update the header: keep the ownership claim ("this module owns the entire theme contract …
      nothing else in `src/dashboard/` may read or write `localStorage` for theming") — it is still
      true, because `storage.ts` resolves a HANDLE and never touches `THEME_STORAGE_KEY`. Add a
      sentence naming `BL-03` and stating that the storage HANDLE is now resolved by
      `storage.ts`'s `resolveStorage`, because the property getter throws under blocked site data and
      the previous `?? localStorage` form threw during module evaluation when reached from
      `main.ts`'s module scope.
    - Update `applyThemeMode`'s existing JSDoc paragraph about lazy default resolution so it still
      describes what the function does (the reason it resolves inside the body is unchanged; only
      the resolver moved).

    Then add the `<behavior>` cases to `src/dashboard/theme.test.ts`, following the file's existing
    `fakeStorage`/`doc` stub style.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/theme.test.ts src/dashboard/storage.test.ts && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e 'import {readFileSync} from "node:fs"; const t=readFileSync("src/dashboard/theme.ts","utf8"); const live=t.replace(/\/\*[\s\S]*?\*\//g,"").replace(/\/\/.*$/gm,""); if(/(^|[^.\w])localStorage\b/m.test(live)) throw new Error("theme.ts still dereferences a bare localStorage"); if(!/from ".\/storage.js"/.test(t)) throw new Error("theme.ts does not import from ./storage.js"); if(!/export type ThemeStorage = WebStorage/.test(t)) throw new Error("ThemeStorage was not reduced to an alias of WebStorage"); if((t.match(/resolveStorage\(/g)||[]).length<2) throw new Error("expected resolveStorage at BOTH theme.ts:93 and theme.ts:130, found "+((t.match(/resolveStorage\(/g)||[]).length)); if(!/storage: ThemeStorage \| null|ThemeStorage \| null/.test(t)) throw new Error("readStoredMode / ApplyThemeOptions were not widened to accept null"); if(!/if \(!storage\)/.test(t)) throw new Error("no explicit falsy-handle early return in readStoredMode"); if(!t.includes("BL-03")) throw new Error("theme.ts header does not cite BL-03"); if(!/THEME_STORAGE_KEY/.test(t)) throw new Error("theme.ts lost its own storage key - the contract must NOT move to storage.ts"); const tt=readFileSync("src/dashboard/theme.test.ts","utf8"); if(!/readStoredMode\(null\)/.test(tt)) throw new Error("no readStoredMode(null) case"); if(!/storage: null/.test(tt)) throw new Error("no applyThemeMode / watchSystemTheme null-handle case"); console.log("theme.ts resolves through the shared guard at both sites and tolerates a null handle");'</automated>
  </verify>
  <acceptance_criteria>
    - `theme.ts`, comments stripped, contains no bare `localStorage` identifier.
    - `theme.ts` imports from `./storage.js`, declares `export type ThemeStorage = WebStorage;`, and
      calls `resolveStorage(` at least twice (once in `applyThemeMode`, once in `watchSystemTheme`).
    - `readStoredMode` accepts `ThemeStorage | null` and opens with an explicit `if (!storage)`
      early return before its try/catch; `ApplyThemeOptions.storage` accepts `null`.
    - `theme.ts` still declares `THEME_STORAGE_KEY`, still parses with `parseThemeMode`'s allow-list,
      and its header cites `BL-03` while keeping the theme-ownership claim.
    - `theme.test.ts` covers `readStoredMode(null)`, an `applyThemeMode` call with `storage: null`
      that still sets `data-theme`, and a `watchSystemTheme` call with `storage: null`.
    - `npx vitest run src/dashboard/theme.test.ts src/dashboard/storage.test.ts` exits 0 and
      `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>The theme engine can no longer throw from a storage global, and a user with blocked site data still gets a correctly themed page that follows their system preference.</done>
</task>

<task type="auto">
  <name>Task 2: Guard the two module-scope-reachable bootstrap reads (main.ts:19, nav.ts:186) and the theme-toggle handler (nav.ts:206)</name>
  <files>src/dashboard/main.ts, src/dashboard/nav.ts</files>
  <read_first>
    - `src/dashboard/main.ts` — the whole file (88 lines). Lines 16-19 (the module-scope re-apply and
      its comment), line 22 (`createNav(...)`, also module scope, which reaches `nav.ts:186`), and
      lines 53-78 (the `onMatch` try/catch that renders "Something went wrong" — the panel BL-03
      proves is unreachable from a module-scope throw).
    - `src/dashboard/nav.ts` — lines 1-20 (the header, including line 10's "this file never touches
      localStorage directly" claim), lines 175-215 (`updateThemeToggle`, the `readStoredMode(localStorage)`
      call at 186, `handleThemeToggleClick` and its read at 206, and the `watchSystemTheme`
      registration below them).
    - `src/dashboard/theme.ts` as left by Task 1 — `readStoredMode`'s widened signature.
    - `src/dashboard/storage.ts` — `resolveStorage`'s signature.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § BL-03 — specifically the
      `main.ts:19` sketch (`applyThemeMode(readStoredMode(resolveStorage()));`).
    - `.planning/phases/22-calendar-week-start-totals/22-VERIFICATION.md` Gap 2's `artifacts:` entry
      for `src/dashboard/main.ts` — the issue as the verifier worded it.
    - Note: there is no `nav.test.ts` in this repo, so `nav.ts`'s change is covered by the repo-wide
      source guard added in Task 3 plus plan `22-12` row R22, not by a unit test.
  </read_first>
  <action>
    In `src/dashboard/main.ts`:

    - Add `import { resolveStorage } from './storage.js';` alongside the existing imports.
    - Change line 19 to `applyThemeMode(readStoredMode(resolveStorage()));`.
    - Extend the comment above it (currently items 1's rationale about re-applying what the inline
      pre-paint script already set). Keep that point and add, naming `BL-03` literally: this
      statement runs at MODULE SCOPE, so an unguarded storage-global read here throws during module
      evaluation under blocked site data and the entire dashboard module graph fails — the page
      renders blank, with no nav and no view, and the `onMatch` try/catch below (which renders the
      generic error panel) is never reached because it wraps `view.mount(...)`, not module
      evaluation. Note that `createNav` on the next statement is module-scope too and carries the
      same requirement.

    In `src/dashboard/nav.ts`:

    - Add `import { resolveStorage } from './storage.js';`.
    - Change line 186 to `updateThemeToggle(readStoredMode(resolveStorage()));`.
    - Change line 206 to `const current = readStoredMode(resolveStorage());`.
    - Update the header comment at line 10. It currently claims "this file never touches localStorage
      directly", which was FALSE at both of those lines as shipped. Keep the claim — it is true after
      this task — and add that the handle comes from `storage.ts`'s `resolveStorage` (`BL-03`), and
      that line 186's read is reached from `main.ts`'s module-scope `createNav` call, which is why it
      had to be guarded and not merely tidied.

    Change nothing else in either file. Do not alter the bootstrap ORDER (theme, then nav chrome,
    then container, then index load, then router) — `main.ts`'s header states that ordering mirrors
    `widget-base.ts`'s `connectedCallback` and it is not this plan's to renegotiate.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e 'import {readFileSync} from "node:fs"; for(const f of ["src/dashboard/main.ts","src/dashboard/nav.ts"]){ const s=readFileSync(f,"utf8"); const live=s.replace(/\/\*[\s\S]*?\*\//g,"").replace(/\/\/.*$/gm,""); if(/(^|[^.\w])localStorage\b/m.test(live)) throw new Error(f+" still dereferences a bare localStorage"); if(/globalThis\.localStorage/.test(live)) throw new Error(f+" still dereferences globalThis.localStorage"); if(!/from ".\/storage.js"/.test(s)) throw new Error(f+" does not import resolveStorage from ./storage.js"); if(!s.includes("BL-03")) throw new Error(f+" does not cite BL-03 in its comment"); } const m=readFileSync("src/dashboard/main.ts","utf8"); if(!/applyThemeMode\(readStoredMode\(resolveStorage\(\)\)\)/.test(m)) throw new Error("main.ts:19 was not rewritten to the guarded form"); if(!/module scope/i.test(m)) throw new Error("main.ts comment does not state that this statement runs at module scope"); if(!/blank/i.test(m)) throw new Error("main.ts comment does not state the actual failure mode (a blank page)"); const n=readFileSync("src/dashboard/nav.ts","utf8"); if((n.match(/readStoredMode\(resolveStorage\(\)\)/g)||[]).length!==2) throw new Error("expected exactly 2 guarded reads in nav.ts (lines 186 and 206), found "+((n.match(/readStoredMode\(resolveStorage\(\)\)/g)||[]).length)); if(!/never touches localStorage directly/.test(n)) throw new Error("nav.ts lost its header claim - it should be KEPT, now that it is finally true"); console.log("bootstrap guarded: main.ts:19, nav.ts:186 and nav.ts:206 all resolve through resolveStorage");'</automated>
  </verify>
  <acceptance_criteria>
    - Neither `main.ts` nor `nav.ts`, comments stripped, contains a bare `localStorage` identifier or
      a `globalThis.localStorage` dereference.
    - `main.ts` contains exactly the guarded form
      `applyThemeMode(readStoredMode(resolveStorage()))`, and its comment cites `BL-03`, states that
      the statement runs at module scope, and states the real failure mode (a blank page, the
      `onMatch` error panel unreachable).
    - `nav.ts` contains exactly two occurrences of `readStoredMode(resolveStorage())` (its lines 186
      and 206), imports from `./storage.js`, and keeps its "never touches localStorage directly"
      header claim with a `BL-03` note explaining where the handle comes from.
    - The bootstrap ordering in `main.ts` (theme → nav → container → index load → router) is
      unchanged.
    - `npm test` exits 0 with no test regressed and `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>Both module-scope-reachable storage reads in the bootstrap are guarded, so blocked site data can no longer prevent the dashboard's module graph from evaluating.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard the detail-charts overlay path, add the repo-wide single-dereference-site guard, and re-run the full phase gate</name>
  <files>src/dashboard/views/detail-charts.ts, src/dashboard/views/detail-charts-logic.ts, src/dashboard/views/detail-charts-logic.test.ts, src/dashboard/storage.test.ts</files>
  <read_first>
    - `src/dashboard/views/detail-charts.ts` lines 175-230 and 370-385 — the `storage?:
      Pick<Storage, 'getItem' | 'setItem'>` option (181), the unguarded resolution at 218, the
      `readStoredOverlayConfig(storage)` read at 219, and the `writeStoredOverlayConfig(storage, ...)`
      write at 377.
    - `src/dashboard/views/detail-charts-logic.ts` lines 290-330 — `OVERLAY_STORAGE_KEY`,
      `DEFAULT_OVERLAY_CONFIG`, `parseOverlayConfig`, `readStoredOverlayConfig` (301-309) and
      `writeStoredOverlayConfig` (315-324), including both existing try/catch blocks.
    - `src/dashboard/views/detail-charts-logic.test.ts` — the existing overlay-config describes, so
      the new null-handle cases match the file's style.
    - `src/dashboard/views/calendar-preferences.ts` — the `if (!storage) return ...;`-before-try/catch
      shape being copied (GC-5j).
    - `src/dashboard/storage.test.ts` as left by plan `22-10` — the file this task appends the
      repo-wide guard to.
    - `src/dashboard/row-semantics.test.ts` and `src/dashboard/styles.test.ts` lines 14-31 — the
      in-repo precedent for source-text guards and for stripping comments before scanning.
    - `.planning/phases/22-calendar-week-start-totals/22-REVIEW.md` § BL-03's closing paragraph —
      the list of sites (`main.ts:19`, `theme.ts:93`, `theme.ts:130`, `detail-charts.ts:218`) the
      repo-wide guard must now find empty.
  </read_first>
  <behavior>
    Write these tests FIRST, watch them fail, then implement:

    In `src/dashboard/views/detail-charts-logic.test.ts`:
    - `readStoredOverlayConfig(null)` returns `DEFAULT_OVERLAY_CONFIG` and does not throw.
    - `writeStoredOverlayConfig(null, someConfig)` does not throw and performs no observable work.
    - The existing throwing-`getItem` / throwing-`setItem` / malformed-JSON cases still pass.

    In `src/dashboard/storage.test.ts`, appended as a new describe:
    - **The repo-wide guard.** Walk every `.ts` file under `src/dashboard/` recursively, excluding
      any path ending in `.test.ts` and excluding `storage.ts` itself. For each, strip block comments
      (`/* ... */`) then line comments (`// ...` to end of line), and assert the remaining text
      matches NEITHER `/globalThis\.(localStorage|sessionStorage)/` NOR a bare
      `localStorage`/`sessionStorage` identifier (anchored so a property access or a longer
      identifier cannot match). Build the failure message so it names the offending FILE, otherwise a
      red run says nothing useful.
    - `storage.ts` itself, comments stripped, contains exactly ONE `globalThis.localStorage`
      occurrence — the guard is a single-site claim, not a zero-site claim, and this half proves the
      site still exists rather than having been deleted along with the feature.
    - Add a comment recording that this is `BL-03`'s app-wide invariant, that six sites carried the
      unguarded shape before Round 3 (`main.ts:19`, `nav.ts:186`, `nav.ts:206`, `theme.ts:93`,
      `theme.ts:130`, `detail-charts.ts:218`), and that comment-stripping is mandatory because
      several of those files discuss `localStorage` in prose.
  </behavior>
  <action>
    In `src/dashboard/views/detail-charts-logic.ts`:

    - Import `type WebStorage` from `../storage.js`.
    - Widen `readStoredOverlayConfig` to `(storage: WebStorage | null): OverlayConfig` and open its
      body with `if (!storage) return DEFAULT_OVERLAY_CONFIG;` BEFORE the existing try/catch.
    - Widen `writeStoredOverlayConfig` to `(storage: WebStorage | null, config: OverlayConfig): void`
      and open its body with an early `return` on a falsy handle, before the existing try/catch.
    - Leave `OVERLAY_STORAGE_KEY`, `parseOverlayConfig`, the JSON handling and both catches exactly as
      they are — the key contract stays in this module (D-06 fence).

    In `src/dashboard/views/detail-charts.ts`:

    - Import `resolveStorage` and `type WebStorage` from `../storage.js`.
    - Change the mount option's type at line 181 from `Pick<Storage, 'getItem' | 'setItem'>` to
      `WebStorage`.
    - Replace line 218 with `const storage = resolveStorage(options.storage);`.
    - Add a short comment naming `BL-03` and stating that the property access is guarded in
      `storage.ts` because it throws under blocked site data, and that a `null` handle degrades this
      view to `DEFAULT_OVERLAY_CONFIG` rather than taking the detail view down.
    - Change nothing else — line 219's read and line 377's write compile unchanged against the
      widened signatures.

    Then append the `<behavior>` cases: the two null-handle cases to
    `src/dashboard/views/detail-charts-logic.test.ts`, and the repo-wide guard describe to
    `src/dashboard/storage.test.ts` (use `readdirSync(dir, { withFileTypes: true })` recursively from
    `new URL('.', import.meta.url)`, or `readdirSync(..., { recursive: true })` — either is fine, but
    the walk must reach `src/dashboard/views/` as well as `src/dashboard/`).

    Finally, re-run the whole phase gate so that plans 22-09, 22-10 and 22-11 together are proven
    green before the Round 3 checkpoint plan stages a build from this tree.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/views/detail-charts-logic.test.ts src/dashboard/storage.test.ts && npm test && npx tsc --noEmit -p tsconfig.json && node --input-type=module -e 'import {readFileSync, readdirSync} from "node:fs"; import {join} from "node:path"; const dl=readFileSync("src/dashboard/views/detail-charts-logic.ts","utf8"); if(!/WebStorage \| null/.test(dl)) throw new Error("the overlay read/write signatures were not widened to WebStorage | null"); if((dl.match(/if \(!storage\)/g)||[]).length<2) throw new Error("expected an explicit falsy-handle early return in BOTH readStoredOverlayConfig and writeStoredOverlayConfig"); if(!/OVERLAY_STORAGE_KEY/.test(dl)) throw new Error("the overlay key contract must stay in detail-charts-logic.ts"); const dc=readFileSync("src/dashboard/views/detail-charts.ts","utf8"); if(!/resolveStorage\(options\.storage\)/.test(dc)) throw new Error("detail-charts.ts:218 was not rewritten to the guarded form"); if(!dc.includes("BL-03")) throw new Error("detail-charts.ts does not cite BL-03"); const walk=(d)=>readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(join(d,e.name)):[join(d,e.name)]); const files=walk("src/dashboard").filter(f=>f.endsWith(".ts")&&!f.endsWith(".test.ts")); const offenders=[]; for(const f of files){ if(f.replace(/\\\\/g,"/").endsWith("src/dashboard/storage.ts")) continue; const live=readFileSync(f,"utf8").replace(/\/\*[\s\S]*?\*\//g,"").replace(/\/\/.*$/gm,""); if(/globalThis\.(localStorage|sessionStorage)/.test(live)||/(^|[^.\w])(localStorage|sessionStorage)\b/m.test(live)) offenders.push(f); } if(offenders.length) throw new Error("BL-03 invariant broken - these files still dereference a storage global: "+offenders.join(", ")); const st=readFileSync("src/dashboard/storage.ts","utf8").replace(/\/\*[\s\S]*?\*\//g,"").replace(/\/\/.*$/gm,""); if((st.match(/globalThis\.localStorage/g)||[]).length!==1) throw new Error("storage.ts must hold exactly one globalThis.localStorage dereference"); const t=readFileSync("src/dashboard/storage.test.ts","utf8"); if(!/readdirSync/.test(t)) throw new Error("storage.test.ts carries no repo-wide walk"); if(!t.includes("BL-03")) throw new Error("the repo-wide guard is not attributed to BL-03"); console.log("app-wide invariant holds: "+files.length+" non-test dashboard modules scanned, exactly one dereference site");' && rm -f /tmp/bw22r3b.log && npm run build-widgets > /tmp/bw22r3b.log 2>&1; BW=$?; tail -3 /tmp/bw22r3b.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw22r3b.log && npm run verify-dashboard && echo "full phase gate green after 22-09 + 22-10 + 22-11"</automated>
  </verify>
  <acceptance_criteria>
    - `detail-charts-logic.ts` widens both overlay functions to `WebStorage | null`, each with an
      explicit `if (!storage)` early return before its existing try/catch, and still declares
      `OVERLAY_STORAGE_KEY` and `parseOverlayConfig`.
    - `detail-charts.ts` resolves via `resolveStorage(options.storage)` and cites `BL-03`.
    - Every `.ts` file under `src/dashboard/` that is not a `*.test.ts` and is not `storage.ts`
      contains, comments stripped, zero `globalThis.(localStorage|sessionStorage)` dereferences and
      zero bare `localStorage`/`sessionStorage` identifiers.
    - `storage.ts`, comments stripped, contains exactly one `globalThis.localStorage` occurrence.
    - `storage.test.ts` carries a repo-wide walk (`readdirSync`) attributed to `BL-03` whose failure
      message names the offending file.
    - `detail-charts-logic.test.ts` covers `readStoredOverlayConfig(null)` and
      `writeStoredOverlayConfig(null, ...)`.
    - `npm test` exits 0 with no test regressed, `npx tsc --noEmit -p tsconfig.json` exits 0,
      `npm run build-widgets` exits 0 with zero `css-syntax-error` in its output, and
      `npm run verify-dashboard` exits 0.
  </acceptance_criteria>
  <done>Every storage-global dereference in the dashboard now goes through one guarded helper, a test proves it repo-wide rather than by grep, and the combined Round 3 tree passes the full phase gate ready for the checkpoint to stage a build from it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser storage policy → module evaluation | Crossed at `main.ts:19` and (via `main.ts:22` → `createNav`) at `nav.ts:186`. A throw here is unrecoverable: no router, no view, no error panel |
| browser storage policy → view mount | Crossed at `detail-charts.ts:218` and (already guarded) `calendar.ts:433`. A throw here is caught by `main.ts`'s `onMatch` catch and degrades to the generic panel — bad, but survivable |
| `localStorage` values → theme attribute / overlay config / grid math | Each value is user- and extension-writable (T-16-TH-01). Every consumer keeps its own allow-list or parse; the shared resolver deliberately handles none of them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-WK-02 | Denial of service | the dashboard bootstrap under blocked site data | mitigate | All six sites resolve through `storage.ts`'s single try/catch, and every reader tolerates a null handle explicitly. Locked repo-wide by Task 3's source guard and observed in a real browser by plan `22-12` row R22, which may not be declined |
| T-16-TH-01 | Tampering | the persisted theme mode | mitigate (unchanged) | `parseThemeMode`'s exact `'light' \| 'dark' \| 'auto'` allow-list stays the only path from storage to `setAttribute('data-theme', ...)`. `resolveStorage` returns a handle and never a value, so the trust boundary is not widened by this refactor |
| T-22-GC3-APP-01 | Tampering | the app-wide single-dereference invariant | mitigate | Task 3's repo-wide, comment-stripped walk fails on ANY future module reintroducing the shape, and names the file. Without it the invariant would be a paragraph in `storage.ts`'s header, which is exactly the class of unenforced convention WR-06 flagged elsewhere in this phase |
| T-22-GC3-APP-02 | Denial of service | the overlay config path | mitigate | `readStoredOverlayConfig(null)` returns `DEFAULT_OVERLAY_CONFIG` rather than throwing a `TypeError` that the detail view's own catch would then present as a broken chart shell |
| T-22-GC3-APP-03 | Repudiation | silent storage failure, app-wide | accept | Every fallback stays silent (no console output), matching `theme.ts`'s and `calendar-preferences.ts`'s existing policy. A blocked-storage user gets a working dashboard on defaults; the trade is recorded rather than left implicit |
| T-22-GC3-APP-04 | Elevation of privilege | scope creep into the deferred preference facility | mitigate | No storage key, parse function or preference contract moves out of its owning module — `THEME_STORAGE_KEY` stays in `theme.ts`, `OVERLAY_STORAGE_KEY` in `detail-charts-logic.ts`, `WEEK_START_STORAGE_KEY` in `calendar-preferences.ts`. Plan `22-10`'s narrowness check on `storage.ts` enforces the other half |
| T-22-SC | Tampering | npm/pip/cargo installs | accept (not applicable) | Zero new packages. `22-RESEARCH.md` § Package Legitimacy Audit records "Not applicable"; no install task exists in any Phase 22 plan |
</threat_model>

<verification>
- Task 1 proves `theme.ts` has no bare `localStorage` left, resolves through the shared helper at
  BOTH former sites, tolerates a null handle explicitly, keeps its own key and allow-list, and still
  sets `data-theme` when storage is unusable.
- Task 2 proves both module-scope-reachable bootstrap reads are guarded, that `main.ts`'s comment
  records the real failure mode, and that `nav.ts`'s header claim is now true rather than aspirational.
- Task 3 proves the overlay path degrades to its default under a null handle, that the app-wide
  invariant holds across every non-test dashboard module by an executed walk rather than by grep,
  that exactly one dereference site remains, and that the combined Round 3 tree passes the full
  phase gate.

**This plan discharges no browser claim.** Automated tests simulate the throw; they do not prove a
real browser's behaviour under a real privacy setting. That is plan `22-12`'s row **R22**, which is
mandatory this round and may not be waived.
</verification>

<success_criteria>
- `resolveStorage` is the only storage-global dereference in `src/dashboard/`, proven repo-wide by a
  test that names any offender.
- All six previously-unguarded sites — including the two in `nav.ts` the locked list did not name,
  one of which is module-scope-reachable — resolve through it.
- Every per-key reader tolerates a null handle with an explicit early return, and no key, parse or
  preference contract moved modules.
- `main.ts`, `nav.ts`, `theme.ts` and `detail-charts.ts` comments describe the real failure mode and
  the real guard.
- `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets` (zero `css-syntax-error`)
  and `npm run verify-dashboard` all exit 0.
- **No requirement is ticked by this plan.**
</success_criteria>

<output>
Create `.planning/phases/22-calendar-week-start-totals/22-11-SUMMARY.md` when done.
</output>
