---
phase: 16-dashboard-shell-data-contract
plan: 15
type: execute
wave: 3
depends_on: [16-14]
gap_closure: true
closes_gaps: ["SC1", "SC3"]
closes_findings: []
files_modified: []
autonomous: false
requirements: [DASH-01, DASH-03]

must_haves:
  truths:
    - "On the LIVE GitHub Pages origin, a human navigated Overview, Activities, Calendar, Records and Trends and back, with no full page reload and no 404"
    - "On the LIVE origin, a hard refresh on each of the five hashes still renders the correct view"
    - "In light mode the theme toggle is legible against the #f5f5f7 nav surface on both a light-OS and a dark-OS machine"
    - "Exactly one theme icon is visible at a time, and the active state uses the accent color"
    - "Cycling the toggle through light, dark and auto changes the page theme and the choice survives a reload"
  artifacts: []
  key_links:
    - from: "https://bacilo.github.io/strava-widgets/"
      to: "src/dashboard/view-registry.ts"
      via: "hash routes resolved by the deployed SPA bundle"
      pattern: "#/"
---

<objective>
Discharge the two chrome-level human checks from the verification report against the live GitHub Pages origin — navigation and theme-toggle visibility — now that the site is actually deployed.

Purpose: the DASH-01 navigation UAT at the 16-09 checkpoint was performed against a local server and, per the verifier, cannot substitute for the live origin — SC1's "on GitHub Pages" clause is what failed. DASH-03's toggle-visibility symptom was human-confirmed as invisible-in-light-mode and its three structural causes were fixed in plan 16-11; the fix's failure mode is visual contrast against a UA-derived `ButtonText` fallback, which no grep can assess. Both checks need a real browser on real hardware.

Output: two recorded human verdicts against `https://bacilo.github.io/strava-widgets/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dashboard-shell-data-contract/16-CONTEXT.md
@.planning/phases/16-dashboard-shell-data-contract/16-UI-SPEC.md

<interfaces>
<!-- The exact routes, labels and expected copy, so the checkpoint text can be
     specific and the human is not asked to guess what "correct" means. -->

Live origin: https://bacilo.github.io/strava-widgets/

Nav entries and routes (src/dashboard/view.types.ts NAV_ORDER, in order):
  Overview   #/
  Activities #/list
  Calendar   #/calendar
  Records    #/records
  Trends     #/trends
The detail route #/activity/:id is deliberately NOT a nav entry.

Expected view content:
  Overview   — headline stat cards (total km, streaks) plus recent activity rows
  Activities — up to 100 newest rows, each with a "View Activity" action, and a
               truncation notice naming the total count
  Calendar   — a titled "coming soon" stub naming Phase 17
  Records    — a titled "coming soon" stub naming Phase 18
  Trends     — a titled "coming soon" stub naming Phase 18

Theme toggle (src/dashboard/nav.ts): a single button at the right end of the nav
bar. Cycles light -> dark -> auto. aria-label reads "Theme: light" / "Theme: dark"
/ "Theme: auto". Persists in localStorage under the theme.ts storage key and is
re-applied before first paint by the inline bootstrap in index.html.

Nav surface color in light mode: --surface #f5f5f7. Text token: --text #333333.
Accent: #fc4c02 light, #ff6b35 dark.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm live hash navigation across all five views</name>
  <files>none — human verification against the live origin</files>
  <read_first>
    - .planning/phases/16-dashboard-shell-data-contract/16-VERIFICATION.md lines 273-277 — human check 1, which specifies why a local server cannot substitute
    - .planning/phases/16-dashboard-shell-data-contract/16-14-SUMMARY.md — the deploy evidence, including the gh-pages sha and the live-URL status table, so the check runs against a confirmed-fresh deploy
    - src/dashboard/view-registry.ts — the six registered routes and their view modules
  </read_first>
  <what-built>
    Plan 16-14 pushed `master`, ran the gated `daily-refresh` workflow, and verified against `origin/gh-pages` that the site root is the dashboard SPA, that `widgets.html` carries the showcase, and that `data/dashboard/`, `data/activities/` and `data/streams/` are all published. Every one of those assertions ran against the remote and the live origin over HTTP — none against a local build. The router itself (dual `hashchange` + `DOMContentLoaded` triggers) was verified in plan 16-03 and confirmed locally at the 16-09 checkpoint; what has never been tested is that behavior on the deployed origin.
  </what-built>
  <action>
    Before pausing, confirm the human will be sent to a fresh, reachable deploy rather than a stale page. Run `curl -sS -o /dev/null -w '%{http_code}\n' https://bacilo.github.io/strava-widgets/` and require 200, and run `curl -sS https://bacilo.github.io/strava-widgets/ | grep -c 'id="app-nav-root"'` and require at least 1 — proving the served root document is the SPA. Report `git log -1 --format='%H %cI' origin/gh-pages` in the checkpoint message so the human knows exactly which deploy they are testing.

    Then pause for the human verification described below. Do not attempt to simulate the browser steps: there is no headless browser in this repo's toolchain, and a full page reload, a 404, and a hash-routed view change are indistinguishable to `curl`. Simulating them is what let the 16-09 checkpoint pass against a local server while SC1 failed.
  </action>
  <verify>
    <automated>curl -sS https://bacilo.github.io/strava-widgets/ | grep -c 'id="app-nav-root"'</automated>
  </verify>
  <done>The human returned an explicit verdict covering all seven numbered steps, recorded verbatim in the SUMMARY along with the confirmation that the address bar read bacilo.github.io.</done>
  <how-to-verify>
    Open `https://bacilo.github.io/strava-widgets/` in a browser. This must be the live URL, not `localhost` and not a `file://` path.

    1. Confirm the landing view is the Overview home: headline stat cards with real numbers (total km, streaks) and a list of recent activities — not a "coming soon" stub and not the old static widget showcase.
    2. Open the browser devtools Network tab and switch it to record. Click each nav entry in turn: Activities, Calendar, Records, Trends, then the brand link back to Overview. After each click confirm the address bar hash changed to `#/list`, `#/calendar`, `#/records`, `#/trends`, `#/` respectively, that the view content changed, and that the Network tab shows NO document-type request for the page itself — a full page reload would appear as a new document request and would mean routing is broken.
    3. Confirm Calendar names Phase 17 in its coming-soon copy, and Records and Trends each name Phase 18.
    4. Deep-refresh test: for each of `#/`, `#/list`, `#/calendar`, `#/records`, `#/trends`, put the URL in the address bar directly and hard-refresh (Cmd-Shift-R / Ctrl-Shift-R). Each must render its own view, not a 404 and not the Overview fallback.
    5. Type a deliberately bogus hash such as `#/nonsense` and confirm it falls back to the Overview view rather than showing a blank page or an error.
    6. Confirm the nav shows exactly five entries in the order Overview, Activities, Calendar, Records, Trends — and no entry for an individual activity.
    7. Narrow the browser window below 640px and confirm the nav collapses behind a hamburger button that is comfortably tappable, that opening it lists the same five entries, and that clicking one both navigates and closes the menu.

    Report any 404, any full page reload, any view that renders blank, or any mismatch against the expected content above.
  </how-to-verify>
  <acceptance_criteria>
    - All five nav entries navigate to their stated hash with no document-type network request (no full page reload).
    - Hard refresh on each of the five hashes renders that view, not a 404 and not a fallback.
    - `#/nonsense` falls back to Overview.
    - Calendar names Phase 17; Records and Trends name Phase 18.
    - The nav lists exactly the five entries, in order, with no detail entry.
    - Below 640px the nav collapses behind a hamburger that opens, lists all five, and closes on selection.
    - The human explicitly confirms the browser address bar showed `bacilo.github.io`, not `localhost`.
  </acceptance_criteria>
  <resume-signal>Type "approved" if every numbered step passed, or describe exactly which step failed and what you saw instead.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the theme toggle is legible and single-icon in light mode on both a light-OS and a dark-OS machine</name>
  <files>none — human verification against the live origin</files>
  <read_first>
    - .planning/phases/16-dashboard-shell-data-contract/16-VERIFICATION.md lines 285-289 — human check 3 and why it cannot be assessed by grep
    - .planning/phases/16-dashboard-shell-data-contract/16-11-SUMMARY.md — the four CSS edits and the mutation-check result, so the human knows precisely what changed
    - src/dashboard/styles.css — the corrected `.theme-toggle`, `.theme-toggle__icon` and `color-scheme` rules
  </read_first>
  <what-built>
    Plan 16-11 fixed the three compounding defects behind the invisible toggle, all in `src/dashboard/styles.css` with no change to `nav.ts`: `color-scheme` is now declared per `data-theme` block so the browser's used scheme follows the app's theme instead of the OS; `.theme-toggle` and `.app-nav__toggle` now pin `color: var(--text)` so the icons' `currentColor` resolves to a theme token rather than the UA's `ButtonText`; and the icon swap is now done with `display: none` / `display: inline` plus `color: var(--accent)` on the active icon, replacing a `fill: var(--accent)` rule that SVG presentation attributes always overrode. A committed `styles.test.ts` asserts all of it and was shown to fail when the fix is reverted.
  </what-built>
  <action>
    Before pausing, prove that the CSS fix from plan 16-11 actually reached the deployed bundle, so a human "still invisible" verdict can be attributed to contrast rather than to a stale deploy. Extract the built stylesheet URL with `curl -sS https://bacilo.github.io/strava-widgets/ | grep -o 'assets/[^"]*\.css'`, fetch that asset, and check it for three things, matching without whitespace because the output is minified: it contains `color-scheme:light` and `color-scheme:dark`; the active-icon rule contains `display:inline`; and it contains no `fill:var(--accent)`. Report all three results in the checkpoint message.

    Then pause for the human verification below. Do not attempt to assess legibility or contrast programmatically — the failure mode is a UA-derived `ButtonText` value resolved at render time against the OS appearance, which is exactly what a human on real hardware is for.
  </action>
  <verify>
    <automated>curl -sS https://bacilo.github.io/strava-widgets/ | grep -o 'assets/[^"]*\.css'</automated>
  </verify>
  <done>The human returned an explicit verdict from both a light-OS and a dark-OS machine, with the OS appearance setting recorded alongside each verdict.</done>
  <how-to-verify>
    Do this on `https://bacilo.github.io/strava-widgets/`, twice: once on a machine (or OS setting) whose system appearance is LIGHT, and once whose system appearance is DARK. The dark-OS pass is the important one — that is the configuration where the old `ButtonText` fallback painted a near-white icon on the near-white `#f5f5f7` nav bar.

    On each machine:
    1. Clear the site's localStorage (devtools, Application, Local Storage, delete the theme key) and reload, so the toggle starts in `auto`.
    2. Look at the right end of the nav bar. Exactly ONE icon must be visible — a sun or a moon, never both.
    3. Click the toggle until the page is in LIGHT mode (background white, nav bar `#f5f5f7`, body text dark). In this state, confirm the toggle icon is clearly legible against the nav bar — you should be able to see its shape without hunting for it. This is the exact symptom that failed at the 16-09 checkpoint.
    4. Confirm the visible icon is rendered in the Strava-orange accent (`#fc4c02` in light mode, `#ff6b35` in dark mode), not in a washed-out or system-default color.
    5. Click through the full cycle light, dark, auto and back to light. On each press confirm: the page theme changes, exactly one icon remains visible, and the icon that is visible matches the effective theme.
    6. Hover the toggle and read its accessible name (or inspect its `aria-label`) — it must read `Theme: light`, `Theme: dark` or `Theme: auto`, matching the current mode.
    7. Set the toggle to dark, reload the page, and watch the very first paint. The page must appear dark immediately with no white flash.
    8. Set the toggle to `auto`, then change the OS appearance setting while the page is open. The page theme must follow the OS change without a reload.
    9. Narrow the window below 640px and confirm the hamburger icon is also legible against the nav surface in light mode — it has the same `currentColor` dependency and received the same fix.

    Report the machine's OS appearance setting alongside each verdict, since the defect was OS-dependent.
  </how-to-verify>
  <acceptance_criteria>
    - On BOTH a light-OS and a dark-OS machine, exactly one theme icon is visible at any time.
    - On BOTH machines, in light mode, the toggle icon is legible against the `#f5f5f7` nav surface.
    - The visible icon renders in the accent color (`#fc4c02` light, `#ff6b35` dark).
    - Cycling light, dark, auto changes the page theme and the visible icon matches the effective theme at each step.
    - The accessible name matches the current mode.
    - A dark-mode reload paints dark on the first frame with no white flash.
    - In `auto`, changing the OS appearance changes the page theme with no reload.
    - The hamburger icon is legible in light mode below 640px.
    - The human explicitly records which OS appearance setting each pass was performed under.
  </acceptance_criteria>
  <resume-signal>Type "approved" if both the light-OS and dark-OS passes succeeded, or describe which machine, which mode, and what the toggle looked like.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public origin to viewer | The live site renders committed archive data to anyone who loads the URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-15-01 | Spoofing | The verification target itself | mitigate | Both checkpoints require the human to confirm the address bar reads `bacilo.github.io`, not `localhost` — this is the exact substitution that let the 16-09 UAT pass while SC1 failed. |
| T-16-15-02 | Information Disclosure | Content rendered on the public nav and overview | accept | The overview's headline stats and recent activity names are intentionally public, consistent with the already-published widget data. Plan 16-16 carries the closer look at per-activity location strings. |
| T-16-15-SC | Tampering | npm installs | accept | This plan installs no packages and modifies no files; it is verification only. |
</threat_model>

<verification>
1. Both checkpoints returned an explicit human verdict, recorded verbatim in the SUMMARY.
2. Each verdict names the URL origin used and, for Task 2, the OS appearance setting.
3. Any failure is recorded as a named gap with the observed behavior, not paraphrased as "mostly works".
</verification>

<success_criteria>
- SC1's "on GitHub Pages" clause is discharged by a human navigating the live origin, not a local server.
- SC3's toggle-visibility symptom is confirmed resolved on both a light-OS and a dark-OS machine.
- Any residual defect is captured verbatim with the machine configuration that produced it.
</success_criteria>

<output>
Create `.planning/phases/16-dashboard-shell-data-contract/16-15-SUMMARY.md` when done. Record each checkpoint verdict verbatim, including the OS appearance settings used for Task 2. If either checkpoint comes back PARTIAL, list the residual gaps explicitly rather than marking the plan complete.
</output>
