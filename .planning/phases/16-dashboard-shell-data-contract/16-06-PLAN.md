---
phase: 16-dashboard-shell-data-contract
plan: 06
type: execute
wave: 2
depends_on: [16-02, 16-03]
files_modified:
  - src/dashboard/index.html
  - src/dashboard/nav.ts
  - src/dashboard/views/stub-view.ts
  - src/dashboard/views/calendar.stub.ts
  - src/dashboard/views/records.stub.ts
  - src/dashboard/views/trends.stub.ts
autonomous: true
requirements: [DASH-01, DASH-03]

must_haves:
  truths:
    - "The page applies the persisted theme before first paint, so a dark-mode user never sees a light flash"
    - "The top nav lists Overview, Activities, Calendar, Records and Trends in that order, and never lists the detail route"
    - "Below 640px the nav collapses behind a hamburger whose hit target is at least 44x44px"
    - "Calendar, Records and Trends render the exact UI-SPEC coming-soon copy naming the right future phase"
  artifacts:
    - path: "src/dashboard/index.html"
      provides: "The Vite SPA entry with the pre-paint theme bootstrap and app shell containers"
      contains: "data-theme"
    - path: "src/dashboard/nav.ts"
      provides: "Top nav bar construction, active-route marking, hamburger collapse, theme toggle"
      exports: ["createNav"]
    - path: "src/dashboard/views/stub-view.ts"
      provides: "Shared coming-soon panel factory driving all three stubs"
      exports: ["createStubView"]
  key_links:
    - from: "src/dashboard/index.html"
      to: "src/dashboard/styles.css"
      via: "stylesheet link in head, after the synchronous theme bootstrap"
      pattern: "styles.css"
    - from: "src/dashboard/nav.ts"
      to: "src/dashboard/view.types.ts"
      via: "NAV_ORDER drives the rendered links"
      pattern: "NAV_ORDER"
    - from: "src/dashboard/views/*.stub.ts"
      to: "src/dashboard/view.types.ts"
      via: "STUB_PHASE supplies the phase number in the body copy"
      pattern: "STUB_PHASE"
---

<objective>
Build the visible shell: the HTML entry with its pre-paint theme bootstrap, the top navigation bar with theme toggle and mobile collapse, and the three coming-soon stub views for the routes Phase 17 and 18 will fill.

Purpose: DASH-01's navigable chrome and DASH-03's flash-free theming. D-07 explicitly allows unbuilt views to render titled stubs, but the stubs are part of the contract — the UI-SPEC fixes their exact copy so the nav never leads anywhere broken.
Output: `src/dashboard/index.html`, `src/dashboard/nav.ts`, and four modules under `src/dashboard/views/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dashboard-shell-data-contract/16-UI-SPEC.md
@.planning/phases/16-dashboard-shell-data-contract/16-CONTEXT.md
@.planning/phases/16-dashboard-shell-data-contract/16-02-SUMMARY.md
@.planning/phases/16-dashboard-shell-data-contract/16-03-SUMMARY.md

<interfaces>
<!-- Contracts these files render against. -->

From plan 03 — `src/dashboard/view.types.js`:
```typescript
export interface ViewMountContext { container: HTMLElement; routeParams: Record<string,string>; query: URLSearchParams }
export interface DashboardView { route: string; title: string; navEntry?: { label: string; order: number }; mount(ctx): void | Promise<void>; unmount?(): void }
export const ROUTES = { OVERVIEW: '/', LIST: '/list', CALENDAR: '/calendar', RECORDS: '/records', TRENDS: '/trends', DETAIL: '/activity/:id' }
export const NAV_ORDER: readonly { route: string; label: string; order: number }[]  // Overview, Activities, Calendar, Records, Trends
export const STUB_PHASE: Readonly<Record<string, '17' | '18'>>  // calendar->17, records->18, trends->18
```

From plan 03 — `src/dashboard/router.js`: `navigateTo(path, query?)`, `parseHash(hash)`.

From plan 02 — `src/dashboard/theme.js`:
`THEME_STORAGE_KEY = 'dashboard-theme'`, `parseThemeMode`, `resolveEffectiveTheme`, `cycleThemeMode`, `readStoredMode`, `applyThemeMode`, `watchSystemTheme`, types `Theme` / `ThemeMode`.

From plan 02 — `src/dashboard/styles.css` provides these class hooks (already authored, do not add new ones):
`.app-nav`, `.app-nav__links`, `.app-nav__link`, `.app-nav__toggle`, `.theme-toggle`, `.theme-toggle__icon--active`, `.view`, `.card`, `.stat-grid`, `.activity-list`, `.activity-row`, `.activity-row__name`, `.activity-row__meta`, `.badge`, `.cta`, `.loading-indicator`, `.error-state`, `.stub-panel`, plus `.text-body` / `.text-label` / `.text-heading` / `.text-display`. The nav opens on mobile via `data-open="true"` on the nav element.

UI-SPEC copy contract (verbatim strings, no paraphrase):
- Stub heading: `"{View Name} — coming soon"` (em dash U+2014)
- Stub body: `"This view lands in Phase {17 or 18}. Browse Activities or check the Overview for now."`
- Nav order: Overview, Activities, Calendar, Records, Trends. Detail is drill-in only.

Existing analog for the standalone-page HTML shape: `src/pages/heatmap.html` (58 lines). Its `<script src="./x.iife.js">` tag is a BUILT-WIDGET reference, not a Vite entry pattern — the dashboard entry instead uses `<script type="module" src="./main.ts">`, which Vite rewrites at build time.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SPA entry with a pre-paint theme bootstrap</name>
  <files>src/dashboard/index.html</files>
  <read_first>
    - src/pages/heatmap.html (full file — the meta/viewport/title conventions and nav markup to stay consistent with; note it loads external CDN libs which the dashboard must NOT, per D-01 zero-dependency)
    - .planning/phases/16-dashboard-shell-data-contract/16-RESEARCH.md (Pitfall 4 "Flash of wrong theme on load" and Pattern 3 — this task's entire reason for existing)
    - .planning/phases/16-dashboard-shell-data-contract/16-PATTERNS.md ("No Analog Found" table — the pre-paint inline script has no precedent in this repo; widgets solve theming inside `connectedCallback`, which does not apply at document scope)
    - src/dashboard/theme.ts (plan 02 — the storage key, the allow-list, and the rule that `data-theme` holds only `light` or `dark`)
    - src/dashboard/styles.css (plan 02 — confirm the selectors are `:root[data-theme="light"|"dark"]`, so the attribute must exist before the stylesheet is evaluated for paint)
  </read_first>
  <action>
    Create `src/dashboard/index.html` as the Vite entry that becomes `dist/widgets/index.html` (D-08, wired by plan 08).

    Head, in this exact order — the ordering is the whole point:
    1. `<meta charset="UTF-8">` and `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
    2. `<title>Training Dashboard — Strava Analytics</title>`
    3. A `<meta name="color-scheme" content="light dark">` so the browser's own UA surfaces (scrollbars, form controls) match.
    4. A `<meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'">` — RESEARCH.md Security Domain flags CSP as a discretionary defense-in-depth measure for this public site; `'unsafe-inline'` on script is required by the bootstrap in step 5 and on style by Vite's asset handling. `connect-src 'self'` is meaningful here because every data fetch is same-origin by design (D-11).
    5. A SYNCHRONOUS, NON-MODULE `<script>` block — the theme bootstrap. It MUST be inline and must run before the stylesheet link. It cannot import `theme.ts`, because a `type="module"` script is deferred by spec and would paint the wrong theme first (RESEARCH.md Pitfall 4). Keep it to roughly ten lines and duplicate only the minimum logic: read `localStorage.getItem('dashboard-theme')` inside a try/catch, allow-list the value against `['light','dark','auto']` falling back to `'auto'`, resolve `'auto'` via `window.matchMedia('(prefers-color-scheme: dark)').matches`, and set `document.documentElement.setAttribute('data-theme', effective)`. Add a comment above it stating that `src/dashboard/theme.ts` is the source of truth and that this duplication is deliberate and must stay behaviourally identical — including the allow-list, which is the T-16-TH-01 mitigation and must not be dropped from the inline copy.
    6. `<link rel="stylesheet" href="./styles.css">`

    Body:
    - `<header id="app-nav-root"></header>` — the mount point `nav.ts` populates. Leave it empty; the nav is built in TypeScript so `NAV_ORDER` stays the single enumeration point (D-03) rather than being duplicated as static markup.
    - `<main id="app" class="view"></main>` — the single container every view mounts into.
    - `<noscript>` block explaining the dashboard requires JavaScript, with links to the existing static pages `./heatmap.html`, `./pinmap.html` and `./routes.html` so a no-JS visitor is not stranded.
    - `<script type="module" src="./main.ts"></script>` as the last element in body. `main.ts` arrives in plan 07; the reference is intentionally forward-looking and is not built until plan 08 wires the Vite entry.

    Do not inline any styling beyond what is described; `styles.css` is the only stylesheet (D-04). Do not add any external script, font, or CDN reference (D-01).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('src/dashboard/index.html','utf8');const iBoot=h.indexOf('dashboard-theme');const iCss=h.indexOf('styles.css');const iMod=h.indexOf('type=\"module\"');if(iBoot<0)throw new Error('no theme bootstrap');if(iCss<0)throw new Error('no stylesheet link');if(iBoot>iCss)throw new Error('theme bootstrap must precede the stylesheet link (Pitfall 4)');if(iMod>=0&&iMod<iBoot)throw new Error('module script must not precede the synchronous bootstrap');const boot=h.slice(h.lastIndexOf('<script',iBoot),h.indexOf('</script>',iBoot));if(boot.includes('type=\"module\"'))throw new Error('theme bootstrap must be a non-module synchronous script');for(const t of ['light','dark','auto'])if(!boot.includes(\"'\"+t+\"'\")&&!boot.includes('\"'+t+'\"'))throw new Error('bootstrap allow-list missing '+t);if(!boot.includes('prefers-color-scheme'))throw new Error('bootstrap must resolve auto via prefers-color-scheme');if(!h.includes('id=\"app\"'))throw new Error('missing #app container');if(!h.includes('id=\"app-nav-root\"'))throw new Error('missing nav mount point');if(!h.includes('Content-Security-Policy'))throw new Error('missing CSP meta');if(/https?:\/\//.test(h.replace(/<!--[\s\S]*?-->/g,'').replace(/http-equiv/g,'')))throw new Error('external URL reference found (D-01 forbids CDN deps)');if(!h.includes('./main.ts'))throw new Error('missing module entry');console.log('index.html contract OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/index.html` exists and the verify command exits 0
    - The theme bootstrap `<script>` has no `type="module"` attribute and appears in `<head>` BEFORE the `styles.css` `<link>`
    - The bootstrap allow-lists exactly `light`, `dark`, `auto` and resolves `auto` through `window.matchMedia('(prefers-color-scheme: dark)')`
    - The bootstrap reads the key `dashboard-theme` (identical to `THEME_STORAGE_KEY` in `src/dashboard/theme.ts`)
    - The document contains `<main id="app" class="view">` and `<header id="app-nav-root">`
    - The document contains a `Content-Security-Policy` meta with `default-src 'self'` and `connect-src 'self'`
    - The document contains no `http://` or `https://` resource reference (no CDN, no external font)
    - The document's last body element is `<script type="module" src="./main.ts"></script>`
  </acceptance_criteria>
  <done>The SPA entry applies the persisted or system theme before the stylesheet can paint, declares a same-origin CSP, and exposes the two mount points the shell needs.</done>
</task>

<task type="auto">
  <name>Task 2: Build the top nav bar with theme toggle and mobile collapse</name>
  <files>src/dashboard/nav.ts</files>
  <read_first>
    - src/dashboard/view.types.ts (plan 03 — `NAV_ORDER` is the single enumeration source; do not hardcode labels)
    - src/dashboard/theme.ts (plan 02 — `readStoredMode`, `cycleThemeMode`, `applyThemeMode`, `watchSystemTheme`; the toggle must go through these, never touch localStorage directly)
    - src/dashboard/styles.css (plan 02 — `.app-nav`, `.app-nav__links`, `.app-nav__link`, `.app-nav__toggle`, `.theme-toggle`, `.theme-toggle__icon--active`, and the `data-open="true"` mobile-open hook; use these classes, do not invent new ones)
    - src/dashboard/router.ts (plan 03 — `navigateTo`; nav links are real `href="#/..."` anchors so middle-click and copy-link work, but the active-state update is driven by the router callback)
    - .planning/phases/16-dashboard-shell-data-contract/16-UI-SPEC.md ("Nav structure (D-05)", "Responsive breakpoint", "Theme toggle (D-14)", and the accent-reserved list)
    - src/pages/heatmap.html (lines 20-50 — the existing `nav a.active` accent-underline convention this rebuilds with tokens instead of hardcoded hex)
  </read_first>
  <action>
    Create `src/dashboard/nav.ts` exporting `export function createNav(root: HTMLElement): { setActiveRoute(route: string): void; destroy(): void }`.

    Build the nav with DOM APIs (`document.createElement`, `textContent`, `append`) rather than an `innerHTML` template. Nav labels come from `NAV_ORDER`, not from literals in this file.

    Structure:
    - A `<nav class="app-nav">` element containing a brand element (`<a class="app-nav__brand" href="#/">` with `textContent = 'Training Dashboard'`), a `<button class="app-nav__toggle">` hamburger, a `<ul class="app-nav__links">`, and a `<button class="theme-toggle">`.
    - One `<li><a class="app-nav__link">` per entry in `NAV_ORDER` sorted by `order`, with `href` set to `'#' + entry.route` and `textContent` set to `entry.label`. Because `NAV_ORDER` excludes `ROUTES.DETAIL`, the drill-in route can never appear here (D-05).

    Behaviour:
    - `setActiveRoute(route)` sets `aria-current="page"` on the matching link and removes it from all others. Style comes from the existing `.app-nav__link[aria-current="page"]` rule; do not set inline styles. Plan 07's bootstrap calls this on every route match.
    - The hamburger toggles `data-open` between `'true'` and `'false'` on the `<nav>` element and keeps `aria-expanded` in sync. Give it `aria-label="Toggle navigation"` and `aria-controls` pointing at the `<ul>`'s id. Clicking any nav link closes the menu (set `data-open="false"`).
    - Hand-author the hamburger icon as an inline `<svg>` built with `document.createElementNS('http://www.w3.org/2000/svg', ...)` — three horizontal lines. No icon library, no icon font (UI-SPEC: zero new npm deps).
    - The theme toggle cycles the mode with `cycleThemeMode(readStoredMode(localStorage))`, applies it with `applyThemeMode(next)`, updates its own `aria-label` to name the new mode (`'Theme: light'` / `'Theme: dark'` / `'Theme: auto'`), and swaps its icon: a hand-authored sun `<svg>` for light, a moon `<svg>` for dark, and both rendered at reduced opacity with the currently-effective one carrying `.theme-toggle__icon--active` for auto. Initialize the toggle's label and icon from `readStoredMode` on construction.
    - Call `watchSystemTheme` so that while the mode is `auto`, a system theme change re-applies the effective theme and refreshes the toggle icon. Store the returned unsubscribe function and invoke it from `destroy()`, alongside removing the listeners this function added.

    Never write `innerHTML` in this file; every text node goes through `textContent`. Never read or write `localStorage` directly — go through `theme.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs');const s=fs.readFileSync('src/dashboard/nav.ts','utf8');if(s.includes('innerHTML'))throw new Error('nav.ts must not use innerHTML');if(/localStorage\./.test(s))throw new Error('nav.ts must go through theme.ts, not localStorage directly');for(const t of ['NAV_ORDER','aria-current','aria-expanded','data-open','createElementNS','cycleThemeMode','applyThemeMode','watchSystemTheme','readStoredMode','app-nav__toggle','theme-toggle'])if(!s.includes(t))throw new Error('missing: '+t);for(const lit of ['Overview','Activities','Calendar','Records','Trends'])if(s.includes(\"'\"+lit+\"'\"))throw new Error('nav label hardcoded instead of read from NAV_ORDER: '+lit);if(/https?:\/\/(?!www\.w3\.org)/.test(s))throw new Error('external URL in nav.ts');console.log('nav.ts contract OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/nav.ts` exports `createNav` returning an object with `setActiveRoute` and `destroy`
    - Nav link labels are read from `NAV_ORDER`; none of `Overview`, `Activities`, `Calendar`, `Records`, `Trends` appears as a string literal in `nav.ts`
    - `nav.ts` contains no `innerHTML` assignment and no direct `localStorage.` access
    - The hamburger button carries `aria-label`, `aria-expanded`, and `aria-controls`, and toggles `data-open` on the nav element
    - The theme toggle calls `cycleThemeMode`, `applyThemeMode`, and `readStoredMode` from `theme.ts`, and registers `watchSystemTheme` with its unsubscribe wired into `destroy()`
    - Icons are constructed via `document.createElementNS` with the SVG namespace; no icon package is imported and `package.json` dependencies are unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The top nav renders the five UI-SPEC entries from the single registry source, marks the active route accessibly, collapses behind a 44px hamburger on mobile, and cycles light/dark/auto through the theme module.</done>
</task>

<task type="auto">
  <name>Task 3: Build the shared stub panel and the three coming-soon views</name>
  <files>src/dashboard/views/stub-view.ts, src/dashboard/views/calendar.stub.ts, src/dashboard/views/records.stub.ts, src/dashboard/views/trends.stub.ts</files>
  <read_first>
    - .planning/phases/16-dashboard-shell-data-contract/16-UI-SPEC.md (Copywriting Contract — the empty-state heading and body strings are verbatim requirements, and "Stub views" under Phase-Specific Notes fixes the panel treatment)
    - src/dashboard/view.types.ts (plan 03 — `DashboardView`, `ViewMountContext`, `ROUTES`, `STUB_PHASE`)
    - src/dashboard/styles.css (plan 02 — `.stub-panel`, `.text-heading`, `.text-body`; the panel must carry no accent color)
    - src/widgets/shared/widget-base.ts (lines 250-275 — the `showError` empty-state rendering shape these stubs loosely follow)
  </read_first>
  <action>
    Create `src/dashboard/views/stub-view.ts` exporting
    `export function createStubView(route: string, viewName: string): DashboardView`.

    Its `mount(ctx)` clears `ctx.container` (`replaceChildren()`), then appends a `<section class="stub-panel">` containing:
    - an `<h1 class="text-heading">` whose `textContent` is `` `${viewName} — coming soon` `` using the em dash U+2014, exactly as the UI-SPEC declares
    - a `<p class="text-body">` whose `textContent` is `` `This view lands in Phase ${STUB_PHASE[route]}. Browse Activities or check the Overview for now.` ``

    Set the returned view's `title` to `viewName` and its `route` to the passed route. Do NOT set `navEntry` here — nav membership is `NAV_ORDER`'s job (plan 03), and duplicating it would create two sources of truth.

    Use `textContent` for both strings; no `innerHTML`, no template-string HTML.

    Then create three one-line-plus-export modules, each default-free and named:
    - `src/dashboard/views/calendar.stub.ts` → `export const calendarView = createStubView(ROUTES.CALENDAR, 'Calendar')`
    - `src/dashboard/views/records.stub.ts` → `export const recordsView = createStubView(ROUTES.RECORDS, 'Records')`
    - `src/dashboard/views/trends.stub.ts` → `export const trendsView = createStubView(ROUTES.TRENDS, 'Trends')`

    Because `STUB_PHASE` maps calendar to `'17'` and records/trends to `'18'`, the body copy lands on the correct future phase without any per-file conditional.

    Remember the `.js` extension on every relative import (`module: Node16`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs');const s=fs.readFileSync('src/dashboard/views/stub-view.ts','utf8');if(s.includes('innerHTML'))throw new Error('stub-view must not use innerHTML');if(!s.includes('— coming soon'))throw new Error('heading copy must use the em dash form \"{View Name} — coming soon\"');if(!s.includes('This view lands in Phase '))throw new Error('body copy mismatch');if(!s.includes('Browse Activities or check the Overview for now.'))throw new Error('body copy tail mismatch');if(!s.includes('STUB_PHASE'))throw new Error('phase number must come from STUB_PHASE');if(s.includes('navEntry'))throw new Error('stubs must not declare navEntry; NAV_ORDER owns nav membership');if(s.includes('--accent')||s.includes('accent'))throw new Error('stub panels must not use accent color');for(const f of ['calendar','records','trends']){const v=fs.readFileSync('src/dashboard/views/'+f+'.stub.ts','utf8');if(!v.includes('createStubView'))throw new Error(f+' must use createStubView');}console.log('stub views OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/views/stub-view.ts` exports `createStubView` returning a `DashboardView`
    - The rendered heading text is exactly `{View Name} — coming soon` with the em dash character U+2014
    - The rendered body text is exactly `This view lands in Phase {17|18}. Browse Activities or check the Overview for now.`, with the phase number sourced from `STUB_PHASE`
    - `calendar.stub.ts` exports `calendarView`, `records.stub.ts` exports `recordsView`, `trends.stub.ts` exports `trendsView`, each built via `createStubView`
    - No stub file assigns `innerHTML`, declares `navEntry`, or references the accent color
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All three unbuilt routes render the exact UI-SPEC coming-soon panel naming the correct future phase, so no nav entry leads to a blank or broken view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → pre-paint DOM attribute | The inline bootstrap reads browser-writable storage and writes a document attribute before any module code runs |
| document → served scripts/styles | The published page defines what origins its own content may load from |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-SH-01 | Tampering | Inline theme bootstrap in `src/dashboard/index.html` | mitigate | The inline copy repeats `theme.ts`'s allow-list (`'light' \| 'dark' \| 'auto'`, else `'auto'`) inside a try/catch, so a poisoned `dashboard-theme` value can never be written verbatim into `setAttribute('data-theme', ...)`. Grep-asserted in the verify step so the guard cannot be trimmed away as "duplication" |
| T-16-SH-02 | Tampering | DOM construction in `nav.ts` and `stub-view.ts` | mitigate | Both files are `innerHTML`-free and build every node with `createElement`/`createElementNS` + `textContent`, establishing the pattern before plan 07 introduces athlete free text. The `route-browser` widget's `innerHTML` list-rendering line is the named anti-pattern NOT to copy (PATTERNS.md) |
| T-16-SH-03 | Tampering | Third-party script/style injection on the published page | mitigate | A `Content-Security-Policy` meta with `default-src 'self'` and `connect-src 'self'` is declared in the entry; combined with D-01's zero external dependencies, the page loads nothing off-origin. Defense-in-depth per RESEARCH.md Security Domain V14 |
| T-16-SH-04 | Information Disclosure | Nav exposing routes | accept | All routes are public read-only views of already-public data on a single-athlete site; there is no privileged route to hide (ASVS V4 not applicable) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 across the new nav and view modules
- `npm test` full suite green (these files add no tests — DOM mounting is manually verified per this repo's zero-DOM-test precedent, covered by plan 08's checkpoint)
- All three contract scripts (index.html ordering, nav.ts hygiene, stub copy) exit 0
</verification>

<success_criteria>
- The theme is applied before the stylesheet can paint, with the allow-list guard preserved in the inline copy
- Nav renders exactly the five UI-SPEC entries from `NAV_ORDER`, marks the active one with `aria-current`, and collapses behind an accessible 44px hamburger below 640px
- The theme toggle cycles light → dark → auto through `theme.ts` and reacts to system changes while on auto
- Calendar, Records and Trends render the verbatim coming-soon copy with the correct phase number
- No file in this plan uses `innerHTML`, an external URL, or a new npm dependency
</success_criteria>

<output>
Create `.planning/phases/16-dashboard-shell-data-contract/16-06-SUMMARY.md` when done
</output>
