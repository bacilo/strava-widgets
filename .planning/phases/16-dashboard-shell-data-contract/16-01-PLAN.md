---
phase: 16-dashboard-shell-data-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/best-effort-exclusions.json
  - src/analytics/best-effort.types.ts
  - src/analytics/best-effort-exclusions.ts
  - src/analytics/best-effort-exclusions.test.ts
  - src/analytics/compute-best-efforts.ts
  - src/analytics/compute-best-efforts.test.ts
autonomous: true
requirements: [DASH-02]

must_haves:
  truths:
    - "Activities listed in data/best-effort-exclusions.json never appear in any per-distance PR ranking"
    - "Excluded activities still have their efforts computed and present in best-efforts.json, flagged rather than deleted"
    - "The next-best genuine effort is promoted to rank 1 when the previous rank-1 holder is excluded"
    - "A missing or malformed exclusions file leaves the pipeline running with zero exclusions instead of crashing"
  artifacts:
    - path: "data/best-effort-exclusions.json"
      provides: "Committed, user-maintained exclusion list"
      contains: "3475726256"
    - path: "src/analytics/best-effort-exclusions.ts"
      provides: "Exclusion loader + pure matcher"
      exports: ["loadExclusions", "buildExclusionIndex", "isExcluded"]
    - path: "src/analytics/best-effort-exclusions.test.ts"
      provides: "Unit coverage for loader tolerance and matcher semantics"
  key_links:
    - from: "src/analytics/compute-best-efforts.ts"
      to: "src/analytics/best-effort-exclusions.ts"
      via: "loadExclusions + isExcluded gate before the byDistance accumulator push"
      pattern: "isExcluded\\("
    - from: "src/analytics/best-effort.types.ts"
      to: "BestEffort / ActivityBestEfforts"
      via: "excludedFromRecords flags consumed by the dashboard index generator (plan 04)"
      pattern: "excludedFromRecords"
---

<objective>
Give the best-effort engine a user-maintained exclusion list so GPS-untrustworthy activities stop holding personal records, while keeping their computed efforts visible and reconcilable.

Purpose: Closes the folded todo `2026-08-10-manual-exclusion-of-activities-from-best-efforts.md`. The dashboard data contract (plan 04) surfaces an `excludedFromRecords` badge per D-09, so the flag must exist in `best-efforts.json` before the index generator reads it.
Output: `data/best-effort-exclusions.json`, an exclusion loader/matcher module with tests, and `computeBestEfforts` wired to honour it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dashboard-shell-data-contract/16-CONTEXT.md
@.planning/todos/pending/2026-08-10-manual-exclusion-of-activities-from-best-efforts.md

<interfaces>
<!-- Contracts the executor works against. Extracted from the codebase — no exploration needed. -->

From src/analytics/best-effort.types.ts (current, pre-change):
- `export const BEST_EFFORTS_SCHEMA_VERSION = 1`
- `export type TargetDistanceKey = '400m' | '1k' | '1mi' | '5k' | '10k' | 'half' | 'marathon'`
- `export const TARGET_ORDER: readonly TargetDistanceKey[]`
- `export interface ComputedEffort { distance; durationSec; paceSecPerKm; startOffsetSec; endOffsetSec; lowConfidence }`
- `export interface BestEffort extends ComputedEffort { wasPRAtTheTime: boolean }`
- `export interface ActivityBestEfforts { activityId; startDate; distanceSource; efforts: BestEffort[] }`
- `export interface PRRankingEntry { rank; activityId; startDate; durationSec; paceSecPerKm; lowConfidence }`
- `export interface BestEffortsDocument { schemaVersion; generatedAt; note; totals {...}; rankings; rejected; activities }`

From src/storage/file-store.ts:
- `class FileStore { constructor(baseDir: string); writeJson(path, data): Promise<void>; readJson<T>(path): Promise<T> }`
- `readJson` throws `File not found: <fullPath>` on ENOENT.

From src/analytics/best-effort-utils.ts (NO changes needed in this plan):
- `markPRs<T extends {startDate, durationSec}>(effortsForDistance: T[]): (T & {wasPRAtTheTime: boolean})[]`
- `rankTopN(effortsForDistance, n = TOP_N): PRRankingEntry[]`
Both operate only on what the caller pushes into them, so gating the accumulator push is sufficient — promotion of the next-best effort is automatic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the exclusion contract and seed the committed exclusion list</name>
  <files>src/analytics/best-effort.types.ts, data/best-effort-exclusions.json</files>
  <read_first>
    - src/analytics/best-effort.types.ts (full file — you are extending existing interfaces; match its doc-comment style and the "invariants" framing at the top)
    - .planning/todos/pending/2026-08-10-manual-exclusion-of-activities-from-best-efforts.md (source of the two activity IDs and their reason)
  </read_first>
  <action>
    Extend `src/analytics/best-effort.types.ts` with the exclusion contract, keeping `BEST_EFFORTS_SCHEMA_VERSION` at `1` (additive optional-to-consumers change; no recomputation coordination needed beyond the re-run in task 3):

    Add `export const BEST_EFFORT_EXCLUSIONS_SCHEMA_VERSION = 1`.

    Add `export interface BestEffortExclusion { activityId: string; distances: TargetDistanceKey[] | null; reason: string }` — `distances: null` means every target distance is excluded for that activity; a non-empty array narrows the exclusion to those distances only.

    Add `export interface BestEffortExclusionsFile { schemaVersion: 1; note: string; exclusions: BestEffortExclusion[] }`.

    Add a required `excludedFromRecords: boolean` field to `BestEffort` (NOT to `ComputedEffort` — exclusion is a records-pass concept, not a sweep concept, same layering as `wasPRAtTheTime`).

    Add a required `excludedFromRecords: boolean` field to `ActivityBestEfforts`, documented as "true when this activity matched at least one entry in the exclusion list".

    Add `effortsExcluded: number` to the `BestEffortsDocument['totals']` object, documented as "efforts computed and retained but withheld from PR marking and ranking".

    Then create `data/best-effort-exclusions.json` — a COMMITTED file (data/ is not gitignored except data/stats/ and data/tokens.json, so no .gitignore change is needed). Content: `schemaVersion` 1; a `note` explaining the file is hand-maintained by the developer and read by `node dist/index.js compute-best-efforts`; and two `exclusions` entries with `distances: null`:
    - activityId `"3475726256"`, reason naming the inaccurate GPS device and that its 400m time is not trusted as a genuine PR
    - activityId `"3475725513"`, reason naming the same device class and its 1k time

    Format the JSON with 2-space indentation to match `FileStore.writeJson`'s `JSON.stringify(data, null, 2)` output convention.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "excludedFromRecords\|effortsExcluded" ; node -e "const f=require('./data/best-effort-exclusions.json'); if(f.schemaVersion!==1) throw new Error('schemaVersion'); const ids=f.exclusions.map(e=>e.activityId); if(!ids.includes('3475726256')||!ids.includes('3475725513')) throw new Error('missing seeded ids'); for(const e of f.exclusions){ if(typeof e.reason!=='string'||e.reason.length<10) throw new Error('reason too short: '+e.activityId); if(e.distances!==null&&!Array.isArray(e.distances)) throw new Error('bad distances'); } console.log('exclusions file OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/analytics/best-effort.types.ts` contains `export const BEST_EFFORT_EXCLUSIONS_SCHEMA_VERSION = 1`
    - `src/analytics/best-effort.types.ts` contains `export interface BestEffortExclusion` with an `activityId: string`, a `distances: TargetDistanceKey[] | null`, and a `reason: string` member
    - `src/analytics/best-effort.types.ts` contains `export interface BestEffortExclusionsFile`
    - `BestEffort` declares `excludedFromRecords: boolean`; `ComputedEffort` does NOT
    - `ActivityBestEfforts` declares `excludedFromRecords: boolean`
    - `BestEffortsDocument['totals']` declares `effortsExcluded: number`
    - `BEST_EFFORTS_SCHEMA_VERSION` is still `1`
    - `node -e "require('./data/best-effort-exclusions.json')"` exits 0 and the file lists both `3475726256` and `3475725513` with `distances: null` and a non-empty `reason`
    - `git check-ignore data/best-effort-exclusions.json` exits non-zero (file is NOT ignored — it must be committable)
  </acceptance_criteria>
  <done>The exclusion types compile (modulo the not-yet-populated fields in compute-best-efforts.ts, fixed in task 2) and the seeded exclusion list parses with both known-bad activity IDs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the exclusion loader/matcher and gate the PR accumulator on it</name>
  <files>src/analytics/best-effort-exclusions.ts, src/analytics/best-effort-exclusions.test.ts, src/analytics/compute-best-efforts.ts, src/analytics/compute-best-efforts.test.ts</files>
  <read_first>
    - src/analytics/compute-best-efforts.ts (full file — especially `ComputeBestEffortsOptions` at ~line 121, the defaulted-options block at ~line 147, the per-activity loop at ~lines 174-232 where efforts are pushed into `byDistance`, the rankings loop at ~lines 236-252, and the `doc` assembly + console summary at ~lines 269-320)
    - src/analytics/best-effort.types.ts (as extended in task 1)
    - src/analytics/compute-best-efforts.test.ts (lines 1-45 — fixture-builder style, temp-dir setup, `describe`/`it` phrasing to match)
    - src/storage/file-store.ts (lines 1-70 — `readJson` ENOENT error message shape you must tolerate)
    - src/analytics/best-effort-utils.ts (lines 165-200 — confirm `markPRs`/`rankTopN` need no change)
  </read_first>
  <behavior>
    `best-effort-exclusions.test.ts`:
    - `buildExclusionIndex` with `{activityId: 'a1', distances: null}` → `isExcluded(index, 'a1', '400m')` is true and `isExcluded(index, 'a1', 'marathon')` is true
    - `buildExclusionIndex` with `{activityId: 'a2', distances: ['1k']}` → `isExcluded(index, 'a2', '1k')` true, `isExcluded(index, 'a2', '5k')` false
    - An activity absent from the list → `isExcluded` false for every `TARGET_ORDER` key
    - `null` distances wins over a narrower duplicate entry for the same activityId (union semantics, all-distances absorbs)
    - Unknown distance strings in the file are dropped, and the surviving known distances in the same entry still apply
    - `loadExclusions` on a nonexistent path resolves to an empty index (zero exclusions), does not throw
    - `loadExclusions` on a file whose JSON parses but has no `exclusions` array resolves to an empty index, does not throw
    - `loadExclusions` on a file with a non-object entry, a missing `activityId`, or a non-string `activityId` skips that entry and keeps the valid siblings

    `compute-best-efforts.test.ts` (added cases, using the existing temp-dir fixture harness):
    - With two synthetic activities where the faster one is excluded (`distances: null`), `rankings['1k'][0].activityId` is the slower, non-excluded activity
    - The excluded activity is still present in `doc.activities`, its `efforts` array is non-empty, every one of its efforts has `excludedFromRecords: true` and `wasPRAtTheTime: false`, and `doc.activities[excludedId].excludedFromRecords` is true
    - `doc.totals.effortsExcluded` equals the number of efforts on the excluded activity, and `doc.totals.effortsComputed` still counts them (exclusion withholds from ranking, it does not un-compute)
    - A per-distance exclusion (`distances: ['1k']`) removes that activity from `rankings['1k']` but leaves it ranked in `rankings['5k']`
  </behavior>
  <action>
    Create `src/analytics/best-effort-exclusions.ts` exporting three things:

    `export type ExclusionIndex = Map<string, Set<TargetDistanceKey> | 'all'>` — keyed by activity id as a string.

    `export function buildExclusionIndex(entries: unknown): ExclusionIndex` — pure, total, never throws. Accepts the raw parsed `exclusions` value. Skips any entry that is not a non-null object or whose `activityId` is not a non-empty string. Coerces `activityId` with `String(...)`. `distances === null` or `undefined` maps to `'all'`. An array maps to a `Set` filtered to members of `TARGET_ORDER` (drop unknown strings); an entry whose filtered set is empty is skipped. When the same `activityId` appears twice, `'all'` absorbs everything and two sets union.

    `export function isExcluded(index: ExclusionIndex, activityId: string, distance: TargetDistanceKey): boolean` — pure; `'all'` returns true for every distance.

    `export async function loadExclusions(fileStore: FileStore, exclusionsPath: string): Promise<ExclusionIndex>` — reads the JSON via `fileStore.readJson<BestEffortExclusionsFile>`, passes `file.exclusions` to `buildExclusionIndex`, and returns an EMPTY index on any read/parse failure after a single `console.warn` naming the path and the reason (threat T-16-EX-01: a hand-edited or absent exclusions file must never abort a 1,867-activity pipeline run). Log a one-line summary of how many activity ids were loaded on success.

    Then wire it into `src/analytics/compute-best-efforts.ts`:
    1. Add `exclusionsPath?: string` to `ComputeBestEffortsOptions`, defaulted in the function body to `'data/best-effort-exclusions.json'` alongside the existing `activitiesDir`/`streamsDir`/`streamsManifestPath`/`statsDir` defaults.
    2. After `const manifest = await loadManifest(...)`, call `const exclusions = await loadExclusions(fileStore, exclusionsPath)`.
    3. Add an `effortsExcluded` counter next to the existing `effortsRejected` / `lowConfidenceEfforts` counters.
    4. In the per-activity loop, inside `for (const effort of result.efforts)`, compute `const isEffortExcluded = isExcluded(exclusions, id, effort.distance)`. When true, increment `effortsExcluded` and SKIP the `byDistance.get(effort.distance)!.push(...)` call. Leave the existing `lowConfidenceEfforts` increment unconditional so low-confidence totals stay reconcilable with `effortsComputed`.
    5. When assembling `activities[id]`, map efforts to `{ ...e, wasPRAtTheTime: false, excludedFromRecords: isExcluded(exclusions, id, e.distance) }`, and set `excludedFromRecords: exclusions.has(id)` on the `ActivityBestEfforts` record itself.
    6. The rankings loop needs no change: `markPRs` and `rankTopN` both consume only what was pushed into `byDistance`, so an excluded activity is invisible to both and the next-best effort is promoted automatically. Do NOT modify `src/analytics/best-effort-utils.ts`.
    7. Add `effortsExcluded` to the `doc.totals` object literal and a `- Efforts excluded (records): ${doc.totals.effortsExcluded}` line to the console summary, positioned after the low-confidence line.
  </action>
  <verify>
    <automated>npx vitest run src/analytics/best-effort-exclusions.test.ts src/analytics/compute-best-efforts.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/analytics/best-effort-exclusions.ts` exports `buildExclusionIndex`, `isExcluded`, and `loadExclusions`
    - `npx vitest run src/analytics/best-effort-exclusions.test.ts src/analytics/compute-best-efforts.test.ts --reporter=dot` exits 0
    - `src/analytics/best-effort-utils.ts` is byte-identical to its pre-plan state (`git diff --exit-code src/analytics/best-effort-utils.ts` exits 0)
    - `grep -c "isExcluded(" src/analytics/compute-best-efforts.ts` returns at least 2
    - `grep -n "effortsExcluded" src/analytics/compute-best-efforts.ts` matches the counter declaration, the totals literal, and the console summary line
    - `npx tsc --noEmit` exits 0
    - `loadExclusions(new FileStore('.'), 'data/does-not-exist.json')` resolves rather than rejecting (covered by the loader test)
  </acceptance_criteria>
  <done>Exclusions are honoured end to end in the pure test harness: excluded activities keep their computed efforts, carry `excludedFromRecords: true`, never appear in a ranking, and the next-best effort holds rank 1.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the real archive computation and confirm the PR promotions</name>
  <files>data/stats/best-efforts.json</files>
  <read_first>
    - src/analytics/compute-best-efforts.ts (as modified in task 2 — confirm the exclusionsPath default before running)
    - src/index.ts (lines 185-204, `computeBestEffortsCommand` — this is the entry point you invoke; it already passes no `exclusionsPath`, so the default must be correct)
  </read_first>
  <action>
    Run `npm run build && node dist/index.js compute-best-efforts` against the real committed archive (1,867 manifest entries, 1,843 with streams).

    Confirm the console summary reports a non-zero `Efforts excluded (records)` count and that `Activities considered`, `Efforts computed`, and `Low-confidence efforts` are unchanged from the pre-exclusion run (exclusion withholds from ranking; it must not change what was computed).

    Then assert the two specific PR promotions the folded todo asks for. Pre-change state, read from the current `data/stats/best-efforts.json`:
    - `rankings['400m'][0]` was activityId `3475726256`, durationSec `44`; the next entry was `4556693525`, durationSec `45.2`
    - `rankings['1k'][0]` was activityId `3475725513`, durationSec `148.9`; the next entry was `4598855187`, durationSec `161.3`

    After the run, `rankings['400m'][0].activityId` must be `4556693525` and `rankings['1k'][0].activityId` must be `4598855187`, and neither excluded id may appear at any rank in any of the seven distances.

    `data/stats/` is gitignored, so this task commits no data file — it is a verification run. If either promotion does not hold, the wiring in task 2 is wrong; fix it there rather than editing the output.
  </action>
  <verify>
    <automated>npm run build && node dist/index.js compute-best-efforts && node -e "const b=require('./data/stats/best-efforts.json'); const ex=['3475726256','3475725513']; if(b.rankings['400m'][0].activityId!=='4556693525') throw new Error('400m rank1 = '+b.rankings['400m'][0].activityId); if(b.rankings['1k'][0].activityId!=='4598855187') throw new Error('1k rank1 = '+b.rankings['1k'][0].activityId); for(const k of Object.keys(b.rankings)) for(const r of b.rankings[k]) if(ex.includes(r.activityId)) throw new Error('excluded id ranked in '+k); if(!(b.totals.effortsExcluded>0)) throw new Error('effortsExcluded is 0'); for(const id of ex){ const a=b.activities[id]; if(!a) throw new Error('excluded activity dropped from doc: '+id); if(a.excludedFromRecords!==true) throw new Error('flag missing: '+id); if(a.efforts.length===0) throw new Error('efforts un-computed: '+id); if(a.efforts.some(e=>e.wasPRAtTheTime)) throw new Error('excluded effort still marked PR: '+id);} console.log('archive run OK, effortsExcluded='+b.totals.effortsExcluded);"</automated>
  </verify>
  <acceptance_criteria>
    - `node dist/index.js compute-best-efforts` exits 0 against the real archive
    - `data/stats/best-efforts.json` has `rankings['400m'][0].activityId === '4556693525'`
    - `data/stats/best-efforts.json` has `rankings['1k'][0].activityId === '4598855187'`
    - Neither `3475726256` nor `3475725513` appears in any entry of any `rankings[*]` array
    - Both excluded activities are still present in `activities`, each with `excludedFromRecords: true`, a non-empty `efforts` array, and no effort marked `wasPRAtTheTime: true`
    - `totals.effortsExcluded > 0`
    - `git status --porcelain data/stats` produces no output (data/stats is gitignored — nothing to commit from this task)
  </acceptance_criteria>
  <done>The real archive recomputation promotes the next-best genuine efforts at 400m and 1k, and the excluded activities remain in the document flagged rather than deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo file → compute pipeline | `data/best-effort-exclusions.json` is hand-edited by the developer and read unvalidated by a CI-run batch job |
| compute output → published site | `best-efforts.json` fields flow into the dashboard index (plan 04) and onto a public GitHub Pages site |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-EX-01 | Denial of Service | `loadExclusions` in `src/analytics/best-effort-exclusions.ts` | mitigate | Missing, unparseable, or structurally wrong exclusions file resolves to an empty index after one `console.warn`; never throws, so a hand-edit typo cannot abort the nightly 1,867-activity pipeline |
| T-16-EX-02 | Tampering | `buildExclusionIndex` entry parsing | mitigate | Total function: non-object entries, missing/non-string `activityId`, and unknown distance keys are dropped individually; a single bad row cannot poison the whole index or inject an unexpected `TargetDistanceKey` |
| T-16-EX-03 | Information Disclosure | `reason` strings in the committed exclusions file | accept | Reasons describe GPS device quality on the developer's own activities; the file is committed to a public repo but carries no third-party or credential data — same trust level as `data/activities/*.json` already committed |
| T-16-EX-04 | Repudiation | Silent PR-ranking changes | mitigate | Excluded efforts are retained in `activities[id].efforts` with `excludedFromRecords: true` and counted in `totals.effortsExcluded`, so a ranking change is always traceable to a named exclusion instead of an unexplained gap |
</threat_model>

<verification>
- `npx vitest run src/analytics --reporter=dot` passes (exclusion unit tests plus the existing best-effort suite)
- `npx tsc --noEmit` exits 0
- `npm test` (full suite) passes — the exclusion change touches a module other analytics tests import
- Real-archive run promotes 400m rank 1 to `4556693525` and 1k rank 1 to `4598855187`
</verification>

<success_criteria>
- `data/best-effort-exclusions.json` exists, is committed (not gitignored), and lists both known-bad device activities with reasons
- `BestEffort.excludedFromRecords`, `ActivityBestEfforts.excludedFromRecords`, and `BestEffortsDocument.totals.effortsExcluded` exist in `best-effort.types.ts`
- `computeBestEfforts` withholds excluded efforts from `markPRs`/`rankTopN` without modifying `best-effort-utils.ts`
- Excluded activities remain in `best-efforts.json` flagged, with `wasPRAtTheTime: false` on every effort
- A missing/malformed exclusions file degrades to zero exclusions with a warning
</success_criteria>

<output>
Create `.planning/phases/16-dashboard-shell-data-contract/16-01-SUMMARY.md` when done
</output>
