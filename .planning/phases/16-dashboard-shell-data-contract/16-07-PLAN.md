---
phase: 16-dashboard-shell-data-contract
plan: 07
type: execute
wave: 3
depends_on: [16-05, 16-06]
files_modified:
  - src/dashboard/views/overview.ts
  - src/dashboard/views/list.ts
  - src/dashboard/views/detail.ts
  - src/dashboard/view-registry.ts
  - src/dashboard/view-registry.test.ts
  - src/dashboard/main.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "D-06: The landing view shows real archive headline stats and real recent activities from the index manifest"
    - "The activities list renders real index rows, each with a View Activity action linking to that activity's detail route"
    - "Opening an activity lazy-fetches only that activity's detail and stream files and renders its stats header"
    - "A failed detail load shows the exact error copy with a Retry action that issues a real new request"
    - "The registry exposes exactly the six declared routes, with the detail route absent from the nav"
  artifacts:
    - path: "src/dashboard/view-registry.ts"
      provides: "The single enumeration point mapping routes to view modules (D-03)"
      exports: ["VIEWS", "getView"]
    - path: "src/dashboard/view-registry.test.ts"
      provides: "Registry shape, route coverage, and nav-exclusion coverage"
    - path: "src/dashboard/main.ts"
      provides: "Bootstrap: theme, nav, router, index prefetch, view mount/unmount"
    - path: "src/dashboard/views/detail.ts"
      provides: "The D-07 proving slice — lazy fetch, loading, error, retry"
  key_links:
    - from: "src/dashboard/views/list.ts"
      to: "src/dashboard/data/index-client.ts"
      via: "rows read from the already-loaded in-memory index, never re-fetched"
      pattern: "getRows|loadIndex"
    - from: "src/dashboard/views/detail.ts"
      to: "src/dashboard/data/detail-client.ts"
      via: "loadDetail called only from mount, only after id validation"
      pattern: "loadDetail"
    - from: "src/dashboard/main.ts"
      to: "src/dashboard/router.ts"
      via: "createRouter start() dispatching matched routes into the registry"
      pattern: "createRouter"
---

<objective>
Close the D-07 proving slice: render real activities from the index manifest, and make clicking one lazy-fetch and render that activity's committed detail data end to end.

Purpose: This is the task that de-risks Phase 17. If the index contract, the lazy fetch contract, and the routing seam hold together for one real activity, Phase 17 can build browse and detail views on top without renegotiating any of them.
Output: three real views, the view registry with tests, and the bootstrap that wires theme, nav, router and data together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dashboard-shell-data-contract/16-CONTEXT.md
@.planning/phases/16-dashboard-shell-data-contract/16-UI-SPEC.md
@.planning/phases/16-dashboard-shell-data-contract/16-05-SUMMARY.md
@.planning/phases/16-dashboard-shell-data-contract/16-06-SUMMARY.md

<interfaces>
<!-- Everything these views consume. -->

Plan 03 — `src/dashboard/view.types.js`: `DashboardView`, `ViewMountContext { container, routeParams, query }`, `ROUTES`, `NAV_ORDER`, `STUB_PHASE`.
Plan 03 — `src/dashboard/router.js`: `parseHash`, `matchRoute`, `resolveHash`, `isValidActivityId`, `createRouter({routes,onMatch,onNoMatch})`, `navigateTo`.
Plan 02 — `src/dashboard/theme.js`: `applyThemeMode`, `readStoredMode`.
Plan 06 — `src/dashboard/nav.js`: `createNav(root) -> { setActiveRoute, destroy }`.
Plan 06 — `src/dashboard/views/{calendar,records,trends}.stub.js`: `calendarView`, `recordsView`, `trendsView`.
Plan 05 — `src/dashboard/data/index-client.js`: `createIndexClient(opts) -> { loadIndex, getRows, getRow, reset }`.
Plan 05 — `src/dashboard/data/detail-client.js`: `createDetailClient(opts) -> { loadDetail, clear }`, `InvalidActivityIdError`, `ActivityDetail { id, activity, stream }`.
Plan 03 — `src/analytics/dashboard-index.types.js`: `DashboardIndexRow { id, startDate, startDateLocal, name, distanceM, movingTimeSec, paceSecPerKm, elevationGainM, avgHr, maxHr, avgCadenceRpm, location, sportType, streams, lowConfidence, excludedFromRecords, prCount }`.

Overview data sources (already published today by `copyDataFiles()` — no new pipeline work, D-06):
- `data/stats/all-time-totals.json` → live shape `{ totalKm: 22390.26, totalRuns: 1866, totalHours: 2135.98, totalElevation: 124325.3, avgPaceMinPerKm: 5.72, firstActivityDate, lastActivityDate, generatedAt }`
- `data/stats/streaks.json` → live shape `{ currentStreak: 0, longestStreak: 31, withinCurrentStreak: false, currentStreakStart, longestStreakStart, longestStreakEnd, weeklyConsistency: {...} }`
Do NOT fetch `data/stats/best-efforts.json` from the browser — it is 2.5MB. "Recent PRs" comes from index rows with `prCount > 0` (the generator already resolved PR counts at build time, plan 04).

Existing helper — `src/analytics/date-utils.ts` is UTC-safe and already used by `compute-stats.ts`; prefer it over hand-rolled date math (RESEARCH.md Don't Hand-Roll).

Styling hooks available from plan 02's `styles.css` (use these, add none): `.view`, `.card`, `.stat-grid`, `.text-display`, `.text-heading`, `.text-label`, `.text-body`, `.activity-list`, `.activity-row`, `.activity-row__name`, `.activity-row__meta`, `.badge`, `.cta`, `.loading-indicator`, `.error-state`.

UI-SPEC copy (verbatim):
- Primary CTA text: `View Activity`
- Error heading: `Couldn't load this activity` (U+2019 apostrophe)
- Error body: `Check your connection and try again.`
- Error action: `Retry`

Repo constraints: `module: Node16` (`.js` extensions on relative imports); vitest `environment: 'node'` with no jsdom, so `view-registry.test.ts` may only assert on registry DATA, never call a `mount()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the overview and activities-list views on the index manifest</name>
  <files>src/dashboard/views/overview.ts, src/dashboard/views/list.ts</files>
  <read_first>
    - src/widgets/route-browser/index.ts (lines 118-150 — the list-item construction pattern to keep: create element, set class, build a meta line, attach a click handler. Line ~139 sets `item.innerHTML` with an unescaped `route.name`: that specific line is the named anti-pattern and MUST NOT be copied, per PATTERNS.md and RESEARCH.md Pitfall 5)
    - src/dashboard/data/index-client.ts (plan 05 — `loadIndex`/`getRows`/`getRow`; the index is loaded once by the bootstrap, so views read from it rather than fetching)
    - src/analytics/dashboard-index.types.ts (plan 03 — every row field these views render)
    - src/dashboard/styles.css (plan 02 — the class hooks listed in `<interfaces>`; do not introduce new class names)
    - src/analytics/date-utils.ts (the UTC-safe formatting helpers to reuse instead of hand-rolled date math)
    - .planning/phases/16-dashboard-shell-data-contract/16-UI-SPEC.md ("Proving slice rendering (D-07)" and the Typography usage list — Display for headline stat numbers, Heading for card headers, Label for dates/meta/badges, Body for row text)
  </read_first>
  <action>
    Create `src/dashboard/views/overview.ts` exporting `export function createOverviewView(deps: { indexClient: IndexClient; fetchImpl?: FetchLike }): DashboardView` with `route: ROUTES.OVERVIEW` and `title: 'Overview'`.

    Its `mount(ctx)` clears the container with `replaceChildren()`, renders a `.loading-indicator` element, then in parallel awaits `indexClient.loadIndex()` and two small stats fetches (`data/stats/all-time-totals.json`, `data/stats/streaks.json`) using the same non-ok error shape as the data clients. Both stats fetches are individually try/catch-guarded: a missing stats file renders that card with an em dash placeholder rather than failing the whole view (those files are gitignored and regenerated, so a fresh clone before a compute run legitimately lacks them).

    Render, inside `.view`:
    - A `Headline Stats` `<section class="card">` with an `<h2 class="text-heading">` header and a `.stat-grid` of stat cards. Each card is a `.text-display` number over a `.text-label` caption. Cards: total distance (`totalKm`, one decimal, `km`), total runs (`totalRuns`), total hours (`totalHours`, rounded), total elevation (`totalElevation`, rounded, `m`), current streak (`streaks.currentStreak`, `days`), longest streak (`streaks.longestStreak`, `days`).
    - A `Recent PRs` `<section class="card">` listing the 5 most recent index rows with `prCount > 0`, each as an `.activity-row` showing name, local date, distance, and a `.badge` reading the PR count. If no rows qualify, render a `.text-body` line reading `No personal records in the archive yet.`
    - A `Recent Activities` `<section class="card">` listing the 10 newest index rows, using the shared row renderer described below.

    Create `src/dashboard/views/list.ts` exporting `export function createListView(deps: { indexClient: IndexClient }): DashboardView` with `route: ROUTES.LIST` and `title: 'Activities'`.

    Its `mount(ctx)` awaits `indexClient.loadIndex()` (a no-op returning the cached document if the bootstrap already loaded it — this is exactly the fetch-once property plan 05 guarantees), then renders an `<h1 class="text-heading">Activities</h1>`, a `.text-label` count line reading `` `${rows.length} activities` ``, and an `.activity-list` of rows.

    Scope guard: Phase 16 ships NO sorting, filtering, search, or pagination controls — those are BROWSE-01..06 in Phase 17. To keep the DOM honest at 1,867 rows without inventing pagination UI, render only the newest 100 rows and append a `.text-label` line reading `` `Showing the 100 most recent of ${rows.length} activities. Browsing, sorting and filtering land in Phase 17.` ``. Do not add page controls.

    Export a shared row renderer from `list.ts` — `export function renderActivityRow(row: DashboardIndexRow): HTMLElement` — and import it into `overview.ts` so both views produce identical rows. It builds an `.activity-row` containing:
    - `<div class="activity-row__name">` with `textContent = row.name` — `textContent`, never `innerHTML`. `name` is athlete free text (threat T-16-VW-01).
    - `<div class="activity-row__meta">` with a `textContent` line composed of the local date (via `date-utils.ts`), distance in km to one decimal, moving time as `h:mm:ss`, and pace as `m:ss/km` when `paceSecPerKm` is non-null (rendering `—` when null).
    - `.badge` elements, each built with `textContent`: `No streams` when `row.streams.available` is false (append the `reason` in parentheses), `No HR` when the streams are available but `streams.hr` is false, `Low confidence` when `row.lowConfidence`, `Excluded from records` when `row.excludedFromRecords`, and `` `${row.prCount} PR` `` when `prCount > 0`. Badges never use accent color (UI-SPEC reserved list).
    - An `<a class="cta">` whose `textContent` is exactly `View Activity` and whose `href` is `` `#/activity/${row.id}` ``. Use a real anchor so middle-click, copy-link and bookmarking work — this is the D-07 proving-slice trigger and the D-02 bookmarkable-route proof in one element.

    Both views must implement `unmount()` clearing the container, and must never call `createDetailClient` or fetch a per-activity file — that is the lazy boundary this plan is proving.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs');for(const f of ['src/dashboard/views/overview.ts','src/dashboard/views/list.ts']){const s=fs.readFileSync(f,'utf8');if(/innerHTML/.test(s))throw new Error(f+' uses innerHTML');if(/best-efforts\.json/.test(s))throw new Error(f+' must not fetch the 2.5MB best-efforts file');if(/data\/(activities|streams)\//.test(s))throw new Error(f+' must not fetch per-activity detail files (lazy boundary)');}const l=fs.readFileSync('src/dashboard/views/list.ts','utf8');if(!l.includes('View Activity'))throw new Error('missing CTA copy');if(!l.includes('#/activity/'))throw new Error('CTA must link to the detail route');if(!l.includes('renderActivityRow'))throw new Error('shared row renderer not exported');if(!/textContent\s*=\s*row\.name/.test(l))throw new Error('row name must be set via textContent');const o=fs.readFileSync('src/dashboard/views/overview.ts','utf8');if(!o.includes('renderActivityRow'))throw new Error('overview must reuse renderActivityRow');for(const t of ['all-time-totals.json','streaks.json'])if(!o.includes(t))throw new Error('overview missing stats source '+t);console.log('views contract OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/views/overview.ts` exports `createOverviewView`; `src/dashboard/views/list.ts` exports `createListView` and `renderActivityRow`
    - Neither file contains an `innerHTML` assignment; `row.name` is written with `textContent`
    - Neither file references `data/activities/` or `data/streams/` (the lazy boundary holds — only `detail.ts` touches those)
    - Neither file fetches `data/stats/best-efforts.json`; recent PRs derive from `row.prCount`
    - `overview.ts` fetches `data/stats/all-time-totals.json` and `data/stats/streaks.json`, each individually try/catch-guarded
    - `overview.ts` imports and reuses `renderActivityRow` from `list.ts` (one row rendering, two views)
    - The CTA element's text is exactly `View Activity` and its `href` is `#/activity/${row.id}`
    - `list.ts` renders at most 100 rows and states the truncation, with no sort/filter/search/pagination control
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Landing and list views render real archive data from the single in-memory index, with every athlete-authored string routed through `textContent` and no per-activity fetch triggered.</done>
</task>

<task type="auto">
  <name>Task 2: Build the detail view — the lazy-fetch proving slice with loading, error and retry</name>
  <files>src/dashboard/views/detail.ts</files>
  <read_first>
    - src/widgets/shared/widget-base.ts (lines 286-311 — `fetchDataAndRender`'s try/catch/showError shape to mirror; the deliberate deviation is that the dashboard adds a Retry action the widget lacks, per PATTERNS.md)
    - src/dashboard/data/detail-client.ts (plan 05 — `loadDetail`, `InvalidActivityIdError`, `ActivityDetail`; note failed loads are evicted from the cache so Retry genuinely re-fetches)
    - src/dashboard/router.ts (plan 03 — `isValidActivityId`; the view must not re-implement it)
    - src/streams/stream.types.ts (`CanonicalStream` — `t`, `d`, optional `hr`, `cadence`, `elevation` channels; ~76KB per file)
    - src/types/strava.types.ts (`StravaActivity` — the fields the stats header displays)
    - .planning/phases/16-dashboard-shell-data-contract/16-UI-SPEC.md (Copywriting Contract error row, and "Proving slice rendering (D-07)": accent loading indicator while pending, stats-header numbers minimum on success, full charts are Phase 17 scope)
  </read_first>
  <action>
    Create `src/dashboard/views/detail.ts` exporting `export function createDetailView(deps: { detailClient: DetailClient; indexClient: IndexClient }): DashboardView` with `route: ROUTES.DETAIL` and `title: 'Activity'`. It has no `navEntry` — the detail route is drill-in only (D-05).

    `mount(ctx)` flow:
    1. Read `const id = ctx.routeParams.id ?? ''`.
    2. If `!isValidActivityId(id)`, render the error state immediately with NO fetch and NO Retry button (retrying a malformed URL cannot succeed); instead offer a `.cta` anchor back to `#/list` labelled `Browse Activities`. Never write the raw `id` into the DOM — not even via `textContent` — because echoing an unvalidated param is the exact habit RESEARCH.md Pitfall 5 warns re-opens a DOM-XSS path in later phases. Log the offending id to `console.warn` only.
    3. Otherwise clear the container, render a `.loading-indicator` element with `textContent = 'Loading activity…'` and `role="status"`, and call `detailClient.loadDetail(id)`.
    4. Guard against a stale response: capture a monotonically increasing request token in a closure before awaiting, and discard the result if the view was unmounted or a newer mount started while the ~76KB stream was in flight. Without this, fast back-and-forth navigation between two activities can paint the wrong one.
    5. On success, render the stats header: an `<h1 class="text-heading">` with `textContent = detail.activity.name` (free text — `textContent` only), a `.text-label` line with the local start date, and a `.stat-grid` of `.text-display` / `.text-label` pairs for distance (km, one decimal), moving time (`h:mm:ss`), average pace (`m:ss/km`), elevation gain (m), average HR, max HR, and average cadence (labelled `rpm (single-leg)` to match the index contract's raw-rpm semantics). Render an em dash for any null field.
    6. Also render a `.card` stream summary proving the lazy stream fetch landed: sample count (`stream.t.length`), which channels are present (HR / cadence / elevation, derived from the presence of those arrays), and the stream's `distanceSource`. When `stream` is `null`, render a `.text-body` line reading `No stream data for this activity.` plus, when the index row is available via `indexClient.getRow(id)`, the row's `streams.reason` in a `.badge`. This is the STREAM-03 degraded state rendering cleanly rather than breaking.
    7. Render a `.cta` anchor back to `#/list` labelled `Back to Activities`.
    8. On failure, render the UI-SPEC error state: a `.error-state` section with an `<h2 class="text-heading">` whose `textContent` is exactly `Couldn't load this activity`, a `<p class="text-body">` whose `textContent` is exactly `Check your connection and try again.`, and a `<button class="cta">` whose `textContent` is exactly `Retry` and whose click handler re-runs the same mount flow from step 3. Do not include the underlying error message in the DOM; `console.error` it.

    Implement `unmount()` to clear the container and invalidate the in-flight request token.

    Explicitly out of scope this phase: charts, route map, splits table, zone breakdown (DETAIL-02..05 are Phase 17). The stats header plus the stream summary is exactly the "minimum" the UI-SPEC sanctions for the proving slice. Do not import Chart.js or Leaflet here.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs');const s=fs.readFileSync('src/dashboard/views/detail.ts','utf8');if(/innerHTML/.test(s))throw new Error('detail.ts uses innerHTML');for(const t of [\"Couldn't load this activity\",'Check your connection and try again.','Retry','isValidActivityId','loadDetail'])if(!s.includes(t))throw new Error('missing: '+t);if(/chart\.js|leaflet/i.test(s))throw new Error('charts and maps are Phase 17 scope');if(!/textContent\s*=\s*(detail\.)?activity\.name|textContent\s*=\s*[a-zA-Z.]*activity\.name/.test(s))throw new Error('activity name must be rendered via textContent');const idEcho=/textContent\s*=\s*[^;]*\bid\b/.test(s.split('isValidActivityId')[0]||'');if(idEcho)throw new Error('raw route param must not be echoed into the DOM');console.log('detail.ts contract OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/views/detail.ts` exports `createDetailView` producing a `DashboardView` with `route === ROUTES.DETAIL` and no `navEntry`
    - `mount` calls `isValidActivityId` before `loadDetail`, and an invalid id produces zero calls to `loadDetail`
    - The error state renders the exact strings `Couldn't load this activity`, `Check your connection and try again.`, and `Retry`
    - The Retry button re-invokes the load path (not a page reload, not `location.reload`)
    - A null stream renders `No stream data for this activity.` instead of throwing
    - The raw route param is never written into the DOM; `activity.name` is written with `textContent`
    - The file contains no `innerHTML`, no Chart.js import, and no Leaflet import
    - A stale-response token guards against a superseded mount painting its result
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Opening an activity fetches exactly that activity's two committed files, renders its stats header and stream summary, degrades cleanly when no stream exists, and offers a working Retry on failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assemble the view registry and the bootstrap</name>
  <files>src/dashboard/view-registry.ts, src/dashboard/view-registry.test.ts, src/dashboard/main.ts</files>
  <read_first>
    - scripts/build-widgets.mjs (lines 14-81 — the `widgets` array is the structural model: one array, one object literal per unit, one place to add the next one. D-03 asks the registry to mirror it)
    - src/dashboard/view.types.ts (plan 03 — `DashboardView`, `ROUTES`, `ALL_ROUTES`, `NAV_ORDER`)
    - src/dashboard/router.ts (plan 03 — `createRouter`'s `RouterOptions` and its dual-trigger `start()`)
    - src/dashboard/nav.ts (plan 06 — `createNav(root)` and its `setActiveRoute`)
    - src/dashboard/theme.ts (plan 02 — `applyThemeMode` / `readStoredMode`, re-applied at bootstrap to reconcile with the inline pre-paint script)
    - src/widgets/shared/widget-base.ts (lines 130-153 — `connectedCallback`'s ordering: styles, then theme, then layout, then loading, then fetch. `main.ts`'s bootstrap ordering mirrors it)
    - src/analytics/compute-best-efforts.test.ts (lines 1-45 — the test phrasing style for `view-registry.test.ts`)
    - vitest.config.ts (node environment — the registry test may assert on route/title/navEntry DATA only, never call `mount()`)
  </read_first>
  <behavior>
    `view-registry.test.ts` (data-only assertions; no `mount()` invocation, no DOM):
    - `VIEWS.map(v => v.route).sort()` equals `[...ALL_ROUTES].sort()` — every declared route has exactly one view and no view declares an undeclared route
    - `VIEWS` contains no duplicate `route` values
    - Every entry has a non-empty `title` and a `mount` of type `function`
    - `getView('/')`, `getView('/list')`, `getView('/calendar')`, `getView('/records')`, `getView('/trends')`, `getView('/activity/:id')` each return a view whose `route` matches the argument
    - `getView('/nope')` returns `undefined`
    - `getView(ROUTES.DETAIL)!.navEntry` is `undefined` — the detail route must never surface in the nav (D-05)
    - Every route present in `NAV_ORDER` resolves through `getView` to a defined view (no nav link can point at a route with no view)
    - `NAV_ORDER` contains no entry whose route is `ROUTES.DETAIL`
  </behavior>
  <action>
    Create `src/dashboard/view-registry.ts` — the single enumeration point (D-03).

    It constructs the shared clients once (`const indexClient = createIndexClient()`, `const detailClient = createDetailClient()`), exports them as `export const clients = { indexClient, detailClient }` so `main.ts` can prefetch the index, and exports:
    - `export const VIEWS: readonly DashboardView[]` — a flat array literal, one entry per route, in this order: `createOverviewView({ indexClient })`, `createListView({ indexClient })`, `calendarView`, `recordsView`, `trendsView`, `createDetailView({ detailClient, indexClient })`. One array, one line per view, exactly like `build-widgets.mjs`'s `widgets` array.
    - `export function getView(route: string): DashboardView | undefined` — an O(1) lookup backed by a `Map` built once from `VIEWS`.

    Add a doc comment stating the extension rule in concrete terms: to add a Phase 18 view, create `src/dashboard/views/<name>.ts`, add its route to `ROUTES` and `ALL_ROUTES` in `view.types.ts`, add one line to `VIEWS` here, and — only if it needs a nav slot — one entry to `NAV_ORDER`.

    Create `src/dashboard/view-registry.test.ts` covering every `<behavior>` case, importing `ALL_ROUTES`, `NAV_ORDER` and `ROUTES` from `'./view.types.js'` so the assertions compare against the real tables.

    Create `src/dashboard/main.ts` — the bootstrap, mirroring `widget-base.ts`'s `connectedCallback` ordering (theme, then chrome, then loading, then data):
    1. `applyThemeMode(readStoredMode(localStorage))` — re-applies at module scope what the inline pre-paint script already set, so the module-side state and the DOM attribute agree and the toggle starts from the right mode.
    2. `const nav = createNav(document.getElementById('app-nav-root')!)`.
    3. Resolve `const container = document.getElementById('app')!`.
    4. Kick off `clients.indexClient.loadIndex()` immediately WITHOUT awaiting it, and attach a `.catch(() => {})` so an early rejection cannot surface as an unhandled rejection — the views await the same memoized promise and each render their own error state. This is DASH-02's "loads a compact activity index manifest immediately".
    5. `createRouter({ routes: ALL_ROUTES, onMatch, onNoMatch }).start()`.
    6. `onMatch(match)`: call the previous view's `unmount?.()` if any, look the view up with `getView(match.route)`, set `document.title` to `` `${view.title} — Strava Analytics` ``, call `nav.setActiveRoute(match.route)`, clear the container, and `await` the view's `mount({ container, routeParams: match.routeParams, query: match.query })` inside a try/catch that renders a generic `.error-state` panel and `console.error`s on throw. Track the mounted view in a module-level variable so the next `onMatch` can unmount it.
    7. `onNoMatch(path)`: `console.warn` the unmatched path and call `navigateTo(ROUTES.OVERVIEW)` so an unknown hash lands on the overview home rather than a blank page. Do not render the unmatched path into the DOM.

    `main.ts` is the only file that touches `document.getElementById`, `document.title`, or module-level side effects; keep every view free of global DOM lookups.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/view-registry.test.ts --reporter=dot && npx tsc --noEmit && node -e "const fs=require('fs');const m=fs.readFileSync('src/dashboard/main.ts','utf8');for(const t of ['applyThemeMode','createNav','createRouter','ALL_ROUTES','loadIndex','setActiveRoute','unmount','navigateTo'])if(!m.includes(t))throw new Error('main.ts missing: '+t);if(/innerHTML/.test(m))throw new Error('main.ts uses innerHTML');if(/await\s+clients\.indexClient\.loadIndex\(\)/.test(m))throw new Error('index prefetch must not block bootstrap');const r=fs.readFileSync('src/dashboard/view-registry.ts','utf8');for(const t of ['createOverviewView','createListView','calendarView','recordsView','trendsView','createDetailView','getView','VIEWS'])if(!r.includes(t))throw new Error('registry missing: '+t);console.log('registry+bootstrap contract OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/view-registry.test.ts --reporter=dot` exits 0
    - `VIEWS` covers exactly the six routes in `ALL_ROUTES` with no duplicates, asserted by test
    - `getView(ROUTES.DETAIL)!.navEntry` is `undefined`, asserted by test
    - Every `NAV_ORDER` route resolves to a defined view, asserted by test
    - `src/dashboard/view-registry.ts` constructs the index and detail clients exactly once and exports them as `clients`
    - `src/dashboard/main.ts` applies the theme, mounts the nav, starts the router with `ALL_ROUTES`, and kicks off `loadIndex()` without awaiting it
    - `main.ts` unmounts the previous view before mounting the next, and sets `document.title` from the matched view's `title`
    - An unmatched route redirects to `ROUTES.OVERVIEW` and the unmatched path is never written to the DOM
    - `npx tsc --noEmit` exits 0 and `npm test` is green
  </acceptance_criteria>
  <done>One registry array enumerates all six views, the bootstrap wires theme, nav, router and the up-front index load together, and view swapping unmounts cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| athlete free text → DOM | `activity.name` and `row.name` originate as user-editable Strava/intervals.icu text and are rendered on a public page |
| route param → DOM / fetch | `:id` reaches the detail view straight from the address bar |
| published JSON → rendered values | Index and detail JSON are parsed and displayed without schema enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-VW-01 | Tampering | `renderActivityRow` in `list.ts`, stats header in `detail.ts`, recent-activity cards in `overview.ts` | mitigate | Every athlete-authored string is written with `textContent` on a dedicated element; no view assigns `innerHTML`, grep-asserted in each task's verify. This is the explicit deviation from the `route-browser` widget analog, whose `item.innerHTML = ...${route.name}...` line is the repo's live instance of this bug |
| T-16-VW-02 | Tampering | Route param handling in `detail.ts` and `onNoMatch` in `main.ts` | mitigate | `isValidActivityId` gates the fetch, and neither the invalid id nor an unmatched path is ever written into the DOM — only to `console.warn`. Closes the "error message echoing the raw param" path RESEARCH.md Pitfall 5 calls out as the real long-term risk |
| T-16-VW-03 | Denial of Service | Rendering 1,867 rows at once in `list.ts` | mitigate | The list renders the newest 100 rows with an explicit truncation notice; full browsing with pagination is BROWSE-01 in Phase 17. Prevents a multi-thousand-node synchronous DOM build on mobile |
| T-16-VW-04 | Denial of Service | Stale detail responses painting over a newer navigation | mitigate | A monotonically increasing request token captured before the await discards superseded results, so rapid navigation between activities cannot render the wrong activity's data |
| T-16-VW-05 | Information Disclosure | Underlying fetch error text surfaced to the page | mitigate | The error state renders only the fixed UI-SPEC copy; the underlying status/message goes to `console.error`, so response internals are not painted into the public page |
</threat_model>

<verification>
- `npx vitest run src/dashboard src/analytics --reporter=dot` passes
- `npm test` full suite green
- `npx tsc --noEmit` exits 0
- All three contract-grep scripts exit 0
- No `innerHTML` assignment exists anywhere under `src/dashboard/` (`grep -rn 'innerHTML' src/dashboard/ | grep -v '\.test\.ts'` returns nothing)
</verification>

<success_criteria>
- The overview home renders real all-time totals, streaks, recent PRs, and recent activities
- The activities list renders real index rows, each with a working `View Activity` link to `#/activity/<id>`
- Opening an activity fetches only that activity's detail and stream files and renders its stats header
- A missing stream renders a clean degraded state; a failed load renders the exact UI-SPEC error copy with a working Retry
- The registry enumerates exactly the six declared routes and keeps the detail route out of the nav
</success_criteria>

<output>
Create `.planning/phases/16-dashboard-shell-data-contract/16-07-SUMMARY.md` when done
</output>
