---
phase: 19-design-system-control-styling
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/dashboard/styles.css
autonomous: true
requirements: [UI-01, ACT-01]
must_haves:
  decisions: [D-01, D-02, D-03]
  truths:
    - "Every input, select and textarea in the dashboard is matched by a rule declaring border, background, padding, radius, min-height and font"
    - "The checkbox on the detail chart overlay does not receive the text-field box treatment"
    - "No vendor pseudo-element rule and no appearance:none was added to any form control"
    - "Zero TypeScript files changed — the treatment reaches all 13 control-creation sites purely by element selector"
  artifacts:
    - path: "src/dashboard/styles.css"
      provides: "Phase 19 banner block plus the input/select/textarea baseline and the type-reset override"
      contains: "Phase 19 — Design System & Control Styling"
  key_links:
    - from: "src/dashboard/styles.css input, select, textarea"
      to: "the 13 createElement('input'|'select') sites across list.ts, calendar.ts, detail-charts.ts, trends.ts"
      via: "bare element selector, no class and no TypeScript change"
      pattern: "^input,"
---

<objective>
Give every `input`, `select` and `textarea` in the dashboard one shared box treatment via bare element selectors, closing UI-01's root cause: the 1,092-line stylesheet currently has **zero** `input`/`select`/`textarea` rules, so all 13 control-creation sites render unstyled browser defaults.

Purpose: this is the single change that fixes the "raw" feel. Using bare element selectors (D-01) rather than a `.control` class means zero risk of a missed site — which is exactly how the raw feel would return — and any control Phases 20-25 add inherits the treatment for free. It also means ACT-01's "Activities screen controls adopt the shared styling" is satisfied with zero edits to `list.ts`, which holds 10 of the 13 sites.
Output: one new banner-commented Phase 19 block at the end of the file containing two rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-design-system-control-styling/19-CONTEXT.md
@.planning/phases/19-design-system-control-styling/19-UI-SPEC.md
@.planning/phases/19-design-system-control-styling/19-PATTERNS.md
@.planning/phases/19-design-system-control-styling/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open the Phase 19 banner block and add the input/select/textarea baseline</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/styles.css` — the end of the file (the Phase 18 block, whose content ends at line 1092), the two existing banner comments at lines 362-366 and 925-931, and the token block at lines 15-110 so you know which token names exist
    - `src/dashboard/styles.css` lines 447-461 (`.filter-toggle`), 482-492 (`.preset-chip`), 544-552 (`.pagination__button`), 622-636 (`.calendar-day`) — the existing sibling control classes whose 1px/`--border` border, 4px radius and 32px min-height this baseline is being aligned to
    - `.planning/phases/19-design-system-control-styling/19-UI-SPEC.md` § Control Baseline Contract — the exact rule, declaration by declaration, and § Typography § "Control typography contract" for what `font: inherit` resolves to
    - `.planning/phases/19-design-system-control-styling/19-PATTERNS.md` § 1 (banner shape to reproduce, including the Phase-19-specific "warn against deletion, not renaming" phrasing), § 4 (the confirmed `min-height: 32px` expression), and **Landmine 4** (this will be the first shorthand `font:` declaration in the file's history)
    - `.planning/phases/19-design-system-control-styling/19-RESEARCH.md` § "D-01 leakage surface — actual `input` type enumeration" — the ground-truth type table
  </read_first>
  <action>
Append a new banner-commented section at the very end of `src/dashboard/styles.css`, after the Phase 18 block's last rule (`.year-heatmap__cell--tint-4`, currently ending at line 1092). Follow the Phase 17/18 banner shape exactly — the `/* ===...=== */` box, the phase number and name (`Phase 19 — Design System & Control Styling`), and a short contract paragraph. Because Phase 19 introduces **no new classes**, the contract paragraph must warn against *deletion* of the element-level rules rather than against renaming: state that removing them requires re-auditing every `createElement('button'|'input'|'select')` site across the 8 view files. Also include a one-line warning, per PATTERNS Landmine 4, that this block contains the file's only shorthand `font:` declaration and that a second one must not be added without checking cascade order, since every existing class overrides with longhand `font-size`/`font-weight`/`line-height`.

Inside that block, add the first rule with the selector list `input`, `select`, `textarea` (three selectors, comma-separated, one per line, matching the file's existing multi-selector formatting such as `.error-state,` / `.stub-panel`). Declare exactly these six properties:

- `border` → `1px solid var(--border)` (matches `.filter-toggle`, `.pagination__button`, `.calendar-day`, `.preset-chip`, `.chip`)
- `background` → `var(--surface)` (the 30% secondary role, so inputs read as chrome on the page)
- `color` → `var(--text)`
- `padding` → `var(--space-xs) var(--space-sm)` (4px block / 8px inline)
- `border-radius` → `var(--radius-control)` (the token plan 19-01 added)
- `min-height` → `32px` written as the literal value, matching the `.pagination__button` / `.calendar-day` / `.records-jump__link` precedent — there is no `--control-height` token in this file and none is to be invented
- `font` → `inherit`

Do **not** add an explicit `font-size` to this rule. `font: inherit` is deliberate: each site renders in whichever type role is contextually active, which resolves to Label (14px/400/1.5) at all 13 sites today. That "resolves to Label everywhere today" claim is a human-checkpoint expectation, not a value to hard-code.

**Hard prohibitions from D-02, which is locked:**
- No `appearance: none` on this rule or any rule in this block.
- No vendor pseudo-element rules of any kind — not `::-webkit-calendar-picker-indicator`, not `::-webkit-inner-spin-button`, not `::-webkit-search-cancel-button`, not `::-moz-*`. `color-scheme: light`/`dark` is already declared per `[data-theme]` block, so native date pickers, month pickers, number spinners and the search clear-X already re-theme correctly at zero cost. A half-restyled native picker is the documented worse outcome. Do not add these even if a native picker looks slightly inconsistent with the box around it — report it at the checkpoint instead.

Note in the summary that `textarea` has **zero** live sites today (there is no `createElement('textarea')` anywhere in the codebase) — it is in the selector list purely as forward-proofing, which is intended, not an error.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 19 — Design System & Control Styling' src/dashboard/styles.css` returns `1`.
    - `grep -c '^input,' src/dashboard/styles.css` returns `1`.
    - `node -e "const c=require('fs').readFileSync('src/dashboard/styles.css','utf8');const n=c.replace(/\/\*[\s\S]*?\*\//g,'');const g=/([^{}]+)\{([^}]*)\}/g;let m,hit=null;while((m=g.exec(n))){const sel=m[1].split(',').map(s=>s.trim());if(sel.includes('input')&&sel.includes('select')&&sel.includes('textarea')){hit=m[2];break}}if(!hit)throw new Error('no input/select/textarea rule');for(const d of ['border: 1px solid var(--border)','background: var(--surface)','color: var(--text)','padding: var(--space-xs) var(--space-sm)','border-radius: var(--radius-control)','min-height: 32px','font: inherit'])if(!hit.includes(d))throw new Error('missing declaration: '+d);console.log('control baseline complete')"` exits 0 and prints `control baseline complete`.
    - `grep -c '::-webkit-' src/dashboard/styles.css` returns `0` (D-02: no vendor pseudo-element rule was added; note `-webkit-overflow-scrolling` is a property, not a `::` pseudo-element, and is untouched).
    - `grep -c 'appearance: none' src/dashboard/styles.css` returns `1` — the single pre-existing occurrence on `.records-jump__link` (line 965). A value of `2` or more means `appearance: none` was added in violation of D-02.
    - `grep -c 'font: ' src/dashboard/styles.css` returns `1` — this block contains the file's only shorthand `font:` declaration.
    - `git status --porcelain src/dashboard/views src/dashboard/nav.ts` is empty.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>A Phase 19 banner block exists at the end of the file with a deletion warning and the shorthand-`font:` caution, containing an `input, select, textarea` rule declaring all seven properties above, and no `appearance: none` or vendor pseudo-element rule was introduced anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Add the type-reset override so the box treatment does not leak onto the checkbox</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/styles.css` — the Phase 19 block just created in Task 1, so the override lands immediately after the baseline rule
    - `.planning/phases/19-design-system-control-styling/19-UI-SPEC.md` § Control Baseline Contract, sub-section `type="button"` / `type="checkbox"` / `type="radio"` reset — the exact override rule and the rationale for an override rule over a `:not()` chain
    - `.planning/phases/19-design-system-control-styling/19-RESEARCH.md` § "D-01 leakage surface" — the ground-truth type enumeration and **Pitfall 2**, which corrects CONTEXT.md's framing
    - `src/dashboard/views/detail-charts.ts` around line 373 — the single live `input[type="checkbox"]` site (the overlay-cap checkbox), read only to confirm it exists and is a bare `<input>` with no class of its own. Do not modify this file.
  </read_first>
  <action>
Immediately after the baseline rule from Task 1, add a following override rule (not `:not()` exclusions on the base selector) with the three selectors `input[type="button"]`, `input[type="checkbox"]`, `input[type="radio"]`, declaring:

- `border` → `none`
- `background` → `transparent`
- `padding` → `0`
- `min-height` → `auto`
- `width` → `auto`

An override rule is chosen over a `:not()` chain because a `:not()` chain grows harder to read every time a new `type` needs excluding, and this codebase already relies on "narrower rule wins by specificity and position" elsewhere. It is also extensible when Phase 24's curation UI adds more checkbox/radio sites, without touching the base rule.

**Be precise about what this actually fixes, per RESEARCH § Pitfall 2.** Only the `input[type="checkbox"]` arm is load-bearing today: there is exactly one live site (`detail-charts.ts:373`, the overlay-cap checkbox), and without this reset it would incorrectly receive the text-field border, background and 32px box. The `input[type="button"]` and `input[type="radio"]` arms target **zero** elements in this codebase — every one of the 31 `type = 'button'` assignments is on a `document.createElement('button')` result, i.e. a `<button>` tag, and a CSS `input` type selector never matches a `<button>` element regardless of its `type` attribute. Keep both arms anyway (cheap and forward-compatible), but do **not** write any assertion, comment or summary sentence claiming they fix a current leak — they do not, and CONTEXT.md's framing on this point is imprecise.

Add a short comment above the override recording that split: checkbox is the one live case, button/radio are forward-proofing with zero current sites.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const c=require('fs').readFileSync('src/dashboard/styles.css','utf8');const n=c.replace(/\/\*[\s\S]*?\*\//g,'');const g=/([^{}]+)\{([^}]*)\}/g;let m,hit=null;while((m=g.exec(n))){const sel=m[1].split(',').map(s=>s.trim());if(sel.includes('input[type=\"checkbox\"]')){hit=m[2];break}}if(!hit)throw new Error('no checkbox reset rule');for(const d of ['border: none','background: transparent','padding: 0','min-height: auto','width: auto'])if(!hit.includes(d))throw new Error('missing declaration: '+d);console.log('type reset complete')"` exits 0 and prints `type reset complete`.
    - `grep -c 'input\[type="button"\]' src/dashboard/styles.css` returns `1` and `grep -c 'input\[type="radio"\]' src/dashboard/styles.css` returns `1`.
    - The override rule appears **after** the `input, select, textarea` rule in the file: `node -e "const c=require('fs').readFileSync('src/dashboard/styles.css','utf8');if(c.indexOf('input[type=\"checkbox\"]')<c.indexOf('\ninput,'))throw new Error('override precedes baseline');console.log('order OK')"` exits 0.
    - Neither the plan summary nor any comment or assertion claims the `input[type="button"]` arm fixes a present-day leak. Grep the summary for the word `button` and confirm every occurrence describing this rule is qualified as forward-proofing with zero current sites.
    - `git status --porcelain src/dashboard/views src/dashboard/nav.ts` is empty.
    - `npx vitest run src/dashboard/styles.test.ts` and `npx tsc --noEmit -p tsconfig.json` both exit 0.
  </acceptance_criteria>
  <done>The three-type override rule follows the baseline in file order and declares all five resets; the checkbox arm is documented as the only live case and the button/radio arms as zero-site forward-proofing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bare element selector → every current and future control | A `0,0,1` element rule reaches markup nobody re-read; the leakage surface is every `<input>`, `<select>` and `<textarea>` the app ever renders |
| CSS box treatment → native widget chrome | Restyling the box around a native date/month/number/search widget risks a half-restyled composite the stylesheet cannot see |

**Not applicable, stated explicitly rather than padded:** V5 Input Validation does not apply — this plan adds zero input handling and zero TypeScript change; the inputs it styles are already-existing and already-validated. No auth (V2), session (V3), access control (V4) or cryptography (V6) surface is touched. No network call, no data file, no build-script change, no new dependency.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-CASCADE-05 | Tampering (unintended restyle) | `input` element selector reaching `type="checkbox"` | mitigate | Task 2's override rule with a Node assertion on all five reset declarations; the one live checkbox site (`detail-charts.ts:373`) is named and confirmed at the human checkpoint's disabled-state row |
| T-19-A11Y-06 | Denial of Service (control unusable) | native picker chrome half-restyled by `appearance: none` or a vendor pseudo-element | mitigate | D-02 forbids both; Task 1's acceptance criteria assert `grep -c '::-webkit-'` is `0` and `grep -c 'appearance: none'` is exactly `1` (the pre-existing `.records-jump__link` occurrence), so any addition fails the gate |
| T-19-FONT-07 | Tampering (cascade order dependence) | first shorthand `font:` declaration in the file | accept | Safe today because all 12 button classes and control-adjacent classes override with longhand sub-properties, which win regardless of the shorthand. Accepted with a written warning in the banner comment and `grep -c 'font: '` asserted to be `1`, so a second shorthand cannot be added silently |
| T-19-FALSEGREEN-08 | Repudiation (green gate hiding a rendering defect) | text assertions over CSS source | mitigate | Explicitly not mitigated by more assertions. A text assertion proves a rule exists in the source and nothing about a rendered box. Discharged only by plan 19-05's human browser checkpoint under `/strava-widgets` — the project has shipped rendering defects behind a fully green gate three times |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs authorized. RESEARCH.md § Package Legitimacy Audit records `Disposition: N/A`. If a task appears to need a dependency, stop and escalate rather than installing |
</threat_model>

<verification>
- `npx vitest run src/dashboard/styles.test.ts` exits 0 after each task.
- `npx tsc --noEmit -p tsconfig.json` exits 0 (tripwire for scope creep into TypeScript).
- `git status --porcelain src/dashboard/views src/dashboard/nav.ts` is empty — the 13 control-creation sites were reached by cascade, not by edit.
- **Not proven by any of the above:** that a single input renders with the intended border, padding or background; that native date/month/number/search chrome still themes correctly; or that the inherited font resolves to Label at all 13 sites. There is no CSSOM and no rendering engine in this repo. All of that is deferred to plan 19-05's human checkpoint.
</verification>

<success_criteria>
Every `input`, `select` and `textarea` the dashboard renders is matched by one rule declaring border, background, colour, padding, radius, min-height and inherited font; the single live checkbox is excluded from that box treatment; no `appearance: none` and no vendor pseudo-element rule was added; and no TypeScript file changed.
</success_criteria>

<output>
Create `.planning/phases/19-design-system-control-styling/19-02-SUMMARY.md` when done. Record: the exact banner text used, the seven baseline declarations as written, the five override declarations as written, the confirmation that `textarea` has zero live sites and that the `button`/`radio` reset arms have zero live sites (forward-proofing only), and the confirmed `grep` counts for `::-webkit-`, `appearance: none` and `font: `.
</output>
