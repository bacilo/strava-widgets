---
phase: 19-design-system-control-styling
plan: 13
type: execute
wave: 13
depends_on: [19-12]
files_modified:
  - .planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md
  - .planning/phases/19-design-system-control-styling/19-VALIDATION.md
autonomous: false
requirements: [UI-02]
user_setup: []

must_haves:
  truths:
    - "The actual containing block, ancestor overflow chain and scroll container of the sticky nav are established from a probe run against the rendered, production-shaped build — not from reading styles.css"
    - "Exactly one root cause for GAP 7 is confirmed, and every competing candidate is excluded by a named field of a recorded probe output"
    - "If the probe cannot discriminate between candidates, the plan stops and reports rather than nominating a fix"
    - "No fix is written in this plan and no source file is touched"
  artifacts:
    - path: ".planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md"
      provides: "Candidate root causes H1-H5, the discrimination matrix, the verbatim probe outputs, and the single confirmed root cause (or an explicit INCONCLUSIVE stop)"
      contains: "## Confirmed Root Cause"
    - path: ".planning/phases/19-design-system-control-styling/19-VALIDATION.md"
      provides: "GAP 7 gains a dated Round 4 diagnosis subsection carrying the evidence, with the verdict-token count of the file unchanged at 6"
      contains: "GAP 7"
  key_links:
    - from: "19-GAP7-DIAGNOSIS.md § Confirmed Root Cause"
      to: "19-14-PLAN.md Task 1 fix decision table"
      via: "hypothesis id (H1..H5 or INCONCLUSIVE) read by the next plan before any CSS is written"
      pattern: "H[1-5]|INCONCLUSIVE"
    - from: "Probe E1/E2 output"
      to: "each excluded hypothesis"
      via: "a named probe field cited as the exclusion evidence"
      pattern: "excluded by:"
  decisions: [D-09, D-10, D-12]
---

<objective>
Establish, empirically and against the rendered page, why `.app-nav` does not remain
on screen while scrolling (GAP 7), and confirm exactly one root cause by excluding
every competitor with recorded evidence.

Purpose: this phase has now recorded FOUR distinct false-green mechanisms, the newest
being *a defect diagnosed and fixed entirely from source-code reading, never checked
against the rendered page*. GAP 7 was found by a probe, and it will be diagnosed by a
probe. No fix may be written until this plan names a root cause on evidence.

Output: `19-GAP7-DIAGNOSIS.md` carrying the candidate set, the discrimination matrix,
two verbatim probe outputs, and one confirmed root cause — or an explicit INCONCLUSIVE
stop. Plus a dated diagnosis subsection appended to GAP 7 in `19-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-design-system-control-styling/19-VALIDATION.md
@.planning/phases/19-design-system-control-styling/19-12-SUMMARY.md
@src/dashboard/styles.css
@src/dashboard/index.html
@src/dashboard/nav.ts
@src/dashboard/main.ts
</context>

<constraints>
**This plan writes no source file.** `git status --porcelain src scripts` must be empty
at the end of every task. Diagnosing is the entire deliverable.

**This plan adds zero verdict tokens to `19-VALIDATION.md`.** Plan 19-17's gate counts
the literal string `VERDICT` followed by a colon across the whole file and expects
exactly 11 (6 from Round 3, 5 from Round 4). Plan 19-12's own summary records a
deviation where descriptive prose broke its own gate this exact way. Any prose this
plan adds must say "PASS/FAIL verdict token", never the literal prefixed form.

**No literal `|` inside any table cell** written into `19-VALIDATION.md`; use a
semicolon or the word "and". This broke parsing in Round 3.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate candidate root causes, write the discrimination matrix, stage the rendered build</name>
  <files>.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md</files>
  <read_first>
    - `src/dashboard/styles.css` lines 145-250 — the `body` rule, the whole sticky-layer ladder comment, and the `.app-nav` / `.app-nav__links` / `.app-nav__link` / `.app-nav__toggle` rules as shipped
    - `src/dashboard/styles.css` lines 420-450 — the `@media (max-width: 640px)` block, which is the only place any `.app-nav` property is re-declared
    - `src/dashboard/index.html` lines 55-70 — the `<header id="app-nav-root">` / `<main id="app" class="view">` shell, and confirmation that no stylesheet other than `styles.css` is linked
    - `src/dashboard/main.ts` lines 20-30 — `createNav(document.getElementById('app-nav-root')!)`
    - `src/dashboard/nav.ts` lines 125-160 — `navEl.className = 'app-nav'` and what is appended inside it
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` § Gap-Closure Record GAP 6 and GAP 7, and § Round 3 Probe Outputs (Probe D)
    - `.planning/phases/19-design-system-control-styling/19-12-PLAN.md` Task 1 § staging — the absolute-source-path symlink recipe and the `MOUNT_PREFIX` reason it exists
  </read_first>
  <action>
Create `.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md` with these
sections, in this order: `## Fact under diagnosis`, `## Candidate root causes`,
`## Discrimination matrix`, `## Probe E1 — structural chain`, `## Probe E2 — scroll
behaviour`, `## Probe outputs (verbatim)` (left empty for Task 3), `## Confirmed Root
Cause` (left as the literal placeholder `_pending Task 3_`).

`## Fact under diagnosis` quotes, verbatim and without interpretation: the developer's
Round 3 words "If i scroll down the navbar disappears upwards", and Probe D's output
`{parent: "HEADER#app-nav-root", navH: 77, parentH: 77, navPos: "sticky", parentPos: "static"}`.
State plainly that no root cause has been established and that source reading alone will
not establish one — cite the fourth false-green mechanism recorded in `19-12-SUMMARY.md`.

`## Candidate root causes` enumerates exactly these five, each with an id, a one-sentence
mechanism, and the field of E1 or E2 that would confirm it:

- **H1 — zero-travel containing block.** A sticky element is constrained to its own
  containing block, which is the content box of `#app-nav-root`. If that box is exactly
  as tall as the nav, the sticky element has zero travel distance and leaves the viewport
  with its parent. Confirmed by E1 `#app-nav-root.ch` equal to `.app-nav.oh`, together
  with E2 showing `after.navTop` equal to `after.parTop` and both approximately minus
  `after.sy`.
- **H2 — an ancestor establishes a clip or scroll container.** Any ancestor between
  `.app-nav` and the document root whose computed `overflow-x` or `overflow-y` is
  anything other than `visible` becomes the sticky element's scrollport. Confirmed by E1
  reporting a non-`visible` overflow on any chain entry.
- **H3 — the page's scroll container is not the document.** If a wrapper element scrolls
  rather than the viewport, `window.scrollY` never changes and the nav never enters its
  stuck state. Confirmed by E2 `after.sy` remaining at or near 0 while the content
  visibly moved, or by E1 `scrollingIsDoc` reporting false.
- **H4 — an ancestor caps the travel by height or display.** An explicit non-`auto`
  `height` on `#app-nav-root`, `body` or `html`, or a `display` value that removes or
  reshapes a box in the chain (`contents`, `flex`, `grid`), can reduce the sticky range
  independently of H1. Confirmed by E1 `h` or `disp` on a chain entry above `.app-nav`.
- **H5 — the element is not sticky at the tested viewport width.** Confirmed by E1
  reporting `pos` other than `sticky` on the `.app-nav` chain entry at the recorded
  `w`.

`## Discrimination matrix` is a table with one row per hypothesis and these columns:
`Hypothesis`, `Confirming signature`, `Excluding signature`. Every cell must name
concrete E1/E2 field names and values, not prose like "if the nav does not stick".
H1 and H4 overlap — state the split explicitly: H1 is specifically
`#app-nav-root.ch === .app-nav.oh` with every chain overflow `visible`; H4 is any
travel cap originating above `#app-nav-root`.

`## Probe E1 — structural chain` and `## Probe E2 — scroll behaviour` each carry: the
route to run it on, the exact single-paste console snippet, the fields it returns, and
the instruction to paste the result back verbatim including braces and quotes.

Probe E1 walks from `.app-nav` up through every `parentElement` to `html`, recording for
each: the element as tag plus id plus class list, and computed `position`, `top`,
`overflow-x`, `overflow-y`, `display`, `height`, `transform`, `filter`, `will-change`,
`contain`, plus `clientHeight`, `scrollHeight` and `offsetHeight`. It also records
`innerWidth`, whether `document.scrollingElement` is `document.documentElement`, and the
document element's `scrollHeight` and `clientHeight`. Return it as a single
`JSON.stringify` so the developer can copy one line.

Probe E2 records `scrollY`, `.app-nav`'s `getBoundingClientRect().top` and its
`parentElement`'s `getBoundingClientRect().top` before a programmatic
`scrollTo(0, 600)`, then re-reads all three plus `document.scrollingElement.scrollTop`
after a settle delay of roughly 400ms, and returns both snapshots as one
`JSON.stringify`. Write it as an expression that resolves to the string so a console
paste prints it without a second step. Include an explicit invalidation rule in the
document: if `after.sy` is below 400 the route was too short to scroll and the run
proves nothing — it must be re-run on a taller route and logged as a failed capture,
exactly as Probe C's first attempt was logged in Round 3 rather than counted.

E1 runs on `#/list` only. E2 runs on `#/list` and again on `#/records` — two separate
recorded outputs, because GAP 7 was reported on Activities and row 18 covered both
routes.

Then stage the rendered build, using the sequence that is known to work: capture the
repository root into a variable while still inside the repository, create `/tmp/gh-pages`,
create the symlink `/tmp/gh-pages/strava-widgets` pointing at the absolute `dist/widgets`
path under that captured root, confirm `/tmp/gh-pages/strava-widgets/index.html` resolves,
then change directory to `/tmp/gh-pages` and serve port 8099 with `python3 -m http.server`.
Run `npm run build-widgets` first so the served bundle is current. Serving at the bare
server root is not acceptable: `scripts/verify-dashboard-publish.mjs` carries
`MOUNT_PREFIX = '/strava-widgets'` because Phase 16 shipped a black page behind a 15/15
green gate that only mounted at the root.

Do not write any candidate fix, any recommendation, or any sentence beginning "the fix
is" into this document. Task 1 produces questions and a way to answer them.
  </action>
  <verify>
    <automated>rm -f /tmp/bw13.log && npm run build-widgets > /tmp/bw13.log 2>&1; BW=$?; cat /tmp/bw13.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw13.log && test -f /tmp/gh-pages/strava-widgets/index.html && node -e "const s=require('fs').readFileSync('.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md','utf8');for(const h of ['H1','H2','H3','H4','H5'])if(!s.includes(h))throw new Error('missing hypothesis '+h);for(const sec of ['## Fact under diagnosis','## Candidate root causes','## Discrimination matrix','## Probe E1','## Probe E2','## Confirmed Root Cause'])if(!s.includes(sec))throw new Error('missing section '+sec);if(!s.includes('_pending Task 3_'))throw new Error('Confirmed Root Cause is not left pending');if(!/navH:\s*77/.test(s))throw new Error('Probe D output not quoted verbatim');if(/the fix is|recommend/i.test(s))throw new Error('Task 1 nominated a fix - forbidden before diagnosis');console.log('diagnosis scaffold ok, five hypotheses, no fix nominated')" && git status --porcelain src scripts | tee /dev/stderr | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - `19-GAP7-DIAGNOSIS.md` contains all six required section headings, exactly five hypothesis ids H1 through H5, and `## Confirmed Root Cause` left as `_pending Task 3_`.
    - The discrimination matrix names concrete E1/E2 field names in every Confirming and Excluding cell; no cell contains only prose.
    - Probe D's Round 3 output is quoted verbatim, including `navH: 77` and `parentH: 77`.
    - The document contains no fix, recommendation or phrase matching `the fix is` / `recommend` — asserted by the gate's regex.
    - Both probe snippets are single-paste expressions returning one `JSON.stringify` result; E2 carries the "`after.sy` below 400 means failed capture, re-run" invalidation rule in writing.
    - `npm run build-widgets` exits 0 with zero `css-syntax-error` occurrences, established with the redirect-and-capture form (not a `cmd | tee log; ! grep` pipeline, whose exit status reflects `tee` and a possibly-stale log).
    - `test -f /tmp/gh-pages/strava-widgets/index.html` succeeds and `readlink /tmp/gh-pages/strava-widgets` prints an absolute path beginning with the repository root.
    - `git status --porcelain src scripts` is empty.
  </acceptance_criteria>
  <done>A diagnosis document exists that states the fact, enumerates five competing explanations, says exactly which measured field would confirm or exclude each, and a current production-shaped build is being served at `http://localhost:8099/strava-widgets/` ready to be probed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run Probe E1 once and Probe E2 twice in a real browser, return all three outputs verbatim</name>
  <files>.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md` as written by Task 1 — the two probe snippets and the E2 invalidation rule
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` § Round 3 Probe Outputs — the recording format this session's outputs must match, and Probe C's failed first capture as the precedent for logging a bad run rather than counting it
  </read_first>
  <action>
Change no file under `src/` or `scripts/`. This task collects three console outputs and
nothing else. There is no visual judgement to make and no verdict to give: the developer
is not being asked whether anything looks right, only to paste three results.

Do not accept a paraphrase. "It scrolled away" is not a probe output. If a paste comes
back truncated, ask for it again — a truncated chain is a failed capture, logged as such,
not a partial result.

If E2's `after.sy` is below 400 on either route, that run is a failed capture: say so,
ask the developer to scroll-check that the route really is taller than the viewport, and
re-run. Record the failed capture in the document alongside the good one, exactly as
Round 3 recorded Probe C's first attempt.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md','utf8');const b=s.split('## Probe outputs (verbatim)')[1]||'';if(!b.trim())throw new Error('no probe outputs recorded');const chains=(b.match(/scrollingIsDoc/g)||[]).length;if(chains<1)throw new Error('Probe E1 output not recorded');const runs=(b.match(/\"before\"|before:/g)||[]).length;if(runs<2)throw new Error('expected two Probe E2 runs (list and records), found '+runs);for(const r of ['#/list','#/records'])if(!b.includes(r))throw new Error('no output recorded for route '+r);console.log('E1 recorded, E2 recorded on both routes')" && git status --porcelain src scripts | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - Probe E1's full output is pasted into `## Probe outputs (verbatim)` unedited, including every chain entry from `.app-nav` up to `html`, and containing the `scrollingIsDoc` field.
    - Probe E2's output is recorded twice, one labelled `#/list` and one labelled `#/records`, each carrying both the before and after snapshots.
    - Every recorded run states the route it ran on and the theme is irrelevant here and is stated to be irrelevant — this probe measures geometry, not appearance.
    - Any run with `after.sy` below 400 is labelled a failed capture and is accompanied by a valid re-run; it is never used as evidence.
    - `git status --porcelain src scripts` is empty — no source file was touched during the session.
    - The session ran against a URL beginning `http://localhost:8099/strava-widgets/`, recorded as such.
  </acceptance_criteria>
  <what-built>
Nothing was built. Round 3 found that the top nav does not stay on screen when you scroll
(your words: "If i scroll down the navbar disappears upwards"), and the house rule says a
still-failing row is recorded with no fix and no theory attached. So no fix has been
written and none will be until we know why.

Four times in this phase something has been declared correct on the strength of reading
the source rather than looking at the page, and four times that was wrong. The most
recent example is the very defect this probe is about: the nav's `z-index` was diagnosed,
fixed and shipped entirely from reading `styles.css`, and it turned out the situation it
fixes may never occur, because the nav does not stay put in the first place.

So this session asks you for measurements, not opinions. Three console pastes. Nothing to
look at, nothing to judge.
  </what-built>
  <how-to-verify>
**Staging.** Task 1 rebuilt the widgets and started a server. Confirm the URL bar reads
`http://localhost:8099/strava-widgets/...` — if it does not contain `/strava-widgets/`,
stop and say so; a root-served build is how Phase 16 shipped a black page behind a green
gate.

**Probe E1 — one paste, on Activities.** Open `http://localhost:8099/strava-widgets/#/list`,
open the console, paste the E1 snippet from `19-GAP7-DIAGNOSIS.md`, press Enter, and paste
the entire result back. It will be one long line of JSON. Do not trim it, do not summarise
it, do not remove the middle. If your console shows it collapsed, right-click the output
and copy it whole.

**Probe E2 — one paste, on Activities.** Still on `#/list`, paste the E2 snippet and press
Enter. The page will scroll itself down by 600 pixels; roughly half a second later a
result appears. Paste it back whole.

If the result's `after.sy` is under 400, the page was not tall enough to scroll properly —
tell me, scroll back to the top, and we will re-run it. A short page proves nothing.

**Probe E2 again — one paste, on Records.** Go to
`http://localhost:8099/strava-widgets/#/records`, scroll back to the very top, paste the
same E2 snippet, press Enter, paste the result back.

That is all three. You are not being asked whether the nav looks right, whether anything
is opaque, or whether either theme is better. If you happen to notice something, say it
and it will be logged, but it is not what this session is for.
  </how-to-verify>
  <resume-signal>Paste the three outputs — E1 on Activities, E2 on Activities, E2 on Records — each labelled with the route it came from. Raw JSON, not a description.</resume-signal>
  <done>Three verbatim probe outputs are recorded in `19-GAP7-DIAGNOSIS.md` under `## Probe outputs (verbatim)`, each labelled with its route, with any failed capture logged as such rather than used as evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the discrimination matrix, confirm exactly one root cause or stop</name>
  <files>.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md, .planning/phases/19-design-system-control-styling/19-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md` — the discrimination matrix written in Task 1 and the three outputs recorded in Task 2
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` § Gap-Closure Record — the shape of GAP 1 through GAP 7 entries, so the new subsection matches, and the existing count of verdict tokens which this task must not change
    - `src/dashboard/views/records.ts` lines 168-182 — `updateJumpOffset` reads the LIVE `.app-nav` rendered height to position the jump bar; the diagnosis must state whether the confirmed root cause implicates this coupling, because the next plan's fix must not break it
  </read_first>
  <action>
Fill `## Confirmed Root Cause` by applying the Task 1 matrix to the Task 2 outputs.
Mechanical, not narrative: for each of H1 through H5, write one line reading either
`H{n} — CONFIRMED by: {field} = {value}` or `H{n} — excluded by: {field} = {value}`. Every
line must cite a field name and the literal value from a recorded output. A hypothesis
excluded "on inspection", "by reasoning" or with no field cited is not excluded.

Exactly one hypothesis may be CONFIRMED. If two or more survive, or if none does, write
`## Confirmed Root Cause` as the single line `INCONCLUSIVE` followed by which fields were
ambiguous and what additional measurement would separate them — then STOP. Do not nominate
a fix, do not pick the most plausible survivor, and state in the summary that plan 19-14
must not run until a further probe round discriminates. Guessing a fix from a plausible
survivor is precisely the mechanism that produced GAP 6.

State explicitly whether the confirmed root cause implicates `records.ts`'s
`updateJumpOffset`, which measures the live `.app-nav` rendered height — one sentence,
either way, so the next plan's fix carries that constraint rather than rediscovering it.

Then append to GAP 7 in `19-VALIDATION.md` a subsection headed
`**Diagnosis (Round 4, {date})**` carrying: the confirmed hypothesis id and its one-line
mechanism, the single field-and-value that confirmed it, the field-and-value that excluded
each competitor, and a pointer to `19-GAP7-DIAGNOSIS.md` for the full outputs. Change
nothing else in that file: GAP 1 through GAP 6 stay untouched, rows 1-19 stay
byte-identical, the frontmatter stays `nyquist_compliant: false`, and the file-wide count
of the literal string `VERDICT` followed by a colon stays at exactly 6. Write "PASS/FAIL
verdict token" if that concept needs naming in prose.

Do not write a fix, a patch, a diff or a CSS declaration anywhere in this task.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md','utf8');const sec=d.split('## Confirmed Root Cause')[1]||'';if(!sec.trim()||sec.includes('_pending Task 3_'))throw new Error('Confirmed Root Cause still pending');const inc=/INCONCLUSIVE/.test(sec);if(!inc){const conf=(sec.match(/H[1-5][^\n]*CONFIRMED/g)||[]);if(conf.length!==1)throw new Error('expected exactly 1 CONFIRMED hypothesis, found '+conf.length);const exc=(sec.match(/H[1-5][^\n]*excluded by:/g)||[]);if(exc.length!==4)throw new Error('expected 4 excluded hypotheses, found '+exc.length);}if(!/updateJumpOffset|jump bar/i.test(sec))throw new Error('diagnosis does not state whether records.ts updateJumpOffset is implicated');const v=fs.readFileSync('.planning/phases/19-design-system-control-styling/19-VALIDATION.md','utf8');const t=(v.match(/VERDICT:\s*(PASS|FAIL)/g)||[]).length;if(t!==6)throw new Error('validation file verdict token count changed to '+t+' - must stay 6');if(!/nyquist_compliant: false/.test(v))throw new Error('nyquist_compliant must stay false');if(!/Diagnosis \(Round 4/.test(v))throw new Error('GAP 7 has no Round 4 diagnosis subsection');console.log(inc?'INCONCLUSIVE recorded - 19-14 must not run':'one root cause confirmed, four excluded by cited fields')" && git status --porcelain src scripts | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - `## Confirmed Root Cause` contains either exactly one `CONFIRMED` line plus four `excluded by:` lines, or the single word `INCONCLUSIVE` with the ambiguous fields named and the next measurement proposed.
    - Every CONFIRMED and every excluded line cites a probe field name and its literal recorded value; the gate asserts the counts, and a reviewer can trace each to `## Probe outputs (verbatim)`.
    - The diagnosis states in one sentence whether `records.ts`'s `updateJumpOffset` live-height coupling is implicated by the confirmed cause.
    - GAP 7 in `19-VALIDATION.md` gains a dated `Diagnosis (Round 4)` subsection; `git diff` on that file touches GAP 7 only — not rows 1-19, not GAP 1-6, not the Approval line.
    - The file-wide count of the literal string `VERDICT` followed by a colon in `19-VALIDATION.md` is still exactly 6, and `nyquist_compliant` is still `false`.
    - No `|` character appears inside any table cell this task writes.
    - `git status --porcelain src scripts` is empty — this plan changed no source file at all.
    - If INCONCLUSIVE: the summary states plainly that plan 19-14 must not execute and a further probe round is required.
  </acceptance_criteria>
  <done>GAP 7 has one root cause established on measured evidence with every competitor excluded by a cited field value, or an honest INCONCLUSIVE stop — and in neither case has a line of CSS been written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source reading → a claim about rendered behaviour | The boundary this phase has crossed wrongly four times, most recently on this very defect. Probes E1/E2 are the control; nothing in this plan may assert rendered behaviour without a recorded field value. |
| a plausible hypothesis → a nominated fix | Picking the first survivor is how GAP 6 was produced. The control is the exactly-one-CONFIRMED gate and the mandatory INCONCLUSIVE stop. |
| local build → production URL shape | Serving at the bare server root reproduces Phase 16's black page behind a green gate. Mitigated by the `/strava-widgets` mount assertion. |

**Application-security surface: not applicable, stated rather than padded.** This plan
writes two planning documents and no source file. There is no new data flow, no auth or
session surface, no user input handling, no network egress beyond a localhost static
server already present on the machine, and no persisted user data. The assessment would
change if a probe were ever automated by installing a headless browser, or if the served
directory were widened beyond `dist/widgets`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-D7-01 | Repudiation (a root cause asserted without evidence) | `19-GAP7-DIAGNOSIS.md` § Confirmed Root Cause | mitigate | Gate requires exactly one CONFIRMED line and four `excluded by:` lines, each citing a probe field and literal value traceable to a recorded output. |
| T-19-D7-02 | Tampering (a fix smuggled into a diagnosis plan) | `src/dashboard/styles.css` | mitigate | Every task asserts `git status --porcelain src scripts` is empty; Task 1's gate additionally rejects the strings `the fix is` and `recommend` in the diagnosis document. |
| T-19-D7-03 | Repudiation (a paraphrased probe result standing in for the measurement) | Task 2 checkpoint record | mitigate | Gate asserts the presence of raw field names (`scrollingIsDoc`, before/after snapshots) and one output per route; truncated or described results are logged as failed captures, per the Probe C precedent. |
| T-19-D7-04 | Tampering (a self-inflicted gate break in the shared validation file) | `19-VALIDATION.md` | mitigate | Task 3's gate pins the file-wide verdict-token count at 6 and `nyquist_compliant` at false; plan prose is required to avoid the literal token form, per the deviation recorded in `19-12-SUMMARY.md`. |
| T-19-D7-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized. The server is `python3 -m http.server`, already present. Do not install jsdom, puppeteer or playwright to automate these probes — no headless substitute is permitted for a rendered-evidence claim in this phase. |
</threat_model>

<verification>
- `npm run build-widgets` exits 0 with zero `css-syntax-error`, and the build is served under `/strava-widgets`.
- `19-GAP7-DIAGNOSIS.md` carries five hypotheses, a field-level discrimination matrix, three verbatim probe outputs, and either one confirmed root cause with four cited exclusions or an explicit INCONCLUSIVE stop.
- `19-VALIDATION.md` gains a GAP 7 Round 4 diagnosis subsection and is otherwise unchanged; verdict-token count still 6; `nyquist_compliant` still false.
- `git status --porcelain src scripts` empty after every task.
</verification>

<success_criteria>
GAP 7's root cause is established from measured rendered behaviour with every competing
explanation excluded by a named field value — or the plan stops and says so — and not one
character of CSS was written on the strength of a theory.
</success_criteria>

<output>
Create `.planning/phases/19-design-system-control-styling/19-13-SUMMARY.md` when done.
Record: the staging command sequence with the absolute source path; all three probe
outputs verbatim alongside the hypothesis each field bears on; the confirmed root cause
id with its confirming field and the four exclusions; whether `updateJumpOffset` is
implicated; the verdict-token count of `19-VALIDATION.md` before and after (both 6);
and — if INCONCLUSIVE — an explicit statement that plan 19-14 must not execute.
</output>
