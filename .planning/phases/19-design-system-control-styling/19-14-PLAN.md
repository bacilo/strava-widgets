---
phase: 19-design-system-control-styling
plan: 14
type: execute
wave: 14
depends_on: [19-13]
files_modified:
  - src/dashboard/styles.css
  - src/dashboard/styles.test.ts
  - .planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md
autonomous: false
requirements: [UI-02]
user_setup: []

must_haves:
  truths:
    - "The top nav remains on screen while the page scrolls, measured on the rendered production-shaped build on both Activities and Records"
    - "The fix applied is the one the confirmed root cause prescribes, not the one that seemed most plausible"
    - "The positioning precondition of the sticky-layer ladder is pinned by a test that fails when position sticky is deleted"
    - "The Records jump bar's live-height coupling to the rendered nav still resolves after the fix"
  artifacts:
    - path: "src/dashboard/styles.css"
      provides: "The sticky rung that actually holds while the page scrolls"
      contains: "position: sticky"
    - path: "src/dashboard/styles.test.ts"
      provides: "A ladder assertion that pins the positioning precondition, not only the z-index numbers"
      contains: "position: sticky"
    - path: ".planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md"
      provides: "Probe F output recorded verbatim on two routes as the rendered evidence the fix works"
      contains: "## Fix confirmation (Round 4)"
  key_links:
    - from: "19-GAP7-DIAGNOSIS.md § Confirmed Root Cause"
      to: "Task 1's fix decision table"
      via: "hypothesis id selecting exactly one prescribed edit"
      pattern: "H[1-5]"
    - from: "the sticky rung rule in styles.css"
      to: "src/dashboard/views/records.ts updateJumpOffset"
      via: "getBoundingClientRect().height read off the live .app-nav element"
      pattern: "querySelector\\('\\.app-nav'\\)"
    - from: "the ladder test in styles.test.ts"
      to: "the sticky rung rule"
      via: "an assertion on position: sticky that fails under deletion"
      pattern: "position: sticky"
  decisions: [D-09, D-10, D-12]
---

<objective>
Fix GAP 7 — the top nav does not remain on screen while scrolling — by applying the edit
that plan 19-13's confirmed root cause prescribes, pin the positioning precondition the
ladder test currently leaves unguarded, and confirm the result by measuring the rendered
page rather than by reading the stylesheet back.

Purpose: UI-02 is the last open requirement in this phase and row 18 failed because this
defect made its premise unreachable. Until the nav holds on a real page, nothing about
paint order under it can be observed at all.

Output: one prescribed CSS edit, one mutation-proven guard, and a verbatim Probe F output
from two routes showing the nav pinned at the viewport top after a programmatic scroll.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md
@.planning/phases/19-design-system-control-styling/19-REVIEW-round3.md
@src/dashboard/styles.css
@src/dashboard/styles.test.ts
@src/dashboard/index.html
@src/dashboard/views/records.ts
</context>

<hard_precondition>
**Do not start this plan if `19-GAP7-DIAGNOSIS.md` § Confirmed Root Cause reads
`INCONCLUSIVE`.** Stop, report, and say that a further probe round is required. Choosing
the most plausible surviving hypothesis is the exact mechanism that produced GAP 6 — a
fix shipped, its declaration confirmed present in the bundle, and its effect never
observable.
</hard_precondition>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply the fix the confirmed root cause prescribes, and pin it with a failing-first guard</name>
  <files>src/dashboard/styles.css, src/dashboard/styles.test.ts</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md` § Confirmed Root Cause and § Probe outputs — the hypothesis id, its confirming field value, and whether `updateJumpOffset` is implicated
    - `src/dashboard/styles.css` lines 145-250 — the sticky-layer ladder comment and the `.app-nav` rule as shipped, including `background: var(--surface)`, `border-bottom` and `padding: var(--space-md)`
    - `src/dashboard/styles.css` lines 420-450 — the `@media (max-width: 640px)` block, the only place `.app-nav` descendants are re-declared
    - `src/dashboard/styles.css` lines 940-965 — the `.splits-table__km` rule and its own ladder comment
    - `src/dashboard/styles.test.ts` lines 654-689 — the four-rung ladder test, which reads `.app-nav`'s body for its z-index and will throw if that declaration moves
    - `src/dashboard/index.html` lines 55-62 — `<header id="app-nav-root">` and `<main id="app" class="view">`, confirming `#app-nav-root` is a real element with no existing rule in `styles.css`
    - `src/dashboard/views/records.ts` lines 168-182 — `updateJumpOffset` reads the live `.app-nav` rendered height to set the jump bar's `top`
    - `.planning/phases/19-design-system-control-styling/19-REVIEW-round3.md` § R3-WR-01 — the executed mutation proving the ladder test survives deletion of `position: sticky`
  </read_first>
  <behavior>
    - The sticky rung rule declares `position: sticky` and `top: 0`; deleting either from `styles.css` makes `npx vitest run src/dashboard/styles.test.ts` fail.
    - The four-rung ladder still reads 20 / 10 / 2 / 1 as parsed numbers, in strictly descending order, with rung 4 read from whichever selector now carries it.
    - Exactly one rule in `styles.css` declares `position: sticky` for the top nav; a second, nested sticky declaration on the inner element is absent, because a sticky element inside a zero-travel sticky parent is the shape GAP 7 was.
    - `.records-jump` and `.splits-table__km` still declare `position: sticky`, asserted alongside the nav so the whole ladder cannot go inert silently.
  </behavior>
  <action>
Read the confirmed hypothesis id from `19-GAP7-DIAGNOSIS.md` and apply exactly the edit
this table prescribes for it. Do not blend two rows, and do not improve on a row.

- **H1 (zero-travel containing block).** Add a new rule `#app-nav-root` immediately above
  the `.app-nav` rule, declaring `position: sticky`, `top: 0` and `z-index: 20`, and
  remove those same three declarations from `.app-nav`. Leave `.app-nav`'s `display`,
  `flex-direction`, `align-items`, `background`, `border-bottom` and `padding` exactly as
  they are — the opaque chrome stays on the inner element, only the sticky rung moves to
  the element whose containing block is the page. Update the ladder comment's rung-4 line
  to name `#app-nav-root` instead of `.app-nav`, a one-line label correction only; the
  comment's prose is plan 19-16's work, not this task's.
- **H2 (an ancestor establishes a clip or scroll container).** Remove the offending
  `overflow` declaration from the ancestor the probe named, if and only if that
  declaration exists purely to clip children cosmetically — the same judgement D-10 made
  when plan 19-04 removed `overflow: hidden` from `.segmented`. If the overflow is
  load-bearing (a real scroll region with content wider or taller than its box, evidenced
  by that entry's `scrollHeight` exceeding its `clientHeight` in Probe E1), STOP and
  report: the fix is structural and needs its own decision, not a silent removal.
- **H3 (the page's scroll container is not the document).** STOP and report. Changing which
  element scrolls is a shell/markup change outside this phase's CSS-only fence and needs a
  new decision recorded in a context pass.
- **H4 (an ancestor caps the travel by height or display).** Remove the height cap or
  correct the `display` value on the ancestor the probe named, naming the field and value
  from Probe E1 in the rule's comment. If that ancestor is `html` or `body` and the cap is
  load-bearing for another screen, STOP and report.
- **H5 (the element is not sticky at the tested viewport width).** Correct the media-query
  declaration that overrides it, naming the recorded `w` and the overriding rule.

Whichever row applies, the edit must not change `.app-nav`'s rendered height: `records.ts`'s
`updateJumpOffset` positions the Records jump bar from that live measurement, and a height
change there silently mis-parks the jump bar. State in the summary which of `.app-nav`'s
box properties, if any, moved.

Then close R3-WR-01 in `src/dashboard/styles.test.ts`. The four-rung ladder test at
`:669-689` reads only z-index values, and the review proved by executed mutation that
deleting `position: sticky` from both sticky rungs leaves it green while making the whole
CR-01 fix inert. In the same test, add assertions that the sticky rung's rule declares
`position: sticky`, that `.records-jump` declares `position: sticky`, that
`.splits-table__km` declares `position: sticky`, and that `:focus-visible` declares
`position: relative`. Point rung 4's z-index read at whichever selector now carries the
declaration.

Add one further assertion carrying the invariant this round's root cause teaches, phrased
against the confirmed hypothesis rather than generically. For H1 that invariant is: the
inner `.app-nav` rule must NOT declare `position: sticky` — a nested sticky inside a
zero-travel parent is the defect's exact shape and would reintroduce it. For H2/H4/H5,
assert the corrected declaration is present on the named selector and that the reverted
form is absent. Write a comment above the assertion naming GAP 7, the hypothesis id and
the date, so a future reader knows what it defends.

Watch each new assertion fail before it passes. Delete `position: sticky` from the sticky
rung, run `npx vitest run src/dashboard/styles.test.ts`, observe the failure, restore it,
observe green. Record both observed outputs in the summary — this is a mutation result, not
a claim that an assertion exists.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npx tsc --noEmit -p tsconfig.json && rm -f /tmp/bw14.log && npm run build-widgets > /tmp/bw14.log 2>&1; BW=$?; cat /tmp/bw14.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw14.log && node -e "const t=require('fs').readFileSync('src/dashboard/styles.test.ts','utf8');const lad=t.split(/it\('the sticky-layer ladder/)[1]||'';if(!lad)throw new Error('ladder test not found');const n=(lad.split('});')[0].match(/position: sticky/g)||[]).length;if(n<3)throw new Error('ladder test pins position: sticky on fewer than 3 rungs, found '+n);if(!/position: relative/.test(lad.split('});')[0]))throw new Error('ladder test does not pin the focus ring positioning precondition');if(!/GAP 7/.test(t))throw new Error('no assertion comment names GAP 7');console.log('ladder pins positioning precondition on all rungs')"</automated>
  </verify>
  <acceptance_criteria>
    - The edit applied matches exactly one row of the decision table, and the summary names the hypothesis id it came from and quotes the confirming field value from `19-GAP7-DIAGNOSIS.md`.
    - **Mutation, executed and recorded:** deleting `position: sticky` from the sticky rung's rule makes `npx vitest run src/dashboard/styles.test.ts` FAIL, and restoring it makes it pass. Both terminal outputs are quoted in the summary. An assertion that merely exists does not satisfy this criterion — the review proved the previous ladder assertion green under exactly this mutation.
    - **Mutation, executed and recorded:** deleting `z-index: 20` from the sticky rung still makes the ladder test fail (the pre-existing guard must not have been weakened by pointing rung 4 at a new selector).
    - The ladder test pins `position: sticky` on at least three rungs and `position: relative` on the focus ring, asserted by the gate's parse of the test body.
    - One assertion, commented with `GAP 7`, the hypothesis id and the date, carries the invariant the confirmed root cause teaches — for H1, that `.app-nav` does not itself declare `position: sticky`.
    - `npm run build-widgets` exits 0 with zero `css-syntax-error` occurrences, established with the redirect-and-capture gate form rather than a `cmd | tee log; ! grep` pipeline.
    - `npx vitest run src/dashboard/styles.test.ts` is green and `npx tsc --noEmit` exits 0.
    - `git diff src/dashboard/styles.css` touches only the sticky rung rule, the new rule if one was added, and the single rung-4 label line in the ladder comment. No other selector's declarations change.
    - `.app-nav`'s box properties (`padding`, `border-bottom`, `background`, `display`) are unchanged, or the summary names exactly which moved and states the effect on `updateJumpOffset`'s live height read.
  </acceptance_criteria>
  <done>The stylesheet carries the prescribed fix, the ladder test now fails when the positioning precondition is deleted, and both mutations were executed and their output recorded rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild, restage under the production path, and prepare the fix-confirmation probe</name>
  <files>.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md` § Probe E2 — the before/after snapshot shape and the "`after.sy` below 400 is a failed capture" invalidation rule this probe reuses
    - `.planning/phases/19-design-system-control-styling/19-12-PLAN.md` Task 1 § staging — the absolute-source-path symlink recipe and the `MOUNT_PREFIX` reason
    - `src/dashboard/styles.css` — the shipped state of the sticky rung after Task 1, so the probe reads the selector that actually carries it
  </read_first>
  <action>
Run the full gate and record every exit code and count: `npm test`, `npx tsc --noEmit -p
tsconfig.json`, `npm run build-widgets`, `npm run verify-dashboard`. All four must exit 0
before the checkpoint may open and `build-widgets` must emit zero `css-syntax-error`
occurrences. If `verify-dashboard` fails on stale data artifacts rather than a stylesheet
problem, run `npm run build` and `node dist/index.js compute-all-stats` first and record
that you did. If `npm test` reports the five known live-data file-level failures logged in
`deferred-items.md` (a worktree characteristic, `ENOENT` on `data/stats/*.json` at module
load), record the counts and proceed — but state explicitly that
`npx vitest run src/dashboard/styles.test.ts` is this plan's actual surface and is green.

Do not rewrite the gate into a `cmd | tee log; ! grep -q needle log` pipeline: the `;`
makes the grep the sole determinant of exit status and `cmd | tee f` reports `tee`'s status,
so a stale log answers for a run that never happened. `PIPESTATUS` is not the fix either —
the developer's shell is zsh, where it is `pipestatus` and 1-indexed.

Restage the freshly built output: capture the repository root into a variable while still
inside the repository, create `/tmp/gh-pages`, create the symlink
`/tmp/gh-pages/strava-widgets` pointing at the absolute `dist/widgets` path under that
captured root, confirm `/tmp/gh-pages/strava-widgets/index.html` resolves, then change
directory to `/tmp/gh-pages` and serve port 8099 with `python3 -m http.server`.

Add a `## Fix confirmation (Round 4)` section to `19-GAP7-DIAGNOSIS.md` containing Probe F,
its expected values, and two empty labelled slots for the recorded outputs.

Probe F is a single-paste console expression that reads `#app-nav-root` and `.app-nav`,
calls `scrollTo(0, 600)`, waits roughly 400ms, then returns one `JSON.stringify` carrying
`scrollY`, both elements' `getBoundingClientRect().top`, and both elements' computed
`position`. State the pass condition in the document as concrete numbers: `sy` at or above
400, and both `top` values at or below 1. State the failed-capture rule: `sy` below 400
means the route was too short and the run proves nothing.

State in the same section, in one sentence, why a source-level assertion cannot discharge
this claim: `styles.test.ts` runs in vitest's `node` environment with no DOM and no CSSOM,
so no assertion in this repository can observe whether a sticky element holds. That is the
whole reason Task 3 is a human checkpoint.
  </action>
  <verify>
    <automated>rm -f /tmp/bw14b.log && npm run build-widgets > /tmp/bw14b.log 2>&1; BW=$?; cat /tmp/bw14b.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw14b.log && npm run verify-dashboard && test -f /tmp/gh-pages/strava-widgets/index.html && readlink /tmp/gh-pages/strava-widgets | grep -q "^/" && node -e "const s=require('fs').readFileSync('.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md','utf8');const sec=s.split('## Fix confirmation (Round 4)')[1]||'';if(!sec.trim())throw new Error('Fix confirmation section missing');for(const f of ['scrollTo','app-nav-root','getBoundingClientRect'])if(!sec.includes(f))throw new Error('Probe F missing field or call: '+f);if(!/#\/list/.test(sec)||!/#\/records/.test(sec))throw new Error('Probe F does not name both routes');if(!/400/.test(sec)||!/(at or below 1|<= 1|less than or equal to 1)/.test(sec))throw new Error('Probe F pass condition is not stated as concrete numbers');console.log('probe F staged with numeric pass condition on both routes')" && grep -c "node" /dev/null; true</automated>
  </verify>
  <acceptance_criteria>
    - All four gate commands exit 0 with each exit code, the vitest counts and the `verify-dashboard` assertion count recorded in the summary; any known live-data file-level failures are named and separated from this plan's surface.
    - `npm run build-widgets` emits zero `css-syntax-error` occurrences, established with the redirect-and-capture form.
    - `test -f /tmp/gh-pages/strava-widgets/index.html` succeeds and `readlink /tmp/gh-pages/strava-widgets` prints an absolute path beginning with the repository root.
    - `19-GAP7-DIAGNOSIS.md` gains `## Fix confirmation (Round 4)` carrying Probe F as a single-paste expression, its numeric pass condition (`sy` at or above 400, both `top` values at or below 1), the failed-capture rule, and two labelled empty slots for `#/list` and `#/records`.
    - The section states in writing why no assertion in `styles.test.ts` can discharge this claim (vitest `node` environment, no DOM, no CSSOM).
    - `git status --porcelain src scripts` is empty — this task changes no source file.
  </acceptance_criteria>
  <done>A current build carrying the fix is served under `http://localhost:8099/strava-widgets/`, and the probe that will decide whether the fix actually works is written down with a numeric pass condition before anyone looks at the page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm on the rendered page that the nav now holds, on two routes</name>
  <files>.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md` § Fix confirmation (Round 4) — Probe F, its numeric pass condition and its failed-capture rule
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` § Round 3 Probe Outputs — the verbatim recording format
    - `.planning/STATE.md` § Accumulated Context — the 16-09 / 17-15 / 19-05 / 19-12 PARTIAL precedent, which governs what happens if this probe does not pass
  </read_first>
  <action>
Change no file under `src/` or `scripts/` in this task. Collect two Probe F outputs and
record them verbatim.

This is the fix-time rendered smoke check, not the phase gate. Plan 19-17 restates it as a
gate-quality row with its own PASS/FAIL verdict token, both themes and a full agenda. Here
the only question is numeric: did the nav stay at the top of the viewport after a
programmatic scroll, on both routes.

If either run fails the numeric condition, record it verbatim, do NOT patch the stylesheet
in this task, and report the plan as PARTIAL — the confirmed root cause did not produce the
predicted rendered behaviour, which means the diagnosis needs another round rather than the
fix needing a tweak. Patching under checkpoint pressure is the house rule's named failure
mode since 16-09.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/19-design-system-control-styling/19-GAP7-DIAGNOSIS.md','utf8');const sec=s.split('## Fix confirmation (Round 4)')[1]||'';const runs=[...sec.matchAll(/\{[^{}]*\"?sy\"?\s*:\s*(-?\d+(?:\.\d+)?)[^{}]*\}/g)];if(runs.length<2)throw new Error('expected two recorded Probe F outputs, found '+runs.length);for(const r of ['#/list','#/records'])if(!sec.includes(r))throw new Error('no recorded run labelled '+r);const tops=[...sec.matchAll(/\"?(?:navTop|rootTop)\"?\s*:\s*(-?\d+(?:\.\d+)?)/g)].map(m=>Number(m[1]));if(tops.length<4)throw new Error('expected navTop and rootTop on both routes, found '+tops.length+' values');console.log('two Probe F runs recorded; top values:',tops.join(' '))" && git status --porcelain src scripts | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - Probe F's output is recorded verbatim twice, one slot labelled `#/list` and one labelled `#/records`, each carrying `sy`, both `top` values and both computed `position` values.
    - **Rendered-evidence criterion, not a source assertion:** on both routes `sy` is at or above 400 and both recorded `top` values are at or below 1. A green `styles.test.ts` does not satisfy this and must not be cited as if it did.
    - Any run with `sy` below 400 is labelled a failed capture and accompanied by a valid re-run; it is never used as evidence.
    - If either route fails the numeric condition, the plan reports PARTIAL, records the outputs verbatim, changes no stylesheet, and states that the diagnosis needs another round.
    - `git status --porcelain src scripts` is empty at the end of the session.
    - The session ran against a URL beginning `http://localhost:8099/strava-widgets/`, recorded as such.
  </acceptance_criteria>
  <what-built>
The nav-disappears-upwards defect you reported in Round 3 was diagnosed by measurement in
the previous plan, and the fix that measurement prescribed has now been applied to
`src/dashboard/styles.css`. One stylesheet changed; no TypeScript, no markup.

Nothing about that proves it works. The last three times something in this phase was
declared fixed on the strength of the source file being correct, the page disagreed. So
this is one console paste per route, and the answer is a number, not an impression.
  </what-built>
  <how-to-verify>
**Staging.** The previous task rebuilt everything and started the server. Confirm the URL
bar contains `/strava-widgets/`. If it does not, stop and say so.

**Probe F on Activities.** Open `http://localhost:8099/strava-widgets/#/list`, scroll to the
very top, open the console, paste the Probe F snippet from `19-GAP7-DIAGNOSIS.md`, press
Enter. The page scrolls itself down by 600 pixels and prints a result about half a second
later. Paste the whole result back.

**Probe F on Records.** Go to `http://localhost:8099/strava-widgets/#/records`, scroll to the
very top, paste the same snippet, press Enter, paste the result back.

What a pass looks like: `sy` around 600, and both `rootTop` and `navTop` at 0 or very close
to it — the nav sitting flush against the top of the window while the content behind it has
moved. If `sy` comes back under 400 the page was too short to scroll and the run does not
count; tell me and we will re-run.

If you also want to say what you saw with your eyes — the nav stayed put, or it still slid
away — say it, and it will be recorded in your words. But the numbers are what decide this
task.

You are not being asked about themes, focus rings, opacity or paint order here. That is the
next session's agenda, and it only becomes answerable if this one passes.
  </how-to-verify>
  <resume-signal>Paste the two Probe F outputs, each labelled with its route. If either one shows the nav still leaving the top of the window, say so plainly — nothing will be patched in this session.</resume-signal>
  <done>Two verbatim Probe F outputs are recorded, both showing the nav pinned at the viewport top after a real scroll on a real page, or the plan is PARTIAL with the failing output recorded unpatched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a confirmed root cause → the edit actually applied | The decision table is the control: one hypothesis selects one prescribed edit, and three of the five rows are STOP rows rather than fixes, because not every cause is fixable inside a CSS-only fence. |
| a green source assertion → a rendered guarantee | `styles.test.ts` runs with no DOM and no CSSOM. Task 3's numeric probe is the only evidence permitted for the stickiness claim. |
| a fix edit → the Records jump bar's live-height coupling | `updateJumpOffset` reads `.app-nav`'s rendered height. An edit that changes that height silently mis-parks the jump bar on a route this plan does not otherwise touch. |
| checkpoint pressure → defect handling | A failing Probe F means the diagnosis was wrong, not that the fix needs a quick tweak. Patching in-session is the 16-09 failure mode. |

**Application-security surface: not applicable, stated rather than padded.** This plan
changes a static client-side stylesheet, a source-text test file and one planning document.
No new data flow, no authentication, session, access-control or cryptography surface, no
user input handling, no network egress beyond a localhost static server already present.
The assessment would change if a fix ever required a markup or TypeScript change (H3's STOP
row exists partly for that reason), or if a headless browser were installed to automate the
probe.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-F7-01 | Tampering (a fix chosen by plausibility rather than by evidence) | `src/dashboard/styles.css` | mitigate | The hard precondition blocks execution on INCONCLUSIVE; the decision table binds one hypothesis id to one edit and makes H3 (and load-bearing H2/H4) a STOP rather than a judgement call. |
| T-19-F7-02 | Repudiation (an existence-based guard passing off as a regression guard) | `src/dashboard/styles.test.ts` ladder test | mitigate | Two executed mutations are required and their terminal output quoted: deleting `position: sticky` must fail the suite, deleting `z-index: 20` must still fail it. R3-WR-01 proved the prior assertion green under the first. |
| T-19-F7-03 | Denial of service (a fix that silently mis-parks the Records jump bar) | `views/records.ts` `updateJumpOffset` | mitigate | `.app-nav`'s box properties must be unchanged or the change named with its effect stated; plan 19-17 row 22 re-observes the jump bar's ordering on the rendered page. |
| T-19-F7-04 | Repudiation (a source-only claim standing in for rendered behaviour) | Task 3 acceptance | mitigate | The acceptance criterion is numeric and comes from a probe on a served production-shaped build; the plan states in writing that a green suite cannot discharge it. |
| T-19-F7-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized. `python3 -m http.server` only. Do not install jsdom, puppeteer or playwright to automate Task 3 — this phase does not accept a headless substitute for rendered evidence. |
</threat_model>

<verification>
- `npx vitest run src/dashboard/styles.test.ts` green; `npx tsc --noEmit` exit 0; `npm run build-widgets` exit 0 with zero `css-syntax-error`; `npm run verify-dashboard` exit 0.
- Both required mutations executed, observed failing, reverted, and their output recorded.
- Probe F recorded verbatim on `#/list` and `#/records`, both meeting the numeric pass condition.
- `git diff src/dashboard/styles.css` confined to the sticky rung and one comment label line.
</verification>

<success_criteria>
The top nav stays on screen while a real page scrolls, on two routes, measured rather than
asserted; the fix applied is traceable to a confirmed root cause; and the ladder test now
fails when the positioning precondition that makes the whole ladder meaningful is deleted.
</success_criteria>

<output>
Create `.planning/phases/19-design-system-control-styling/19-14-SUMMARY.md` when done.
Record: the hypothesis id and confirming field value the fix came from; the exact diff of
`styles.css`; both mutation runs with their terminal output; whether any `.app-nav` box
property moved and its effect on `updateJumpOffset`; the four gate exit codes and counts;
the verbatim staging sequence; both Probe F outputs with the route labels; and whether the
plan is complete or PARTIAL.
</output>
