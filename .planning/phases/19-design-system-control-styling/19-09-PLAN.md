---
phase: 19-design-system-control-styling
plan: 09
type: execute
wave: 9
depends_on: ["19-06", "19-07", "19-08"]
files_modified:
  - .planning/phases/19-design-system-control-styling/19-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements: [UI-01, UI-02]
must_haves:
  decisions: [D-01, D-02, D-03, D-05, D-09, D-10, D-12]
  truths:
    - "The four rows that were blocked or failed have been re-observed by a human in a real browser, after the fixes, under a production-shaped /strava-widgets URL"
    - "The rounding a human judged is confirmed to be live rounding, by an in-browser probe of the computed value, not by trusting the source"
    - "Any remaining failure is recorded verbatim and unpatched, and nyquist_compliant flips only on a clean sweep of all four rows"
    - "The nine rows that passed at 19-05 are not re-litigated, and the global stacking change did not regress the Activities row-click reference pattern"
  artifacts:
    - path: ".planning/phases/19-design-system-control-styling/19-VALIDATION.md"
      provides: "Re-verification observations for rows 1, 3, 6 and 12, an updated Approval line, and a resolved nyquist_compliant value"
      contains: "nyquist_compliant"
    - path: ".planning/REQUIREMENTS.md"
      provides: "UI-01 and UI-02 status resolved from the re-verification outcome"
      contains: "UI-01"
  key_links:
    - from: "19-VALIDATION.md Gap-Closure Record"
      to: "the four re-verified rows"
      via: "each GAP entry resolved or restated with a new dated observation"
      pattern: "Gap-Closure Record"
    - from: "the built bundle under /strava-widgets"
      to: "a live --radius-control value of 4px"
      via: "an in-browser getComputedStyle probe run before any row is judged"
      pattern: "radius-control"
---

<objective>
Re-observe, in a real browser, the four rows that could not be signed off at 19-05 — and only those four.

Purpose: `nyquist_compliant` is `false` and the phase gate is shut. Rows 1, 3 and 12 were judged against square controls whose rounding was inert, so those observations are recorded as BLOCKED BY DEFECT rather than passes. Row 6 failed outright. Plans 19-06 through 19-08 changed what the browser parses and what paints on top of what; none of those changes is provable by any test in this repository. This checkpoint is the only instrument that can close UI-01 and UI-02.

Output: four new dated observations, a resolved `Approval:` line, a `nyquist_compliant` value consistent with it, and UI-01/UI-02 statuses in `REQUIREMENTS.md` that match what was actually seen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-design-system-control-styling/19-VALIDATION.md
@.planning/phases/19-design-system-control-styling/19-VERIFICATION.md
@.planning/phases/19-design-system-control-styling/19-05-PLAN.md
@.planning/phases/19-design-system-control-styling/19-CONTEXT.md
</context>

<scope_fence>
**Four rows. Not thirteen.** Rows 1, 3, 6 and 12 of `19-VALIDATION.md` § Manual-Only Verifications are the entire agenda. Rows 2, 4, 5, 7, 8, 9, 10, 11 and 13 passed at 19-05 on their own terms, none of them depends on `--radius-control` or on paint order, and re-running them would invite re-litigating settled observations and inflate an already long session. Do not re-run them and do not overwrite their observation cells.

**Three known non-Phase-19 issues will be encountered during this session.** They are logged in `19-VALIDATION.md` § Non-Phase-19 Issues and are out of scope: a WebKit-worded `SyntaxError: Unexpected token '{'` on every page load that the developer could not localise (all 33 built bundles pass `node --check`); Records "View Activity" links not navigating despite well-formed `#/activity/…` hrefs; and the Calendar month picker rendering as a plain wide text field because Safari does not support `<input type="month">` (`calendar.ts:280`). Phase 19 shipped exactly `src/dashboard/styles.css` and `src/dashboard/styles.test.ts`, so none of them can originate here. Note them if they recur, do not count them against any row, and do not fix them in this plan.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Run the full gate, stage the build under a production-shaped path, and prepare the objective probe</name>
  <files>.planning/phases/19-design-system-control-styling/19-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` § Human Checkpoint Staging — the sequence 18-16 and 19-05 used, including the `MOUNT_PREFIX` rationale
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` § Manual-Only Verifications rows 1, 3, 6, 12 — the four Observation cells being replaced, including the exact BLOCKED BY DEFECT wording that must be preserved as history
    - `.planning/phases/19-design-system-control-styling/19-06-SUMMARY.md`, `19-07-SUMMARY.md`, `19-08-SUMMARY.md` — what actually changed, so the checkpoint brief describes the shipped state rather than the planned one
    - `scripts/verify-dashboard-publish.mjs` — its `MOUNT_PREFIX` constant, so the summary can state the prefix the publish check asserts
    - `.planning/STATE.md` § Accumulated Context — the 16-09, 17-15 and 19-05 PARTIAL records, the wording precedent this checkpoint's result must match
  </read_first>
  <action>
Run the full gate from a clean working tree and record every exit code and count: `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets`, `npm run verify-dashboard`. All four must exit 0 before the checkpoint may open. `npm run build-widgets` must additionally emit **zero** `css-syntax-error` occurrences — at 19-05 that warning was present and was misclassified as non-blocking, which is GAP 3; its absence is now a gate condition, not a nicety. If `verify-dashboard` fails on stale data artifacts rather than a stylesheet problem, run `npm run build` and `node dist/index.js compute-all-stats` first, as plan 18-16 did, and record that you did so.

Stage the built output under a production-shaped path. **Use an absolute source path.** At 19-05 the developer's symlink broke because `ln -sfn "$PWD/dist/widgets" …` was run after `cd /tmp/gh-pages`, so `$PWD` resolved to the staging directory rather than the repository. Capture the repository root into a variable while still in it, create the symlink using that variable, and only then change directory to serve:

- capture the repo root before changing directory
- `mkdir -p /tmp/gh-pages`
- create the symlink `/tmp/gh-pages/strava-widgets` pointing at the absolute `dist/widgets` path under the captured root
- verify the link resolves — listing `/tmp/gh-pages/strava-widgets/index.html` must succeed — **before** starting the server
- then change directory to `/tmp/gh-pages` and serve on port 8099

Serving at the bare server root is not acceptable. `scripts/verify-dashboard-publish.mjs` carries `MOUNT_PREFIX = '/strava-widgets'` because Phase 16 shipped a black page behind a 15/15 green gate that only mounted at the root.

Prepare `19-VALIDATION.md` for the checkpoint without deciding its outcome:
1. In each of the four Observation cells for rows 1, 3, 6 and 12, keep the existing 19-05 text as history and append a clearly marked `**Re-verification {date}:** _pending_` sentinel. Do not delete the BLOCKED BY DEFECT and FAILS wording — the record of a defect having shipped is worth more than a tidy cell.
2. Leave the other nine Observation cells untouched.
3. Leave `nyquist_compliant: false` and do not alter the `Approval:` line. Task 3 resolves both.
4. Do not modify `.planning/REQUIREMENTS.md` in this task.

Record the exact staging commands you ran, verbatim, in the SUMMARY, so the next checkpoint can copy a sequence that is known to work rather than rediscover the `$PWD` trap.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json && npm run build-widgets 2>&1 | tee /tmp/bw9.log && npm run verify-dashboard; ! grep -q 'css-syntax-error' /tmp/bw9.log && test -f /tmp/gh-pages/strava-widgets/index.html && echo "staging link resolves"</automated>
  </verify>
  <acceptance_criteria>
    - All four gate commands exit 0, with each exit code, the vitest passing count and the `verify-dashboard` assertion count recorded in the SUMMARY.
    - `npm run build-widgets` output contains zero occurrences of `css-syntax-error`, recorded explicitly as a contrast with the warning present at 19-05.
    - `test -f /tmp/gh-pages/strava-widgets/index.html` succeeds, proving the symlink resolves to the repository's `dist/widgets` and not to a path relative to the staging directory.
    - `readlink /tmp/gh-pages/strava-widgets` prints an absolute path beginning with the repository root.
    - The four target Observation cells each retain their original 19-05 text and carry an appended, clearly marked re-verification sentinel; the other nine cells are byte-identical to before: `git diff .planning/phases/19-design-system-control-styling/19-VALIDATION.md` shows changes confined to those four rows.
    - `grep -c 'nyquist_compliant: false' .planning/phases/19-design-system-control-styling/19-VALIDATION.md` returns `1` at the end of this task.
    - `git status --porcelain src scripts .planning/REQUIREMENTS.md` is empty — this task changes no source file and no requirement status.
    - The verbatim staging command sequence is in the SUMMARY, using an absolute source path.
  </acceptance_criteria>
  <done>The gate is green with the GAP 3 warning gone, the build is served under `http://localhost:8099/strava-widgets/` from a symlink proven to resolve, and the validation record is staged for four new observations with its history intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-verify rows 1, 3, 6 and 12 in a real browser</name>
  <files>.planning/phases/19-design-system-control-styling/19-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` § Manual-Only Verifications rows 1, 3, 6, 12 as staged by Task 1, and § Gap-Closure Record GAP 1 and GAP 2
    - `.planning/phases/19-design-system-control-styling/19-06-SUMMARY.md` — the comment repair and the parse gate, needed to describe what rows 1, 3 and 12 are now looking at
    - `.planning/phases/19-design-system-control-styling/19-07-SUMMARY.md` — the stacking fix and its blast-radius audit, needed to judge row 6 and to know what the Activities tripwire is guarding
    - `src/dashboard/styles.css` in its final shipped state — so any reported defect can be tied to a specific rule rather than a screenshot
    - `.planning/STATE.md` § Blockers/Concerns — the 16-09 / 17-15 / 19-05 PARTIAL wording this result must match
  </read_first>
  <action>
Do not write or change any file under `src/` or `scripts/` in this task. Walk the four rows in `<how-to-verify>`, record a **verbatim observation** for each in `19-VALIDATION.md`, and set nothing else.

**Run the objective probe first.** Rows 1, 3 and 12 were previously judged under a condition the developer had no way to see. Before any visual judgement, confirm in the browser console that `--radius-control` now resolves and that a sampled control actually computes a 4px radius. If the probe does not return `4px`, stop — do not judge the rows, record the probe output verbatim and mark the checkpoint PARTIAL. A row judged under an unverified precondition is exactly what produced this gap-closure pass.

**Checkpoint result protocol — house rule since 16-09, reaffirmed by 17-15 and 19-05.** If any row fails, record it verbatim in the developer's own words, mark the checkpoint **PARTIAL**, and leave `nyquist_compliant: false`. Defects are never patched under checkpoint pressure: fix nothing in this task, and do not paraphrase a failure into something that sounds smaller than it is. `approved` is permitted only when all four rows pass and the objective probe returned `4px`.
  </action>
  <verify>
    <automated>node -e "const p='.planning/phases/19-design-system-control-styling/19-VALIDATION.md';const s=require('fs').readFileSync(p,'utf8');if(/Re-verification[^|]*_pending_/.test(s))throw new Error('a re-verification sentinel is still pending');const a=(s.split('Approval:')[1]||'').split('\n')[0];if(!/approved|PARTIAL/.test(a))throw new Error('no verdict on the Approval line');const t=s.split('## Manual-Only Verifications')[1].split('###')[0];const rows=t.split('\n').filter(l=>/^\|/.test(l)&&!/^\|\s*-+/.test(l)&&!/^\|\s*Behavior/.test(l));if(rows.length!==13)throw new Error('expected 13 manual rows, found '+rows.length);console.log('verdict recorded, all four sentinels resolved, 13 rows intact')"</automated>
  </verify>
  <acceptance_criteria>
    - The browser probe output is recorded verbatim, and it shows `--radius-control` resolving to `4px` and a sampled control computing `border-radius: 4px`. If it does not, the checkpoint is PARTIAL and no row is judged.
    - All four target rows carry a new, dated re-verification observation of at least a full sentence. No sentinel reading `_pending_` remains.
    - The other nine Observation cells are unchanged: `git diff .planning/phases/19-design-system-control-styling/19-VALIDATION.md` touches only rows 1, 3, 6, 12, the `Approval:` line and the frontmatter.
    - The row 6 observation names, separately: the detail view's x-axis segmented control (both the bottom edge against the first chart and the right edge against the sibling "Time" option), light theme and dark theme, whether the ring is doubled, and whether the Activities pagination ring still renders — the two sub-checks that passed at 19-05 and must not have regressed.
    - The row 3 observation states whether the three-plus class-less calendar buttons named in row 3's amended instructions look consistent with the twelve classed treatments.
    - The Activities regression tripwire result is recorded — whether a row click still navigates after the global stacking change. This is a tripwire, not a re-run of row 8.
    - The `Approval:` line reads `approved {date}` or `PARTIAL {date}` and no longer reads the 19-05 verdict verbatim without a re-verification note.
    - `nyquist_compliant: true` appears only if all four rows passed and the probe returned `4px`.
    - `git status --porcelain src scripts` is empty — no source file was modified during the checkpoint.
    - The session ran against a URL beginning `http://localhost:8099/strava-widgets/`, recorded as such.
  </acceptance_criteria>
  <what-built>
Since the 19-05 checkpoint, three plans changed exactly two source files. No TypeScript view file was touched, so every difference you will see reaches the screen by cascade onto markup that already existed.

- **The dead token is alive (GAP 1, rows 1, 3, 12).** A comment in `styles.css` contained the sequence `*/` inside the prose `--space-*/--zone-*/--load-*`, which terminated it early; CSS error recovery then discarded `--radius-control: 4px` entirely. Every input, every button, and both ends of every segmented control were rendering `border-radius: 0` — including everything you looked at and approved on rows 1, 3 and 12. The comment is reworded; the token now parses. Five rules depend on it, so this is the first time the intended 4px control radius has ever rendered.
- **The build was already telling us (GAP 3).** `npm run build-widgets` emitted `▲ [WARNING] Expected ":" [css-syntax-error]` at line 56 and it was filed as a harmless comment warning. It was the defect's signature. That warning is now gone, and the test suite gained a gate that parses the stylesheet rather than pattern-matching its text — it was watched failing against the real defect before it was allowed to pass.
- **The focus ring should now paint on top (GAP 2, row 6).** Your words were: "The ring appears hidden partially in the bottom by the first chart and to the right by 'Time'." That was not clipping — plan 19-04 had already removed the `overflow: hidden` that clips. The ring painted, and then other things painted over it: the sibling "Time" button is later in the document so it covered the right edge, and the chart's canvas wrapper is a positioned element so it covered the bottom edge. A focused element now establishes its own layer above both. It stays below the Records sticky jump bar deliberately.
- **Container and children now agree (WR-01).** `.segmented`'s own corner radius was a hardcoded `4px` while its end children used the broken token, so the container drew a rounded corner around square buttons. Both now use the same token.
- **The calendar's unclassed buttons (WR-02).** Three or more `<button>` elements carry no class at all — the multi-run picker's Close button and the two month-navigation buttons. The bare `button` baseline is the first CSS ever applied to them, so their font size, height and corners genuinely changed. That is intended, but the comment claiming "nothing currently rendered changes shape or color" was false and has been corrected. They are named in row 3's instructions so you can look at them.
- **Test-tooling hardening (WR-03).** A helper split selectors on every comma, including commas inside `:not(...)`, which made one disabled-state assertion pass by coincidence. It now splits only at the top level. No behaviour you can see changed.
  </what-built>
  <how-to-verify>
**Staging.** Task 1 has already run the four gate commands and created the symlink. Confirm the server is up and open the screens with devtools Console visible for the whole session:

- `http://localhost:8099/strava-widgets/#/` (Overview)
- `http://localhost:8099/strava-widgets/#/list` (Activities)
- `http://localhost:8099/strava-widgets/#/calendar` (Calendar)
- `http://localhost:8099/strava-widgets/#/records` (Records)
- `http://localhost:8099/strava-widgets/#/trends` (Trends)
- a detail view reached by clicking any Activities row — this is where the segmented control and the charts live

Not a root-served build. If the URL does not contain `/strava-widgets/`, stop and restage.

**Step 0 — objective probe, before any looking.** In the console, read the computed value of `--radius-control` from the document element, and read the computed `border-radius` of a sampled control — `button.app-nav__toggle` is the element the 19-05 probe sampled, so using it again makes the two runs directly comparable. Paste the result verbatim.

The 19-05 run returned `{radiusControl: "(EMPTY)", radiusPanel: "8px", sample: "button.app-nav__toggle", borderRadius: "0px"}`. This run must return a non-empty `4px` for `radiusControl` and `4px` for the sampled `borderRadius`. **If it does not, stop here.** Record the output, mark PARTIAL, and do not judge rows 1, 3 or 12 — the whole reason this pass exists is that those rows were once judged against a precondition nobody had checked.

**Row 1 — every input renders intentionally (UI-01).** Load all five screens and inspect every text, date, number and search input. Activities is the dense one: ten of the thirteen control sites live there — two date inputs, four number inputs, two text inputs, one search. Confirm none reads as an unstyled browser default, and that the corners are now visibly rounded rather than square. Your previous answer was "sure", given while every one of them was square. Say what you actually see this time.

**Row 3 — buttons and selects read as one treatment (UI-02).** Compare the twelve button treatments across the five screens: `.cta`, `.filter-toggle`, `.preset-chip`, `.pagination__button`, `.segmented__option`, `.chip__remove`, `.chip-clear-all`, `.records-jump__link`, `.activity-table__sort-button`, `.calendar-day`, `.theme-toggle`, `.app-nav__toggle`. All of them were rounding to 0 when you last judged them.

Then look specifically at the Calendar's **unclassed** buttons, which no checkpoint row has ever named: the multi-run day picker's "Close" button, and the two month-navigation buttons either side of the month label. These receive their first-ever CSS from the bare `button` baseline — a larger font, a 32px height floor and rounded corners. Confirm they look deliberate next to the twelve classed treatments, and say so if they look oversized or out of place.

**Row 6 — the focus ring renders fully unoccluded (UI-02).** This is the row that failed outright. Tab through every control on all five screens **and the detail view**, in **light theme and again in dark theme**. Check light theme first: its ring/background contrast is 3.40:1 against the 3:1 floor, versus 6.02:1 in dark, so it is the pair most likely to look technically-passing-but-weak.

Focus specifically on the detail view's x-axis segmented control, which is where it failed:
- With "Distance" focused, is the **bottom** edge of the ring still covered by the first chart?
- With "Distance" focused, is the **right** edge still covered by the "Time" option?
- Then focus "Time" and check its edges too.

Also confirm the two sub-checks that **passed** last time have not regressed: the ring is still clearly visible around the current pagination page on Activities (the solid `--accent-strong` fill), and there is exactly **one** ring — no thin extra outer line, which would mean the browser's own outline is painting alongside the box-shadow. Finally, check the Records jump list and the Splits table horizontal scrollers, which you did not separately report last time.

**Row 12 — the segmented control's silhouette (UI-02).** Its rounded corners come from `:first-child`/`:last-child` radii plus the container's own border and radius, not from clipping. Last time you said "all good" while the whole control was uniformly square, so the square-inner-corner condition this row exists to catch was invisible. Now that both container and children round, look closely at all four corners in both themes: any square inner corner, any visible seam or sliver between the container's border and the end options, and any change to the divider between the two options.

**Regression tripwire — not a row.** Plan 19-07 changed the focus ring globally, which touches every focusable element in the dashboard including Activities. While you are on Activities for row 3, click one row and confirm it still navigates to the detail view. This is a one-action tripwire on a global change, not a re-run of row 8, which passed and is not being re-litigated.

**Rows you are not being asked about.** Rows 2, 4, 5, 7, 8, 9, 10, 11 and 13 passed at 19-05 and are out of scope. If something on one of them looks wrong, say so — it will be logged as a new finding, not folded into this pass.

**Known issues you will hit that are not Phase 19's.** A `SyntaxError: Unexpected token '{'` in the console on every page load; Records "View Activity" links not navigating; the Calendar month picker being a plain wide text box. All three are logged in `19-VALIDATION.md` § Non-Phase-19 Issues. Confirm whether each still occurs, but none of them counts against any row.

**Then record the outcome.** Fill the four re-verification cells with what you actually saw. Follow the 16-09 / 17-15 / 19-05 precedent exactly: if any row fails, record the checkpoint as **PARTIAL** and log the failure verbatim as gap-closure work rather than patching it now.
  </how-to-verify>
  <resume-signal>Type "approved" only if the probe returned 4px and all four rows (1, 3, 6, 12) passed. Otherwise describe each failure verbatim, naming its row number. Anything less than all four passing is PARTIAL, not approved.</resume-signal>
  <done>Rows 1, 3, 6 and 12 each carry a new dated verbatim observation, the objective probe result is on the record, and `19-VALIDATION.md` carries an explicit `approved` or `PARTIAL` verdict with every failure logged unpatched.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve the sign-off, the gap register and the requirement statuses</name>
  <files>.planning/phases/19-design-system-control-styling/19-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` as filled by Task 2 — the four observations, the Approval line, the Validation Sign-Off checklist and the existing Gap-Closure Record
    - `.planning/REQUIREMENTS.md` lines 14-16, 47 and 75-78 — the UI-01 and UI-02 checkbox entries with their "reverted from complete" annotations, and the phase status table rows
    - `.planning/STATE.md` § Accumulated Context — the wording precedent for recording a PARTIAL, and the Phase 19 entry that will need to reflect the new outcome
  </read_first>
  <action>
Resolve the record from Task 2's outcome. Fix no defect in this task.

1. **Sign-off.** Tick the Validation Sign-Off items actually delivered. Set `nyquist_compliant: true` in the frontmatter **only** if the verdict is `approved`; leave it `false` for `PARTIAL`.
2. **Gap-Closure Record.** Update the existing GAP 1 and GAP 2 entries in place rather than deleting them: append a dated resolution line to each stating whether the re-verification confirmed closure, quoting the relevant new observation. Do the same for GAP 3, whose closure is the absent `css-syntax-error` in Task 1's build output plus the parse gate from plan 19-06. If any row still fails, add a new numbered gap entry in the same shape as the existing ones — verbatim quote, blocked requirement, implicated decisions, and no suggested fix or root-cause theory.
3. **Requirement statuses in `REQUIREMENTS.md`.** UI-01 depends on rows 1-2; UI-02 on rows 3-6 and 10-13. Rows 2, 4, 5, 10, 11 and 13 already passed at 19-05. Therefore a clean sweep of rows 1, 3, 6 and 12 clears both. On a clean sweep: tick UI-01 and UI-02, remove the "reverted from complete" annotation from UI-02's entry replacing it with a dated re-verification note, and set both phase-status table rows to `Complete`. On anything less: change nothing except to update the parenthetical reason so it names the row that is still open. Never tick a requirement whose mapped rows did not all pass.
4. **Record what this gap-closure pass taught.** Add a short note to the validation record stating the two mechanisms that produced the original miss, so a future phase inherits them: a source-text assertion cannot see a parse result, and a build warning classified as cosmetic was the defect's only automated signal. Name the mitigations now in place — the parse-level gate and the `css-syntax-error` gate condition.
5. If the verdict is `PARTIAL`, state plainly in the SUMMARY that the phase gate does not close and that another `/gsd-plan-phase 19 --gaps` pass is required.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/19-design-system-control-styling/19-VALIDATION.md';const s=fs.readFileSync(p,'utf8');const a=(s.split('Approval:')[1]||'').split('\n')[0];const ok=/approved/.test(a),partial=/PARTIAL/.test(a);if(!ok&&!partial)throw new Error('no verdict');if(ok&&!/nyquist_compliant: true/.test(s))throw new Error('approved but nyquist_compliant is not true');if(partial&&!/nyquist_compliant: false/.test(s))throw new Error('PARTIAL but nyquist_compliant is true');if(!/## Gap-Closure Record/.test(s))throw new Error('Gap-Closure Record missing');const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const ui1=/- \[x\] \*\*UI-01\*\*/.test(r),ui2=/- \[x\] \*\*UI-02\*\*/.test(r);if(ok&&!(ui1&&ui2))throw new Error('approved but UI-01/UI-02 not both ticked');if(partial&&(ui1&&ui2))throw new Error('PARTIAL but UI-01 and UI-02 are both ticked');console.log('sign-off, gap register and requirement statuses are consistent with the verdict:',ok?'approved':'PARTIAL')"</automated>
  </verify>
  <acceptance_criteria>
    - `nyquist_compliant` is `true` only when the `Approval:` line says `approved`, and `false` otherwise.
    - UI-01 and UI-02 are ticked in `REQUIREMENTS.md` only on a clean sweep of rows 1, 3, 6 and 12; the two phase-status table rows agree with the checkboxes.
    - The GAP 1, GAP 2 and GAP 3 entries each carry a dated resolution line quoting the evidence, and none of the original text was deleted.
    - If PARTIAL: a new gap entry exists per still-failing row, each with a verbatim quote, the blocked requirement, the implicated decision IDs, and **no** suggested fix or root-cause theory.
    - The lessons note names both mechanisms and both mitigations.
    - `git status --porcelain src scripts` is empty — no source file was modified by this plan at all.
    - `npm test` still exits 0 and `npm run build-widgets` still emits no `css-syntax-error`, confirming nothing slipped in during the checkpoint session.
  </acceptance_criteria>
  <done>The validation record and `REQUIREMENTS.md` both state exactly what a human confirmed, `nyquist_compliant` matches the verdict, the gap register shows each gap resolved or restated, and no defect was patched under checkpoint pressure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → production URL shape | Serving at the bare server root reproduces the class of defect that shipped Phase 16's black page behind a 15/15 green gate. |
| green automated gate → phase sign-off | Four green commands were green at 19-05 too, against a stylesheet that discarded a token. Green is the precondition for opening this checkpoint, never a substitute for it. |
| human judgement → phase gate | This checkpoint is the only instrument that can observe the rendered outcome of GAP 1's and GAP 2's fixes. |
| unverified precondition → human judgement | The specific failure of 19-05: three rows were judged confidently against a broken condition the developer had no way to see. The Step 0 probe exists to close this boundary before any looking happens. |
| checkpoint pressure → defect handling | Fixing a small defect mid-session is how a phase absorbs a gap instead of recording it. |

**Application-security surface: not applicable, stated rather than padded.** This plan changes two planning documents and no source file. No input handling (ASVS V5), authentication (V2), session management (V3), access control (V4) or cryptography (V6) surface is involved. No package is installed. No private data file is staged — unlike plan 18-16, this checkpoint needs no temporary private config, so that precedent's `data/private/` exposure path does not apply.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19G-VERIFY-14 | Repudiation (false-green gate) | serving the build at the server root | mitigate | Task 1 stages under `/strava-widgets` matching `verify-dashboard-publish.mjs`'s `MOUNT_PREFIX`, using an absolute source path to avoid the `$PWD` trap that broke the 19-05 symlink; the link is proven to resolve before the server starts, and the recorded URL must begin `http://localhost:8099/strava-widgets/`. |
| T-19G-VERIFY-15 | Repudiation (a row judged under an unverified precondition) | rows 1, 3 and 12, which were previously passed against inert rounding | mitigate | Step 0 requires an in-browser computed-value probe returning `4px` before any row is judged, directly comparable to the 19-05 probe that returned `(EMPTY)`/`0px`. A probe failure stops the checkpoint rather than merely being noted. |
| T-19G-VERIFY-16 | Repudiation (paraphrased or softened failure) | checkpoint recording | mitigate | Each of the four rows requires a new dated verbatim observation of at least a full sentence, asserted programmatically via the pending-sentinel check; Task 3 forbids adding a fix or root-cause theory to the gap register. |
| T-19G-VERIFY-17 | Repudiation (silent absorption of a defect) | fixing something during the checkpoint | mitigate | Tasks 2 and 3 both forbid modifying anything under `src/` or `scripts/` and assert `git status --porcelain src scripts` is empty; Task 3 re-runs `npm test` and the `css-syntax-error` check after sign-off. |
| T-19G-VERIFY-18 | Repudiation (scope drift re-litigating settled rows) | the nine rows that passed at 19-05 | mitigate | The `<scope_fence>` limits the agenda to four rows and Task 2's acceptance criteria require the diff to touch only those four Observation cells, the Approval line and the frontmatter. New findings on other rows are logged as new findings, not folded into this pass. |
| T-19G-A11Y-19 | Denial of Service (keyboard users still lose the focus indicator) | the ring on the detail view's segmented control | mitigate | Row 6 checks each edge of each option against each named occluder, in both themes, and re-confirms the two sub-checks that passed at 19-05 so the stacking fix cannot have traded one failure for another. There is no automated substitute. |
| T-19G-ACT-20 | Tampering (global stacking change regressing the Phase 20 reference pattern) | Activities row-click, unmodified by design and unobservable by any test | mitigate | A one-action regression tripwire is embedded in the row 3 session and its result is a required acceptance criterion. Row 8 itself is not re-run; the tripwire plus plan 19-07's written blast-radius audit are the guard. |
| T-19G-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized in this plan. The server is `python3 -m http.server`, already present. Do not `npm install` a static server, and do not install jsdom, puppeteer or playwright to "automate" any row here — `19-VALIDATION.md`'s two-mechanism constraint forbids it and no automated substitute for these four rows exists. |
</threat_model>

<verification>
- All four gate commands exit 0 and `build-widgets` emits zero `css-syntax-error` occurrences.
- `readlink /tmp/gh-pages/strava-widgets` prints an absolute path under the repository root, and `/tmp/gh-pages/strava-widgets/index.html` exists.
- The in-browser probe result is recorded verbatim and shows `--radius-control` resolving to `4px`.
- All four re-verification sentinels are resolved; no `_pending_` remains; the Manual-Only table still has 13 rows.
- The diff to `19-VALIDATION.md` from Task 2 touches only rows 1, 3, 6, 12, the Approval line and the frontmatter.
- `nyquist_compliant` matches the verdict, and `REQUIREMENTS.md`'s UI-01/UI-02 statuses match the rows that actually passed.
- `git status --porcelain src scripts` is empty after both Task 2 and Task 3.

**Note for the phase gate:** UI-03 and ACT-01 were independently corroborated in `19-VERIFICATION.md` on evidence distinct from the compromised checkpoint and are not re-verified here. Their statuses must remain `Complete` and must not be altered by this plan.
</verification>

<success_criteria>
The four rows that could not be signed off at 19-05 have been re-observed by a human in a real browser under a production-shaped `/strava-widgets` URL, after an objective probe confirmed the precondition that invalidated three of them is genuinely fixed — and `nyquist_compliant`, the `Approval:` line and `REQUIREMENTS.md` all state exactly what was seen, with any remaining failure recorded verbatim and unpatched.
</success_criteria>

<output>
Create `.planning/phases/19-design-system-control-styling/19-09-SUMMARY.md` when done. Record: the four gate exit codes with the vitest and `verify-dashboard` counts and explicit confirmation that no `css-syntax-error` was emitted; the verbatim staging commands using the absolute source path; the verbatim browser probe output alongside the 19-05 probe for comparison; all four new observations verbatim; the Activities regression tripwire result; whether each of the three known non-Phase-19 issues still occurs; the verdict; the resulting `nyquist_compliant` value and the UI-01/UI-02 statuses written to `REQUIREMENTS.md`; and — if PARTIAL — that the phase gate does not close and another gap-closure pass is required.
</output>
