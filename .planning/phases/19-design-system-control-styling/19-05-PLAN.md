---
phase: 19-design-system-control-styling
plan: 05
type: execute
wave: 5
depends_on: ["19-01", "19-02", "19-03", "19-04"]
files_modified:
  - .planning/phases/19-design-system-control-styling/19-VALIDATION.md
autonomous: false
requirements: [UI-01, UI-02, UI-03, ACT-01]
must_haves:
  decisions: [D-02, D-06, D-07, D-08, D-09, D-10, D-11, D-14]
  truths:
    - "Every rendering claim this phase makes has been looked at by a human in a real browser, under a production-shaped /strava-widgets URL"
    - "The focus ring was checked by tabbing through controls in both themes, with light theme scrutinised first"
    - "Activities' row-click interaction model was exercised by mouse and by keyboard and confirmed unchanged"
    - "The validation record states which requirement IDs were confirmed clean and logs every failure verbatim as gap-closure work"
  artifacts:
    - path: ".planning/phases/19-design-system-control-styling/19-VALIDATION.md"
      provides: "Completed per-task verification map, the filled Manual-Only Verifications table, and the checkpoint verdict"
      contains: "nyquist_compliant"
  key_links:
    - from: "19-VALIDATION.md § Manual-Only Verifications"
      to: "the checkpoint agenda in this plan"
      via: "every row resolved with a verbatim observation, not a tick"
      pattern: "Manual-Only Verifications"
---

<objective>
Close the phase with the only verification the automated suite structurally cannot perform: a human, in a real browser, under a production-shaped `/strava-widgets` URL — then reconcile the validation record with what actually shipped.

Purpose: this is the phase's real gate. **Phase 16 shipped a black page behind a 15/15 green gate. Phase 16's `16-09` and Phase 17's `17-15` each came back PARTIAL with real rendering defects, two of which are still OPEN.** This project has now shipped rendering defects behind a fully green automated gate three times. Phase 19 is maximally exposed to that class: `vitest` runs `environment: 'node'`, there is no jsdom, no happy-dom, no @testing-library, no puppeteer and no playwright anywhere in the dependency tree, and this phase authorizes no new test dependency. Every automated check in this phase is a text assertion over the literal characters of `styles.css`. A green suite proves a rule exists in the source and proves **nothing** about computed styles, rendered boxes, native picker chrome, perceptual contrast, or click behaviour.
Output: a completed validation record carrying an explicit verdict of `approved` or `PARTIAL`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-design-system-control-styling/19-VALIDATION.md
@.planning/phases/19-design-system-control-styling/19-UI-SPEC.md
@.planning/phases/19-design-system-control-styling/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated gate and reconcile the validation record</name>
  <files>.planning/phases/19-design-system-control-styling/19-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` — the whole file: the Per-Task Verification Map with its seven `TBD` plan cells, the Wave 0 Requirements section, the Manual-Only Verifications table, the Contrast requirement sub-section, the Human Checkpoint Staging block and the Validation Sign-Off checklist
    - Every `19-0*-SUMMARY.md` from plans 01 through 04 — the rules actually written, the two derived corrections to D-06 recorded by plan 19-03, the `.stub-panel` side effect recorded by plan 19-01, the segmented silhouette-mechanism call-out and the mutation-check results recorded by plan 19-04
    - `scripts/verify-dashboard-publish.mjs` — specifically its `MOUNT_PREFIX` constant, so the summary can state which prefix the publish check actually asserts
  </read_first>
  <action>
Run the full gate from a clean working tree and record every exit code and count:

`npm test`, then `npx tsc --noEmit -p tsconfig.json`, then `npm run build-widgets`, then `npm run verify-dashboard`.

All four must exit 0 before the checkpoint in Task 2 may open. If `npm run verify-dashboard` fails on stale data artifacts rather than on a stylesheet problem, run `npm run build` and `node dist/index.js compute-all-stats` first — the sequence plan 18-16 used — and record that you did so.

Then update `.planning/phases/19-design-system-control-styling/19-VALIDATION.md`:

1. **Per-Task Verification Map** — replace every `TBD` in the `Plan` column with the real plan number, and correct the `Wave` column to the waves that actually shipped (19-01 wave 1, 19-02 wave 2, 19-03 wave 3, 19-04 wave 4, this plan wave 5). The seeded map assumed all four implementation rows were wave 1; they are not, because every plan writes to `src/dashboard/styles.css` and concurrent writes were sequenced deliberately. Fill the `Threat Ref` column from each plan's `<threat_model>` rather than leaving `—`. Set each row's `Status` to ✅ green, or ❌/⚠️ with a note.
2. **Add two rows the seeded map does not contain**, both from plan 19-03's derived corrections to D-06: the `:where(:not(...))` hover exclusion list, and the `.cta:hover` repair. Mark both `manual only` for their rendered outcome, referencing threat `T-19-CASCADE-09` and `T-19-SPEC-13`.
3. **Manual-Only Verifications** — extend the table from nine rows to thirteen by appending the four new rows enumerated in Task 2's `<how-to-verify>` (rows 10-13). Leave every observation cell empty; Task 2 fills them.
4. **Contrast requirement sub-section** — leave the two computed ratios exactly as they are (light `#fc4c02` on `#ffffff` = 3.40:1; dark `#ff6b35` on `#1a1a2e` = 6.02:1) and confirm in the summary that no luminance helper was added to `styles.test.ts`, per D-11.
5. **Wave 0 Requirements** — the seeded record says "None", which plan 19-04 confirmed: `styles.test.ts` already existed with both helpers. Record that plan 19-04 added exactly one new helper (`ruleWithHeadContaining`) because `selectorListDeclares()` cannot match a rule head whose commas sit inside `:where(:not(…))`. Mark this as an in-phase addition, not a Wave 0 gap, and do not tick anything that was not delivered.
6. Do **not** set `nyquist_compliant: true` and do **not** change `Approval: pending` in this task. Task 2's outcome decides both.

Also record a short "Rules that shipped" section listing every selector this phase added or edited, so a future phase can audit the element-level baseline without re-deriving it: the two `:root` radius tokens, the `input, select, textarea` baseline, the three-type override, the `button` baseline, the scoped hover, the `:disabled, [aria-disabled="true"]` rule, the repaired `.cta:hover`, the retrofitted `.activity-table tbody tr:hover`, the replaced `:focus-visible`, `.segmented` minus its `overflow`, the two `.segmented__option` end-child radius rules, `.splits-scroll`'s padding, and the five D-13 panel/grid edits.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json && npm run build-widgets && npm run verify-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - All four gate commands exit 0; each exit code, the vitest passing count and the `verify-dashboard` assertion count are recorded in the summary.
    - `npm test`'s passing count is strictly greater than the pre-Phase-19 baseline of 884.
    - `grep -c 'TBD' .planning/phases/19-design-system-control-styling/19-VALIDATION.md` returns `0`.
    - The Manual-Only Verifications table has exactly 13 data rows: `node -e "const s=require('fs').readFileSync('.planning/phases/19-design-system-control-styling/19-VALIDATION.md','utf8');const t=s.split('## Manual-Only Verifications')[1].split('###')[0];const rows=t.split('\n').filter(l=>/^\|/.test(l)&&!/^\|\s*-+/.test(l)&&!/^\|\s*Behavior/.test(l));if(rows.length!==13)throw new Error('expected 13 manual rows, found '+rows.length);console.log('13 manual rows')"` exits 0.
    - Each of UI-01, UI-02, UI-03 and ACT-01 appears at least once in the Per-Task Verification Map with a non-`TBD` plan number.
    - `grep -c 'nyquist_compliant: false' .planning/phases/19-design-system-control-styling/19-VALIDATION.md` returns `1` and `grep -c 'Approval:.*pending' ...` returns `1` at the end of this task.
    - `grep -ci 'luminance' src/dashboard/styles.test.ts` returns `0`.
    - The "Rules that shipped" section names all 14 selector groups listed in the action.
    - `git status --porcelain src scripts` is empty — this task changes no source file.
  </acceptance_criteria>
  <done>The four-command gate is green with counts recorded, the validation record has no `TBD` cells, the Manual-Only table carries 13 rows awaiting observations, the two derived-correction rows are present, and the sign-off is still explicitly pending.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-browser verification of every rendering claim in the phase</name>
  <files>.planning/phases/19-design-system-control-styling/19-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` as reconciled by Task 1 — the 13-row Manual-Only Verifications table you are about to fill, the Contrast requirement sub-section with the two computed ratios, and the Human Checkpoint Staging block
    - `.planning/phases/19-design-system-control-styling/19-UI-SPEC.md` § Human Checkpoint Script — the seven-step script this agenda expands on, and § Phase-Specific Notes § "Success criterion 4 as an explicit non-goal list" so the Activities row knows exactly what must NOT have changed
    - `.planning/phases/19-design-system-control-styling/19-03-SUMMARY.md` — the two derived corrections to D-06 with their specificity arithmetic, needed to judge rows 10, 11 and 13
    - `.planning/phases/19-design-system-control-styling/19-04-SUMMARY.md` — the segmented silhouette-mechanism change, needed to judge row 12
    - `src/dashboard/styles.css` — the final shipped state of the rules being observed, so a reported defect can be tied to a specific rule rather than a screenshot
    - `.planning/STATE.md` § Blockers/Concerns — the `16-09` and `17-15` PARTIAL records, which are the wording precedent this checkpoint's result must match
  </read_first>
  <action>
Do not write or change any file under `src/` or `scripts/` in this task. Serve the already-built publish directory under a production-shaped `/strava-widgets` path, walk all 13 rows of the agenda in `<how-to-verify>` below on all five screens, and record a **verbatim observation** for each row in `19-VALIDATION.md`'s Manual-Only Verifications table — not a tick. Then set `nyquist_compliant` and the `Approval:` line from the outcome.

**Checkpoint result protocol — house rule since 16-09, reaffirmed by 17-15.** If any row fails, record it **verbatim as gap-closure work in the developer's own words**, mark the checkpoint **PARTIAL**, leave `nyquist_compliant: false`, and list in `requirements-completed` **only** the requirement IDs whose rows were all confirmed clean. Defects are never patched under checkpoint pressure — do not fix anything in this task, and do not paraphrase a failure into something that sounds fixable. `approved` is permitted only when all 13 rows pass.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/19-design-system-control-styling/19-VALIDATION.md','utf8');const a=(s.split('Approval:')[1]||'pending').split('\n')[0];if(/pending/.test(a))throw new Error('Approval line still pending');if(!/approved|PARTIAL/.test(a))throw new Error('no verdict recorded on the Approval line');const t=s.split('## Manual-Only Verifications')[1].split('###')[0];const rows=t.split('\n').filter(l=>/^\|/.test(l)&&!/^\|\s*-+/.test(l)&&!/^\|\s*Behavior/.test(l));if(rows.length!==13)throw new Error('expected 13 manual rows, found '+rows.length);const blank=rows.filter(r=>{const c=r.split('|').map(x=>x.trim());return !c[c.length-2]||c[c.length-2].length<10});if(blank.length)throw new Error(blank.length+' manual row(s) have no recorded observation');console.log('verdict recorded, 13 observations present')"</automated>
  </verify>
  <acceptance_criteria>
    - All 13 Manual-Only rows have a non-empty recorded observation of at least a full sentence — no blank cells, no bare ticks, no "OK".
    - The `Approval:` line reads either `approved {date}` or `PARTIAL {date}` and no longer reads `pending`.
    - If PARTIAL: every failure is quoted verbatim in the developer's own words, tied to its row number, and the confirmed requirement IDs are listed explicitly and separately from the failing ones.
    - `nyquist_compliant: true` appears only when the verdict is `approved`.
    - The observation for row 8 (Activities row-click) explicitly states the result of all four sub-checks: mouse click navigates, Tab/Enter activates the activity-name anchor, return-from-detail highlight still flashes, and sort/filter/pagination behave as before.
    - The observation for row 6 (focus ring) explicitly names light theme and dark theme separately, and names `.segmented`, `.records-jump`, `.splits-scroll`, the current pagination page and the active segmented option.
    - `git status --porcelain src scripts` is empty — no source file was modified in this task.
    - The checkpoint was performed against a URL beginning `http://localhost:8099/strava-widgets/`, recorded as such in the summary — **not** against a root-served build.
  </acceptance_criteria>
  <what-built>
Phase 19 changed exactly two files — `src/dashboard/styles.css` and `src/dashboard/styles.test.ts` — and zero TypeScript view files. Every visible change reaches the screen by cascade onto markup that already existed.

- **Control baseline (UI-01):** the stylesheet had **zero** `input`/`select`/`textarea` rules across 1,092 lines, so all 13 control-creation sites rendered unstyled browser defaults. There is now one bare-element rule declaring a 1px `--border` border, `--surface` background, `--text` colour, `--space-xs`/`--space-sm` padding, the new 4px control radius, a 32px min-height and `font: inherit`. The single live `<input type="checkbox">` (the detail-chart overlay-cap checkbox) is reset out of that box treatment. No `appearance: none` and no vendor pseudo-element rule was added — native date, month, number and search chrome is left entirely to the existing `color-scheme` declarations.
- **Button baseline (UI-02):** every `<button>` now inherits `--font-stack`, a pointer cursor, a 32px floor and the control radius. One shared hover uses `color-mix(in srgb, var(--surface) 92%, var(--text))`, so it darkens in light theme and lightens in dark. None of the 31 `createElement('button')` sites was refactored and none of the 12 button classes was renamed.
- **Two derived corrections you should scrutinise (rows 10, 11, 13):** a bare `button:hover` is specificity `0,1,1` and would have outranked every `0,1,0` class modifier — silently stripping the `--accent-strong` fill from the current pagination page and the active segmented option (leaving white text on a near-white surface) and flattening the calendar distance-tint scale on hover. The shipped rule wraps those exclusions in `:where()`, which costs zero specificity. Separately, D-06 stated the shared hover would repair the dead `.cta:hover`; it cannot, because `.cta:hover` is `0,2,0` and five of `.cta`'s ten sites are `<a>` elements a `button` selector never reaches — so `.cta:hover` was edited directly to `color-mix(in srgb, var(--accent) 92%, var(--text))`, accent-based because `.cta`'s fill is accent.
- **Disabled treatment (UI-02):** the stylesheet had **no `:disabled` rule at all**, so all five live disabled states rendered identically to enabled controls. There is now one unscoped `:disabled, [aria-disabled="true"]` rule at `--text-secondary`, `opacity: 0.6`, `cursor: default`, and disabled controls are excluded from the hover rule.
- **Focus ring (UI-02):** the global `outline: 2px solid var(--accent)` is replaced in place by `box-shadow: 0 0 0 2px var(--bg), 0 0 0 4px var(--accent)` with `outline: none` in the same rule. It stays unscoped so links, and the clickable rows Phase 20 adds, inherit it. `.segmented`'s `overflow: hidden` — which clipped the ring exactly as it clipped the old outline — is gone, and its rounded silhouette is now produced by `:first-child`/`:last-child` radii instead of by clipping square children. `.splits-scroll` gained 4px of padding so the ring can paint inside the scroll box. `.records-jump` was deliberately left alone (its existing 8px already suffices).
- **Card and spacing rhythm (UI-03):** `--radius-control: 4px` and `--radius-panel: 8px` are named as theme-invariant `:root` tokens over values already in use. `.error-state`, `.empty-state`, `.calendar-picker` and `.config-notice` all now carry the panel radius and `--space-lg` padding, replacing a three-way radius split and 48px/16px padding outliers. `.stat-grid`'s gap came down from 32px to 24px. `.error-state` shares its rule body with `.stub-panel`, so `.stub-panel` was restyled too — an acknowledged, unlisted side effect. Overview received all of this purely by cascade with zero markup change; its stacked-div PR rows and headline stats remain Phase 21's work.
- **Row hover (ACT-01):** `.activity-table tbody tr:hover` mixed toward literal `black`, which over-darkened an already-dark surface in dark theme. It now mixes toward `var(--text)`. This is a deliberate, documented deviation from "Activities visually unchanged" — see row 9.
  </what-built>
  <how-to-verify>
**Stage the build under a production-shaped path.** Serving at the bare server root would hide the exact class of defect that produced Phase 16's black page, so mount under `/strava-widgets` the way `scripts/verify-dashboard-publish.mjs` does with its `MOUNT_PREFIX = '/strava-widgets'` constant. This is the sequence plan 18-16 actually ran and the developer approved:

1. Confirm the full gate from Task 1 is green: `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets`, `npm run verify-dashboard` — all four exit 0.
2. `mkdir -p /tmp/gh-pages && ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets`
3. `cd /tmp/gh-pages && python3 -m http.server 8099`
4. Open all five screens with devtools Console visible for the entire session:
   - `http://localhost:8099/strava-widgets/#/` (Overview)
   - `http://localhost:8099/strava-widgets/#/list` (Activities)
   - `http://localhost:8099/strava-widgets/#/calendar` (Calendar)
   - `http://localhost:8099/strava-widgets/#/records` (Records)
   - `http://localhost:8099/strava-widgets/#/trends` (Trends)

Use a current, standard browser. `color-mix(in srgb, …)` is used by the two hover formulas; it was already used 11 times in this stylesheet before Phase 19, so a hover that shows literally *no* change (rather than a wrong colour) means an outdated engine, not a code defect — check the browser version before reporting it.

**Work through all 13 rows. Record a verbatim observation for each — not a tick.** Rows 1-9 are `19-VALIDATION.md`'s existing Manual-Only Verifications agenda; rows 10-13 were added by this plan.

1. **Every input renders intentionally (UI-01).** Load all five screens and visually inspect every text, date, number and search input. Confirm none reads as an unstyled browser default. Activities is the dense one — 10 of the 13 control sites live there (two date inputs, four number inputs, two text inputs, one search).
2. **Native chrome still themes correctly (UI-01, D-02).** Open the Activities date pickers and the Calendar month picker in **both** themes. Use the number spinners on the distance/pace range inputs. Type into the search input and click its clear-X. Confirm each native widget still re-renders for the active theme. If a native picker looks slightly inconsistent with the box around it, that is the documented accepted trade-off — record it, do not treat it as a defect.
3. **Buttons and selects read as one treatment (UI-02).** Compare all 12 button treatments across the five screens: `.cta`, `.filter-toggle`, `.preset-chip`, `.pagination__button`, `.segmented__option`, `.chip__remove`, `.chip-clear-all`, `.records-jump__link`, `.activity-table__sort-button`, `.calendar-day`, `.theme-toggle`, `.app-nav__toggle`. Confirm none regressed in shape or colour.
4. **`.cta` hover is now visibly distinguishable (UI-02, D-06).** Hover the primary CTA in both themes, on both element types: a `<button class="cta">` Retry button (force a load failure, or use the Trends/Records/detail error state) and an `<a class="cta">` "View Activity" link in an Activities row or a Records PR table. Confirm the background visibly changes. It has never had hover feedback before this phase.
5. **All five disabled states look disabled (UI-02, D-07).** Pagination Prev on page 1 and Next on the last page; the detail-chart overlay checkbox once 2 overlays are selected; the Trends Banister toggle in its disabled condition; and a Calendar rest day (`aria-disabled`) plus an outside-month cell (`disabled`). Confirm all five visibly differ from their enabled siblings — they were pixel-identical before this phase.
6. **The focus ring renders fully unclipped, in both themes (UI-02, D-09/D-10/D-12).** Tab through every control on all five screens, **in light theme and again in dark theme**. Scrutinise light theme first: its computed margin is 3.40:1 against the 3:1 non-text floor, versus 6.02:1 in dark, so it is the pair most likely to look technically-passing-but-weak. Pay specific attention to: the detail view's x-axis segmented control (the `overflow: hidden` fix), the Records jump list and the Splits table horizontal scrollers, and the current pagination page and active segmented option — those last two showed an `--accent` ring on an `--accent-strong` fill before this phase, i.e. orange on orange. Confirm there is exactly one ring, not a doubled ring with a thin extra outer line (that would mean the UA outline is painting alongside the box-shadow).
7. **Spacing, density and card rhythm read as one system (UI-03, D-13/D-14).** Compare all five screens side by side, Overview included. Look specifically at the error and empty states (previously 48px padding with no radius), the Calendar month picker panel, the Records/Trends config notice, and Overview's stat grid (gap down from 32px to 24px). Confirm no screen's focal point or visual hierarchy reads differently than before — if it does, that is a regression, not an improvement.
8. **Activities' row-click interaction model is unchanged (ACT-01).** This is the phase's single highest-risk untestable claim: `list.ts` is unmodified by design, so nothing automated can observe it, and a CSS specificity mistake on `pointer-events` or `cursor` could silently break the reference pattern Phase 20 depends on. Click rows to navigate. Tab to the activity-name anchor and press Enter. Return from the detail view and confirm the row highlight still flashes and then fades. Exercise sort (every column, both directions), the filter panel and its chips, and pagination. Confirm **only** visual chrome changed.
9. **Row-hover retrofit — acknowledged intentional deviation (ACT-01, D-08).** Hover activity-table rows in **both** themes. Dark theme should now lighten rather than darken toward black; light theme should look essentially as before. This is a deliberate, recorded deviation from success criterion 4's "visually unchanged", called out here so it is not a surprise. Record whether you accept it.
10. **The two `--accent-strong` fills survive hover (UI-02, derived correction to D-06).** Hover the **current** pagination page number on Activities, and the **active** option of the detail view's x-axis segmented control. Both must keep their solid `--accent-strong` fill with their white label readable. If either turns grey-on-white while hovered, the `:where(:not(...))` exclusion list failed and this is a blocking defect — white on that surface mix is roughly 1.1:1.
11. **The calendar distance-tint scale survives hover (UI-02, derived correction to D-06).** On Calendar, hover several days across the tint range (tint-1 faintest through tint-4 strongest). Each must keep its accent tint while hovered rather than flattening to grey, so the scale still reads under the pointer.
12. **The segmented control's silhouette after the mechanism change (UI-02, D-10).** Its rounded corners previously came *entirely* from `overflow: hidden` clipping square children; they now come from `:first-child`/`:last-child` radii plus the container's own 1px border and 4px radius. Look closely at all four corners of the x-axis toggle in both themes: no square inner corner, no visible seam or sliver between the container border and the end options, and no change to the divider between options.
13. **Button text size after `font: inherit` (UI-02/UI-03, consequence of D-05).** Four button classes pin no `font-size` of their own — `.cta`, `.pagination__button`, `.segmented__option` and `.chip__remove` — so `font: inherit` moves them off the browser's ~13.3px UA button default onto the inherited 16px body size. This is a real, visible size increase and success criterion 3 requires the existing visual language not be altered. Confirm each still fits its 32px control height, that pagination numbers and segmented labels are not crowded or wrapping, and that the `.chip__remove` × glyph still sits correctly inside its 24px box. Record whether the new size is acceptable or should be pinned in a follow-up.

**Then record the outcome in `19-VALIDATION.md`.** Fill every one of the 13 observation cells with what you actually saw. Follow the 16-09 / 17-15 precedent exactly: if any row fails, record the checkpoint as **PARTIAL**, list only the passing requirement IDs, and log each failure verbatim as gap-closure work rather than patching it now. Set `nyquist_compliant` and `Approval` accordingly.
  </how-to-verify>
  <resume-signal>Type "approved" only if all 13 rows passed. Otherwise describe each failure verbatim, naming its row number. Anything less than all 13 passing is recorded as PARTIAL, not approved.</resume-signal>
  <done>All 13 agenda rows carry a recorded verbatim observation and `19-VALIDATION.md` carries an explicit verdict of `approved` or `PARTIAL`, with every failure logged unpatched and the confirmed requirement IDs listed separately.</done>
</task>

<task type="auto">
  <name>Task 3: Record the verdict, the gap-closure register and the phase sign-off</name>
  <files>.planning/phases/19-design-system-control-styling/19-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/19-design-system-control-styling/19-VALIDATION.md` — the Manual-Only Verifications table as filled by Task 2, and the Validation Sign-Off checklist
    - `.planning/STATE.md` § Blockers/Concerns — the existing PARTIAL records for `16-09` and `17-15`, so this record matches their wording and structure
  </read_first>
  <action>
Finalize the validation record from Task 2's outcome. Do not fix any defect in this task.

1. **Validation Sign-Off checklist** — tick the items actually delivered. Set `nyquist_compliant: true` in the frontmatter **only** if the verdict is `approved`; leave it `false` for `PARTIAL`.
2. **If the verdict is PARTIAL, add a `## Gap-Closure Record` section** in the shape used by `17-VALIDATION.md`. For each failing row: the row number and its behaviour text, the developer's observation quoted verbatim, the requirement ID it blocks, and the decision IDs implicated. Do not add a suggested fix, a root-cause theory or a severity downgrade — a gap is recorded, not triaged, at this point.
3. **State `requirements-completed` explicitly.** List only the requirement IDs every one of whose rows passed. Map rows to requirements as follows: UI-01 depends on rows 1-2; UI-02 on rows 3-6 and 10-13; UI-03 on rows 7 and 13; ACT-01 on rows 8-9. A requirement with any failing row is **not** completed and must be named as such, separately from the confirmed set.
4. **Record the four constraint facts** this phase operated under, so a future phase does not re-derive them: `vitest` runs `environment: 'node'` with no CSSOM and no rendering engine; this phase authorized no new test dependency; the two UI-02 contrast ratios were discharged by closed-form computation rather than by a test; and every automated check in the phase is a text assertion over `styles.css` or the existing `verify-dashboard` integration check.
5. If the verdict is `PARTIAL`, state plainly in the summary that `/gsd-plan-phase 19 --gaps` is the next step and that the phase gate does not close until those gaps are planned and executed.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/19-design-system-control-styling/19-VALIDATION.md','utf8');const a=(s.split('Approval:')[1]||'').split('\n')[0];const approved=/approved/.test(a);const partial=/PARTIAL/.test(a);if(!approved&&!partial)throw new Error('no verdict');if(approved&&!/nyquist_compliant: true/.test(s))throw new Error('approved but nyquist_compliant not true');if(partial&&!/nyquist_compliant: false/.test(s))throw new Error('PARTIAL but nyquist_compliant is true');if(partial&&!/## Gap-Closure Record/.test(s))throw new Error('PARTIAL without a Gap-Closure Record');if(!/requirements-completed/.test(s))throw new Error('requirements-completed not stated');console.log('sign-off consistent with verdict:',approved?'approved':'PARTIAL')"</automated>
  </verify>
  <acceptance_criteria>
    - The frontmatter `nyquist_compliant` value is consistent with the `Approval:` verdict: `true` only for `approved`, `false` for `PARTIAL`.
    - If `PARTIAL`: a `## Gap-Closure Record` section exists with one entry per failing row, each carrying a verbatim quote, the blocked requirement ID and the implicated decision IDs, and **no** suggested fix or root-cause theory.
    - `requirements-completed` names only requirements whose every mapped row passed, and any requirement with a failing row is named separately as not completed.
    - The four constraint facts from the action are all present in the record.
    - `git status --porcelain src scripts` is empty — no source file was modified by this plan at all.
    - `npm test` still exits 0 (no accidental change slipped in during the checkpoint).
  </acceptance_criteria>
  <done>The validation record carries a verdict, a sign-off consistent with that verdict, an explicit confirmed-versus-blocked requirement split, a gap-closure register if anything failed, and the four constraint facts — with no source file touched and no defect patched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → production URL shape | Serving at the bare server root instead of under `/strava-widgets` reproduces the exact class of defect that shipped Phase 16's black page behind a 15/15 green gate |
| green automated gate → phase sign-off | A green suite here proves only that rules exist in the stylesheet text; treating it as proof of rendering is this project's documented recurring failure, three times over |
| human judgement → phase gate | This checkpoint is the sole verification of every rendering, cascade and interaction claim in the phase |
| checkpoint pressure → defect handling | The temptation to fix a small visual defect mid-checkout is how a phase silently absorbs a gap instead of recording it |

**Not applicable, stated explicitly rather than padded:** this plan changes no source file and no data file. No input handling (V5), auth (V2), session (V3), access control (V4) or cryptography (V6) surface is involved. Unlike plan 18-16 there is **no** PII threat here — this phase requires no temporary private config file, so the `data/private/` exposure path from that precedent does not apply.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-VERIFY-19 | Repudiation (false-green gate) | serving the build at the server root | mitigate | The checkpoint mounts under `/strava-widgets` via the symlink sequence, matching production and matching `verify-dashboard-publish.mjs`'s `MOUNT_PREFIX`; Task 2's acceptance criteria require the recorded URL to begin `http://localhost:8099/strava-widgets/` |
| T-19-VERIFY-20 | Repudiation (paraphrased failure) | checkpoint recording | mitigate | Every one of the 13 rows requires a verbatim observation of at least a full sentence, asserted programmatically; failures are logged verbatim as gap-closure work under the 16-09/17-15 precedent and Task 3 forbids adding a fix or root-cause theory to the register |
| T-19-VERIFY-21 | Repudiation (silent absorption of a defect) | fixing a defect during the checkpoint | mitigate | Task 2 forbids modifying anything under `src/` or `scripts/` and asserts `git status --porcelain src scripts` is empty; Task 3 repeats the assertion after sign-off |
| T-19-A11Y-22 | Denial of Service (keyboard access lost in production) | focus ring clipped by an ancestor no text assertion can see | mitigate | Row 6 tabs every control on all five screens in both themes, naming `.segmented`, `.records-jump`, `.splits-scroll` and the two `--accent-strong` fills; a text assertion can prove `overflow: hidden` left `.segmented` but cannot prove no *other* ancestor clips the ring |
| T-19-CASCADE-23 | Tampering (unreadable control from cascade leakage) | `button:hover` outranking `--accent-strong` fills and calendar tints | mitigate | Rows 10 and 11 exist specifically to observe the rendered result of the `:where(:not(...))` exclusion list; row 10 states the ~1.1:1 white-on-surface-mix failure mode explicitly so it is recognised rather than shrugged at |
| T-19-ACT-24 | Tampering (silent regression of the Phase 20 reference pattern) | `list.ts` row-click model | mitigate | Row 8 exercises mouse click, Tab/Enter, return-from-detail highlight, sort, filter and pagination, with all four sub-results required in the recorded observation. There is no automated substitute — `list.ts` is unmodified by design |
| T-19-PUBLISH-25 | Denial of Service (assets unreachable under the prefix) | `dist/widgets` publish shape | mitigate | Task 1's gate runs `npm run verify-dashboard`, which mounts under `/strava-widgets` and treats a root-absolute asset URL as a hard failure — both fixes made after the Phase 16 postmortem |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs authorized. The verification server is `python3 -m http.server` (Python 3.11.11 already present); do not `npm install` a static server, and do not install jsdom/puppeteer/playwright to "automate" any row above. RESEARCH.md § Package Legitimacy Audit records `Disposition: N/A` for this phase |
</threat_model>

<verification>
- All four gate commands exit 0 before the checkpoint opens, with counts recorded.
- The checkpoint was performed against `http://localhost:8099/strava-widgets/`, not a root-served build.
- All 13 agenda rows have a recorded verbatim observation of at least a full sentence.
- `git status --porcelain src scripts` is empty after both Task 2 and Task 3.
- The sign-off's `nyquist_compliant` value is consistent with the recorded verdict.
</verification>

<success_criteria>
Every rendering, cascade and interaction claim Phase 19 makes has either been seen working by a human in a real browser under a production-shaped `/strava-widgets` URL, or has been recorded verbatim and unpatched as gap-closure work — with the confirmed requirement IDs listed separately from the blocked ones.
</success_criteria>

<output>
Create `.planning/phases/19-design-system-control-styling/19-05-SUMMARY.md` when done. Record: the four gate exit codes with the vitest and `verify-dashboard` counts, the exact URL the checkpoint ran against, the verdict, all 13 observations, the confirmed versus blocked requirement IDs, the gap-closure register verbatim if PARTIAL, the developer's verdict on the two derived corrections to D-06 (rows 10-11), on the segmented silhouette mechanism change (row 12) and on the button text-size increase (row 13), and — if PARTIAL — that `/gsd-plan-phase 19 --gaps` is the next step.
</output>
