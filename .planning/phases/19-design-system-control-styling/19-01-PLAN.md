---
phase: 19-design-system-control-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/styles.css
autonomous: true
requirements: [UI-03]
must_haves:
  decisions: [D-13, D-14]
  truths:
    - "Two named radius tokens exist in the bare :root block and are not duplicated into either [data-theme] block"
    - "The four panel outliers (.error-state, .empty-state, .calendar-picker, .config-notice) all declare the same panel radius and the same --space-lg padding"
    - "No grid gap in the stylesheet uses --space-xl any more"
    - "Overview receives the corrected rhythm purely by cascade, with zero markup or TypeScript change"
  artifacts:
    - path: "src/dashboard/styles.css"
      provides: "--radius-control/--radius-panel tokens and the normalized panel/grid rhythm"
      contains: "--radius-panel: 8px"
  key_links:
    - from: "src/dashboard/styles.css :root"
      to: ".error-state/.empty-state/.calendar-picker/.config-notice"
      via: "var(--radius-panel) reference"
      pattern: "border-radius: var\\(--radius-panel\\)"
---

<objective>
Land the two named radius tokens and normalize the card/panel/grid rhythm so that the four real panel outliers and the one out-of-step grid gap read as one system (UI-03). This plan goes first because both the control baseline (plan 19-02) and the segmented-control radii (plan 19-04) reference `--radius-control`, which must exist before anything can use it.

Purpose: UI-03 says spacing, density and card treatment read as one rhythm across all five screens. Today radius splits three ways across the four panels being retrofitted (8px / 4px / none), `.error-state` and `.empty-state` sit at 48px padding where every other panel uses 16-24px, and `.stat-grid` is the only grid gap in the file at 32px. Fixing these is Overview's entire stake in the phase (D-14) — it gets the corrected rhythm by cascade and nothing else.
Output: two new theme-invariant tokens in `:root`, five edited rules, zero new selectors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-design-system-control-styling/19-CONTEXT.md
@.planning/phases/19-design-system-control-styling/19-UI-SPEC.md
@.planning/phases/19-design-system-control-styling/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare --radius-control and --radius-panel as theme-invariant :root tokens</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/styles.css` lines 15-110 — the bare `:root` block, the `[data-theme="light"]` block and the `[data-theme="dark"]` block, so you can see which shape a theme-invariant token takes in this file
    - `.planning/phases/19-design-system-control-styling/19-PATTERNS.md` § 2 (`:root` / `[data-theme]` token block layout) — the two declaration shapes and why radius tokens take shape 2
    - `.planning/phases/19-design-system-control-styling/19-UI-SPEC.md` § Spacing Scale, sub-section "New non-spacing tokens this phase" — the two token names and values
  </read_first>
  <action>
Insert exactly two custom-property declarations into the **bare `:root` block only**, immediately after the `--space-3xl: 64px;` line (currently line 53) and before the `/* Font stack ... */` comment (currently line 55):

- `--radius-control` with value `4px`
- `--radius-panel` with value `8px`

Both are theme-invariant (identical in light and dark), so follow the `--space-*` / `--font-stack` / `--zone-1..5` / `--load-*` precedent: declare once in the bare `:root`, and do **not** add them to `[data-theme="light"]` or `[data-theme="dark"]`. Adding them to either theme block is the wrong shape and creates two copies that can drift.

Neither value is a new number: `4px` already appears literally on `.filter-toggle`, `.pagination__button`, `.calendar-day`, `.segmented`, `.config-notice` and `.badge`; `8px` already appears on `.card`, `.activity-row`, `.route-map`, `.chart-band` and `.filter-panel--open`. The tokens name what is already there so Phases 21-24 have something to reference.

Add a short preceding comment in the style of the neighbouring Phase 17/18 token comments, naming these as the Phase 19 radius scale and stating that they are theme-invariant.

**Do not back-substitute these tokens into any existing literal `border-radius: 8px` / `4px` declaration** anywhere in the file. Per 19-UI-SPEC § "Token back-substitution scope", the scope is deliberately narrow: only the new and retrofitted rules in this phase reference the tokens. `.card`, `.activity-row`, `.route-map`, `.chart-band`, `.filter-panel--open`, `.filter-toggle`, `.pagination__button`, `.calendar-day` and `.badge` all keep their literal values untouched — a blanket replacement would enlarge the diff that Activities' checkpoint has to be re-verified against for zero visual change.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--radius-control: 4px' src/dashboard/styles.css` returns `1`.
    - `grep -c -- '--radius-panel: 8px' src/dashboard/styles.css` returns `1`.
    - `node -e "const c=require('fs').readFileSync('src/dashboard/styles.css','utf8');const m=c.match(/:root\[data-theme=\"(light|dark)\"\]\s*\{[^}]*\}/g)||[];if(m.some(b=>b.includes('--radius-')))throw new Error('radius token leaked into a [data-theme] block');console.log('theme-invariant shape OK')"` exits 0.
    - Both declarations appear between the literal text `--space-3xl: 64px;` and the literal text `--font-stack:` in the file (verifiable by line numbers from `grep -n`).
    - `npx vitest run src/dashboard/styles.test.ts` exits 0 (the pre-existing `:root` token assertions still pass — this proves the insertion did not break the `declarationsFor(':root')` regex match).
    - `git diff --stat src/dashboard/styles.css` shows insertions only, zero deletions.
  </acceptance_criteria>
  <done>`--radius-control: 4px` and `--radius-panel: 8px` are declared exactly once each, in the bare `:root` block only, adjacent to the spacing scale, with no existing literal radius value changed.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize the four panel outliers and the one out-of-step grid gap</name>
  <files>src/dashboard/styles.css</files>
  <read_first>
    - `src/dashboard/styles.css` — the five rules being edited in place: `.card` (lines 235-240, the source-of-truth treatment, **not edited**), `.stat-grid` (242-246), `.error-state, .stub-panel` (312-318), `.empty-state` (573-578), `.calendar-picker` (689-693), `.config-notice` (985-994)
    - `.planning/phases/19-design-system-control-styling/19-PATTERNS.md` § 5 — each target rule quoted verbatim as it exists today, plus **Landmine 1** (`.error-state` shares its rule body with `.stub-panel`)
    - `.planning/phases/19-design-system-control-styling/19-UI-SPEC.md` § Card & Spacing Rhythm Contract — the before/after table for all four panels
    - `src/dashboard/views/overview.ts` — read only to confirm which of these classes Overview actually renders, so the summary can state what D-14's cascade-only stake actually amounts to. Do not modify this file.
  </read_first>
  <action>
Edit five existing rules **in place** at their existing locations. Do not move them, do not duplicate them into a new block, do not add new selectors.

1. **`.error-state, .stub-panel`** (line 312) — add `border-radius: var(--radius-panel);` and change `padding: var(--space-2xl)` to `padding: var(--space-lg)`.
   **Called-out scope expansion (PATTERNS Landmine 1):** `.error-state` shares one rule body with `.stub-panel`, so this edit necessarily restyles `.stub-panel` too. `.stub-panel` is named nowhere in CONTEXT.md, RESEARCH.md or 19-UI-SPEC.md. Editing the shared body is the only way to change `.error-state` at all — splitting them into two rules would be a larger, unrequested change. Record this in the plan summary as a deliberate, acknowledged side effect, not a silent inheritance. Note in the summary that plan 18-16 removed both `STUB_PHASE` entries, so `.stub-panel` may already be dead CSS.
2. **`.empty-state`** (line 573) — add `border-radius: var(--radius-panel);` and change `padding: var(--space-2xl) var(--space-lg)` to the single value `padding: var(--space-lg)`. A single value normalizes both axes to `--space-lg`, which is what 19-UI-SPEC asks for.
3. **`.calendar-picker`** (line 689) — add `border-radius: var(--radius-panel);` and change `padding: var(--space-md)` to `padding: var(--space-lg)`.
4. **`.config-notice`** (line 985) — change the literal `border-radius: 4px` to `border-radius: var(--radius-panel)` and change `padding: var(--space-md)` to `padding: var(--space-lg)`. A notice box is a panel, so it takes the panel radius (8px), not the control radius. Leave its `margin-bottom`, `font-size`, `font-weight` and `line-height` declarations untouched.
5. **`.stat-grid`** (line 242) — change `gap: var(--space-xl)` to `gap: var(--space-lg)`. Leave `display` and `grid-template-columns` untouched.

Do **not** touch `.card` itself — it is already correct at `border-radius: 8px` and stays on its literal value per the narrow back-substitution scope from Task 1.

D-14 requires no work here beyond this: `overview.ts` has zero form controls and zero markup changes, so whatever `.stat-grid` and these four panel classes cascade onto Overview is Overview's entire stake in the phase. Do not make any structural or hierarchy change to Overview — the stacked-div PR rows and headline stats are Phase 21's rebuild.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/styles.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'border-radius: var(--radius-panel)' src/dashboard/styles.css` returns `4`.
    - `grep -c 'gap: var(--space-xl)' src/dashboard/styles.css` returns `0`.
    - `grep -c 'padding: var(--space-2xl)' src/dashboard/styles.css` returns `0`.
    - `grep -c 'border-radius: 4px;' src/dashboard/styles.css` returns `5` (was **6**, not 5 — `.config-notice`'s literal at line 988 is the only one that goes). The five that keep their literal are `.badge` (282), `.filter-toggle` (455), `.pagination__button` (550), `.calendar-day` (628) and `.segmented` (782). `.badge` is the sixth site 19-UI-SPEC's original enumeration missed and 19-RESEARCH.md corrected; it must **not** be edited to satisfy this count — Task 1's narrow back-substitution rule forbids it, and touching it would enlarge the diff Activities' checkpoint is re-verified against for zero visual change.
    - `node -e "const c=require('fs').readFileSync('src/dashboard/styles.css','utf8');const g=/([^{}]+)\{([^}]*)\}/g;let m,seen=0;while((m=g.exec(c))){const h=m[1],b=m[2];if(/(^|,)\s*\.card\s*$/.test(h.split('\n').pop()||h)){}};for(const s of ['.error-state','.empty-state','.calendar-picker','.config-notice']){const re=new RegExp(s.replace('.','\\\\.')+'[^{]*\\\\{([^}]*)\\\\}');const mm=c.match(re);if(!mm||!mm[1].includes('var(--radius-panel)')||!mm[1].includes('padding: var(--space-lg)'))throw new Error('missing panel treatment on '+s);seen++}console.log('panels normalized:',seen)"` exits 0 and prints `panels normalized: 4`.
    - `git status --porcelain src/dashboard/views src/dashboard/*.ts` is empty — no TypeScript file was modified.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - The plan summary contains an explicit paragraph naming `.stub-panel` as an acknowledged, unlisted side effect of editing the shared `.error-state` rule body.
  </acceptance_criteria>
  <done>All four panel outliers declare `border-radius: var(--radius-panel)` and `padding: var(--space-lg)`, `.stat-grid` uses `--space-lg`, no `--space-2xl` padding or `--space-xl` gap remains, `.card` is untouched, and the `.stub-panel` side effect is recorded rather than silently absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS cascade → already-rendered markup | This plan changes presentation of markup it never reads; a low-specificity or shared-body edit can reach a selector it was not aimed at |
| local build → `/strava-widgets` production URL shape | Any stylesheet change must still be reachable under the project-page prefix, not just at the server root |

**Not applicable, stated explicitly rather than padded:** no input handling (V5 Input Validation — zero TypeScript change, zero new inputs), no authentication or session surface (V2/V3), no access control (V4), no cryptography (V6), no network calls, no data files, no build-script change, and zero package installs. This plan adds two custom properties and edits five declarations.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-CASCADE-01 | Tampering (unintended restyle) | `.error-state, .stub-panel` shared rule body | mitigate | `.stub-panel` is named explicitly in Task 2's action and required in its acceptance criteria to be recorded in the summary as an acknowledged side effect; the alternative (splitting the rule) is a larger unrequested change |
| T-19-CASCADE-02 | Tampering (token drift) | `--radius-control`/`--radius-panel` duplicated into a `[data-theme]` block | mitigate | Task 1's acceptance criteria include a Node assertion that neither token appears inside either `:root[data-theme=...]` block |
| T-19-DIFF-03 | Tampering (diff inflation) | blanket back-substitution of literal radii | mitigate | Task 1 forbids back-substitution and Task 2 asserts `grep -c 'border-radius: 4px;'` returns exactly `5` — six today (`.badge`, `.filter-toggle`, `.pagination__button`, `.calendar-day`, `.segmented`, `.config-notice`), minus only `.config-notice` — so an over-broad find-and-replace fails the gate |
| T-19-PUBLISH-04 | Denial of Service (assets unreachable under the prefix) | `dist/widgets` publish shape | transfer | Discharged at the phase gate by `npm run verify-dashboard` (`MOUNT_PREFIX = '/strava-widgets'`) in plan 19-05, not per-plan; this plan adds no new asset |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs authorized in this phase. RESEARCH.md § Package Legitimacy Audit records `Disposition: N/A` because no `npm install` occurs. If any task appears to need a dependency, stop and escalate rather than installing |
</threat_model>

<verification>
- `npx vitest run src/dashboard/styles.test.ts` exits 0 after each task.
- `npx tsc --noEmit -p tsconfig.json` exits 0 (tripwire: this phase makes zero TypeScript changes).
- `git status --porcelain` shows `src/dashboard/styles.css` as the only modified source file.
- **Not proven by any of the above:** that any panel actually renders with the new radius or padding. There is no CSSOM and no rendering engine in this repo. Every rendering claim is deferred to plan 19-05's human browser checkpoint.
</verification>

<success_criteria>
The stylesheet declares two named radius tokens once each, in the bare `:root` only; the four panel outliers and `.stat-grid` reference the shared rhythm instead of their previous three-way radius split and 48px/32px outlier values; no existing literal radius was back-substituted; and no TypeScript file was touched.
</success_criteria>

<output>
Create `.planning/phases/19-design-system-control-styling/19-01-SUMMARY.md` when done. Record: the exact insertion point used for the two tokens, the before/after value for each of the five edited rules, the `.stub-panel` side effect as an acknowledged scope expansion, and which of the five retrofitted classes `overview.ts` actually renders (D-14's real cascade footprint).
</output>
