---
phase: 21-overview-rebuild
plan: 08
type: execute
wave: 5
depends_on: ["21-05", "21-07"]
gap_closure: true
files_modified:
  - .planning/phases/21-overview-rebuild/21-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [OVR-03]
must_haves:
  decisions: [D-01, D-11, D-16]
  truths:
    - "OVR-03's year scope is observed re-ranking REAL RENDERED ROWS: with 'This year' selected, one distance table shows rows whose rank and date a human reads back and checks against a stated expectation — closing Round 1's R7 BLOCKED"
    - "D-01/D-11: the read-back asserts the rank's VALUE, not a row's presence — a table that passed source ranks through instead of re-ranking 1..N over the filtered subset renders #4 and #9 where a correct implementation renders #1 and #2"
    - "D-16: the 2026 entries exist ONLY in `dist/widgets/data/stats/best-efforts.json`; the repository copy at `data/stats/best-efforts.json` is proven unmodified BY CONTENT, because `data/stats/` is gitignored and a `git status` check there is vacuous"
    - "D-16: the build is served and browsed from `127.0.0.1` under `/strava-widgets`, and a HARD RELOAD is confirmed by its own row before the re-rank row is judged — Round 1's R13 only passed after a hard reload cleared a stale cached `streaks.json` in the observing tab"
    - "Round 1 is not re-litigated: R1-R6 and R8-R13 keep their recorded verdicts and R7's BLOCKED cell is left verbatim; Round 2 asks exactly two rows"
    - "The fixture is discarded after the checkpoint, so no fabricated 2026 PR data survives in the staged build to be mistaken for archive data later"
  artifacts:
    - path: ".planning/phases/21-overview-rebuild/21-VALIDATION.md"
      provides: "a Round 2 section: the fixture disclosure, a two-row agenda (R14, R15) with one verdict, one required detail and one named observer per row, and the Round 2 outcome"
      contains: "R21-VERDICT"
    - path: ".planning/REQUIREMENTS.md"
      provides: "OVR-03's checkbox and status-table row gated on its FULL row map (R6, R8, R9, R10 from Round 1 plus R14, R15 from Round 2)"
      contains: "OVR-03"
  key_links:
    - from: "dist/widgets/data/stats/best-efforts.json fixture"
      to: "row R15"
      via: "the 400m rank-4 and rank-9 entries redated to 2026-03-14 and 2026-06-02, their source ranks deliberately left at 4 and 9"
      pattern: "#1"
    - from: "records.ts:661 filterRankingsToYear call"
      to: "the rendered 400m This-year table"
      via: "filter to getUTCFullYear() === 2026, then re-rank 1..N"
      pattern: "Mar 14, 2026"
---

<objective>
Close the one open gap in Phase 21: `21-VERIFICATION.md` scores 5/6 because Success Criterion 3 /
OVR-03's core function — actually re-ranking and displaying current-year rows — was never observed
against real rendered output. Round 1's R7 is recorded BLOCKED, because with "This year" selected
every one of the seven distance tables renders an empty state: the live archive holds zero
best-effort ranking entries dated 2026 in any distance.

Purpose: this is a dataset limitation, not a code defect. `filterRankingsToYear`
(`src/dashboard/views/records-logic.ts:176-191`) is pure, correctly wired at `records.ts:661`,
covered by 7 targeted unit tests, and independently confirmed correct by `21-REVIEW.md`. But this
project's house rule since checkpoint 16-09 is **"Never cite an automated result as evidence for a
manual row"** — a unit-test pass cannot discharge R7. The gap closes the same way FIX-01's identical
problem closed in 21-07: a disclosed, discriminator-designed staged-build fixture, plus a checkpoint
that reads a VALUE back rather than confirming a presence.

Output: a Round 2 section in `21-VALIDATION.md` carrying two answered rows, and OVR-03 ticked in
`REQUIREMENTS.md` only if both pass.

**Note on ROADMAP.md line 71 — deliberately no task here.** `21-VERIFICATION.md` recommended
reverting the phase's `[x]` to `[ ]`. That was already acted on in commit `4359ed1` ("revert
premature phase completion"); the line now reads
`- [ ] **Phase 21: Overview Rebuild** - ... (7/7 plans executed 2026-08-18; verification
gaps_found — OVR-03 open, success criterion 3 not human-observed)`, which is truthful. This plan
therefore does NOT touch ROADMAP.md. Closing the phase checkbox belongs to re-verification and
`/gsd-progress`, not to this gap-closure round.

**Explicitly not in scope.** No change to `filterRankingsToYear`, its tests, the `.segmented`
control, or `records.ts` render wiring — the code is verified correct and the gap is observational.
No re-ask of R1-R6 or R8-R13. No edit to the repository copy of `data/stats/best-efforts.json` or to
the pipeline that produces it. Neither Info finding from `21-REVIEW.md` (IN-01 duplicated
`isRecord`/`hasOwn`; IN-02 `buildStatCard`'s truthy `sublabel` check).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-overview-rebuild/21-CONTEXT.md
@.planning/phases/21-overview-rebuild/21-VALIDATION.md
@.planning/phases/21-overview-rebuild/21-VERIFICATION.md
@.planning/phases/21-overview-rebuild/21-07-PLAN.md
@.planning/phases/21-overview-rebuild/21-07-SUMMARY.md
@.planning/phases/21-overview-rebuild/21-05-SUMMARY.md

<interfaces>
The executor does not need to explore the codebase to design this fixture. The render contract is
fixed and is reproduced here.

From `src/dashboard/views/records-logic.ts`:
```typescript
export function filterRankingsToYear(
  entries: readonly PRRankingEntry[] | undefined,
  year: number
): PRRankingEntry[];
// filters on new Date(epochMs).getUTCFullYear() === year, preserves source array order,
// then returns inYear.map((entry, i) => ({ ...entry, rank: i + 1 }))
```

From `src/dashboard/views/records.ts:657-664` (`renderTables`, unchanged by this plan):
```typescript
const allTimeEntries = bestEfforts.rankings[distance];
const entries = currentScope === 'this-year' ? filterRankingsToYear(allTimeEntries, year) : allTimeEntries;
```
`year` is resolved once per section build as `new Date().getUTCFullYear()` (`records.ts:630`, D-11).

From `src/dashboard/views/records.ts:490-525` (`buildPrTable` cell text):
- Rank cell renders `` `#${row.rank}` ``
- Time cell renders `formatEffortDuration(row.durationSec)`
- Date cell renders `formatActivityDate(row.startDate)`

From `src/dashboard/views/list.ts`:
```typescript
export function formatActivityDate(isoLocal: string): string;
// -> `${MONTH_NAMES[d.getUTCMonth()]} ${d.getUTCDate()}, ${d.getUTCFullYear()}`, e.g. "Mar 14, 2026"
export function formatEffortDuration(totalSeconds: number): string;
// -> "m:ss" under an hour, e.g. formatEffortDuration(54.6) === "0:55", formatEffortDuration(62.1) === "1:02"
```

`data/stats/best-efforts.json` shape (`rankings` is keyed `400m | 1k | 1mi | 5k | 10k | half | marathon`;
each value is an array sorted ascending by `durationSec` with `rank` 1..N):
```json
{ "activityId": "3475732221", "startDate": "2018-09-04T16:26:06Z", "durationSec": 54.6,
  "paceSecPerKm": 136.6, "lowConfidence": false, "rank": 4 }
```
</interfaces>

<house_rules>
Non-negotiable, established across checkpoints 16-09, 17-15, 19-05, 19-12, 19-17, 20-05, 20-11,
20-18, 20-20 and 21-07:

- **One named verdict per row, and the row's own required detail with it.** A blanket statement
  covering several rows is not evidence.
- **Rows are presented ONE AT A TIME**, in order, never as a list.
- **Every row names its observer**, using exactly one of: "observed by developer", "observed by
  agent (browser automation against the staged build)", or "observed by developer, corroborated by
  agent".
- **Rows marked "Observer required: developer's own eyes" cannot be answered by agent automation.**
  If the developer declines, the row is recorded BLOCKED with the decline quoted.
- **A failing row is recorded verbatim and left unpatched.** No suggested fix, no root-cause theory
  for a still-failing row. The next planning round owns diagnosis.
- **Never cite an automated result as evidence for a manual row.** `npm test` cannot observe a
  re-rank or a rendered date. The 7 unit tests covering `filterRankingsToYear` are precisely what
  this round exists to supplement, not to substitute for.
- **Serve under `/strava-widgets`, never at the server root**, and browse **`127.0.0.1`, never
  `localhost`**.
- **The four-state verdict vocabulary is kept:** PASS, FAIL, BLOCKED, NOT EXERCISABLE.
- **"I could not tell" and "I would rather not" are legitimate answers**, recorded as given.
</house_rules>

<fixture_disclosure>
The archive cannot produce a current-year PR ranking row. Row R15 is observed against a deliberate,
disclosed edit to the STAGED build only — `dist/widgets/data/stats/best-efforts.json`, never the
repository copy at `data/stats/best-efforts.json` (2.9 MB, gitignored pipeline output).

Exactly two fields change, both `startDate` values inside `rankings["400m"]`. No `rank`, no
`durationSec`, no `activityId`, no other distance, no other file:

| Array index | activityId | `rank` (unchanged) | `durationSec` | Repository `startDate` | Staged `startDate` |
|-------------|-----------|--------------------|---------------|------------------------|--------------------|
| 3 | `3475732221` | `4` | `54.6` | `2018-09-04T16:26:06Z` | `2026-03-14T09:12:00Z` |
| 8 | `14122328106` | `9` | `62.1` | `2025-04-09T00:02:51Z` | `2026-06-02T07:30:00Z` |

**The source ranks are left at 4 and 9 ON PURPOSE. They are the discriminator.** `rankings["400m"]`
is sorted ascending by `durationSec`, and `filterRankingsToYear` preserves that order, so the
filtered 2026 subset is `[54.6s, 62.1s]` and a correct 1..N re-rank renders:

| Under "This year", 400m | Rank cell | Time cell | Date cell |
|-------------------------|-----------|-----------|-----------|
| first row | `#1` | `0:55` | `Mar 14, 2026` |
| second row | `#2` | `1:02` | `Jun 2, 2026` |

An implementation that merely filtered and passed the SOURCE ranks through would render **`#4`** and
**`#9`** instead. R15 is therefore not answerable by "a row appeared" — `#4`/`#9` is a recordable
FAIL with the numbers quoted, exactly as `ended Aug 10, 2026` was for 21-07's R12/R13.

**Three honest limitations of this fixture, disclosed so nothing downstream over-reads it:**
1. `data/stats/age-grading.json` and the `activities` map inside `best-efforts.json` are NOT
   edited, so the 400m Age-Grade cell and the 400m PR-Evolution chart still reflect the real 2018
   and 2025 dates. Neither is part of R14's or R15's read-back.
2. Under the fixture the 400m table is no longer empty under "This year". Round 1's R10 used the
   400m empty state (`"No 400m efforts in 2026"`) as its example and PASSED against the
   un-fixtured build; that verdict stands on its own evidence and is NOT re-litigated here. The six
   other distances still render their empty states under the fixture.
3. `dist/widgets/*` is gitignored (`.gitignore:4`), so the fixture cannot be committed by accident —
   but Task 3 still restores the staged file explicitly, because "it can't be committed" is not the
   same as "it can't be mistaken for archive data by the next person who opens the staged build".
</fixture_disclosure>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate, production-shaped staging, the 2026 best-efforts fixture, and the two-row Round 2 agenda</name>
  <files>.planning/phases/21-overview-rebuild/21-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/21-overview-rebuild/21-07-PLAN.md` Task 1 in full — the precedent this task
      mirrors: gate first, symlink-stage under `/strava-widgets`, apply the fixture with a pre-edit
      guard, a post-edit landing assertion, a repository-copy CONTENT assertion, and a
      `git status --porcelain src scripts` assertion, then serve on `127.0.0.1:8099` and stage an
      unanswered agenda with zero verdict tokens.
    - `.planning/phases/21-overview-rebuild/21-VALIDATION.md` in full — the frontmatter this task
      extends (`round: 1`, `round1_staged`, `round1_answered`, `status: partial`,
      `nyquist_compliant: false`), the `<house_rules>` block already written into the Round 1
      section, the Round 1 preamble's fixture-disclosure format that Round 2's preamble mirrors,
      the five-column table shape, and **row R7 at line 174** — the exact wording R15 re-asks.
    - `.planning/phases/21-overview-rebuild/21-VERIFICATION.md` frontmatter `gaps:` block — the two
      `missing:` items this task and Task 2 discharge.
    - `.planning/phases/21-overview-rebuild/21-CONTEXT.md` D-16 (staged-fixture precedent and the
      `127.0.0.1` trap) and D-01/D-11 (the toggle's home and the UTC clock rule).
    - `scripts/build-widgets.mjs:139` — confirm `{ src: 'data/stats', dest: 'dist/widgets/data/stats' }`
      still holds before relying on the staged path.
    - `.gitignore:4` and `.gitignore:11` — `dist/widgets/*` and `data/stats/` are both gitignored.
      This is load-bearing: it is why the repository-copy guard must be a CONTENT assertion and not
      a `git status` check.
  </read_first>
  <action>
    Run the established gate first, so the fixture lands on freshly-built output rather than a stale
    `dist/`: `npm test`, then `npx tsc --noEmit -p tsconfig.json`, then `npm run build-widgets` with
    its log captured and grepped for `css-syntax-error` (the Phase 19 GAP 1/GAP 3 signature), then
    `npm run verify-dashboard`. The fresh build also overwrites Round 1's `streaks.json` fixture,
    which is intended — Round 2 exercises OVR-03 only.

    Stage the build the way GitHub Pages serves it, under the `/strava-widgets` project path and
    never at the server root:
    `mkdir -p /tmp/gh-pages && ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets`, then assert
    `/tmp/gh-pages/strava-widgets/index.html` exists and the symlink target is absolute.

    Apply the fixture to `dist/widgets/data/stats/best-efforts.json` ONLY, editing exactly two
    `startDate` values inside `rankings["400m"]`, with these guards:

    1. BEFORE the edit, assert `rankings["400m"]` has 10 entries, that index 3 is
       `activityId 3475732221` with `startDate 2018-09-04T16:26:06Z` and `rank 4`, and that index 8
       is `activityId 14122328106` with `startDate 2025-04-09T00:02:51Z` and `rank 9`. Also assert
       that NO entry in ANY distance's rankings currently has a `startDate` beginning `2026-` — a
       staged file already carrying 2026 entries means `dist/` is stale or already fixture-edited,
       and the whole session would be verifying the wrong bundle.
    2. Set index 3's `startDate` to `2026-03-14T09:12:00Z` and index 8's `startDate` to
       `2026-06-02T07:30:00Z`. Change NOTHING else — leave both `rank` fields at `4` and `9`, both
       `durationSec` at `54.6` and `62.1`, both `activityId`s, every other 400m entry, every other
       distance, and every other top-level key untouched. The untouched `rank: 4` / `rank: 9` are
       the discriminator R15 depends on. Write the JSON back with two-space indentation.
    3. AFTER the edit, re-read the staged file and assert both new dates landed, that index 3 still
       reads `rank: 4` and index 8 still reads `rank: 9`, that exactly two 400m entries now start
       with `2026-`, that those two are at array indices 3 and 8 in that order (so the filtered
       subset really is `[54.6s, 62.1s]` and the expected `#1`/`#2` mapping holds), and that no
       other distance gained a `2026-` entry.
    4. Assert the REPOSITORY copy `data/stats/best-efforts.json` still reads
       `2018-09-04T16:26:06Z` at 400m index 3 and `2025-04-09T00:02:51Z` at index 8, and still has
       ZERO `2026-` entries across all seven distances. This content assertion is the real guard and
       it is not optional: `data/stats/` is gitignored (`.gitignore:11`), so
       `git status --porcelain data` is vacuously empty and would report clean even if the
       repository copy had been rewritten. Record that reasoning in the plan output.
    5. Assert `git status --porcelain src scripts` is empty, and that
       `git status --porcelain .planning/phases/21-overview-rebuild/21-01-PLAN.md ...
       21-07-PLAN.md` is empty — the seven executed plans are history and are not edited by this
       round.

    Then serve: `python3 -m http.server 8099 --directory /tmp/gh-pages` in the background, and
    confirm `http://127.0.0.1:8099/strava-widgets/index.html` responds 200 AND that
    `http://127.0.0.1:8099/strava-widgets/data/stats/best-efforts.json` — fetched over HTTP, not
    read from disk — contains `2026-03-14T09:12:00Z` and `2026-06-02T07:30:00Z`. That second fetch
    separates "the server is serving the fixture" from "the browser tab has the fixture", which is
    exactly the distinction Round 1's stale-`streaks.json` incident turned on. Use `127.0.0.1`
    everywhere, never `localhost`.

    Append a `## Round 2` section to `21-VALIDATION.md`, BELOW the existing
    `## Round 1 Gap-Closure Record`, leaving every Round 1 line byte-identical. It carries, in
    order:
    - A preamble stating: why Round 2 exists (Round 1's R7 was BLOCKED because the archive has no
      2026 ranking entries in any distance); the served URL prefix
      `http://127.0.0.1:8099/strava-widgets/`; the one route this session uses,
      `http://127.0.0.1:8099/strava-widgets/#/records`; the bundle's `assets/index-*.js` filename
      read from the staged `index.html`; and the full fixture disclosure table from this plan's
      `<fixture_disclosure>` block, including the expected-render table (`#1` / `0:55` /
      `Mar 14, 2026` and `#2` / `1:02` / `Jun 2, 2026`), the `#4` / `#9` pass-through wrong values,
      and all three disclosed limitations.
    - A **`### Cache trap (Round 1's R13 incident)`** paragraph stating that serving from
      `127.0.0.1` is NOT sufficient on its own: in Round 1 the observing tab rendered a stale cached
      `streaks.json` and only corrected after a hard reload. Round 2 therefore makes the hard reload
      its own row (R14), which must be PASS before R15 may be judged.
    - A two-row table with columns `Row | Behavior | Requirement | Instructions | Observation`, one
      row per entry in `<checkpoint_rows>` below, each Instructions cell ending with its own
      `Required detail:` clause and its own `Observer required:` clause, and each Observation cell
      left EMPTY.
    - A `### Row-to-requirement map (Round 2)` stating: OVR-03 → R6, R8, R9, R10 (Round 1, PASS) +
      R14, R15 (Round 2); R15 supersedes Round 1's R7, which stays recorded BLOCKED and is not
      edited. State that OVR-03 ticks only when every one of those rows is PASS.

    Rows continue Round 1's numbering at R14 and R15 — the same across-rounds convention Phase 20
    used (Round 2's R13, Round 3's R18-R24, Round 5's R34-R43). Do NOT create a second row named
    R7.

    Set `21-VALIDATION.md`'s frontmatter `round: 2`, add a `round2_staged` date key, keep
    `status: partial` and `nyquist_compliant: false`, and keep `round: 1`'s companion keys
    (`round1_staged`, `round1_answered`) exactly as they are. Do NOT write any verdict token into
    the Round 2 section in this task — the agenda is staged, not answered.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json && rm -f /tmp/bw21r2.log && npm run build-widgets > /tmp/bw21r2.log 2>&1; BW=$?; cat /tmp/bw21r2.log; test $BW -eq 0 && ! grep -q 'css-syntax-error' /tmp/bw21r2.log && npm run verify-dashboard && mkdir -p /tmp/gh-pages && ln -sfn "$PWD/dist/widgets" /tmp/gh-pages/strava-widgets && test -f /tmp/gh-pages/strava-widgets/index.html && readlink /tmp/gh-pages/strava-widgets | grep -q '^/' && node --input-type=module -e 'import {readFileSync,writeFileSync} from "node:fs"; const P="dist/widgets/data/stats/best-efforts.json", R="data/stats/best-efforts.json"; const A={i:3,id:"3475732221",old:"2018-09-04T16:26:06Z",neu:"2026-03-14T09:12:00Z",rank:4,dur:54.6}, B={i:8,id:"14122328106",old:"2025-04-09T00:02:51Z",neu:"2026-06-02T07:30:00Z",rank:9,dur:62.1}; const s=JSON.parse(readFileSync(P,"utf8")); const r=s.rankings["400m"]; if(!Array.isArray(r)||r.length!==10) throw new Error("staged 400m rankings is not the expected 10-entry archive array (len="+(r&&r.length)+") - dist/ may be stale"); for(const E of [A,B]){ const e=r[E.i]; if(e.activityId!==E.id||e.startDate!==E.old||e.rank!==E.rank||e.durationSec!==E.dur) throw new Error("staged 400m index "+E.i+" is not the expected pre-edit archive entry: "+JSON.stringify(e)); } for(const [k,v] of Object.entries(s.rankings)) if(v.some(e=>String(e.startDate).startsWith("2026-"))) throw new Error("staged best-efforts.json already has a 2026 entry in "+k+" - dist/ is stale or already fixture-edited"); r[A.i].startDate=A.neu; r[B.i].startDate=B.neu; writeFileSync(P, JSON.stringify(s,null,2)); const back=JSON.parse(readFileSync(P,"utf8")).rankings; const b4=back["400m"]; if(b4[A.i].startDate!==A.neu||b4[B.i].startDate!==B.neu) throw new Error("the staged fixture edit did not land"); if(b4[A.i].rank!==4||b4[B.i].rank!==9) throw new Error("source ranks were altered - the #4/#9 pass-through discriminator is gone"); if(b4[A.i].durationSec!==A.dur||b4[B.i].durationSec!==B.dur) throw new Error("durationSec was altered - the expected 0:55 / 1:02 read-back is gone"); const hits=b4.map((e,i)=>[i,e]).filter(([,e])=>String(e.startDate).startsWith("2026-")); if(hits.length!==2) throw new Error("expected exactly two 2026 entries in staged 400m, found "+hits.length); if(hits[0][0]!==A.i||hits[1][0]!==B.i) throw new Error("the two 2026 entries are not at indices 3 then 8 - the expected #1/#2 re-rank mapping would not hold"); for(const [k,v] of Object.entries(back)) if(k!=="400m" && v.some(e=>String(e.startDate).startsWith("2026-"))) throw new Error("distance "+k+" gained a 2026 entry - the fixture must touch 400m only"); const repo=JSON.parse(readFileSync(R,"utf8")); const r4=repo.rankings["400m"]; if(r4[A.i].startDate!==A.old||r4[B.i].startDate!==B.old) throw new Error("the REPOSITORY copy of best-efforts.json was modified - only the staged copy may be edited (git status cannot catch this: data/stats/ is gitignored)"); for(const [k,v] of Object.entries(repo.rankings)) if(v.some(e=>String(e.startDate).startsWith("2026-"))) throw new Error("the repository copy gained a 2026 entry in "+k); console.log("fixture landed on the staged 400m rankings only; repository copy intact and 2026-free");' && test -z "$(git status --porcelain src scripts)" && test -z "$(git status --porcelain .planning/phases/21-overview-rebuild/21-01-PLAN.md .planning/phases/21-overview-rebuild/21-02-PLAN.md .planning/phases/21-overview-rebuild/21-03-PLAN.md .planning/phases/21-overview-rebuild/21-04-PLAN.md .planning/phases/21-overview-rebuild/21-05-PLAN.md .planning/phases/21-overview-rebuild/21-06-PLAN.md .planning/phases/21-overview-rebuild/21-07-PLAN.md)" && curl -sf -o /dev/null http://127.0.0.1:8099/strava-widgets/index.html && curl -sf http://127.0.0.1:8099/strava-widgets/data/stats/best-efforts.json | grep -q '2026-03-14T09:12:00Z' && curl -sf http://127.0.0.1:8099/strava-widgets/data/stats/best-efforts.json | grep -q '2026-06-02T07:30:00Z' && node --input-type=module -e 'import {readFileSync} from "node:fs"; const p=".planning/phases/21-overview-rebuild/21-VALIDATION.md"; const s=readFileSync(p,"utf8"); if((s.match(/## Round 2/g)||[]).length!==1) throw new Error("expected exactly one Round 2 heading"); const t=s.split("## Round 2")[1]; const ids=["R14","R15"]; const rows=t.split("\n").filter(l=>l.startsWith("|")).filter(r=>ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(r))); if(rows.length!==2) throw new Error("expected 2 Round 2 agenda rows, found "+rows.length); ids.forEach((id,i)=>{ if(!new RegExp("^\\|\\s*"+id+"\\.").test(rows[i])) throw new Error("agenda row "+(i+1)+" should begin with "+id+"."); if(rows[i].split("|").length<6) throw new Error("row "+id+" does not have five columns"); }); const rd=(t.match(/Required detail/g)||[]).length; if(rd!==2) throw new Error("expected one Required detail clause per Round 2 row, found "+rd); const ob=(t.match(/Observer required/g)||[]).length; if(ob!==2) throw new Error("expected one Observer required clause per Round 2 row, found "+ob); if((t.match(/R21-VERDICT/g)||[]).length!==0) throw new Error("Round 2 verdict tokens must be zero before the checkpoint"); const pre=t.split("| Row")[0]; for(const n of ["127.0.0.1:8099/strava-widgets/","dist/widgets/data/stats/best-efforts.json","2026-03-14","2026-06-02","Mar 14, 2026","Jun 2, 2026","0:55","1:02","#1","#2","#4","#9"]) if(!pre.includes(n)) throw new Error("the Round 2 preamble is missing: "+n); if(!/hard reload/i.test(pre)) throw new Error("the Round 2 preamble does not carry the hard-reload cache-trap paragraph"); if(!/Cache trap/i.test(pre)) throw new Error("the Cache trap heading is missing"); if(/localhost:8099/.test(s)) throw new Error("localhost:8099 appears - D-16 requires 127.0.0.1"); if(!/Row-to-requirement map \(Round 2\)/.test(t)) throw new Error("the Round 2 row-to-requirement map is missing"); const r1=s.split("## Round 2")[0]; const r1ids=["R1","R2","R3","R4","R5","R6","R7","R8","R9","R10","R11","R12","R13"]; const r1rows=r1.split("\n").filter(l=>l.startsWith("|")).filter(x=>r1ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(x))); if(r1rows.length!==13) throw new Error("Round 1 no longer has its 13 rows - it must not be edited"); if((r1.match(/R21-VERDICT/g)||[]).length!==13) throw new Error("Round 1 no longer carries exactly 13 verdicts"); const r7=r1rows[6]; if(!/^\|\s*R7\./.test(r7)||!/R21-VERDICT:\s*BLOCKED/.test(r7)) throw new Error("Round 1 R7 is no longer recorded BLOCKED verbatim"); const fm=s.split("---")[1]||""; if(!/round: 2/.test(fm)) throw new Error("frontmatter round is not 2"); if(!/round2_staged/.test(fm)) throw new Error("round2_staged date missing"); if(!/round1_staged/.test(fm)||!/round1_answered/.test(fm)) throw new Error("Round 1 frontmatter keys were dropped"); if(!/nyquist_compliant: false/.test(fm)) throw new Error("nyquist_compliant must stay false until Round 2 is answered"); if(!/status: partial/.test(fm)) throw new Error("status must stay partial until Round 2 is answered"); console.log("2 staged Round 2 rows, 0 Round 2 verdicts, fixture disclosed, Round 1 intact, 127.0.0.1 enforced");'</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run build-widgets` and
      `npm run verify-dashboard` all exit 0, and `/tmp/bw21r2.log` contains zero `css-syntax-error`.
    - `/tmp/gh-pages/strava-widgets/index.html` exists and `readlink /tmp/gh-pages/strava-widgets`
      returns an absolute path.
    - Before the edit, staged `rankings["400m"]` had 10 entries, index 3 was
      `{activityId: "3475732221", startDate: "2018-09-04T16:26:06Z", rank: 4, durationSec: 54.6}`,
      index 8 was `{activityId: "14122328106", startDate: "2025-04-09T00:02:51Z", rank: 9,
      durationSec: 62.1}`, and no distance held any `2026-` entry.
    - After the edit, staged 400m index 3 reads `startDate: "2026-03-14T09:12:00Z"` with
      `rank: 4` and `durationSec: 54.6`; index 8 reads `startDate: "2026-06-02T07:30:00Z"` with
      `rank: 9` and `durationSec: 62.1`; exactly two 400m entries begin `2026-`, at indices 3 then
      8; no other distance holds a `2026-` entry.
    - `data/stats/best-efforts.json` still reads `2018-09-04T16:26:06Z` and `2025-04-09T00:02:51Z`
      at 400m indices 3 and 8 and holds zero `2026-` entries in any distance.
    - `git status --porcelain src scripts` is empty, and `git status --porcelain` for
      `21-01-PLAN.md` through `21-07-PLAN.md` is empty.
    - `http://127.0.0.1:8099/strava-widgets/index.html` responds 200, and the HTTP-fetched
      `.../data/stats/best-efforts.json` contains both `2026-03-14T09:12:00Z` and
      `2026-06-02T07:30:00Z`.
    - `21-VALIDATION.md` has exactly one `## Round 2` heading, two agenda rows R14 then R15 in
      order with five columns each, one `Required detail` and one `Observer required` clause per
      row, zero `R21-VERDICT` tokens inside the Round 2 section, a `Cache trap` heading, a preamble
      containing `127.0.0.1:8099/strava-widgets/`,
      `dist/widgets/data/stats/best-efforts.json`, `2026-03-14`, `2026-06-02`, `Mar 14, 2026`,
      `Jun 2, 2026`, `0:55`, `1:02`, `#1`, `#2`, `#4`, `#9` and the phrase `hard reload`, and a
      `Row-to-requirement map (Round 2)`.
    - The string `localhost:8099` appears nowhere in `21-VALIDATION.md`.
    - Round 1 is untouched: still 13 rows R1..R13, still exactly 13 `R21-VERDICT` tokens, and R7's
      cell still reads `R21-VERDICT: BLOCKED`.
    - Frontmatter: `round: 2`, a `round2_staged` date, `round1_staged` and `round1_answered` still
      present, `nyquist_compliant: false`, `status: partial`.
  </acceptance_criteria>
  <done>A fresh production-shaped build is served from 127.0.0.1 with a disclosed two-entry 2026 fixture in the staged 400m rankings, and two unanswered Round 2 rows are staged below an untouched Round 1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] The two-row Round 2 browser checkpoint — R14 (fixture freshness) then R15 (the re-rank, re-asking R7)</name>
  <what-built>
    Nothing new was built. Plan 21-05 shipped `filterRankingsToYear` and the `.segmented`
    All time / This year control; 21-07's Round 1 checkpoint verified the control itself (R6, R8,
    R9, R10 PASS) but recorded R7 BLOCKED, because the live archive holds zero 2026-dated
    best-effort ranking entries in any of the seven distances, so every table rendered an empty
    state under "This year" and no rank or date could be read back.

    Task 1 re-ran the full gate, rebuilt and re-staged the build under `/strava-widgets`, and
    injected a disclosed fixture into the STAGED `dist/widgets/data/stats/best-efforts.json` only:
    the 400m ranking entries at array indices 3 and 8 were redated from `2018-09-04` and
    `2025-04-09` to `2026-03-14` and `2026-06-02`. Their source `rank` values were deliberately
    LEFT at `4` and `9`.
  </what-built>
  <how-to-verify>
    Open `http://127.0.0.1:8099/strava-widgets/#/records`. **Confirm the URL bar reads `127.0.0.1`,
    not `localhost`, and includes `/strava-widgets/`, before judging any row.**

    **Then hard-reload before judging anything.** `127.0.0.1` alone is NOT sufficient: in Round 1
    the observing tab rendered a stale cached `streaks.json` and only corrected after a hard reload.
    Use Cmd+Shift+R (or DevTools open with "Disable cache" ticked, then reload). R14 exists solely
    to record that this happened and that the fixture actually reached the tab. R14 must be PASS
    before R15 may be judged — if R14 is not PASS, record R15 BLOCKED and stop.

    Present the two rows from `21-VALIDATION.md`'s Round 2 table **one at a time**, in R14 then R15
    order, following every rule in the `<house_rules>` block. For each row, record the developer's
    own words in the Observation cell together with one
    `R21-VERDICT: PASS|FAIL|BLOCKED|NOT EXERCISABLE` token, the row's own required detail, and its
    observer attribution.

    R15 is not answerable by "a row appeared". Read the Rank cell and the Date cell back verbatim.
    `#1` / `Mar 14, 2026` on the first row and `#2` / `Jun 2, 2026` on the second are correct.
    `#4` and `#9` are the pass-through failure the fixture is designed to expose and must be
    recorded FAIL with the numbers quoted.

    Do not re-ask R1-R6 or R8-R13. Their Round 1 verdicts stand and are not re-litigated.
  </how-to-verify>
  <action>
    Present the two Round 2 rows from `.planning/phases/21-overview-rebuild/21-VALIDATION.md` to the
    developer ONE AT A TIME, in R14 then R15 order, quoting each row's Instructions cell including
    its `Required detail:` and `Observer required:` clauses.

    Before R14, confirm with the developer that the URL bar reads
    `http://127.0.0.1:8099/strava-widgets/#/records` — not `localhost`, not a bare server root —
    and ask them to perform a hard reload (Cmd+Shift+R, or DevTools "Disable cache" plus reload).

    R14 asks the developer to read back the Date cells of the 400m ALL-TIME rows ranked `#4` and
    `#9`. Under a correctly-loaded fixture these read `Mar 14, 2026` and `Jun 2, 2026`. If either
    still reads `Sep 4, 2018` or `Apr 9, 2025`, the tab is serving a stale `best-efforts.json`:
    record R14 FAIL with what was seen, do not judge R15, and do not attempt a fix.

    R15 asks the developer to click `This year` and read back the 400m table's two rows. Expected:
    first row Rank `#1`, Time `0:55`, Date `Mar 14, 2026`; second row Rank `#2`, Time `1:02`, Date
    `Jun 2, 2026`. Ranks reading `#4` and `#9` mean the source ranks were passed through instead of
    re-ranked 1..N over the filtered subset — record FAIL with the numbers quoted.

    When writing a PASS observation, quote only what was actually rendered. Do NOT write the
    wrong-fix values `#4` or `#9` into a PASS cell for R15 — Task 3's automated check treats their
    presence in a PASS cell as a contradiction and will reject it. The wrong values belong in the
    agenda's Instructions cell, which already states them.

    Capture each answer in the developer's own words and hold them for Task 3, which writes them
    into the Observation cells. Do not summarise, do not merge the two answers, do not substitute an
    agent computed-style or DOM read for a row whose observer is the developer's own eyes, and do
    not edit any source file if a row fails.
  </action>
  <read_first>
    - `.planning/phases/21-overview-rebuild/21-VALIDATION.md` § Round 2 — the staged agenda, the
      preamble's fixture disclosure with its expected-render table, and the `Cache trap` paragraph
      Task 1 wrote into it.
    - `.planning/phases/21-overview-rebuild/21-VALIDATION.md` § Round 1 row R7 (line 174) and the
      `## Round 1 Gap-Closure Record` — the exact wording and BLOCKED disposition R15 supersedes.
    - `.planning/phases/21-overview-rebuild/21-VALIDATION.md` § Round 1 `<house_rules>` block, and
      this plan's `<house_rules>` and `<fixture_disclosure>` blocks.
    - `.planning/phases/21-overview-rebuild/21-07-SUMMARY.md` — what actually happened in Round 1,
      including the stale-`streaks.json` hard-reload incident R14 exists to pre-empt.
  </read_first>
  <verify>
    <human-check>Both rows have an answer from the developer, each with the row's own required detail and a named observer; the URL bar was confirmed to read 127.0.0.1 and /strava-widgets/ and a hard reload was performed before R14 was judged; R15 was judged only after R14 was PASS.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer confirmed the URL bar reads `http://127.0.0.1:8099/strava-widgets/#/records`
      before R14 was judged.
    - A hard reload was performed and the method used is named in R14's answer.
    - R14 has an answer quoting the Date cell text of the 400m All-time rows ranked `#4` and `#9`.
    - R15 has an answer quoting the Rank cell and Date cell of both rows in the 400m This-year
      table, or an explicit decline / BLOCKED with the reason.
    - R15 was judged only if R14 was PASS; otherwise R15 is recorded BLOCKED naming R14.
    - No row other than R14 and R15 was asked; Round 1's rows were not re-presented.
    - No source file was edited during the session: `git status --porcelain src scripts` is empty.
  </acceptance_criteria>
  <done>Two developer answers are in hand, each attributable to a named observer, with the rendered rank and date values quoted verbatim.</done>
  <resume-signal>Per-row verdicts recorded. Type "done" when both rows have an answer, or name the row you are declining.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the Round 2 outcome, gate OVR-03 on its full row map, and discard the fixture</name>
  <files>.planning/phases/21-overview-rebuild/21-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/21-overview-rebuild/21-VALIDATION.md` § Round 2 — the agenda as answered in
      Task 2 — and its Per-Task Verification Map row for OVR-03's manual-only behavior (currently
      `⚠️ BLOCKED (R6, R8, R9, R10 PASS; R7 BLOCKED — no current-year rows exist to re-rank)`),
      which this task updates.
    - `.planning/phases/21-overview-rebuild/21-07-PLAN.md` Task 3 — the precedent for the three
      recording sections and for gating `REQUIREMENTS.md` strictly on the row map.
    - `.planning/REQUIREMENTS.md` line 28 (OVR-03's entry, currently `[ ]` with the Round 1 gap
      note) and line 85 (`| OVR-03 | Phase 21 | Pending |` in the status table).
    - `.planning/phases/21-overview-rebuild/21-VERIFICATION.md` `gaps:` block — the two `missing:`
      items, so the record states plainly which one each task discharged.
  </read_first>
  <action>
    Write the two verdicts into the Round 2 Observation cells, then add three sections below the
    Round 2 table:

    - `## Checkpoint Outcome (Round 2)` — the count of PASS / FAIL / BLOCKED / NOT EXERCISABLE
      across R14 and R15, and one sentence per non-PASS row naming what was not established. No
      fix, no root-cause theory.
    - `## Evidence Quality (Round 2)` — which row was observed by the developer's own eyes and
      which by agent automation; that a hard reload was performed and by what method; and an
      explicit note that R15 rests on a staged-build fixture rather than organic archive data,
      naming the file `dist/widgets/data/stats/best-efforts.json`, the two edited `startDate`
      values, the untouched `rank: 4` / `rank: 9` discriminator, and the three disclosed
      limitations (age-grading and the `activities` map are un-edited; the 400m empty state that
      Round 1's R10 used no longer renders under the fixture, which does not disturb R10's own
      verdict; `dist/widgets/*` is gitignored).
    - `## Round 2 Gap-Closure Record` — one subsection per FAIL or BLOCKED row, quoting the
      developer's words verbatim, with the requirement it blocks. Omit this section entirely if
      both rows are PASS.

    Update the Per-Task Verification Map's OVR-03 manual-only row to the state the Round 2 evidence
    supports, keeping the Round 1 row references and adding the Round 2 ones.

    Set frontmatter `round2_answered` to today's date. Set `nyquist_compliant: true` and
    `status: passed` ONLY if BOTH Round 2 rows are PASS **and** Round 1 had no FAIL or NOT
    EXERCISABLE rows — Round 1's only non-PASS was R7, which R15 supersedes, so a clean Round 2
    closes the file. Any FAIL, BLOCKED or NOT EXERCISABLE in Round 2 keeps `nyquist_compliant:
    false` and `status: partial`.

    In `REQUIREMENTS.md`, tick OVR-03's checkbox and set its status-table row from `Pending` to
    `Complete` ONLY if R6, R8, R9, R10 (Round 1) AND R14, R15 (Round 2) are ALL PASS. Rewrite
    OVR-03's trailing note to state that R7's Round 1 BLOCKED disposition was superseded by Round
    2's R15, and record — the way FIX-01's entry records its own fixture — that the year-scope
    re-rank was confirmed against a staged-build fixture (`dist/widgets/data/stats/best-efforts.json`,
    two 400m entries redated into 2026), not organic archive data, because the archive holds no
    current-year best-effort ranking entries in any distance. Leave OVR-01, OVR-02, OVR-04 and
    FIX-01 exactly as they stand.

    Do NOT touch `.planning/ROADMAP.md`. Line 71 was already reverted to `[ ]` with the open-OVR-03
    gap documented in commit `4359ed1`; it is truthful as it stands, and closing the phase checkbox
    belongs to re-verification and `/gsd-progress`, not to this round.

    Finally, discard the fixture. Restore the staged file by copying the repository copy over it —
    `cp data/stats/best-efforts.json dist/widgets/data/stats/best-efforts.json` — then assert the
    staged file holds ZERO `2026-` entries in every distance and that 400m index 3 and index 8 read
    `2018-09-04T16:26:06Z` and `2025-04-09T00:02:51Z` again. Stop the background
    `python3 -m http.server 8099` process. This teardown is not optional: it is the mitigation for
    T-21-08-04, and "`dist/widgets/*` is gitignored so it cannot be committed" is not the same as
    "no one will later open the staged build and read fabricated PR data as archive data".

    Do not edit any source file. Do not patch a failing row. Do not edit Round 1.
  </action>
  <verify>
    <automated>node --input-type=module -e 'import {readFileSync} from "node:fs"; const p=".planning/phases/21-overview-rebuild/21-VALIDATION.md"; const s=readFileSync(p,"utf8"); if((s.match(/## Round 2/g)||[]).length!==1) throw new Error("expected exactly one Round 2 heading"); const r1=s.split("## Round 2")[0]; const r1ids=["R1","R2","R3","R4","R5","R6","R7","R8","R9","R10","R11","R12","R13"]; const r1rows=r1.split("\n").filter(l=>l.startsWith("|")).filter(x=>r1ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(x))); if(r1rows.length!==13) throw new Error("Round 1 no longer has its 13 rows - it must not be edited"); if(!/R21-VERDICT:\s*BLOCKED/.test(r1rows[6])) throw new Error("Round 1 R7 is no longer recorded BLOCKED verbatim"); const t=s.split("## Round 2")[1]; const ids=["R14","R15"]; const rows=t.split("\n").filter(l=>l.startsWith("|")).filter(x=>ids.some(id=>new RegExp("^\\|\\s*"+id+"\\.").test(x))); if(rows.length!==2) throw new Error("expected 2 Round 2 rows, found "+rows.length); const obs=rows.map(x=>x.split("|")[5]||""); const v=obs.map(o=>(o.match(/R21-VERDICT:\s*(PASS|FAIL|BLOCKED|NOT EXERCISABLE)/)||[])[1]); v.forEach((x,i)=>{ if(!x) throw new Error("row "+ids[i]+" has no R21-VERDICT token"); }); obs.forEach((o,i)=>{ const body=o.replace(/R21-VERDICT:\s*(PASS|FAIL|BLOCKED|NOT EXERCISABLE)/,"").trim(); if(body.length<60) throw new Error("row "+ids[i]+" observation is too thin ("+body.length+" chars)"); if(!/observed by (developer|agent)/i.test(o)) throw new Error("row "+ids[i]+" does not name its observer"); }); if(obs[0].trim()===obs[1].trim()) throw new Error("R14 and R15 share an identical observation"); const pass=i=>v[i]==="PASS"; if(pass(0)){ for(const n of ["Mar 14, 2026","Jun 2, 2026"]) if(!obs[0].includes(n)) throw new Error("R14 is PASS without quoting the fixture date "+n+" - the tab may have been stale"); if(/Sep 4, 2018|Apr 9, 2025/.test(obs[0])) throw new Error("R14 quotes a pre-fixture date - that is a stale tab and a FAIL, not a PASS"); if(!/hard.?reload|hard.?refresh|disable cache/i.test(obs[0])) throw new Error("R14 is PASS without naming the hard reload that was performed"); } if(pass(1)){ if(!pass(0)) throw new Error("R15 cannot be PASS while R14 is not PASS - the fixture was never proven live in the tab"); for(const n of ["#1","#2","Mar 14, 2026","Jun 2, 2026"]) if(!obs[1].includes(n)) throw new Error("R15 is PASS without quoting the read-back value "+n); if(/#4|#9/.test(obs[1])) throw new Error("R15 quotes the pass-through source ranks - that is a FAIL, not a PASS (and wrong-fix values must not be written into a PASS cell)"); } const nonpass=v.filter(x=>x!=="PASS").length; if(!/## Checkpoint Outcome \(Round 2\)/.test(s)) throw new Error("missing Checkpoint Outcome (Round 2)"); if(!/## Evidence Quality \(Round 2\)/.test(s)) throw new Error("missing Evidence Quality (Round 2)"); const eq=s.split("## Evidence Quality (Round 2)")[1]||""; if(!/dist\/widgets\/data\/stats\/best-efforts\.json/.test(eq)) throw new Error("Evidence Quality (Round 2) does not name the fixture file"); for(const n of ["2026-03-14","2026-06-02"]) if(!eq.includes(n)) throw new Error("Evidence Quality (Round 2) does not name the injected date "+n); if(nonpass>0 && !/## Round 2 Gap-Closure Record/.test(s)) throw new Error(nonpass+" non-PASS Round 2 rows but no Round 2 Gap-Closure Record"); if(nonpass===0 && /## Round 2 Gap-Closure Record/.test(s)) throw new Error("all Round 2 rows pass but a Gap-Closure Record was written"); const fm=s.split("---")[1]||""; if(!/round2_answered/.test(fm)) throw new Error("round2_answered date missing"); const claimed=/nyquist_compliant: true/.test(fm); if(claimed && nonpass>0) throw new Error("nyquist_compliant is true without a clean Round 2"); if(claimed && !/status: passed/.test(fm)) throw new Error("nyquist_compliant true requires status: passed"); if(!claimed && nonpass===0) throw new Error("both Round 2 rows pass but nyquist_compliant was not set true"); if(/⚠️ BLOCKED \(R6, R8, R9, R10 PASS; R7 BLOCKED/.test(s)) throw new Error("the Per-Task Verification Map still carries the un-updated OVR-03 manual-only status"); const req=readFileSync(".planning/REQUIREMENTS.md","utf8"); const ovr3Line=req.split("\n").find(l=>/\*\*OVR-03\*\*/.test(l))||""; const ovr3Ticked=/^-\s*\[x\]/.test(ovr3Line); const allPass=nonpass===0; if(ovr3Ticked!==allPass) throw new Error("OVR-03 checkbox state ("+ovr3Ticked+") does not match its Round 2 row map (all pass: "+allPass+")"); const statusRow=req.split("\n").find(l=>/^\|\s*OVR-03\s*\|/.test(l))||""; if(allPass && !/Complete/.test(statusRow)) throw new Error("OVR-03 status-table row was not set to Complete"); if(!allPass && !/Pending/.test(statusRow)) throw new Error("OVR-03 status-table row must stay Pending"); if(allPass && !/best-efforts\.json/.test(ovr3Line)) throw new Error("OVR-03 is ticked without recording that its evidence rests on a staged-build fixture"); for(const other of ["OVR-01","OVR-02","OVR-04","FIX-01"]){ const l=req.split("\n").find(x=>new RegExp("\\*\\*"+other+"\\*\\*").test(x))||""; if(!/^-\s*\[x\]/.test(l)) throw new Error(other+" lost its tick - only OVR-03 may change this round"); } console.log("2 Round 2 verdicts recorded, "+nonpass+" non-PASS, OVR-03 gated on its full row map");' && node --input-type=module -e 'import {readFileSync} from "node:fs"; const P="dist/widgets/data/stats/best-efforts.json"; const s=JSON.parse(readFileSync(P,"utf8")); for(const [k,v] of Object.entries(s.rankings)) if(v.some(e=>String(e.startDate).startsWith("2026-"))) throw new Error("the staged build still carries a 2026 fixture entry in "+k+" - the fixture was not discarded"); const r=s.rankings["400m"]; if(r[3].startDate!=="2018-09-04T16:26:06Z"||r[8].startDate!=="2025-04-09T00:02:51Z") throw new Error("the staged 400m rankings were not restored to the archive values"); console.log("staged fixture discarded; staged best-efforts.json matches the archive");' && test -z "$(git status --porcelain src scripts)" && test -z "$(git status --porcelain .planning/ROADMAP.md)" && test -z "$(git status --porcelain .planning/phases/21-overview-rebuild/21-01-PLAN.md .planning/phases/21-overview-rebuild/21-02-PLAN.md .planning/phases/21-overview-rebuild/21-03-PLAN.md .planning/phases/21-overview-rebuild/21-04-PLAN.md .planning/phases/21-overview-rebuild/21-05-PLAN.md .planning/phases/21-overview-rebuild/21-06-PLAN.md .planning/phases/21-overview-rebuild/21-07-PLAN.md)"</automated>
  </verify>
  <acceptance_criteria>
    - Round 1 is byte-intact: still 13 rows R1..R13, and R7's cell still reads
      `R21-VERDICT: BLOCKED`.
    - R14 and R15 each carry exactly one `R21-VERDICT` token, an observation of at least 60
      characters excluding that token, and a named observer; the two observations are not identical.
    - R14, if PASS, quotes both `Mar 14, 2026` and `Jun 2, 2026`, contains neither `Sep 4, 2018` nor
      `Apr 9, 2025`, and names the hard reload that was performed.
    - R15, if PASS, quotes `#1`, `#2`, `Mar 14, 2026` and `Jun 2, 2026`, contains neither `#4` nor
      `#9`, and is PASS only when R14 is PASS.
    - `## Checkpoint Outcome (Round 2)` and `## Evidence Quality (Round 2)` both exist; the latter
      names `dist/widgets/data/stats/best-efforts.json`, `2026-03-14` and `2026-06-02`.
    - `## Round 2 Gap-Closure Record` exists if and only if a Round 2 row is non-PASS.
    - Frontmatter carries `round2_answered`; `nyquist_compliant: true` and `status: passed` appear
      only on a clean Round 2, otherwise `false` / `partial`.
    - The Per-Task Verification Map no longer carries the string
      `⚠️ BLOCKED (R6, R8, R9, R10 PASS; R7 BLOCKED`.
    - `REQUIREMENTS.md`: OVR-03 is ticked and its status-table row reads `Complete` if and only if
      both Round 2 rows are PASS; a ticked OVR-03 line names `best-efforts.json` as the fixture its
      evidence rests on; OVR-01, OVR-02, OVR-04 and FIX-01 all keep their `[x]`.
    - `dist/widgets/data/stats/best-efforts.json` holds zero `2026-` entries in any distance, and
      400m indices 3 and 8 read `2018-09-04T16:26:06Z` and `2025-04-09T00:02:51Z`.
    - `git status --porcelain src scripts` is empty, `git status --porcelain .planning/ROADMAP.md`
      is empty, and `git status --porcelain` for `21-01-PLAN.md` through `21-07-PLAN.md` is empty.
  </acceptance_criteria>
  <done>R14 and R15 have a stated disposition, OVR-03 is ticked only on evidence that was actually given, and no fabricated 2026 PR data survives in the staged build.</done>
</task>

</tasks>

<checkpoint_rows>
Two rows for Task 1's Round 2 agenda. Each Instructions cell must end with its own `Required
detail:` and `Observer required:` clauses, worded as given. Rows continue Round 1's numbering; do
not create a second row named R7.

**R14. The fixture actually reached the tab (D-16, the Round 1 cache trap).** On
`http://127.0.0.1:8099/strava-widgets/#/records`, confirm the URL bar reads `127.0.0.1` and
`/strava-widgets/`, then hard-reload the page (Cmd+Shift+R, or open DevTools with "Disable cache"
ticked and reload). With **All time** selected, scroll to the 400m table and read the Date cells of
the rows ranked `#4` and `#9`. Under the loaded fixture these read `Mar 14, 2026` and `Jun 2, 2026`.
If either still reads `Sep 4, 2018` or `Apr 9, 2025`, the tab is serving a stale
`best-efforts.json`; record FAIL and do not judge R15.
*Required detail:* whether a hard reload was performed and by what method, and the Date cell text of
the 400m All-time rows ranked `#4` and `#9`, quoted exactly. *Observer required:* developer's own
eyes.

**R15. The year scope re-ranks — re-ask of Round 1's R7 (OVR-03, D-01, D-11).** Only if R14 is
PASS. Click **This year**. The 400m table now has rows. Read both of them. A correct 1..N re-rank
over the filtered subset renders first row `#1` / `0:55` / `Mar 14, 2026` and second row `#2` /
`1:02` / `Jun 2, 2026`. Ranks reading `#4` and `#9` mean the source ranks were passed straight
through instead of being re-ranked within the scope — that is a FAIL, quoted.
*Required detail:* the rank shown in the 400m table's first row and the date in that row, and the
rank and date in its second row, all quoted exactly as rendered. *Observer required:* developer's
own eyes.
</checkpoint_rows>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository `data/stats/` -> staged `dist/widgets/data/stats/` | The fixture edit must cross this boundary in one direction only |
| staged `dist/` -> served `127.0.0.1` origin -> browser tab | Three hops, each of which can serve a stale copy of the one file the verdict depends on |
| fixture data -> recorded evidence -> `REQUIREMENTS.md` | Fabricated PR data is presented in a session whose verdicts tick a requirement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08-01 | Spoofing | fabricated 2026 PR data presented as archive data | mitigate | Task 1 asserts the staged 400m entries held their exact pre-edit archive values and that no distance held any `2026-` entry before the edit; the repository copy is asserted unchanged BY CONTENT and 2026-free (a `git status` check is vacuous — `data/stats/` is gitignored at `.gitignore:11`); the Round 2 preamble discloses the file, both edited fields, both dates, the untouched `rank: 4`/`rank: 9` discriminator and three honest limitations; Task 3 requires `## Evidence Quality (Round 2)` to name the file and both injected dates, and requires a ticked OVR-03 entry to record that its evidence rests on a fixture |
| T-21-08-02 | Spoofing | a stale cached tab, or a `localhost`/root-served build, standing in for the fixtured build | mitigate | Task 1 stages under `/strava-widgets` via an absolute symlink, asserts `index.html` responds 200 on `127.0.0.1`, and separately asserts the HTTP-FETCHED `data/stats/best-efforts.json` contains both injected dates — proving the server side independently of the browser; the string `localhost:8099` is rejected anywhere in `21-VALIDATION.md`; R14 exists solely to prove the fixture reached the observing tab after an explicit hard reload, and Task 3 rejects an R15 PASS whose R14 is not PASS. This closes the exact failure mode Round 1 hit, where the agent's tab rendered a stale `streaks.json` until a hard reload |
| T-21-08-03 | Repudiation | R15 passing on a row's presence rather than the rank's value | mitigate | The fixture is discriminator-designed: source ranks `4` and `9` are deliberately preserved, so a pass-through implementation renders `#4`/`#9` where a correct re-rank renders `#1`/`#2`. Task 3 rejects an R15 PASS that does not quote `#1`, `#2`, `Mar 14, 2026` and `Jun 2, 2026`, and rejects one that contains `#4` or `#9`. Every row needs one four-state verdict token, a named observer and at least 60 characters of observation |
| T-21-08-04 | Tampering | the fixture surviving into a later session or a published build | mitigate | The edit is confined to `dist/widgets/*`, which is gitignored at `.gitignore:4`, so it cannot be committed; no deploy, publish or `gh-pages` command is authorized anywhere in this plan. Task 3 additionally restores the staged file from the repository copy and asserts zero `2026-` entries remain in any distance, and stops the background server — because "cannot be committed" is not "cannot be misread by the next person who opens the staged build" |
| T-21-08-05 | Tampering | patching a defect mid-checkpoint, or re-litigating Round 1 | mitigate | `git status --porcelain src scripts` must be empty in Tasks 1 and 3; `git status --porcelain` for `21-01-PLAN.md` through `21-07-PLAN.md` must be empty; Task 1 and Task 3 both assert Round 1 still has its 13 rows, its 13 verdict tokens and R7's verbatim `BLOCKED` cell. The house rule since 16-09 is that a failing row is recorded verbatim and left unpatched |
| T-21-08-06 | Elevation of Privilege | requirement state changed beyond this round's mandate | mitigate | Task 3 asserts OVR-01, OVR-02, OVR-04 and FIX-01 all keep their `[x]`, that OVR-03's checkbox matches its full row map exactly, and that `.planning/ROADMAP.md` is unmodified — line 71 is already truthful after commit `4359ed1` and closing the phase checkbox is re-verification's job, not this round's |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs authorized; no `## Package Legitimacy Audit` is required because no package-manager install task exists. The server is `python3 -m http.server`, already present. Do not install jsdom, happy-dom, puppeteer, playwright or any headless browser to automate R14 or R15 — no automated substitute exists in this repository and, per the house rule in force since 16-09, this phase does not accept one for a manual row |
</threat_model>

<verification>
The gate for this plan is Task 1's script (full suite, `tsc`, `build-widgets` with no
`css-syntax-error`, `verify-dashboard`, production-shaped staging, the five fixture guards, the
HTTP-fetched fixture assertion and the staged-agenda shape check) followed by Task 3's script
(per-row verdict, observer, substance, the R14 freshness predicate, the R15 value predicate, the
R14-before-R15 ordering rule, the OVR-03 row-map gate, and the fixture-teardown assertion).

No automated result in this repository is evidence for either row. `npm test` cannot observe a
re-rank or a rendered date, and the 7 unit tests already covering `filterRankingsToYear` are exactly
what Round 1's R7 was blocked from supplementing.

Round 1 is not re-run and not re-asked. R1-R6 and R8-R13 keep their recorded verdicts; R7 keeps its
BLOCKED cell verbatim and is superseded, not overwritten, by R15.
</verification>

<success_criteria>
- The build is rebuilt, re-staged under `/strava-widgets` from an absolute symlink, and served from `127.0.0.1`.
- Exactly two `startDate` values in the STAGED 400m rankings are redated into 2026; the repository copy is proven unchanged by content and still holds zero 2026 entries.
- The served HTTP response for `data/stats/best-efforts.json` carries both injected dates, proving the server independently of the browser tab.
- R14 records an explicit hard reload and reads back the fixture dates, and is PASS before R15 is judged.
- R15 reads back `#1` / `Mar 14, 2026` and `#2` / `Jun 2, 2026` — a value, not a presence — closing Round 1's R7 BLOCKED.
- `REQUIREMENTS.md` ticks OVR-03 only if R6, R8, R9, R10, R14 and R15 are all PASS, and records the fixture its evidence rests on.
- Round 1's thirteen rows, plans 21-01..21-07, `ROADMAP.md` and every source file are unmodified.
- The staged fixture is discarded and the local server stopped before the plan closes.
</success_criteria>

<output>
Create `.planning/phases/21-overview-rebuild/21-08-SUMMARY.md` when done.
</output>
